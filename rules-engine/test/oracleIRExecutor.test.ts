import { describe, it, expect, vi } from 'vitest';
import type { GameState } from '../../shared/src';
import { parseOracleTextToIR } from '../src/oracleIRParser';
import { applyOracleIRStepsToGameState, buildOracleIRExecutionContext } from '../src/oracleIRExecutor';
import { DelayedTriggerTiming, checkDelayedTriggers, processDelayedTriggers } from '../src/delayedTriggeredAbilities';
import { applyStaticAbilitiesToBattlefield } from '../src/staticAbilities';
import {
  executeTriggeredAbilityEffectWithOracleIR,
  parseTriggeredAbilitiesFromText,
  processEventAndExecuteTriggeredOracle,
  TriggerEvent,
} from '../src/triggeredAbilities';
import { canCreatureBlock, createCombatCreature } from '../src/combatAutomation';
import { executeCleanupStep as executeTurnCleanupStep, executeUntapStep } from '../src/actions/turnActions';
import { makeMerfolkIterationState } from './helpers/merfolkIterationFixture';

function makeState(overrides: Partial<GameState> = {}): GameState {
  return {
    id: 'game1',
    format: 'commander',
    players: [
      {
        id: 'p1',
        name: 'P1',
        seat: 0,
        life: 40,
        library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
        hand: [],
        graveyard: [],
      } as any,
    ],
    startingLife: 40,
    life: {},
    turnPlayer: 'p1',
    priority: 'p1',
    stack: [],
    battlefield: [],
    commandZone: {} as any,
    phase: 'pre_game' as any,
    active: true,
    ...overrides,
  } as any;
}

describe('Oracle IR Executor', () => {
  it('records structured automation gaps for unsupported executor skips and saves them on state', () => {
    const start = makeState();
    const steps = [
      {
        kind: 'draw',
        who: { kind: 'you' },
        amount: { kind: 'unknown', raw: 'that many' },
        raw: 'Draw that many cards.',
      } as any,
    ];

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Gap Source' });
    const savedGaps = (result.state as any).oracleAutomationGaps || [];

    expect(result.skippedSteps).toHaveLength(1);
    expect(result.automationGaps).toHaveLength(1);
    expect(result.automationGaps[0]?.reasonCode).toBe('unknown_amount');
    expect(result.automationGaps[0]?.stepKind).toBe('draw');
    expect(result.automationGaps[0]?.sourceName).toBe('Gap Source');
    expect(savedGaps).toHaveLength(1);
    expect(savedGaps[0]?.raw).toBe('Draw that many cards.');
  });

  it('applies Myr Battlesphere tap-count followup using selected untapped Myr', () => {
    const ir = parseOracleTextToIR(
      'Whenever Myr Battlesphere attacks, you may tap X untapped Myr you control. If you do, Myr Battlesphere gets +X/+0 until end of turn and deals X damage to defending player.',
      'Myr Battlesphere'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      battlefield: [
        { id: 'battlesphere', controller: 'p1', name: 'Myr Battlesphere', type_line: 'Artifact Creature — Myr Construct', power: 4, toughness: 7, tapped: false } as any,
        { id: 'myr1', controller: 'p1', name: 'Silver Myr', type_line: 'Artifact Creature — Myr', power: 1, toughness: 1, tapped: false } as any,
        { id: 'myr2', controller: 'p1', name: 'Iron Myr', type_line: 'Artifact Creature — Myr', power: 1, toughness: 1, tapped: false } as any,
        { id: 'myr3', controller: 'p1', name: 'Gold Myr', type_line: 'Artifact Creature — Myr', power: 1, toughness: 1, tapped: false } as any,
        { id: 'oppMyr', controller: 'p2', name: 'Leaden Myr', type_line: 'Artifact Creature — Myr', power: 1, toughness: 1, tapped: false } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'battlesphere',
        sourceName: 'Myr Battlesphere',
        selectorContext: {
          targetOpponentId: 'p2',
          chosenObjectIds: ['myr1', 'myr2'],
        },
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const battlesphere = result.state.battlefield.find((perm: any) => perm.id === 'battlesphere') as any;
    const myr1 = result.state.battlefield.find((perm: any) => perm.id === 'myr1') as any;
    const myr2 = result.state.battlefield.find((perm: any) => perm.id === 'myr2') as any;
    const myr3 = result.state.battlefield.find((perm: any) => perm.id === 'myr3') as any;

    expect(p2.life).toBe(38);
    expect((battlesphere.modifiers || []).some((modifier: any) => modifier?.type === 'powerToughness' && modifier?.power === 2)).toBe(true);
    expect(myr1.tapped).toBe(true);
    expect(myr2.tapped).toBe(true);
    expect(myr3.tapped).toBe(false);
    expect(result.skippedSteps).toHaveLength(0);
  });

  it('does not apply Myr Battlesphere conditional followup when no untapped Myr are tapped', () => {
    const ir = parseOracleTextToIR(
      'Whenever Myr Battlesphere attacks, you may tap X untapped Myr you control. If you do, Myr Battlesphere gets +X/+0 until end of turn and deals X damage to defending player.',
      'Myr Battlesphere'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      battlefield: [
        { id: 'battlesphere', controller: 'p1', name: 'Myr Battlesphere', type_line: 'Artifact Creature — Myr Construct', power: 4, toughness: 7, tapped: true } as any,
        { id: 'myr1', controller: 'p1', name: 'Silver Myr', type_line: 'Artifact Creature — Myr', power: 1, toughness: 1, tapped: true } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'battlesphere',
        sourceName: 'Myr Battlesphere',
        selectorContext: { targetOpponentId: 'p2' },
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const battlesphere = result.state.battlefield.find((perm: any) => perm.id === 'battlesphere') as any;

    expect(p2.life).toBe(40);
    expect(battlesphere.modifiers || []).toHaveLength(0);
    expect(result.skippedSteps.map(step => step.kind)).toEqual(['conditional']);
  });

  it('applies draw steps for "you"', () => {
    const ir = parseOracleTextToIR('Draw two cards.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetPlayersAttacked' },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.hand).toHaveLength(2);
    expect(p1.library).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
  });

  it('records impulse permissions on exiled cards (until end of your next turn)', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until the end of your next turn, you may play that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({ turnNumber: 10 } as any);
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test Source' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(Array.isArray(p1.exile)).toBe(true);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0].canBePlayedBy).toBe('p1');
    expect(p1.exile[0].playableUntilTurn).toBe(11);

    const pfe = (result.state as any).playableFromExile?.p1;
    expect(pfe).toBeTruthy();
    expect(pfe[String(p1.exile[0].id)]).toBe(11);
  });

  it('does not grant cast-from-exile permission to lands', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until end of turn, you may cast that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 3,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'land1', name: 'Forest', type_line: 'Basic Land � Forest' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetPlayersAttacked' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0].id).toBe('land1');
    expect(p1.exile[0].canBePlayedBy).toBeUndefined();
    expect((result.state as any).playableFromExile?.p1?.land1).toBeUndefined();
  });

  it('applies draw for "each of your opponents" (normalized to each_opponent)', () => {
    const ir = parseOracleTextToIR('Each of your opponents draws a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetPlayersAttacked' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.hand).toHaveLength(1);
    expect(p2.library).toHaveLength(1);
    expect(p3.hand).toHaveLength(1);
    expect(p3.library).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
  });

  it('applies mill for "your opponents" (normalized to each_opponent)', () => {
    const ir = parseOracleTextToIR('Your opponents mill a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetPlayersAttacked' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(1);
    expect(p3.library).toHaveLength(1);
    expect(p3.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
  });

  it("applies exile_top for each of your opponents' libraries", () => {
    const ir = parseOracleTextToIR("Exile the top card of each of your opponents' libraries.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetPlayersAttacked' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top for those opponents' libraries via relational selector context binding", () => {
    const ir = parseOracleTextToIR("Exile the top card of those opponents' libraries.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top for all of those opponents' libraries via relational selector context binding", () => {
    const ir = parseOracleTextToIR("Exile the top card of all of those opponents' libraries.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it('applies exile_top for each opponent�s library (curly apostrophe)', () => {
    const ir = parseOracleTextToIR('Exile the top card of each opponent’s library.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetVampireCount' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top for each opponent's library (straight apostrophe)", () => {
    const ir = parseOracleTextToIR("Exile the top card of each opponent's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster',
      sourceName: 'Outfitted Jouster',
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top for 'put the top card of each opponent's library into exile'", () => {
    const ir = parseOracleTextToIR("Put the top card of each opponent's library into exile.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster-choice',
      sourceName: 'Outfitted Jouster',
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it('applies impulse_exile_top by exiling the top card(s) of your library', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. You may play that card this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster',
      sourceName: 'Outfitted Jouster',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top for combined look+exile template in 1v1 (no face down)', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library and exile those cards. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster-choice',
      sourceName: 'Outfitted Jouster',
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top for look-then-exile face-down template (your library)', () => {
    const ir = parseOracleTextToIR(
      'Look at the top card of your library, then exile it face down. You may play it for as long as it remains exiled.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster',
      sourceName: 'Outfitted Jouster',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it("applies impulse_exile_top for look-then-exile face-down template (those opponents' alias via relational context)", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of those opponents' libraries, then exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it("applies split-clause look-then-exile face-down impulse for those opponents' alias via relational context", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of those opponents' libraries. Then exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies split-clause look-top-two then exile-those-cards face-down impulse in 1v1', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library. Then exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster-choice',
      sourceName: 'Outfitted Jouster',
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies split-clause look-top-two then exile-those-cards impulse in 1v1 (no face down)', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library. Then exile those cards. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect(p2.exile || []).toHaveLength(2);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
  });

  it('applies impulse_exile_top for target_opponent remains-exiled cast + mana-rider template', () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of target opponent's library, then exile it face down. You may cast that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applies standalone split-clause look-top-two then exile-those-cards face-down exile_top in 1v1', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library. Then exile those cards face down.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
  });

  it("applies standalone look+exile exile_top for those opponents' alias via relational context (no face down)", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of those opponents' libraries and exile those cards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
  });

  it('applies standalone split-clause look-top-two then exile-those-cards exile_top in 1v1 (no face down)', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library. Then exile those cards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
  });

  it("applies standalone look+exile face-down exile_top for those opponents' alias via relational context", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of those opponents' libraries and exile those cards face down.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with leading until-the-end-of-turn permission', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. Until the end of turn, you may play that card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with a during-your-next-turn permission', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. During your next turn, you may play that card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with an until-your-next-upkeep permission', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until the beginning of your next upkeep, you may play that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with an immediate you-may-cast permission (no explicit duration)', () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of your library. You may cast that card. If you don't, it deals 2 damage to each opponent.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with an immediate without-paying-mana-cost permission that has a trailing if restriction', () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of your library. You may cast it without paying its mana cost if it's a spell with mana value 2 or less. If you don't, put it into your graveyard.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when until-next-turn permission has a trailing if restriction', () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of your library. Until your next turn, you may cast it if it's an instant or sorcery spell.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when remains-exiled permission has a trailing if restriction', () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of your library. For as long as it remains exiled, you may cast it if it's a creature spell.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top for combined look+exile face-down template (each opponent)', () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of each opponent's library and exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it("applies impulse_exile_top for combined look+exile face-down template (those opponents' alias via relational context)", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of those opponents' libraries and exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission is gated by an unmodeled leading if condition', () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of your library. If it's a Goblin creature card, you may cast that card until the end of your next turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when among-clause restricts to an artifact spell (this turn)', () => {
    const ir = parseOracleTextToIR('Exile the top five cards of your library. You may cast an artifact spell from among them this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }, { id: 'c4' }, { id: 'c5' }, { id: 'c6' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(5);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['c1', 'c2', 'c3', 'c4', 'c5']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when among-clause restricts to instant or sorcery (until end of next turn)', () => {
    const ir = parseOracleTextToIR(
      'Exile the top five cards of your library. Until the end of your next turn, you may cast an instant or sorcery spell from among those exiled cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }, { id: 'c4' }, { id: 'c5' }, { id: 'c6' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(5);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['c1', 'c2', 'c3', 'c4', 'c5']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast spells from among those cards', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of turn, you may cast spells from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast spells from among those cards exiled this way', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until end of turn, you may cast spells from among those cards exiled this way.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says play lands and cast spells from among those cards', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until end of turn, you may play lands and cast spells from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says play lands and cast spells from among those cards until end of your next turn', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of your next turn, you may play lands and cast spells from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says play one of those cards this turn', () => {
    const ir = parseOracleTextToIR('Exile the top two cards of your library. You may play one of those cards this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast one of those cards this turn', () => {
    const ir = parseOracleTextToIR('Exile the top two cards of your library. You may cast one of those cards this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast a spell from among those cards', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until end of turn, you may cast a spell from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast a spell from among those cards until end of your next turn', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of your next turn, you may cast a spell from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast spells from among the exiled cards', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until end of turn, you may cast spells from among the exiled cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when among-clause says until the end of this turn', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of this turn, you may cast spells from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when among-clause says play lands and cast spells until the end of this turn', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of this turn, you may play lands and cast spells from among those cards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when among-clause says cast spells through end of turn', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. You may cast spells from among those cards through end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission references the exiled card', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. You may play the exiled card this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission references the card exiled this way', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You may play the card exiled this way until the end of your next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission is granted to "its owner" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" for "its owner" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can cast it this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c66' }, { id: 'p2c67' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c67']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c66']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission is granted to "its owner" for plural exile (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top two cards of their library. Its owner may cast them this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission references spells they exiled this way (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top two cards of their library. Its owner may cast spells they exiled this way this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', types: ['instant'] },
            { id: 'p2c2', types: ['sorcery'] },
            { id: 'p2c3', types: ['land'] },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission references the spell they exiled this way (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast the spell they exiled this way this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1', types: ['instant'] }, { id: 'p2c2', types: ['land'] }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of this turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it until the end of this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through end of this turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it through end of this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "during their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the end of their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can cast it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c72' }, { id: 'p2c73' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c73']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c72']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the beginning of their next upkeep" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until your next end step" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until your next end step" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can cast it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c74' }, { id: 'p2c75' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c75']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c74']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can cast it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c76' }, { id: 'p2c77' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c77']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c76']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next upkeep" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next end step" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next end step" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can cast it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c78' }, { id: 'p2c79' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c79']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c78']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until end of combat on their next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may cast it until end of combat on their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses leading "until your next turn" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its owner may cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses leading "until your next turn" with owner can-cast permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its owner can cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c68' }, { id: 'p2c69' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c69']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c68']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its owner may play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land1', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land1']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land1).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants owner can-play permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its owner can play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land69', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land69']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land69).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "during their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land14', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land14']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land14).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "during their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land74', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land74']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land74).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land15', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land15']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land15).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the end of their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land84', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land84']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land84).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next upkeep" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land16', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land16']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land16).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next upkeep" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land75', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land75']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land75).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next end step" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land17', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land17']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land17).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next end step" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land85', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land85']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land85).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land33', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land33']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land33).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through their next turn" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land86', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land86']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land86).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the beginning of their next upkeep" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land34', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land34']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land34).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the beginning of their next upkeep" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land76', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land76']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land76).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until your next end step" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner may play it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land35', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land35']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land35).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until your next end step" with owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its owner can play it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land87', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land87']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land87).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next end step" grants owner play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next end step, its owner may play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land36', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land36']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land36).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its controller may play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land2', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land2']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land2).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants controller can-play permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its controller can play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land70', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land70']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land70).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its controller may cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell1']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell1).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next end step" grants controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next end step, its controller may cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell2', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell2']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell2).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next end step" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land3', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land3']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land3).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "during their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land18', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land18']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land18).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "during their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land77', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land77']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land77).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land68', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land68']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land68).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next end step" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land88', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land88']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land88).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next end step" grants controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next end step, its controller may play it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land37', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land37']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land37).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land19', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land19']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land19).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the end of their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land89', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land89']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land89).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next upkeep" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land20', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land20']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land20).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next upkeep" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land78', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land78']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land78).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it("applies Vincent's Limit Break by resolving the chosen tiered body and temporary dies trigger", () => {
    const oracleText =
      "Tiered (Choose one additional cost.)\n"
      + "Until end of turn, target creature you control gains \"When this creature dies, return it to the battlefield tapped under its owner's control\" and has the chosen base power and toughness.\n"
      + "\u2022 Galian Beast - {0} - 3/2.\n"
      + "\u2022 Death Gigas - {1} - 5/2.\n"
      + "\u2022 Hellmasker - {3} - 7/2.";
    const ir = parseOracleTextToIR(oracleText, "Vincent's Limit Break");
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'creature1',
          controller: 'p1',
          owner: 'p1',
          card: {
            id: 'bear-cub',
            name: 'Bear Cub',
            type_line: 'Creature - Bear',
            power: '2',
            toughness: '2',
          },
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: "Vincent's Limit Break",
        targetCreatureId: 'creature1',
      },
      {
        selectedModeIds: ['Death Gigas'],
      }
    );

    const processed = applyStaticAbilitiesToBattlefield(result.state.battlefield as any);
    const creature = processed.find((perm: any) => perm.id === 'creature1') as any;

    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(result.appliedSteps.some(step => step.kind === 'set_base_pt')).toBe(true);
    expect(creature?.effectivePower).toBe(5);
    expect(creature?.effectiveToughness).toBe(2);
    expect((result.state as any).delayedTriggerRegistry).toBeTruthy();
  });

  it('applies impulse_exile_top when permission uses "through their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land21', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land21']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land21).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through their next turn" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land90', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land90']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land90).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the beginning of their next upkeep" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land22', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land22']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land22).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the beginning of their next upkeep" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can play it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land79', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land79']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land79).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until your next end step" with controller play-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may play it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2land23', name: 'Forest', type_line: 'Basic Land � Forest' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2land23']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2land23).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next upkeep" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell3', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell3']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell3).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next upkeep" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it through your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell71', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell71']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell71).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "this turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell9', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell9']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(9);
    expect((result.state as any).playableFromExile?.p2?.p2spell9).toBe(9);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of this turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it until the end of this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell10', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell10']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(9);
    expect((result.state as any).playableFromExile?.p2?.p2spell10).toBe(9);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through end of this turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it through end of this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell11', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell11']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(9);
    expect((result.state as any).playableFromExile?.p2?.p2spell11).toBe(9);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until your next end step" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell12', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell12']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell12).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until your next end step" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell80', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell80']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell80).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through your next end step" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell13', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell13']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell13).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through your next end step" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell81', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell81']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell81).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "during their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell4', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell4']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell4).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "during their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it during their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell72', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell72']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell72).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "through their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell5', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell5']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell5).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "through their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it through their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell82', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell82']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell82).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the end of their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell6', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell6']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell6).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the end of their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it until the end of their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell83', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell83']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell83).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until the beginning of their next upkeep" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell7', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell7']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell7).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "can" + "until the beginning of their next upkeep" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller can cast it until the beginning of their next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell73', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell73']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell73).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses "until end of combat on their next turn" with controller cast-permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Its controller may cast it until end of combat on their next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2spell8', name: 'Grizzly Bears', type_line: 'Creature � Bear' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2spell8']);
    expect(p2.exile[0].canBePlayedBy).toBe('p2');
    expect(p2.exile[0].playableUntilTurn).toBe(10);
    expect((result.state as any).playableFromExile?.p2?.p2spell8).toBe(10);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when permission uses leading "until your next end step" (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next end step, its owner may cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when leading "until your next turn" grants controller can-cast permission (target player)', () => {
    const ir = parseOracleTextToIR(
      'Target player exiles the top card of their library. Until your next turn, its controller can cast it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c70' }, { id: 'p2c71' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c71']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c70']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with a without-paying-mana-cost suffix', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. You may cast it this turn without paying its mana cost.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('treats scry as a deterministic no-op when library is empty', () => {
    const ir = parseOracleTextToIR('Scry 1.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.appliedSteps.some(s => s.kind === 'scry')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'scry')).toBe(false);
  });

  it('treats surveil as a deterministic no-op when library is empty', () => {
    const ir = parseOracleTextToIR('Surveil 1.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.appliedSteps.some(s => s.kind === 'surveil')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'surveil')).toBe(false);
  });

  it('treats fateseal as a deterministic no-op when the targeted opponent library is empty', () => {
    const ir = parseOracleTextToIR('Fateseal 2.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    expect(result.appliedSteps.some(s => s.kind === 'fateseal')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'fateseal')).toBe(false);
  });

  it('applies fateseal deterministically by leaving the looked-at cards in place', () => {
    const ir = parseOracleTextToIR('Fateseal 2.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'top-a', name: 'A' }, { id: 'top-b', name: 'B' }, { id: 'top-c', name: 'C' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.some(s => s.kind === 'fateseal')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'fateseal')).toBe(false);
    expect((p2.library || []).map((card: any) => card.id)).toEqual(['top-a', 'top-b', 'top-c']);
  });

  it('applies time travel by removing a time counter from each eligible controlled permanent and exiled card', () => {
    const ir = parseOracleTextToIR('Time travel.', 'Time Beetle');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'suspended-a', name: 'Ancestral Vision', counters: { time: 2 }, owner: 'p1', suspend: true },
          ],
        } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'fireplace',
          controller: 'p1',
          owner: 'p1',
          counters: { time: 2 },
          card: { id: 'fireplace-card', name: 'Rotating Fireplace', type_line: 'Artifact' },
        },
        {
          id: 'their-permanent',
          controller: 'p2',
          owner: 'p2',
          counters: { time: 3 },
          card: { id: 'their-permanent-card', name: 'Their Card', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const fireplace = (result.state.battlefield || []).find((perm: any) => perm.id === 'fireplace') as any;
    const theirs = (result.state.battlefield || []).find((perm: any) => perm.id === 'their-permanent') as any;

    expect(result.appliedSteps.some(s => s.kind === 'time_travel')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'time_travel')).toBe(false);
    expect(fireplace?.counters?.time).toBe(1);
    expect(theirs?.counters?.time).toBe(3);
    expect(p1.exile[0]?.counters?.time).toBe(1);
  });

  it('applies repeated time travel by repeating the deterministic time-counter removal', () => {
    const ir = parseOracleTextToIR('Time travel three times.', 'The Tenth Doctor');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'impending-a',
          controller: 'p1',
          owner: 'p1',
          counters: { time: 3 },
          card: { id: 'impending-a-card', name: 'Impending Test', type_line: 'Enchantment Creature' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const permanent = (result.state.battlefield || []).find((perm: any) => perm.id === 'impending-a') as any;

    expect(result.appliedSteps.some(s => s.kind === 'time_travel')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'time_travel')).toBe(false);
    expect(permanent?.counters?.time).toBeUndefined();
  });

  it('treats defending-player scry as deterministic no-op when target-opponent library is empty', () => {
    const ir = parseOracleTextToIR('Defending player scries 1.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    expect(result.appliedSteps.some(s => s.kind === 'scry')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'scry')).toBe(false);
  });

  it('treats the-defending-player surveil as deterministic no-op when target-opponent library is empty', () => {
    const ir = parseOracleTextToIR('The defending player surveils 1.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    expect(result.appliedSteps.some(s => s.kind === 'surveil')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'surveil')).toBe(false);
  });

  it('treats each-opponent scry as a deterministic no-op when all libraries are empty', () => {
    const ir = parseOracleTextToIR('Each opponent scries 1.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.appliedSteps.some(s => s.kind === 'scry')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'scry')).toBe(false);
  });

  it('applies impulse_exile_top when permission says cast that spell', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. You may cast that spell this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with remains-exiled permission window', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You may play that card for as long as it remains exiled.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with leading remains-exiled permission (and trailing if restriction)', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. For as long as that card remains exiled, you may play it if you control a Kavu.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with next-end-step permission window', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until your next end step, you may play that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with trailing next-end-step can-play permission window', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You can play it until your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with trailing through-next-end-step can-play permission window', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You can play it through your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with next-turn permission window', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until your next turn, you may play that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when a look clause intervenes', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You may look at that card for as long as it remains exiled. You may play that card this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person can-cast this-turn wording', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library. You can cast it this turn.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person may-cast until-end-of-your-next-turn wording', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until the end of your next turn, you may cast that card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person next-turn cast + mana-rider wording', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. Until the end of your next turn, you may cast that card and you may spend mana as though it were mana of any color to cast that spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person leading next-turn play-them wording', () => {
    const ir = parseOracleTextToIR(
      'Exile the top two cards of your library. Until the end of your next turn, you may play them.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(2);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(p1.exile[1]?.id).toBe('c2');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person can-play until-end-of-your-next-turn wording', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You can play that card until end of your next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top with first-person can-play until-end-of-the-next-turn wording', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You can play that card until end of the next turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when multiple reminder clauses intervene', () => {
    const ir = parseOracleTextToIR(
      'Exile the top card of your library. You may look at that card any time. You may spend mana as though it were mana of any color to cast it. You may play that card this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it("applies impulse_exile_top for each player by exiling from each player's library", () => {
    const ir = parseOracleTextToIR("Exile the top card of each player's library. You may play those cards this turn.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library).toHaveLength(1);
    expect(p2.library).toHaveLength(0);
    expect(p1.exile).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);

    expect(p1.exile[0]?.id).toBe('p1c1');
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
  });

  it("applies impulse_exile_top for each player with next-turn mana-spend reminder suffix", () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of each player's library. Until the end of your next turn, you may play those cards, and mana of any type can be spent to cast those spells.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library).toHaveLength(1);
    expect(p2.library).toHaveLength(0);
    expect(p1.exile).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('p1c1');
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top for target-opponent choose-one next-turn mana rider template', () => {
    const ir = parseOracleTextToIR(
      "Exile the top three cards of target opponent's library. Choose one of them. Until the end of your next turn, you may play that card, and you may spend mana as though it were mana of any color to cast it.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library).toHaveLength(2);
    expect(p2.library).toHaveLength(0);
    expect(p1.exile).toHaveLength(0);
    expect(p2.exile).toHaveLength(3);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies exile_top by exiling the top card of your library', () => {
    const ir = parseOracleTextToIR('Exile the top card of your library.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(1);
    expect(p1.exile[0]?.id).toBe('c1');
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top for each player by exiling from each player's library", () => {
    const ir = parseOracleTextToIR("Exile the top two cards of each player's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }, { id: 'p1c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.library).toHaveLength(1);
    expect(p2.library).toHaveLength(0);
    expect(p1.exile).toHaveLength(2);
    expect(p2.exile).toHaveLength(1);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies exile_top when defending player exiles the top card of their library (1v1)", () => {
    const ir = parseOracleTextToIR('Defending player exiles the top card of their library.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applies exile_top parsed from triggered ability effect', () => {
    const ir = parseOracleTextToIR("Whenever Etali attacks, exile the top card of each player's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1c1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1']);
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it('applies exile_top parsed from replacement effect (instead pattern)', () => {
    const ir = parseOracleTextToIR('If you would draw a card, exile the top two cards of your library instead.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library.map((c: any) => c.id)).toEqual(['c3']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['c1', 'c2']);
    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
  });

  it("applies impulse_exile_top for each opponent by exiling from opponents' libraries", () => {
    const ir = parseOracleTextToIR("Exile the top card of each opponent's library. You may play those cards this turn.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;
    expect(p1.library).toHaveLength(1);
    expect(p1.exile).toHaveLength(0);
    expect(p2.library).toHaveLength(1);
    expect(p3.library).toHaveLength(0);
    expect(p2.exile).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
  });

  it('skips impulse_exile_top when amount is unknown', () => {
    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      [
        {
          kind: 'impulse_exile_top',
          who: { kind: 'you' },
          amount: { kind: 'x' },
          duration: 'this_turn',
          permission: 'play',
          raw: 'Exile the top X cards of your library. You may play those cards this turn.',
        } as any,
      ],
      { controllerId: 'p1' }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.library).toHaveLength(2);
    expect(p1.exile).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
  });

  it("skips impulse_exile_top for corpus Dead Man's Chest wording when amount is unknown", () => {
    const ir = parseOracleTextToIR(
      "When enchanted creature dies, exile cards equal to its power from the top of its owner's library. You may cast spells from among those cards for as long as they remain exiled, and mana of any type can be spent to cast them.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.exile || []).toHaveLength(0);
  });

  it('applies impulse_exile_top loop for corpus Transforming Flourish wording (until nonland)', () => {
    const ir = parseOracleTextToIR(
      "Destroy target artifact or creature you don't control. If that permanent is destroyed this way, its controller exiles cards from the top of their library until they exile a nonland card, then they may cast that card without paying its mana cost.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', name: 'Island', type_line: 'Basic Land � Island' },
            { id: 'p2c2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual([]);
    expect(p1.exile || []).toHaveLength(0);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
  });

  it('applies impulse_exile_top loop for corpus Bismuth Mindrender wording (target player until nonland)', () => {
    const ir = parseOracleTextToIR(
      "Whenever this creature deals combat damage to a player, that player exiles cards from the top of their library until they exile a nonland card. You may cast that card by paying life equal to the spell's mana value rather than paying its mana cost.",
      'Test'
    );
    const ability = ir.abilities.find(a => a.type === 'triggered');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Basic Land � Swamp' },
            { id: 'p2c2', type_line: 'Land' },
            { id: 'p2c3', type_line: 'Creature � Horror' },
            { id: 'p2c4', type_line: 'Instant' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4']);
    expect(p1.exile || []).toHaveLength(0);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
  });

  it('skips impulse_exile_top for corpus Possibility Storm wording when reference spell type context is missing', () => {
    const ir = parseOracleTextToIR(
      'Whenever a player casts a spell from their hand, that player exiles it, then exiles cards from the top of their library until they exile a card that shares a card type with it. That player may cast that card without paying its mana cost. Then they put all cards exiled with this enchantment on the bottom of their library in a random order.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.exile || []).toHaveLength(0);
  });

  it('applies impulse_exile_top loop for corpus Possibility Storm wording when reference spell type context is provided', () => {
    const ir = parseOracleTextToIR(
      'Whenever a player casts a spell from their hand, that player exiles it, then exiles cards from the top of their library until they exile a card that shares a card type with it. That player may cast that card without paying its mana cost. Then they put all cards exiled with this enchantment on the bottom of their library in a random order.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Basic Land � Forest' },
            { id: 'p2c2', type_line: 'Creature � Elf' },
            { id: 'p2c3', type_line: 'Instant' },
            { id: 'p2c4', type_line: 'Sorcery' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
      referenceSpellTypes: ['instant'],
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4', 'p2c1', 'p2c2', 'p2c3']);
    expect(p1.exile || []).toHaveLength(0);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies Cascade reminder text when reference spell mana value context is provided', () => {
    const ir = parseOracleTextToIR(
      "When you cast this spell, exile cards from the top of your library until you exile a nonland card whose mana value is less than this spell's mana value. You may cast it without paying its mana cost. Put the exiled cards on the bottom of your library in a random order.",
      'Bloodbraid Elf'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'p1c1', name: 'Forest', type_line: 'Basic Land - Forest' },
            { id: 'p1c2', name: 'Colossal Dreadmaw', type_line: 'Creature - Dinosaur', mana_value: 6 },
            { id: 'p1c3', name: 'Lightning Bolt', type_line: 'Instant', mana_value: 1 },
            { id: 'p1c4', name: 'Mountain', type_line: 'Basic Land - Mountain' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      referenceSpellManaValue: 4,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'unknown')).toBe(true);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c4', 'p1c1', 'p1c2', 'p1c3']);
    expect((p1.exile || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies Living weapon reminder text by creating a Germ and attaching the source Equipment to it', () => {
    const ir = parseOracleTextToIR(
      'When this Equipment enters, create a 0/0 black Phyrexian Germ creature token, then attach this Equipment to it.',
      'Flayer Husk'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'flayer-husk',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Flayer Husk',
          cardType: 'Artifact - Equipment',
          type_line: 'Artifact - Equipment',
          tapped: false,
          summoningSick: false,
          counters: {},
          card: {
            id: 'flayer-husk-card',
            name: 'Flayer Husk',
            type_line: 'Artifact - Equipment',
          },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'flayer-husk',
      sourceName: 'Flayer Husk',
    });
    const equipment = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'flayer-husk');
    const germ = (result.state.battlefield as any[]).find((perm: any) => perm.id !== 'flayer-husk');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token', 'attach']);
    expect(germ?.isToken).toBe(true);
    expect(germ?.basePower).toBe(0);
    expect(germ?.baseToughness).toBe(0);
    expect(germ?.card?.colors).toContain('B');
    expect(germ?.controller).toBe('p1');
    expect(equipment?.attachedTo).toBe(germ?.id);
  });

  it('applies Possibility Storm cleanup when no shared card type is found (returns all revealed cards)', () => {
    const ir = parseOracleTextToIR(
      'Whenever a player casts a spell from their hand, that player exiles it, then exiles cards from the top of their library until they exile a card that shares a card type with it. That player may cast that card without paying its mana cost. Then they put all cards exiled with this enchantment on the bottom of their library in a random order.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Creature � Elf' },
            { id: 'p2c2', type_line: 'Land' },
            { id: 'p2c3', type_line: 'Creature � Human' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
      referenceSpellTypes: ['instant'],
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies impulse_exile_top loop for corpus Chaos Wand wording (until instant or sorcery)', () => {
    const ir = parseOracleTextToIR(
      "{4}, {T}: Target opponent exiles cards from the top of their library until they exile an instant or sorcery card. You may cast that card without paying its mana cost. Then put the exiled cards that weren't cast this way on the bottom of that library in a random order.",
      'Test'
    );
    const ability = ir.abilities.find(a => a.type === 'activated');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', name: 'Forest', type_line: 'Basic Land � Forest' },
            { id: 'p2c2', name: 'Runeclaw Bear', type_line: 'Creature � Bear' },
            { id: 'p2c3', name: 'Shock', type_line: 'Instant' },
            { id: 'p2c4', name: 'Opt', type_line: 'Instant' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetWhiteOppCreatures', targetOpponentId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4', 'p2c1', 'p2c2', 'p2c3']);
    expect(p1.exile || []).toHaveLength(0);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies Chaos Wand bottom-of-library rider when no instant or sorcery is found', () => {
    const ir = parseOracleTextToIR(
      "{4}, {T}: Target opponent exiles cards from the top of their library until they exile an instant or sorcery card. You may cast that card without paying its mana cost. Then put the exiled cards that weren't cast this way on the bottom of that library in a random order.",
      'Test'
    );
    const ability = ir.abilities.find(a => a.type === 'activated');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Basic Land � Plains' },
            { id: 'p2c2', type_line: 'Creature � Knight' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies impulse_exile_top loop for corpus Dream Harvest wording (total mana value threshold)', () => {
    const ir = parseOracleTextToIR(
      'Each opponent exiles cards from the top of their library until they have exiled cards with total mana value 5 or greater this way. Until end of turn, you may cast cards exiled this way without paying their mana costs.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', mana_value: 2 },
            { id: 'p2c2', mana_value: 2 },
            { id: 'p2c3', mana_value: 1 },
            { id: 'p2c4', mana_value: 6 },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [
            { id: 'p3c1', mana_value: 3 },
            { id: 'p3c2', mana_value: 3 },
            { id: 'p3c3', mana_value: 1 },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c3']);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1', 'p3c2']);
  });

  it('applies impulse_exile_top loop for corpus Wand of Wonder wording (each opponent until instant or sorcery)', () => {
    const ir = parseOracleTextToIR(
      '{4}, {T}: Roll a d20. Each opponent exiles cards from the top of their library until they exile an instant or sorcery card, then shuffles the rest into their library. You may cast up to X instant and/or sorcery spells from among cards exiled this way without paying their mana costs.',
      'Test'
    );
    const ability = ir.abilities.find(a => a.type === 'activated');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Basic Land � Forest' },
            { id: 'p2c2', type_line: 'Creature � Bear' },
            { id: 'p2c3', type_line: 'Instant' },
            { id: 'p2c4', type_line: 'Sorcery' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [
            { id: 'p3c1', type_line: 'Sorcery' },
            { id: 'p3c2', type_line: 'Creature � Elf' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4', 'p2c1', 'p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c3']);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c2']);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1']);
  });

  it('applies Wand of Wonder cleanup when an opponent has no instant or sorcery (returns all revealed cards)', () => {
    const ir = parseOracleTextToIR(
      '{4}, {T}: Roll a d20. Each opponent exiles cards from the top of their library until they exile an instant or sorcery card, then shuffles the rest into their library. You may cast up to X instant and/or sorcery spells from among cards exiled this way without paying their mana costs.',
      'Test'
    );
    const ability = ir.abilities.find(a => a.type === 'activated');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Creature � Elf' },
            { id: 'p2c2', type_line: 'Land' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [
            { id: 'p3c1', type_line: 'Instant' },
            { id: 'p3c2', type_line: 'Creature � Human' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual([]);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c2']);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1']);
  });

  it('applies life gain and life loss', () => {
    const ir = parseOracleTextToIR('You gain 3 life. You lose 2 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.life).toBe(41);
    expect(result.appliedSteps.filter(s => s.kind === 'gain_life')).toHaveLength(1);
    expect(result.appliedSteps.filter(s => s.kind === 'lose_life')).toHaveLength(1);
  });

  it('applies add_mana for "you" by updating state.manaPool', () => {
    const ir = parseOracleTextToIR('Add {R}{R}.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const pool = (result.state as any).manaPool?.['p1'];
    expect(pool).toBeTruthy();
    expect(pool.red).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'add_mana')).toBe(true);
  });

  it('applies add_mana for each player', () => {
    const ir = parseOracleTextToIR('Each player adds {G}.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const manaPool = (result.state as any).manaPool;

    expect(manaPool?.['p1']?.green).toBe(1);
    expect(manaPool?.['p2']?.green).toBe(1);
    expect(result.appliedSteps.some(s => s.kind === 'add_mana')).toBe(true);
  });

  it('applies add_mana for defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('Defending player adds {G}.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const manaPool = (result.state as any).manaPool;

    expect(manaPool?.['p1']?.green ?? 0).toBe(0);
    expect(manaPool?.['p2']?.green).toBe(1);
    expect(result.appliedSteps.some(s => s.kind === 'add_mana')).toBe(true);
  });

  it('applies add_mana for the defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('The defending player adds {G}.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const manaPool = (result.state as any).manaPool;

    expect(manaPool?.['p1']?.green ?? 0).toBe(0);
    expect(manaPool?.['p2']?.green).toBe(1);
    expect(result.appliedSteps.some(s => s.kind === 'add_mana')).toBe(true);
  });

  it('applies deal_damage to each opponent (deterministic player target)', () => {
    const ir = parseOracleTextToIR('It deals 3 damage to each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(37);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to "that player" via target_player selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to that player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetWhitePlayerCreatures', targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to each of those opponents via relational selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each of those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(38);
  });

  it('applies deal_damage to those opponents via relational selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(38);
  });

  it('applies deal_damage to all of those opponents via relational selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to all of those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(38);
  });

  it('applies deal_damage to all those opponents via relational selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to all those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(38);
  });

  it('applies deal_damage to "him or her" via target_player selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to him or her.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to "its controller" via target_player selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to its controller.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to "its owner" via target_player selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to its owner.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that creature's controller\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that creature's controller.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that permanent's owner\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that permanent's owner.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that creature's owner\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that creature's owner.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that card's controller\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that card's controller.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that card's owner\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that card's owner.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it("applies deal_damage to \"that enchantment's owner\" via target_player selector context binding", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to that enchantment's owner.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to "that opponent" via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to that opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to "defending player" via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to defending player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to "the defending player" via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to the defending player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(38);
  });

  it('applies deal_damage to each player (deterministic player target)', () => {
    const ir = parseOracleTextToIR('Deal 2 damage to each player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(38);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature (deterministic battlefield group)', () => {
    const ir = parseOracleTextToIR('It deals 3 damage to each creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'a1', controller: 'p2', owner: 'p2', card: { id: 'a1_card', name: 'Sol Ring', type_line: 'Artifact' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;
    const a1 = (result.state.battlefield || []).find((p: any) => p.id === 'a1') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(3);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(3);
    expect(a1?.counters?.damage ?? a1?.markedDamage ?? a1?.damage ?? 0).toBe(0);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('records damage provenance on creatures damaged by a deterministic deal_damage effect', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature - Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature - Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'lava-source',
      sourceName: 'Lava Source',
    });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.damageSourceIds).toEqual(['lava-source']);
    expect(c2?.damageSourceIds).toEqual(['lava-source']);
  });

  it('applies deal_damage to each creature and each planeswalker (deterministic battlefield group)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and each planeswalker.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'pw1', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw1_card', name: 'Test Walker', type_line: 'Planeswalker � Test' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const pw1 = (result.state.battlefield || []).find((p: any) => p.id === 'pw1') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(pw1?.loyalty ?? pw1?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to effective creatures in deterministic battlefield group resolution', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'printedDamageCreature', controller: 'p1', owner: 'p1', card: { id: 'printed_damage_creature_card', name: 'Grizzly Bears', type_line: 'Creature - Bear' } } as any,
        { id: 'effectiveDamageCreature', controller: 'p2', owner: 'p2', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Artifact', 'Creature'], power: 3, toughness: 3, card: { id: 'effective_damage_creature_card', name: 'Animated Relic', type_line: 'Artifact' } } as any,
        { id: 'plainArtifactDamage', controller: 'p2', owner: 'p2', card: { id: 'plain_artifact_damage_card', name: 'Relic', type_line: 'Artifact' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const printed = (result.state.battlefield || []).find((p: any) => p.id === 'printedDamageCreature') as any;
    const effective = (result.state.battlefield || []).find((p: any) => p.id === 'effectiveDamageCreature') as any;
    const plainArtifact = (result.state.battlefield || []).find((p: any) => p.id === 'plainArtifactDamage') as any;

    expect(printed?.counters?.damage ?? printed?.markedDamage ?? printed?.damage).toBe(2);
    expect(effective?.counters?.damage ?? effective?.markedDamage ?? effective?.damage).toBe(2);
    expect(plainArtifact?.counters?.damage ?? plainArtifact?.markedDamage ?? plainArtifact?.damage ?? 0).toBe(0);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each battle (deterministic battlefield group)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each battle.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'b1', controller: 'p2', owner: 'p2', counters: { defense: 5 }, card: { id: 'b1_card', name: 'Test Siege', type_line: 'Battle � Siege' } } as any,
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const b1 = (result.state.battlefield || []).find((p: any) => p.id === 'b1') as any;

    expect(b1?.counters?.defense).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each of your opponents (common player group phrasing)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each of your opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to your opponents (common player group phrasing without "each")', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to your opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to your opponents' creatures (shorthand possessive selector without each/all)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to your opponents' creatures.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to your opponents' planeswalkers (shorthand possessive selector without each/all)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to your opponents' planeswalkers.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'pw_home', controller: 'p1', owner: 'p1', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_home_card', name: 'Home Walker', type_line: 'Planeswalker � Test' } } as any,
        { id: 'pw_opp', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_opp_card', name: 'Opp Walker', type_line: 'Planeswalker � Test' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const pwHome = (result.state.battlefield || []).find((p: any) => p.id === 'pw_home') as any;
    const pwOpp = (result.state.battlefield || []).find((p: any) => p.id === 'pw_opp') as any;

    expect(pwHome?.loyalty ?? pwHome?.counters?.loyalty).toBe(5);
    expect(pwOpp?.loyalty ?? pwOpp?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to your opponents' battles (shorthand possessive selector without each/all)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to your opponents' battles.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'b_home', controller: 'p1', owner: 'p1', counters: { defense: 5 }, card: { id: 'b_home_card', name: 'Home Siege', type_line: 'Battle � Siege' } } as any,
        { id: 'b_opp', controller: 'p2', owner: 'p2', counters: { defense: 5 }, card: { id: 'b_opp_card', name: 'Opp Siege', type_line: 'Battle � Siege' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const bHome = (result.state.battlefield || []).find((p: any) => p.id === 'b_home') as any;
    const bOpp = (result.state.battlefield || []).find((p: any) => p.id === 'b_opp') as any;

    expect(bHome?.counters?.defense).toBe(5);
    expect(bOpp?.counters?.defense).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to your creatures (shorthand selector without each/all)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to your creatures.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage ?? 0).toBe(0);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to your opponents' creatures and you (mixed target using shorthand selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to your opponents' creatures and you.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to your opponents' creatures or planeswalkers and you (mixed target + shorthand union selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to your opponents' creatures or planeswalkers and you.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
        { id: 'pw1', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw1_card', name: 'Test Walker', type_line: 'Planeswalker � Test' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;
    const pw1 = (result.state.battlefield || []).find((p: any) => p.id === 'pw1') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(pw1?.loyalty ?? pw1?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to creatures your opponents control and each opponent (mixed target + controller-suffix selector without each/all)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to creatures your opponents control and each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c_you', controller: 'p1', owner: 'p1', card: { id: 'c_you_card', name: 'Your Bear', type_line: 'Creature � Bear' } } as any,
        { id: 'c_opp_2', controller: 'p2', owner: 'p2', card: { id: 'c_opp_2_card', name: 'Opp 2 Giant', type_line: 'Creature � Giant' } } as any,
        { id: 'c_opp_3', controller: 'p3', owner: 'p3', card: { id: 'c_opp_3_card', name: 'Opp 3 Lion', type_line: 'Creature � Cat' } } as any,
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const p3 = result.state.players.find((p: any) => p.id === 'p3') as any;
    const cYou = (result.state.battlefield || []).find((p: any) => p.id === 'c_you') as any;
    const cOpp2 = (result.state.battlefield || []).find((p: any) => p.id === 'c_opp_2') as any;
    const cOpp3 = (result.state.battlefield || []).find((p: any) => p.id === 'c_opp_3') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(p3.life).toBe(38);

    expect(cYou?.counters?.damage ?? cYou?.markedDamage ?? cYou?.damage ?? 0).toBe(0);
    expect(cOpp2?.counters?.damage ?? cOpp2?.markedDamage ?? cOpp2?.damage).toBe(2);
    expect(cOpp3?.counters?.damage ?? cOpp3?.markedDamage ?? cOpp3?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to planeswalkers your opponents control and you (mixed target + controller-suffix selector without each/all)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to planeswalkers your opponents control and you.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'pw_you', controller: 'p1', owner: 'p1', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_you_card', name: 'Your Walker', type_line: 'Planeswalker � Test' } } as any,
        { id: 'pw_opp', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_opp_card', name: 'Opp Walker', type_line: 'Planeswalker � Test' } } as any,
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const pwYou = (result.state.battlefield || []).find((p: any) => p.id === 'pw_you') as any;
    const pwOpp = (result.state.battlefield || []).find((p: any) => p.id === 'pw_opp') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(pwYou?.loyalty ?? pwYou?.counters?.loyalty).toBe(5);
    expect(pwOpp?.loyalty ?? pwOpp?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and each opponent (mixed deterministic targets)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and defending player (mixed target + target_opponent context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and defending player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and the defending player (mixed target + target_opponent context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and the defending player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and that player (mixed target + target_player context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and that player.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and that opponent (mixed target + target_opponent context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and that opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and each of those opponents (mixed relational selector context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and each of those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p3'] as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const p3 = result.state.players.find((p: any) => p.id === 'p3') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(40);
    expect(p3.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and all of those opponents (mixed relational selector context)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and all of those opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
    });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const p3 = result.state.players.find((p: any) => p.id === 'p3') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(p3.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature and your opponents (mixed deterministic targets, player group shorthand)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature and your opponents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature or planeswalker and each opponent (mixed deterministic targets with union selector)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature or planeswalker and each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'pw1', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw1_card', name: 'Test Walker', type_line: 'Planeswalker � Test' } } as any,
        { id: 'a1', controller: 'p2', owner: 'p2', card: { id: 'a1_card', name: 'Sol Ring', type_line: 'Artifact' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const pw1 = (result.state.battlefield || []).find((p: any) => p.id === 'pw1') as any;
    const a1 = (result.state.battlefield || []).find((p: any) => p.id === 'a1') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(pw1?.loyalty ?? pw1?.counters?.loyalty).toBe(3);
    expect(a1?.counters?.damage ?? a1?.markedDamage ?? a1?.damage ?? 0).toBe(0);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each of your opponents' creatures (of + possessive selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each of your opponents' creatures.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to you and each creature (mixed deterministic targets)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to you and each creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature, each planeswalker, and each opponent (mixed deterministic targets)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature, each planeswalker, and each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
        {
          id: 'pw1',
          controller: 'p2',
          owner: 'p2',
          loyalty: 5,
          counters: { loyalty: 5 },
          card: { id: 'pw1_card', name: 'Test Walker', type_line: 'Planeswalker � Test' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;
    const pw1 = (result.state.battlefield || []).find((p: any) => p.id === 'pw1') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(pw1?.loyalty ?? pw1?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature you control and each opponent (mixed deterministic targets w/ controller filter)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature you control and each opponent.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(40);
    expect(p2.life).toBe(38);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage).toBe(2);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage ?? 0).toBe(0);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each planeswalker your opponents control and you (mixed deterministic targets w/ controller filter)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each planeswalker your opponents control and you.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'pw1',
          controller: 'p2',
          owner: 'p2',
          loyalty: 5,
          counters: { loyalty: 5 },
          card: { id: 'pw1_card', name: 'Test Walker', type_line: 'Planeswalker � Test' },
        } as any,
        {
          id: 'pw2',
          controller: 'p1',
          owner: 'p1',
          loyalty: 4,
          counters: { loyalty: 4 },
          card: { id: 'pw2_card', name: 'Home Walker', type_line: 'Planeswalker � Test' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const pw1 = (result.state.battlefield || []).find((p: any) => p.id === 'pw1') as any;
    const pw2 = (result.state.battlefield || []).find((p: any) => p.id === 'pw2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(pw1?.loyalty ?? pw1?.counters?.loyalty).toBe(3);
    expect(pw2?.loyalty ?? pw2?.counters?.loyalty).toBe(4);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each creature you don't control (opponent-scope controller filter)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each creature you don't control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies deal_damage to each creature an opponent controls (opponent-scope controller filter)', () => {
    const ir = parseOracleTextToIR('It deals 2 damage to each creature an opponent controls.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each creature you don�t control (curly apostrophe variant)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each creature you don’t control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each creature you don�t control and you (mixed deterministic targets, curly apostrophe)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each creature you don’t control and you.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each opponent's creatures (possessive opponent-scope selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each opponent's creatures.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each opponent�s creatures and you (possessive + curly apostrophe mixed target)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each opponent’s creatures and you.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'Grizzly Bears', type_line: 'Creature � Bear' } } as any,
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'Hill Giant', type_line: 'Creature � Giant' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find((p: any) => p.id === 'p1') as any;
    const p2 = result.state.players.find((p: any) => p.id === 'p2') as any;
    const c1 = (result.state.battlefield || []).find((p: any) => p.id === 'c1') as any;
    const c2 = (result.state.battlefield || []).find((p: any) => p.id === 'c2') as any;

    expect(p1.life).toBe(38);
    expect(p2.life).toBe(40);
    expect(c1?.counters?.damage ?? c1?.markedDamage ?? c1?.damage ?? 0).toBe(0);
    expect(c2?.counters?.damage ?? c2?.markedDamage ?? c2?.damage).toBe(2);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each opponent's planeswalkers (possessive opponent-scope selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each opponent's planeswalkers.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'pw_home', controller: 'p1', owner: 'p1', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_home_card', name: 'Home Walker', type_line: 'Planeswalker � Test' } } as any,
        { id: 'pw_opp', controller: 'p2', owner: 'p2', loyalty: 5, counters: { loyalty: 5 }, card: { id: 'pw_opp_card', name: 'Opp Walker', type_line: 'Planeswalker � Test' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const pwHome = (result.state.battlefield || []).find((p: any) => p.id === 'pw_home') as any;
    const pwOpp = (result.state.battlefield || []).find((p: any) => p.id === 'pw_opp') as any;

    expect(pwHome?.loyalty ?? pwHome?.counters?.loyalty).toBe(5);
    expect(pwOpp?.loyalty ?? pwOpp?.counters?.loyalty).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it("applies deal_damage to each opponent's battles (possessive opponent-scope selector)", () => {
    const ir = parseOracleTextToIR("It deals 2 damage to each opponent's battles.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'b_home', controller: 'p1', owner: 'p1', counters: { defense: 5 }, card: { id: 'b_home_card', name: 'Home Siege', type_line: 'Battle � Siege' } } as any,
        { id: 'b_opp', controller: 'p2', owner: 'p2', counters: { defense: 5 }, card: { id: 'b_opp_card', name: 'Opp Siege', type_line: 'Battle � Siege' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const bHome = (result.state.battlefield || []).find((p: any) => p.id === 'b_home') as any;
    const bOpp = (result.state.battlefield || []).find((p: any) => p.id === 'b_opp') as any;

    expect(bHome?.counters?.defense).toBe(5);
    expect(bOpp?.counters?.defense).toBe(3);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
  });

  it('applies destroy for "all creatures" by removing matching permanents from battlefield', () => {
    const ir = parseOracleTextToIR('Destroy all creatures.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_creature_1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
        {
          id: 'bf2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c_creature_2', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'bf3',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_artifact_1', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.graveyard).toHaveLength(1);
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
  });

  it('applies destroy for "all creatures" to effective creatures', () => {
    const ir = parseOracleTextToIR('Destroy all creatures.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'printedDestroyCreature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'printed_destroy_creature_card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
        },
        {
          id: 'effectiveDestroyCreature',
          controller: 'p1',
          owner: 'p1',
          cardType: 'Artifact',
          type_line: 'Artifact',
          effectiveTypes: ['Artifact', 'Creature'],
          card: { id: 'effective_destroy_creature_card', name: 'Animated Relic', type_line: 'Artifact' },
        },
        {
          id: 'plainDestroyArtifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'plain_destroy_artifact_card', name: 'Relic', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect((result.state.battlefield || []).map((p: any) => p.id)).toEqual(['plainDestroyArtifact']);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual([
      'printed_destroy_creature_card',
      'effective_destroy_creature_card',
    ]);
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
  });

  it('applies exile for "all artifacts you control" by moving them to exile', () => {
    const ir = parseOracleTextToIR('Exile all artifacts you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_art_1', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'bf2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c_art_2', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Mind Stone');
    expect(p1.exile).toHaveLength(1);
    expect(p2.exile).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'exile')).toBe(true);
  });

  it('applies exile for "artifacts you control" (controller-suffix selector without each/all)', () => {
    const ir = parseOracleTextToIR('Exile artifacts you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_art_1', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'bf2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c_art_2', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Mind Stone');
    expect(p1.exile).toHaveLength(1);
    expect(p2.exile).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'exile')).toBe(true);
  });

  it('applies exile for "artifacts you control" when controller ids contain whitespace', () => {
    const ir = parseOracleTextToIR('Exile artifacts you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: '  p1  ',
          owner: 'p1',
          card: { id: 'c_art_1', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'bf2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c_art_2', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(names).not.toContain('Sol Ring');
    expect(names).toContain('Mind Stone');
    expect(p1.exile).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'exile')).toBe(true);
  });

  it('ignores malformed-controller permanents for opponent controller filters', () => {
    const ir = parseOracleTextToIR("Exile your opponents' artifacts.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c_art_1', name: 'P1 Artifact', type_line: 'Artifact' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c_art_2', name: 'P2 Artifact', type_line: 'Artifact' } },
        { id: 'bf3', controller: '   ', owner: 'p2', card: { id: 'c_art_3', name: 'Malformed Artifact', type_line: 'Artifact' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(names).toContain('P1 Artifact');
    expect(names).not.toContain('P2 Artifact');
    expect(names).toContain('Malformed Artifact');
    expect((p2.exile || []).some((c: any) => c?.name === 'P2 Artifact')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'exile')).toBe(true);
  });

  it('applies exile for "your creatures" (shorthand selector without each/all)', () => {
    const ir = parseOracleTextToIR('Exile your creatures.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p1', owner: 'p1', card: { id: 'a1', name: 'Sol Ring', type_line: 'Artifact' } },
        { id: 'bf3', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).not.toContain('P1 Bear');
    expect(names).toContain('Sol Ring');
    expect(names).toContain('P2 Lion');

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.exile).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'exile')).toBe(true);
  });

  it('applies move_zone for returning all creature cards from your graveyard to your hand', () => {
    const ir = parseOracleTextToIR('Return all creature cards from your graveyard to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h0', name: 'Existing', type_line: 'Instant' }],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
            { id: 'g3', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['h0', 'g1', 'g3']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for exiling all creature cards from your graveyard', () => {
    const ir = parseOracleTextToIR('Exile all creature cards from your graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
            { id: 'g3', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          exile: [{ id: 'e0', name: 'Already Exiled', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['e0', 'g1', 'g3']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from your graveyard into your hand', () => {
    const ir = parseOracleTextToIR('Put all creature cards from your graveyard into your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h0', name: 'Existing', type_line: 'Instant' }],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
            { id: 'g3', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['h0', 'g1', 'g3']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from your graveyard into exile', () => {
    const ir = parseOracleTextToIR('Put all creature cards from your graveyard into exile.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
            { id: 'g3', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          exile: [{ id: 'e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['e0', 'g1', 'g3']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone bounce for returning all creatures to their owners\' hands', () => {
    const ir = parseOracleTextToIR("Return all creatures to their owners' hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'c1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c1card', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
        {
          id: 'c2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2card', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'a1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'a1card', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');
    expect(p1.hand.map((c: any) => c.id)).toEqual(['c1card']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['c2card']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone bounce for returning all artifacts you control to owners\' hands (stolen artifact returns to owner)', () => {
    const ir = parseOracleTextToIR("Return all artifacts you control to their owners' hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'stolen',
          controller: 'p1',
          owner: 'p2',
          card: { id: 'stolenCard', name: 'Mind Stone', type_line: 'Artifact' },
        },
        {
          id: 'yours',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'yoursCard', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'p2thing',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'p2thingCard', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Hill Giant');
    expect(p1.hand.map((c: any) => c.id)).toEqual(['yoursCard']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['stolenCard']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone bounce for returning "your creatures" to their owners\' hands (shorthand selector without each/all)', () => {
    const ir = parseOracleTextToIR("Return your creatures to their owners' hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'p1Creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'p1CreatureCard', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
        {
          id: 'stolenCreature',
          controller: 'p1',
          owner: 'p2',
          card: { id: 'stolenCreatureCard', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'p2Creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'p2CreatureCard', name: 'Silvercoat Lion', type_line: 'Creature � Cat' },
        },
        {
          id: 'p1Artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'p1ArtifactCard', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    const battlefieldNames = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(battlefieldNames).toEqual(expect.arrayContaining(['Silvercoat Lion', 'Sol Ring']));
    expect(battlefieldNames).not.toEqual(expect.arrayContaining(['Grizzly Bears', 'Hill Giant']));

    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1CreatureCard']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['stolenCreatureCard']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone bounce for returning "your opponents\u2019 creatures" to their owners\u2019 hands (shorthand selector without each/all)', () => {
    const ir = parseOracleTextToIR('Return your opponents\u2019 creatures to their owners\u2019 hands.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'p1Creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'p1CreatureCard', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
        {
          id: 'p2Creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'p2CreatureCard', name: 'Silvercoat Lion', type_line: 'Creature � Cat' },
        },
        {
          id: 'stolenCreature',
          controller: 'p2',
          owner: 'p1',
          card: { id: 'stolenCreatureCard', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'p2Artifact',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'p2ArtifactCard', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    const battlefieldNames = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(battlefieldNames).toEqual(expect.arrayContaining(['Grizzly Bears', 'Mind Stone']));
    expect(battlefieldNames).not.toEqual(expect.arrayContaining(['Silvercoat Lion', 'Hill Giant']));

    expect(p1.hand.map((c: any) => c.id)).toEqual(['stolenCreatureCard']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2CreatureCard']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from your graveyard onto the battlefield', () => {
    const ir = parseOracleTextToIR('Put all creature cards from your graveyard onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Opt', type_line: 'Instant' },
            { id: 'g3', name: 'Silvercoat Lion', type_line: 'Creature � Cat' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.state.battlefield).toHaveLength(2);
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).toContain('Grizzly Bears');
    expect(names).toContain('Silvercoat Lion');
  });

  it('applies destroy for "all artifacts and enchantments" by removing both types', () => {
    const ir = parseOracleTextToIR('Destroy all artifacts and enchantments.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_artifact_1', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'bf2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c_ench_1', name: 'Seal of Strength', type_line: 'Enchantment' },
        },
        {
          id: 'bf3',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_creature_1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Grizzly Bears');
  });

  it('applies destroy for "all nonland permanents" by keeping only lands', () => {
    const ir = parseOracleTextToIR('Destroy all nonland permanents.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_land_1', name: 'Plains', type_line: 'Land' },
        },
        {
          id: 'bf2',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_creature_1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Plains');
  });

  it('applies destroy for "all planeswalkers" by removing planeswalkers only', () => {
    const ir = parseOracleTextToIR('Destroy all planeswalkers.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_pw_1', name: 'Test Walker', type_line: 'Planeswalker � Test' },
        },
        {
          id: 'bf2',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_creature_1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Grizzly Bears');
  });

  it('applies destroy for "all battles" by removing battles only', () => {
    const ir = parseOracleTextToIR('Destroy all battles.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bf1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_battle_1', name: 'Test Siege', type_line: 'Battle � Siege' },
        },
        {
          id: 'bf2',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'c_creature_1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Grizzly Bears');
  });

  it('applies destroy for "all creatures you control" by removing only your creatures', () => {
    const ir = parseOracleTextToIR('Destroy all creatures you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('P2 Lion');
  });

  it('applies destroy for "creatures you control" (controller-suffix selector without each/all)', () => {
    const ir = parseOracleTextToIR('Destroy creatures you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('P2 Lion');
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
  });

  it('applies destroy for "all creatures your opponents control" by removing only opponent creatures', () => {
    const ir = parseOracleTextToIR('Destroy all creatures your opponents control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('P1 Bear');
  });

  it('applies destroy for "creatures your opponents control" (controller-suffix selector without each/all)', () => {
    const ir = parseOracleTextToIR('Destroy creatures your opponents control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
        { id: 'bf3', controller: 'p2', owner: 'p2', card: { id: 'a1', name: 'Sol Ring', type_line: 'Artifact' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).toContain('P1 Bear');
    expect(names).toContain('Sol Ring');
    expect(names).not.toContain('P2 Lion');
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
  });

  it("applies destroy for \"your opponents' creatures\" (shorthand selector without each/all)", () => {
    const ir = parseOracleTextToIR("Destroy your opponents' creatures.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
        { id: 'bf3', controller: 'p2', owner: 'p2', card: { id: 'a1', name: 'Sol Ring', type_line: 'Artifact' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).toContain('P1 Bear');
    expect(names).toContain('Sol Ring');
    expect(names).not.toContain('P2 Lion');

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.graveyard).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
  });

  it('applies destroy for "all creatures you don\'t control" by removing only non-owned creatures', () => {
    const ir = parseOracleTextToIR("Destroy all creatures you don't control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'bf1', controller: 'p1', owner: 'p1', card: { id: 'c1', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'bf2', controller: 'p2', owner: 'p2', card: { id: 'c2', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('P1 Bear');
  });

  it('applies move_zone for putting all creature cards from your graveyard onto the battlefield tapped', () => {
    const ir = parseOracleTextToIR('Put all creature cards from your graveyard onto the battlefield tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(result.state.battlefield).toHaveLength(1);
    const perm = (result.state.battlefield as any[])[0];
    expect(String(perm.card?.name)).toBe('Grizzly Bears');
    expect(perm.tapped).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for returning all creature cards from your exile to your hand', () => {
    const ir = parseOracleTextToIR('Return all creature cards from your exile to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear', canBePlayedBy: 'p1', playableUntilTurn: 123 },
          ],
        } as any,
      ],
    });

    (start as any).playableFromExile = { p1: { p1e2: 123 } };

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h0', 'p1e2']);

    // Leaving exile should clear impulse markers.
    expect((result.state as any).playableFromExile?.p1?.p1e2).toBeUndefined();
    const moved = p1.hand.find((c: any) => c.id === 'p1e2');
    expect(moved?.canBePlayedBy).toBeUndefined();
    expect(moved?.playableUntilTurn).toBeUndefined();

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from your exile onto the battlefield tapped', () => {
    const ir = parseOracleTextToIR('Put all creature cards from your exile onto the battlefield tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(result.state.battlefield).toHaveLength(1);
    const perm = (result.state.battlefield as any[])[0];
    expect(String(perm.card?.name)).toBe('Grizzly Bears');
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p1');
    expect(perm.tapped).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's exile onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each player's exile onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p2e2', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e1']);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning all creature cards from all exiles to their owners' hands", () => {
    const ir = parseOracleTextToIR("Return all creature cards from all exiles to their owners' hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h0', name: 'Existing2', type_line: 'Land' }],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);

    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h0', 'p1e2']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h0', 'p2e1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's exile onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's exile onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Opt', type_line: 'Instant' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p3e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p3.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    expect(giant).toBeTruthy();
    expect(bears).toBeTruthy();

    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p1');
    expect(String(bears.owner)).toBe('p3');
    expect(String(bears.controller)).toBe('p1');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from all exiles onto the battlefield tapped', () => {
    const ir = parseOracleTextToIR('Put all creature cards from all exiles onto the battlefield tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');
    expect(bears.tapped).toBe(true);
    expect(giant.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning all creature cards from each opponent's exile to their owners' hands", () => {
    const ir = parseOracleTextToIR(
      "Return all creature cards from each opponent's exile to their owners' hands.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h0', name: 'Existing2', type_line: 'Land' }],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p3e1', name: 'Silvercoat Lion', type_line: 'Creature � Cat' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Only opponents (p2, p3) are affected.
    expect(p1.exile).toHaveLength(1);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h0']);

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h0', 'p2e1']);

    expect(p3.exile).toHaveLength(0);
    expect(p3.hand.map((c: any) => c.id)).toEqual(['p3e1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from all exiles into their owners' graveyards", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from all exiles into their owners' graveyards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g0', name: 'Existing', type_line: 'Sorcery' }],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g0', 'p1e2']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2e1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's exile into their owners' graveyards", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's exile into their owners' graveyards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g0', name: 'Existing', type_line: 'Sorcery' }],
          exile: [{ id: 'p1e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p3g0', name: 'Existing3', type_line: 'Land' }],
          exile: [{ id: 'p3e1', name: 'Silvercoat Lion', type_line: 'Creature � Cat' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Only opponents (p2, p3) are affected.
    expect(p1.exile).toHaveLength(1);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g0']);

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2e1']);

    expect(p3.exile).toHaveLength(0);
    expect(p3.graveyard.map((c: any) => c.id)).toEqual(['p3g0', 'p3e1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from all exiles onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      'Put all creature cards from all exiles onto the battlefield under your control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    // Controller is the spell controller for all moved cards.
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p1');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("skips move_zone for putting all creature cards from each opponent's exile onto the battlefield without explicit control override", () => {
    const ir = parseOracleTextToIR('Put all creature cards from each opponent\'s exile onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.exile).toHaveLength(1);
    expect(result.state.battlefield).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's exile onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's exile onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Opt', type_line: 'Instant' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p3e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p3.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    expect(giant).toBeTruthy();
    expect(bears).toBeTruthy();
    // Under owners' control: controller should match owner (and not the spell controller).
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');
    expect(String(bears.owner)).toBe('p3');
    expect(String(bears.controller)).toBe('p3');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from all exiles onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from all exiles onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's exile onto the battlefield tapped under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's exile onto the battlefield tapped under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p3e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p3.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    expect(giant).toBeTruthy();
    expect(bears).toBeTruthy();
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');
    expect(String(bears.owner)).toBe('p3');
    expect(String(bears.controller)).toBe('p3');
    expect(giant.tapped).toBe(true);
    expect(bears.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from all exiles onto the battlefield tapped under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from all exiles onto the battlefield tapped under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');
    expect(bears.tapped).toBe(true);
    expect(giant.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's exile onto the battlefield tapped under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's exile onto the battlefield tapped under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p3e1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);
    expect(p3.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    expect(giant).toBeTruthy();
    expect(bears).toBeTruthy();
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p1');
    expect(String(bears.owner)).toBe('p3');
    expect(String(bears.controller)).toBe('p1');
    expect(giant.tapped).toBe(true);
    expect(bears.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from all exiles onto the battlefield tapped under your control', () => {
    const ir = parseOracleTextToIR('Put all creature cards from all exiles onto the battlefield tapped under your control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p1');
    expect(bears.tapped).toBe(true);
    expect(giant.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's exile onto the battlefield (default owners' control)", () => {
    const ir = parseOracleTextToIR("Put all creature cards from each player's exile onto the battlefield.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController).toBeUndefined();

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's exile onto the battlefield tapped under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each player's exile onto the battlefield tapped under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p1e1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1e2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2e2', name: 'Opt', type_line: 'Instant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e2']);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.owner)).toBe('p1');
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(giant.controller)).toBe('p1');
    expect(bears.tapped).toBe(true);
    expect(giant.tapped).toBe(true);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('skips move_zone for targeting-dependent graveyard-to-battlefield moves', () => {
    const ir = parseOracleTextToIR('Put target creature card from your graveyard onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.state.battlefield).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
  });

  it("applies move_zone for exiling all cards from each player's graveyard", () => {
    const ir = parseOracleTextToIR("Exile all cards from each player's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [{ id: 'p1e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e0', 'p1g1', 'p1g2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for exiling all cards from each player\'s hand', () => {
    const ir = parseOracleTextToIR("Exile all cards from each player's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [
            { id: 'p1h1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1h2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h1', name: 'Shock', type_line: 'Instant' }],
          graveyard: [],
          exile: [{ id: 'p2e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.hand).toHaveLength(0);
    expect(p2.hand).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1h1', 'p1h2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e0', 'p2h1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling all cards from target player's graveyard when targetPlayerId is bound", () => {
    const ir = parseOracleTextToIR("Exile all cards from target player's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2g2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
          exile: [{ id: 'p2e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e0', 'p2g1', 'p2g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for exiling all cards from target player's graveyard", () => {
    const ir = parseOracleTextToIR("Exile all cards from target player's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(1);
    expect(p1.exile || []).toHaveLength(0);
  });

  it("applies move_zone for exiling all cards from each opponent's graveyard", () => {
    const ir = parseOracleTextToIR("Exile all cards from each opponent's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p3g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [{ id: 'p3e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller's graveyard should be untouched.
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p1.exile || []).toHaveLength(0);

    expect(p2.graveyard).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2g1']);

    expect(p3.graveyard).toHaveLength(0);
    expect(p3.exile.map((c: any) => c.id)).toEqual(['p3e0', 'p3g1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling all cards from each opponent's hand", () => {
    const ir = parseOracleTextToIR("Exile all cards from each opponent's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h1', name: 'Opt', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h1', name: 'Shock', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [
            { id: 'p3h1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'p3h2', name: 'Opt', type_line: 'Instant' },
          ],
          graveyard: [],
          exile: [{ id: 'p3e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller's hand should be untouched.
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h1']);
    expect(p1.exile || []).toHaveLength(0);

    expect(p2.hand).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2h1']);

    expect(p3.hand).toHaveLength(0);
    expect(p3.exile.map((c: any) => c.id)).toEqual(['p3e0', 'p3h1', 'p3h2']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling all cards from target opponent's graveyard when targetOpponentId is bound", () => {
    const ir = parseOracleTextToIR("Exile all cards from target opponent's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2g2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
          exile: [{ id: 'p2e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e0', 'p2g1', 'p2g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for exiling all cards from target player's hand when targetPlayerId is bound", () => {
    const ir = parseOracleTextToIR("Exile all cards from target player's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h1', name: 'Opt', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [
            { id: 'p2h1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2h2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
          graveyard: [],
          exile: [{ id: 'p2e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h1']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.hand).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e0', 'p2h1', 'p2h2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for exiling all cards from target opponent's graveyard", () => {
    const ir = parseOracleTextToIR("Exile all cards from target opponent's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g2', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(1);
    expect(p2.exile || []).toHaveLength(0);
  });

  it("applies move_zone for returning all creature cards from target opponent's exile to their owners' hands when targetOpponentId is bound", () => {
    const ir = parseOracleTextToIR(
      "Return all creature cards from target opponent's exile to their owners' hands.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Opt', type_line: 'Instant' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2e2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p1.hand || []).toHaveLength(0);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e1']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h0', 'p2e2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting all creature cards from target opponent's graveyard onto the battlefield under your control when targetOpponentId is bound", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from target opponent's graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];
    const move = steps.find(s => s.kind === 'move_zone') as any;

    expect(move?.to).toBe('battlefield');
    expect(move?.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2g2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm?.card?.id)).toBe('p2g2');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting all creature cards from target player's exile onto the battlefield under their owners' control when targetPlayerId is bound", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from target player's exile onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];
    const move = steps.find(s => s.kind === 'move_zone') as any;

    expect(move?.to).toBe('battlefield');
    expect(move?.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [{ id: 'p1e1', name: 'Opt', type_line: 'Instant' }],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'p2e1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2e2', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1e1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e1']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p2');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm?.card?.id)).toBe('p2e2');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning two creature cards from target opponent's graveyard to their hand when exactly two match", () => {
    const ir = parseOracleTextToIR(
      "Return two creature cards from target opponent's graveyard to their hand.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
            { id: 'p2g2', name: 'Silvercoat Lion', type_line: 'Creature ï¿½ Cat' },
            { id: 'p2g3', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g3']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h0', 'p2g1', 'p2g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for returning two creature cards from target opponent's graveyard when more than two match", () => {
    const ir = parseOracleTextToIR(
      "Return two creature cards from target opponent's graveyard to their hand.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
            { id: 'p2g2', name: 'Silvercoat Lion', type_line: 'Creature ï¿½ Cat' },
            { id: 'p2g3', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1', 'p2g2', 'p2g3']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h0']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.automationGaps.some((gap: any) => gap.reasonCode === 'player_choice_required')).toBe(true);
  });

  it("applies move_zone for putting two creature cards from target opponent's graveyard onto the battlefield under their owners' control when exactly two match", () => {
    const ir = parseOracleTextToIR(
      "Put two creature cards from target opponent's graveyard onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];
    const move = steps.find(s => s.kind === 'move_zone') as any;

    expect(move?.to).toBe('battlefield');
    expect(move?.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
            { id: 'p2g2', name: 'Silvercoat Lion', type_line: 'Creature ï¿½ Cat' },
            { id: 'p2g3', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const battlefield = result.state.battlefield as any[];

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g3']);
    expect(battlefield).toHaveLength(2);
    expect(battlefield.map((perm: any) => String(perm.owner))).toEqual(['p2', 'p2']);
    expect(battlefield.map((perm: any) => String(perm.controller))).toEqual(['p2', 'p2']);
    expect(battlefield.map((perm: any) => String(perm.card?.id))).toEqual(['p2g1', 'p2g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for putting two creature cards from target opponent's graveyard onto the battlefield without explicit control override", () => {
    const ir = parseOracleTextToIR(
      "Put two creature cards from target opponent's graveyard onto the battlefield.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature ï¿½ Giant' },
            { id: 'p2g2', name: 'Silvercoat Lion', type_line: 'Creature ï¿½ Cat' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1', 'p2g2']);
    expect(result.state.battlefield).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(
      result.automationGaps.some((gap: any) => gap.reasonCode === 'battlefield_requires_explicit_control_override')
    ).toBe(true);
  });

  it("applies move_zone for exiling all creature cards from each opponent's graveyard", () => {
    const ir = parseOracleTextToIR("Exile all creature cards from each opponent's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p2g2', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p3g1', name: 'Silvercoat Lion', type_line: 'Creature � Cat' },
            { id: 'p3g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [{ id: 'p3e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller is not an opponent; should remain unchanged.
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p1.exile || []).toHaveLength(0);

    // Opponents: only creature cards exiled.
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2g2']);

    expect(p3.graveyard.map((c: any) => c.id)).toEqual(['p3g2']);
    expect(p3.exile.map((c: any) => c.id)).toEqual(['p3e0', 'p3g1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all instant cards from each opponent's hand into their graveyards", () => {
    const ir = parseOracleTextToIR(
      "Put all instant cards from each opponent's hand into their graveyards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h1', name: 'Opt', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [
            { id: 'p2h1', name: 'Opt', type_line: 'Instant' },
            { id: 'p2h2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          graveyard: [{ id: 'p2g0', name: 'Existing', type_line: 'Sorcery' }],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [
            { id: 'p3h1', name: 'Shock', type_line: 'Instant' },
            { id: 'p3h2', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller untouched.
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h1']);
    expect(p1.graveyard || []).toHaveLength(0);

    // Opponents: instants moved, non-instants kept.
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2h2']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g0', 'p2h1']);

    expect(p3.hand.map((c: any) => c.id)).toEqual(['p3h2']);
    expect(p3.graveyard.map((c: any) => c.id)).toEqual(['p3h1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for exiling all creature cards from all graveyards', () => {
    const ir = parseOracleTextToIR('Exile all creature cards from all graveyards.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [{ id: 'p2e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['p1g2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2e0', 'p2g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning all creature cards from each opponent's graveyard to their hands", () => {
    const ir = parseOracleTextToIR("Return all creature cards from each opponent's graveyard to their hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [
            { id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'p1g2', name: 'Opt', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [{ id: 'p3h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [{ id: 'p3g1', name: 'Silvercoat Lion', type_line: 'Creature � Cat' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller is not an opponent; should be untouched.
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1', 'p1g2']);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h0']);

    // Opponents: only creature cards returned from graveyard to hand.
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g2']);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2g1']);

    expect(p3.graveyard).toHaveLength(0);
    expect(p3.hand.map((c: any) => c.id)).toEqual(['p3h0', 'p3g1']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2g2', name: 'Opt', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p3g1', name: 'Silvercoat Lion', type_line: 'Creature � Cat' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // Controller's graveyard should be untouched (opponents only).
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);

    // Opponents: only creature cards moved from graveyard.
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g2']);
    expect(p3.graveyard).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    const lion = bf.find(p => String(p?.card?.name) === 'Silvercoat Lion');
    expect(giant).toBeTruthy();
    expect(lion).toBeTruthy();
    expect(String(giant.controller)).toBe('p1');
    expect(String(lion.controller)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');
    expect(String(lion.owner)).toBe('p3');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each opponent's graveyard onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each opponent's graveyard onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' },
            { id: 'p2g2', name: 'Opt', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    // Controller's graveyard should be untouched (opponents only).
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);

    // Opponent: only creature cards moved from graveyard.
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g2']);

    expect(result.state.battlefield).toHaveLength(1);
    const perm = (result.state.battlefield as any[])[0];
    expect(String(perm.card?.name)).toBe('Hill Giant');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.controller)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each player's graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard).toHaveLength(0);

    expect(result.state.battlefield).toHaveLength(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(String(perm.controller)).toBe('p1');
    }

    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(String(bears.owner)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's graveyard onto the battlefield tapped under your control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each player's graveyard onto the battlefield tapped under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');
    expect(move.entersTapped).toBe(true);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(String(perm.controller)).toBe('p1');
      expect(perm.tapped).toBe(true);
    }
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from each player's graveyard onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from each player's graveyard onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(String(bears.controller)).toBe('p1');
    expect(String(bears.owner)).toBe('p1');
    expect(String(giant.controller)).toBe('p2');
    expect(String(giant.owner)).toBe('p2');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all creature cards from all graveyards onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all creature cards from all graveyards onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.controller)).toBe('p1');
    expect(String(bears.owner)).toBe('p1');
    expect(String(giant.controller)).toBe('p2');
    expect(String(giant.owner)).toBe('p2');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("skips move_zone for putting all creature cards from each opponent's graveyard onto the battlefield without an explicit control override", () => {
    const ir = parseOracleTextToIR("Put all creature cards from each opponent's graveyard onto the battlefield.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    // Unchanged.
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1']);
    expect(result.state.battlefield || []).toHaveLength(0);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning all cards from each player's graveyard to their hands", () => {
    const ir = parseOracleTextToIR("Return all cards from each player's graveyard to their hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h0', name: 'Existing', type_line: 'Sorcery' }],
          graveyard: [{ id: 'p1g1', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p2g1', name: 'Shock', type_line: 'Instant' },
            { id: 'p2g2', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['p1h0', 'p1g1']);

    expect(p2.graveyard).toHaveLength(0);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2g1', 'p2g2']);

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from all graveyards onto the battlefield', () => {
    const ir = parseOracleTextToIR('Put all creature cards from all graveyards onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    // Graveyards lose only creature cards.
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard).toHaveLength(0);

    // Battlefield gains two creatures under their owners' control.
    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.controller)).toBe('p1');
    expect(String(bears.owner)).toBe('p1');
    expect(String(giant.controller)).toBe('p2');
    expect(String(giant.owner)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from all graveyards onto the battlefield under your control', () => {
    const ir = parseOracleTextToIR('Put all creature cards from all graveyards onto the battlefield under your control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'p1g1', name: 'Opt', type_line: 'Instant' },
            { id: 'p1g2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    // Graveyards lose only creature cards.
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard).toHaveLength(0);

    // Battlefield gains two creatures, both under the controller's control.
    expect(result.state.battlefield).toHaveLength(2);
    const bf = result.state.battlefield as any[];
    const bears = bf.find(p => String(p?.card?.name) === 'Grizzly Bears');
    const giant = bf.find(p => String(p?.card?.name) === 'Hill Giant');
    expect(bears).toBeTruthy();
    expect(giant).toBeTruthy();
    expect(String(bears.controller)).toBe('p1');
    expect(String(giant.controller)).toBe('p1');

    // Ownership remains tied to the graveyard they came from (best-effort).
    expect(String(bears.owner)).toBe('p1');
    expect(String(giant.owner)).toBe('p2');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all creature cards from all graveyards onto the battlefield tapped under your control', () => {
    const ir = parseOracleTextToIR(
      'Put all creature cards from all graveyards onto the battlefield tapped under your control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('you');
    expect(move.entersTapped).toBe(true);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'Hill Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    expect(result.state.battlefield).toHaveLength(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(String(perm.controller)).toBe('p1');
      expect(perm.tapped).toBe(true);
    }
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all cards from your hand into your graveyard', () => {
    const ir = parseOracleTextToIR('Put all cards from your hand into your graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [
            { id: 'h1', name: 'Shock', type_line: 'Instant' },
            { id: 'h2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          graveyard: [{ id: 'g0', name: 'Existing', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand).toHaveLength(0);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g0', 'h1', 'h2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for putting all land cards from your hand onto the battlefield tapped', () => {
    const ir = parseOracleTextToIR('Put all land cards from your hand onto the battlefield tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.entersTapped).toBe(true);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [
            { id: 'hLand', name: 'Plains', type_line: 'Basic Land � Plains' },
            { id: 'hSpell', name: 'Shock', type_line: 'Instant' },
          ],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['hSpell']);
    expect(result.state.battlefield).toHaveLength(1);
    const perm = (result.state.battlefield as any[])[0];
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p1');
    expect(perm.tapped).toBe(true);
    expect(String(perm?.card?.id)).toBe('hLand');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting all instant cards from each opponent's hand onto the battlefield under their owners' control", () => {
    const ir = parseOracleTextToIR(
      "Put all instant cards from each opponent's hand onto the battlefield under their owners' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const move = steps.find(s => s.kind === 'move_zone') as any;
    expect(move).toBeTruthy();
    expect(move.to).toBe('battlefield');
    expect(move.battlefieldController?.kind).toBe('owner_of_moved_cards');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [
            { id: 'p2i', name: 'Opt', type_line: 'Instant' },
            { id: 'p2c', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
          ],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2c']);
    expect(result.state.battlefield).toHaveLength(1);
    const perm = (result.state.battlefield as any[])[0];
    expect(String(perm.controller)).toBe('p2');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm?.card?.id)).toBe('p2i');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('skips move_zone for put-into when targeting-dependent', () => {
    const ir = parseOracleTextToIR('Put target creature card from your graveyard into your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for put-into when target creature card from your graveyard is bound', () => {
    const ir = parseOracleTextToIR('Put target creature card from your graveyard into your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('applies move_zone for exiling all creature cards from your hand', () => {
    const ir = parseOracleTextToIR('Exile all creature cards from your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [
            { id: 'h1', name: 'Shock', type_line: 'Instant' },
            { id: 'h2', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'h3', name: 'Hill Giant', type_line: 'Creature � Giant' },
          ],
          graveyard: [],
          exile: [{ id: 'e0', name: 'Existing', type_line: 'Sorcery' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['h1']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['e0', 'h2', 'h3']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('skips move_zone for hand moves when targeting-dependent', () => {
    const ir = parseOracleTextToIR('Exile target creature card from your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand).toHaveLength(1);
    expect(p1.exile).toHaveLength(0);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for hand moves when target creature card from your hand is bound', () => {
    const ir = parseOracleTextToIR('Exile target creature card from your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [
            { id: 'h1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'h2', name: 'Shock', type_line: 'Instant' },
          ],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'h1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['h2']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['h1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('skips move_zone when it is targeting-dependent or otherwise unsupported', () => {
    const ir = parseOracleTextToIR('Return target creature card from your graveyard to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for return when target creature card from your graveyard is bound', () => {
    const ir = parseOracleTextToIR('Return target creature card from your graveyard to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];
    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('applies move_zone for battlefield move when target creature card from your graveyard is bound', () => {
    const ir = parseOracleTextToIR('Put target creature card from your graveyard onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p1');
    expect(String(perm?.card?.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('skips move_zone for exile-from-graveyard when it is targeting-dependent', () => {
    const ir = parseOracleTextToIR('Exile target creature card from your graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.exile).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it('applies move_zone for exile-from-graveyard when target creature card is bound', () => {
    const ir = parseOracleTextToIR('Exile target creature card from your graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('applies move_zone for return-from-exile when target creature card is bound', () => {
    const ir = parseOracleTextToIR('Return target creature card from your exile to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'e1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'e2', name: 'Shock', type_line: 'Instant' },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'e1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['e1']);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['e2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('applies move_zone for exile-to-battlefield when target creature card is bound', () => {
    const ir = parseOracleTextToIR('Put target creature card from your exile onto the battlefield.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'e1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' },
            { id: 'e2', name: 'Shock', type_line: 'Instant' },
          ],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'e1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p1.exile.map((c: any) => c.id)).toEqual(['e2']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p1');
    expect(String(perm?.card?.id)).toBe('e1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning target creature card from target opponent's graveyard to their hand when bindings are present", () => {
    const ir = parseOracleTextToIR("Return target creature card from target opponent's graveyard to their hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'g1',
      selectorContext: { targetOpponentId: 'p2' } as any,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning target creature card from a graveyard to its owner's hand when direct target binding is present", () => {
    const ir = parseOracleTextToIR("Return target creature card from a graveyard to its owner's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card from a graveyard onto the battlefield under your control when direct target binding is present", () => {
    const ir = parseOracleTextToIR("Put target creature card from a graveyard onto the battlefield under your control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm?.card?.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for exiling target creature card from a graveyard when direct target binding is present", () => {
    const ir = parseOracleTextToIR('Exile target creature card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target card from a graveyard on top of its owner's library when direct target binding is present", () => {
    const ir = parseOracleTextToIR("Put target card from a graveyard on top of its owner's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard).toHaveLength(0);
    expect(p2.library.map((c: any) => c.id)).toEqual(['g1', 'l1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target card from a graveyard on the bottom of its owner's library when direct target binding is present", () => {
    const ir = parseOracleTextToIR("Put target card from a graveyard on the bottom of its owner's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard).toHaveLength(0);
    expect(p2.library.map((c: any) => c.id)).toEqual(['l1', 'g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning target artifact or enchantment card from a graveyard to its owner's hand", () => {
    const ir = parseOracleTextToIR("Return target artifact or enchantment card from a graveyard to its owner's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Sol Ring', type_line: 'Artifact' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p2', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning target creature or land card from a graveyard to its owner's hand", () => {
    const ir = parseOracleTextToIR("Return target creature or land card from a graveyard to its owner's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Forest', type_line: 'Basic Land - Forest' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting target artifact or creature card from a graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR("Put target artifact or creature card from a graveyard onto the battlefield under your control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting target creature or planeswalker card from a graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put target creature or planeswalker card from a graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Jace Beleren', type_line: 'Legendary Planeswalker - Jace' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling target instant or sorcery card from a graveyard", () => {
    const ir = parseOracleTextToIR('Exile target instant or sorcery card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting target artifact, instant, or sorcery card from a graveyard on the bottom of its owner's library", () => {
    const ir = parseOracleTextToIR(
      "Put target artifact, instant, or sorcery card from a graveyard on the bottom of its owner's library.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.library.map((c: any) => c.id)).toEqual(['l1', 'g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling target artifact card from a graveyard", () => {
    const ir = parseOracleTextToIR('Exile target artifact card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Sol Ring', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling target land card from a graveyard", () => {
    const ir = parseOracleTextToIR('Exile target land card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Forest', type_line: 'Basic Land - Forest' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling target nonland card from a graveyard", () => {
    const ir = parseOracleTextToIR('Exile target nonland card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting target land card from a graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR("Put target land card from a graveyard onto the battlefield under your control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Forest', type_line: 'Basic Land - Forest' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for putting target non-Dragon creature card from a graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put target non-Dragon creature card from a graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling up to one target Saga card from a graveyard when direct target binding is present", () => {
    const ir = parseOracleTextToIR('Exile up to one target Saga card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'The Kami War', type_line: 'Enchantment - Saga' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for exiling target card from a graveyard when direct target binding is present", () => {
    const ir = parseOracleTextToIR('Exile target card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for exiling up to one target card from a graveyard when direct target binding is present", () => {
    const ir = parseOracleTextToIR('Exile up to one target card from a graveyard.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.exile.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning target card from a graveyard to its owner's hand", () => {
    const ir = parseOracleTextToIR("Return target card from a graveyard to its owner's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for returning up to one target instant, sorcery, or creature card from a graveyard to its owner's hand", () => {
    const ir = parseOracleTextToIR(
      "Return up to one target instant, sorcery, or creature card from a graveyard to its owner's hand.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.hand.map((c: any) => c.id)).toEqual(['g1']);
    expect(p1.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting up to one target creature or planeswalker card from a graveyard onto the battlefield under your control", () => {
    const ir = parseOracleTextToIR(
      "Put up to one target creature or planeswalker card from a graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Jace Beleren', type_line: 'Legendary Planeswalker - Jace' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card from a graveyard onto the battlefield under its owner's control", () => {
    const ir = parseOracleTextToIR("Put target creature card from a graveyard onto the battlefield under its owner's control.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p2');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting up to one target card from a graveyard on the bottom of its owner's library", () => {
    const ir = parseOracleTextToIR("Put up to one target card from a graveyard on the bottom of its owner's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.library.map((c: any) => c.id)).toEqual(['l1', 'g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting up to one other target card from a graveyard on the bottom of its owner's library", () => {
    const ir = parseOracleTextToIR("Put up to one other target card from a graveyard on the bottom of its owner's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard).toHaveLength(0);
    expect(p1.library.map((c: any) => c.id)).toEqual(['l1', 'g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card from a graveyard onto the battlefield under your control tapped", () => {
    const ir = parseOracleTextToIR(
      "Put target creature card from a graveyard onto the battlefield under your control tapped.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(Boolean(perm.tapped)).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card from a graveyard onto the battlefield under your control with a corpse counter on it", () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from a graveyard onto the battlefield under your control with a corpse counter on it.',
      'From the Catacombs'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(perm.counters).toEqual({ corpse: 1 });
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card with mana value 4 or less from a graveyard onto the battlefield under your control with a finality counter on it", () => {
    const ir = parseOracleTextToIR(
      'Put target creature card with mana value 4 or less from a graveyard onto the battlefield under your control with a finality counter on it.',
      'Coalstoke Gearhulk'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear', mana_value: 2 }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm.card.id)).toBe('g1');
    expect(perm.counters).toEqual({ finality: 1 });
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'impossible_action')).toBe(false);
  });

  it('applies Spell mastery conditional battlefield-entry counters when your graveyard has enough instant and/or sorcery cards', () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from a graveyard onto the battlefield under your control. Spell mastery — If there are two or more instant and/or sorcery cards in your graveyard, that creature enters with two additional +1/+1 counters on it.',
      'Necromantic Summons'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'i1', name: 'Shock', type_line: 'Instant' },
            { id: 's1', name: 'Sign in Blood', type_line: 'Sorcery' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const perm = (result.state.battlefield as any[])[0];

    expect(result.state.battlefield).toHaveLength(1);
    expect(perm.counters).toEqual({ '+1/+1': 2 });
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('does not apply Spell mastery battlefield-entry counters when your graveyard is below the threshold', () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from a graveyard onto the battlefield under your control. Spell mastery — If there are two or more instant and/or sorcery cards in your graveyard, that creature enters with two additional +1/+1 counters on it.',
      'Necromantic Summons'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'i1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const perm = (result.state.battlefield as any[])[0];

    expect(result.state.battlefield).toHaveLength(1);
    expect(perm.counters || {}).toEqual({});
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it('applies life loss equal to the reanimated creature mana value for Reanimate-style followups', () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from a graveyard onto the battlefield under your control. You lose life equal to its mana value.',
      'Reanimate'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Rune-Scarred Demon', type_line: 'Creature - Demon', mana_value: 7 }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(p1.life).toBe(33);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'lose_life')).toBe(false);
  });

  it('applies Necromancy-style return-and-attach followups to the returned creature', () => {
    const ir = parseOracleTextToIR(
      'When this enchantment enters, if it\'s on the battlefield, it becomes an Aura with "enchant creature put onto the battlefield with Necromancy." Put target creature card from a graveyard onto the battlefield under your control and attach this enchantment to it.',
      'Necromancy'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature - Bear', mana_value: 2 }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'necromancy-aura',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          attachedTo: undefined,
          card: {
            id: 'necromancy-card',
            name: 'Necromancy',
            type_line: 'Enchantment',
            oracle_text:
              'When Necromancy enters, if it\'s on the battlefield, it becomes an Aura with enchant creature put onto the battlefield with Necromancy.',
          },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'necromancy-aura',
      sourceName: 'Necromancy',
      targetCreatureId: 'g1',
    });

    const returnedCreature = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'g1') as any;
    const necromancy = (result.state.battlefield as any[]).find(perm => perm?.id === 'necromancy-aura') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(returnedCreature).toBeTruthy();
    expect(returnedCreature.controller).toBe('p1');
    expect(returnedCreature.owner).toBe('p2');
    expect(necromancy.attachedTo).toBe(returnedCreature.id);
    expect(returnedCreature.attachments).toContain('necromancy-aura');
    expect(p2.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'attach')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'attach')).toBe(false);
  });

  it("safe-skips move_zone for a target creature card from a graveyard when the bound card fails a static mana value cap", () => {
    const ir = parseOracleTextToIR(
      'Put target creature card with mana value 4 or less from a graveyard onto the battlefield under your control with a finality counter on it.',
      'Coalstoke Gearhulk'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Ancient Brontodon', type_line: 'Creature - Dinosaur', mana_value: 8 }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((card: any) => card.id)).toEqual(['g1']);
    expect(result.state.battlefield).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'impossible_action')).toBe(false);
  });

  it("preserves Skullbriar counters when moving it from a graveyard back onto the battlefield", () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from a graveyard onto the battlefield under your control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'g1',
              name: 'Skullbriar, the Walking Grave',
              type_line: 'Legendary Creature - Elemental Zombie',
              oracle_text: "Haste\nWhenever Skullbriar, the Walking Grave deals combat damage to a player, put a +1/+1 counter on it.\nCounters remain on it as it moves to any zone other than a player's hand or library.",
              counters: { '+1/+1': 2 },
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'g1' });
    const perm = (result.state.battlefield as any[])[0];

    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.card.id)).toBe('g1');
    expect(perm.counters).toEqual({ '+1/+1': 2 });
    expect((perm.card as any).counters).toBeUndefined();
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for putting target card from a graveyard on your choice of top or bottom of its owner's library", () => {
    const ir = parseOracleTextToIR("Put target card from a graveyard on your choice of the top or bottom of its owner's library.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'l1', name: 'Island', type_line: 'Basic Land - Island' }],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetPermanentId: 'g1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g1']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['l1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("skips move_zone for target creature card from a graveyard when the direct target binding is unavailable", () => {
    const ir = parseOracleTextToIR("Return target creature card from a graveyard to its owner's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand).toHaveLength(0);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("applies move_zone for exiling target creature card from target player's hand when bindings are present", () => {
    const ir = parseOracleTextToIR("Exile target creature card from target player's hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [
            { id: 'h1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'h2', name: 'Shock', type_line: 'Instant' },
          ],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'h1',
      selectorContext: { targetPlayerId: 'p2' } as any,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand.map((c: any) => c.id)).toEqual(['h2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['h1']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for returning target creature card from target opponent's exile to their hand when bindings are present", () => {
    const ir = parseOracleTextToIR("Return target creature card from target opponent's exile to their hand.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'e1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'e2', name: 'Shock', type_line: 'Instant' },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'e1',
      selectorContext: { targetOpponentId: 'p2' } as any,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.hand.map((c: any) => c.id)).toEqual(['e1']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['e2']);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("applies move_zone for putting target creature card from target opponent's graveyard onto the battlefield under your control when bindings are present", () => {
    const ir = parseOracleTextToIR(
      "Put target creature card from target opponent's graveyard onto the battlefield under your control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature Ã¯Â¿Â½ Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'g1',
      selectorContext: { targetOpponentId: 'p2' } as any,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const perm = (result.state.battlefield as any[])[0];

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String(perm.controller)).toBe('p1');
    expect(String(perm.owner)).toBe('p2');
    expect(String(perm?.card?.id)).toBe('g1');
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(false);
  });

  it("skips move_zone for putting target creature card from target opponent's graveyard onto the battlefield without explicit control override", () => {
    const ir = parseOracleTextToIR("Put target creature card from target opponent's graveyard onto the battlefield.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'g1',
      selectorContext: { targetOpponentId: 'p2' } as any,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['g1']);
    expect(result.state.battlefield).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
  });

  it("skips move_zone for target player's graveyard card when the bound card is unavailable in that zone", () => {
    const ir = parseOracleTextToIR("Exile target creature card from target player's graveyard.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'g1', name: 'Grizzly Bears', type_line: 'Creature ï¿½ Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'g1',
      selectorContext: { targetPlayerId: 'p2' } as any,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g1']);
    expect(p2.graveyard).toHaveLength(0);
  });

  it('applies sacrifice when it is deterministic (each opponent sacrifices a creature if they have <= 1 creature)', () => {
    const ir = parseOracleTextToIR('Each opponent sacrifices a creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'p2creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'p2artifact',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'a2', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('Defending player sacrifices a creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'p2creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'p2artifact',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'a2', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for the defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('The defending player sacrifices a creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'p2creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
        {
          id: 'p2artifact',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'a2', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('skips sacrifice when it requires choice (player controls more than N matching permanents)', () => {
    const ir = parseOracleTextToIR('Each opponent sacrifices a creature.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'p2creature1',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2a', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
        },
        {
          id: 'p2creature2',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2b', name: 'Hill Giant', type_line: 'Creature � Giant' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(2);
    expect(p2.graveyard).toHaveLength(0);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for "all artifacts" (you) by moving them to graveyard', () => {
    const ir = parseOracleTextToIR('Sacrifice all artifacts.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'a1',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'solring', name: 'Sol Ring', type_line: 'Artifact' },
        },
        {
          id: 'a2',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'mindstone', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.state.battlefield).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(2);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for "your creatures" (shorthand selector without all/a/an)', () => {
    const ir = parseOracleTextToIR('Sacrifice your creatures.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'a1', controller: 'p1', owner: 'p1', card: { id: 'a1_card', name: 'Sol Ring', type_line: 'Artifact' } },
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).toContain('Sol Ring');
    expect(names).toContain('P2 Lion');
    expect(names).not.toContain('P1 Bear');

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for "creatures you control" (controller-suffix selector without all/a/an)', () => {
    const ir = parseOracleTextToIR('Sacrifice creatures you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'c1', controller: 'p1', owner: 'p1', card: { id: 'c1_card', name: 'P1 Bear', type_line: 'Creature � Bear' } },
        { id: 'a1', controller: 'p1', owner: 'p1', card: { id: 'a1_card', name: 'Sol Ring', type_line: 'Artifact' } },
        { id: 'c2', controller: 'p2', owner: 'p2', card: { id: 'c2_card', name: 'P2 Lion', type_line: 'Creature � Cat' } },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));
    expect(names).toContain('Sol Ring');
    expect(names).toContain('P2 Lion');
    expect(names).not.toContain('P1 Bear');

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for corpus enchantment template (A-Baleful Beholder)', () => {
    const ir = parseOracleTextToIR('Each opponent sacrifices an enchantment.', 'A-Baleful Beholder');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'p2enchantment',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'e2', name: 'Pacifism', type_line: 'Enchantment — Aura' },
        },
        {
          id: 'p2creature',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'c2', name: 'Hill Giant', type_line: 'Creature — Giant' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Hill Giant');
    expect(p2.graveyard).toHaveLength(1);
    expect(String(p2.graveyard[0]?.name || p2.graveyard[0]?.card?.name || '')).toBe('Pacifism');
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('applies sacrifice for target-player enchantment template via selector binding', () => {
    const ir = parseOracleTextToIR('Target player sacrifices an enchantment.', 'A-Baleful Beholder');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'p2enchantment',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'e2', name: 'Pacifism', type_line: 'Enchantment — Aura' },
        },
        {
          id: 'p2artifact',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'a2', name: 'Sol Ring', type_line: 'Artifact' },
        },
      ] as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name)).toBe('Sol Ring');
    expect(p2.graveyard).toHaveLength(1);
    expect(String(p2.graveyard[0]?.name || p2.graveyard[0]?.card?.name || '')).toBe('Pacifism');
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
  });

  it('treats optional land sacrifice from Excavating Anurid as pending by default and applies it when forced', () => {
    const ir = parseOracleTextToIR('When this creature enters, you may sacrifice a land.', 'Excavating Anurid');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'p1land',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'l1', name: 'Forest', type_line: 'Basic Land — Forest' },
        },
      ] as any,
    });

    const skipped = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1Skipped = skipped.state.players.find(p => p.id === 'p1') as any;
    expect(skipped.pendingOptionalSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(skipped.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(skipped.state.battlefield).toHaveLength(1);
    expect(p1Skipped.graveyard).toHaveLength(0);

    const forced = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' }, { allowOptional: true });
    const p1Forced = forced.state.players.find(p => p.id === 'p1') as any;
    expect(forced.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(forced.state.battlefield).toHaveLength(0);
    expect(p1Forced.graveyard).toHaveLength(1);
    expect(String(p1Forced.graveyard[0]?.name || p1Forced.graveyard[0]?.card?.name || '')).toBe('Forest');
  });

  it("applies optional permanent sacrifice from Baldur's Gate Wilderness when forced", () => {
    const ir = parseOracleTextToIR('You may sacrifice a permanent.', "Baldur's Gate Wilderness");
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'p1artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'a1', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const forced = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' }, { allowOptional: true });
    const p1 = forced.state.players.find(p => p.id === 'p1') as any;

    expect(forced.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(forced.state.battlefield).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(String(p1.graveyard[0]?.name || p1.graveyard[0]?.card?.name || '')).toBe('Mind Stone');
  });

  it('applies deterministic self-sacrifice for corpus-style "sacrifice this creature" text when source is known', () => {
    const ir = parseOracleTextToIR('At the beginning of the end step, sacrifice this creature.', 'Impetuous Devils');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'self-sac-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'self-sac-card', name: 'Impetuous Devils', type_line: 'Creature — Devil' },
        },
        {
          id: 'other-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'other-card', name: 'Hill Giant', type_line: 'Creature — Giant' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'self-sac-creature',
      sourceName: 'Impetuous Devils',
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(names).toContain('Hill Giant');
    expect(names).not.toContain('Impetuous Devils');
    expect(String(p1.graveyard[0]?.name || p1.graveyard[0]?.card?.name || '')).toBe('Impetuous Devils');
  });

  it('keeps self-sacrifice with prevention text as a persisted skip instead of forcing it', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your upkeep, sacrifice this creature unless you pay {U}.',
      'Phantasmal Forces'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'forces-self',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'forces-card', name: 'Phantasmal Forces', type_line: 'Creature — Illusion' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'forces-self',
      sourceName: 'Phantasmal Forces',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
    expect(((result.state as any).oracleAutomationGaps || []).length).toBeGreaterThan(0);
    expect(result.state.battlefield).toHaveLength(1);
  });

  it('applies contextual sacrifice for "that creature" via target permanent binding', () => {
    const ir = parseOracleTextToIR('Sacrifice that creature.', 'Context Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'context-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'context-creature-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
        {
          id: 'safe-artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetPermanentId: 'context-creature',
    });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Mind Stone');
    expect(names).not.toContain('Runeclaw Bear');
  });

  it('applies contextual sacrifice for "that permanent" via target permanent binding', () => {
    const ir = parseOracleTextToIR('Sacrifice that permanent.', 'Context Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'context-permanent',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'context-permanent-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
        {
          id: 'safe-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-creature-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetPermanentId: 'context-permanent',
    });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Bear Cub');
    expect(names).not.toContain('Mind Stone');
  });

  it('applies contextual sacrifice for "the token" via chosen object binding', () => {
    const ir = parseOracleTextToIR('Sacrifice the token.', 'Context Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'context-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'context-token-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'safe-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'safe-token-card', name: 'Treasure', type_line: 'Artifact - Treasure' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { chosenObjectIds: ['context-token'] } as any,
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['safe-token']);
  });

  it('applies contextual sacrifice for "those creatures" via chosen object binding', () => {
    const ir = parseOracleTextToIR('Sacrifice those creatures.', 'Context Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'context-creature-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'context-creature-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
        {
          id: 'context-creature-b',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'context-creature-b-card', name: 'Goblin Piker', type_line: 'Creature - Goblin Warrior' },
        },
        {
          id: 'safe-artifact-perm',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-perm-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { chosenObjectIds: ['context-creature-a', 'context-creature-b'] } as any,
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['safe-artifact-perm']);
  });

  it('skips contextual sacrifice for "that creature" when no binding is available', () => {
    const ir = parseOracleTextToIR('Sacrifice that creature.', 'Context Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'unbound-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'unbound-creature-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'no_deterministic_target')).toBe(true);
  });

  it('applies sacrifice for "enchanted creature" using the source attachment binding', () => {
    const ir = parseOracleTextToIR('Sacrifice enchanted creature.', 'Cocoon');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'cocoon-aura',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'enchanted-bear',
          card: { id: 'cocoon-card', name: 'Cocoon', type_line: 'Enchantment - Aura' },
        },
        {
          id: 'enchanted-bear',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'enchanted-bear-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
        {
          id: 'safe-artifact-after-aura',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-after-aura-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cocoon-aura',
      sourceName: 'Cocoon',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['cocoon-aura', 'safe-artifact-after-aura']);
  });

  it('applies sacrifice for a named attached Equipment when there is one deterministic match', () => {
    const ir = parseOracleTextToIR('Sacrifice an Equipment attached to Outfitted Jouster.', 'Outfitted Jouster');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'outfitted-jouster',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'outfitted-jouster-card', name: 'Outfitted Jouster', type_line: 'Creature - Human Knight' },
        },
        {
          id: 'short-sword',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'outfitted-jouster',
          card: { id: 'short-sword-card', name: 'Short Sword', type_line: 'Artifact - Equipment' },
        },
        {
          id: 'safe-aura-after-equipment',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'outfitted-jouster',
          card: { id: 'safe-aura-after-equipment-card', name: 'Sentinel Eyes', type_line: 'Enchantment - Aura' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster',
      sourceName: 'Outfitted Jouster',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['outfitted-jouster', 'safe-aura-after-equipment']);
  });

  it('puts a targeted Aura from a graveyard onto the battlefield attached to this creature', () => {
    const ir = parseOracleTextToIR(
      'Put target Aura card from a graveyard onto the battlefield under your control attached to this creature.',
      'Iridescent Drake'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'dead-aura',
              name: 'Flight of Fancy',
              type_line: 'Enchantment - Aura',
              oracle_text: 'Enchant creature\nWhen Flight of Fancy enters, draw two cards.',
            },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'iridescent-drake',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          card: { id: 'iridescent-drake-card', name: 'Iridescent Drake', type_line: 'Creature - Drake' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'iridescent-drake',
      sourceName: 'Iridescent Drake',
      targetPermanentId: 'dead-aura',
    });

    const aura = (result.state.battlefield as any[]).find(
      perm => perm?.card?.id === 'dead-aura' || perm?.card?.name === 'Flight of Fancy'
    ) as any;
    const drake = (result.state.battlefield as any[]).find(perm => perm?.id === 'iridescent-drake') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(aura).toBeTruthy();
    expect(aura.controller).toBe('p1');
    expect(aura.owner).toBe('p2');
    expect(aura.attachedTo).toBe('iridescent-drake');
    expect(drake.attachments).toContain(aura.id);
    expect(p2.graveyard).toHaveLength(0);
  });

  it('puts a targeted Aura from a graveyard onto the battlefield attached to your only creature', () => {
    const ir = parseOracleTextToIR(
      'Put target Aura card from a graveyard onto the battlefield under your control attached to a creature you control.',
      'Nomad Mythmaker'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'dead-ward',
              name: 'Hyena Umbra',
              type_line: 'Enchantment - Aura',
              oracle_text: 'Enchant creature\nEnchanted creature gets +1/+1 and has first strike.',
            },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'lone-bear',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          card: { id: 'lone-bear-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'nomad-mythmaker',
      sourceName: 'Nomad Mythmaker',
      targetPermanentId: 'dead-ward',
    });

    const aura = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'dead-ward') as any;
    const bear = (result.state.battlefield as any[]).find(perm => perm?.id === 'lone-bear') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(aura).toBeTruthy();
    expect(aura.attachedTo).toBe('lone-bear');
    expect(bear.attachments).toContain(aura.id);
  });

  it('classifies Aura reanimation attached to a creature you control as player choice when multiple creatures fit', () => {
    const ir = parseOracleTextToIR(
      'Put target Aura card from a graveyard onto the battlefield under your control attached to a creature you control.',
      'Nomad Mythmaker'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'choice-aura',
              name: 'Spirit Mantle',
              type_line: 'Enchantment - Aura',
              oracle_text: 'Enchant creature\nEnchanted creature gets +1/+1 and has protection from creatures.',
            },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'choice-bear-a',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          card: { id: 'choice-bear-a-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
        {
          id: 'choice-bear-b',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          card: { id: 'choice-bear-b-card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'nomad-mythmaker',
      sourceName: 'Nomad Mythmaker',
      targetPermanentId: 'choice-aura',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(p2.graveyard).toHaveLength(1);
    expect((result.state.battlefield as any[]).some(perm => perm?.card?.id === 'choice-aura')).toBe(false);
  });

  it('returns a creature with Necromancy and then attaches the source enchantment to it', () => {
    const ir = parseOracleTextToIR(
      'When this enchantment enters, if it\'s on the battlefield, it becomes an Aura with "enchant creature put onto the battlefield with Necromancy." Put target creature card from a graveyard onto the battlefield under your control and attach this enchantment to it.',
      'Necromancy'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'dead-demon',
              name: 'Abyssal Specter',
              type_line: 'Creature - Specter',
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'necromancy-source',
          controller: 'p1',
          owner: 'p1',
          attachments: [],
          card: {
            id: 'necromancy-card',
            name: 'Necromancy',
            type_line: 'Enchantment',
          },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'necromancy-source',
      sourceName: 'Necromancy',
      targetCreatureId: 'dead-demon',
    });

    const returnedCreature = (result.state.battlefield as any[]).find(
      perm => perm?.card?.id === 'dead-demon' || perm?.card?.name === 'Abyssal Specter'
    ) as any;
    const necromancy = (result.state.battlefield as any[]).find(perm => perm?.id === 'necromancy-source') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'attach')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'attach')).toBe(false);
    expect(returnedCreature).toBeTruthy();
    expect(returnedCreature.controller).toBe('p1');
    expect(necromancy.attachedTo).toBe(returnedCreature.id);
    expect(returnedCreature.attachments).toContain('necromancy-source');
    expect(p2.graveyard).toHaveLength(0);
  });

  it('applies Portal to Phyrexia-style upkeep reanimation under your control', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your upkeep, put target creature card from a graveyard onto the battlefield under your control.',
      'Portal to Phyrexia'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'portal-target', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Portal to Phyrexia',
      targetCreatureId: 'portal-target',
    });

    const permanent = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'portal-target') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
    expect(p2.graveyard).toHaveLength(0);
  });

  it('applies Junji-style non-Dragon reanimation and preserves the non-Dragon exclusion', () => {
    const ir = parseOracleTextToIR(
      '• Put target non-Dragon creature card from a graveyard onto the battlefield under your control.',
      'Junji, the Midnight Sky'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const successStart = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'junji-legal', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const successResult = applyOracleIRStepsToGameState(successStart, steps, {
      controllerId: 'p1',
      sourceName: 'Junji, the Midnight Sky',
      targetCreatureId: 'junji-legal',
    });

    const skippedStart = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'junji-dragon', name: 'Shivan Dragon', type_line: 'Creature - Dragon' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const skippedResult = applyOracleIRStepsToGameState(skippedStart, steps, {
      controllerId: 'p1',
      sourceName: 'Junji, the Midnight Sky',
      targetCreatureId: 'junji-dragon',
    });
    const skippedP2 = skippedResult.state.players.find(p => p.id === 'p2') as any;

    expect(successResult.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect((successResult.state.battlefield as any[]).some(perm => perm?.card?.id === 'junji-legal')).toBe(true);
    expect(skippedResult.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(skippedResult.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(skippedP2.graveyard.map((card: any) => card.id)).toEqual(['junji-dragon']);
  });

  it('applies Beacon of Unrest-style artifact recursion under your control', () => {
    const ir = parseOracleTextToIR(
      'Put target artifact or creature card from a graveyard onto the battlefield under your control.',
      'Beacon of Unrest'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'beacon-target', name: 'Worn Powerstone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Beacon of Unrest',
      targetPermanentId: 'beacon-target',
    });

    const permanent = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'beacon-target') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
  });

  it('creates a Liliana-style emblem from quoted emblem text', () => {
    const ir = parseOracleTextToIR(
      '−7: You get an emblem with "At the beginning of combat on your turn, put target creature card from a graveyard onto the battlefield under your control."',
      'Liliana, Waker of the Dead'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
          emblems: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'liliana-waker',
      sourceName: 'Liliana, Waker of the Dead',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const emblem = Array.isArray(p1?.emblems) ? p1.emblems[0] : null;

    expect(result.appliedSteps.some(s => s.kind === 'create_emblem')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'create_emblem')).toBe(false);
    expect(p1.emblems).toHaveLength(1);
    expect(emblem?.name).toBe('Liliana, Waker of the Dead Emblem');
    expect(emblem?.createdBy).toBe('Liliana, Waker of the Dead');
    expect(emblem?.sourceId).toBe('liliana-waker');
    expect(emblem?.abilities).toEqual([
      'At the beginning of combat on your turn, put target creature card from a graveyard onto the battlefield under your control.',
    ]);
  });

  it.each([
    {
      name: 'The Cruelty of Gix',
      text: 'III â€” Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'cruelty-target', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
    },
    {
      name: 'Vat Emergence',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'vat-target', name: 'Baleful Strix', type_line: 'Artifact Creature - Bird' },
    },
    {
      name: 'Chainer, Dementia Master',
      text: '{B}{B}{B}, Pay 3 life: Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'chainer-target', name: 'Phyrexian Rager', type_line: 'Creature - Horror' },
    },
    {
      name: 'Too Greedily, Too Deep',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'greed-target', name: 'Sakura-Tribe Elder', type_line: 'Creature - Snake Shaman' },
    },
    {
      name: "Debtors' Knell",
      text: 'At the beginning of your upkeep, put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'knell-target', name: 'Sun Titan', type_line: 'Creature - Giant' },
    },
    {
      name: 'Gravespawn Sovereign',
      text: 'Tap five untapped Zombies you control: Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'sovereign-target', name: 'Diregraf Colossus', type_line: 'Creature - Zombie Giant' },
    },
    {
      name: 'Demon of Dark Schemes',
      text: '{2}{B}, Pay {E}{E}{E}{E}: Put target creature card from a graveyard onto the battlefield under your control tapped.',
      targetKey: 'targetCreatureId',
      card: { id: 'demon-target', name: 'Burnished Hart', type_line: 'Artifact Creature - Elk' },
      tapped: true,
    },
    {
      name: 'Quest for the Necropolis',
      text: '{5}{B}, Sacrifice this enchantment: Put target creature card from a graveyard onto the battlefield under your control.',
      targetKey: 'targetCreatureId',
      card: { id: 'quest-target', name: 'Kokusho, the Evening Star', type_line: 'Creature - Dragon Spirit' },
    },
  ])('applies queue-backed graveyard recursion for $name', ({ name, text, targetKey, card, tapped }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: name === 'Veteran Survivor'
        ? [
            {
              id: 'veteran-survivor',
              controller: 'p1',
              owner: 'p1',
              tapped: true,
              card: { id: 'veteran-survivor-card', name: 'Veteran Survivor', type_line: 'Creature - Human Survivor' },
            } as any,
          ]
        : [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: name,
      [targetKey]: card.id,
    } as any);

    const permanent = (result.state.battlefield as any[]).find(perm => perm?.card?.id === card.id) as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
    expect(p2.graveyard).toHaveLength(0);
    if (tapped) {
      expect(permanent.tapped).toBe(true);
    }
  });

  it.each([
    {
      name: 'Fated Return',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      zone: 'opponent_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'fated-target', name: 'Massacre Wurm', type_line: 'Creature - Wurm' },
    },
    {
      name: 'Rise from the Grave',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      zone: 'opponent_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'rise-target', name: 'Acidic Slime', type_line: 'Creature - Ooze' },
    },
    {
      name: 'Ashiok, Sculptor of Fears',
      text: '−5: Put target creature card from a graveyard onto the battlefield under your control.',
      zone: 'opponent_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'ashiok-target', name: 'Mulldrifter', type_line: 'Creature - Elemental' },
    },
    {
      name: 'Grave Upheaval',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      zone: 'opponent_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'upheaval-target', name: 'Ravenous Chupacabra', type_line: 'Creature - Beast Horror' },
    },
    {
      name: 'Endless Obedience',
      text: 'Put target creature card from a graveyard onto the battlefield under your control.',
      zone: 'opponent_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'obedience-target', name: 'Gray Merchant of Asphodel', type_line: 'Creature - Zombie' },
    },
    {
      name: 'Sheoldred, Whispering One',
      text: 'At the beginning of your upkeep, return target creature card from your graveyard to the battlefield.',
      zone: 'your_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'sheoldred-target', name: 'Shriekmaw', type_line: 'Creature - Elemental' },
    },
    {
      name: 'Dread Return',
      text: 'Return target creature card from your graveyard to the battlefield.',
      zone: 'your_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'dread-target', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' },
    },
    {
      name: 'Unburial Rites',
      text: 'Return target creature card from your graveyard to the battlefield.',
      zone: 'your_graveyard',
      targetKey: 'targetCreatureId',
      card: { id: 'rites-target', name: 'Rune-Scarred Demon', type_line: 'Creature - Demon' },
    },
  ])('applies queue-backed direct reanimation for $name', ({ name, text, zone, targetKey, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: zone === 'your_graveyard' ? [card] : [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: zone === 'opponent_graveyard' ? [card] : [],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: name,
      [targetKey]: card.id,
    } as any);

    const permanent = (result.state.battlefield as any[]).find((perm: any) => perm?.card?.id === card.id) as any;
    const sourcePlayer = result.state.players.find(p => p.id === (zone === 'your_graveyard' ? 'p1' : 'p2')) as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
    expect(sourcePlayer.graveyard).toHaveLength(0);
  });

  it('applies Phyrexian Delver-style self-graveyard return plus mana-value life loss', () => {
    const ir = parseOracleTextToIR(
      "When Phyrexian Delver enters, return target creature card from your graveyard to the battlefield. You lose life equal to that card's mana value.",
      'Phyrexian Delver'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'delver-target', name: 'Grave Titan', type_line: 'Creature - Giant', mana_value: 6 }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Phyrexian Delver',
      targetCreatureId: 'delver-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const permanent = (result.state.battlefield as any[]).find((perm: any) => perm?.card?.id === 'delver-target') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(p1.life).toBe(34);
    expect(p1.graveyard).toHaveLength(0);
  });

  it.each([
    {
      name: 'Naya Charm',
      text: "Return target card from a graveyard to its owner's hand.",
      card: { id: 'naya-target', name: 'Lightning Bolt', type_line: 'Instant' },
    },
    {
      name: 'Pulse of Murasa',
      text: "Return target creature or land card from a graveyard to its owner's hand.",
      card: { id: 'pulse-target', name: 'Eternal Witness', type_line: 'Creature - Human Shaman' },
    },
    {
      name: 'Revive the Fallen',
      text: "Return target creature card from a graveyard to its owner's hand.",
      card: { id: 'revive-target', name: 'Wall of Blossoms', type_line: 'Creature - Plant Wall' },
    },
    {
      name: 'Mine Excavation',
      text: "Return target artifact or enchantment card from a graveyard to its owner's hand.",
      card: { id: 'excavation-target', name: 'Seal of Cleansing', type_line: 'Enchantment' },
    },
    {
      name: 'Endbringer\'s Revel',
      text: "Return target creature card from a graveyard to its owner's hand.",
      card: { id: 'revel-target', name: 'Baleful Strix', type_line: 'Artifact Creature - Bird' },
    },
    {
      name: 'Disturbing Plot',
      text: "Return target creature card from a graveyard to its owner's hand.",
      card: { id: 'plot-target', name: 'Satyr Wayfinder', type_line: 'Creature - Satyr' },
    },
  ])('applies queue-backed graveyard-to-hand recursion for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'veteran-survivor',
          controller: 'p1',
          owner: 'p1',
          tapped: true,
          card: { id: 'veteran-survivor-card', name: 'Veteran Survivor', type_line: 'Creature - Human Survivor' },
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'veteran-survivor',
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect(p2.hand.map((entry: any) => entry.id)).toContain(card.id);
  });

  it.each([
    {
      name: 'Buried Ruin',
      text: 'Return target artifact card from your graveyard to your hand.',
      card: { id: 'buried-target', name: 'Mind Stone', type_line: 'Artifact' },
    },
    {
      name: 'Phyrexian Reclamation',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'reclamation-target', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' },
    },
    {
      name: 'Archaeomancer',
      text: 'Return target instant or sorcery card from your graveyard to your hand.',
      card: { id: 'archaeomancer-target', name: 'Counterspell', type_line: 'Instant' },
    },
    {
      name: 'Evolution Witness',
      text: 'Return target permanent card from your graveyard to your hand.',
      card: { id: 'evolution-target', name: 'Pernicious Deed', type_line: 'Enchantment' },
    },
    {
      name: 'Haven of the Spirit Dragon',
      text: 'Return target Dragon creature card or Ugin planeswalker card from your graveyard to your hand.',
      card: { id: 'haven-target', name: 'Thundermaw Hellkite', type_line: 'Creature - Dragon' },
    },
    {
      name: 'Timeless Witness',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'timeless-target', name: 'Kodama\'s Reach', type_line: 'Sorcery' },
    },
    {
      name: 'Regrowth',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'regrowth-target', name: 'Swords to Plowshares', type_line: 'Instant' },
    },
    {
      name: 'Honest Rutstein',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'rutstein-target', name: 'Llanowar Elves', type_line: 'Creature - Elf Druid' },
    },
    {
      name: 'Tortured Existence',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'tortured-target', name: 'Reclamation Sage', type_line: 'Creature - Elf Shaman' },
    },
    {
      name: 'Nyx Weaver',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'nyx-target', name: 'Beast Within', type_line: 'Instant' },
    },
    {
      name: 'Trading Post',
      text: 'Return target artifact card from your graveyard to your hand.',
      card: { id: 'trading-post-target', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' },
    },
    {
      name: 'Codex Shredder',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'codex-target', name: 'Heroic Intervention', type_line: 'Instant' },
    },
    {
      name: 'Memorial to Folly',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'memorial-target', name: 'Yavimaya Elder', type_line: 'Creature - Human Druid' },
    },
    {
      name: 'Tezzeret, Master of the Bridge',
      text: 'Return target artifact card from your graveyard to your hand.',
      card: { id: 'tezzeret-target', name: 'Chromatic Lantern', type_line: 'Artifact' },
    },
    {
      name: "Stormchaser's Talent",
      text: 'Return target instant or sorcery card from your graveyard to your hand.',
      card: { id: 'stormchaser-target', name: 'Expressive Iteration', type_line: 'Sorcery' },
    },
    {
      name: "Kolaghan's Command",
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'kolaghan-target', name: 'Goblin Engineer', type_line: 'Creature - Goblin Artificer' },
    },
    {
      name: 'Trailtracker Scout',
      text: 'Return up to one target permanent card from your graveyard to your hand.',
      card: { id: 'trailtracker-target', name: 'Mirari\'s Wake', type_line: 'Enchantment' },
    },
    {
      name: 'Colossal Skyturtle',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'skyturtle-target', name: 'Heroic Intervention', type_line: 'Instant' },
    },
    {
      name: 'Scour for Scrap',
      text: 'Return target artifact card from your graveyard to your hand.',
      card: { id: 'scour-target', name: 'Mishra\'s Bauble', type_line: 'Artifact' },
    },
    {
      name: 'Nissa, Vital Force',
      text: 'Return target permanent card from your graveyard to your hand.',
      card: { id: 'nissa-target', name: 'The Great Henge', type_line: 'Legendary Artifact' },
    },
    {
      name: 'Palace Siege',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'siege-target', name: 'Plaguecrafter', type_line: 'Creature - Human Shaman' },
    },
    {
      name: 'Mystic Retrieval',
      text: 'Return target instant or sorcery card from your graveyard to your hand.',
      card: { id: 'retrieval-target', name: 'Lightning Bolt', type_line: 'Instant' },
    },
    {
      name: "Hanna, Ship's Navigator",
      text: 'Return target artifact or enchantment card from your graveyard to your hand.',
      card: { id: 'hanna-target', name: 'Seal of Cleansing', type_line: 'Enchantment' },
    },
    {
      name: 'Cavalier of Dawn',
      text: 'Return target artifact or enchantment card from your graveyard to your hand.',
      card: { id: 'cavalier-target', name: 'Mind Stone', type_line: 'Artifact' },
    },
    {
      name: 'Auroral Procession',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'auroral-target', name: 'Negate', type_line: 'Instant' },
    },
    {
      name: 'The Underworld Cookbook',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'cookbook-target', name: 'Cauldron Familiar', type_line: 'Creature - Cat' },
    },
    {
      name: 'Den Protector',
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'den-protector-target', name: 'Beast Within', type_line: 'Instant' },
    },
    {
      name: 'Wrenn and Six',
      text: 'Return up to one target land card from your graveyard to your hand.',
      card: { id: 'wrenn-target', name: 'Wooded Foothills', type_line: 'Land' },
    },
    {
      name: 'Cormela, Glamour Thief',
      text: 'Return up to one target instant or sorcery card from your graveyard to your hand.',
      card: { id: 'cormela-target', name: 'Ponder', type_line: 'Sorcery' },
    },
    {
      name: 'Gloomshrieker',
      text: 'Return target permanent card from your graveyard to your hand.',
      card: { id: 'gloomshrieker-target', name: 'Sylvan Library', type_line: 'Enchantment' },
    },
    {
      name: 'Turntimber Sower',
      text: 'Return target land card from your graveyard to your hand.',
      card: { id: 'turntimber-target', name: 'Blast Zone', type_line: 'Land' },
    },
    {
      name: "Dryad's Revival",
      text: 'Return target card from your graveyard to your hand.',
      card: { id: 'dryad-target', name: 'Nature\'s Lore', type_line: 'Sorcery' },
    },
    {
      name: 'Flood of Recollection',
      text: 'Return target instant or sorcery card from your graveyard to your hand.',
      card: { id: 'flood-target', name: 'Ponder', type_line: 'Sorcery' },
    },
    {
      name: 'Evolution Charm',
      text: 'Return target creature card from your graveyard to your hand.',
      card: { id: 'evolution-charm-target', name: 'Elvish Visionary', type_line: 'Creature - Elf Shaman' },
    },
    {
      name: 'Spring-Leaf Avenger',
      text: 'Return target permanent card from your graveyard to your hand.',
      card: { id: 'spring-leaf-target', name: 'Lightning Greaves', type_line: 'Artifact - Equipment' },
    },
  ])('applies queue-backed self graveyard-to-hand recursion for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: name === 'Veteran Survivor'
        ? [
            {
              id: 'veteran-survivor',
              controller: 'p1',
              owner: 'p1',
              tapped: true,
              card: { id: 'veteran-survivor-card', name: 'Veteran Survivor', type_line: 'Creature - Human Survivor' },
            } as any,
          ]
        : [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      ...(name === 'Veteran Survivor' ? { sourceId: 'veteran-survivor' } : {}),
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain(card.id);
  });

  it.each([
    {
      name: 'Samwise Gamgee',
      text: 'Return target historic card from your graveyard to your hand.',
      card: { id: 'samwise-target', name: "Mox Amber", type_line: 'Legendary Artifact' },
    },
    {
      name: 'Mudflat Village',
      text: 'Return target Bat, Lizard, Rat, or Squirrel card from your graveyard to your hand.',
      card: { id: 'mudflat-target', name: 'Lord Skitter, Sewer King', type_line: 'Legendary Creature - Rat Noble' },
    },
    {
      name: 'Lord of the Undead',
      text: 'Return target Zombie card from your graveyard to your hand.',
      card: { id: 'lord-target', name: 'Mire Triton', type_line: 'Creature - Zombie Merfolk' },
    },
    {
      name: 'Cid, Freeflier Pilot',
      text: 'Return target Equipment or Vehicle card from your graveyard to your hand.',
      card: { id: 'cid-target', name: 'Esika\'s Chariot', type_line: 'Legendary Artifact - Vehicle' },
    },
    {
      name: 'Atzocan Seer',
      text: 'Return target Dinosaur card from your graveyard to your hand.',
      card: { id: 'atzocan-target', name: 'Quartzwood Crasher', type_line: 'Creature - Dinosaur Beast' },
    },
    {
      name: 'Satsuki, the Living Lore',
      text: 'Return target Saga card from your graveyard to your hand.',
      card: { id: 'satsuki-target', name: 'The Restoration of Eiganjo', type_line: 'Enchantment - Saga' },
    },
    {
      name: 'Red XIII, Proud Warrior',
      text: 'Return target Aura or Equipment card from your graveyard to your hand.',
      card: { id: 'red-xiii-target', name: 'Rancor', type_line: 'Enchantment - Aura' },
    },
    {
      name: 'Call to the Netherworld',
      text: 'Return target black creature card from your graveyard to your hand.',
      card: {
        id: 'netherworld-target',
        name: 'Graveyard Trespasser',
        type_line: 'Creature - Human Werewolf',
        colors: ['B'],
      },
    },
    {
      name: 'Layla Hassan',
      text: 'Return target historic card from your graveyard to your hand.',
      card: { id: 'layla-target', name: 'Weatherlight', type_line: 'Legendary Artifact - Vehicle' },
    },
    {
      name: 'Ruxa, Patient Professor',
      text: 'Return target creature card with no abilities from your graveyard to your hand.',
      card: {
        id: 'ruxa-target',
        name: 'Gigantosaurus Cub',
        type_line: 'Creature - Dinosaur',
        oracle_text: '',
        keywords: [],
      },
    },
    {
      name: 'Elena, Turk Recruit',
      text: 'Return target non-Assassin historic card from your graveyard to your hand.',
      card: { id: 'elena-target', name: 'Teshar, Ancestor\'s Apostle', type_line: 'Legendary Creature - Bird Cleric' },
    },
  ])('applies expanded graveyard selector recursion for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain(card.id);
  });

  it('rejects Assassin historic targets for Elena, Turk Recruit', () => {
    const ir = parseOracleTextToIR(
      'Return target non-Assassin historic card from your graveyard to your hand.',
      'Elena, Turk Recruit'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'elena-assassin-target',
              name: 'Ezio Auditore da Firenze',
              type_line: 'Legendary Creature - Human Assassin',
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Elena, Turk Recruit',
      targetPermanentId: 'elena-assassin-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(p1.graveyard.map((entry: any) => entry.id)).toContain('elena-assassin-target');
    expect(p1.hand).toHaveLength(0);
  });

  it('applies life gain plus optional graveyard return for Sword of Light and Shadow when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'Whenever equipped creature deals combat damage to a player, you gain 3 life and you may return up to one target creature card from your graveyard to your hand.',
      'Sword of Light and Shadow'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 37,
          library: [],
          hand: [],
          graveyard: [{ id: 'sword-target', name: 'Wall of Omens', type_line: 'Creature - Wall' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Sword of Light and Shadow',
        targetPermanentId: 'sword-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['gain_life', 'move_zone']);
    expect(p1.life).toBe(40);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('sword-target');
    expect(p1.graveyard).toHaveLength(0);
  });

  it('applies graveyard return plus graveyard exile sweep for Abstergo Entertainment', () => {
    const ir = parseOracleTextToIR(
      'Return up to one target historic card from your graveyard to your hand, then exile all graveyards.',
      'Abstergo Entertainment'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'abstergo-target', name: 'Mox Amber', type_line: 'Legendary Artifact' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'abstergo-opp-target', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Abstergo Entertainment',
      targetPermanentId: 'abstergo-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'move_zone']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('abstergo-target');
    expect(p1.graveyard).toHaveLength(0);
    expect((p1.exile || []).map((entry: any) => entry.id)).not.toContain('abstergo-target');
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('abstergo-opp-target');
  });

  it('applies graveyard return plus token creation for Spawnbed Protector', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your end step, return up to one target Eldrazi creature card from your graveyard to your hand. Create two 1/1 colorless Eldrazi Scion creature tokens with "Sacrifice this token: Add {C}."',
      'Spawnbed Protector'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'spawnbed-target', name: 'Bane of Bala Ged', type_line: 'Creature - Eldrazi' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Spawnbed Protector',
      targetPermanentId: 'spawnbed-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'create_token']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('spawnbed-target');
    expect(p1.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(2);
  });

  it('applies graveyard return plus Food token creation for Revive the Shire', () => {
    const ir = parseOracleTextToIR(
      'Return target permanent card from your graveyard to your hand. Create a Food token.',
      'Revive the Shire'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'shire-target', name: 'Sylvan Library', type_line: 'Enchantment' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Revive the Shire',
      targetPermanentId: 'shire-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'create_token']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('shire-target');
    expect(p1.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
  });

  it('applies graveyard return plus Clue token creation for True Ancestry', () => {
    const ir = parseOracleTextToIR(
      'Return up to one target permanent card from your graveyard to your hand. Create a Clue token.',
      'True Ancestry'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'ancestry-target', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'True Ancestry',
      targetPermanentId: 'ancestry-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'create_token']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('ancestry-target');
    expect(p1.graveyard).toHaveLength(0);
    expect(result.state.battlefield).toHaveLength(1);
  });

  it('applies your graveyard return even when Witch of the Moors sacrifice choice remains manual', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your end step, if you gained life this turn, each opponent sacrifices a creature of their choice and you return up to one target creature card from your graveyard to your hand.',
      'Witch of the Moors'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'witch-target', name: 'Plaguecrafter', type_line: 'Creature - Human Shaman' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'opp-creature-a',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'opp-creature-a-card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
        } as any,
        {
          id: 'opp-creature-b',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'opp-creature-b-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        } as any,
      ],
      lifeGainedThisTurn: { p1: 1 },
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Witch of the Moors',
      targetPermanentId: 'witch-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('witch-target');
    expect(p1.graveyard).toHaveLength(0);
  });

  it('applies Unnatural Restoration when proliferate has exactly one eligible target', () => {
    const ir = parseOracleTextToIR(
      'Return target permanent card from your graveyard to your hand. Proliferate.',
      'Unnatural Restoration'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'restoration-target', name: 'Evolution Sage', type_line: 'Creature - Elf Druid' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'scales-creature',
          controller: 'p1',
          owner: 'p1',
          counters: { '+1/+1': 1 },
          card: { id: 'scales-creature-card', name: 'Conclave Mentor', type_line: 'Creature - Centaur Cleric' },
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Unnatural Restoration',
      targetPermanentId: 'restoration-target',
    });

    const p1 = result.state.players.find((player) => player.id === 'p1') as any;
    const proliferated = (result.state.battlefield as any[]).find((permanent) => permanent?.id === 'scales-creature') as any;

    expect(result.appliedSteps.map((step) => step.kind)).toEqual(['move_zone', 'proliferate']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('restoration-target');
    expect(p1.graveyard).toHaveLength(0);
    expect(proliferated?.counters?.['+1/+1']).toBe(2);
  });

  it("applies Sam's Desperate Rescue when Ring-bearer choice is deterministic", () => {
    const ir = parseOracleTextToIR(
      'Return target creature card from your graveyard to your hand. The Ring tempts you.',
      "Sam's Desperate Rescue"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'sams-target', name: 'Farhaven Elf', type_line: 'Creature - Elf Druid' }],
          exile: [],
          emblems: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'ring-bearer-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'ring-bearer-creature-card', name: 'Samwise Gamgee', type_line: 'Legendary Creature - Halfling Peasant' },
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: "Sam's Desperate Rescue",
      targetPermanentId: 'sams-target',
    });

    const p1 = result.state.players.find((player) => player.id === 'p1') as any;
    const emblem = Array.isArray(p1?.emblems) ? p1.emblems.find((entry: any) => entry?.name === 'The Ring') : null;
    const ringBearer = (result.state.battlefield as any[]).find((permanent) => permanent?.id === 'ring-bearer-creature') as any;

    expect(result.appliedSteps.map((step) => step.kind)).toEqual(['move_zone', 'ring_tempts_you']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('sams-target');
    expect(p1.graveyard).toHaveLength(0);
    expect(emblem?.abilities).toEqual([
      "Your Ring-bearer is legendary and can't be blocked by creatures with greater power.",
    ]);
    expect(p1.ringTemptations).toBe(1);
    expect(p1.ringBearerId).toBe('ring-bearer-creature');
    expect(ringBearer?.isRingBearer).toBe(true);
  });

  it('applies Fungal Rebirth token followup when a creature died this turn', () => {
    const ir = parseOracleTextToIR(
      'Return target permanent card from your graveyard to your hand. If a creature died this turn, create two 1/1 green Saproling creature tokens.',
      'Fungal Rebirth'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creatureDiedThisTurn: true as any,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'fungal-target', name: 'Arcane Signet', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Fungal Rebirth',
      targetPermanentId: 'fungal-target',
    });

    const p1 = result.state.players.find((player) => player.id === 'p1') as any;

    expect(result.appliedSteps.map((step) => step.kind)).toEqual(['move_zone', 'create_token']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('fungal-target');
    expect(result.state.battlefield).toHaveLength(2);
  });

  it('applies Toph, Hardheaded Teacher when allowOptional enables the discard gate', () => {
    const ir = parseOracleTextToIR(
      'You may discard a card. If you do, return target instant or sorcery card from your graveyard to your hand.',
      'Toph, Hardheaded Teacher'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'discard-me', name: 'Forest', type_line: 'Land' }],
          graveyard: [{ id: 'toph-target', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Toph, Hardheaded Teacher',
        targetPermanentId: 'toph-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find((player) => player.id === 'p1') as any;

    expect(result.appliedSteps.map((step) => step.kind)).toEqual(['discard', 'move_zone']);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('toph-target');
    expect(p1.graveyard).toHaveLength(1);
    expect(p1.graveyard.map((entry: any) => entry.id)).toContain('discard-me');
  });

  it('applies optional graveyard exile for Blood Operative when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may exile target card from a graveyard.',
      'Blood Operative'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'blood-operative-target', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Blood Operative',
        targetPermanentId: 'blood-operative-target',
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('blood-operative-target');
  });

  it('applies optional self graveyard-to-hand recursion for Oversold Cemetery when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your upkeep, if you have four or more creature cards in your graveyard, you may return target creature card from your graveyard to your hand.',
      'Oversold Cemetery'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'oversold-target', name: 'Wall of Blossoms', type_line: 'Creature - Plant Wall' },
            { id: 'oversold-two', name: 'Llanowar Elves', type_line: 'Creature - Elf Druid' },
            { id: 'oversold-three', name: 'Eternal Witness', type_line: 'Creature - Human Shaman' },
            { id: 'oversold-four', name: 'Sakura-Tribe Elder', type_line: 'Creature - Snake Shaman' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Oversold Cemetery',
        targetPermanentId: 'oversold-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard.map((entry: any) => entry.id)).not.toContain('oversold-target');
    expect(p1.hand.map((entry: any) => entry.id)).toContain('oversold-target');
  });

  it('applies optional self graveyard-to-hand recursion for Graveshifter when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may return target creature card from your graveyard to your hand.',
      'Graveshifter'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'graveshifter-target', name: 'Mire Triton', type_line: 'Creature - Zombie Merfolk' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Graveshifter',
        targetPermanentId: 'graveshifter-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('graveshifter-target');
  });

  it('applies optional self graveyard-to-hand recursion for Auramancer when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may return target enchantment card from your graveyard to your hand.',
      'Auramancer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'auramancer-target', name: 'Seal of Primordium', type_line: 'Enchantment' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Auramancer',
        targetPermanentId: 'auramancer-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('auramancer-target');
  });

  it('applies optional self graveyard-to-hand recursion for Greenwarden of Murasa when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may return target card from your graveyard to your hand.',
      'Greenwarden of Murasa'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'greenwarden-target', name: 'Cultivate', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Greenwarden of Murasa',
        targetPermanentId: 'greenwarden-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('greenwarden-target');
  });

  it('applies optional self graveyard-to-hand recursion for Mnemonic Wall when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may return target instant or sorcery card from your graveyard to your hand.',
      'Mnemonic Wall'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'mnemonic-wall-target', name: 'Preordain', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Mnemonic Wall',
        targetPermanentId: 'mnemonic-wall-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.hand.map((entry: any) => entry.id)).toContain('mnemonic-wall-target');
  });

  it.each([
    {
      name: 'Noxious Revival',
      text: "Put target card from a graveyard on top of its owner's library.",
      placement: 'top',
      card: { id: 'revival-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Junktroller',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      placement: 'bottom',
      card: { id: 'junktroller-target', name: 'Mortify', type_line: 'Instant' },
    },
    {
      name: 'Vessel of Endless Rest',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      placement: 'bottom',
      card: { id: 'vessel-target', name: 'Wrath of God', type_line: 'Sorcery' },
    },
  ])('applies queue-backed graveyard-to-library recursion for $name', ({ name, text, placement, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'lib-a' }, { id: 'lib-b' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'opp-lib-a' }, { id: 'opp-lib-b' }],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'veteran-survivor',
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    if (placement === 'top') {
      expect(p2.library[0]?.id).toBe(card.id);
    } else {
      expect(p2.library[p2.library.length - 1]?.id).toBe(card.id);
    }
  });

  it('applies self graveyard-to-library topdeck recursion for Golgari Thug', () => {
    const ir = parseOracleTextToIR(
      'Put target creature card from your graveyard on top of your library.',
      'Golgari Thug'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'library-bottom' }],
          hand: [],
          graveyard: [{ id: 'thug-target', name: 'Sakura-Tribe Elder', type_line: 'Creature - Snake Shaman' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Golgari Thug',
      targetPermanentId: 'thug-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p1.graveyard).toHaveLength(0);
    expect(p1.library[0]?.id).toBe('thug-target');
  });

  it.each([
    {
      name: 'Deathrite Shaman',
      text: 'Exile target land card from a graveyard.',
      card: { id: 'deathrite-target', name: 'Wasteland', type_line: 'Land' },
    },
    {
      name: 'Return to Nature',
      text: 'Exile target card from a graveyard.',
      card: { id: 'return-nature-target', name: 'Swords to Plowshares', type_line: 'Instant' },
    },
    {
      name: 'Soul-Guide Lantern',
      text: 'Exile target card from a graveyard.',
      card: { id: 'lantern-target', name: 'Explosive Vegetation', type_line: 'Sorcery' },
    },
    {
      name: "Agatha's Soul Cauldron",
      text: 'Exile target card from a graveyard.',
      card: { id: 'cauldron-target', name: 'Birds of Paradise', type_line: 'Creature - Bird' },
    },
    {
      name: 'Scavenging Ooze',
      text: 'Exile target card from a graveyard.',
      card: { id: 'ooze-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Lion Sash',
      text: 'Exile target card from a graveyard.',
      card: { id: 'sash-target', name: 'Flooded Strand', type_line: 'Land' },
    },
    {
      name: 'The Scarab God',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'scarab-target', name: 'Murderous Rider', type_line: 'Creature - Zombie Knight' },
    },
    {
      name: 'Cemetery Reaper',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'reaper-target', name: 'Reassembling Skeleton', type_line: 'Creature - Skeleton Warrior' },
    },
    {
      name: 'Heritage Reclamation',
      text: 'Exile up to one target card from a graveyard.',
      card: { id: 'heritage-target', name: 'Rampant Growth', type_line: 'Sorcery' },
    },
    {
      name: "General's Enforcer",
      text: 'Exile target card from a graveyard.',
      card: { id: 'enforcer-target', name: 'Path to Exile', type_line: 'Instant' },
    },
    {
      name: 'Mnemonic Deluge',
      text: 'Exile target instant or sorcery card from a graveyard.',
      card: { id: 'deluge-target', name: 'Ponder', type_line: 'Sorcery' },
    },
    {
      name: 'Ghost Vacuum',
      text: 'Exile target card from a graveyard.',
      card: { id: 'vacuum-target', name: 'Animate Dead', type_line: 'Enchantment - Aura' },
    },
    {
      name: 'Cremate',
      text: 'Exile target card from a graveyard.',
      card: { id: 'cremate-target', name: 'Wrath of God', type_line: 'Sorcery' },
    },
    {
      name: 'Myr Welder',
      text: 'Exile target artifact card from a graveyard.',
      card: { id: 'welder-target', name: 'Mind Stone', type_line: 'Artifact' },
    },
    {
      name: 'Honored Heirloom',
      text: 'Exile target card from a graveyard.',
      card: { id: 'heirloom-target', name: 'Beast Within', type_line: 'Instant' },
    },
    {
      name: 'Cling to Dust',
      text: 'Exile target card from a graveyard.',
      card: { id: 'cling-target', name: 'Arcane Signet', type_line: 'Artifact' },
    },
  ])('applies queue-backed graveyard exile for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      ...(name === 'Veteran Survivor' ? { sourceId: 'veteran-survivor' } : {}),
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain(card.id);
  });

  it.each([
    {
      name: 'Chrome Companion',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'chrome-target', name: 'Ponder', type_line: 'Sorcery' },
    },
    {
      name: 'Hoverstone Pilgrim',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'hoverstone-target', name: 'Arcane Signet', type_line: 'Artifact' },
    },
    {
      name: 'Reito Lantern',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'lantern-target', name: 'Swords to Plowshares', type_line: 'Instant' },
    },
    {
      name: 'Reito Sentinel',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'sentinel-target', name: 'Deep Analysis', type_line: 'Sorcery' },
    },
    {
      name: 'Jade-Cast Sentinel',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'jade-target', name: 'Faithless Looting', type_line: 'Sorcery' },
    },
    {
      name: 'Malevolent Chandelier',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'chandelier-target', name: 'Rakdos Signet', type_line: 'Artifact' },
    },
    {
      name: 'Dutiful Knowledge Seeker',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'dutiful-target', name: 'Impulse', type_line: 'Instant' },
    },
    {
      name: 'Phyrexian Archivist',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'archivist-target', name: 'Damn', type_line: 'Sorcery' },
    },
    {
      name: 'Cogwork Archivist',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'cogwork-target', name: 'Counterspell', type_line: 'Instant' },
    },
    {
      name: 'Nantuko Tracer',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'tracer-target', name: 'Terror', type_line: 'Instant' },
    },
    {
      name: 'Keeper of the Cadence',
      text: "Put target artifact, instant, or sorcery card from a graveyard on the bottom of its owner's library.",
      card: { id: 'keeper-target', name: 'Thoughtcast', type_line: 'Sorcery' },
    },
    {
      name: 'Grazing Kelpie',
      text: "Put target card from a graveyard on the bottom of its owner's library.",
      card: { id: 'kelpie-target', name: 'Negate', type_line: 'Instant' },
    },
  ])('applies the remaining ordered graveyard-to-library item for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'self-lib-a' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'opp-lib-a' }, { id: 'opp-lib-b' }],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: name,
      targetPermanentId: card.id,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect(p2.library[p2.library.length - 1]?.id).toBe(card.id);
  });

  it.each([
    {
      name: 'Deathrite Shaman',
      text: 'Exile target land card from a graveyard.',
      card: { id: 'deathrite-target', name: 'Windswept Heath', type_line: 'Land' },
    },
    {
      name: 'Return to Nature',
      text: 'Exile target card from a graveyard.',
      card: { id: 'return-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Soul-Guide Lantern',
      text: 'Exile target card from a graveyard.',
      card: { id: 'lantern-exile-target', name: 'Brainstorm', type_line: 'Instant' },
    },
    {
      name: "Agatha's Soul Cauldron",
      text: 'Exile target card from a graveyard.',
      card: { id: 'cauldron-target', name: 'Birds of Paradise', type_line: 'Creature - Bird' },
    },
    {
      name: 'Heritage Reclamation',
      text: 'Exile up to one target card from a graveyard.',
      card: { id: 'heritage-target', name: 'Sign in Blood', type_line: 'Sorcery' },
    },
    {
      name: "General's Enforcer",
      text: 'Exile target card from a graveyard.',
      card: { id: 'enforcer-target', name: 'Path to Exile', type_line: 'Instant' },
    },
  ])('applies the next clean graveyard-exile queue item for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: name,
      targetPermanentId: card.id,
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain(card.id);
  });

  it.each([
    {
      name: 'Scrabbling Claws',
      text: 'Exile target card from a graveyard.',
      card: { id: 'claws-target', name: 'Counterspell', type_line: 'Instant' },
    },
    {
      name: "Wizard's Spellbook",
      text: 'Exile target instant or sorcery card from a graveyard.',
      card: { id: 'spellbook-target', name: 'Dig Through Time', type_line: 'Instant' },
    },
    {
      name: 'Dawnhand Dissident',
      text: 'Exile target card from a graveyard.',
      card: { id: 'dissident-target', name: 'Kodama\'s Reach', type_line: 'Sorcery' },
    },
    {
      name: 'Pharika, God of Affliction',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'pharika-target', name: 'Elvish Mystic', type_line: 'Creature - Elf Druid' },
    },
    {
      name: 'Withered Wretch',
      text: 'Exile target card from a graveyard.',
      card: { id: 'wretch-target', name: 'Demonic Tutor', type_line: 'Sorcery' },
    },
    {
      name: 'Necrogenesis',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'necrogenesis-target', name: 'Blood Artist', type_line: 'Creature - Vampire' },
    },
    {
      name: 'Keen-Eyed Curator',
      text: 'Exile target card from a graveyard.',
      card: { id: 'curator-target', name: 'Farseek', type_line: 'Sorcery' },
    },
    {
      name: 'The Ooze',
      text: 'Exile target card from a graveyard.',
      card: { id: 'ooze-two-target', name: 'Fact or Fiction', type_line: 'Instant' },
    },
    {
      name: 'Lantern of the Lost',
      text: 'Exile target card from a graveyard.',
      card: { id: 'lost-target', name: 'Nature\'s Lore', type_line: 'Sorcery' },
    },
    {
      name: 'Heap Doll',
      text: 'Exile target card from a graveyard.',
      card: { id: 'heap-target', name: 'Chaos Warp', type_line: 'Instant' },
    },
    {
      name: 'Dimir Doppelganger',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'doppelganger-target', name: 'Baleful Mastery', type_line: 'Creature - Spirit' },
    },
    {
      name: 'Stonecloaker',
      text: 'Exile target card from a graveyard.',
      card: { id: 'stonecloaker-target', name: 'Austere Command', type_line: 'Sorcery' },
    },
    {
      name: 'Venom, Deadly Devourer',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'venom-target', name: 'Sakura-Tribe Elder', type_line: 'Creature - Snake Shaman' },
    },
    {
      name: 'Rotten Reunion',
      text: 'Exile up to one target card from a graveyard.',
      card: { id: 'rotten-target', name: 'Faithless Looting', type_line: 'Sorcery' },
    },
    {
      name: 'Break Ties',
      text: 'Exile target card from a graveyard.',
      card: { id: 'break-target', name: 'Regrowth', type_line: 'Sorcery' },
    },
    {
      name: 'Magic Pot',
      text: 'Exile target card from a graveyard.',
      card: { id: 'pot-target', name: 'Swan Song', type_line: 'Instant' },
    },
    {
      name: 'Cleanup Crew',
      text: 'Exile target card from a graveyard.',
      card: { id: 'cleanup-target', name: 'Read the Bones', type_line: 'Sorcery' },
    },
    {
      name: 'Jack-o\'-Lantern',
      text: 'Exile up to one target card from a graveyard.',
      card: { id: 'jack-target', name: 'Ponder', type_line: 'Sorcery' },
    },
    {
      name: 'Mechanical Mobster',
      text: 'When this creature enters, exile up to one target card from a graveyard.',
      card: { id: 'mobster-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Ambush Wolf',
      text: 'When this creature enters, exile up to one target card from a graveyard.',
      card: { id: 'wolf-target', name: 'Rampant Growth', type_line: 'Sorcery' },
    },
    {
      name: 'Crossroads Candleguide',
      text: 'When this creature enters, exile up to one target card from a graveyard.',
      card: { id: 'candleguide-target', name: 'Solemn Simulacrum', type_line: 'Artifact Creature - Golem' },
    },
    {
      name: 'Wreck Remover',
      text: 'Whenever this creature enters or attacks, exile up to one target card from a graveyard.',
      card: { id: 'wrecker-target', name: 'Faithless Looting', type_line: 'Sorcery' },
    },
    {
      name: 'Phyrexian Furnace',
      text: 'Exile target card from a graveyard.',
      card: { id: 'furnace-target', name: 'Anguished Unmaking', type_line: 'Instant' },
    },
    {
      name: 'Veteran Survivor',
      text: 'At the beginning of your second main phase, if this creature is tapped, exile up to one target card from a graveyard.',
      card: { id: 'survivor-target', name: 'Talisman of Dominance', type_line: 'Artifact' },
    },
    {
      name: 'Sungold Sentinel',
      text: 'Whenever this creature enters or attacks, exile up to one target card from a graveyard.',
      card: { id: 'sentinel-target', name: 'Preordain', type_line: 'Sorcery' },
    },
    {
      name: 'Offalsnout',
      text: 'When this creature leaves the battlefield, exile target card from a graveyard.',
      card: { id: 'offalsnout-target', name: 'Damn', type_line: 'Sorcery' },
    },
    {
      name: "Ashnod's Harvester",
      text: 'Whenever this creature attacks, exile target card from a graveyard.',
      card: { id: 'harvester-target', name: 'Painful Truths', type_line: 'Sorcery' },
    },
    {
      name: 'Verdant Command',
      text: 'Exile target card from a graveyard.',
      card: { id: 'verdant-target', name: 'Thought Vessel', type_line: 'Artifact' },
    },
    {
      name: 'Dawnbringer Cleric',
      text: 'Exile target card from a graveyard.',
      card: { id: 'cleric-target', name: 'Night\'s Whisper', type_line: 'Sorcery' },
    },
    {
      name: 'Coffin Purge',
      text: 'Exile target card from a graveyard.',
      card: { id: 'purge-target', name: 'Doom Blade', type_line: 'Instant' },
    },
    {
      name: 'Crook of Condemnation',
      text: 'Exile target card from a graveyard.',
      card: { id: 'crook-target', name: 'Mortify', type_line: 'Instant' },
    },
    {
      name: 'Territorial Kavu',
      text: 'Exile up to one target card from a graveyard.',
      card: { id: 'kavu-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Purify the Grave',
      text: 'Exile target card from a graveyard.',
      card: { id: 'purify-target', name: 'Growth Spiral', type_line: 'Instant' },
    },
    {
      name: 'Gravestone Strider',
      text: 'Exile target card from a graveyard.',
      card: { id: 'gravestone-target', name: 'Negate', type_line: 'Instant' },
    },
    {
      name: 'Apostle of Purifying Light',
      text: 'Exile target card from a graveyard.',
      card: { id: 'apostle-target', name: 'Cultivate', type_line: 'Sorcery' },
    },
    {
      name: 'Fade from Memory',
      text: 'Exile target card from a graveyard.',
      card: { id: 'fade-target', name: 'Memory Deluge', type_line: 'Instant' },
    },
    {
      name: 'Crypt Creeper',
      text: 'Exile target card from a graveyard.',
      card: { id: 'creeper-target', name: 'Swords to Plowshares', type_line: 'Instant' },
    },
    {
      name: 'Shadowfeed',
      text: 'Exile target card from a graveyard.',
      card: { id: 'shadowfeed-target', name: 'Thoughtseize', type_line: 'Sorcery' },
    },
    {
      name: 'Soul-Guide Gryff',
      text: 'When this creature enters, exile up to one target card from a graveyard.',
      card: { id: 'gryff-target', name: 'Arcane Denial', type_line: 'Instant' },
    },
    {
      name: 'Thraben Heretic',
      text: 'Exile target creature card from a graveyard.',
      card: { id: 'thraben-target', name: 'Stinkweed Imp', type_line: 'Creature - Imp' },
    },
    {
      name: 'Rise of Extus',
      text: 'Exile up to one target instant or sorcery card from a graveyard.',
      card: { id: 'extus-target', name: 'Expressive Iteration', type_line: 'Sorcery' },
    },
    {
      name: 'Mortiphobia',
      text: 'Exile target card from a graveyard.',
      card: { id: 'mortiphobia-target', name: 'Wear // Tear', type_line: 'Instant' },
    },
    {
      name: 'Sewerdreg',
      text: 'Exile target card from a graveyard.',
      card: { id: 'sewerdreg-target', name: 'Electrolyze', type_line: 'Instant' },
    },
    {
      name: 'Moratorium Stone',
      text: 'Exile target card from a graveyard.',
      card: { id: 'moratorium-target', name: 'Farseek', type_line: 'Sorcery' },
    },
    {
      name: 'Steamclaw',
      text: 'Exile target card from a graveyard.',
      card: { id: 'steamclaw-target', name: 'Frantic Search', type_line: 'Instant' },
    },
    {
      name: 'Trained Blessed Mind',
      text: 'Exile target card from a graveyard.',
      card: { id: 'mind-target', name: 'Impulse', type_line: 'Instant' },
    },
  ])('applies later queue-backed graveyard exile for $name', ({ name, text, card }) => {
    const ir = parseOracleTextToIR(text, name);
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [card],
          exile: [],
        } as any,
      ],
      battlefield:
        name === 'Veteran Survivor'
          ? [
              {
                id: 'veteran-survivor',
                controller: 'p1',
                owner: 'p1',
                tapped: true,
                card: { id: 'veteran-survivor-card', name: 'Veteran Survivor', type_line: 'Creature - Human Survivor' },
              } as any,
            ]
          : [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      ...(name === 'Veteran Survivor' ? { sourceId: 'veteran-survivor' } : {}),
      sourceName: name,
      targetPermanentId: card.id,
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain(card.id);
  });

  it('applies saga chapter graveyard reanimation for creature-or-planeswalker targets under your control', () => {
    const ir = parseOracleTextToIR(
      'III — Put target creature or planeswalker card from a graveyard onto the battlefield under your control.',
      'The Eldest Reborn'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'fallen-walker',
              name: 'Liliana of the Veil',
              type_line: 'Legendary Planeswalker - Liliana',
            },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'The Eldest Reborn',
      targetPermanentId: 'fallen-walker',
    });

    const permanent = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'fallen-walker') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
    expect(p2.graveyard).toHaveLength(0);
  });

  it('applies saga chapter graveyard land recursion under your control', () => {
    const ir = parseOracleTextToIR(
      'II — Put target land card from a graveyard onto the battlefield under your control.',
      'Waking the Trolls'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'strip-mine',
              name: 'Strip Mine',
              type_line: 'Land',
            },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Waking the Trolls',
      targetPermanentId: 'strip-mine',
    });

    const land = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'strip-mine') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(land).toBeTruthy();
    expect(land.controller).toBe('p1');
    expect(p2.graveyard).toHaveLength(0);
  });

  it("applies exact-target legendary permanent recursion with a 'not named this permanent' exclusion", () => {
    const ir = parseOracleTextToIR(
      "When Staff of Eden enters, put target legendary permanent card not named Staff of Eden, Vault's Key from a graveyard onto the battlefield under your control.",
      "Staff of Eden, Vault's Key"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'legendary-relic',
              name: 'Blackblade Reforged',
              type_line: 'Legendary Artifact - Equipment',
            },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: "Staff of Eden, Vault's Key",
      targetPermanentId: 'legendary-relic',
    });

    const permanent = (result.state.battlefield as any[]).find(perm => perm?.card?.id === 'legendary-relic') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(permanent).toBeTruthy();
    expect(permanent.controller).toBe('p1');
    expect(p2.graveyard).toHaveLength(0);
  });

  it("safe-skips exact-target legendary permanent recursion when the target is named like 'this permanent'", () => {
    const ir = parseOracleTextToIR(
      "When Staff of Eden enters, put target legendary permanent card not named Staff of Eden, Vault's Key from a graveyard onto the battlefield under your control.",
      "Staff of Eden, Vault's Key"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'staff-copy',
              name: "Staff of Eden, Vault's Key",
              type_line: 'Legendary Artifact',
            },
          ],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: "Staff of Eden, Vault's Key",
      targetPermanentId: 'staff-copy',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect((result.state.battlefield as any[]).some(perm => perm?.card?.id === 'staff-copy')).toBe(false);
    expect(p2.graveyard).toHaveLength(1);
  });

  it('classifies attached-Equipment sacrifice as player choice when multiple matches exist', () => {
    const ir = parseOracleTextToIR('Sacrifice an Equipment attached to Outfitted Jouster.', 'Outfitted Jouster');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'outfitted-jouster-choice',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'outfitted-jouster-choice-card', name: 'Outfitted Jouster', type_line: 'Creature - Human Knight' },
        },
        {
          id: 'short-sword-choice',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'outfitted-jouster-choice',
          card: { id: 'short-sword-choice-card', name: 'Short Sword', type_line: 'Artifact - Equipment' },
        },
        {
          id: 'bone-saw-choice',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'outfitted-jouster-choice',
          card: { id: 'bone-saw-choice-card', name: 'Bonesaw', type_line: 'Artifact - Equipment' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'outfitted-jouster-choice',
      sourceName: 'Outfitted Jouster',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('classifies "of their choice" sacrifice text as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR('Each opponent sacrifices a creature of their choice.', 'Choice Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'choice-creature-a',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'choice-creature-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
        {
          id: 'choice-creature-b',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'choice-creature-b-card', name: 'Goblin Piker', type_line: 'Creature - Goblin Warrior' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('classifies "any number of creatures" sacrifice text as player choice', () => {
    const ir = parseOracleTextToIR('Sacrifice any number of creatures.', 'Choice Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'any-number-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'any-number-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
        {
          id: 'any-number-b',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'any-number-b-card', name: 'Goblin Piker', type_line: 'Creature - Goblin Warrior' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
  });

  it('classifies "one or more Treasures" sacrifice text as player choice', () => {
    const ir = parseOracleTextToIR('You may sacrifice one or more Treasures.', 'Treasure Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'treasure-choice-a',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'treasure-choice-a-card', name: 'Treasure', type_line: 'Artifact - Treasure' },
        },
        {
          id: 'treasure-choice-b',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'treasure-choice-b-card', name: 'Treasure', type_line: 'Artifact - Treasure' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' }, { allowOptional: true });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('classifies sacrifice-or-pay wording as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR('Whenever this creature attacks, you may sacrifice a Food or pay {2}{W}.', 'Nimble Hobbit');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'nimble-hobbit-food',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'nimble-hobbit-food-card', name: 'Food', type_line: 'Artifact - Food' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'nimble-hobbit', sourceName: 'Nimble Hobbit' },
      { allowOptional: true }
    );

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('classifies sacrifice-and-pay wording as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR('You may sacrifice this enchantment and pay {2}{G}{G}.', 'Preferred Selection');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'preferred-selection',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'preferred-selection-card', name: 'Preferred Selection', type_line: 'Enchantment' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'preferred-selection', sourceName: 'Preferred Selection' },
      { allowOptional: true }
    );

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('classifies prevention-cost self-sacrifice text as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your upkeep, sacrifice this creature unless you pay {U}.',
      'Phantasmal Forces'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'forces-self-choice',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'forces-choice-card', name: 'Phantasmal Forces', type_line: 'Creature - Illusion' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'forces-self-choice',
      sourceName: 'Phantasmal Forces',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('applies word-number sacrifice counts like "two creatures" deterministically when exact count matches', () => {
    const ir = parseOracleTextToIR('Sacrifice two creatures.', 'Count Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'two-creatures-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'two-creatures-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
        {
          id: 'two-creatures-b',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'two-creatures-b-card', name: 'Goblin Piker', type_line: 'Creature - Goblin Warrior' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.state.battlefield).toHaveLength(0);
    expect((p1?.graveyard || []).length).toBe(2);
  });

  it('applies subtype-token sacrifice text like "a Blood token"', () => {
    const ir = parseOracleTextToIR('Sacrifice a Blood token.', 'Token Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'blood-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'blood-token-card', name: 'Blood', type_line: 'Artifact - Blood' },
        },
        {
          id: 'food-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'food-token-card', name: 'Food', type_line: 'Artifact - Food' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['food-token']);
  });

  it('applies self-sacrifice for "this Equipment"', () => {
    const ir = parseOracleTextToIR('Sacrifice this Equipment.', 'Equipment Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'self-equipment',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'self-equipment-card', name: 'Short Sword', type_line: 'Artifact - Equipment' },
        },
        {
          id: 'safe-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-creature-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'self-equipment',
      sourceName: 'Short Sword',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['safe-creature']);
  });

  it('applies self-sacrifice for named source references', () => {
    const ir = parseOracleTextToIR('Sacrifice Night Out in Vegas.', 'Night Out in Vegas');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'named-self-enchantment',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'named-self-enchantment-card', name: 'Night Out in Vegas', type_line: 'Enchantment' },
        },
        {
          id: 'safe-permanent-after-name',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-permanent-after-name-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'named-self-enchantment',
      sourceName: 'Night Out in Vegas',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['safe-permanent-after-name']);
  });

  it('applies the sacrifice half of a split conjunction and records a gap for the unsupported follow-up', () => {
    const ir = parseOracleTextToIR('Sacrifice this enchantment and counter that spell.', 'Hesitation');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'hesitation-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'hesitation-source-card', name: 'Hesitation', type_line: 'Enchantment' },
        },
        {
          id: 'safe-artifact-after-split',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-after-split-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'hesitation-source',
      sourceName: 'Hesitation',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'unknown')).toBe(true);
    expect(result.automationGaps.some(gap => String(gap.raw || '').toLowerCase().includes('counter that spell'))).toBe(true);
    expect(remainingIds).toEqual(['safe-artifact-after-split']);
  });

  it('applies the sacrifice half of an explicit self-reference "it deals" conjunction and records the unresolved follow-up', () => {
    const ir = parseOracleTextToIR(
      'When another creature enters, sacrifice this creature and it deals 3 damage to target player or planeswalker.',
      'Mogg Bombers'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'mogg-bombers-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'mogg-bombers-source-card', name: 'Mogg Bombers', type_line: 'Creature - Goblin' },
        },
        {
          id: 'safe-artifact-after-it-deals-split',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-after-it-deals-split-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'mogg-bombers-source',
      sourceName: 'Mogg Bombers',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'unknown' || s.kind === 'damage' || s.kind === 'deal_damage')).toBe(true);
    expect(result.automationGaps.some(gap => String(gap.raw || '').toLowerCase().includes('it deals 3 damage'))).toBe(true);
    expect(remainingIds).toEqual(['safe-artifact-after-it-deals-split']);
  });

  it('keeps leading conditional sacrifice conjunctions persisted as a conditional gap when the wrapper condition is unsupported', () => {
    const ir = parseOracleTextToIR("If you don't, sacrifice this artifact and draw three cards.", "Sorcerer's Strongbox");
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'strongbox-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'strongbox-source-card', name: "Sorcerer's Strongbox", type_line: 'Artifact' },
        },
        {
          id: 'safe-artifact-after-conditional',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-artifact-after-conditional-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'strongbox-source',
      sourceName: "Sorcerer's Strongbox",
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps).toHaveLength(0);
    expect(result.skippedSteps.some(s => s.kind === 'conditional')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'unsupported_condition_clause')).toBe(true);
    expect(remainingIds).toEqual(['strongbox-source', 'safe-artifact-after-conditional']);
  });

  it('applies wrapped conditional sacrifice steps when the condition can be safely evaluated', () => {
    const ir = parseOracleTextToIR('If you control an artifact, sacrifice this creature and draw a card.', 'Conditional Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'conditional-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'conditional-source-card', name: 'Conditional Test', type_line: 'Creature - Wizard' },
        },
        {
          id: 'supporting-artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'supporting-artifact-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'conditional-source',
      sourceName: 'Conditional Test',
    });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((p1?.hand || []).length).toBe(1);
    expect(remainingIds).toEqual(['supporting-artifact']);
  });

  it('applies mana-spent conditional sacrifice wrappers when the source tracks mana spent to cast it', () => {
    const ir = parseOracleTextToIR(
      'If eight or more mana was spent to cast that spell, sacrifice Tellah and it deals that much damage to each opponent.',
      'Tellah, Great Sage'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }],
          hand: [],
          graveyard: [],
        },
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'd1' }],
          hand: [],
          graveyard: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'tellah-source',
          controller: 'p1',
          owner: 'p1',
          manaSpentTotal: 8,
          card: { id: 'tellah-source-card', name: 'Tellah, Great Sage', type_line: 'Legendary Creature - Wizard', manaSpentTotal: 8 },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'tellah-source',
      sourceName: 'Tellah, Great Sage',
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
    expect((result.state.battlefield as any[])).toHaveLength(0);
    expect(p2?.life).toBe(32);
  });

  it('applies "if you can\'t" sacrifice fallbacks when the prior graveyard move is impossible', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, exile two cards from your graveyard. If you can't, sacrifice Egon and draw a card.",
      'Egon, God of Death'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'drawn-card', name: 'Drawn Card' }],
          hand: [],
          graveyard: [{ id: 'grave-card-1', name: 'Only Card', type_line: 'Instant' }],
          exile: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'egon-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'egon-source-card', name: 'Egon, God of Death', type_line: 'Legendary Creature - God' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'egon-source',
      sourceName: 'Egon, God of Death',
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'impossible_action')).toBe(false);
    expect((result.state.battlefield as any[])).toHaveLength(0);
    expect((p1?.hand || []).map((card: any) => card.id)).toEqual(['drawn-card']);
    expect((p1?.graveyard || []).map((card: any) => card.id)).toEqual(['grave-card-1', 'egon-source-card']);
  });

  it('does not apply "if you can\'t" sacrifice fallbacks when the prior graveyard move succeeds exactly', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, exile two cards from your graveyard. If you can't, sacrifice Egon and draw a card.",
      'Egon, God of Death'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'drawn-card', name: 'Drawn Card' }],
          hand: [],
          graveyard: [
            { id: 'grave-card-1', name: 'First Card', type_line: 'Instant' },
            { id: 'grave-card-2', name: 'Second Card', type_line: 'Sorcery' },
          ],
          exile: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'egon-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'egon-source-card', name: 'Egon, God of Death', type_line: 'Legendary Creature - God' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'egon-source',
      sourceName: 'Egon, God of Death',
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(false);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['egon-source']);
    expect((p1?.graveyard || [])).toHaveLength(0);
    expect((p1?.exile || []).map((card: any) => card.id)).toEqual(['grave-card-1', 'grave-card-2']);
  });

  it('keeps exact-count graveyard move fallbacks off when the move is possible but needs a card choice', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, exile two cards from your graveyard. If you can't, sacrifice Egon and draw a card.",
      'Egon, God of Death'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'drawn-card', name: 'Drawn Card' }],
          hand: [],
          graveyard: [
            { id: 'grave-card-1', name: 'First Card', type_line: 'Instant' },
            { id: 'grave-card-2', name: 'Second Card', type_line: 'Sorcery' },
            { id: 'grave-card-3', name: 'Third Card', type_line: 'Creature - Zombie' },
          ],
          exile: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'egon-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'egon-source-card', name: 'Egon, God of Death', type_line: 'Legendary Creature - God' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'egon-source',
      sourceName: 'Egon, God of Death',
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(false);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'unsupported_condition_clause')).toBe(false);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['egon-source']);
    expect((p1?.graveyard || []).map((card: any) => card.id)).toEqual(['grave-card-1', 'grave-card-2', 'grave-card-3']);
    expect((p1?.exile || [])).toHaveLength(0);
  });

  it('applies source-counter conditional sacrifice wrappers for Bucket List style conditions', () => {
    const ir = parseOracleTextToIR(
      'If all five types on Bucket List have counters over them, sacrifice it and draw one more card.',
      'Bucket List'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'bucket-list-source',
          controller: 'p1',
          owner: 'p1',
          counters: { artifact: 1, creature: 1, enchantment: 1, instant: 1, sorcery: 1 },
          card: { id: 'bucket-list-source-card', name: 'Bucket List', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'bucket-list-source',
      sourceName: 'Bucket List',
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect(result.automationGaps.some(gap => String(gap.raw || '').toLowerCase().includes('draw one more card'))).toBe(false);
    expect((result.state.battlefield as any[])).toHaveLength(0);
    expect((p1?.hand || [])).toHaveLength(1);
  });

  it('applies chosen-name conditional sacrifice wrappers when the top card matches the source chosen name', () => {
    const ir = parseOracleTextToIR(
      'Choose a card name, then reveal the top card of your library. If that card has the chosen name, sacrifice this artifact and draw three cards.',
      "Diviner's Lockbox"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'top-card', name: 'Counterspell', type_line: 'Instant' },
            { id: 'draw-2', name: 'Island', type_line: 'Land' },
            { id: 'draw-3', name: 'Swamp', type_line: 'Land' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'lockbox-source',
          controller: 'p1',
          owner: 'p1',
          chosenCardName: 'Counterspell',
          card: { id: 'lockbox-source-card', name: "Diviner's Lockbox", type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'lockbox-source',
      sourceName: "Diviner's Lockbox",
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((result.state.battlefield as any[])).toHaveLength(0);
    expect((p1?.hand || [])).toHaveLength(3);
  });

  it('applies coin-flip conditional sacrifice wrappers when the runtime context says the flip was won', () => {
    const ir = parseOracleTextToIR(
      'Flip a coin. If you win the flip, sacrifice this artifact and draw three cards.',
      "Sorcerer's Strongbox"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        },
      ] as any,
      battlefield: [
        {
          id: 'strongbox-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'strongbox-source-card', name: "Sorcerer's Strongbox", type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'strongbox-source',
      sourceName: "Sorcerer's Strongbox",
      wonCoinFlip: true,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((result.state.battlefield as any[])).toHaveLength(0);
    expect((p1?.hand || [])).toHaveLength(3);
  });

  it('applies vote-result conditional sacrifice wrappers when the runtime context provides the winning vote', () => {
    const ir = parseOracleTextToIR(
      'If carnage gets more votes, sacrifice this artifact and destroy all nonland permanents.',
      'Coercive Portal'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'portal-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'portal-source-card', name: 'Coercive Portal', type_line: 'Artifact' },
        },
        {
          id: 'creature-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'creature-a-card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
        },
        {
          id: 'land-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'land-a-card', name: 'Forest', type_line: 'Land' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'portal-source',
      sourceName: 'Coercive Portal',
      winningVoteChoice: 'carnage',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'destroy')).toBe(true);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['land-a']);
  });

  it('applies parsed vote steps and winner-conditioned follow-up when the runtime context provides the winning vote', () => {
    const ir = parseOracleTextToIR(
      'Starting with you, each player votes for homage or carnage. If homage gets more votes, draw a card.',
      'Coercive Portal'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'draw-a', name: 'Card A' }],
          hand: [],
          graveyard: [],
          exile: [],
        },
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Coercive Portal',
      winningVoteChoice: 'homage',
    });

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.some(s => s.kind === 'vote')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['draw-a']);
  });

  it('applies tied-vote follow-up when the runtime context marks the vote as tied', () => {
    const ir = parseOracleTextToIR(
      'Starting with you, each player votes for homage or carnage. If carnage gets more votes or the vote is tied, draw a card.',
      'Coercive Portal'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'draw-a', name: 'Card A' }],
          hand: [],
          graveyard: [],
          exile: [],
        },
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Coercive Portal',
      winningVoteChoice: null,
    });

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.some(s => s.kind === 'vote')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['draw-a']);
  });

  it('derives the winning vote from vote counts for winner-conditioned follow-up', () => {
    const ir = parseOracleTextToIR(
      'Starting with you, each player votes for homage or carnage. If homage gets more votes, draw a card.',
      'Coercive Portal'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'draw-a', name: 'Card A' }],
          hand: [],
          graveyard: [],
          exile: [],
        },
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Coercive Portal',
      voteChoiceCounts: { homage: 2, carnage: 1 },
    });

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.some(s => s.kind === 'vote')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'draw')).toBe(true);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['draw-a']);
  });

  it('applies per-choice vote payoff steps for Tivit', () => {
    const ir = parseOracleTextToIR(
      'Starting with you, each player votes for evidence or bribery. For each evidence vote, investigate. For each bribery vote, create a Treasure token.',
      'Tivit, Seller of Secrets'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Tivit, Seller of Secrets',
      voteChoiceCounts: { evidence: 2, bribery: 1 },
    });

    const names = (result.state.battlefield as any[]).map((perm: any) => String(perm?.card?.name || '').toLowerCase());
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['vote', 'investigate', 'create_token']);
    expect(names.filter(name => name.includes('clue'))).toHaveLength(2);
    expect(names.filter(name => name.includes('treasure'))).toHaveLength(1);
  });

  it('applies numeric per-choice vote scaling for Orchard Elemental', () => {
    const ir = parseOracleTextToIR(
      'Starting with you, each player votes for sprout or harvest. Put two +1/+1 counters on this creature for each sprout vote. You gain 3 life for each harvest vote.',
      'Orchard Elemental'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] },
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
      battlefield: [
        {
          id: 'orchard-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'orchard-source-card', name: 'Orchard Elemental', type_line: 'Creature - Elemental' },
          counters: {},
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'orchard-source',
      sourceName: 'Orchard Elemental',
      voteChoiceCounts: { sprout: 2, harvest: 1 },
    });

    const orchard = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'orchard-source') as any;
    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['vote', 'add_counter', 'gain_life']);
    expect(Number(orchard?.counters?.['+1/+1'] || 0)).toBe(4);
    expect(p1.life).toBe(43);
  });

  it('treats specific \"you do not ...\" conditional wrappers as false after a successful antecedent action', () => {
    const ir = parseOracleTextToIR(
      'You may sacrifice a Forest. If you do not sacrifice a Forest, sacrifice this creature and it deals 7 damage to you.',
      'Gargantuan Gorilla'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
      battlefield: [
        {
          id: 'gorilla-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'gorilla-source-card', name: 'Gargantuan Gorilla', type_line: 'Creature - Gorilla' },
        },
        {
          id: 'forest-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'forest-a-card', name: 'Snow-Covered Forest', type_line: 'Basic Snow Land - Forest' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'gorilla-source',
        sourceName: 'Gargantuan Gorilla',
      },
      { allowOptional: true }
    );
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice' && /forest/i.test(s.raw))).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(false);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['gorilla-source']);
    expect(p1?.life).toBe(40);
  });

  it('keeps the antecedent action outcome for \"you do not\" wrappers even when an unsupported bonus clause sits between them', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, you may sacrifice a Forest. If you sacrifice a snow Forest this way, this creature gains trample until end of turn. If you don't sacrifice a Forest, sacrifice this creature and it deals 7 damage to you.",
      'Gargantuan Gorilla'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] },
      ] as any,
      battlefield: [
        {
          id: 'gorilla-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'gorilla-source-card', name: 'Gargantuan Gorilla', type_line: 'Creature - Gorilla' },
        },
        {
          id: 'forest-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'forest-a-card', name: 'Snow-Covered Forest', type_line: 'Basic Snow Land - Forest' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'gorilla-source',
        sourceName: 'Gargantuan Gorilla',
      },
      { allowOptional: true }
    );
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice' && /forest/i.test(s.raw))).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(false);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['gorilla-source']);
    expect(p1?.life).toBe(40);
  });

  it('applies temporary evasion grants that combat automation respects and cleanup removes', () => {
    const ir = parseOracleTextToIR(
      "Double the number of +1/+1 counters on target creature. That creature can't be blocked by creatures with power 2 or less this turn.",
      'Bright-Palm, Soul Awakener'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'bright-palm-target',
          controller: 'p1',
          owner: 'p1',
          counters: { '+1/+1': 2 },
          card: {
            id: 'bright-palm-target-card',
            name: 'Fox Monk',
            type_line: 'Creature - Fox Monk',
            power: '4',
            toughness: '4',
          },
        },
        {
          id: 'small-blocker',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'small-blocker-card', name: 'Bear Cub', type_line: 'Creature - Bear', power: '2', toughness: '2' },
        },
        {
          id: 'large-blocker',
          controller: 'p2',
          owner: 'p2',
          card: { id: 'large-blocker-card', name: 'Hill Giant', type_line: 'Creature - Giant', power: '3', toughness: '3' },
        },
      ] as any,
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] },
        { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'bright-palm-source',
        sourceName: 'Bright-Palm, Soul Awakener',
        targetCreatureId: 'bright-palm-target',
      },
      { allowOptional: true }
    );

    const attackerPerm = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'bright-palm-target');
    expect(attackerPerm?.counters?.['+1/+1']).toBe(4);
    expect(attackerPerm?.temporaryEffects?.some((effect: any) => String(effect?.description || '').includes('power 2 or less'))).toBe(true);

    const attacker = createCombatCreature(attackerPerm as any);
    const smallBlocker = createCombatCreature(((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'small-blocker') as any);
    const largeBlocker = createCombatCreature(((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'large-blocker') as any);

    expect(canCreatureBlock(smallBlocker, attacker, []).legal).toBe(false);
    expect(canCreatureBlock(largeBlocker, attacker, []).legal).toBe(true);

    const cleanup = executeTurnCleanupStep(result.state as any, 'p1');
    const cleanedAttacker = ((cleanup.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'bright-palm-target');
    expect(cleanedAttacker?.temporaryEffects || []).toEqual([]);
  });

  it('applies detain until the detainer next turn begins', () => {
    const ir = parseOracleTextToIR('Detain target permanent.', 'Azorius Arrester');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'detained-target',
          controller: 'p2',
          owner: 'p2',
          card: {
            id: 'detained-target-card',
            name: 'Runeclaw Bear',
            type_line: 'Creature - Bear',
            power: '2',
            toughness: '2',
          },
        },
      ] as any,
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] },
        { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'azorius-arrester',
        sourceName: 'Azorius Arrester',
        targetPermanentId: 'detained-target',
      },
      { allowOptional: true }
    );

    const detained = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'detained-target');
    expect(detained?.temporaryEffects?.map((effect: any) => effect.description)).toEqual(
      expect.arrayContaining(["can't attack", "can't block", "activated abilities can't be activated"])
    );
    expect(detained?.temporaryEffects?.every((effect: any) => effect.expiresOnControllerTurn === 'p1')).toBe(true);

    const afterOpponentUntap = executeUntapStep(result.state as any, 'p2');
    const stillDetained = ((afterOpponentUntap.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'detained-target');
    expect(stillDetained?.temporaryEffects?.length).toBeGreaterThan(0);

    const afterDetainerUntap = executeUntapStep(afterOpponentUntap.state as any, 'p1');
    const released = ((afterDetainerUntap.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'detained-target');
    expect(released?.temporaryEffects || []).toEqual([]);
  });

  it("applies remove-counter antecedents before \"if you can't\" sacrifice fallbacks for Cocoon-style text", () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, remove a pupa counter from this Aura. If you can't, sacrifice it, put a +1/+1 counter on enchanted creature, and that creature gains flying.",
      'Cocoon'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'cocoon-source',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'cocoon-bear',
          counters: {},
          card: { id: 'cocoon-source-card', name: 'Cocoon', type_line: 'Enchantment - Aura' },
        },
        {
          id: 'cocoon-bear',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'cocoon-bear-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cocoon-source',
      sourceName: 'Cocoon',
    });

    expect(result.appliedSteps.some(s => s.kind === 'remove_counter')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'impossible_action')).toBe(false);
    expect(result.automationGaps.some(gap => String(gap.raw || '').toLowerCase().includes('put a +1/+1 counter'))).toBe(true);
    expect((result.state.battlefield as any[]).map((perm: any) => perm.id)).toEqual(['cocoon-bear']);
  });

  it('applies die-roll conditional sacrifice wrappers for Captain Rex Nebula style text', () => {
    const ir = parseOracleTextToIR(
      "If the result is equal to this Vehicle's mana value, sacrifice this Vehicle, then it deals that much damage to any target.",
      'Captain Rex Nebula'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lastDieRollByPlayer: { p1: 4 } as any,
      battlefield: [
        {
          id: 'rex-vehicle',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'rex-vehicle-card', name: 'Shiny Rock', type_line: 'Artifact - Vehicle', mana_value: 4 },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'rex-vehicle',
      sourceName: 'Captain Rex Nebula',
    });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(false);
    expect(result.automationGaps.some(gap => String(gap.raw || '').toLowerCase().includes('that much damage to any target'))).toBe(true);
    expect((result.state.battlefield as any[])).toHaveLength(0);
  });

  it('applies subtype-wide sacrifice text like "all Dragons you control"', () => {
    const ir = parseOracleTextToIR('Sacrifice all Dragons you control.', 'Subtype Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'dragon-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'dragon-a-card', name: 'Shivan Dragon', type_line: 'Creature - Dragon' },
        },
        {
          id: 'dragon-b',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'dragon-b-card', name: 'Moonveil Dragon', type_line: 'Creature - Dragon' },
        },
        {
          id: 'not-dragon',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'not-dragon-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const remainingIds = (result.state.battlefield as any[]).map(perm => perm.id);

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(remainingIds).toEqual(['not-dragon']);
  });

  it('classifies X-count sacrifice text as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR('Sacrifice X creatures.', 'Choice Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'x-creature-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'x-creature-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
  });

  it('classifies "one of them" sacrifice text as player choice instead of unsupported', () => {
    const ir = parseOracleTextToIR('Sacrifice one of them.', 'Choice Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'one-of-them-a',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'one-of-them-a-card', name: 'Bear Cub', type_line: 'Creature - Bear' },
        },
        {
          id: 'one-of-them-b',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'one-of-them-b-card', name: 'Goblin Piker', type_line: 'Creature - Goblin Warrior' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.automationGaps.some(gap => gap.classification === 'player_choice')).toBe(true);
  });

  it('applies deterministic "another creature" sacrifice by excluding the source permanent', () => {
    const ir = parseOracleTextToIR('Whenever this creature attacks, you may sacrifice another creature.', 'Brawl-Bash Ogre');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'brawl-bash-ogre',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'bbo-card', name: 'Brawl-Bash Ogre', type_line: 'Creature — Ogre Warrior' },
        },
        {
          id: 'sac-fodder',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'fodder-card', name: 'Goblin Token', type_line: 'Creature — Goblin' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'brawl-bash-ogre', sourceName: 'Brawl-Bash Ogre' },
      { allowOptional: true }
    );
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Brawl-Bash Ogre');
    expect(names).not.toContain('Goblin Token');
    expect(String(p1.graveyard[0]?.name || p1.graveyard[0]?.card?.name || '')).toBe('Goblin Token');
  });

  it('skips deterministic "another creature" sacrifice when more than one non-source candidate exists', () => {
    const ir = parseOracleTextToIR('Whenever this creature attacks, you may sacrifice another creature.', 'Brawl-Bash Ogre');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'brawl-bash-ogre-choice',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'bbo-choice-card', name: 'Brawl-Bash Ogre', type_line: 'Creature — Ogre Warrior' },
        },
        {
          id: 'fodder-one',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'fodder-one-card', name: 'Goblin Token', type_line: 'Creature — Goblin' },
        },
        {
          id: 'fodder-two',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'fodder-two-card', name: 'Bear Cub', type_line: 'Creature — Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'brawl-bash-ogre-choice', sourceName: 'Brawl-Bash Ogre' },
      { allowOptional: true }
    );

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.state.battlefield).toHaveLength(3);
  });

  it('applies deterministic mixed-type sacrifice when "artifact or creature" leaves one legal option', () => {
    const ir = parseOracleTextToIR('When this creature enters, you may sacrifice an artifact or creature.', 'Buzzard-Wasp Colony');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'only-artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'only-artifact-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
        {
          id: 'ignore-land',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'ignore-land-card', name: 'Forest', type_line: 'Basic Land — Forest' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'buzzard-wasp-colony', sourceName: 'Buzzard-Wasp Colony' },
      { allowOptional: true }
    );
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.state.battlefield).toHaveLength(1);
    expect(String((result.state.battlefield[0] as any)?.card?.name || '')).toBe('Forest');
    expect(String(p1.graveyard[0]?.name || p1.graveyard[0]?.card?.name || '')).toBe('Mind Stone');
  });

  it('skips mixed-type sacrifice when "artifact or creature" still leaves a real choice', () => {
    const ir = parseOracleTextToIR('When this creature enters, you may sacrifice an artifact or creature.', 'Buzzard-Wasp Colony');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'choice-artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'choice-artifact-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
        {
          id: 'choice-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'choice-creature-card', name: 'Bear Cub', type_line: 'Creature — Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceName: 'Buzzard-Wasp Colony' },
      { allowOptional: true }
    );

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.state.battlefield).toHaveLength(2);
  });

  it('applies deterministic mixed-type "another artifact or creature" by excluding the source permanent', () => {
    const ir = parseOracleTextToIR(
      'Whenever this Vehicle attacks, you may sacrifice another artifact or creature.',
      'Mukotai Soulripper'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'mukotai-soulripper',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'mukotai-card', name: 'Mukotai Soulripper', type_line: 'Artifact — Vehicle' },
        },
        {
          id: 'artifact-fodder',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'artifact-fodder-card', name: 'Servo Token', type_line: 'Artifact Creature — Servo' },
        },
        {
          id: 'safe-land',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'safe-land-card', name: 'Swamp', type_line: 'Basic Land — Swamp' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'mukotai-soulripper', sourceName: 'Mukotai Soulripper' },
      { allowOptional: true }
    );
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Mukotai Soulripper');
    expect(names).toContain('Swamp');
    expect(names).not.toContain('Servo Token');
  });

  it('applies subtype-based sacrifice for a Food when it is deterministic', () => {
    const ir = parseOracleTextToIR('You may sacrifice a Food.', 'Food Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'food-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'food-token-card', name: 'Food', type_line: 'Token Artifact — Food' },
        },
        {
          id: 'non-food-artifact',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'non-food-artifact-card', name: 'Mind Stone', type_line: 'Artifact' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' }, { allowOptional: true });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Mind Stone');
    expect(names).not.toContain('Food');
  });

  it('applies subtype-based sacrifice for a Vampire when it is deterministic', () => {
    const ir = parseOracleTextToIR('Sacrifice a Vampire.', 'Vampire Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'vampire-perm',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'vampire-card', name: 'Blood Artist', type_line: 'Creature — Vampire' },
        },
        {
          id: 'non-vampire-perm',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'non-vampire-card', name: 'Bear Cub', type_line: 'Creature — Bear' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const names = (result.state.battlefield as any[]).map(p => String((p as any)?.card?.name || ''));

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Bear Cub');
    expect(names).not.toContain('Blood Artist');
  });

  it('skips subtype-based sacrifice when multiple matching subtypes exist', () => {
    const ir = parseOracleTextToIR('At the beginning of your upkeep, sacrifice a Homunculus.', 'Puppet Conjurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'homunculus-one',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'homunculus-one-card', name: 'Homunculus One', type_line: 'Creature — Homunculus' },
        },
        {
          id: 'homunculus-two',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'homunculus-two-card', name: 'Homunculus Two', type_line: 'Creature — Homunculus' },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps.some(gap => gap.reasonCode === 'player_choice_required')).toBe(true);
    expect(result.state.battlefield).toHaveLength(2);
  });

  it('applies discard when it is deterministic (discard all cards in hand)', () => {
    const ir = parseOracleTextToIR('Discard two cards.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h1' }, { id: 'h2' }],
          graveyard: [],
        } as any,
      ],
    });
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.hand).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(2);
    expect(result.appliedSteps.some(s => s.kind === 'discard')).toBe(true);
  });

  it('skips discard when it requires a choice (hand has more than N cards)', () => {
    const ir = parseOracleTextToIR('Discard a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h1' }, { id: 'h2' }],
          graveyard: [],
        } as any,
      ],
    });
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.hand).toHaveLength(2);
    expect(p1.graveyard).toHaveLength(0);
    expect(result.skippedSteps.some(s => s.kind === 'discard')).toBe(true);
  });

  it('applies discard for "Discard your hand" (deterministic discard-hand shorthand)', () => {
    const ir = parseOracleTextToIR('Discard your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.hand).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(3);
    expect(result.appliedSteps.some(s => s.kind === 'discard')).toBe(true);
  });

  it('applies discard for "Each opponent discards their hand" (deterministic group discard-hand)', () => {
    const ir = parseOracleTextToIR('Each opponent discards their hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h1' }],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h1' }, { id: 'p2h2' }],
          graveyard: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p1.hand).toHaveLength(1);
    expect(p1.graveyard).toHaveLength(0);
    expect(p2.hand).toHaveLength(0);
    expect(p2.graveyard).toHaveLength(2);
    expect(p3.hand).toHaveLength(0);
    expect(p3.graveyard).toHaveLength(0);
    expect(result.appliedSteps.some(s => s.kind === 'discard')).toBe(true);
  });

  it('applies each-player discard when it is deterministic for all players', () => {
    const ir = parseOracleTextToIR('Each player discards a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'p1h1' }],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [{ id: 'p2h1' }],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect(p1.hand).toHaveLength(0);
    expect(p2.hand).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'discard')).toBe(true);
  });

  it('applies mill for you (deterministic)', () => {
    const ir = parseOracleTextToIR('Mill two cards.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }, { id: 'c2' }, { id: 'c3' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(1);
    expect(p1.graveyard).toHaveLength(2);
    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
  });

  it('mills at most library size', () => {
    const ir = parseOracleTextToIR('Mill three cards.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'c1' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.library).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
  });

  it('applies each-player mill', () => {
    const ir = parseOracleTextToIR('Each player mills a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.library).toHaveLength(0);
    expect(p2.library).toHaveLength(0);
    expect(p1.graveyard).toHaveLength(1);
    expect(p2.graveyard).toHaveLength(1);
    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
  });

  it('applies Trepanation Blade reveal-until-land clause as deterministic mill loop', () => {
    const ir = parseOracleTextToIR(
      'Whenever equipped creature attacks, defending player reveals cards from the top of their library until they reveal a land card. The creature gets +1/+0 until end of turn for each card revealed this way. That player puts the revealed cards into their graveyard.',
      'Trepanation Blade'
    );
    const ability = ir.abilities.find(a => a.type === 'triggered');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Creature � Human' },
            { id: 'p2c2', type_line: 'Instant' },
            { id: 'p2c3', type_line: 'Land' },
            { id: 'p2c4', type_line: 'Sorcery' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'blade1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Trepanation Blade',
          attachedTo: 'attacker1',
          cardType: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'attacker1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Equipped Attacker',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: true,
          attacking: 'p2',
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'blade1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const attacker = ((result.state as any).battlefield || []).find((p: any) => p.id === 'attacker1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'mill')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect(attacker.lastTrepanationBonus).toBe(3);
  });

  it('applies X-based modify_pt where X is the number of cards discarded this way', () => {
    const ir = parseOracleTextToIR(
      'Each player discards a card. Target creature gets +X/+0 until end of turn where X is the number of cards discarded this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [{ id: 'p1h1' }],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [{ id: 'p2h1' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creatureDiscardedThisWay',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureDiscardedThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'discard')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'discard')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards exiled this way', () => {
    const ir = parseOracleTextToIR(
      'Each player exiles the top card of their library. Target creature gets +X/+0 until end of turn where X is the number of cards exiled this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creatureExiledThisWay',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureExiledThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total power of the cards exiled this way', () => {
    const ir = parseOracleTextToIR(
      'Each player exiles the top card of their library. Target creature gets +X/+0 until end of turn where X is the total power of the cards exiled this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1', type_line: 'Creature — Elf', power: 2, toughness: 2 }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1', type_line: 'Creature — Giant', power: 5, toughness: 5 }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creatureExiledPowerThisWay',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureExiledPowerThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'exile_top')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies goad steps to deterministic creature targets', () => {
    const steps = [
      {
        kind: 'goad',
        target: { kind: 'raw', text: 'target creature' },
        raw: 'Goad target creature.',
      },
    ] as any;

    const start = makeState({
      turnNumber: 10,
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'onlyCreatureToGoad',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 4,
          toughness: 4,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'onlyCreatureToGoad') as any;

    expect(result.appliedSteps.some(s => s.kind === 'goad')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'goad')).toBe(false);
    expect(creature.goadedBy).toContain('p1');
    expect(creature.goadedUntil?.p1).toBe(11);
  });

  it('applies X-based modify_pt where X is the total power of creatures goaded this way', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the total power of creatures goaded this way.',
      'Test'
    );
    const modifyPtStep = ir.abilities[0]?.steps?.find(step => step.kind === 'modify_pt');
    const steps = [
      {
        kind: 'goad',
        target: { kind: 'raw', text: 'chosen creatures' },
        raw: 'Goad those creatures.',
      },
      modifyPtStep,
    ].filter(Boolean) as any;

    const start = makeState({
      turnNumber: 7,
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'goadedPowerTarget',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Buff Target',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'goadedPowerA',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Opponent A',
          cardType: 'Creature',
          power: 3,
          toughness: 3,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'goadedPowerB',
          ownerId: 'p3',
          controller: 'p3',
          name: 'Opponent B',
          cardType: 'Creature',
          power: 5,
          toughness: 5,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'goadedPowerTarget',
      selectorContext: {
        chosenObjectIds: ['goadedPowerA', 'goadedPowerB'],
      } as any,
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'goadedPowerTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'goad')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total power of the creatures sacrificed this way', () => {
    const ir = parseOracleTextToIR(
      'Each opponent sacrifices a creature. Target creature you control gets +X/+0 until end of turn where X is the total power of the creatures sacrificed this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);
    const modifyStep = steps.find(step => step.kind === 'modify_pt') as any;

    expect(modifyStep).toBeTruthy();
    expect(Boolean(modifyStep?.powerUsesX)).toBe(true);
    expect(modifyStep?.condition?.kind).toBe('where');

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creatureSacrificedThisWay',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Target Bear',
          cardType: 'Creature',
          type_line: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'oppSacA',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Opponent A Creature',
          cardType: 'Creature',
          type_line: 'Creature',
          power: 3,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'oppSacB',
          ownerId: 'p3',
          controller: 'p3',
          name: 'Opponent B Creature',
          cardType: 'Creature',
          type_line: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureSacrificedThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'sacrifice')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'sacrifice')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies Giant Growth style target creature +3/+3 until end of turn via composable modify_pt', () => {
    const ir = parseOracleTextToIR('Target creature gets +3/+3 until end of turn.', 'Giant Growth');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(3);
    expect(ptMod.duration).toBe('end_of_turn');
  });

  it('treats animated noncreatures as valid target creatures in executor resolution', () => {
    const ir = parseOracleTextToIR('Target creature gets +3/+3 until end of turn.', 'Giant Growth');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'animatedTarget',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Animated Relic',
          isCreature: true,
          type_line: 'Artifact',
          card: {
            name: 'Animated Relic',
            type_line: 'Artifact',
            power: '2',
            toughness: '2',
            oracle_text: '',
          },
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'animatedTarget') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(3);
  });

  it('safely skips modify_pt with unknown for-each scaler', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +1/+1 until end of turn for each opponent you attacked with a creature this combat.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(modifiers.some((m: any) => m?.type === 'powerToughness')).toBe(false);
  });

  it('skips leading conditional modify_pt when the condition is false', () => {
    const ir = parseOracleTextToIR(
      'If you control an artifact, target creature gets +2/+2 until end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'conditional')).toBe(true);
    expect(modifiers.some((m: any) => m?.type === 'powerToughness')).toBe(false);
  });

  it('applies modify_pt when supported if-condition is true (you control an artifact)', () => {
    const ir = parseOracleTextToIR(
      'If you control an artifact, target creature gets +2/+2 until end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'artifact1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Relic',
          cardType: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies modify_pt with supported where-clause condition metadata', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +2/+2 until end of turn where X is the number of artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'artifact1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Relic',
          cardType: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt using supported where-clause value resolution', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'artifact1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Relic A',
          cardType: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'artifact2',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Relic B',
          cardType: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt with where-clause cards-in-graveyard evaluation', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [{ id: 'g1' }, { id: 'g2' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];
    const ptMod = modifiers.find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt where X is the number of opponents you have', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of opponents you have.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt where X is the number of artifacts your opponents control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of artifacts your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppa1', ownerId: 'p2', controller: 'p2', name: 'Opp Relic 1', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppa2', ownerId: 'p3', controller: 'p3', name: 'Opp Relic 2', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt where X is the number of islands target opponent controls', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of islands target opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppIslandCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2Island1', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land � Island', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2Island2', ownerId: 'p2', controller: 'p2', name: 'Dual', type_line: 'Land � Island Swamp', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2NonIsland', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land � Mountain', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppIslandCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands defending player controls', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of lands defending player controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDefendingLands', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2Land1', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land � Island', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2Land2', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land � Mountain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p1Land', ownerId: 'p1', controller: 'p1', name: 'Plains', type_line: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDefendingLands') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures those opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];
    const first = steps[0] as any;
    if (first?.condition?.kind === 'where') {
      first.condition.raw = 'x is the number of creatures those opponents control';
    }

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqThoseOppCreatures', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetThoseOppCreatures', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetThoseOppCreatures', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p2CreatureA', ownerId: 'p2', controller: 'p2', name: 'P2 Creature', type_line: 'Creature � Zombie', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p3CreatureA', ownerId: 'p3', controller: 'p3', name: 'P3 Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p3Artifact', ownerId: 'p3', controller: 'p3', name: 'P3 Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p1Creature', ownerId: 'p1', controller: 'p1', name: 'P1 Creature', type_line: 'Creature � Human', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqThoseOppCreatures',
      selectorContext: { eachOfThoseOpponents: ['p2' as any, 'p3' as any] },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetThoseOppCreatures') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is half your life total, rounded up', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is half your life total, rounded up.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 39, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(20);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that creature\'s power', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is that creature's power.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 4, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is this creature\'s power', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is this creature's power.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'thisCreaturePowerTarget', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 6, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'thisCreaturePowerTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is its power', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is its power.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'itsPowerTarget', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 7, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'itsPowerTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the tapped creature\'s power (alias)', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the tapped creature's power.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'tappedPowerTarget', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 5, toughness: 2,
          tapped: true, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tappedPowerTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards exiled with this permanent', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of cards exiled with this permanent.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [],
          exile: [
            { id: 'ex1', exiledBy: 'src-where' },
            { id: 'ex2', exiledBy: 'src-where' },
            { id: 'ex3', exiledBy: 'other-src' },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'src-where' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt where X is the number of creature cards exiled with this creature', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creature cards exiled with this creature.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [],
          exile: [
            { id: 'ex1', exiledBy: 'src-typed', type_line: 'Creature — Spirit' },
            { id: 'ex2', exiledWithSourceId: 'src-typed', type_line: 'Creature — Zombie' },
            { id: 'ex3', exiledBy: 'src-typed', type_line: 'Instant' },
            { id: 'ex4', exiledBy: 'other-src', type_line: 'Creature — Human' },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'src-typed' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('tracks move-zone exile cards as exiled with the source for later same-ability references', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. This creature gets +X/+0 until end of turn where X is the number of cards exiled with this creature.',
      'Link Tester'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'linked-target', name: 'Sign in Blood', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'link-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Link Tester',
          cardType: 'Creature',
          type_line: 'Creature - Horror',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'link-source',
      sourceName: 'Link Tester',
      targetPermanentId: 'linked-target',
    });

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'link-source') as any;
    const ptMod = (Array.isArray(source?.modifiers) ? source.modifiers : []).find((m: any) => m?.type === 'powerToughness');
    const exiledCard = ((result.state.players.find((p: any) => p.id === 'p2') as any)?.exile || [])[0] as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'modify_pt']);
    expect(exiledCard.exiledWithSourceId).toBe('link-source');
    expect(exiledCard.exiledWith).toBe('link-source');
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies Intrepid Paleontologist linked-exile permissions to owned Dinosaur cards with finality metadata', () => {
    const ir = parseOracleTextToIR(
      `{T}: Add one mana of any color.
{2}: Exile target card from a graveyard.
You may cast Dinosaur creature spells from among cards you own exiled with this creature. If you cast a spell this way, that creature enters with a finality counter on it. (If a creature with a finality counter on it would die, exile it instead.)`,
      'Intrepid Paleontologist'
    );
    const steps = ir.abilities[2]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 31,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [
              { id: 'intrepid-dino', name: 'Rampaging Raptor', type_line: 'Creature - Dinosaur', exiledWith: 'intrepid-source', exiledWithSourceId: 'intrepid-source' },
              { id: 'intrepid-nondino', name: 'Shock', type_line: 'Instant', exiledWith: 'intrepid-source', exiledWithSourceId: 'intrepid-source' },
              { id: 'other-link', name: 'Ancient Brontodon', type_line: 'Creature - Dinosaur', exiledWith: 'other-source', exiledWithSourceId: 'other-source' },
            ],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'intrepid-source',
        sourceName: 'Intrepid Paleontologist',
      },
      { allowOptional: true }
    );

    const exile = ((result.state.players[0] as any)?.exile || []) as any[];
    expect((result.state as any).playableFromExile?.p1?.['intrepid-dino']).toBe(Number.MAX_SAFE_INTEGER);
    expect(exile.find(card => card.id === 'intrepid-dino')?.entersBattlefieldWithCounters).toEqual({ finality: 1 });
    expect(exile.find(card => card.id === 'intrepid-dino')?.canBePlayedBy).toBe('p1');
    expect((result.state as any).playableFromExile?.p1?.['intrepid-nondino']).toBeUndefined();
    expect((result.state as any).playableFromExile?.p1?.['other-link']).toBeUndefined();
  });

  it('applies Boiling Rock Rioter linked-exile permissions to owned Ally cards', () => {
    const ir = parseOracleTextToIR(
      `Firebending 1 (Whenever this creature attacks, add {R}. This mana lasts until end of combat.)
Tap an untapped Ally you control: Exile target card from a graveyard.
Whenever this creature attacks, you may cast an Ally spell from among cards you own exiled with this creature.`,
      'Boiling Rock Rioter'
    );
    const steps = ir.abilities[2]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 32,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [
              {
                id: 'rioter-ally',
                name: 'Kazandu Blademaster',
                type_line: 'Creature - Human Soldier Ally',
                owner: 'p1',
                ownerId: 'p1',
                exiledWith: 'rioter-source',
                exiledWithSourceId: 'rioter-source',
              },
              {
                id: 'rioter-other',
                name: 'Lightning Bolt',
                type_line: 'Instant',
                owner: 'p1',
                ownerId: 'p1',
                exiledWith: 'rioter-source',
                exiledWithSourceId: 'rioter-source',
              },
            ],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'rioter-source',
        sourceName: 'Boiling Rock Rioter',
      },
      { allowOptional: true }
    );

    const exile = (((result.state.players || [])[0] as any)?.exile || []) as any[];

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['grant_exile_permission']);
    expect(result.skippedSteps).toHaveLength(0);
    expect((result.state as any).playableFromExile?.p1?.['rioter-ally']).toBe(32);
    expect(exile.find(card => card.id === 'rioter-ally')?.canBePlayedBy).toBe('p1');
    expect(exile.find(card => card.id === 'rioter-ally')?.playableUntilTurn).toBe(32);
    expect((result.state as any).playableFromExile?.p1?.['rioter-other']).toBeUndefined();
    expect(exile.find(card => card.id === 'rioter-other')?.canBePlayedBy).toBeUndefined();
  });

  it('puts an Emperor of Bones linked exiled creature onto the battlefield with a finality counter', () => {
    const ir = parseOracleTextToIR(
      `At the beginning of combat on your turn, exile up to one target card from a graveyard.
{1}{B}: Adapt 2.
Whenever one or more +1/+1 counters are put on this creature, put a creature card exiled with this creature onto the battlefield under your control with a finality counter on it. It gains haste. Sacrifice it at the beginning of the next end step.`,
      'Emperor of Bones'
    );
    const steps = ir.abilities[2]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [{ id: 'emperor-target', name: 'Grizzly Bears', type_line: 'Creature - Bear', exiledWith: 'emperor-source', exiledWithSourceId: 'emperor-source' }],
          } as any,
        ],
        battlefield: [
          {
            id: 'emperor-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Emperor of Bones',
            cardType: 'Creature',
            type_line: 'Creature - Skeleton Noble',
            power: 2,
            toughness: 2,
            tapped: false,
            summoningSick: false,
            counters: { '+1/+1': 2 },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'emperor-source',
        sourceName: 'Emperor of Bones',
      },
      { allowOptional: true }
    );

    const returned = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.card?.id === 'emperor-target') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'schedule_delayed_battlefield_action']);
    expect(returned?.controller).toBe('p1');
    expect(returned?.counters).toEqual({ finality: 1 });
    expect((((result.state.players || [])[0] as any)?.exile || []).map((card: any) => card.id)).not.toContain('emperor-target');
  });

  it('applies Adapt only when the source has no +1/+1 counters', () => {
    const ir = parseOracleTextToIR('Adapt 2', 'Adaptive Familiar');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'adapt-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Adaptive Familiar',
            cardType: 'Creature',
            type_line: 'Creature - Beast',
            power: 2,
            toughness: 2,
            tapped: false,
            summoningSick: false,
            counters: {},
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'adapt-source',
        sourceName: 'Adaptive Familiar',
      }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'adapt-source') as any;
    expect(source?.counters).toEqual({ '+1/+1': 2 });
    expect(result.appliedSteps.some(step => step.kind === 'add_counter')).toBe(true);
    expect(result.skippedSteps).toHaveLength(0);
  });

  it('skips Adapt when the source already has a +1/+1 counter', () => {
    const ir = parseOracleTextToIR('Adapt 2', 'Adaptive Familiar');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'adapt-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Adaptive Familiar',
            cardType: 'Creature',
            type_line: 'Creature - Beast',
            power: 2,
            toughness: 2,
            tapped: false,
            summoningSick: false,
            counters: { '+1/+1': 1 },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'adapt-source',
        sourceName: 'Adaptive Familiar',
      }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'adapt-source') as any;
    expect(source?.counters).toEqual({ '+1/+1': 1 });
    expect(result.appliedSteps.some(step => step.kind === 'add_counter')).toBe(false);
    expect(result.skippedSteps.map(step => step.kind)).toEqual(['conditional']);
  });

  it("applies The Spot, Living Portal's linked-exile ETB", () => {
    const ir = parseOracleTextToIR(
      "When The Spot enters, exile up to one target nonland permanent and up to one target nonland permanent card from a graveyard.\nWhen The Spot dies, put him on the bottom of his owner's library. If you do, return the exiled cards to their owners' hands.",
      'The Spot, Living Portal'
    );
    const etbSteps = ir.abilities[0]?.steps ?? [];
    const etbStart = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'spot-grave', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'spot-source',
          owner: 'p1',
          controller: 'p1',
          tapped: false,
          summoningSickness: false,
          counters: {},
          attachments: [],
          modifiers: [],
          card: {
            id: 'spot-source-card',
            name: 'The Spot, Living Portal',
            type_line: 'Legendary Creature - Human Scientist Villain',
          },
        } as any,
        {
          id: 'spot-perm',
          owner: 'p2',
          controller: 'p2',
          tapped: false,
          summoningSickness: false,
          counters: {},
          attachments: [],
          modifiers: [],
          card: {
            id: 'spot-perm-card',
            name: 'Goblin Bombardment',
            type_line: 'Enchantment',
          },
        } as any,
      ],
    } as any);

    const etbResult = applyOracleIRStepsToGameState(
      etbStart,
      etbSteps,
      {
        controllerId: 'p1',
        sourceId: 'spot-source',
        sourceName: 'The Spot, Living Portal',
        targetPermanentId: 'spot-perm',
        selectorContext: { chosenObjectIds: ['spot-grave'] },
      },
      { allowOptional: true }
    );

    const p2 = etbResult.state.players.find((player: any) => player.id === 'p2') as any;
    expect(etbResult.appliedSteps.map(step => step.kind)).toEqual(['exile', 'move_zone']);
    expect(etbResult.skippedSteps).toHaveLength(0);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['spot-perm-card', 'spot-grave']);
    expect((p2.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect((etbResult.state.battlefield || []).map((perm: any) => perm.id)).toEqual(['spot-source']);
    expect(etbResult.automationGaps).toHaveLength(0);
  });

  it("applies The Spot, Living Portal's linked-exile death trigger", () => {
    const ir = parseOracleTextToIR(
      "When The Spot enters, exile up to one target nonland permanent and up to one target nonland permanent card from a graveyard.\nWhen The Spot dies, put him on the bottom of his owner's library. If you do, return the exiled cards to their owners' hands.",
      'The Spot, Living Portal'
    );
    const deathSteps = ir.abilities[1]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [
              { id: 'spot-grave', name: 'Mind Stone', type_line: 'Artifact', exiledWith: 'spot-source', exiledWithSourceId: 'spot-source' },
              { id: 'spot-perm', name: 'Goblin Bombardment', type_line: 'Enchantment', exiledWith: 'spot-source', exiledWithSourceId: 'spot-source' },
            ],
          } as any,
        ],
        battlefield: [
          {
            id: 'spot-source',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSickness: false,
            counters: {},
            attachments: [],
            modifiers: [],
            card: {
              id: 'spot-source-card',
              name: 'The Spot, Living Portal',
              type_line: 'Legendary Creature - Human Scientist Villain',
            },
          } as any,
        ],
      } as any),
      deathSteps,
      {
        controllerId: 'p1',
        sourceId: 'spot-source',
        sourceName: 'The Spot, Living Portal',
        targetPermanentId: 'spot-source',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'move_zone']);
    expect((p2.hand || []).map((card: any) => card.id)).toEqual(expect.arrayContaining(['spot-grave', 'spot-perm']));
    expect((p2.exile || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.library || []).map((card: any) => card.id)).toContain('spot-source-card');
  });

  it('applies Ardyn, the Usurper by exiling a graveyard creature and creating the modified token copy', () => {
    const ir = parseOracleTextToIR(
      "Starscourge — At the beginning of combat on your turn, exile up to one target creature card from a graveyard. If a card is exiled this way, create a token that's a copy of it, except it's a 1/1 black Spirit creature in addition to its other types.",
      'Ardyn, the Usurper'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'ardyn-target',
                name: 'Grizzly Bears',
                type_line: 'Creature — Bear',
                oracle_text: '',
                power: '2',
                toughness: '2',
                colors: ['G'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'ardyn-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Ardyn, the Usurper',
            cardType: 'Legendary Creature — Human Noble',
            type_line: 'Legendary Creature — Human Noble',
            power: 3,
            toughness: 3,
            tapped: false,
            summoningSick: false,
            counters: {},
            attachments: [],
            modifiers: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'ardyn-source',
        sourceName: 'Ardyn, the Usurper',
        selectorContext: { chosenObjectIds: ['ardyn-target'] },
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    const exiled = (p2.exile || []).find((card: any) => card.id === 'ardyn-target') as any;
    const token = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id !== 'ardyn-source') as any;

    expect(result.skippedSteps).toHaveLength(0);
    expect(exiled?.exiledWithSourceId).toBe('ardyn-source');
    expect(token?.isToken).toBe(true);
    expect(token?.card?.name).toBe('Grizzly Bears');
    expect(token?.power).toBe(1);
    expect(token?.toughness).toBe(1);
    expect(token?.card?.colors).toEqual(['B']);
    expect(String(token?.card?.type_line || '')).toContain('Spirit');
  });

  it('applies Dino DNA by linking the exiled creature card and creating the modified token copy later', () => {
    const ir = parseOracleTextToIR(
      `Imprint — {1}, {T}: Exile target creature card from a graveyard.
{6}, {T}: Create a token that's a copy of a creature card exiled with Dino DNA, except it's a 6/6 green Dinosaur creature with trample in addition to its other types. Activate only as a sorcery.`,
      'Dino DNA'
    );
    const imprintSteps = ir.abilities[0]?.steps ?? [];
    const copySteps = (ir.abilities[1]?.steps ?? []).filter((step: any) => step.kind !== 'unknown');

    const afterImprint = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dino-target',
                name: 'Llanowar Elves',
                type_line: 'Creature — Elf Druid',
                oracle_text: '{T}: Add {G}.',
                power: '1',
                toughness: '1',
                colors: ['G'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'dino-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Dino DNA',
            cardType: 'Artifact',
            type_line: 'Artifact',
            tapped: false,
            summoningSick: false,
            counters: {},
            attachments: [],
            modifiers: [],
          } as any,
        ],
      } as any),
      imprintSteps,
      {
        controllerId: 'p1',
        sourceId: 'dino-source',
        sourceName: 'Dino DNA',
        selectorContext: { chosenObjectIds: ['dino-target'] },
      },
      { allowOptional: true }
    );

    const afterCopy = applyOracleIRStepsToGameState(
      afterImprint.state,
      copySteps,
      {
        controllerId: 'p1',
        sourceId: 'dino-source',
        sourceName: 'Dino DNA',
        selectorContext: { chosenObjectIds: ['dino-target'] },
      },
      { allowOptional: true }
    );

    const p2 = afterCopy.state.players.find((player: any) => player.id === 'p2') as any;
    const exiled = (p2.exile || []).find((card: any) => card.id === 'dino-target') as any;
    const token = ((afterCopy.state.battlefield || []) as any[]).find((perm: any) => perm.id !== 'dino-source') as any;

    expect(afterImprint.skippedSteps).toHaveLength(0);
    expect(exiled?.exiledWithSourceId).toBe('dino-source');
    expect(afterCopy.skippedSteps).toHaveLength(0);
    expect(token?.isToken).toBe(true);
    expect(token?.card?.name).toBe('Llanowar Elves');
    expect(token?.power).toBe(6);
    expect(token?.toughness).toBe(6);
    expect(token?.card?.colors).toEqual(['G']);
    expect(String(token?.card?.type_line || '')).toContain('Dinosaur');
    expect(String(token?.card?.oracle_text || '')).toContain('Trample');
  });

  it('applies Dimir Doppelganger by exiling the target and copying its characteristics onto the source permanent', () => {
    const ir = parseOracleTextToIR(
      '{1}{U}{B}: Exile target creature card from a graveyard. This creature becomes a copy of that card, except it has this ability.',
      'Dimir Doppelganger'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'doppelganger-target',
                name: 'Baleful Strix',
                type_line: 'Artifact Creature â€” Bird',
                oracle_text: 'Flying, deathtouch\nWhen this creature enters, draw a card.',
                power: '1',
                toughness: '1',
                colors: ['U', 'B'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'doppelganger-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Dimir Doppelganger',
            cardType: 'Creature â€” Shapeshifter',
            type_line: 'Creature â€” Shapeshifter',
            oracle_text: '{1}{U}{B}: Exile target creature card from a graveyard. This creature becomes a copy of that card, except it has this ability.',
            power: 0,
            toughness: 2,
            tapped: false,
            summoningSick: false,
            counters: {},
            attachments: [],
            modifiers: [],
            card: {
              id: 'doppelganger-source-card',
              name: 'Dimir Doppelganger',
              type_line: 'Creature â€” Shapeshifter',
              oracle_text: '{1}{U}{B}: Exile target creature card from a graveyard. This creature becomes a copy of that card, except it has this ability.',
              power: '0',
              toughness: '2',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'doppelganger-source',
        sourceName: 'Dimir Doppelganger',
        selectorContext: { chosenObjectIds: ['doppelganger-target'] },
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    const exiled = (p2.exile || []).find((card: any) => card.id === 'doppelganger-target') as any;
    const copied = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'doppelganger-source') as any;

    expect(result.skippedSteps).toHaveLength(0);
    expect(exiled?.exiledWithSourceId).toBe('doppelganger-source');
    expect(copied?.name).toBe('Baleful Strix');
    expect(copied?.type_line).toBe('Artifact Creature â€” Bird');
    expect(copied?.power).toBe(1);
    expect(copied?.toughness).toBe(1);
    expect(String(copied?.oracle_text || '')).toContain('Flying, deathtouch');
    expect(String(copied?.oracle_text || '')).toContain('This creature becomes a copy of that card, except it has this ability.');
    expect(copied?.grantedAbilities).toContain(
      '{1}{U}{B}: Exile target creature card from a graveyard. This creature becomes a copy of that card, except it has this ability.'
    );
  });

  it.skip('applies Mirror Golem by projecting the linked exiled card types into protection text', () => {
    const ir = parseOracleTextToIR(
      `Imprint â€” When this creature enters, you may exile target card from a graveyard.
This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)`,
      'Mirror Golem'
    );
    const steps = (ir.abilities.find((ability: any) => ability.type === 'triggered')?.steps ?? []).filter(
      (step: any) => step.kind !== 'unknown'
    );

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'mirror-target',
                name: 'Solemn Simulacrum',
                type_line: 'Artifact Creature â€” Golem',
                oracle_text: 'x',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'mirror-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Mirror Golem',
            cardType: 'Artifact Creature â€” Golem',
            type_line: 'Artifact Creature â€” Golem',
            oracle_text:
              "This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            card: {
              id: 'mirror-source-card',
              name: 'Mirror Golem',
              type_line: 'Artifact Creature â€” Golem',
              oracle_text:
                "Imprint â€” When this creature enters, you may exile target card from a graveyard.\nThis creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            },
          } as any,
          {
            id: 'mirror-blocker',
            ownerId: 'p2',
            controller: 'p2',
            name: 'Bronze Sable',
            cardType: 'Artifact Creature â€” Sable',
            type_line: 'Artifact Creature â€” Sable',
            card: {
              id: 'mirror-blocker-card',
              name: 'Bronze Sable',
              type_line: 'Artifact Creature â€” Sable',
              oracle_text: '',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'mirror-source',
        sourceName: 'Mirror Golem',
        selectorContext: { chosenObjectIds: ['mirror-target'] },
      },
      { allowOptional: true }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-source') as any;
    const blocker = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-blocker') as any;

    expect(result.skippedSteps).toHaveLength(0);
    expect(source?.grantedAbilities).toEqual(
      expect.arrayContaining(['protection from artifact', 'protection from creature'])
    );

    const blockCheck = canCreatureBlock(
      createCombatCreature(blocker),
      createCombatCreature(source),
      []
    );
    expect(blockCheck.legal).toBe(false);
    expect(blockCheck.reason).toContain('protection from artifact');
  });

  it.skip('applies Mirror Golem by projecting linked exiled card types into protection text', () => {
    const ir = parseOracleTextToIR(
      `Imprint — When this creature enters, you may exile target card from a graveyard.
This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)`,
      'Mirror Golem'
    );
    const steps = (ir.abilities.find((ability: any) => ability.type === 'triggered')?.steps ?? []).filter(
      (step: any) => step.kind !== 'unknown'
    );

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'mirror-target',
                name: 'Solemn Simulacrum',
                type_line: 'Artifact Creature - Golem',
                oracle_text: 'x',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'mirror-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Mirror Golem',
            cardType: 'Artifact Creature - Golem',
            type_line: 'Artifact Creature - Golem',
            oracle_text:
              "This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            card: {
              id: 'mirror-source-card',
              name: 'Mirror Golem',
              type_line: 'Artifact Creature - Golem',
              oracle_text:
                "Imprint — When this creature enters, you may exile target card from a graveyard.\nThis creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            },
          } as any,
          {
            id: 'mirror-blocker',
            ownerId: 'p2',
            controller: 'p2',
            name: 'Bronze Sable',
            cardType: 'Artifact Creature - Sable',
            type_line: 'Artifact Creature - Sable',
            card: {
              id: 'mirror-blocker-card',
              name: 'Bronze Sable',
              type_line: 'Artifact Creature - Sable',
              oracle_text: '',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'mirror-source',
        sourceName: 'Mirror Golem',
        selectorContext: { chosenObjectIds: ['mirror-target'] },
      },
      { allowOptional: true }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-source') as any;
    const blocker = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-blocker') as any;

    expect(result.skippedSteps).toHaveLength(0);
    expect(source?.grantedAbilities).toEqual(
      expect.arrayContaining(['protection from artifact', 'protection from creature'])
    );

    const blockCheck = canCreatureBlock(
      createCombatCreature(blocker),
      createCombatCreature(source),
      []
    );
    expect(blockCheck.legal).toBe(false);
    expect(blockCheck.reason).toContain('protection from artifact');
  });

  it('applies Mirror Golem linked-exile protection projection with clean imprint text', () => {
    const ir = parseOracleTextToIR(
      `Imprint — When this creature enters, you may exile target card from a graveyard.
This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)`,
      'Mirror Golem'
    );
    const steps = (ir.abilities.find((ability: any) => ability.type === 'triggered')?.steps ?? []).filter(
      (step: any) => step.kind !== 'unknown'
    );

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'mirror-target',
                name: 'Solemn Simulacrum',
                type_line: 'Artifact Creature - Golem',
                oracle_text: 'x',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'mirror-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Mirror Golem',
            cardType: 'Artifact Creature - Golem',
            type_line: 'Artifact Creature - Golem',
            oracle_text:
              "This creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            card: {
              id: 'mirror-source-card',
              name: 'Mirror Golem',
              type_line: 'Artifact Creature - Golem',
              oracle_text:
                "Imprint — When this creature enters, you may exile target card from a graveyard.\nThis creature has protection from each of the exiled card's card types. (Artifact, battle, creature, enchantment, instant, kindred, land, planeswalker, and sorcery are card types.)",
            },
          } as any,
          {
            id: 'mirror-blocker',
            ownerId: 'p2',
            controller: 'p2',
            name: 'Bronze Sable',
            cardType: 'Artifact Creature - Sable',
            type_line: 'Artifact Creature - Sable',
            card: {
              id: 'mirror-blocker-card',
              name: 'Bronze Sable',
              type_line: 'Artifact Creature - Sable',
              oracle_text: '',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'mirror-source',
        sourceName: 'Mirror Golem',
        selectorContext: { chosenObjectIds: ['mirror-target'] },
      },
      { allowOptional: true }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-source') as any;
    const blocker = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'mirror-blocker') as any;

    expect(result.skippedSteps).toHaveLength(0);
    expect(source?.grantedAbilities).toEqual(
      expect.arrayContaining(['protection from artifact', 'protection from creature'])
    );

    const blockCheck = canCreatureBlock(
      createCombatCreature(blocker),
      createCombatCreature(source),
      []
    );
    expect(blockCheck.legal).toBe(false);
    expect(blockCheck.reason).toContain('protection from artifact');
  });

  it("applies Mourner's Shield by preventing damage from a same-color linked source this turn", () => {
    const ir = parseOracleTextToIR(
      `Imprint — When this artifact enters, you may exile target card from a graveyard.
{2}, {T}: Prevent all damage that would be dealt this turn by target source of your choice that shares a color with the exiled card.`,
      "Mourner's Shield"
    );
    const imprintSteps = ir.abilities.find((ability: any) => ability.type === 'triggered')?.steps ?? [];
    const activatedSteps = ir.abilities.find((ability: any) => ability.type === 'activated')?.steps ?? [];

    const imprinted = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 11,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'mourner-target',
                name: "Night's Whisper",
                type_line: 'Sorcery',
                mana_cost: '{1}{B}',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'mourner-shield',
            ownerId: 'p1',
            controller: 'p1',
            name: "Mourner's Shield",
            cardType: 'Artifact',
            type_line: 'Artifact',
            card: {
              id: 'mourner-shield-card',
              name: "Mourner's Shield",
              type_line: 'Artifact',
              oracle_text:
                "Imprint — When this artifact enters, you may exile target card from a graveyard.\n{2}, {T}: Prevent all damage that would be dealt this turn by target source of your choice that shares a color with the exiled card.",
            },
          } as any,
          {
            id: 'crypt-rats',
            ownerId: 'p2',
            controller: 'p2',
            name: 'Crypt Rats',
            cardType: 'Creature - Rat',
            type_line: 'Creature - Rat',
            colors: ['B'],
            card: {
              id: 'crypt-rats-card',
              name: 'Crypt Rats',
              type_line: 'Creature - Rat',
              mana_cost: '{2}{B}',
              oracle_text: '',
            },
          } as any,
        ],
      } as any),
      imprintSteps,
      {
        controllerId: 'p1',
        sourceId: 'mourner-shield',
        sourceName: "Mourner's Shield",
        selectorContext: { chosenObjectIds: ['mourner-target'] },
      },
      { allowOptional: true }
    );

    const prevented = applyOracleIRStepsToGameState(
      imprinted.state,
      activatedSteps,
      {
        controllerId: 'p1',
        sourceId: 'mourner-shield',
        sourceName: "Mourner's Shield",
        targetPermanentId: 'crypt-rats',
      },
      { allowOptional: true }
    );

    const damageResult = applyOracleIRStepsToGameState(
      prevented.state,
      [
        {
          kind: 'deal_damage',
          amount: { kind: 'number', value: 3 },
          target: { kind: 'raw', text: 'target player' },
          raw: 'This creature deals 3 damage to target player.',
        } as any,
      ],
      {
        controllerId: 'p2',
        sourceId: 'crypt-rats',
        sourceName: 'Crypt Rats',
        selectorContext: { targetPlayerId: 'p1' },
      }
    );

    const p1 = damageResult.state.players.find((player: any) => player.id === 'p1') as any;
    const preventionEffects = ((prevented.state as any).damagePreventionEffects || []) as any[];

    expect(prevented.appliedSteps.map(step => step.kind)).toEqual(['prevent_damage']);
    expect(preventionEffects.some(effect => effect.targetSourceId === 'crypt-rats')).toBe(true);
    expect(damageResult.appliedSteps.map(step => step.kind)).toEqual(['deal_damage']);
    expect(p1.life).toBe(40);
    expect(damageResult.log.some(entry => String(entry).includes('Prevented 3 damage'))).toBe(true);
  });

  it('applies Psionic Ritual by exiling the spell card and replaying its copied instructions', () => {
    const ir = parseOracleTextToIR(
      'Exile target instant or sorcery card from a graveyard and copy it. You may cast the copy without paying its mana cost.',
      'Psionic Ritual'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [
              { id: 'top-a', name: 'Forest', type_line: 'Basic Land - Forest' },
              { id: 'top-b', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'psionic-target',
                name: 'Divination',
                type_line: 'Sorcery',
                oracle_text: 'Draw two cards.',
              },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'psionic-ritual',
        sourceName: 'Psionic Ritual',
        targetPermanentId: 'psionic-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'copy_spell']);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['psionic-target']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['top-a', 'top-b']);
    expect(p1.library || []).toEqual([]);
  });

  it("applies Wizard's Spellbook 10-19 branch by paying {1} for the copied spell", () => {
    const randomSpy = vi.spyOn(Math, 'random').mockReturnValue(0.6);
    try {
      const ir = parseOracleTextToIR(
        "{T}: Exile target instant or sorcery card from a graveyard. Roll a d20. Activate only as a sorcery.\n1-9 | Copy that card. You may cast the copy.\n10-19 | Copy that card. You may cast the copy by paying {1} rather than paying its mana cost.\n20 | Copy each card exiled with this artifact. You may cast any number of the copies without paying their mana costs.",
        "Wizard's Spellbook"
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const result = applyOracleIRStepsToGameState(
        makeState({
          players: [
            {
              id: 'p1',
              name: 'P1',
              seat: 0,
              life: 40,
              library: [
                { id: 'spellbook-draw-1', name: 'Plains', type_line: 'Basic Land - Plains' },
                { id: 'spellbook-draw-2', name: 'Island', type_line: 'Basic Land - Island' },
              ],
              hand: [],
              graveyard: [],
              exile: [],
            } as any,
            {
              id: 'p2',
              name: 'P2',
              seat: 1,
              life: 40,
              library: [],
              hand: [],
              graveyard: [
                {
                  id: 'spellbook-target',
                  name: 'Treasure Cruise',
                  type_line: 'Sorcery',
                  mana_cost: '{7}{U}',
                  oracle_text: 'Draw two cards.',
                },
              ],
              exile: [],
            } as any,
          ],
          manaPool: {
            p1: { white: 0, blue: 0, black: 0, red: 0, green: 0, colorless: 1 },
          } as any,
        } as any),
        steps,
        {
          controllerId: 'p1',
          sourceId: 'wizards-spellbook',
          sourceName: "Wizard's Spellbook",
          targetPermanentId: 'spellbook-target',
        },
        { allowOptional: true }
      );

      const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
      const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
      expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'roll_die', 'copy_spell', 'die_roll_results']);
      expect((p2.exile || []).map((card: any) => card.id)).toEqual(['spellbook-target']);
      expect((p1.hand || []).map((card: any) => card.id)).toEqual(['spellbook-draw-1', 'spellbook-draw-2']);
      expect((result.state as any).manaPool?.p1?.colorless).toBe(0);
      expect((result.state as any).lastDieRollByPlayer?.p1?.result).toBe(13);
    } finally {
      randomSpy.mockRestore();
    }
  });

  it("applies flattened inline Wizard's Spellbook 10-19 branch by paying {1} for the copied spell", () => {
    const randomSpy = vi.spyOn(Math, 'random').mockReturnValue(0.6);
    try {
      const ir = parseOracleTextToIR(
        "{3}{U}, {T}: Exile target instant or sorcery card from a graveyard and roll a d20. 1-9 | Copy that card. You may cast the copy. 10-19 | Copy that card. You may cast the copy by paying {1} rather than paying its mana cost. 20 | Copy each card exiled with this artifact. You may cast any number of the copies without paying their mana costs.",
        "Wizard's Spellbook"
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const result = applyOracleIRStepsToGameState(
        makeState({
          players: [
            {
              id: 'p1',
              name: 'P1',
              seat: 0,
              life: 40,
              library: [
                { id: 'spellbook-inline-draw-1', name: 'Plains', type_line: 'Basic Land - Plains' },
                { id: 'spellbook-inline-draw-2', name: 'Island', type_line: 'Basic Land - Island' },
              ],
              hand: [],
              graveyard: [],
              exile: [],
            } as any,
            {
              id: 'p2',
              name: 'P2',
              seat: 1,
              life: 40,
              library: [],
              hand: [],
              graveyard: [
                {
                  id: 'spellbook-inline-target',
                  name: 'Treasure Cruise',
                  type_line: 'Sorcery',
                  mana_cost: '{7}{U}',
                  oracle_text: 'Draw two cards.',
                },
              ],
              exile: [],
            } as any,
          ],
          manaPool: {
            p1: { white: 0, blue: 0, black: 0, red: 0, green: 0, colorless: 1 },
          } as any,
        } as any),
        steps,
        {
          controllerId: 'p1',
          sourceId: 'wizards-spellbook-inline',
          sourceName: "Wizard's Spellbook",
          targetPermanentId: 'spellbook-inline-target',
        },
        { allowOptional: true }
      );

      const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
      const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
      expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'roll_die', 'copy_spell', 'die_roll_results']);
      expect((p2.exile || []).map((card: any) => card.id)).toEqual(['spellbook-inline-target']);
      expect((p1.hand || []).map((card: any) => card.id)).toEqual(['spellbook-inline-draw-1', 'spellbook-inline-draw-2']);
      expect((result.state as any).manaPool?.p1?.colorless).toBe(0);
      expect((result.state as any).lastDieRollByPlayer?.p1?.result).toBe(13);
    } finally {
      randomSpy.mockRestore();
    }
  });

  it("applies Wizard's Spellbook 20 branch by replaying each linked exiled card for free", () => {
    const randomSpy = vi.spyOn(Math, 'random').mockReturnValue(0.99);
    try {
      const ir = parseOracleTextToIR(
        "{T}: Exile target instant or sorcery card from a graveyard. Roll a d20. Activate only as a sorcery.\n1-9 | Copy that card. You may cast the copy.\n10-19 | Copy that card. You may cast the copy by paying {1} rather than paying its mana cost.\n20 | Copy each card exiled with this artifact. You may cast any number of the copies without paying their mana costs.",
        "Wizard's Spellbook"
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const result = applyOracleIRStepsToGameState(
        makeState({
          players: [
            {
              id: 'p1',
              name: 'P1',
              seat: 0,
              life: 40,
              library: [
                { id: 'spellbook-free-1', name: 'Forest', type_line: 'Basic Land - Forest' },
                { id: 'spellbook-free-2', name: 'Mountain', type_line: 'Basic Land - Mountain' },
                { id: 'spellbook-free-3', name: 'Swamp', type_line: 'Basic Land - Swamp' },
              ],
              hand: [],
              graveyard: [],
              exile: [
                {
                  id: 'spellbook-old-exile',
                  name: 'Think Twice',
                  type_line: 'Instant',
                  mana_cost: '{1}{U}',
                  oracle_text: 'Draw a card.',
                  exiledBy: 'wizards-spellbook',
                },
              ],
            } as any,
            {
              id: 'p2',
              name: 'P2',
              seat: 1,
              life: 40,
              library: [],
              hand: [],
              graveyard: [
                {
                  id: 'spellbook-new-target',
                  name: 'Divination',
                  type_line: 'Sorcery',
                  mana_cost: '{2}{U}',
                  oracle_text: 'Draw two cards.',
                },
              ],
              exile: [],
            } as any,
          ],
        } as any),
        steps,
        {
          controllerId: 'p1',
          sourceId: 'wizards-spellbook',
          sourceName: "Wizard's Spellbook",
          targetPermanentId: 'spellbook-new-target',
        },
        { allowOptional: true }
      );

      const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
      const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
      expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'roll_die', 'copy_spell', 'die_roll_results']);
      expect((p1.exile || []).map((card: any) => card.id)).toEqual(['spellbook-old-exile']);
      expect((p2.exile || []).map((card: any) => card.id)).toEqual(['spellbook-new-target']);
      expect((p1.hand || []).map((card: any) => card.id)).toEqual([
        'spellbook-free-1',
        'spellbook-free-2',
        'spellbook-free-3',
      ]);
      expect((result.state as any).lastDieRollByPlayer?.p1?.result).toBe(20);
    } finally {
      randomSpy.mockRestore();
    }
  });

  it("applies The Many Deeds of Belzenlok by exiling the Saga and replaying the chosen chapter ability", () => {
    const ir = parseOracleTextToIR(
      'I — Exile up to one target Saga card from a graveyard. Copy its chapter I ability.\nII — Exile up to one target Saga card from a graveyard. Copy its chapter II ability.\nIII — Exile up to one target Saga card from a graveyard. Copy its chapter III ability.',
      'The Many Deeds of Belzenlok'
    );
    const steps = ir.abilities[1]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [
              { id: 'belzenlok-draw-1', name: 'Plains', type_line: 'Basic Land - Plains' },
              { id: 'belzenlok-draw-2', name: 'Island', type_line: 'Basic Land - Island' },
              { id: 'belzenlok-draw-3', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            ],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'belzenlok-saga',
                name: 'Test Saga',
                type_line: 'Enchantment - Saga',
                oracle_text: 'I — Draw a card.\nII — Draw two cards.\nIII — Gain 3 life.',
              },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'many-deeds',
        sourceName: 'The Many Deeds of Belzenlok',
        targetPermanentId: 'belzenlok-saga',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'draw', 'copy_chapter_ability']);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['belzenlok-saga']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['belzenlok-draw-1', 'belzenlok-draw-2']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['belzenlok-draw-3']);
  });

  it("applies Summoner's Sending by creating the token, scheduling its exile, and using the exiled card's mana value for counters", () => {
    const ir = parseOracleTextToIR(
      "Exile target creature card from a graveyard. Create a tapped 2/2 black Zombie creature token. At the beginning of the next end step, exile that token. Put X +1/+1 counters on it, where X is the exiled card's mana value.",
      "Summoner's Sending"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'sending-target',
                name: 'Air Elemental',
                type_line: 'Creature - Elemental',
                mana_cost: '{3}{U}{U}',
                mana_value: 5,
                power: '4',
                toughness: '4',
              },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'summoners-sending',
        sourceName: "Summoner's Sending",
        targetPermanentId: 'sending-target',
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    const zombie = (result.state.battlefield || [])[0] as any;
    const delayed = ((result.state as any).delayedTriggerRegistry?.triggers || []) as any[];

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token', 'add_counter']);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['sending-target']);
    expect(zombie?.tapped).toBe(true);
    expect(zombie?.counters?.['+1/+1']).toBe(5);
    expect(delayed.some((trigger: any) => String(trigger?.effect || '').toLowerCase().includes('exile that token'))).toBe(true);
  });

  it('applies X-based modify_pt where X is the number of creature cards exiled with a named permanent', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creature cards exiled with Verdant Sungrove.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [],
          exile: [
            { id: 'ex1', exiledBy: 'named-typed', type_line: 'Creature — Bird' },
            { id: 'ex2', exiledBy: 'named-typed', type_line: 'Sorcery' },
            { id: 'ex3', exiledWith: 'named-typed', cardType: 'Creature' },
          ],
        } as any,
      ],
      battlefield: [
        {
          id: 'named-typed', ownerId: 'p1', controller: 'p1', name: 'Verdant Sungrove', cardType: 'Artifact',
          tapped: false, summoningSick: false, counters: {},
        } as any,
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 3,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creature cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the number of creature cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'g1', type_line: 'Creature � Elf' },
            { id: 'g2', type_line: 'Instant' },
            { id: 'g3', type_line: 'Creature � Human' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2,
          tapped: false, summoningSick: false, counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(2);
  });

  it('applies X-based modify_pt where X is the number of instant and sorcery cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of instant and sorcery cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'is1', type_line: 'Instant' },
            { id: 'is2', type_line: 'Sorcery' },
            { id: 'is3', type_line: 'Creature � Elf' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetInstSor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetInstSor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanent cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of permanent cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'perm1', type_line: 'Creature � Elf' },
            { id: 'perm2', type_line: 'Artifact' },
            { id: 'perm3', type_line: 'Land' },
            { id: 'perm4', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetPermGy', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetPermGy') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of land cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of land cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'landGy1', type_line: 'Basic Land � Forest' },
            { id: 'landGy2', type_line: 'Land � Cave' },
            { id: 'spellGy1', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetLandGy', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLandGy') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of zombies you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of zombies you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqZombie', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetZombie', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetZombie', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'z1', ownerId: 'p1', controller: 'p1', name: 'Zombie 1', type_line: 'Creature � Zombie', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'z2', ownerId: 'p1', controller: 'p1', name: 'Zombie 2', type_line: 'Creature � Zombie Wizard', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonz', ownerId: 'p1', controller: 'p1', name: 'Elf', type_line: 'Creature � Elf', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqZombie' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetZombie') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one plus the number of artifacts you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is one plus the number of artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'a1', ownerId: 'p1', controller: 'p1', name: 'Relic', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is 2 plus the number of creatures and/or artifacts you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is 2 plus the number of creatures and/or artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetAndOrPlus', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactAndOrPlus', ownerId: 'p1', controller: 'p1', name: 'Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAndOrPlus') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is 2 plus the number of creatures and/or spacecraft you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is 2 plus the number of creatures and artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];
    const first = steps[0] as any;
    if (first?.condition?.kind === 'where') {
      first.condition.raw = 'x is 2 plus the number of creatures and/or spacecraft you control';
    }

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetAndOrSpacecraft', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'spacecraftAndOr', ownerId: 'p1', controller: 'p1', name: 'Skyhulk', type_line: 'Artifact � Spacecraft', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAndOrSpacecraft') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is twice the number of artifacts your opponents control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is twice the number of artifacts your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oa1', ownerId: 'p2', controller: 'p2', name: 'Opp Relic 1', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oa2', ownerId: 'p2', controller: 'p2', name: 'Opp Relic 2', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of white creatures target opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of white creatures target opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqWhiteOppCreatures', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact — Equipment', type_line: 'Artifact — Equipment', attachedTo: 'targetWhiteOppCreatures', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetWhiteOppCreatures', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whiteOpp1', ownerId: 'p2', controller: 'p2', name: 'Spirit', type_line: 'Creature — Spirit', colors: ['W'], power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whiteOpp2', ownerId: 'p2', controller: 'p2', name: 'Knight', type_line: 'Creature — Knight', colors: ['W', 'U'], power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonWhiteOpp', ownerId: 'p2', controller: 'p2', name: 'Goblin', type_line: 'Creature — Goblin', colors: ['R'], power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whiteControllerCreature', ownerId: 'p1', controller: 'p1', name: 'Soldier', type_line: 'Creature — Soldier', colors: ['W'], power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqWhiteOppCreatures',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetWhiteOppCreatures') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is twice the number of white creatures that player controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is twice the number of white creatures that player controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqWhitePlayerCreatures', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact — Equipment', type_line: 'Artifact — Equipment', attachedTo: 'targetWhitePlayerCreatures', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetWhitePlayerCreatures', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whitePlayer1', ownerId: 'p2', controller: 'p2', name: 'Cleric', type_line: 'Creature — Cleric', colors: ['W'], power: 1, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whitePlayer2', ownerId: 'p2', controller: 'p2', name: 'Angel', type_line: 'Creature — Angel', colors: ['W'], power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonWhitePlayer', ownerId: 'p2', controller: 'p2', name: 'Zombie', type_line: 'Creature — Zombie', colors: ['B'], power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqWhitePlayerCreatures',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetWhitePlayerCreatures') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of artifacts on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of artifacts on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'a1', ownerId: 'p2', controller: 'p2', name: 'Relic', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'a2', ownerId: 'p1', controller: 'p1', name: 'Bauble', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in your hand minus 1', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in your hand minus 1.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in your hand', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in your hand.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }, { id: 'h4' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creatureHandCount', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureHandCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in their hand', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in their hand.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureTheirHandCount', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureTheirHandCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the number of cards in target opponent's hand", () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in target opponent's hand.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }, { id: 'h4' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureTargetOppHandCount', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureTargetOppHandCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the number of cards in target opponent's hand minus 1", () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in target opponent's hand minus 1.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }, { id: 'h4' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureTargetOppHandMinusOne', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureTargetOppHandMinusOne') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the number of creature cards in target opponent's graveyard", () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creature cards in target opponent's graveyard.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [
            { id: 'gCreature1', name: 'Bear Cub', type_line: 'Creature � Bear' },
            { id: 'gCreature2', name: 'Goblin', type_line: 'Creature � Goblin' },
            { id: 'gNonCreature', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creatureTargetOppGyCount', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureTargetOppGyCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is 3 minus the number of cards in their hand', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is 3 minus the number of cards in their hand.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureMinusTheirHand', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureMinusTheirHand') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one minus the number of cards in their hand', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is one minus the number of cards in their hand.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureOneMinusTheirHand', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureOneMinusTheirHand') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(-1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in their hand minus 4', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in their hand minus 4.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }, { id: 'h4' }, { id: 'h5' }, { id: 'h6' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureTheirHandMinusFour', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureTheirHandMinusFour') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the number of cards in your hand minus the number of cards in that player's hand", () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in your hand minus the number of cards in that player's hand.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [{ id: 'h1' }, { id: 'h2' }, { id: 'h3' }, { id: 'h4' }, { id: 'h5' }], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'o1' }, { id: 'o2' }, { id: 'o3' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureHandDifference', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureHandDifference') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your life total', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your life total.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 17, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(17);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your speed', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your speed.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, speed: 4, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureSpeed', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureSpeed') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of experience counters you have', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of experience counters you have.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, experienceCounters: 5, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureExperience', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureExperience') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of times this creature has mutated', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of times this creature has mutated.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'creatureMutated',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Mutated Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          mutationCount: 3,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'creatureMutated' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureMutated') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the result', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the result.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureResult', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      lastDieRollByPlayer: {
        p1: { sides: 20, result: 13, timestamp: 1 },
      },
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureResult') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(13);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the result minus 1', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the result minus 1.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureResultMinus', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      lastDieRollByPlayer: {
        p1: { sides: 6, result: 5, timestamp: 1 },
      },
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureResultMinus') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of lands you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creatureLandCount', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land1', ownerId: 'p1', controller: 'p1', name: 'Plains', cardType: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land2', ownerId: 'p1', controller: 'p1', name: 'Island', cardType: 'Basic Land � Island', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land3', ownerId: 'p1', controller: 'p1', name: 'Mountain', cardType: 'Basic Land � Mountain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactNotLand', ownerId: 'p1', controller: 'p1', name: 'Signet', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureLandCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of +1/+1 counters on it', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of +1/+1 counters on it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'creatureCounterTarget',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Hydra',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: { '+1/+1': 2 },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'creatureCounterTarget' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creatureCounterTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among creatures you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the greatest power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Big', cardType: 'Creature', power: 5, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifact1', ownerId: 'p1', controller: 'p1', name: 'Relic', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the greatest mana value among permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppPerm1', ownerId: 'p2', controller: 'p2', name: 'Relic', cardType: 'Artifact', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppPerm2', ownerId: 'p2', controller: 'p2', name: 'Titanic Ward', cardType: 'Enchantment', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'blade1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'creature1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature1', ownerId: 'p2', controller: 'p2', name: 'Opp One', cardType: 'Creature', power: 3, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature2', ownerId: 'p2', controller: 'p2', name: 'Opp Two', cardType: 'Creature', power: 1, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'blade1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among creatures your opponents control with target creature you control selector', () => {
    const ir = parseOracleTextToIR(
      'Target creature you control gets +X/+0 until end of turn where X is the greatest toughness among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature1', ownerId: 'p2', controller: 'p2', name: 'Opp One', cardType: 'Creature', power: 3, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature2', ownerId: 'p2', controller: 'p2', name: 'Opp Two', cardType: 'Creature', power: 1, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt with target creature your opponents control selector', () => {
    const ir = parseOracleTextToIR(
      'Target creature your opponents control gets +X/+0 until end of turn where X is the greatest power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'p1Creature1', ownerId: 'p1', controller: 'p1', name: 'Big Ally', cardType: 'Creature', power: 5, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p1Creature2', ownerId: 'p1', controller: 'p1', name: 'Small Ally', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature1', ownerId: 'p2', controller: 'p2', name: 'Target Opponent Creature', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'oppCreature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt with target creature an opponent controls selector', () => {
    const ir = parseOracleTextToIR(
      'Target creature an opponent controls gets +X/+0 until end of turn where X is the greatest power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'p1Creature1', ownerId: 'p1', controller: 'p1', name: 'Big Ally', cardType: 'Creature', power: 6, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature1', ownerId: 'p2', controller: 'p2', name: 'Target Opponent Creature', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'oppCreature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'Target creature your opponents control gets +X/+0 until end of turn where X is the greatest power among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppCreature1', ownerId: 'p2', controller: 'p2', name: 'Opp Big Power', cardType: 'Creature', power: 8, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'oppCreature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among other creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among other creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eq1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'target1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally1', ownerId: 'p1', controller: 'p1', name: 'Ally One', cardType: 'Creature', power: 3, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally2', ownerId: 'p1', controller: 'p1', name: 'Ally Two', cardType: 'Creature', power: 1, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eq1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among other creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among other creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOpp1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'oppTarget', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p1Creature1', ownerId: 'p1', controller: 'p1', name: 'Ally', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppTarget', ownerId: 'p2', controller: 'p2', name: 'Target Opponent Creature', cardType: 'Creature', power: 9, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppOther1', ownerId: 'p2', controller: 'p2', name: 'Other Opponent Creature', cardType: 'Creature', power: 6, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppOther2', ownerId: 'p2', controller: 'p2', name: 'Other Opponent Creature 2', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqOpp1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'oppTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among other permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among other permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSelf1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetSelf1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSelf1', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', manaValue: 9, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyPerm1', ownerId: 'p1', controller: 'p1', name: 'Ally Perm 1', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyPerm2', ownerId: 'p1', controller: 'p1', name: 'Ally Perm 2', cardType: 'Enchantment', manaValue: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqSelf1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSelf1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among other permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among other permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOpp2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetOpp2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOpp2', ownerId: 'p2', controller: 'p2', name: 'Target Opponent Creature', cardType: 'Creature', manaValue: 11, power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppPermA', ownerId: 'p2', controller: 'p2', name: 'Opponent Perm A', cardType: 'Artifact', manaValue: 8, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppPermB', ownerId: 'p2', controller: 'p2', name: 'Opponent Perm B', cardType: 'Enchantment', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqOpp2',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOpp2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqBattle1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetBattle1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetBattle1', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', manaValue: 9, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyBattle1', ownerId: 'p1', controller: 'p1', name: 'Ally Permanent', cardType: 'Enchantment', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppBattle1', ownerId: 'p2', controller: 'p2', name: 'Opponent Permanent', cardType: 'Artifact', manaValue: 12, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqBattle1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetBattle1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(12);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among other permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among other permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqBattle2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 2, attachedTo: 'targetBattle2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetBattle2', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 15, power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyBattle2', ownerId: 'p1', controller: 'p1', name: 'Ally Permanent', cardType: 'Enchantment', manaValue: 8, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppBattle2', ownerId: 'p2', controller: 'p2', name: 'Opponent Permanent', cardType: 'Artifact', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqBattle2',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetBattle2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqGlobalPower1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetGlobalPower1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetGlobalPower1', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyGlobalPower1', ownerId: 'p1', controller: 'p1', name: 'Ally Creature', cardType: 'Creature', power: 7, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppGlobalPower1', ownerId: 'p2', controller: 'p2', name: 'Opponent Creature', cardType: 'Creature', power: 11, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqGlobalPower1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetGlobalPower1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(11);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective creatures in greatest-power battlefield where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqEffectiveGlobalPower', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', type_line: 'Artifact', attachedTo: 'targetEffectiveGlobalPower', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetEffectiveGlobalPower', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'printedGlobalPower', ownerId: 'p2', controller: 'p2', name: 'Printed Creature', cardType: 'Creature', type_line: 'Creature', power: 6, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveGlobalPower', ownerId: 'p2', controller: 'p2', name: 'Animated Colossus', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Artifact', 'Creature'], power: 13, toughness: 13, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqEffectiveGlobalPower',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveGlobalPower') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(13);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among other creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among other creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqGlobalTough1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetGlobalTough1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetGlobalTough1', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', power: 9, toughness: 13, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyGlobalTough1', ownerId: 'p1', controller: 'p1', name: 'Ally Creature', cardType: 'Creature', power: 4, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppGlobalTough1', ownerId: 'p2', controller: 'p2', name: 'Opponent Creature', cardType: 'Creature', power: 3, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqGlobalTough1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetGlobalTough1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among non-Human creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among non-Human creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNH1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNH1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNH1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonHumanBig1', ownerId: 'p1', controller: 'p1', name: 'Non-Human Big', cardType: 'Creature', type_line: 'Creature � Beast', power: 7, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanBig1', ownerId: 'p1', controller: 'p1', name: 'Human Big', cardType: 'Creature Human', type_line: 'Creature � Human Knight', power: 10, toughness: 10, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNH1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNH1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among non-Human creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among non-Human creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNH2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'oppTargetNH1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'p1Support1', ownerId: 'p1', controller: 'p1', name: 'Support', cardType: 'Creature', type_line: 'Creature � Elf', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppTargetNH1', ownerId: 'p2', controller: 'p2', name: 'Target Opponent Creature', cardType: 'Creature', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonHumanBig1', ownerId: 'p2', controller: 'p2', name: 'Non-Human Big', cardType: 'Creature', type_line: 'Creature � Zombie', power: 3, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHumanBig1', ownerId: 'p2', controller: 'p2', name: 'Human Big', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 12, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNH2',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'oppTargetNH1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among other non-Human creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among other non-Human creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqONH1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetONH1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetONH1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 9, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonHumanONH1', ownerId: 'p1', controller: 'p1', name: 'Other Non-Human', cardType: 'Creature', type_line: 'Creature � Elf', power: 6, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanONH1', ownerId: 'p1', controller: 'p1', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Knight', power: 11, toughness: 11, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqONH1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetONH1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among other non-Human creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among other non-Human creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqONH2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetONH2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetONH2', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 3, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonHumanONH2', ownerId: 'p2', controller: 'p2', name: 'Other Non-Human', cardType: 'Creature', type_line: 'Creature � Zombie', power: 2, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanONH2', ownerId: 'p2', controller: 'p2', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 12, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqONH2',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetONH2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among nonartifact creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among nonartifact creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNA1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNA1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNA1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactCreatureNA1', ownerId: 'p1', controller: 'p1', name: 'Artifact Creature', cardType: 'Artifact Creature', type_line: 'Artifact Creature � Golem', power: 12, toughness: 12, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactCreatureNA1', ownerId: 'p1', controller: 'p1', name: 'Nonartifact Creature', cardType: 'Creature', type_line: 'Creature � Beast', power: 7, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNA1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNA1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among other nonland creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among other nonland creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNL1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNL1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNL1', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 3, toughness: 10, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landCreatureNL1', ownerId: 'p1', controller: 'p1', name: 'Land Creature', cardType: 'Land Creature', type_line: 'Land Creature � Dryad', power: 2, toughness: 15, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonLandCreatureNL1', ownerId: 'p2', controller: 'p2', name: 'Nonland Creature', cardType: 'Creature', type_line: 'Creature � Zombie', power: 4, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNL1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNL1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among nonartifact permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among nonartifact permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNAP1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetNAP1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNAP1', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', manaValue: 2, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactNAP1', ownerId: 'p1', controller: 'p1', name: 'Big Artifact', cardType: 'Artifact', manaValue: 12, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactNAP1', ownerId: 'p1', controller: 'p1', name: 'Big Enchantment', cardType: 'Enchantment', manaValue: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNAP1',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNAP1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of nonartifact creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of nonartifact creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqNC1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactCreatureNC1', ownerId: 'p1', controller: 'p1', name: 'Artifact Creature', cardType: 'Artifact Creature', type_line: 'Artifact Creature � Golem', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'plainCreatureNC1', ownerId: 'p1', controller: 'p1', name: 'Plain Creature', cardType: 'Creature', type_line: 'Creature � Beast', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonhuman creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonhuman creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC2', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonHumanNC2', ownerId: 'p2', controller: 'p2', name: 'Other Nonhuman', cardType: 'Creature', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanNC2', ownerId: 'p2', controller: 'p2', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of nonartifact permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of nonartifact permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC3', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactNC3', ownerId: 'p2', controller: 'p2', name: 'Artifact', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactNC3A', ownerId: 'p1', controller: 'p1', name: 'Enchantment', cardType: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactNC3B', ownerId: 'p2', controller: 'p2', name: 'Land', cardType: 'Land', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC3' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonland creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonland creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC4', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC4', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC4', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonlandCreatureNC4', ownerId: 'p2', controller: 'p2', name: 'Other Nonland Creature', cardType: 'Creature', type_line: 'Creature � Zombie', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landCreatureNC4', ownerId: 'p2', controller: 'p2', name: 'Land Creature', cardType: 'Land Creature', type_line: 'Land Creature � Elemental', power: 8, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC4' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC4') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonartifact permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonartifact permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqNC5', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC5', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC5', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactPermNC5', ownerId: 'p1', controller: 'p1', name: 'Artifact', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactPermNC5', ownerId: 'p1', controller: 'p1', name: 'Enchantment', cardType: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC5' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC5') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonartifact permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonartifact permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC6', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC6', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC6', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtifactPermNC6', ownerId: 'p2', controller: 'p2', name: 'Opp Artifact', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonArtifactPermNC6', ownerId: 'p2', controller: 'p2', name: 'Opp Enchantment', cardType: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC6' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC6') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonhuman creatures an opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonhuman creatures an opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC6A', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC6A', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC6A', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonHumanNC6A', ownerId: 'p2', controller: 'p2', name: 'Opp Nonhuman', cardType: 'Creature', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHumanNC6A', ownerId: 'p2', controller: 'p2', name: 'Opp Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC6A' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC6A') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other nonartifact permanents you do not control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other nonartifact permanents you do not control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC6B', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC6B', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC6B', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtifactNC6B', ownerId: 'p2', controller: 'p2', name: 'Opp Artifact', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonArtifactNC6B', ownerId: 'p2', controller: 'p2', name: 'Opp Enchantment', cardType: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC6B' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC6B') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X uses hyphenated non-human count wording', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of non-human creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqNC7', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC7', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC7', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonHumanNC7', ownerId: 'p1', controller: 'p1', name: 'Other Nonhuman', cardType: 'Creature', type_line: 'Creature � Beast', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanNC7', ownerId: 'p1', controller: 'p1', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC7' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC7') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X uses spaced non human count wording', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of non human creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC8', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC8', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC8', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Zombie', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherNonHumanNC8', ownerId: 'p2', controller: 'p2', name: 'Other Nonhuman', cardType: 'Creature', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanNC8', ownerId: 'p2', controller: 'p2', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC8' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC8') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X uses spaced non artifact permanent wording', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of non artifact permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNC9', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNC9', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNC9', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactNC9', ownerId: 'p2', controller: 'p2', name: 'Artifact', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactNC9A', ownerId: 'p1', controller: 'p1', name: 'Enchantment', cardType: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactNC9B', ownerId: 'p2', controller: 'p2', name: 'Land', cardType: 'Land', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNC9' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNC9') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least power among creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 5, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallLeast1', ownerId: 'p1', controller: 'p1', name: 'Small', cardType: 'Creature', power: 1, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigLeast1', ownerId: 'p1', controller: 'p1', name: 'Big', cardType: 'Creature', power: 8, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least toughness among creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least toughness among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast2', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSmallLeast2', ownerId: 'p2', controller: 'p2', name: 'Opp Small', cardType: 'Creature', power: 7, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppBigLeast2', ownerId: 'p2', controller: 'p2', name: 'Opp Big', cardType: 'Creature', power: 2, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least power among creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least power among creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast3', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 5, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSmallLeast3', ownerId: 'p2', controller: 'p2', name: 'Opp Small', cardType: 'Creature', power: 0, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppBigLeast3', ownerId: 'p2', controller: 'p2', name: 'Opp Big', cardType: 'Creature', power: 6, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast3' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(0);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast4', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 3, attachedTo: 'targetLeast4', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast4', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 5, power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'permLowLeast4', ownerId: 'p1', controller: 'p1', name: 'Low Perm', cardType: 'Enchantment', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'permHighLeast4', ownerId: 'p1', controller: 'p1', name: 'High Perm', cardType: 'Artifact', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast4' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast4') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast5', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast5', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast5', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLowLeast5', ownerId: 'p2', controller: 'p2', name: 'Opp Low', cardType: 'Enchantment', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHighLeast5', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast5' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast5') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast6', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast6', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast6', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 4, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyLeast6', ownerId: 'p1', controller: 'p1', name: 'Ally Perm', cardType: 'Enchantment', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLeast6', ownerId: 'p2', controller: 'p2', name: 'Opp Perm', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast6' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast6') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least toughness among other creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least toughness among other creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast7', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast7', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast7', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallLeast7', ownerId: 'p1', controller: 'p1', name: 'Small', cardType: 'Creature', power: 1, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigLeast7', ownerId: 'p1', controller: 'p1', name: 'Big', cardType: 'Creature', power: 5, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast7' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast7') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least power among other creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least power among other creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast8', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast8', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast8', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSmallLeast8', ownerId: 'p2', controller: 'p2', name: 'Opp Small', cardType: 'Creature', power: 1, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppBigLeast8', ownerId: 'p2', controller: 'p2', name: 'Opp Big', cardType: 'Creature', power: 6, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast8' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast8') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least power among nonhuman creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least power among nonhuman creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast9', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast9', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast9', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 7, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonHumanSmallLeast9', ownerId: 'p1', controller: 'p1', name: 'Nonhuman Small', cardType: 'Creature', type_line: 'Creature � Beast', power: 1, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanLeast9', ownerId: 'p1', controller: 'p1', name: 'Human', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 0, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast9' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast9') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the least toughness among other non-Human creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the least toughness among other non-Human creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast10', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast10', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast10', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 5, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonHumanSmallLeast10', ownerId: 'p2', controller: 'p2', name: 'Opp Nonhuman Small', cardType: 'Creature', type_line: 'Creature � Zombie', power: 3, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHumanLeast10', ownerId: 'p2', controller: 'p2', name: 'Opp Human', cardType: 'Creature Human', type_line: 'Creature � Human Knight', power: 2, toughness: 0, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast10' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast10') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among other permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among other permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast11', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 3, attachedTo: 'targetLeast11', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast11', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 10, power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyLeast11', ownerId: 'p1', controller: 'p1', name: 'Ally Perm', cardType: 'Enchantment', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLeast11', ownerId: 'p2', controller: 'p2', name: 'Opp Perm', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast11' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast11') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among nonartifact permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among nonartifact permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast12', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast12', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast12', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 8, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactLeast12', ownerId: 'p1', controller: 'p1', name: 'Artifact', cardType: 'Artifact', manaValue: 0, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactLeast12A', ownerId: 'p1', controller: 'p1', name: 'Nonartifact A', cardType: 'Enchantment', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactLeast12B', ownerId: 'p1', controller: 'p1', name: 'Nonartifact B', cardType: 'Creature', manaValue: 5, power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast12' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast12') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the lowest mana value among other nonland permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest mana value among other nonland permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast13', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast13', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast13', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', manaValue: 9, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLandLeast13', ownerId: 'p2', controller: 'p2', name: 'Opp Land', cardType: 'Land', manaValue: 0, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonlandLeast13A', ownerId: 'p2', controller: 'p2', name: 'Opp Nonland A', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppNonlandLeast13B', ownerId: 'p2', controller: 'p2', name: 'Opp Nonland B', cardType: 'Enchantment', manaValue: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast13' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast13') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X uses lowest-power synonym among creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the lowest power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast14', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast14', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast14', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 7, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallLeast14', ownerId: 'p1', controller: 'p1', name: 'Small', cardType: 'Creature', power: 1, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast14' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast14') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one plus the lowest mana value among permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is one plus the lowest mana value among permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast15', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 3, attachedTo: 'targetLeast15', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast15', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 8, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'lowLeast15', ownerId: 'p2', controller: 'p2', name: 'Low', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highLeast15', ownerId: 'p1', controller: 'p1', name: 'High', cardType: 'Enchantment', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast15' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast15') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is twice the least toughness among creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is twice the least toughness among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast16', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast16', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast16', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLeast16A', ownerId: 'p2', controller: 'p2', name: 'Opp A', cardType: 'Creature', power: 4, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLeast16B', ownerId: 'p2', controller: 'p2', name: 'Opp B', cardType: 'Creature', power: 8, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast16' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast16') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is half the lowest mana value among permanents you control, rounded down', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is half the lowest mana value among permanents you control, rounded down.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqLeast17', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 3, attachedTo: 'targetLeast17', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast17', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 7, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'lowLeast17', ownerId: 'p1', controller: 'p1', name: 'Low', cardType: 'Enchantment', manaValue: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highLeast17', ownerId: 'p1', controller: 'p1', name: 'High', cardType: 'Artifact', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast17' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast17') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is half the least power among creatures on the battlefield, rounded up', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is half the least power among creatures on the battlefield, rounded up.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLeast18', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetLeast18', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLeast18', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 8, toughness: 8, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'lowLeast18', ownerId: 'p2', controller: 'p2', name: 'Low', cardType: 'Creature', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highLeast18', ownerId: 'p1', controller: 'p1', name: 'High', cardType: 'Creature', power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLeast18' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLeast18') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest power among creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest power among creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqHighest1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetHighest1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetHighest1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigHighest1', ownerId: 'p1', controller: 'p1', name: 'Big', cardType: 'Creature', power: 7, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallHighest1', ownerId: 'p1', controller: 'p1', name: 'Small', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqHighest1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetHighest1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one plus the highest mana value among permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is one plus the highest mana value among permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqHighest2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetHighest2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetHighest2', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 2, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'midHighest2', ownerId: 'p2', controller: 'p2', name: 'Mid', cardType: 'Enchantment', manaValue: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigHighest2', ownerId: 'p1', controller: 'p1', name: 'Big', cardType: 'Artifact', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqHighest2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetHighest2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(10);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest mana value among other nonartifact permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest mana value among other nonartifact permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqHighest3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetHighest3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetHighest3', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 10, power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactHighest3', ownerId: 'p1', controller: 'p1', name: 'Big Artifact', cardType: 'Artifact', manaValue: 15, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'enchantmentHighest3', ownerId: 'p2', controller: 'p2', name: 'Big Enchantment', cardType: 'Enchantment', manaValue: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqHighest3',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetHighest3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest toughness among creatures your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest toughness among creatures your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSmallest1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetSmallest1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSmallest1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSmallest1A', ownerId: 'p2', controller: 'p2', name: 'Opp A', cardType: 'Creature', power: 6, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSmallest1B', ownerId: 'p2', controller: 'p2', name: 'Opp B', cardType: 'Creature', power: 4, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSmallest1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSmallest1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is twice the smallest mana value among permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is twice the smallest mana value among permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqSmallest2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 2, attachedTo: 'targetSmallest2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSmallest2', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 8, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'lowSmallest2', ownerId: 'p1', controller: 'p1', name: 'Low', cardType: 'Enchantment', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highSmallest2', ownerId: 'p1', controller: 'p1', name: 'High', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSmallest2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSmallest2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest mana value among other nonland permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest mana value among other nonland permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSmallest3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetSmallest3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSmallest3', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 6, power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landSmallest3', ownerId: 'p2', controller: 'p2', name: 'Big Land', cardType: 'Land', manaValue: 0, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallestNonland3', ownerId: 'p2', controller: 'p2', name: 'Small Enchantment', cardType: 'Enchantment', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'biggerNonland3', ownerId: 'p1', controller: 'p1', name: 'Big Artifact', cardType: 'Artifact', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqSmallest3',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSmallest3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest converted mana cost among permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest converted mana cost among permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqCmc1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetCmc1', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetCmc1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 3, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highCmc1', ownerId: 'p2', controller: 'p2', name: 'High', cardType: 'Enchantment', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqCmc1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCmc1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one plus the highest converted mana cost among permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is one plus the highest converted mana cost among permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any],
      battlefield: [
        { id: 'eqCmc2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetCmc2', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetCmc2', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 4, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'highCmc2', ownerId: 'p1', controller: 'p1', name: 'High', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqCmc2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCmc2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest converted mana cost among nonartifact permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest converted mana cost among nonartifact permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqCmc3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetCmc3', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetCmc3', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 6, power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtifactCmc3', ownerId: 'p2', controller: 'p2', name: 'Artifact', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppEnchantmentCmc3', ownerId: 'p2', controller: 'p2', name: 'Enchantment', cardType: 'Enchantment', manaValue: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqCmc3',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCmc3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest power among creatures you don\'t control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest power among creatures you don\'t control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOppAlias1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias1', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHigh1', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Creature', power: 8, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLow1', ownerId: 'p2', controller: 'p2', name: 'Opp Low', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest toughness among creatures an opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest toughness among creatures an opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOppAlias2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias2', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppA2', ownerId: 'p2', controller: 'p2', name: 'Opp A', cardType: 'Creature', power: 5, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppB2', ownerId: 'p2', controller: 'p2', name: 'Opp B', cardType: 'Creature', power: 3, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is one plus the highest converted mana cost among permanents you don\'t control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is one plus the highest converted mana cost among permanents you don\'t control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetOppAlias3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias3', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 4, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHighCmc3', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias3' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest power among creatures you do not control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest power among creatures you do not control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias4', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOppAlias4', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias4', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHigh4', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Creature', power: 9, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias4' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias4') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest converted mana cost among permanents you do not control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest converted mana cost among permanents you do not control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias5', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetOppAlias5', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias5', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', manaValue: 4, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLow5', ownerId: 'p2', controller: 'p2', name: 'Opp Low', cardType: 'Enchantment', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHigh5', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Artifact', manaValue: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias5' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias5') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the highest power among other creatures you do not control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the highest power among other creatures you do not control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias6', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOppAlias6', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias6', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', power: 10, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppHigh6', ownerId: 'p2', controller: 'p2', name: 'Opp High', cardType: 'Creature', power: 6, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppLow6', ownerId: 'p2', controller: 'p2', name: 'Opp Low', cardType: 'Creature', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias6' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias6') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the smallest converted mana cost among other nonartifact permanents an opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the smallest converted mana cost among other nonartifact permanents an opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppAlias7', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOppAlias7', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppAlias7', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 1, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtifact7', ownerId: 'p2', controller: 'p2', name: 'Opp Artifact', cardType: 'Artifact', manaValue: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppEnchantment7', ownerId: 'p2', controller: 'p2', name: 'Opp Enchantment', cardType: 'Enchantment', manaValue: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppAlias7' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppAlias7') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among nonland permanents your opponents control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among nonland permanents your opponents control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNLP1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetNLP1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNLP1', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 3, power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landNLP1', ownerId: 'p2', controller: 'p2', name: 'Big Land', cardType: 'Land', manaValue: 15, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonLandNLP1', ownerId: 'p2', controller: 'p2', name: 'Big Nonland', cardType: 'Artifact', manaValue: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNLP1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNLP1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among other nonartifact permanents on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among other nonartifact permanents on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqONAP1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetONAP1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetONAP1', ownerId: 'p2', controller: 'p2', name: 'Target Creature', cardType: 'Creature', manaValue: 10, power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactONAP1', ownerId: 'p1', controller: 'p1', name: 'Big Artifact', cardType: 'Artifact', manaValue: 14, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactONAP1', ownerId: 'p2', controller: 'p2', name: 'Big Enchantment', cardType: 'Enchantment', manaValue: 8, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqONAP1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetONAP1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest power among nonhuman creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest power among nonhuman creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNH3', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNH3', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNH3', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature � Elf', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonHumanBig3', ownerId: 'p1', controller: 'p1', name: 'Nonhuman Big', cardType: 'Creature', type_line: 'Creature � Beast', power: 6, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanBig3', ownerId: 'p1', controller: 'p1', name: 'Human Big', cardType: 'Creature Human', type_line: 'Creature � Human Knight', power: 11, toughness: 11, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNH3',
      selectorContext: { targetPlayerId: 'p1' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNH3') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest toughness among non-Human creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest toughness among non-Human creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNH4', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetNH4', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNH4', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonHumanBig4', ownerId: 'p1', controller: 'p1', name: 'Nonhuman Big', cardType: 'Creature', type_line: 'Creature � Dragon', power: 4, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'humanBig4', ownerId: 'p2', controller: 'p2', name: 'Human Big', cardType: 'Creature Human', type_line: 'Creature � Human Soldier', power: 8, toughness: 13, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqNH4',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNH4') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creature cards in all graveyards', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creature cards in all graveyards.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [{ id: 'g1', type_line: 'Creature � Elf' }, { id: 'g2', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [{ id: 'g3', type_line: 'Creature � Human' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in that player\'s graveyard', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in that player's graveyard.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [{ id: 'g1' }, { id: 'g2' }, { id: 'g3' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creature cards in their graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creature cards in their graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [{ id: 'g1', type_line: 'Creature � Elf' }, { id: 'g2', type_line: 'Instant' }, { id: 'g3', type_line: 'Creature � Human' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is this permanent\'s power', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is this permanent's power.",
      'Test Creature'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Creature', cardType: 'Creature', power: 4, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'creature1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creature cards in its controller\'s graveyard', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creature cards in its controller's graveyard.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [{ id: 'p1g1', type_line: 'Creature � Elf' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [
            { id: 'p2g1', type_line: 'Creature � Zombie' },
            { id: 'p2g2', type_line: 'Creature � Human' },
            { id: 'p2s1', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'sourceItsControllerGy', ownerId: 'p2', controller: 'p2', name: 'Source Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetItsControllerGy', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sourceItsControllerGy' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetItsControllerGy') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt for Consuming Aberration style opponents-graveyards wording', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+X until end of turn where X is the number of cards in your opponents' graveyards.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [{ id: 'self1' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [{ id: 'g1' }, { id: 'g2' }],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }],
          hand: [],
          graveyard: [{ id: 'g3' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(3);
  });

  it('applies X-based modify_pt where X is the number of cards in all players\' hands', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in all players' hands.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [{ id: 'h1' }, { id: 'h2' }], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'h3' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of card types among cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of card types among cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'gType1', name: 'Shock', type_line: 'Instant' },
            { id: 'gType2', name: 'Bear', type_line: 'Creature � Bear' },
            { id: 'gType3', name: 'Signet', type_line: 'Artifact' },
            { id: 'gType4', name: 'Aquitect\'s Will', type_line: 'Kindred Sorcery — Merfolk' },
            { id: 'gType5', name: 'Another Artifact', type_line: 'Artifact' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetTypeCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetTypeCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in all graveyards with the same name as that spell', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in all graveyards with the same name as that spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sName1', name: 'Shock', type_line: 'Instant' },
            { id: 'sName2', name: 'Opt', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [
            { id: 'sName3', name: 'Shock', type_line: 'Instant' },
            { id: 'sName4', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      stack: [
        { id: 'spellNameRef', type: 'spell', cardName: 'Shock', spell: { name: 'Shock' } } as any,
      ],
      battlefield: [
        { id: 'targetSameName', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellNameRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSameName') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in all graveyards with the same name as the spell (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in all graveyards with the same name as the spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'g1', name: 'Shock', type_line: 'Instant' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [{ id: 'g3', name: 'Bolt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'spellAliasTarget', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'spellAliasSource', ownerId: 'p1', controller: 'p1', name: 'Shock', cardType: 'Instant', manaCost: '{R}' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'spellAliasSource',
      selectorContext: { targetCreatureId: 'spellAliasTarget' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'spellAliasTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you gained this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life you gained this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 6 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeGain', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeGain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeGain', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeGain' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeGain') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents have gained this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents have gained this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 2, p2: 3, p3: 5 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeGain', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeGain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeGain', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeGain' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeGain') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents gained this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents gained this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 2, p2: 4, p3: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeGainedNoHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeGainedNoHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeGainedNoHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeGainedNoHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeGainedNoHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents gained', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents gained.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 2, p2: 6, p3: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppGainedNoTurnNoHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppGainedNoTurnNoHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppGainedNoTurnNoHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppGainedNoTurnNoHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppGainedNoTurnNoHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you gained', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life you gained.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 7 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeGainNoTurn', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeGainNoTurn', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeGainNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeGainNoTurn' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeGainNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it.each([
    ["x is the amount of life opponents have gained this turn", 7],
    ["x is the amount of life opponents gained this turn", 7],
    ["x is the amount of life opponents have gained", 7],
    ["x is the amount of life opponents gained", 7],
    ["x is the amount of life you have lost this turn", 4],
    ["x is the amount of life you have lost", 4],
    ["x is the amount of life opponents lost this turn", 8],
    ["x is the amount of life opponents lost", 8],
    ["x is the total amount of life your opponents lost this turn", 8],
    ["x is the total amount of life your opponents lost", 8],
  ])('applies X-based modify_pt where %s (alias batch)', (wherePhrase, expectedPower) => {
    const ir = parseOracleTextToIR(
      `The creature gets +X/+0 until end of turn where ${wherePhrase}.`,
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 3, p2: 2, p3: 5 },
      lifeLostThisTurn: { p1: 4, p2: 6, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAliasLifeBatch', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetAliasLifeBatch', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAliasLifeBatch', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAliasLifeBatch' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAliasLifeBatch') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(expectedPower);
    expect(ptMod.toughness).toBe(0);
  });

  it.each([
    ["x is the number of spells opponents have cast this turn", 7],
    ["x is the number of lands opponents have played this turn", 5],
    ["x is the number of lands opponents played this turn", 5],
    ["x is the number of cards opponents have drawn this turn", 8],
    ["x is the number of cards opponents drew this turn", 8],
    ["x is the number of cards opponents have discarded this turn", 6],
    ["x is the number of cards opponents discarded this turn", 6],
    ["x is the number of permanents opponents have sacrificed this turn", 7],
  ])('applies X-based modify_pt where %s (opponent-activity alias batch)', (wherePhrase, expectedPower) => {
    const ir = parseOracleTextToIR(
      `The creature gets +X/+0 until end of turn where ${wherePhrase}.`,
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      spellsCastThisTurn: { p1: 1, p2: 3, p3: 4 },
      landsPlayedThisTurn: { p1: 1, p2: 2, p3: 3 },
      cardsDrawnThisTurn: { p1: 2, p2: 3, p3: 5 },
      cardsDiscardedThisTurn: { p1: 1, p2: 2, p3: 4 },
      permanentsSacrificedThisTurn: { p1: 2, p2: 1, p3: 6 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAliasOppActivityBatch', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetAliasOppActivityBatch', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAliasOppActivityBatch', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAliasOppActivityBatch' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAliasOppActivityBatch') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(expectedPower);
    expect(ptMod.toughness).toBe(0);
  });

  it.each([
    ["x is the number of cards in target opponent's graveyard", 3],
    ["x is the number of cards in target opponent's library", 4],
    ["x is the number of cards in target opponent's exile", 2],
  ])('applies X-based modify_pt where %s (target-opponent zone alias batch)', (wherePhrase, expectedPower) => {
    const ir = parseOracleTextToIR(
      `The creature gets +X/+0 until end of turn where ${wherePhrase}.`,
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }, { id: 'p2l2' }, { id: 'p2l3' }, { id: 'p2l4' }], hand: [], graveyard: [{ id: 'p2g1' }, { id: 'p2g2' }, { id: 'p2g3' }], exile: [{ id: 'p2e1' }, { id: 'p2e2' }] } as any,
      ],
      battlefield: [
        { id: 'eqAliasOppZoneBatch', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetAliasOppZoneBatch', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAliasOppZoneBatch', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqAliasOppZoneBatch',
      selectorContext: { targetOpponentId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAliasOppZoneBatch') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(expectedPower);
    expect(ptMod.toughness).toBe(0);
  });

  it.each([
    ["x is the mana value of this spell", 6],
    ["x is this spell's mana value", 6],
    ["x is the mana value of this card", 6],
    ["x is this card's mana value", 6],
    ["x is the mana value of that card", 6],
    ["x is the card's mana value", 6],
  ])('applies X-based modify_pt where %s (spell/card mana-value alias batch)', (wherePhrase, expectedPower) => {
    const ir = parseOracleTextToIR(
      `Target creature gets +X/+0 until end of turn where ${wherePhrase}.`,
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellAliasBatchRef', type: 'spell', manaValue: 6, spell: { manaValue: 6 } } as any,
      ],
      battlefield: [
        { id: 'targetAliasSpellMvBatch', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellAliasBatchRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAliasSpellMvBatch') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(expectedPower);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you have gained this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life you have gained this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 6 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeGainedHaveAlias', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeGainedHaveAlias', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeGainedHaveAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeGainedHaveAlias' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeGainedHaveAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you\'ve gained this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the amount of life you've gained this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeGainedThisTurn: { p1: 5 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeGainedApostropheAlias', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeGainedApostropheAlias', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeGainedApostropheAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeGainedApostropheAlias' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeGainedApostropheAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the damage dealt to your opponents this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the damage dealt to your opponents this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      damageTakenThisTurnByPlayer: { p1: 2, p2: 3, p3: 5 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDamageOpps', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDamageOpps', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDamageOpps', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDamageOpps' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDamageOpps') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents have lost this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents have lost this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 1, p2: 4, p3: 7 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeLost', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeLost', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeLost', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeLost' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeLost') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(11);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total amount of life your opponents have lost this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the total amount of life your opponents have lost this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 2, p2: 6, p3: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeLostTotal', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeLostTotal', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeLostTotal', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeLostTotal' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeLostTotal') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents lost this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents lost this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 1, p2: 5, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeLostNoHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeLostNoHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeLostNoHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeLostNoHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeLostNoHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents have lost', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents have lost.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 2, p2: 3, p3: 6 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLifeLostNoTurn', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLifeLostNoTurn', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLifeLostNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLifeLostNoTurn' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeLostNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life your opponents lost', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life your opponents lost.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 1, p2: 4, p3: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLostNoHaveNoTurn', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLostNoHaveNoTurn', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLostNoHaveNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLostNoHaveNoTurn' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLostNoHaveNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you lost this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the amount of life you lost this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 5 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeLost', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeLost', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeLost', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeLost' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeLost') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life you\'ve lost this turn', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the amount of life you've lost this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      lifeLostThisTurn: { p1: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLifeLostApos', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLifeLostApos', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLifeLostApos', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLifeLostApos' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLifeLostApos') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards you\'ve discarded this turn', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the number of cards you've discarded this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDiscardedThisTurn: { p1: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDiscardedTurnApos', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDiscardedTurnApos', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDiscardedTurnApos', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDiscardedTurnApos' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiscardedTurnApos') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards you have discarded this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards you have discarded this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDiscardedThisTurn: { p1: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDiscardedTurnHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDiscardedTurnHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDiscardedTurnHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDiscardedTurnHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiscardedTurnHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards you\'ve drawn this turn', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the number of cards you've drawn this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDrawnThisTurn: { p1: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDrawnTurnApos', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDrawnTurnApos', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDrawnTurnApos', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDrawnTurnApos' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDrawnTurnApos') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards you drew this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards you drew this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDrawnThisTurn: { p1: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDrewTurn', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDrewTurn', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDrewTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDrewTurn' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDrewTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards your opponents have drawn this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards your opponents have drawn this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDrawnThisTurn: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppDrawnHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppDrawnHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppDrawnHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppDrawnHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDrawnHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards your opponents drew this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards your opponents drew this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDrawnThisTurn: { p1: 0, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppDrew', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppDrew', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppDrew', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppDrew' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDrew') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of spells you\'ve cast this turn', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the number of spells you've cast this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      spellsCastThisTurn: { p1: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSpellsCastApos', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetSpellsCastApos', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSpellsCastApos', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSpellsCastApos' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellsCastApos') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of spells you cast this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of spells you cast this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      spellsCastThisTurn: { p1: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSpellsCast', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetSpellsCast', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSpellsCast', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSpellsCast' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellsCast') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of spells your opponents have cast this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of spells your opponents have cast this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      spellsCastThisTurn: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppSpellsCastHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppSpellsCastHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppSpellsCastHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppSpellsCastHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSpellsCastHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of spells your opponents cast this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of spells your opponents cast this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      spellsCastThisTurn: { p1: 0, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppSpellsCast', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppSpellsCast', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppSpellsCast', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppSpellsCast' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSpellsCast') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands you\'ve played this turn', () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the number of lands you've played this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      landsPlayedThisTurn: { p1: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLandsPlayedApos', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLandsPlayedApos', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLandsPlayedApos', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLandsPlayedApos' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLandsPlayedApos') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands you played this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of lands you played this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      landsPlayedThisTurn: { p1: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLandsPlayed', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLandsPlayed', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLandsPlayed', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLandsPlayed' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLandsPlayed') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands your opponents have played this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of lands your opponents have played this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      landsPlayedThisTurn: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLandsPlayedHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLandsPlayedHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLandsPlayedHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLandsPlayedHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLandsPlayedHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of lands your opponents played this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of lands your opponents played this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      landsPlayedThisTurn: { p1: 0, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppLandsPlayed', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppLandsPlayed', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppLandsPlayed', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppLandsPlayed' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLandsPlayed') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards your opponents have discarded this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards your opponents have discarded this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDiscardedThisTurn: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppDiscardedHave', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppDiscardedHave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppDiscardedHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppDiscardedHave' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDiscardedHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards your opponents discarded this turn', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of cards your opponents discarded this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      cardsDiscardedThisTurn: { p1: 0, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOppDiscarded', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppDiscarded', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppDiscarded', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOppDiscarded' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDiscarded') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards revealed this way', () => {
    const ir = parseOracleTextToIR(
      'You reveal cards from the top of your library until you reveal a land card, then put those cards into your graveyard. Target creature gets +X/+0 until end of turn where X is the number of cards revealed this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(a => a?.steps ?? []);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'rev1', type_line: 'Instant' },
            { id: 'rev2', type_line: 'Creature � Elf' },
            { id: 'rev3', type_line: 'Land � Forest' },
            { id: 'rev4', type_line: 'Sorcery' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'eqRevealedThisWay', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetRevealedThisWay', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetRevealedThisWay', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqRevealedThisWay' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetRevealedThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures that died this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 2, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under your control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures that died under your control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 2, p2: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderYou', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderYou') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under your control this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures that died under your control this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 3, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderYouThisTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderYouThisTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under your opponents\' control', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creatures that died under your opponents' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderOpps', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderOpps') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under your opponents\' control this turn', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creatures that died under your opponents' control this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 2, p3: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderOppsThisTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderOppsThisTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under opponents\' control', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creatures that died under opponents' control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderOppsNoYour', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderOppsNoYour') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under an opponent\'s control this turn', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creatures that died under an opponent's control this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderAnOpp', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderAnOpp') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures that died under an opponent\'s control', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of creatures that died under an opponent's control.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 4, p3: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetDiedUnderAnOppNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiedUnderAnOppNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures you control that died this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures you control that died this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 3, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetYouDiedTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetYouDiedTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures you control that died', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures you control that died.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 2, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetYouDiedNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetYouDiedNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures your opponents control that died this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures your opponents control that died this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppDiedTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDiedTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures your opponents control that died', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of creatures your opponents control that died.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      creaturesDiedThisTurnByController: { p1: 1, p2: 2, p3: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppDiedNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppDiedNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents you\'ve sacrificed this turn', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of permanents you've sacrificed this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      permanentsSacrificedThisTurn: { p1: 4, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetSacThisTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacThisTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents you\'ve sacrificed', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of permanents you've sacrificed.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      permanentsSacrificedThisTurn: { p1: 3, p2: 1 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetSacNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents your opponents have sacrificed this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of permanents your opponents have sacrificed this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      permanentsSacrificedThisTurn: { p1: 1, p2: 2, p3: 4 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppSacHave', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSacHave') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents your opponents sacrificed this turn', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of permanents your opponents sacrificed this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      permanentsSacrificedThisTurn: { p1: 0, p2: 3, p3: 2 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppSac', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSac') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents your opponents sacrificed', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of permanents your opponents sacrificed.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      permanentsSacrificedThisTurn: { p1: 1, p2: 2, p3: 3 },
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppSacNoTurn', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSacNoTurn') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the sacrificed creature\'s power', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the sacrificed creature's power.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sacrificedCreature1', name: 'Sacrificed Beast', type_line: 'Creature � Beast', power: 5, toughness: 3, manaValue: 6 },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacPow', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sacrificedCreature1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacPow') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the sacrificed creature\'s toughness', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the sacrificed creature's toughness.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sacrificedCreatureToughness', name: 'Sacrificed Turtle', type_line: 'Creature � Turtle', power: 2, toughness: 7, manaValue: 5 },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacToughness', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sacrificedCreatureToughness' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacToughness') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the sacrificed creature\'s mana value', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the sacrificed creature's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sacrificedCreature2', name: 'Sacrificed Titan', type_line: 'Creature � Giant', power: 6, toughness: 6, manaValue: 8 },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sacrificedCreature2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(8);
    expect(ptMod.toughness).toBe(0);
  });

  it('uses pre-sacrifice last-known toughness for sacrificed-creature where-X evaluation', () => {
    const steps: any[] = [
      {
        kind: 'sacrifice',
        who: { kind: 'you' },
        what: { kind: 'raw', text: 'that creature' },
        raw: 'Sacrifice that creature.',
      },
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is the sacrificed creature's toughness" },
        raw: "Target creature gets +X/+0 until end of turn where X is the sacrificed creature's toughness.",
      },
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'sacLkiCreature',
          owner: 'p1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Huge Beast',
          cardType: 'Creature',
          basePower: 6,
          baseToughness: 6,
          power: 6,
          toughness: 6,
          counters: { '+1/+1': 50 },
          card: { id: 'cardHugeBeast', name: 'Huge Beast', type_line: 'Creature - Beast', power: '6', toughness: '6', manaValue: 6 },
          tapped: false,
          summoningSick: false,
        } as any,
        {
          id: 'targetSacLki',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Target Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'targetSacLki',
      selectorContext: { chosenObjectIds: ['sacLkiCreature'] } as any,
    });

    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacLki') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
    const graveyardCard = (((result.state as any).players || [])[0]?.graveyard || [])[0] as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['sacrifice', 'modify_pt']);
    expect(graveyardCard?.toughness).toBe('6');
    expect(ptMod.power).toBe(56);
    expect(ptMod.toughness).toBe(0);
  });

  it('uses pre-sacrifice last-known toughness for gain-life follow-ups', () => {
    const ir = parseOracleTextToIR(
      "Sacrifice that creature. You gain life equal to the sacrificed creature's toughness.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 10,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'sacLkiLifeCreature',
          owner: 'p1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Huge Beast',
          cardType: 'Creature',
          basePower: 6,
          baseToughness: 6,
          power: 6,
          toughness: 6,
          counters: { '+1/+1': 50 },
          card: { id: 'cardHugeBeastLife', name: 'Huge Beast', type_line: 'Creature - Beast', power: '6', toughness: '6', manaValue: 6 },
          tapped: false,
          summoningSick: false,
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { chosenObjectIds: ['sacLkiLifeCreature'] } as any,
    });

    const player = ((result.state as any).players || []).find((p: any) => p.id === 'p1') as any;
    const graveyardCard = (player?.graveyard || [])[0] as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['sacrifice', 'gain_life']);
    expect(graveyardCard?.toughness).toBe('6');
    expect(player.life).toBe(66);
  });

  it('uses pre-sacrifice last-known toughness for pronoun-style gain-life follow-ups', () => {
    const ir = parseOracleTextToIR(
      'Sacrifice that creature. You gain life equal to that creature\'s toughness.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 10,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'sacPronounCreature',
          owner: 'p1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Huge Beast',
          cardType: 'Creature',
          basePower: 6,
          baseToughness: 6,
          power: 6,
          toughness: 6,
          counters: { '+1/+1': 50 },
          card: { id: 'cardPronounBeast', name: 'Huge Beast', type_line: 'Creature - Beast', power: '6', toughness: '6', manaValue: 6 },
          tapped: false,
          summoningSick: false,
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { chosenObjectIds: ['sacPronounCreature'] } as any,
    });

    const player = ((result.state as any).players || []).find((p: any) => p.id === 'p1') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['sacrifice', 'gain_life']);
    expect(player.life).toBe(66);
  });

  it('applies X-based modify_pt where X is the number of colors that creature was', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of colors that creature was.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sacrificedCreatureColors', name: 'Sacrificed Shapeshifter', type_line: 'Creature — Shapeshifter', colors: ['B', 'G', 'U'], power: 3, toughness: 3, manaValue: 4 },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacColors', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sacrificedCreatureColors' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacColors') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value of a commander you own on the battlefield or in the command zone', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value of a commander you own on the battlefield or in the command zone.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqCommanderMv', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetCommanderMv', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetCommanderMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bfCommander', ownerId: 'p1', controller: 'p1', name: 'Battlefield Commander', type_line: 'Legendary Creature � Human', isCommander: true, manaValue: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      commandZone: {
        objects: [
          { id: 'czCommander', ownerId: 'p1', name: 'Command Zone Commander', isCommander: true, manaValue: 7 },
        ],
      } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqCommanderMv' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCommanderMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your highest commander tax among your commanders', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is your highest commander tax among your commanders.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqCommanderTax', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetCommanderTax', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetCommanderTax', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      commandZone: {
        p1: {
          commanderIds: ['cmdA', 'cmdB'],
          taxById: { cmdA: 2, cmdB: 6 },
          tax: 8,
        },
      } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqCommanderTax' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCommanderTax') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in your opponents\' hands', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the number of cards in your opponents' hands.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [{ id: 'self1' }, { id: 'self2' }], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [{ id: 'opp1' }], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [{ id: 'opp2' }, { id: 'opp3' }], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'creature1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of other creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'source1', ownerId: 'p1', controller: 'p1', name: 'Source Relic', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifact1', ownerId: 'p1', controller: 'p1', name: 'Relic', cardType: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'source1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of tapped creatures you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of tapped creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'artifact1', ownerId: 'p1', controller: 'p1', name: 'Relic', cardType: 'Artifact', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of tapped artifacts you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of tapped artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetTappedArtifacts', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactTapped1', ownerId: 'p1', controller: 'p1', name: 'Relic One', type_line: 'Artifact', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'artifactTapped2', ownerId: 'p1', controller: 'p1', name: 'Relic Two', type_line: 'Artifact', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'artifactUntapped', ownerId: 'p1', controller: 'p1', name: 'Relic Three', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetTappedArtifacts') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of attacking creatures', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of attacking creatures.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAttack1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetAttack1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAttack1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'attackerAttack1', ownerId: 'p2', controller: 'p2', name: 'Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'notAttacking1', ownerId: 'p2', controller: 'p2', name: 'Not Attacking', cardType: 'Creature', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAttack1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAttack1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of attacking creatures an opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of attacking creatures an opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAttack2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetAttack2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAttack2', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppAttacker2', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'youAttacker2', ownerId: 'p1', controller: 'p1', name: 'Your Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAttack2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAttack2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of attacking creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of attacking creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAttackYouControl', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetAttackYouControl', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAttackYouControl', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'youAttackerA', ownerId: 'p1', controller: 'p1', name: 'Your Attacker A', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'youAttackerB', ownerId: 'p1', controller: 'p1', name: 'Your Attacker B', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerA', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker A', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAttackYouControl' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAttackYouControl') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures attacking you', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures attacking you.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAttackYou', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetAttackYou', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAttackYou', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerToYouA', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker A', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerToYouB', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker B', cardType: 'Creature', power: 2, toughness: 2, attackingPlayerId: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerElse', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker Else', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'youAttacker', ownerId: 'p1', controller: 'p1', name: 'Your Attacker', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAttackYou' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAttackYou') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of bobbleheads you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of bobbleheads you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetBobbleheadCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bobblehead1', ownerId: 'p1', controller: 'p1', name: 'Vault Bobblehead', cardType: 'Artifact � Bobblehead', type_line: 'Artifact � Bobblehead', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactNonBobble', ownerId: 'p1', controller: 'p1', name: 'Relic', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetBobbleheadCount' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetBobbleheadCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of bobbleheads you control as you activate this ability', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of bobbleheads you control as you activate this ability.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetBobbleheadActivate', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bobbleheadActivate1', ownerId: 'p1', controller: 'p1', name: 'Vault Bobblehead', cardType: 'Artifact � Bobblehead', type_line: 'Artifact � Bobblehead', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bobbleheadActivate2', ownerId: 'p1', controller: 'p1', name: 'Science Bobblehead', cardType: 'Artifact � Bobblehead', type_line: 'Artifact � Bobblehead', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetBobbleheadActivate' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetBobbleheadActivate') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of shrines you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of shrines you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetShrineCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'shrine1', ownerId: 'p1', controller: 'p1', name: 'Sanctum of Tranquil Light', cardType: 'Legendary Enchantment � Shrine', type_line: 'Legendary Enchantment � Shrine', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'shrine2', ownerId: 'p1', controller: 'p1', name: 'Sanctum of Calm Waters', cardType: 'Legendary Enchantment � Shrine', type_line: 'Legendary Enchantment � Shrine', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetCreatureId: 'targetShrineCount' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetShrineCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of verse counters on this enchantment', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of verse counters on this enchantment.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'verseAura',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Saga',
          cardType: 'Enchantment',
          type_line: 'Enchantment',
          tapped: false,
          summoningSick: false,
          counters: { verse: 3 },
        } as any,
        { id: 'targetVerseCounter', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'verseAura',
      selectorContext: { targetCreatureId: 'targetVerseCounter' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetVerseCounter') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other attacking creatures', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other attacking creatures.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOtherAttack1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOtherAttack1', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOtherAttack1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerOther1', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'notAttackingOther1', ownerId: 'p2', controller: 'p2', name: 'Not Attacking', cardType: 'Creature', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOtherAttack1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOtherAttack1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other attacking creatures an opponent controls', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other attacking creatures an opponent controls.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOtherAttack2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOtherAttack2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOtherAttack2', ownerId: 'p2', controller: 'p2', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'oppAttackerOther2', ownerId: 'p2', controller: 'p2', name: 'Opp Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'youAttackerOther2', ownerId: 'p1', controller: 'p1', name: 'Your Attacker', cardType: 'Creature', power: 3, toughness: 3, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOtherAttack2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOtherAttack2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of other creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of other creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOtherCtrl', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetOtherCtrl', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOtherCtrl', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'friend1', ownerId: 'p1', controller: 'p1', name: 'Friend 1', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'friend2', ownerId: 'p1', controller: 'p1', name: 'Friend 2', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOtherCtrl' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOtherCtrl') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of legendary creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of legendary creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLegend', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetLegend', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLegend', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'legend1', ownerId: 'p1', controller: 'p1', name: 'Legend One', type_line: 'Legendary Creature � Human Wizard', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'legend2', ownerId: 'p1', controller: 'p1', name: 'Legend Two', type_line: 'Legendary Creature � Elf Druid', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonlegend', ownerId: 'p1', controller: 'p1', name: 'Not Legend', type_line: 'Creature � Beast', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLegend' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLegend') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective legendary creatures in executor where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of legendary creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqLegendaryEffective', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', type_line: 'Artifact', attachedTo: 'targetLegendaryEffective', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetLegendaryEffective', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'printedLegendary', ownerId: 'p1', controller: 'p1', name: 'Printed Legend', type_line: 'Legendary Creature - Human Wizard', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveLegendary', ownerId: 'p1', controller: 'p1', name: 'Animated Hero', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Legendary', 'Creature', 'Artifact'], power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'plainArtifact', ownerId: 'p1', controller: 'p1', name: 'Plain Relic', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqLegendaryEffective' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetLegendaryEffective') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures you control with defender', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures you control with defender.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDefender', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDefender', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDefender', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'wall1', ownerId: 'p1', controller: 'p1', name: 'Wall', type_line: 'Creature � Wall Defender', power: 0, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'wall2', ownerId: 'p1', controller: 'p1', name: 'Keyword Defender', type_line: 'Creature � Human Soldier', keywords: ['Defender'], power: 1, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'attacker', ownerId: 'p1', controller: 'p1', name: 'Attacker', type_line: 'Creature � Goblin', power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDefender' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDefender') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of differently named lands you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of differently named lands you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqNames', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetNames', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNames', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landA1', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land � Forest', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landA2', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land � Forest', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landB', ownerId: 'p1', controller: 'p1', name: 'Island', type_line: 'Basic Land � Island', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'landC', ownerId: 'p1', controller: 'p1', name: 'Command Tower', type_line: 'Land', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqNames' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNames') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of basic land types among lands you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of basic land types among lands you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land1', ownerId: 'p1', controller: 'p1', name: 'Tropical', type_line: 'Land � Forest Island', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land2', ownerId: 'p1', controller: 'p1', name: 'Badlands', type_line: 'Land � Swamp Mountain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'land3', ownerId: 'p1', controller: 'p1', name: 'Plains', type_line: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of shrines you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of shrines you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetShrine', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'shrine1', ownerId: 'p1', controller: 'p1', name: 'Honden One', type_line: 'Legendary Enchantment � Shrine', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'shrine2', ownerId: 'p1', controller: 'p1', name: 'Honden Two', type_line: 'Legendary Enchantment � Shrine', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonshrine', ownerId: 'p1', controller: 'p1', name: 'Aura', type_line: 'Enchantment � Aura', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetShrine') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of elves you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of elves you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqElfCount2', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetElfCount2', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetElfCount2', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'elfCount2A', ownerId: 'p1', controller: 'p1', name: 'Elf A', type_line: 'Creature � Elf Druid', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'elfCount2B', ownerId: 'p1', controller: 'p1', name: 'Elf B', type_line: 'Creature � Elf Warrior', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'notElfCount2', ownerId: 'p1', controller: 'p1', name: 'Human', type_line: 'Creature � Human', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqElfCount2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetElfCount2') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of snow permanents you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of snow permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetSnowPerm', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'snow1', ownerId: 'p1', controller: 'p1', name: 'Snow-Covered Forest', type_line: 'Basic Snow Land � Forest', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'snow2', ownerId: 'p1', controller: 'p1', name: 'Coldsteel Heart', type_line: 'Snow Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonsnow1', ownerId: 'p1', controller: 'p1', name: 'Plains', type_line: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSnowPerm') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of caves you control plus cave cards in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of caves you control plus cave cards in your graveyard.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'gyCave1', type_line: 'Land � Cave' },
            { id: 'gyOther1', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetCaveMix', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bfCave1', ownerId: 'p1', controller: 'p1', name: 'Hidden Cave', type_line: 'Land � Cave', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bfOther1', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land � Forest', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCaveMix') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of mountains you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of mountains you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetMountain', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'mountain1', ownerId: 'p1', controller: 'p1', name: 'Mountain', type_line: 'Basic Land � Mountain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'mountain2', ownerId: 'p1', controller: 'p1', name: 'Steam Vents', type_line: 'Land � Island Mountain', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'plains1', ownerId: 'p1', controller: 'p1', name: 'Plains', type_line: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetMountain') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqBattlefieldCreatures', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetBattlefieldCreatures', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetBattlefieldCreatures', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherCreatureA', ownerId: 'p1', controller: 'p1', name: 'Bear Cub', type_line: 'Creature � Bear', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'otherCreatureB', ownerId: 'p2', controller: 'p2', name: 'Opponent Goblin', type_line: 'Creature � Goblin', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonCreaturePerm', ownerId: 'p1', controller: 'p1', name: 'Signet', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqBattlefieldCreatures' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetBattlefieldCreatures') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of forests you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of forests you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetForest', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'forest1', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land � Forest', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'forest2', ownerId: 'p1', controller: 'p1', name: 'Temple Garden', type_line: 'Land � Forest Plains', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'island1', ownerId: 'p1', controller: 'p1', name: 'Island', type_line: 'Basic Land � Island', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetForest') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of swamps you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of swamps you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetSwamp', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'swamp1', ownerId: 'p1', controller: 'p1', name: 'Swamp', type_line: 'Basic Land � Swamp', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'swamp2', ownerId: 'p1', controller: 'p1', name: 'Watery Grave', type_line: 'Land � Island Swamp', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'plains2', ownerId: 'p1', controller: 'p1', name: 'Plains', type_line: 'Basic Land � Plains', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSwamp') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of enchantments you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of enchantments you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetEnchantments', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'enchantment1', ownerId: 'p1', controller: 'p1', name: 'Pacifism', type_line: 'Enchantment � Aura', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'enchantment2', ownerId: 'p1', controller: 'p1', name: 'Darksteel Mutation', type_line: 'Enchantment � Aura', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifact1', ownerId: 'p1', controller: 'p1', name: 'Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEnchantments') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective enchantments in executor where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of enchantments you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetEffectiveEnchant', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveEnchant', ownerId: 'p1', controller: 'p1', name: 'Animated Blessing', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Enchantment', 'Artifact'], tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveEnchant') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(1);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective land subtypes in executor where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of basic land types among lands you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetEffectiveLandTypes', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveForest', ownerId: 'p1', controller: 'p1', name: 'Living Grove', cardType: 'Land', type_line: 'Land', effectiveTypes: ['Land', 'Forest'], tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveIsland', ownerId: 'p1', controller: 'p1', name: 'Living Reef', cardType: 'Land', type_line: 'Land', grantedTypes: ['Island'], tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveLandTypes') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective creatures on the battlefield in executor where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of other creatures on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqEffectiveCreatureCount', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetEffectiveCreatureCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveCreatureCount', ownerId: 'p2', controller: 'p2', name: 'Animated Idol', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Artifact', 'Creature'], power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqEffectiveCreatureCount',
      targetCreatureId: 'targetEffectiveCreatureCount',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveCreatureCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of allies you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of allies you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAlliesCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetAllies', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAllies', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally1', ownerId: 'p1', controller: 'p1', name: 'Ally A', type_line: 'Creature � Human Ally', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally2', ownerId: 'p1', controller: 'p1', name: 'Ally B', type_line: 'Creature � Kor Ally', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppAlly', ownerId: 'p2', controller: 'p2', name: 'Opp Ally', type_line: 'Creature � Human Ally', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAlliesCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAllies') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of soldiers you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of soldiers you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSoldierCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetSoldierCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSoldierCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'soldier1', ownerId: 'p1', controller: 'p1', name: 'Soldier A', type_line: 'Creature � Human Soldier', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'soldier2', ownerId: 'p1', controller: 'p1', name: 'Soldier B', type_line: 'Creature � Cat Soldier', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppSoldier', ownerId: 'p2', controller: 'p2', name: 'Opp Soldier', type_line: 'Creature � Human Soldier', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSoldierCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSoldierCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of vampires you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of vampires you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqVampireCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetVampireCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetVampireCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'vampire1', ownerId: 'p1', controller: 'p1', name: 'Vampire A', type_line: 'Creature � Vampire', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'vampire2', ownerId: 'p1', controller: 'p1', name: 'Vampire B', type_line: 'Creature � Vampire Rogue', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppVampire', ownerId: 'p2', controller: 'p2', name: 'Opp Vampire', type_line: 'Creature � Vampire', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqVampireCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetVampireCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of dragons you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of dragons you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqDragonCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetDragonCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetDragonCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'dragon1', ownerId: 'p1', controller: 'p1', name: 'Dragon A', type_line: 'Creature � Dragon', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'dragon2', ownerId: 'p1', controller: 'p1', name: 'Dragon B', type_line: 'Creature � Dragon Shaman', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppDragon', ownerId: 'p2', controller: 'p2', name: 'Opp Dragon', type_line: 'Creature � Dragon', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqDragonCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDragonCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of planeswalkers you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of planeswalkers you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqWalkerCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetWalkerCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetWalkerCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'walker1', ownerId: 'p1', controller: 'p1', name: 'Walker A', type_line: 'Legendary Planeswalker � Jace', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'walker2', ownerId: 'p1', controller: 'p1', name: 'Walker B', type_line: 'Legendary Planeswalker � Liliana', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppWalker', ownerId: 'p2', controller: 'p2', name: 'Opp Walker', type_line: 'Legendary Planeswalker � Chandra', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqWalkerCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetWalkerCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of clerics on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of clerics on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqClericCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetClericCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetClericCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'clericA', ownerId: 'p1', controller: 'p1', name: 'Cleric A', type_line: 'Creature � Human Cleric', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'clericB', ownerId: 'p2', controller: 'p2', name: 'Cleric B', type_line: 'Creature � Kor Cleric', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonCleric', ownerId: 'p2', controller: 'p2', name: 'Non Cleric', type_line: 'Creature � Human Soldier', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqClericCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetClericCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of elves on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of elves on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqElves', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetElf', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetElf', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'elf1', ownerId: 'p1', controller: 'p1', name: 'Llanowar Elves', type_line: 'Creature � Elf Druid', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'elf2', ownerId: 'p1', controller: 'p1', name: 'Elvish Mystic', type_line: 'Creature � Elf Druid', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'elf3', ownerId: 'p2', controller: 'p2', name: 'Wirewood Hivemaster', type_line: 'Creature � Elf', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'human1', ownerId: 'p2', controller: 'p2', name: 'Soldier', type_line: 'Creature � Human Soldier', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqElves' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetElf') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of goblins on the battlefield', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of goblins on the battlefield.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqGoblins', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetGoblin', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetGoblin', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'goblin1', ownerId: 'p1', controller: 'p1', name: 'Goblin A', type_line: 'Creature � Goblin', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'goblin2', ownerId: 'p1', controller: 'p1', name: 'Goblin B', type_line: 'Creature � Goblin Warrior', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'goblin3', ownerId: 'p2', controller: 'p2', name: 'Goblin C', type_line: 'Creature � Goblin Rogue', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nongoblin', ownerId: 'p2', controller: 'p2', name: 'Human', type_line: 'Creature � Human Soldier', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqGoblins' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetGoblin') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures in your party', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures in your party.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqParty', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetParty', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetParty', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'cleric1', ownerId: 'p1', controller: 'p1', name: 'Cleric', type_line: 'Creature � Human Cleric', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'rogue1', ownerId: 'p1', controller: 'p1', name: 'Rogue', type_line: 'Creature � Merfolk Rogue', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'warrior1', ownerId: 'p1', controller: 'p1', name: 'Warrior', type_line: 'Creature � Orc Warrior', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'wizard1', ownerId: 'p1', controller: 'p1', name: 'Wizard', type_line: 'Creature � Vedalken Wizard', power: 1, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'extraRogue', ownerId: 'p1', controller: 'p1', name: 'Another Rogue', type_line: 'Creature � Human Rogue', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqParty' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetParty') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total power of creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the total power of creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqTotalPower', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetTotalPower', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetTotalPower', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally1', ownerId: 'p1', controller: 'p1', name: 'Ally 1', type_line: 'Creature � Elf', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'ally2', ownerId: 'p1', controller: 'p1', name: 'Ally 2', type_line: 'Creature � Human', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonCreature', ownerId: 'p1', controller: 'p1', name: 'Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqTotalPower' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetTotalPower') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total toughness of creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the total toughness of creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqTotalToughness', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetTotalToughness', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetTotalToughness', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyTough1', ownerId: 'p1', controller: 'p1', name: 'Ally 1', type_line: 'Creature � Elf', power: 3, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyTough2', ownerId: 'p1', controller: 'p1', name: 'Ally 2', type_line: 'Creature � Human', power: 4, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonCreatureTough', ownerId: 'p1', controller: 'p1', name: 'Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqTotalToughness' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetTotalToughness') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(15);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total toughness of other creatures you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the total toughness of other creatures you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOtherTotalToughness', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOtherTotalToughness', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOtherTotalToughness', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyOtherTough1', ownerId: 'p1', controller: 'p1', name: 'Ally 1', type_line: 'Creature � Elf', power: 3, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'allyOtherTough2', ownerId: 'p1', controller: 'p1', name: 'Ally 2', type_line: 'Creature � Human', power: 4, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOtherTotalToughness' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOtherTotalToughness') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(10);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of creatures you control with power 4 or greater', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of creatures you control with power 4 or greater.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqPowerThreshold', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetPowerThreshold', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetPowerThreshold', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'smallCreature', ownerId: 'p1', controller: 'p1', name: 'Small Ally', type_line: 'Creature � Elf', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigCreature1', ownerId: 'p1', controller: 'p1', name: 'Big Ally 1', type_line: 'Creature � Giant', power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bigCreature2', ownerId: 'p1', controller: 'p1', name: 'Big Ally 2', type_line: 'Creature � Beast', power: 6, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqPowerThreshold' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetPowerThreshold') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of colors among permanents you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of colors among permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqColors', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetColors', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetColors', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'bluePerm', ownerId: 'p1', controller: 'p1', name: 'Blue Permanent', type_line: 'Enchantment', colors: ['U'], tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'redPerm', ownerId: 'p1', controller: 'p1', name: 'Red Permanent', type_line: 'Artifact Creature', colors: ['R'], power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'greenWhitePerm', ownerId: 'p1', controller: 'p1', name: 'Hybrid Permanent', type_line: 'Creature � Elf', manaCost: '{G/W}{G/W}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'colorlessPerm', ownerId: 'p1', controller: 'p1', name: 'Colorless Permanent', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqColors' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetColors') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is its mana value', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is its mana value.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'sourceManaValue', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', manaValue: 5, attachedTo: 'targetManaValue', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetManaValue', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sourceManaValue' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetManaValue') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents you control with oil counters on them', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of permanents you control with oil counters on them.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqOilCounterCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOilCounterCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOilCounterCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature � Bear', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: { oil: 1 } } as any,
        { id: 'artifactOilTwo', ownerId: 'p1', controller: 'p1', name: 'Oil Relic', type_line: 'Artifact', tapped: false, summoningSick: false, counters: { oil: 2 } } as any,
        { id: 'landNoOil', ownerId: 'p1', controller: 'p1', name: 'Wastes', type_line: 'Basic Land', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'offColorCounters', ownerId: 'p1', controller: 'p1', name: 'Counter Rock', type_line: 'Artifact', tapped: false, summoningSick: false, counters: { charge: 3 } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOilCounterCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOilCounterCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that spell\'s mana value', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is that spell's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellRef1', type: 'spell', manaValue: 6, spell: { manaValue: 6 } } as any,
      ],
      battlefield: [
        { id: 'targetSpellMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellRef1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the difference between the chosen creatures' powers", () => {
    const ir = parseOracleTextToIR(
      "The creature gets +X/+0 until end of turn where X is the difference between the chosen creatures' powers.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqChosenPowerDiff', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact', type_line: 'Artifact', attachedTo: 'targetChosenPowerDiff', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetChosenPowerDiff', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'chosenPowerB', ownerId: 'p1', controller: 'p1', name: 'Power B', cardType: 'Creature', power: 6, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqChosenPowerDiff',
      selectorContext: {
        chosenObjectIds: ['targetChosenPowerDiff', 'chosenPowerB'],
      } as any,
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetChosenPowerDiff') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total power of attacking creatures', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the total power of attacking creatures.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqAttackingTotalPower', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetAttackingTotalPower', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetAttackingTotalPower', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {}, attacking: 'p2' } as any,
        { id: 'attackingAlly', ownerId: 'p1', controller: 'p1', name: 'Attacking Ally', type_line: 'Creature � Elf', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {}, attacking: 'p2' } as any,
        { id: 'attackingOpponent', ownerId: 'p2', controller: 'p2', name: 'Attacking Opponent', type_line: 'Creature � Human', power: 5, toughness: 5, tapped: false, summoningSick: false, counters: {}, attacking: 'p1' } as any,
        { id: 'nonAttackingCreature', ownerId: 'p1', controller: 'p1', name: 'Nonattacker', type_line: 'Creature � Beast', power: 7, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqAttackingTotalPower' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAttackingTotalPower') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(10);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the mana value of that spell (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the mana value of that spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellRefAlias1', type: 'spell', manaValue: 7, spell: { manaValue: 7 } } as any,
      ],
      battlefield: [
        { id: 'targetSpellMvAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellRefAlias1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellMvAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that creature\'s toughness', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is that creature's toughness.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetToughnessThat', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetToughnessThat') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is its toughness', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is its toughness.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetToughnessIts', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetToughnessIts') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest mana value among artifacts you control', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqGreatestArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetGreatestArtifactMv', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetGreatestArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactMv2', ownerId: 'p1', controller: 'p1', name: 'Signet', type_line: 'Artifact', manaValue: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'artifactMv6', ownerId: 'p1', controller: 'p1', name: 'Big Relic', type_line: 'Artifact', manaValue: 6, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nonArtifactMv9', ownerId: 'p1', controller: 'p1', name: 'Huge Creature', type_line: 'Creature � Beast', manaValue: 9, power: 9, toughness: 9, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqGreatestArtifactMv' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetGreatestArtifactMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the greatest number of creatures you control that have a creature type in common', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest number of creatures you control that have a creature type in common.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqSharedTypeCount', ownerId: 'p1', controller: 'p1', name: 'Buffing Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetSharedTypeCount', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetSharedTypeCount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature � Bear', type_line: 'Creature � Bear', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'sharedElf1', ownerId: 'p1', controller: 'p1', name: 'Elf Scout', type_line: 'Creature � Elf Scout', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'sharedElf2', ownerId: 'p1', controller: 'p1', name: 'Elf Druid', type_line: 'Creature � Elf Druid', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'sharedElf3', ownerId: 'p1', controller: 'p1', name: 'Elf Warrior', type_line: 'Creature � Elf Warrior', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'sharedZombieOnly', ownerId: 'p1', controller: 'p1', name: 'Zombie Rogue', type_line: 'Creature � Zombie Rogue', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqSharedTypeCount' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSharedTypeCount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that creature\'s mana value', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is that creature's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetCreatureMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaValue: 4, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCreatureMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the exiled card\'s mana value (alias)', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the exiled card's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'exiledCardMvRef', type: 'spell', manaValue: 5, spell: { manaValue: 5 } } as any,
      ],
      battlefield: [
        { id: 'targetExiledCardMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'exiledCardMvRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExiledCardMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the revealed card\'s mana value (alias)', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the revealed card's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'revealedCardMvRef', type: 'spell', manaValue: 3, spell: { manaValue: 3 } } as any,
      ],
      battlefield: [
        { id: 'targetRevealedCardMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'revealedCardMvRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetRevealedCardMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the discarded card\'s mana value (alias)', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the discarded card's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'discardedCardMvRef', type: 'spell', manaValue: 2, spell: { manaValue: 2 } } as any,
      ],
      battlefield: [
        { id: 'targetDiscardedCardMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'discardedCardMvRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetDiscardedCardMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts effective artifacts in greatest-mana artifact where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the greatest mana value among artifacts you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqEffectiveArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', manaValue: 1, attachedTo: 'targetEffectiveArtifactMv', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetEffectiveArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Target Creature', cardType: 'Creature', type_line: 'Creature', manaValue: 2, power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'printedArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Printed Relic', cardType: 'Artifact', type_line: 'Artifact', manaValue: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveArtifactMv', ownerId: 'p1', controller: 'p1', name: 'Transmuted Mage', cardType: 'Creature', type_line: 'Creature', effectiveTypes: ['Creature', 'Artifact'], manaValue: 9, power: 4, toughness: 4, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'eqEffectiveArtifactMv',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveArtifactMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(9);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt for exiled/revealed/discarded card power/toughness aliases', () => {
    const whereCases = [
      "X is the power of the exiled card",
      "X is the exiled card's power",
      "X is the toughness of the exiled card",
      "X is the exiled card's toughness",
      "X is the power of the revealed card",
      "X is the revealed card's power",
      "X is the toughness of the revealed card",
      "X is the revealed card's toughness",
      "X is the power of the discarded card",
      "X is the toughness of the discarded card",
    ];

    for (const whereText of whereCases) {
      const canonicalWhere = whereText.includes('toughness') ? "X is that card's toughness" : "X is that card's power";
      const ir = parseOracleTextToIR(
        `Target creature gets +X/+0 until end of turn where ${whereText}.`,
        'Test'
      );
      const canonicalIr = parseOracleTextToIR(
        `Target creature gets +X/+0 until end of turn where ${canonicalWhere}.`,
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const canonicalSteps = canonicalIr.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        ],
        stack: [
          { id: 'cardStatAliasRef', type: 'spell', power: 6, toughness: 4, spell: { power: 6, toughness: 4 } } as any,
        ],
        battlefield: [
          { id: 'targetCardStatAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'cardStatAliasRef' });
      const canonicalResult = applyOracleIRStepsToGameState(start, canonicalSteps, { controllerId: 'p1', sourceId: 'cardStatAliasRef' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCardStatAlias') as any;
      const canonicalCreature = ((canonicalResult.state as any).battlefield || []).find((p: any) => p.id === 'targetCardStatAlias') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      const canonicalPtMod = (Array.isArray(canonicalCreature?.modifiers) ? canonicalCreature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(canonicalResult.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod).toBeTruthy();
      expect(canonicalPtMod).toBeTruthy();
      expect(ptMod.power).toBe(canonicalPtMod.power);
      expect(ptMod.toughness).toBe(0);
    }
  });

  it('applies X-based modify_pt where X is the amount of mana spent to cast that creature (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of mana spent to cast that creature.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'manaSpentThatCreatureRef', type: 'spell', manaPayment: { red: 1, green: 2, colorless: 1 } } as any,
      ],
      battlefield: [
        { id: 'targetManaSpentThatCreature', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'manaSpentThatCreatureRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetManaSpentThatCreature') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is excess damage dealt (alias)', () => {
    const ir = parseOracleTextToIR(
      'It deals 3 damage to each creature. Target creature you control gets +X/+0 until end of turn where X is excess damage dealt.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetExcessDamageBareAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessBareAliasA', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear A', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessBareAliasB', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear B', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExcessDamageBareAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards in all graveyards with the same name as this spell (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of cards in all graveyards with the same name as this spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [
            { id: 'sameNameThisSpell1', name: 'Shock', type_line: 'Instant' },
            { id: 'sameNameThisSpell2', name: 'Opt', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [],
          graveyard: [
            { id: 'sameNameThisSpell3', name: 'Shock', type_line: 'Instant' },
            { id: 'sameNameThisSpell4', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      stack: [
        { id: 'sameNameThisSpellRef', type: 'spell', cardName: 'Shock', spell: { name: 'Shock' } } as any,
      ],
      battlefield: [
        { id: 'targetSameNameThisSpell', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'sameNameThisSpellRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSameNameThisSpell') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt for curly apostrophe aliases', () => {
    const tappedCreatureAlias = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the tapped creature\u2019s power.',
      'Test'
    );
    const tappedSteps = tappedCreatureAlias.abilities[0]?.steps ?? [];

    const tappedState = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetCurlyTappedAlias', ownerId: 'p1', controller: 'p1', name: 'Tapped Bear', cardType: 'Creature', type_line: 'Creature', power: 5, toughness: 2, tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const tappedResult = applyOracleIRStepsToGameState(tappedState, tappedSteps, { controllerId: 'p1' });
    const tappedCreature = ((tappedResult.state as any).battlefield || []).find((p: any) => p.id === 'targetCurlyTappedAlias') as any;
    const tappedPtMod = (Array.isArray(tappedCreature?.modifiers) ? tappedCreature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(tappedResult.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(tappedPtMod).toBeTruthy();
    expect(tappedPtMod.power).toBe(5);
    expect(tappedPtMod.toughness).toBe(0);
  });

  it.each([
    {
      label: 'X is 1 plus the number of basic land types among lands you control',
      whereText: 'X is 1 plus the number of basic land types among lands you control',
      setup: {
        battlefield: [
          { id: 'landTypeForest', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land � Forest' },
          { id: 'landTypeIsland', ownerId: 'p1', controller: 'p1', name: 'Island', type_line: 'Basic Land � Island' },
          { id: 'landTypeMountain', ownerId: 'p1', controller: 'p1', name: 'Mountain', type_line: 'Basic Land � Mountain' },
          { id: 'landTypeForestDup', ownerId: 'p1', controller: 'p1', name: 'Forest 2', type_line: 'Basic Land � Forest' },
        ],
      },
      expected: 4,
    },
    {
      label: 'X is 3 plus the number of artifacts you control',
      whereText: 'X is 3 plus the number of artifacts you control',
      setup: {
        battlefield: [
          { id: 'artifactCountA', ownerId: 'p1', controller: 'p1', name: 'Relic A', type_line: 'Artifact' },
          { id: 'artifactCountB', ownerId: 'p1', controller: 'p1', name: 'Relic B', type_line: 'Artifact' },
          { id: 'artifactCountC', ownerId: 'p2', controller: 'p2', name: 'Opponent Relic', type_line: 'Artifact' },
        ],
      },
      expected: 5,
    },
    {
      label: 'X is 4 minus the number of cards in their hand',
      whereText: 'X is 4 minus the number of cards in their hand',
      setup: {
        players: [
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [{ id: 'p2h1' }], graveyard: [], exile: [] } as any,
        ],
      },
      expected: 3,
      context: { selectorContext: { targetPlayerId: 'p2' } },
    },
    {
      label: 'X is half the number of artifacts you control, rounded down',
      whereText: 'X is half the number of artifacts you control, rounded down',
      setup: {
        battlefield: [
          { id: 'halfArtifactA', ownerId: 'p1', controller: 'p1', name: 'Relic A', type_line: 'Artifact' },
          { id: 'halfArtifactB', ownerId: 'p1', controller: 'p1', name: 'Relic B', type_line: 'Artifact' },
          { id: 'halfArtifactC', ownerId: 'p1', controller: 'p1', name: 'Relic C', type_line: 'Artifact' },
          { id: 'halfArtifactD', ownerId: 'p1', controller: 'p1', name: 'Relic D', type_line: 'Artifact' },
          { id: 'halfArtifactE', ownerId: 'p1', controller: 'p1', name: 'Relic E', type_line: 'Artifact' },
        ],
      },
      expected: 2,
    },
    {
      label: 'X is one plus the number of foods you control',
      whereText: 'X is one plus the number of foods you control',
      setup: {
        battlefield: [
          { id: 'foodCountA', ownerId: 'p1', controller: 'p1', name: 'Food A', type_line: 'Artifact � Food' },
          { id: 'foodCountB', ownerId: 'p1', controller: 'p1', name: 'Food B', type_line: 'Artifact � Food' },
          { id: 'foodCountC', ownerId: 'p2', controller: 'p2', name: 'Opp Food', type_line: 'Artifact � Food' },
        ],
      },
      expected: 3,
    },
  ])('applies X-based modify_pt where %s (new-set arithmetic/class batch)', ({ whereText, setup, expected, context }) => {
    const ir = parseOracleTextToIR(
      `Target creature gets +X/+0 until end of turn where ${whereText}.`,
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const basePlayers = [
      { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l0' }], hand: [], graveyard: [], exile: [] } as any,
    ];

    const players = [...basePlayers];
    const setupPlayers = Array.isArray((setup as any)?.players) ? ((setup as any).players as any[]) : [];
    for (const p of setupPlayers) {
      const idx = players.findIndex(x => x.id === p.id);
      if (idx >= 0) players[idx] = { ...players[idx], ...p };
      else players.push(p as any);
    }

    const battlefield = [
      { id: 'targetNewSetArithmeticBatch', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ...(((setup as any)?.battlefield || []) as any[]),
    ];

    const start = makeState({
      players,
      battlefield,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      ...(context || {}),
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNewSetArithmeticBatch') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is the number of mounts and vehicles you control', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is the number of mounts and vehicles you control' },
        raw: 'The creature gets +X/+0 until end of turn where X is the number of mounts and vehicles you control.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'mountVehicleSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetMountVehicleDirectExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetMountVehicleDirectExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'mountVehicleDirectExecutorA', ownerId: 'p1', controller: 'p1', name: 'Rider Mount', type_line: 'Creature � Mount', cardType: 'Creature � Mount' } as any,
        { id: 'mountVehicleDirectExecutorB', ownerId: 'p1', controller: 'p1', name: 'Sky Vehicle', type_line: 'Artifact � Vehicle', cardType: 'Artifact � Vehicle' } as any,
        { id: 'mountVehicleDirectExecutorC', ownerId: 'p1', controller: 'p1', name: 'Elf', type_line: 'Creature � Elf', cardType: 'Creature' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'mountVehicleSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetMountVehicleDirectExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 2 plus the number of mounts and vehicles you control', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is 2 plus the number of mounts and vehicles you control' },
        raw: 'The creature gets +X/+0 until end of turn where X is 2 plus the number of mounts and vehicles you control.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'mountVehiclePlusSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetMountVehiclePlusExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetMountVehiclePlusExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'mountVehiclePlusExecutorA', ownerId: 'p1', controller: 'p1', name: 'Rider Mount', type_line: 'Creature � Mount', cardType: 'Creature � Mount' } as any,
        { id: 'mountVehiclePlusExecutorB', ownerId: 'p1', controller: 'p1', name: 'Sky Vehicle', type_line: 'Artifact � Vehicle', cardType: 'Artifact � Vehicle' } as any,
        { id: 'mountVehiclePlusExecutorC', ownerId: 'p1', controller: 'p1', name: 'Elf', type_line: 'Creature � Elf', cardType: 'Creature' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'mountVehiclePlusSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetMountVehiclePlusExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is half the number of zombies you control, rounded down', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is half the number of zombies you control, rounded down' },
        raw: 'The creature gets +X/+0 until end of turn where X is half the number of zombies you control, rounded down.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'halfZombieSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetHalfZombieExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetHalfZombieExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'halfZombieExecA', ownerId: 'p1', controller: 'p1', name: 'Zombie A', type_line: 'Creature � Zombie' } as any,
        { id: 'halfZombieExecB', ownerId: 'p1', controller: 'p1', name: 'Zombie B', type_line: 'Creature � Zombie' } as any,
        { id: 'halfZombieExecC', ownerId: 'p1', controller: 'p1', name: 'Zombie C', type_line: 'Creature � Zombie' } as any,
        { id: 'halfZombieExecD', ownerId: 'p1', controller: 'p1', name: 'Zombie D', type_line: 'Creature � Zombie' } as any,
        { id: 'halfZombieExecE', ownerId: 'p1', controller: 'p1', name: 'Zombie E', type_line: 'Creature � Zombie' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'halfZombieSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetHalfZombieExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is one plus the number of opponents who control an artifact', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is one plus the number of opponents who control an artifact' },
        raw: 'Target creature gets +X/+0 until end of turn where X is one plus the number of opponents who control an artifact.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p4', name: 'P4', seat: 3, life: 40, library: [{ id: 'p4l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'oppControllerArtifactSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppControllerArtifactExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppControllerArtifactExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppAArtifactExec', ownerId: 'p2', controller: 'p2', name: 'Relic A', type_line: 'Artifact' } as any,
        { id: 'oppBCreatureOnlyExec', ownerId: 'p3', controller: 'p3', name: 'Elf', type_line: 'Creature � Elf' } as any,
        { id: 'oppCArtifactExec', ownerId: 'p4', controller: 'p4', name: 'Relic C', type_line: 'Artifact � Equipment' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'oppControllerArtifactSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppControllerArtifactExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is the number of opponents who control artifacts', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is the number of opponents who control artifacts' },
        raw: 'The creature gets +X/+0 until end of turn where X is the number of opponents who control artifacts.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p4', name: 'P4', seat: 3, life: 40, library: [{ id: 'p4l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'oppControllerArtifactsSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppControllerArtifactsExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppControllerArtifactsExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtifactsExecA', ownerId: 'p2', controller: 'p2', name: 'Relic A', type_line: 'Artifact' } as any,
        { id: 'oppArtifactsExecB', ownerId: 'p3', controller: 'p3', name: 'Relic B', type_line: 'Artifact � Equipment' } as any,
        { id: 'oppNoArtifactsExec', ownerId: 'p4', controller: 'p4', name: 'Elf', type_line: 'Creature � Elf' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'oppControllerArtifactsSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppControllerArtifactsExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is one plus the number of opponents who control artifacts and/or enchantments', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is one plus the number of opponents who control artifacts and/or enchantments' },
        raw: 'The creature gets +X/+0 until end of turn where X is one plus the number of opponents who control artifacts and/or enchantments.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p4', name: 'P4', seat: 3, life: 40, library: [{ id: 'p4l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'oppControllerArtEnchSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetOppControllerArtEnchExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetOppControllerArtEnchExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppArtEnchExecA', ownerId: 'p2', controller: 'p2', name: 'Relic A', type_line: 'Artifact' } as any,
        { id: 'oppArtEnchExecB', ownerId: 'p3', controller: 'p3', name: 'Aura B', type_line: 'Enchantment � Aura' } as any,
        { id: 'oppNoArtEnchExec', ownerId: 'p4', controller: 'p4', name: 'Elf', type_line: 'Creature � Elf' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'oppControllerArtEnchSourceExecutor' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppControllerArtEnchExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 2 plus the sacrificed creature\'s mana value', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is 2 plus the sacrificed creature's mana value" },
        raw: "The creature gets +X/+0 until end of turn where X is 2 plus the sacrificed creature's mana value.",
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'sacrificedCreaturePlusTwoExec', name: 'Sacrificed Titan', type_line: 'Creature � Giant', power: 6, toughness: 6, manaValue: 4 },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacManaPlusExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'sacrificedCreaturePlusTwoExec',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacManaPlusExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is one plus the mana value of the sacrificed artifact', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is one plus the mana value of the sacrificed artifact' },
        raw: 'The creature gets +X/+0 until end of turn where X is one plus the mana value of the sacrificed artifact.',
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'sacArtifactSourceExecutor', name: 'Sac Relic', type_line: 'Artifact', cardType: 'Artifact', manaValue: 4, manaCost: '{4}' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacArtifactExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'sacArtifactSourceExecutor',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacArtifactExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 1 plus the sacrificed creature\'s mana value', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is 1 plus the sacrificed creature's mana value" },
        raw: "The creature gets +X/+0 until end of turn where X is 1 plus the sacrificed creature's mana value.",
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'sacCreatureSourceExecutor', name: 'Sac Beast', type_line: 'Creature � Beast', cardType: 'Creature', manaValue: 3, manaCost: '{2}{G}' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetSacCreatureExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'sacCreatureSourceExecutor',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSacCreatureExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 1 plus the number of nonbasic land types among lands that player controls', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is 1 plus the number of nonbasic land types among lands that player controls' },
        raw: 'The creature gets +X/+0 until end of turn where X is 1 plus the number of nonbasic land types among lands that player controls.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'nonbasicLandTypesSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetNonbasicLandTypesExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetNonbasicLandTypesExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'nblTypeShock', ownerId: 'p2', controller: 'p2', name: 'Steam Vents', type_line: 'Land � Island Mountain' } as any,
        { id: 'nblTypeTemple', ownerId: 'p2', controller: 'p2', name: 'Temple Garden', type_line: 'Land � Forest Plains' } as any,
        { id: 'nblTypeSwamp', ownerId: 'p2', controller: 'p2', name: 'Watery Grave', type_line: 'Land � Island Swamp' } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'nonbasicLandTypesSourceExecutor',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNonbasicLandTypesExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 1 plus the exiled creature\'s mana value', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is 1 plus the exiled creature's mana value" },
        raw: "The creature gets +X/+0 until end of turn where X is 1 plus the exiled creature's mana value.",
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'exiledCreatureMvAliasRef', name: 'Exiled Beast', type_line: 'Creature � Beast', manaValue: 4, manaCost: '{2}{G}{G}' },
          ],
        } as any,
      ],
      battlefield: [
        { id: 'exiledCreatureAliasSourceExecutor', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetExiledCreatureAliasExecutor', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetExiledCreatureAliasExecutor', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'exiledCreatureMvAliasRef',
      selectorContext: { targetCreatureId: 'targetExiledCreatureAliasExecutor' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExiledCreatureAliasExecutor') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is 2 plus the exiled creature\'s mana value', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is 2 plus the exiled creature's mana value" },
        raw: "The creature gets +X/+0 until end of turn where X is 2 plus the exiled creature's mana value.",
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [],
          exile: [
            { id: 'exiledCreatureMvAliasRefTwo', name: 'Exiled Giant', type_line: 'Creature � Giant', manaValue: 5, manaCost: '{3}{G}{G}' },
          ],
        } as any,
      ],
      battlefield: [
        { id: 'exiledCreatureAliasSourceExecutorTwo', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact � Equipment', type_line: 'Artifact � Equipment', attachedTo: 'targetExiledCreatureAliasExecutorTwo', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetExiledCreatureAliasExecutorTwo', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'exiledCreatureMvAliasRefTwo',
      selectorContext: { targetCreatureId: 'targetExiledCreatureAliasExecutorTwo' as any },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExiledCreatureAliasExecutorTwo') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is the number of cards named Lightning Bolt in all graveyards', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is the number of cards named Lightning Bolt in all graveyards' },
        raw: 'The creature gets +X/+0 until end of turn where X is the number of cards named Lightning Bolt in all graveyards.',
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'lb1', name: 'Lightning Bolt', type_line: 'Instant', cardType: 'Instant' },
            { id: 'other1', name: 'Opt', type_line: 'Instant', cardType: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2l1' }],
          hand: [],
          graveyard: [
            { id: 'lb2', name: 'Lightning Bolt', type_line: 'Instant', cardType: 'Instant' },
            { id: 'lb3', name: 'Lightning Bolt', type_line: 'Instant', cardType: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'targetCardsNamedAllGy', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'targetCardsNamedAllGy' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCardsNamedAllGy') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is the number of cards named Lightning Bolt in all graveyards as you cast this spell', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is the number of cards named Lightning Bolt in all graveyards as you cast this spell' },
        raw: 'The creature gets +X/+0 until end of turn where X is the number of cards named Lightning Bolt in all graveyards as you cast this spell.',
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [{ id: 'lb4', name: 'Lightning Bolt', type_line: 'Instant', cardType: 'Instant' }], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [{ id: 'lb5', name: 'Lightning Bolt', type_line: 'Instant', cardType: 'Instant' }], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetCardsNamedAllGyAsCast', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'targetCardsNamedAllGyAsCast' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCardsNamedAllGyAsCast') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies executor modify_pt where X is the number of cards named Dark Ritual in your graveyard', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: 'X is the number of cards named Dark Ritual in your graveyard' },
        raw: 'The creature gets +X/+0 until end of turn where X is the number of cards named Dark Ritual in your graveyard.',
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'dr1', name: 'Dark Ritual', type_line: 'Instant', cardType: 'Instant' },
            { id: 'dr2', name: 'Dark Ritual', type_line: 'Instant', cardType: 'Instant' },
          ],
          exile: [],
        } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2l1' }], hand: [], graveyard: [{ id: 'dr3', name: 'Dark Ritual', type_line: 'Instant', cardType: 'Instant' }], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetCardsNamedYourGy', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'targetCardsNamedYourGy' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCardsNamedYourGy') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });
  it.each([
    { whereText: 'X is one plus the number of cards named Ancestral Anger in your graveyard', expected: 3 },
    { whereText: 'X is one plus the number of cards named Feast of Flesh in all graveyards', expected: 4 },
    { whereText: 'X is one plus the number of cards named Kjeldoran War Cry in all graveyards', expected: 4 },
    { whereText: 'X is 2 plus the number of cards named Flame Burst in all graveyards', expected: 4 },
    { whereText: 'X is 2 plus the number of cards named Galvanic Bombardment in your graveyard', expected: 5 },
    { whereText: 'X is 2 plus the number of cards named Kindle in all graveyards', expected: 3 },
    { whereText: 'X is 3 plus the number of cards named Muscle Burst in all graveyards', expected: 6 },
    { whereText: 'X is one plus the number of cards named Mind Burst in all graveyards', expected: 2 },
    { whereText: 'X is one plus the number of cards named Aether Burst in all graveyards as you cast this spell', expected: 4 },
    { whereText: 'X is the number of cards named Feast of Flesh in all graveyards', expected: 3 },
    { whereText: 'X is the number of cards named Galvanic Bombardment in your graveyard', expected: 3 },
    { whereText: 'X is 5 minus the number of cards named Feast of Flesh in all graveyards', expected: 2 },
    { whereText: 'X is 4 minus the number of cards named Ancestral Anger in your graveyard', expected: 2 },
    { whereText: 'X is twice the number of cards named Kindle in all graveyards', expected: 2 },
    { whereText: 'X is half the number of cards named Feast of Flesh in all graveyards, rounded down', expected: 1 },
    { whereText: 'X is half the number of cards named Feast of Flesh in all graveyards, rounded up', expected: 2 },
    { whereText: 'X is half the number of cards named Galvanic Bombardment in your graveyard, rounded down', expected: 1 },
    { whereText: 'X is one plus the number of cards named Dark Ritual in your graveyard', expected: 1 },
  ])('applies executor modify_pt cards-named arithmetic lock: $whereText', ({ whereText, expected }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: `The creature gets +X/+0 until end of turn where ${whereText}.`,
      } as any,
    ];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1l1' }],
          hand: [],
          graveyard: [
            { id: 'aa1', name: 'Ancestral Anger', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'aa2', name: 'Ancestral Anger', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'gb1', name: 'Galvanic Bombardment', type_line: 'Instant', cardType: 'Instant' },
            { id: 'gb2', name: 'Galvanic Bombardment', type_line: 'Instant', cardType: 'Instant' },
            { id: 'gb3', name: 'Galvanic Bombardment', type_line: 'Instant', cardType: 'Instant' },
            { id: 'ff1', name: 'Feast of Flesh', type_line: 'Instant', cardType: 'Instant' },
            { id: 'mus1', name: 'Muscle Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'aeb1', name: 'Aether Burst', type_line: 'Instant', cardType: 'Instant' },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2l1' }],
          hand: [],
          graveyard: [
            { id: 'ff2', name: 'Feast of Flesh', type_line: 'Instant', cardType: 'Instant' },
            { id: 'ff3', name: 'Feast of Flesh', type_line: 'Instant', cardType: 'Instant' },
            { id: 'kwc1', name: 'Kjeldoran War Cry', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'kwc2', name: 'Kjeldoran War Cry', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'kwc3', name: 'Kjeldoran War Cry', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'fb1', name: 'Flame Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'fb2', name: 'Flame Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'k1', name: 'Kindle', type_line: 'Instant', cardType: 'Instant' },
            { id: 'mus2', name: 'Muscle Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'mus3', name: 'Muscle Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'mind1', name: 'Mind Burst', type_line: 'Sorcery', cardType: 'Sorcery' },
            { id: 'aeb2', name: 'Aether Burst', type_line: 'Instant', cardType: 'Instant' },
            { id: 'aeb3', name: 'Aether Burst', type_line: 'Instant', cardType: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'cardsNamedArithmeticTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cardsNamedArithmeticTarget',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'cardsNamedArithmeticTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });
  it.each([
    { whereText: "X is Agatha's power", expected: 6, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is Amy Rose's power", expected: 5, ownerName: 'Amy Rose', stat: 'power', statValue: 5 },
    { whereText: "X is Elenda's power", expected: 4, ownerName: 'Elenda', stat: 'power', statValue: 4 },
    { whereText: "X is Fang's power", expected: 3, ownerName: 'Fang', stat: 'power', statValue: 3 },
    { whereText: "X is Fire Lord Zuko's power", expected: 7, ownerName: 'Fire Lord Zuko', stat: 'power', statValue: 7 },
    { whereText: "X is Flavor Disaster's power", expected: 8, ownerName: 'Flavor Disaster', stat: 'power', statValue: 8 },
    { whereText: "X is Halana and Alena's power", expected: 5, ownerName: 'Halana and Alena', stat: 'power', statValue: 5 },
    { whereText: "X is Halana and Alena and Gisa and Geralf's power", expected: 9, ownerName: 'Halana and Alena and Gisa and Geralf', stat: 'power', statValue: 9 },
    { whereText: "X is Helga's power", expected: 4, ownerName: 'Helga', stat: 'power', statValue: 4 },
    { whereText: "X is Jyoti's power", expected: 6, ownerName: 'Jyoti', stat: 'power', statValue: 6 },
    { whereText: "X is Maelstrom Muse's power", expected: 3, ownerName: 'Maelstrom Muse', stat: 'power', statValue: 3 },
    { whereText: "X is Mona Lisa's power", expected: 5, ownerName: 'Mona Lisa', stat: 'power', statValue: 5 },
    { whereText: "X is Obuun's power", expected: 7, ownerName: 'Obuun', stat: 'power', statValue: 7 },
    { whereText: "X is Paladin Elizabeth Taggerdy's power", expected: 4, ownerName: 'Paladin Elizabeth Taggerdy', stat: 'power', statValue: 4 },
    { whereText: "X is Ratatwotwo's power", expected: 6, ownerName: 'Ratatwotwo', stat: 'power', statValue: 6 },
    { whereText: "X is Redshift's power", expected: 5, ownerName: 'Redshift', stat: 'power', statValue: 5 },
    { whereText: "X is Resilient Khenra's power", expected: 2, ownerName: 'Resilient Khenra', stat: 'power', statValue: 2 },
    { whereText: "X is Sarevok's power", expected: 8, ownerName: 'Sarevok', stat: 'power', statValue: 8 },
    { whereText: "X is Syr Faren's power", expected: 2, ownerName: 'Syr Faren', stat: 'power', statValue: 2 },
    { whereText: "X is Tifa Lockhart's power", expected: 9, ownerName: 'Tifa Lockhart', stat: 'power', statValue: 9 },
    { whereText: "X is Tomakul Phoenix's power", expected: 4, ownerName: 'Tomakul Phoenix', stat: 'power', statValue: 4 },
    { whereText: "X is Vadrik's power", expected: 3, ownerName: 'Vadrik', stat: 'power', statValue: 3 },
    { whereText: "X is Vivi Ornitier's power", expected: 2, ownerName: 'Vivi Ornitier', stat: 'power', statValue: 2 },
    { whereText: "X is Arek's intensity", expected: 4, ownerName: 'Arek', stat: 'intensity', statValue: 4 },
    { whereText: "X is Chittering Skullspeaker's intensity", expected: 3, ownerName: 'Chittering Skullspeaker', stat: 'intensity', statValue: 3 },
    { whereText: "X is Legion's Chant's intensity", expected: 2, ownerName: "Legion's Chant", stat: 'intensity', statValue: 2 },
    { whereText: "X is Minthara of the Absolute's intensity", expected: 5, ownerName: 'Minthara of the Absolute', stat: 'intensity', statValue: 5 },
    { whereText: "X is Teysa's intensity", expected: 4, ownerName: 'Teysa', stat: 'intensity', statValue: 4 },
  ])('applies executor modify_pt owner-stat lock: $whereText', ({ whereText, expected, ownerName, stat, statValue }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: `The creature gets +X/+0 until end of turn where ${whereText}.`,
      } as any,
    ];

    const ownerRef: any = {
      id: 'ownerStatRefObject',
      ownerId: 'p1',
      controller: 'p1',
      name: ownerName,
      type_line: stat === 'intensity' ? 'Artifact' : 'Creature',
      cardType: stat === 'intensity' ? 'Artifact' : 'Creature',
      counters: {},
    };
    if (stat === 'power') ownerRef.power = statValue;
    if (stat === 'intensity') ownerRef.intensity = statValue;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        ownerRef,
        { id: 'ownerStatEquip', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact - Equipment', type_line: 'Artifact - Equipment', attachedTo: 'ownerStatTarget', counters: {} } as any,
        { id: 'ownerStatTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'ownerStatEquip',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'ownerStatTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });
  it.each([
    { whereText: "X is one plus Agatha's power", expected: 7, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is 2 plus Amy Rose's power", expected: 7, ownerName: 'Amy Rose', stat: 'power', statValue: 5 },
    { whereText: "X is 10 minus Agatha's power", expected: 4, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is Agatha's power minus 2", expected: 4, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is twice Vadrik's power", expected: 6, ownerName: 'Vadrik', stat: 'power', statValue: 3 },
    { whereText: "X is half the Flavor Disaster's power, rounded down", expected: 4, ownerName: 'Flavor Disaster', stat: 'power', statValue: 8 },
    { whereText: "X is half the Tifa Lockhart's power, rounded up", expected: 5, ownerName: 'Tifa Lockhart', stat: 'power', statValue: 9 },
    { whereText: "X is one plus Arek's intensity", expected: 5, ownerName: 'Arek', stat: 'intensity', statValue: 4 },
    { whereText: "X is twice Legion's Chant's intensity", expected: 4, ownerName: "Legion's Chant", stat: 'intensity', statValue: 2 },
    { whereText: "X is half the Minthara of the Absolute's intensity, rounded down", expected: 2, ownerName: 'Minthara of the Absolute', stat: 'intensity', statValue: 5 },
    { whereText: "X is half the Minthara of the Absolute's intensity, rounded up", expected: 3, ownerName: 'Minthara of the Absolute', stat: 'intensity', statValue: 5 },
  ])('applies executor modify_pt owner-stat arithmetic lock: $whereText', ({ whereText, expected, ownerName, stat, statValue }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: 'The creature gets +X/+0 until end of turn where ' + whereText + '.',
      } as any,
    ];

    const ownerRef: any = {
      id: 'ownerStatArithmeticRefObject',
      ownerId: 'p1',
      controller: 'p1',
      name: ownerName,
      type_line: stat === 'intensity' ? 'Artifact' : 'Creature',
      cardType: stat === 'intensity' ? 'Artifact' : 'Creature',
      counters: {},
    };
    if (stat === 'power') ownerRef.power = statValue;
    if (stat === 'intensity') ownerRef.intensity = statValue;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        ownerRef,
        { id: 'ownerStatArithmeticEquip', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact - Equipment', type_line: 'Artifact - Equipment', attachedTo: 'ownerStatArithmeticTarget', counters: {} } as any,
        { id: 'ownerStatArithmeticTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'ownerStatArithmeticEquip',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'ownerStatArithmeticTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });

  it.each([
    { whereText: "X is Agatha's mana value", expected: 4, ownerName: 'Agatha', baseManaValue: 4 },
    { whereText: "X is one plus Agatha's mana value", expected: 5, ownerName: 'Agatha', baseManaValue: 4 },
    { whereText: "X is 2 plus Amy Rose's mana value", expected: 5, ownerName: 'Amy Rose', baseManaValue: 3 },
    { whereText: "X is 10 minus Agatha's mana value", expected: 6, ownerName: 'Agatha', baseManaValue: 4 },
    { whereText: "X is Agatha's mana value minus 2", expected: 2, ownerName: 'Agatha', baseManaValue: 4 },
    { whereText: "X is twice Vadrik's mana value", expected: 4, ownerName: 'Vadrik', baseManaValue: 2 },
    { whereText: "X is half the Tifa Lockhart's mana value, rounded down", expected: 2, ownerName: 'Tifa Lockhart', baseManaValue: 5 },
    { whereText: "X is half the Tifa Lockhart's mana value, rounded up", expected: 3, ownerName: 'Tifa Lockhart', baseManaValue: 5 },
  ])('applies executor modify_pt owner mana-value arithmetic lock: $whereText', ({ whereText, expected, ownerName, baseManaValue }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: 'The creature gets +X/+0 until end of turn where ' + whereText + '.',
      } as any,
    ];

    const ownerRef: any = {
      id: 'ownerManaValueArithmeticRefObject',
      ownerId: 'p1',
      controller: 'p1',
      name: ownerName,
      type_line: 'Creature',
      cardType: 'Creature',
      manaValue: baseManaValue,
      counters: {},
    };

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        ownerRef,
        { id: 'ownerManaValueArithmeticEquip', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact - Equipment', type_line: 'Artifact - Equipment', attachedTo: 'ownerManaValueArithmeticTarget', counters: {} } as any,
        { id: 'ownerManaValueArithmeticTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'ownerManaValueArithmeticEquip',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'ownerManaValueArithmeticTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });
  it.each([
    { whereText: "X is Agatha's power", expected: 6, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is Amy Rose's power", expected: 5, ownerName: 'Amy Rose', stat: 'power', statValue: 5 },
    { whereText: "X is Elenda's power", expected: 4, ownerName: 'Elenda', stat: 'power', statValue: 4 },
    { whereText: "X is Fang's power", expected: 3, ownerName: 'Fang', stat: 'power', statValue: 3 },
    { whereText: "X is Fire Lord Zuko's power", expected: 7, ownerName: 'Fire Lord Zuko', stat: 'power', statValue: 7 },
    { whereText: "X is Flavor Disaster's power", expected: 8, ownerName: 'Flavor Disaster', stat: 'power', statValue: 8 },
    { whereText: "X is Halana and Alena's power", expected: 5, ownerName: 'Halana and Alena', stat: 'power', statValue: 5 },
    { whereText: "X is Halana and Alena and Gisa and Geralf's power", expected: 9, ownerName: 'Halana and Alena and Gisa and Geralf', stat: 'power', statValue: 9 },
    { whereText: "X is Helga's power", expected: 4, ownerName: 'Helga', stat: 'power', statValue: 4 },
    { whereText: "X is Jyoti's power", expected: 6, ownerName: 'Jyoti', stat: 'power', statValue: 6 },
    { whereText: "X is Maelstrom Muse's power", expected: 3, ownerName: 'Maelstrom Muse', stat: 'power', statValue: 3 },
    { whereText: "X is Mona Lisa's power", expected: 5, ownerName: 'Mona Lisa', stat: 'power', statValue: 5 },
    { whereText: "X is Obuun's power", expected: 7, ownerName: 'Obuun', stat: 'power', statValue: 7 },
    { whereText: "X is Paladin Elizabeth Taggerdy's power", expected: 4, ownerName: 'Paladin Elizabeth Taggerdy', stat: 'power', statValue: 4 },
    { whereText: "X is Ratatwotwo's power", expected: 6, ownerName: 'Ratatwotwo', stat: 'power', statValue: 6 },
    { whereText: "X is Redshift's power", expected: 5, ownerName: 'Redshift', stat: 'power', statValue: 5 },
    { whereText: "X is Resilient Khenra's power", expected: 2, ownerName: 'Resilient Khenra', stat: 'power', statValue: 2 },
    { whereText: "X is Sarevok's power", expected: 8, ownerName: 'Sarevok', stat: 'power', statValue: 8 },
    { whereText: "X is Syr Faren's power", expected: 2, ownerName: 'Syr Faren', stat: 'power', statValue: 2 },
    { whereText: "X is Tifa Lockhart's power", expected: 9, ownerName: 'Tifa Lockhart', stat: 'power', statValue: 9 },
    { whereText: "X is Tomakul Phoenix's power", expected: 4, ownerName: 'Tomakul Phoenix', stat: 'power', statValue: 4 },
    { whereText: "X is Vadrik's power", expected: 3, ownerName: 'Vadrik', stat: 'power', statValue: 3 },
    { whereText: "X is Vivi Ornitier's power", expected: 2, ownerName: 'Vivi Ornitier', stat: 'power', statValue: 2 },
    { whereText: "X is Arek's intensity", expected: 4, ownerName: 'Arek', stat: 'intensity', statValue: 4 },
    { whereText: "X is Chittering Skullspeaker's intensity", expected: 3, ownerName: 'Chittering Skullspeaker', stat: 'intensity', statValue: 3 },
    { whereText: "X is Legion's Chant's intensity", expected: 2, ownerName: "Legion's Chant", stat: 'intensity', statValue: 2 },
    { whereText: "X is Minthara of the Absolute's intensity", expected: 5, ownerName: 'Minthara of the Absolute', stat: 'intensity', statValue: 5 },
    { whereText: "X is Teysa's intensity", expected: 4, ownerName: 'Teysa', stat: 'intensity', statValue: 4 },
  ])('applies executor modify_pt owner-stat lock: $whereText', ({ whereText, expected, ownerName, stat, statValue }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: `The creature gets +X/+0 until end of turn where ${whereText}.`,
      } as any,
    ];

    const ownerRef: any = {
      id: 'ownerStatRefObject',
      ownerId: 'p1',
      controller: 'p1',
      name: ownerName,
      type_line: stat === 'intensity' ? 'Artifact' : 'Creature',
      cardType: stat === 'intensity' ? 'Artifact' : 'Creature',
      counters: {},
    };
    if (stat === 'power') ownerRef.power = statValue;
    if (stat === 'intensity') ownerRef.intensity = statValue;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        ownerRef,
        { id: 'ownerStatEquip', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact - Equipment', type_line: 'Artifact - Equipment', attachedTo: 'ownerStatTarget', counters: {} } as any,
        { id: 'ownerStatTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'ownerStatEquip',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'ownerStatTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });
  it.each([
    { whereText: "X is Agatha's power", expected: 6, ownerName: 'Agatha', stat: 'power', statValue: 6 },
    { whereText: "X is Amy Rose's power", expected: 5, ownerName: 'Amy Rose', stat: 'power', statValue: 5 },
    { whereText: "X is Elenda's power", expected: 4, ownerName: 'Elenda', stat: 'power', statValue: 4 },
    { whereText: "X is Fang's power", expected: 3, ownerName: 'Fang', stat: 'power', statValue: 3 },
    { whereText: "X is Fire Lord Zuko's power", expected: 7, ownerName: 'Fire Lord Zuko', stat: 'power', statValue: 7 },
    { whereText: "X is Flavor Disaster's power", expected: 8, ownerName: 'Flavor Disaster', stat: 'power', statValue: 8 },
    { whereText: "X is Halana and Alena's power", expected: 5, ownerName: 'Halana and Alena', stat: 'power', statValue: 5 },
    { whereText: "X is Halana and Alena and Gisa and Geralf's power", expected: 9, ownerName: 'Halana and Alena and Gisa and Geralf', stat: 'power', statValue: 9 },
    { whereText: "X is Helga's power", expected: 4, ownerName: 'Helga', stat: 'power', statValue: 4 },
    { whereText: "X is Jyoti's power", expected: 6, ownerName: 'Jyoti', stat: 'power', statValue: 6 },
    { whereText: "X is Maelstrom Muse's power", expected: 3, ownerName: 'Maelstrom Muse', stat: 'power', statValue: 3 },
    { whereText: "X is Mona Lisa's power", expected: 5, ownerName: 'Mona Lisa', stat: 'power', statValue: 5 },
    { whereText: "X is Obuun's power", expected: 7, ownerName: 'Obuun', stat: 'power', statValue: 7 },
    { whereText: "X is Paladin Elizabeth Taggerdy's power", expected: 4, ownerName: 'Paladin Elizabeth Taggerdy', stat: 'power', statValue: 4 },
    { whereText: "X is Ratatwotwo's power", expected: 6, ownerName: 'Ratatwotwo', stat: 'power', statValue: 6 },
    { whereText: "X is Redshift's power", expected: 5, ownerName: 'Redshift', stat: 'power', statValue: 5 },
    { whereText: "X is Resilient Khenra's power", expected: 2, ownerName: 'Resilient Khenra', stat: 'power', statValue: 2 },
    { whereText: "X is Sarevok's power", expected: 8, ownerName: 'Sarevok', stat: 'power', statValue: 8 },
    { whereText: "X is Syr Faren's power", expected: 2, ownerName: 'Syr Faren', stat: 'power', statValue: 2 },
    { whereText: "X is Tifa Lockhart's power", expected: 9, ownerName: 'Tifa Lockhart', stat: 'power', statValue: 9 },
    { whereText: "X is Tomakul Phoenix's power", expected: 4, ownerName: 'Tomakul Phoenix', stat: 'power', statValue: 4 },
    { whereText: "X is Vadrik's power", expected: 3, ownerName: 'Vadrik', stat: 'power', statValue: 3 },
    { whereText: "X is Vivi Ornitier's power", expected: 2, ownerName: 'Vivi Ornitier', stat: 'power', statValue: 2 },
    { whereText: "X is Arek's intensity", expected: 4, ownerName: 'Arek', stat: 'intensity', statValue: 4 },
    { whereText: "X is Chittering Skullspeaker's intensity", expected: 3, ownerName: 'Chittering Skullspeaker', stat: 'intensity', statValue: 3 },
    { whereText: "X is Legion's Chant's intensity", expected: 2, ownerName: "Legion's Chant", stat: 'intensity', statValue: 2 },
    { whereText: "X is Minthara of the Absolute's intensity", expected: 5, ownerName: 'Minthara of the Absolute', stat: 'intensity', statValue: 5 },
    { whereText: "X is Teysa's intensity", expected: 4, ownerName: 'Teysa', stat: 'intensity', statValue: 4 },
  ])('applies executor modify_pt owner-stat lock: $whereText', ({ whereText, expected, ownerName, stat, statValue }) => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'equipped_creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: whereText },
        raw: `The creature gets +X/+0 until end of turn where ${whereText}.`,
      } as any,
    ];

    const ownerRef: any = {
      id: 'ownerStatRefObject',
      ownerId: 'p1',
      controller: 'p1',
      name: ownerName,
      type_line: stat === 'intensity' ? 'Artifact' : 'Creature',
      cardType: stat === 'intensity' ? 'Artifact' : 'Creature',
      counters: {},
    };
    if (stat === 'power') ownerRef.power = statValue;
    if (stat === 'intensity') ownerRef.intensity = statValue;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        ownerRef,
        { id: 'ownerStatEquip', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact - Equipment', type_line: 'Artifact - Equipment', attachedTo: 'ownerStatTarget', counters: {} } as any,
        { id: 'ownerStatTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', type_line: 'Creature', cardType: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'ownerStatEquip',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'ownerStatTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(expected);
    expect(ptMod.toughness).toBe(0);
  });
  it('applies executor modify_pt where X is Agatha\'s Soul Cauldron\'s intensity', () => {
    const steps = [
      {
        kind: 'modify_pt',
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "X is Agatha's Soul Cauldron's intensity" },
        raw: "Target creature gets +X/+0 until end of turn where X is Agatha's Soul Cauldron's intensity.",
      } as any,
    ];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1l1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'agathaCauldronRef', ownerId: 'p1', controller: 'p1', name: "Agatha's Soul Cauldron", cardType: 'Artifact', type_line: 'Legendary Artifact', intensity: 3, counters: {} } as any,
        { id: 'targetAgathaIntensity', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'agathaCauldronRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetAgathaIntensity') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of colors of mana spent to cast this spell', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of colors of mana spent to cast this spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellColorsRef', type: 'spell', manaColorsSpent: ['red', 'green', 'green', 'R'] } as any,
      ],
      battlefield: [
        { id: 'targetSpellColors', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellColorsRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellColors') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of colors that spell is', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of colors that spell is.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellColorIdentityRef', type: 'spell', spell: { colors: ['W', 'U', 'U'] } } as any,
      ],
      battlefield: [
        { id: 'targetSpellIdentityColors', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellColorIdentityRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellIdentityColors') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of mana spent to cast that spell', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of mana spent to cast that spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellAmountRef', type: 'spell', manaPayment: { white: 0, blue: 1, black: 0, red: 2, green: 0, colorless: 1, generic: 1 } } as any,
      ],
      battlefield: [
        { id: 'targetSpellAmount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellAmountRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellAmount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of mana spent to cast this creature (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of mana spent to cast this creature.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'creatureAmountRef', type: 'spell', manaPayment: { white: 0, blue: 0, black: 1, red: 1, green: 0, colorless: 0, generic: 2 } } as any,
      ],
      battlefield: [
        { id: 'targetCreatureAmount', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'creatureAmountRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCreatureAmount') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the total amount of mana paid this way', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the total amount of mana paid this way.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'manaPaidRef', type: 'spell', manaSpentTotal: 6 } as any,
      ],
      battlefield: [
        { id: 'targetManaPaid', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'manaPaidRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetManaPaid') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(6);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of mana that player paid this way', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of mana that player paid this way.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'manaPaidByPlayerRef', type: 'spell', manaSpentTotal: 4 } as any,
      ],
      battlefield: [
        { id: 'targetManaPaidByPlayer', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'manaPaidByPlayerRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetManaPaidByPlayer') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of excess damage dealt this way', () => {
    const ir = parseOracleTextToIR(
      'It deals 3 damage to each creature. Target creature you control gets +X/+0 until end of turn where X is the amount of excess damage dealt this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetExcessDamage', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessOne', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear A', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessTwo', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear B', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExcessDamage') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'deal_damage')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('counts excess damage from effective creatures in executor resolution', () => {
    const ir = parseOracleTextToIR(
      'It deals 3 damage to each creature. Target creature you control gets +X/+0 until end of turn where X is the amount of excess damage dealt this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetEffectiveExcessDamage', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'effectiveExcessDamageVictim', ownerId: 'p2', controller: 'p2', name: 'Animated Idol', cardType: 'Artifact', type_line: 'Artifact', effectiveTypes: ['Artifact', 'Creature'], power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'printedExcessDamageVictim', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEffectiveExcessDamage') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the excess damage dealt this way', () => {
    const ir = parseOracleTextToIR(
      'It deals 3 damage to each creature. Target creature you control gets +X/+0 until end of turn where X is the excess damage dealt this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetExcessDamageAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessAliasA', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear A', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessAliasB', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear B', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExcessDamageAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'deal_damage')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'deal_damage')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of cards looked at while scrying this way', () => {
    const ir = parseOracleTextToIR(
      'Scry 0. Target creature gets +X/+0 until end of turn where X is the number of cards looked at while scrying this way.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetScryLookedThisWay', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetScryLookedThisWay') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'scry')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'scry')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(ptMod).toBeTruthy();
    expect(ptMod.power).toBe(0);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of {E} paid this way', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of {E} paid this way.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'energyPaidRef', type: 'spell', manaPayment: { energy: 3, green: 1 } } as any,
      ],
      battlefield: [
        { id: 'targetEnergyPaid', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'energyPaidRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetEnergyPaid') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is the spell's mana value (alias)", () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is the spell's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'spellAliasMvRef', type: 'spell', manaValue: 5 } as any,
      ],
      battlefield: [
        { id: 'targetSpellAliasMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'spellAliasMvRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellAliasMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that excess damage (alias)', () => {
    const ir = parseOracleTextToIR(
      'It deals 3 damage to each creature. Target creature you control gets +X/+0 until end of turn where X is that excess damage.',
      'Test'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetExcessAliasShort', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 5, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessAliasShortA', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear A', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'oppExcessAliasShortB', ownerId: 'p2', controller: 'p2', name: 'Opponent Bear B', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExcessAliasShort') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of life opponents have lost this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of life opponents have lost this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppLifeLostAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      lifeLostThisTurn: { p1: 0, p2: 4, p3: 1 } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppLifeLostAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of spells opponents cast this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of spells opponents cast this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppSpellsAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      spellsCastThisTurn: { p1: 1, p2: 2, p3: 1 } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSpellsAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of permanents opponents sacrificed this turn (alias)', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of permanents opponents sacrificed this turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetOppSacAlias', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      permanentsSacrificedThisTurn: { p1: 1, p2: 3, p3: 2 } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetOppSacAlias') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of players being attacked', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of players being attacked.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqPlayersAttacked', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetPlayersAttacked', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetPlayersAttacked', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'attackerPlayersA', ownerId: 'p1', controller: 'p1', name: 'Attacker A', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'attackerPlayersB', ownerId: 'p1', controller: 'p1', name: 'Attacker B', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, attacking: 'p3', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqPlayersAttacked' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetPlayersAttacked') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of attacking creatures with flying', () => {
    const ir = parseOracleTextToIR(
      'The creature gets +X/+0 until end of turn where X is the number of attacking creatures with flying.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'eqFlyingAttackers', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact', attachedTo: 'targetFlyingAttackers', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'targetFlyingAttackers', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'flyingAttackerA', ownerId: 'p1', controller: 'p1', name: 'Flyer A', cardType: 'Creature', type_line: 'Creature', keywords: ['Flying'], power: 2, toughness: 2, attacking: 'p2', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'flyingAttackerB', ownerId: 'p2', controller: 'p2', name: 'Flyer B', cardType: 'Creature', type_line: 'Creature', text: 'Flying', power: 2, toughness: 2, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
        { id: 'groundAttacker', ownerId: 'p2', controller: 'p2', name: 'Ground', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, attacking: 'p1', tapped: true, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqFlyingAttackers' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetFlyingAttackers') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is this spell's intensity", () => {
    const steps = [
      {
        kind: 'modify_pt',
        raw: "Target creature gets +X/+0 until end of turn where X is this spell's intensity.",
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        toughnessUsesX: false,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "x is this spell's intensity" },
      },
    ] as any;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'intensityRefSpell', type: 'spell', intensity: 4 } as any,
      ],
      battlefield: [
        { id: 'targetSpellIntensity', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'intensityRefSpell' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSpellIntensity') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it("applies X-based modify_pt where X is Skanos's power", () => {
    const steps = [
      {
        kind: 'modify_pt',
        raw: "Target creature gets +X/+0 until end of turn where X is Skanos's power.",
        target: { kind: 'raw', text: 'target creature' },
        power: 1,
        toughness: 0,
        powerUsesX: true,
        toughnessUsesX: false,
        duration: 'end_of_turn',
        condition: { kind: 'where', raw: "x is skanos's power" },
      },
    ] as any;

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetNamedPower', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', type_line: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      stack: [
        { id: 'skanosRef', type: 'spell', name: 'Skanos, Blue Dragon Vassal', power: 7 } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetNamedPower') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the amount of {S} spent to cast this spell', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the amount of {S} spent to cast this spell.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      stack: [
        { id: 'snowSpentRef', type: 'spell', manaPayment: { snow: 2, green: 1, colorless: 1 } } as any,
      ],
      battlefield: [
        { id: 'targetSnowSpent', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'snowSpentRef' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetSnowSpent') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(2);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is that card\'s mana value', () => {
    const ir = parseOracleTextToIR(
      "Target creature gets +X/+0 until end of turn where X is that card's mana value.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [{ id: 'refCard1', manaValue: 4, type_line: 'Sorcery' }],
        } as any,
      ],
      battlefield: [
        { id: 'targetCardMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'refCard1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetCardMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the mana value of the exiled card', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the mana value of the exiled card.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [{ id: 'exiledRef2', manaValue: 7, type_line: 'Creature � Dragon' }],
        } as any,
      ],
      battlefield: [
        { id: 'targetExiledMv', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'exiledRef2' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetExiledMv') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(7);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of +1/+1 counters on this creature', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of +1/+1 counters on this creature.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'counterTarget', ownerId: 'p1', controller: 'p1', name: 'Counter Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: { '+1/+1': 3, charge: 2 } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'counterTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(3);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of verse counters on this enchantment', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of verse counters on this enchantment.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'verseTarget', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'verseSource', ownerId: 'p1', controller: 'p1', name: 'Song', type_line: 'Enchantment � Saga', tapped: false, summoningSick: false, counters: { verse: 4, lore: 1 } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'verseSource' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'verseTarget') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of charge counters on this artifact', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of charge counters on this artifact.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'chargeCounterArtifact', ownerId: 'p1', controller: 'p1', name: 'Charge Relic', cardType: 'Artifact', type_line: 'Artifact', counters: { charge: 4 } } as any,
        { id: 'targetChargeCounterCreature', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'chargeCounterArtifact',
      selectorContext: { targetCreatureId: 'targetChargeCounterCreature' },
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetChargeCounterCreature') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your devotion to blue', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your devotion to blue.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaCost: '{2}{U}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'perm1', ownerId: 'p1', controller: 'p1', name: 'Mind Relic', cardType: 'Artifact', manaCost: '{1}{U}{U}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'perm2', ownerId: 'p1', controller: 'p1', name: 'Hybrid Matrix', cardType: 'Artifact', manaCost: '{U/B}{U/B}', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your devotion to black', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your devotion to black.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'target1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaCost: '{1}{B}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'perm1', ownerId: 'p1', controller: 'p1', name: 'Dark Relic', cardType: 'Artifact', manaCost: '{B}{B}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'perm2', ownerId: 'p1', controller: 'p1', name: 'Hybrid Matrix', cardType: 'Artifact', manaCost: '{U/B}', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'target1') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your devotion to white', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your devotion to white.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetWhiteDevotion', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaCost: '{2}{W}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whitePerm1', ownerId: 'p1', controller: 'p1', name: 'White Relic', cardType: 'Artifact', manaCost: '{W}{W}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'whitePerm2', ownerId: 'p1', controller: 'p1', name: 'Hybrid Matrix', cardType: 'Artifact', manaCost: '{G/W}', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetWhiteDevotion') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is your devotion to green', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your devotion to green.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetGreenDevotion', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaCost: '{1}{G}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'greenPerm1', ownerId: 'p1', controller: 'p1', name: 'Green Relic', cardType: 'Artifact', manaCost: '{G}{G}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'greenPerm2', ownerId: 'p1', controller: 'p1', name: 'Hybrid Matrix', cardType: 'Artifact', manaCost: '{G/U}', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetGreenDevotion') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('applies X-based modify_pt where X is the number of red mana symbols in the mana costs of permanents you control', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is the number of red mana symbols in the mana costs of permanents you control.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        { id: 'targetRedSymbols', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', manaCost: '{2}{R}', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'redPerm1', ownerId: 'p1', controller: 'p1', name: 'Ember Relic', cardType: 'Artifact', manaCost: '{1}{R}{R}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'redPerm2', ownerId: 'p1', controller: 'p1', name: 'Hybrid Forge', cardType: 'Artifact', manaCost: '{R/G}{U/R}', tapped: false, summoningSick: false, counters: {} } as any,
        { id: 'redPerm3', ownerId: 'p1', controller: 'p1', name: 'Neutral Engine', cardType: 'Artifact', manaCost: '{4}', tapped: false, summoningSick: false, counters: {} } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetRedSymbols') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(5);
    expect(ptMod.toughness).toBe(0);
  });

  it('safely skips modify_pt with still-unsupported where expression', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+X until end of turn where X is the square root of your life total.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [{ id: 'g1' }, { id: 'g2' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'creature1',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Test Bear',
          cardType: 'Creature',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'creature1') as any;
    const modifiers = Array.isArray(creature?.modifiers) ? creature.modifiers : [];

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(modifiers.some((m: any) => m?.type === 'powerToughness')).toBe(false);
  });

  it('applies Undercity Informer activated reveal-until-land clause as deterministic mill loop', () => {
    const ir = parseOracleTextToIR(
      '{1}, Sacrifice a creature: Target player reveals cards from the top of their library until they reveal a land card, then puts those cards into their graveyard.',
      'Undercity Informer'
    );
    const ability = ir.abilities.find(a => a.type === 'activated');
    const steps = ability?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [
            { id: 'p2c1', type_line: 'Creature � Human' },
            { id: 'p2c2', type_line: 'Instant' },
            { id: 'p2c3', type_line: 'Land' },
            { id: 'p2c4', type_line: 'Sorcery' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'mill')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'mill')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c4']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
  });

  it('creates a Treasure token', () => {
    const ir = parseOracleTextToIR('Create a Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.controller).toBe('p1');
    expect((token.card?.name || '').toLowerCase()).toContain('treasure');
  });

  it('creates a Treasure token for each player', () => {
    const ir = parseOracleTextToIR('Each player creates a Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    const byController = result.state.battlefield.reduce(
      (acc: Record<string, number>, perm: any) => {
        acc[String(perm.controller)] = (acc[String(perm.controller)] || 0) + 1;
        return acc;
      },
      {} as Record<string, number>
    );
    expect(byController['p1']).toBe(1);
    expect(byController['p2']).toBe(1);
    expect(result.appliedSteps.some(s => s.kind === 'create_token')).toBe(true);
  });

  it('creates a Treasure token for each opponent', () => {
    const ir = parseOracleTextToIR('Each opponent creates a Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    const controllers = result.state.battlefield.map((p: any) => p.controller).sort();
    expect(controllers).toEqual(['p2', 'p3']);
    expect(result.appliedSteps.some(s => s.kind === 'create_token')).toBe(true);
  });

  it('creates a Treasure token for defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('Defending player creates a Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Test',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.controller).toBe('p2');
    expect((token.card?.name || '').toLowerCase()).toContain('treasure');
    expect(result.appliedSteps.some(s => s.kind === 'create_token')).toBe(true);
  });

  it('creates a Treasure token for the defending player via target_opponent selector context binding', () => {
    const ir = parseOracleTextToIR('The defending player creates a Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Test',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.controller).toBe('p2');
    expect((token.card?.name || '').toLowerCase()).toContain('treasure');
    expect(result.appliedSteps.some(s => s.kind === 'create_token')).toBe(true);
  });

  it('creates other common artifact tokens (Gold, Powerstone)', () => {
    const ir = parseOracleTextToIR('Create a Gold token. Create a Powerstone token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    const names = result.state.battlefield.map((p: any) => String(p?.card?.name || '').toLowerCase());
    expect(names.some(n => n.includes('gold'))).toBe(true);
    expect(names.some(n => n.includes('powerstone'))).toBe(true);
  });

  it('creates a tapped token when oracle says tapped', () => {
    const ir = parseOracleTextToIR('Create a tapped Treasure token.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.tapped).toBe(true);
  });

  it('creates a tapped token when oracle says "token tapped"', () => {
    const ir = parseOracleTextToIR('Create a Treasure token tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.tapped).toBe(true);
  });

  it('creates a tapped token when oracle uses a follow-up "enters tapped" clause', () => {
    const ir = parseOracleTextToIR('Create a Treasure token. It enters tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.tapped).toBe(true);
  });

  it('creates multiple tapped tokens when oracle uses a follow-up "They enter tapped" clause', () => {
    const ir = parseOracleTextToIR('Create two Treasure tokens. They enter tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.tapped).toBe(true);
      expect(String(perm.card?.name || '').toLowerCase()).toContain('treasure');
    }
  });

  it('creates tapped tokens for multi-token creation with follow-up "They enter tapped" clause', () => {
    const ir = parseOracleTextToIR('Create a Treasure token and a Clue token. They enter tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    const names = result.state.battlefield.map((p: any) => String(p?.card?.name || '').toLowerCase());
    expect(names.some(n => n.includes('treasure'))).toBe(true);
    expect(names.some(n => n.includes('clue'))).toBe(true);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.tapped).toBe(true);
    }
  });

  it('creates multiple tokens with counters when oracle uses plural follow-up counters wording', () => {
    const ir = parseOracleTextToIR('Create two Treasure tokens. They enter with two +1/+1 counters on them.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.counters?.['+1/+1']).toBe(2);
    }
  });

  it('creates multiple tapped tokens with counters for combined plural follow-up modifiers', () => {
    const ir = parseOracleTextToIR(
      'Create two Treasure tokens. They enter tapped and with two +1/+1 counters on them.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.tapped).toBe(true);
      expect(perm.counters?.['+1/+1']).toBe(2);
    }
  });

  it('creates multiple tapped tokens for semicolon + lowercase follow-up wording', () => {
    const ir = parseOracleTextToIR('Create two Treasure tokens; they enter tapped.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.tapped).toBe(true);
      expect(String(perm.card?.name || '').toLowerCase()).toContain('treasure');
    }
  });

  it('creates multiple tokens with a shield counter for plural follow-up singular counter wording', () => {
    const ir = parseOracleTextToIR('Create two Treasure tokens. They enter with a shield counter on them.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(2);
    for (const perm of result.state.battlefield as any[]) {
      expect(perm.isToken).toBe(true);
      expect(perm.counters?.shield).toBe(1);
    }
  });

  it('creates a token with counters when oracle specifies counters', () => {
    const ir = parseOracleTextToIR(
      'Create a 1/1 white Soldier creature token with two +1/+1 counters on it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.counters?.['+1/+1']).toBe(2);
  });

  it('creates a token with counters when oracle uses "an additional" counter wording', () => {
    const ir = parseOracleTextToIR('Create a Treasure token with an additional +1/+1 counter on it.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.counters?.['+1/+1']).toBe(1);
  });

  it('creates a token with counters when oracle uses a follow-up "enters with counters" clause', () => {
    const ir = parseOracleTextToIR('Create a Treasure token. It enters with two +1/+1 counters on it.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.counters?.['+1/+1']).toBe(2);
  });

  it('attaches "They gain <keywords> until end of turn" as a create_token follow-up modifier', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 white Soldier creature tokens. They gain flying and lifelink until end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const create = steps.find((s: any) => s.kind === 'create_token') as any;
    expect(create).toBeTruthy();
    expect(create.grantsAbilitiesUntilEndOfTurn).toEqual(['flying', 'lifelink']);
  });

  it('creates a token with counters when oracle uses a follow-up "an additional" counters clause', () => {
    const ir = parseOracleTextToIR(
      'Create a Treasure token. It enters with an additional +1/+1 counter on it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.counters?.['+1/+1']).toBe(1);
  });

  it('creates a tapped token with counters for combined follow-up modifiers', () => {
    const ir = parseOracleTextToIR(
      'Create a Treasure token. It enters the battlefield tapped and with two +1/+1 counters on it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceName: 'Test' });

    expect(result.state.battlefield.length).toBe(1);
    const token = result.state.battlefield[0] as any;
    expect(token.isToken).toBe(true);
    expect(token.tapped).toBe(true);
    expect(token.counters?.['+1/+1']).toBe(2);
  });

  it('attaches haste and next-end-step sacrifice follow-ups to create_token IR', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 red Elemental creature tokens. They gain haste. Sacrifice them at the beginning of the next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.grantsHaste).toBe('permanent');
    expect(step.atNextEndStep).toBe('sacrifice');
  });

  it('attaches triggered-template next-end-step cleanup to create_token IR', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 red Elemental creature tokens. They gain haste. At the beginning of the next end step, sacrifice them.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.grantsHaste).toBe('permanent');
    expect(step.atNextEndStep).toBe('sacrifice');
  });

  it('attaches end-of-turn exile cleanup to create_token IR (Oracle shorthand)', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 blue Illusion creature tokens. Exile them at end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.atNextEndStep).toBe('exile');
  });

  it('attaches triggered-template end-of-turn sacrifice cleanup to create_token IR (Oracle shorthand)', () => {
    const ir = parseOracleTextToIR(
      'Create a 2/2 colorless Robot artifact creature token. At end of turn, sacrifice that token.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.atNextEndStep).toBe('sacrifice');
  });

  it('attaches end-of-combat exile follow-up to create_token IR', () => {
    const ir = parseOracleTextToIR(
      "Create two 1/1 blue Illusion creature tokens. Exile those tokens at end of combat.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.atEndOfCombat).toBe('exile');
  });

  it('attaches triggered-template end-of-combat sacrifice follow-up to create_token IR', () => {
    const ir = parseOracleTextToIR(
      'Create a 2/2 colorless Robot artifact creature token. At end of combat, sacrifice that token.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.atEndOfCombat).toBe('sacrifice');
  });

  it('schedules delayed token sacrifice cleanup without removing the created tokens immediately', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 red Elemental creature tokens. At the beginning of the next end step, sacrifice them.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({ turnNumber: 3 } as any);
    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'source-token-cleanup',
      sourceName: 'Test',
    });

    expect(result.state.battlefield).toHaveLength(2);
    const registry = (result.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('Sacrifice those tokens.');
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toHaveLength(2);
  });

  it('fires delayed token exile cleanup against the exact originally created tokens', () => {
    const ir = parseOracleTextToIR(
      'Create two 1/1 blue Illusion creature tokens. Exile those tokens at end of combat.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      battlefield: [
        {
          id: 'preexisting-illusion',
          controller: 'p1',
          owner: 'p1',
          tapped: false,
          summoningSickness: false,
          counters: {},
          attachedTo: undefined,
          attachments: [],
          modifiers: [],
          isToken: true,
          basePower: 1,
          baseToughness: 1,
          card: {
            id: 'preexisting-illusion',
            name: 'Illusion',
            type_line: 'Creature - Illusion',
            oracle_text: '',
            power: '1',
            toughness: '1',
            colors: ['U'],
            mana_cost: '',
            cmc: 0,
            image_uris: {},
          },
        } as any,
      ],
    } as any);

    const created = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'source-token-cleanup',
      sourceName: 'Test',
    });

    expect(created.state.battlefield).toHaveLength(3);

    const registry = (created.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'combat_end', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      created.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect(resolved.state.battlefield).toHaveLength(1);
    expect((resolved.state.battlefield[0] as any)?.id).toBe('preexisting-illusion');
  });

  it('schedules standalone delayed sacrifice cleanup against the exact bound creature', () => {
    const ir = parseOracleTextToIR(
      'Draw a card. At the beginning of the next end step, sacrifice that creature.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 4,
      battlefield: [
        {
          id: 'bound-cleanup-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'bound-cleanup-card', name: 'Ball Lightning', type_line: 'Creature - Elemental' },
        },
        {
          id: 'lookalike-cleanup-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'lookalike-cleanup-card', name: 'Ball Lightning', type_line: 'Creature - Elemental' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cleanup-source',
      sourceName: 'Test',
      targetPermanentId: 'bound-cleanup-creature',
    });

    expect(scheduled.state.battlefield).toHaveLength(2);
    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice that creature.');
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toEqual(['bound-cleanup-creature']);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['lookalike-cleanup-creature']);
  });

  it('schedules delayed sacrifice cleanup with a mana value condition and removes only when the condition is true', () => {
    const ir = parseOracleTextToIR(
      'Sacrifice it at the beginning of the next end step if it has mana value 3 or less.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 4,
      battlefield: [
        {
          id: 'cheap-bound-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'cheap-bound-card', name: 'Cheap Threat', type_line: 'Creature - Elemental', mana_value: 2 },
        },
        {
          id: 'expensive-safe-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'expensive-safe-card', name: 'Expensive Threat', type_line: 'Creature - Elemental', mana_value: 6 },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cheap-bound-creature',
      sourceName: 'Cheap Threat',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice that creature if it has mana value 3 or less.');

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['expensive-safe-creature']);
  });

  it('schedules standalone delayed self-sacrifice cleanup for your next end step', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature attacks, sacrifice this creature at the beginning of your next end step.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 8,
      battlefield: [
        {
          id: 'self-cleanup-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'self-cleanup-card', name: 'Ball Lightning', type_line: 'Creature - Elemental' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'self-cleanup-creature',
      sourceName: 'Ball Lightning',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice that creature.');

    const noFire = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    expect(noFire.triggersToFire).toHaveLength(0);

    const fire = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' as any });
    expect(fire.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(fire.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect(resolved.state.battlefield).toHaveLength(0);
  });

  it('schedules standalone delayed token sacrifice cleanup for your next upkeep', () => {
    const ir = parseOracleTextToIR(
      'Sacrifice those tokens at the beginning of your next upkeep.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 8,
      battlefield: [
        {
          id: 'token-a',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'token-a-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'token-b',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'token-b-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'nontoken-safe',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'nontoken-safe-card', name: 'Safe Creature', type_line: 'Creature - Elemental' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'source-token-cleanup',
      sourceName: 'Test',
      selectorContext: {
        chosenObjectIds: ['token-a', 'token-b'],
      },
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice those tokens.');

    const noFire = checkDelayedTriggers(registry, { type: 'upkeep', activePlayerId: 'p2' as any });
    expect(noFire.triggersToFire).toHaveLength(0);

    const fire = checkDelayedTriggers(registry, { type: 'upkeep', activePlayerId: 'p1' as any });
    expect(fire.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(fire.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['nontoken-safe']);
  });

  it('treats zero-counter sacrifice conditions as deterministic no-ops until the counters are gone', () => {
    const ir = parseOracleTextToIR(
      'Sacrifice this enchantment if there are no echo counters on it.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const withCounters = makeState({
      battlefield: [
        {
          id: 'echo-enchantment',
          controller: 'p1',
          owner: 'p1',
          counters: { echo: 1 },
          card: { id: 'echo-enchantment-card', name: 'Echo Aura', type_line: 'Enchantment - Aura' },
        },
      ] as any,
    });

    const skipped = applyOracleIRStepsToGameState(withCounters, steps, {
      controllerId: 'p1',
      sourceId: 'echo-enchantment',
      sourceName: 'Echo Aura',
    });

    expect(skipped.state.battlefield).toHaveLength(1);
    expect((skipped.state as any).oracleAutomationGaps || []).toHaveLength(0);

    const withoutCounters = makeState({
      battlefield: [
        {
          id: 'echo-enchantment',
          controller: 'p1',
          owner: 'p1',
          counters: {},
          card: { id: 'echo-enchantment-card', name: 'Echo Aura', type_line: 'Enchantment - Aura' },
        },
      ] as any,
    });

    const resolved = applyOracleIRStepsToGameState(withoutCounters, steps, {
      controllerId: 'p1',
      sourceId: 'echo-enchantment',
      sourceName: 'Echo Aura',
    });

    expect(resolved.state.battlefield).toHaveLength(0);
  });

  it('schedules standalone delayed end-of-combat exile cleanup for contextual tokens', () => {
    const ir = parseOracleTextToIR('Draw a card. Exile those tokens at end of combat.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'bound-token-a',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'bound-token-a-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'bound-token-b',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'bound-token-b-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'unbound-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'unbound-token-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cleanup-source',
      sourceName: 'Test',
      selectorContext: { chosenObjectIds: ['bound-token-a', 'bound-token-b'] } as any,
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('Exile those tokens.');

    const delayedCheck = checkDelayedTriggers(registry, { type: 'combat_end', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['unbound-token']);
  });

  it('schedules delayed sacrifice cleanup for a contextual vehicle reference against the exact bound vehicle', () => {
    const ir = parseOracleTextToIR(
      'Draw a card. At the beginning of the next end step, sacrifice that Vehicle.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 12,
      battlefield: [
        {
          id: 'bound-vehicle-cleanup',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'bound-vehicle-cleanup-card', name: 'Sky Skiff', type_line: 'Artifact - Vehicle' },
        },
        {
          id: 'other-vehicle-cleanup',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'other-vehicle-cleanup-card', name: 'Untethered Express', type_line: 'Artifact - Vehicle' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cleanup-source',
      sourceName: 'Test',
      selectorContext: { chosenObjectIds: ['bound-vehicle-cleanup'] } as any,
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toEqual(['bound-vehicle-cleanup']);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['other-vehicle-cleanup']);
  });

  it('schedules delayed sacrifice cleanup for "the token" alias against the exact bound token', () => {
    const ir = parseOracleTextToIR(
      'Draw a card. At the beginning of the next end step, sacrifice the token.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 12,
      battlefield: [
        {
          id: 'bound-token-cleanup',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'bound-token-cleanup-card', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
        {
          id: 'other-token-cleanup',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'other-token-cleanup-card', name: 'Treasure', type_line: 'Artifact - Treasure' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cleanup-source',
      sourceName: 'Test',
      selectorContext: { chosenObjectIds: ['bound-token-cleanup'] } as any,
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice that token.');

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['other-token-cleanup']);
  });

  it('schedules delayed sacrifice cleanup for "enchanted creature" against the currently enchanted permanent', () => {
    const ir = parseOracleTextToIR(
      'Draw a card. At the beginning of the next end step, sacrifice enchanted creature.',
      'Cocoon'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 15,
      battlefield: [
        {
          id: 'cocoon-cleanup-aura',
          controller: 'p1',
          owner: 'p1',
          attachedTo: 'cocoon-bound-creature',
          card: { id: 'cocoon-cleanup-aura-card', name: 'Cocoon', type_line: 'Enchantment - Aura' },
        },
        {
          id: 'cocoon-bound-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'cocoon-bound-creature-card', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
        },
        {
          id: 'cocoon-safe-creature',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'cocoon-safe-creature-card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cocoon-cleanup-aura',
      sourceName: 'Cocoon',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effect).toBe('You sacrifice that creature.');
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toEqual(['cocoon-bound-creature']);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual([
      'cocoon-cleanup-aura',
      'cocoon-safe-creature',
    ]);
  });

  it('tracks token provenance and sacrifices only tokens created with the source', () => {
    const createIR = parseOracleTextToIR('Create two 1/1 red Elemental creature tokens.', 'Form of the Stax Player');
    const createSteps = createIR.abilities[0]?.steps ?? [];
    const created = applyOracleIRStepsToGameState(makeState(), createSteps, {
      controllerId: 'p1',
      sourceId: 'form-stax-player',
      sourceName: 'Form of the Stax Player',
    });

    const createdTokens = (created.state.battlefield as any[]).filter(perm => perm.isToken);
    expect(createdTokens).toHaveLength(2);
    expect(createdTokens.every(token => token.createdBySourceId === 'form-stax-player')).toBe(true);

    const withUnrelatedToken = {
      ...created.state,
      battlefield: [
        ...(created.state.battlefield as any[]),
        {
          id: 'other-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'other-token', name: 'Elemental', type_line: 'Creature - Elemental' },
        },
      ] as any,
    } as any;

    const ir = parseOracleTextToIR('Sacrifice each token created with it.', 'Form of the Stax Player');
    const steps = ir.abilities[0]?.steps ?? [];
    const result = applyOracleIRStepsToGameState(withUnrelatedToken, steps, {
      controllerId: 'p1',
      sourceId: 'form-stax-player',
      sourceName: 'Form of the Stax Player',
    });

    expect((result.state.battlefield as any[]).map(perm => perm.id)).toEqual(['other-token']);
  });

  it('schedules leave-the-battlefield delayed sacrifice cleanup against the watched token', () => {
    const ir = parseOracleTextToIR('Sacrifice Stangg when that token leaves the battlefield.', 'Stangg');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 6,
      battlefield: [
        {
          id: 'stangg-perm',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'stangg-card', name: 'Stangg', type_line: 'Legendary Creature - Human Warrior' },
        },
        {
          id: 'stangg-token',
          controller: 'p1',
          owner: 'p1',
          isToken: true,
          card: { id: 'stangg-token-card', name: 'Stangg Twin', type_line: 'Legendary Creature - Human Warrior' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'stangg-perm',
      sourceName: 'Stangg',
      targetPermanentId: 'stangg-token',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.watchingPermanentId).toBe('stangg-token');
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toEqual(['stangg-perm']);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'permanent_left', permanentId: 'stangg-token' });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect((resolved.state.battlefield as any[]).map(perm => perm.id)).toEqual(['stangg-token']);
  });

  it('schedules lose-control delayed sacrifice cleanup against the watched source', () => {
    const ir = parseOracleTextToIR('Sacrifice the creature when you lose control of this creature.', 'Seraph');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 8,
      battlefield: [
        {
          id: 'seraph-source',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'seraph-card', name: 'Seraph', type_line: 'Creature - Angel' },
        },
        {
          id: 'borrowed-creature',
          controller: 'p1',
          owner: 'p2',
          card: { id: 'borrowed-card', name: 'Borrowed Bear', type_line: 'Creature - Bear' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'seraph-source',
      sourceName: 'Seraph',
      targetPermanentId: 'borrowed-creature',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.timing).toBe(DelayedTriggerTiming.WHEN_CONTROL_LOST);
    expect(registry?.triggers?.[0]?.watchingPermanentId).toBe('seraph-source');
    expect(registry?.triggers?.[0]?.eventDataSnapshot?.chosenObjectIds).toEqual(['borrowed-creature']);
  });

  it('schedules next-cleanup-step sacrifice cleanup and fires on cleanup', () => {
    const ir = parseOracleTextToIR('Sacrifice this Aura at the beginning of the next cleanup step.', 'Test Aura');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 10,
      battlefield: [
        {
          id: 'cleanup-aura',
          controller: 'p1',
          owner: 'p1',
          card: { id: 'cleanup-aura-card', name: 'Test Aura', type_line: 'Enchantment - Aura' },
        },
      ] as any,
    });

    const scheduled = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'cleanup-aura',
      sourceName: 'Test Aura',
    });

    const registry = (scheduled.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.timing).toBe(DelayedTriggerTiming.NEXT_CLEANUP);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'cleanup', activePlayerId: 'p1' as any });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      scheduled.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect(resolved.state.battlefield).toHaveLength(0);
  });

  it('applies immediate sacrifice when a counter-threshold condition is true', () => {
    const ir = parseOracleTextToIR('Then sacrifice it if it has five or more bloodstain counters on it.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'bloodstained-creature',
          controller: 'p1',
          owner: 'p1',
          counters: { bloodstain: 5 },
          card: { id: 'bloodstained-card', name: 'Marked Creature', type_line: 'Creature - Horror', mana_value: 4 },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'bloodstained-creature',
      sourceName: 'Marked Creature',
    });

    expect(result.appliedSteps.some(step => step.kind === 'sacrifice')).toBe(true);
    expect(result.skippedSteps.some(step => step.kind === 'sacrifice')).toBe(false);
    expect(result.state.battlefield).toHaveLength(0);
  });

  it('does not sacrifice when a counter-threshold condition is false and does not record a gap', () => {
    const ir = parseOracleTextToIR('Then sacrifice it if it has five or more bloodstain counters on it.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'safe-creature',
          controller: 'p1',
          owner: 'p1',
          counters: { bloodstain: 4 },
          card: { id: 'safe-card', name: 'Safe Creature', type_line: 'Creature - Horror', mana_value: 4 },
        },
      ] as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'safe-creature',
      sourceName: 'Safe Creature',
    });

    expect(result.state.battlefield).toHaveLength(1);
    expect((result.state.battlefield as any[])[0]?.id).toBe('safe-creature');
    expect(result.appliedSteps.some(step => step.kind === 'sacrifice')).toBe(false);
    expect(result.skippedSteps.some(step => step.kind === 'sacrifice')).toBe(true);
    expect(result.automationGaps).toHaveLength(0);
    expect(((result.state as any).oracleAutomationGaps || []).length).toBe(0);
  });

  it('attaches until-EOT haste follow-up to create_token IR', () => {
    const ir = parseOracleTextToIR(
      'Create a 1/1 red Elemental creature token. It gains haste until end of turn.',
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    expect(steps).toHaveLength(1);
    const step = steps[0] as any;
    expect(step.kind).toBe('create_token');
    expect(step.grantsHaste).toBe('until_end_of_turn');
  });

  it('skips optional steps by default', () => {
    const ir = parseOracleTextToIR('You may draw a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState();
    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    expect(p1.hand).toHaveLength(0);
    expect(result.skippedSteps.length).toBeGreaterThan(0);

    const forced = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' }, { allowOptional: true });
    const p1b = forced.state.players.find(p => p.id === 'p1') as any;
    expect(p1b.hand).toHaveLength(1);
  });

  it('exiles the top card for face-down exile with combined look-and-play permission', () => {
    const text =
      "Exile the top card of each opponent's library face down. You may look at and play those cards this turn.";

    const ir = parseOracleTextToIR(text, 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(p2.library).toHaveLength(1);
    expect(p2.exile).toHaveLength(1);
    expect(p2.exile[0]?.id).toBe('p2c1');
    expect(p3.library).toHaveLength(1);
    expect(p3.exile).toHaveLength(1);
    expect(p3.exile[0]?.id).toBe('p3c1');
    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
  });

  it('applies impulse_exile_top when target_opponent has a single legal candidate (1v1)', () => {
    const ir = parseOracleTextToIR(
      "Exile the top two cards of target opponent's library face down. You may look at and play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect(p2.exile || []).toHaveLength(2);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
  });

  it("applies impulse_exile_top from 'the defending player's library' source wording in 1v1", () => {
    const ir = parseOracleTextToIR(
      "Exile the top card of the defending player's library. You may play that card this turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it("skips impulse_exile_top when it is targeting-dependent (that player's library)", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of that player's library, then exile it face down. You may play that card for as long as it remains exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p2.exile || []).toHaveLength(0);
  });

  it("applies impulse_exile_top when that-player remains-exiled play + mana-rider has explicit targetPlayerId", () => {
    const ir = parseOracleTextToIR(
      "Look at the top card of that player's library, then exile it face down. You may play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p1.exile || []).toHaveLength(0);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applies impulse_exile_top for combined look+exile face-down template when target_opponent has a single legal candidate', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library and exile those cards face down. You may play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect(p2.exile || []).toHaveLength(2);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
  });

  it('applies impulse_exile_top for combined look+exile face-down template with mana-rider cast-them wording', () => {
    const ir = parseOracleTextToIR(
      "Look at the top two cards of target opponent's library and exile those cards face down. You may play those cards for as long as they remain exiled, and mana of any type can be spent to cast them.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c3']);
    expect(p2.exile || []).toHaveLength(2);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
  });

  it('skips impulse_exile_top when target_opponent is ambiguous in multiplayer', () => {
    const ir = parseOracleTextToIR(
      "Exile the top two cards of target opponent's library face down. You may look at and play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }, { id: 'p3c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect(p2.exile || []).toHaveLength(0);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c1', 'p3c2', 'p3c3']);
    expect(p3.exile || []).toHaveLength(0);
  });

  it('applies impulse_exile_top for contextual each_of_those_opponents selector in 1v1 (AtomicCards: Breeches)', () => {
    const ir = parseOracleTextToIR(
      "Whenever one or more Pirates you control deal damage to your opponents, exile the top card of each of those opponents' libraries. You may play those cards this turn, and you may spend mana as though it were mana of any color to cast those spells.",
      'Breeches, Brazen Plunderer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect(p2.exile.map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('skips impulse_exile_top for contextual each_of_those_opponents selector in multiplayer ambiguity', () => {
    const ir = parseOracleTextToIR(
      "Whenever one or more Pirates you control deal damage to your opponents, exile the top card of each of those opponents' libraries. You may play those cards this turn, and you may spend mana as though it were mana of any color to cast those spells.",
      'Breeches, Brazen Plunderer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p2.exile || []).toHaveLength(0);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c1', 'p3c2']);
    expect(p3.exile || []).toHaveLength(0);
  });

  it('applies impulse_exile_top for target_opponent in multiplayer when selectorContext binds the target', () => {
    const ir = parseOracleTextToIR(
      "Exile the top two cards of target opponent's library face down. You may look at and play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }, { id: 'p2c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }, { id: 'p3c3' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p3' },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2', 'p2c3']);
    expect(p2.exile || []).toHaveLength(0);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c3']);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1', 'p3c2']);
  });

  it('applies contextual each_of_those_opponents selector in multiplayer when selectorContext binds antecedent set', () => {
    const ir = parseOracleTextToIR(
      "Whenever one or more Pirates you control deal damage to your opponents, exile the top card of each of those opponents' libraries. You may play those cards this turn, and you may spend mana as though it were mana of any color to cast those spells.",
      'Breeches, Brazen Plunderer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [{ id: 'p3c1' }, { id: 'p3c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p3'] },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p2.exile || []).toHaveLength(0);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c2']);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1']);
  });

  it('applies impulse_exile_top for Gonti-Night-Minister-style look-then-exile play+rider sentence', () => {
    const ir = parseOracleTextToIR(
      "Whenever one or more creatures you control deal combat damage to a player, look at the top card of that player's library, then exile it face down. You may play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.",
      'Test'
    );
    const allSteps = ir.abilities.flatMap(a => a.steps);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, allSteps as any, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applies impulse_exile_top for Thought-String-Analyst-style singular remains-exiled look+play mana-rider text', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, exile the top card of target opponent's library face down. You lose life equal to its mana value. You may look at and play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(false);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applies lose_life to contextual each_of_those_opponents in multiplayer when selectorContext binds antecedent set', () => {
    const ir = parseOracleTextToIR('Each of those opponents loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { eachOfThoseOpponents: ['p2'] },
    });
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'lose_life')).toBe(false);
    expect(p2.life).toBe(39);
    expect(p3.life).toBe(40);
  });

  it('buildOracleIRExecutionContext maps event hint affectedOpponentIds into each_of_those_opponents execution', () => {
    const ir = parseOracleTextToIR(
      "Whenever one or more Pirates you control deal damage to your opponents, exile the top card of each of those opponents' libraries. You may play those cards this turn, and you may spend mana as though it were mana of any color to cast those spells.",
      'Breeches, Brazen Plunderer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }, { id: 'p2c2' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }, { id: 'p3c2' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'p1' },
      { affectedOpponentIds: ['p2'], opponentsDealtDamageIds: ['p3'] }
    );

    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    // affectedOpponentIds has precedence for relational selector binding.
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
    expect(p3.library.map((c: any) => c.id)).toEqual(['p3c1', 'p3c2']);
    expect(p3.exile || []).toHaveLength(0);
  });

  it('uses the merfolk iteration fixture to create eight Merfolk Wizard tokens from Summon the School text', () => {
    const ir = parseOracleTextToIR('Create two 1/1 blue Merfolk Wizard creature tokens.', 'Summon the School');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeMerfolkIterationState();
    const startingBattlefieldCount = (start.battlefield || []).length;
    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Summon the School',
    });

    const createdTokens = ((result.state as any).battlefield || []).filter((perm: any) => perm?.isToken);

    expect(result.appliedSteps.some(s => s.kind === 'create_token')).toBe(true);
    expect((result.state.battlefield || []).length - startingBattlefieldCount).toBe(8);
    expect(createdTokens).toHaveLength(8);
    expect(createdTokens.every((perm: any) => perm.controller === 'p1')).toBe(true);
  });

  it('uses the merfolk iteration fixture for devotion-to-blue where-X evaluation', () => {
    const ir = parseOracleTextToIR(
      'Target creature gets +X/+0 until end of turn where X is your devotion to blue.',
      'Nykthos Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeMerfolkIterationState();
    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'drowner-of-secrets',
    });
    const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'drowner-of-secrets') as any;
    const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
    expect(ptMod.power).toBe(4);
    expect(ptMod.toughness).toBe(0);
  });

  it('uses the merfolk iteration fixture to untap Nykthos via Merrow Reejerey text', () => {
    const ir = parseOracleTextToIR('You may tap or untap target permanent.', 'Merrow Reejerey');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeMerfolkIterationState({
      battlefield: makeMerfolkIterationState().battlefield.map((perm: any) =>
        perm.id === 'nykthos-shrine-to-nyx' ? { ...perm, tapped: true } : perm
      ),
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Merrow Reejerey',
        targetPermanentId: 'nykthos-shrine-to-nyx',
        tapOrUntapChoice: 'untap',
      },
      { allowOptional: true }
    );

    const nykthos = ((result.state as any).battlefield || []).find((p: any) => p.id === 'nykthos-shrine-to-nyx') as any;

    expect(result.appliedSteps.some(s => s.kind === 'tap_or_untap')).toBe(true);
    expect(nykthos.tapped).toBe(false);
  });

  it('buildOracleIRExecutionContext maps spellType hint into referenceSpellTypes', () => {
    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'p1' as any },
      { spellType: 'Instant Sorcery' }
    ) as any;

    expect(ctx.referenceSpellTypes).toEqual(['instant', 'sorcery']);
  });

  it("applies impulse_exile_top when subject is 'its owner' via target_player selector binding", () => {
    const ir = parseOracleTextToIR(
      "Whenever a creature is dealt damage, its owner may exile a card from the top of their library. They may play that card until the end of their next turn.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }, { id: 'p1c2' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }, { id: 'p2c2' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
      turnNumber: 3 as any,
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p1.library.map((c: any) => c.id)).toEqual(['p1c1', 'p1c2']);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('buildOracleIRExecutionContext falls back to targetOpponentId for each_of_those_opponents execution', () => {
    const ir = parseOracleTextToIR(
      "Each of those opponents loses 1 life.",
      'Test Relational'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { targetOpponentId: 'p3' });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'lose_life')).toBe(false);
    expect(p2.life).toBe(40);
    expect(p3.life).toBe(39);
  });

  it('buildOracleIRExecutionContext maps targetOpponentId for multiplayer target_opponent resolution', () => {
    const ir = parseOracleTextToIR(
      "Exile the top two cards of target opponent's library face down. You may look at and play those cards for as long as they remain exiled.",
      'Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }, { id: 'p2c2' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }, { id: 'p3c2' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { targetOpponentId: 'p3' });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'impulse_exile_top')).toBe(true);
    expect(p2.library.map((c: any) => c.id)).toEqual(['p2c1', 'p2c2']);
    expect(p2.exile || []).toHaveLength(0);
    expect(p3.library.map((c: any) => c.id)).toEqual([]);
    expect((p3.exile || []).map((c: any) => c.id)).toEqual(['p3c1', 'p3c2']);
  });

  it('buildOracleIRExecutionContext maps targetOpponentId into target_player fallback', () => {
    const ir = parseOracleTextToIR('Target player loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { targetOpponentId: 'p3' });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(p2.life).toBe(40);
    expect(p3.life).toBe(39);
  });

  it('buildOracleIRExecutionContext falls back to targetPlayerId for each_of_those_opponents when relational sets are absent', () => {
    const ir = parseOracleTextToIR('Each of those opponents loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { targetPlayerId: 'p2' });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(p2.life).toBe(39);
    expect(p3.life).toBe(40);
  });

  it('buildOracleIRExecutionContext sanitizes each_of_those_opponents by removing controller id', () => {
    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'p1' },
      { affectedPlayerIds: ['p1', 'p2', 'p2', 'p3'] }
    );

    expect(ctx.selectorContext?.eachOfThoseOpponents).toEqual(['p2', 'p3']);
  });

  it('buildOracleIRExecutionContext infers targetOpponentId from singleton affectedOpponentIds', () => {
    const ir = parseOracleTextToIR('Target opponent loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { affectedOpponentIds: ['p3'] });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(p2.life).toBe(40);
    expect(p3.life).toBe(39);
  });

  it('buildOracleIRExecutionContext infers targetPlayerId from singleton affectedPlayerIds', () => {
    const ir = parseOracleTextToIR('Target player loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { affectedPlayerIds: ['p2'] });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(p2.life).toBe(39);
    expect(p3.life).toBe(40);
  });

  it('buildOracleIRExecutionContext falls back targetPlayerId from base targetOpponentId', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
        selectorContext: { targetOpponentId: 'p3' },
      },
      { affectedPlayerIds: ['p2', 'p3'] }
    );

    expect(ctx.selectorContext?.targetPlayerId).toBe('p3');
    expect(ctx.selectorContext?.targetOpponentId).toBe('p3');
  });

  it('buildOracleIRExecutionContext falls back targetOpponentId from base targetPlayerId when base target is an opponent', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
        selectorContext: { targetPlayerId: 'p2' },
      },
      { affectedPlayerIds: ['p2', 'p3'] }
    );

    expect(ctx.selectorContext?.targetPlayerId).toBe('p2');
    expect(ctx.selectorContext?.targetOpponentId).toBe('p2');
  });

  it('buildOracleIRExecutionContext precedence: explicit hint wins over inferred singleton and base fallback', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
        selectorContext: { targetPlayerId: 'p2', targetOpponentId: 'p2' },
      },
      {
        targetOpponentId: 'p3',
        affectedOpponentIds: ['p2'],
      }
    );

    expect(ctx.selectorContext?.targetOpponentId).toBe('p3');
    expect(ctx.selectorContext?.targetPlayerId).toBe('p3');
  });

  it('buildOracleIRExecutionContext precedence: inferred singleton wins over base fallback when explicit is absent', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
        selectorContext: { targetPlayerId: 'p2', targetOpponentId: 'p2' },
      },
      {
        affectedOpponentIds: ['p3'],
      }
    );

    expect(ctx.selectorContext?.targetOpponentId).toBe('p3');
    expect(ctx.selectorContext?.targetPlayerId).toBe('p3');
  });

  it('buildOracleIRExecutionContext precedence: base fallback is used when explicit and inferred are absent', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
        selectorContext: { targetPlayerId: 'p2', targetOpponentId: 'p2' },
      },
      {
        affectedOpponentIds: ['p2', 'p3'],
      }
    );

    expect(ctx.selectorContext?.targetOpponentId).toBe('p2');
    expect(ctx.selectorContext?.targetPlayerId).toBe('p2');
  });

  it('buildOracleIRExecutionContext ignores targetOpponentId when it equals controller id', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
      },
      {
        targetOpponentId: 'p1',
      }
    );

    expect(ctx.selectorContext).toBeUndefined();
  });

  it('buildOracleIRExecutionContext ignores controller id from affectedOpponentIds for opponent inference', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
      },
      {
        affectedOpponentIds: ['p1'],
      }
    );

    expect(ctx.selectorContext).toBeUndefined();
  });

  it('buildOracleIRExecutionContext infers targetOpponentId from sanitized singleton affectedOpponentIds', () => {
    const ctx = buildOracleIRExecutionContext(
      {
        controllerId: 'p1',
      },
      {
        affectedOpponentIds: ['p1', 'p3'],
      }
    );

    expect(ctx.selectorContext?.targetOpponentId).toBe('p3');
    expect(ctx.selectorContext?.targetPlayerId).toBe('p3');
  });

  it('buildOracleIRExecutionContext normalizes whitespace-padded targetOpponentId', () => {
    const ir = parseOracleTextToIR('Target opponent loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, { targetOpponentId: '  p3  ' as any });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(true);
    expect(p2.life).toBe(40);
    expect(p3.life).toBe(39);
  });

  it('buildOracleIRExecutionContext normalizes whitespace-padded relational ids', () => {
    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'p1' },
      { affectedOpponentIds: [' p2 ', 'p2', ' p3  '] as any }
    );

    expect(ctx.selectorContext?.eachOfThoseOpponents).toEqual(['p2', 'p3']);
  });

  it('target_opponent resolves from direct selectorContext with whitespace-padded id', () => {
    const ir = parseOracleTextToIR('Target opponent loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: '  p3  ' as any },
    });

    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(39);
  });

  it('each_of_those_opponents resolves from direct selectorContext with normalized ids', () => {
    const ir = parseOracleTextToIR('Each of those opponents loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: {
        eachOfThoseOpponents: [' p2 ', 'p2', '  p3  ', 'ghost' as any],
      },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(39);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(39);
  });

  it('buildOracleIRExecutionContext ignores object-valued target ids', () => {
    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'p1' },
      { targetOpponentId: { bad: true } as any, targetPlayerId: { bad: true } as any }
    );

    expect(ctx.selectorContext).toBeUndefined();
  });

  it('target_opponent does not resolve when controller is not a valid player in state', () => {
    const ir = parseOracleTextToIR('Target opponent loses 2 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext(
      { controllerId: 'ghost-player' as PlayerID },
      { targetOpponentId: 'p2' as PlayerID }
    );
    const result = applyOracleIRStepsToGameState(start, steps, ctx);

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(false);
    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(40);
  });

  it('each_opponent does not resolve when controller is not a valid player in state', () => {
    const ir = parseOracleTextToIR('Each opponent loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p3', name: 'P3', seat: 2, life: 40, library: [{ id: 'p3c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const ctx = buildOracleIRExecutionContext({ controllerId: 'ghost-player' as PlayerID });
    const result = applyOracleIRStepsToGameState(start, steps, ctx);

    expect(result.appliedSteps.some(s => s.kind === 'lose_life')).toBe(false);
    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p3') as any).life).toBe(40);
  });

  it('buildOracleIRExecutionContext relational matrix enforces precedence + sanitization', () => {
    const cases: Array<{
      name: string;
      hint: any;
      expectedEach: string[];
    }> = [
      {
        name: 'affectedOpponentIds wins over other relational sets',
        hint: {
          affectedOpponentIds: ['p3', 'p2', 'p3'],
          opponentsDealtDamageIds: ['p2'],
          affectedPlayerIds: ['p2'],
        },
        expectedEach: ['p3', 'p2'],
      },
      {
        name: 'opponentsDealtDamageIds wins when affectedOpponentIds absent',
        hint: {
          opponentsDealtDamageIds: ['p2', 'p2', 'p3'],
          affectedPlayerIds: ['p3'],
        },
        expectedEach: ['p2', 'p3'],
      },
      {
        name: 'affectedPlayerIds fallback removes controller and dedupes',
        hint: {
          affectedPlayerIds: ['p1', 'p2', 'p2', 'p3'],
        },
        expectedEach: ['p2', 'p3'],
      },
      {
        name: 'targetPlayer fallback removes controller',
        hint: {
          targetPlayerId: 'p1',
        },
        expectedEach: [],
      },
      {
        name: 'targetOpponent fallback yields singleton relational set',
        hint: {
          targetOpponentId: 'p3',
        },
        expectedEach: ['p3'],
      },
    ];

    for (const testCase of cases) {
      const ctx = buildOracleIRExecutionContext({ controllerId: 'p1' }, testCase.hint);
      const actual = ctx.selectorContext?.eachOfThoseOpponents || [];
      expect(actual, testCase.name).toEqual(testCase.expectedEach);
      expect(actual.includes('p1'), `${testCase.name} should exclude controller`).toBe(false);
      expect(new Set(actual).size, `${testCase.name} should dedupe ids`).toBe(actual.length);
    }
  });

  it('buildOracleIRExecutionContext sanitizes base relational context even without hint', () => {
    const ctx = buildOracleIRExecutionContext({
      controllerId: 'p1',
      selectorContext: {
        eachOfThoseOpponents: ['p1', 'p2', 'p2', 'p3'],
      },
    });

    expect(ctx.selectorContext?.eachOfThoseOpponents).toEqual(['p2', 'p3']);
  });

  it('buildOracleIRExecutionContext normalizes whitespace-padded base controllerId', () => {
    const ctx = buildOracleIRExecutionContext({
      controllerId: '  p1  ' as any,
      selectorContext: {
        eachOfThoseOpponents: ['p1', 'p2'],
      },
    });

    expect(ctx.controllerId).toBe('p1');
    expect(ctx.selectorContext?.eachOfThoseOpponents).toEqual(['p2']);
  });

  it('buildOracleIRExecutionContext normalizes base controllerId without selector context or hint', () => {
    const ctx = buildOracleIRExecutionContext({
      controllerId: '  p1  ' as any,
    });

    expect(ctx.controllerId).toBe('p1');
    expect(ctx.selectorContext).toBeUndefined();
  });

  it('buildOracleIRExecutionContext fills missing base targetOpponentId from base targetPlayerId without hint', () => {
    const ctx = buildOracleIRExecutionContext({
      controllerId: 'p1',
      selectorContext: {
        targetPlayerId: 'p2',
      },
    });

    expect(ctx.selectorContext?.targetPlayerId).toBe('p2');
    expect(ctx.selectorContext?.targetOpponentId).toBe('p2');
  });

  it('applyOracleIRStepsToGameState resolves "you" selector with whitespace-padded controllerId', () => {
    const ir = parseOracleTextToIR('You gain 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: '  p1  ' as any,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.some(s => s.kind === 'gain_life')).toBe(true);
    expect(p1.life).toBe(41);
    expect(p2.life).toBe(40);
  });

  it('applyOracleIRStepsToGameState resolves battlefield "you control" filter with whitespace-padded controllerId', () => {
    const ir = parseOracleTextToIR('Destroy all creatures you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'c1',
          controller: 'p1',
          owner: 'p1',
          tapped: false,
          faceDown: false,
          counters: {},
          card: { id: 'c1', name: 'Self Creature', type_line: 'Creature' },
        } as any,
        {
          id: 'c2',
          controller: 'p2',
          owner: 'p2',
          tapped: false,
          faceDown: false,
          counters: {},
          card: { id: 'c2', name: 'Opp Creature', type_line: 'Creature' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: '  p1  ' as any,
    });

    const battlefield = (result.state as any).battlefield as any[];
    expect(battlefield.some(p => p.id === 'c1')).toBe(false);
    expect(battlefield.some(p => p.id === 'c2')).toBe(true);
  });

  it('applyOracleIRStepsToGameState resolves battlefield opponents filter with whitespace-padded controllerId', () => {
    const ir = parseOracleTextToIR('Destroy all creatures your opponents control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      battlefield: [
        {
          id: 'c1',
          controller: 'p1',
          owner: 'p1',
          tapped: false,
          faceDown: false,
          counters: {},
          card: { id: 'c1', name: 'Self Creature', type_line: 'Creature' },
        } as any,
        {
          id: 'c2',
          controller: 'p2',
          owner: 'p2',
          tapped: false,
          faceDown: false,
          counters: {},
          card: { id: 'c2', name: 'Opp Creature', type_line: 'Creature' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: '  p1  ' as any,
    });

    const battlefield = (result.state as any).battlefield as any[];
    expect(battlefield.some(p => p.id === 'c1')).toBe(true);
    expect(battlefield.some(p => p.id === 'c2')).toBe(false);
  });

  it('applyOracleIRStepsToGameState resolves move_zone from your graveyard with whitespace-padded controllerId', () => {
    const ir = parseOracleTextToIR('Return all creature cards from your graveyard to your hand.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'h0', name: 'Existing', type_line: 'Instant' }],
          graveyard: [
            { id: 'g1', name: 'Grizzly Bears', type_line: 'Creature � Bear' },
            { id: 'g2', name: 'Shock', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: '  p1  ' as any,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['g2']);
    expect(p1.hand.map((c: any) => c.id)).toEqual(['h0', 'g1']);
  });

  it('applyOracleIRStepsToGameState resolves each-opponent move_zone with whitespace-padded controllerId', () => {
    const ir = parseOracleTextToIR("Return all creature cards from each opponent's graveyard to their hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'P1 Bear', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'P2 Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: '  p1  ' as any,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p1.hand).toEqual([]);
    expect(p2.graveyard).toEqual([]);
    expect(p2.hand.map((c: any) => c.id)).toEqual(['p2g1']);
  });

  it('applyOracleIRStepsToGameState resolves "that player loses life" via target_player context binding', () => {
    const ir = parseOracleTextToIR('That player loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(39);
  });

  it('applyOracleIRStepsToGameState resolves "that opponent loses life" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('That opponent loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(39);
  });

  it('applyOracleIRStepsToGameState resolves "defending player loses life" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('Defending player loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(39);
  });

  it('applyOracleIRStepsToGameState resolves "the defending player loses life" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('The defending player loses 1 life.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect((result.state.players.find(p => p.id === 'p1') as any).life).toBe(40);
    expect((result.state.players.find(p => p.id === 'p2') as any).life).toBe(39);
  });

  it('applyOracleIRStepsToGameState resolves "that player draws" via target_player context binding', () => {
    const ir = parseOracleTextToIR('That player draws a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect(((result.state.players.find(p => p.id === 'p1') as any).hand || []).length).toBe(0);
    expect(((result.state.players.find(p => p.id === 'p2') as any).hand || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "that creature\'s owner draws" via target_player context binding', () => {
    const ir = parseOracleTextToIR("That creature's owner draws a card.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    expect(((result.state.players.find(p => p.id === 'p1') as any).hand || []).length).toBe(0);
    expect(((result.state.players.find(p => p.id === 'p2') as any).hand || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "defending player draws" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('Defending player draws a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect(((result.state.players.find(p => p.id === 'p1') as any).hand || []).length).toBe(0);
    expect(((result.state.players.find(p => p.id === 'p2') as any).hand || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "the defending player draws" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('The defending player draws a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    expect(((result.state.players.find(p => p.id === 'p1') as any).hand || []).length).toBe(0);
    expect(((result.state.players.find(p => p.id === 'p2') as any).hand || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "that player discards" via target_player context binding', () => {
    const ir = parseOracleTextToIR('That player discards a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [{ id: 'h1', name: 'Card 1', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetPlayerId: 'p2' as any },
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p2.hand || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['h1']);
  });

  it('applyOracleIRStepsToGameState resolves "defending player discards" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('Defending player discards a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [{ id: 'h1', name: 'Card 1', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p2.hand || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['h1']);
  });

  it('applyOracleIRStepsToGameState resolves "the defending player discards" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('The defending player discards a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }],
          hand: [{ id: 'h1', name: 'Card 1', type_line: 'Instant' }],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p2.hand || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['h1']);
  });

  it('applyOracleIRStepsToGameState resolves "that opponent mills" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('That opponent mills a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p1.graveyard || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "defending player mills" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('Defending player mills a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p1.graveyard || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState resolves "the defending player mills" via target_opponent context binding', () => {
    const ir = parseOracleTextToIR('The defending player mills a card.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'p1c1' }, { id: 'p1c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [{ id: 'p2c1' }, { id: 'p2c2' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      selectorContext: { targetOpponentId: 'p2' as any },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    expect((p1.graveyard || []).length).toBe(0);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual(['p2c1']);
  });

  it('applyOracleIRStepsToGameState does not execute each-opponent move_zone when controller is invalid', () => {
    const ir = parseOracleTextToIR("Return all creature cards from each opponent's graveyard to their hands.", 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p1g1', name: 'P1 Bear', type_line: 'Creature � Bear' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'p2g1', name: 'P2 Giant', type_line: 'Creature � Giant' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'ghost-player' as any,
    });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(p1.graveyard.map((c: any) => c.id)).toEqual(['p1g1']);
    expect(p2.graveyard.map((c: any) => c.id)).toEqual(['p2g1']);
    expect(p1.hand).toEqual([]);
    expect(p2.hand).toEqual([]);
  });


  // -- New where-X phrase regression tests -------------------------------------

  describe('timing-qualifier strip in where-X phrases', () => {
    it('strips "as that ability resolves" timing qualifier', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is that creature's power as that ability resolves.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tc1', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 3, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tc1')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(3);
      expect(ptMod?.toughness).toBe(0);
    });

    it('applies X-based modify_pt where X is the number of untapped lands they control', () => {
      const ir = parseOracleTextToIR(
        'Target creature gets +X/+0 until end of turn where X is the number of untapped lands they control.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'targetUntappedLands1', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2land1', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land — Island', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2land2', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land — Mountain', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2land3', ownerId: 'p2', controller: 'p2', name: 'Swamp', type_line: 'Basic Land — Swamp', tapped: true, summoningSick: false, counters: {} } as any,
          { id: 'p1land1', ownerId: 'p1', controller: 'p1', name: 'Forest', type_line: 'Basic Land — Forest', tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, {
        controllerId: 'p1',
        selectorContext: { targetOpponentId: 'p2' },
      });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetUntappedLands1') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(2);
      expect(ptMod.toughness).toBe(0);
    });

    it('applies X-based modify_pt where X is the number of untapped lands that player controls', () => {
      const ir = parseOracleTextToIR(
        'Target creature gets +X/+0 until end of turn where X is the number of untapped lands that player controls.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'targetUntappedLands2', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2land4', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land — Island', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2land5', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land — Mountain', tapped: true, summoningSick: false, counters: {} } as any,
          { id: 'p2land6', ownerId: 'p2', controller: 'p2', name: 'Plains', type_line: 'Basic Land — Plains', tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, {
        controllerId: 'p1',
        selectorContext: { targetPlayerId: 'p2' },
      });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetUntappedLands2') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(2);
      expect(ptMod.toughness).toBe(0);
    });

    it('applies X-based modify_pt where X is the number of untapped lands they controlled at the beginning of this turn', () => {
      const ir = parseOracleTextToIR(
        'Target creature gets +X/+0 until end of turn where X is the number of untapped lands they controlled at the beginning of this turn.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [{ id: 'p1c1' }], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [{ id: 'p2c1' }], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'targetUntappedLandsTurnStart', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn1', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land — Island', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn2', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land — Mountain', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn3', ownerId: 'p2', controller: 'p2', name: 'Swamp', type_line: 'Basic Land — Swamp', tapped: false, summoningSick: false, counters: {} } as any,
        ],
        turnStartBattlefieldSnapshot: [
          { id: 'targetUntappedLandsTurnStart', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn1', ownerId: 'p2', controller: 'p2', name: 'Island', type_line: 'Basic Land — Island', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn2', ownerId: 'p2', controller: 'p2', name: 'Mountain', type_line: 'Basic Land — Mountain', tapped: true, summoningSick: false, counters: {} } as any,
          { id: 'p2landTurn3', ownerId: 'p2', controller: 'p2', name: 'Swamp', type_line: 'Basic Land — Swamp', tapped: true, summoningSick: false, counters: {} } as any,
        ] as any,
        priority: 'p1',
        turnPlayer: 'p1',
      } as any);

      const result = applyOracleIRStepsToGameState(start, steps, {
        controllerId: 'p1',
        selectorContext: { targetOpponentId: 'p2' },
      });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'targetUntappedLandsTurnStart') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(1);
      expect(ptMod.toughness).toBe(0);
    });

    it('strips "when bulk up resolves" timing qualifier', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+X until end of turn where X is that creature's power when bulk up resolves.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tc2', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 4, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tc2')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(4);
    });

    it('strips "as you cast this spell" qualifier', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is that creature's power as you cast this spell.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tc3', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 5, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tc3')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(5);
    });
  });

  describe('alias normalizations in where-X phrases', () => {
    it('resolves "that artifact\'s mana value" as source card MV', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is that artifact's mana value.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        stack: [{ id: 'artSrc', type: 'spell', manaValue: 6, card: { manaValue: 6 } } as any],
        battlefield: [{ id: 'tc4', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'artSrc' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tc4')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(6);
    });

    it('resolves "his power when his last ability resolves" -> its power', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is his power when his last ability resolves.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tHis', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 7, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tHis')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(7);
    });

    it('strips trailing ", then draw three cards" clause', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest mana value among permanents you control, then draw three cards.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tcStrip', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {}, card: { manaValue: 2 } } as any,
          { id: 'artStrip', ownerId: 'p1', controller: 'p1', name: 'Art', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {}, card: { manaValue: 5 } } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcStrip')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(5);
    });
  });

  describe('word-number normalization in where-X phrases', () => {
    it('resolves word number "five" normalization in where-X phrases', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is five plus the number of creatures you control.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tcFive', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcFive')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(6); // five(5) + 1 creature = 6
    });
  });

  describe('greatest power among [subtype] you control', () => {
    it('resolves "greatest power among giants you control"', () => {
      const ir = parseOracleTextToIR(
        "Target creature an opponent controls gets +X/+0 until end of turn where X is the greatest power among giants you control.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'tcGiant', ownerId: 'p2', controller: 'p2', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'g1', ownerId: 'p1', controller: 'p1', name: 'Giant1', cardType: 'Creature', type_line: 'Creature', subtypes: ['Giant'], power: 6, toughness: 6, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'g2', ownerId: 'p1', controller: 'p1', name: 'Giant2', cardType: 'Creature', type_line: 'Creature', subtypes: ['Giant'], power: 9, toughness: 7, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcGiant')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(9);
    });
  });

  describe('greatest power among creature cards in graveyard', () => {
    it('resolves "greatest power among creature cards in your graveyard"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest power among creature cards in your graveyard.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{
          id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], exile: [],
          graveyard: [
            { id: 'gg1', name: 'Bear1', type_line: 'Creature', power: 3 },
            { id: 'gg2', name: 'Bear2', type_line: 'Creature', power: 5 },
            { id: 'gg3', name: 'Fire', type_line: 'Instant' },
          ],
        } as any],
        battlefield: [{ id: 'tcGy', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcGy')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(5);
    });

    it('resolves "greatest power among creature cards in all graveyards"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest power among creature cards in all graveyards.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], exile: [], graveyard: [{ id: 'p1g1', name: 'Bear', type_line: 'Creature', power: 4 }] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], exile: [], graveyard: [{ id: 'p2g1', name: 'Dragon', type_line: 'Creature', power: 8 }] } as any,
        ],
        battlefield: [{ id: 'tcAllGy', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcAllGy')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(8);
    });
  });

  describe('greatest mana value among cards in graveyard', () => {
    it('resolves "greatest mana value among cards in your graveyard"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest mana value among cards in your graveyard.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{
          id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], exile: [],
          graveyard: [
            { id: 'gc1', name: 'Bear', type_line: 'Creature', card: { manaValue: 2 } },
            { id: 'gc2', name: 'Dragon', type_line: 'Creature', card: { manaValue: 7 } },
          ],
        } as any],
        battlefield: [{ id: 'tcMvGy', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcMvGy')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(7);
    });
  });

  describe('greatest mana value among your commanders', () => {
    it('resolves "greatest mana value among your commanders" (commander on battlefield)', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest mana value among your commanders.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tcCmd', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'cmd1', ownerId: 'p1', controller: 'p1', name: 'Commander1', isCommander: true, cardType: 'Legendary Planeswalker', type_line: 'Legendary Planeswalker', tapped: false, summoningSick: false, counters: {}, card: { manaValue: 6 } } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcCmd')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(6);
    });
  });

  describe('greatest number of artifacts an opponent controls', () => {
    it('resolves X as the largest artifact count among opponents', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the greatest number of artifacts an opponent controls.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'tcArt', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2a1', ownerId: 'p2', controller: 'p2', name: 'Rod', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p2a2', ownerId: 'p2', controller: 'p2', name: 'Sword', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'p3a1', ownerId: 'p3', controller: 'p3', name: 'Sol Ring', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcArt')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(2); // p2 has 2, p3 has 1
    });
  });

  describe('number of counters on named permanent', () => {
    it('resolves "number of +1/+1 counters on Aerith Gainsborough"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the number of +1/+1 counters on Aerith Gainsborough.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tcCtr', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'aerith', ownerId: 'p1', controller: 'p1', name: 'Aerith Gainsborough', cardType: 'Legendary Planeswalker', type_line: 'Legendary Planeswalker', tapped: false, summoningSick: false, counters: { '+1/+1': 4 } } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcCtr')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(4);
    });
  });

  describe('difference between power and toughness', () => {
    it('resolves "difference between its power and toughness"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the difference between its power and toughness.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tcDiff', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 7, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcDiff')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(4); // |7 - 3| = 4
    });
  });

  describe('half this artifact\'s intensity', () => {
    it('resolves "half this artifact\'s intensity, rounded down"', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is half this artifact's intensity, rounded down.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [
          { id: 'tcHalfInt', ownerId: 'p1', controller: 'p1', name: 'Target', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'srcArtInt', ownerId: 'p1', controller: 'p1', name: 'Intense Artifact', cardType: 'Artifact', type_line: 'Artifact', tapped: false, summoningSick: false, counters: {}, intensity: 7 } as any,
        ],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'srcArtInt' });
      const ptMod = ((result.state as any).battlefield || [])
        .find((p: any) => p.id === 'tcHalfInt')?.modifiers?.find((m: any) => m?.type === 'powerToughness');
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod?.power).toBe(3); // floor(7/2) = 3
    });
  });

  describe('safe-skips for non-deterministic where-X phrases', () => {
    it('returns null for "a number chosen at random from 0 to 4" (no modifier applied)', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is a number chosen at random from 0 to 4.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tcRand', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcRand') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      expect(ptMod).toBeUndefined();
    });

    it('returns null for "the noted number" (no modifier applied)', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the noted number.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];
      const start = makeState({
        players: [{ id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any],
        battlefield: [{ id: 'tcNoted', ownerId: 'p1', controller: 'p1', name: 'Bear', cardType: 'Creature', type_line: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any],
        priority: 'p1', turnPlayer: 'p1',
      });
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcNoted') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      // noted number is non-deterministic, no modifier should be applied
      expect(ptMod).toBeUndefined();
    });
  });

  describe('all-players spells cast this turn', () => {
    it('sums all players\' spell counts for "number of spells cast this turn"', () => {
      const ir = parseOracleTextToIR(
        'The creature gets +X/+0 until end of turn where X is the number of spells cast this turn.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        spellsCastThisTurn: { p1: 2, p2: 3, p3: 1 },
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'eq1', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact — Equipment', type_line: 'Artifact — Equipment', attachedTo: 'tc1', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tc1', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      } as any);

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eq1' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tc1') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(6); // 2 + 3 + 1
      expect(ptMod.toughness).toBe(0);
    });

    it('handles "one plus the number of spells cast this turn" via arithmetic recursion', () => {
      const ir = parseOracleTextToIR(
        'The creature gets +X/+0 until end of turn where X is one plus the number of spells cast this turn.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        spellsCastThisTurn: { p1: 1, p2: 2 },
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'eqOnePlusSpells', ownerId: 'p1', controller: 'p1', name: 'Equipment', cardType: 'Artifact — Equipment', type_line: 'Artifact — Equipment', attachedTo: 'tcOnePlusSpells', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tcOnePlusSpells', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      } as any);

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'eqOnePlusSpells' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcOnePlusSpells') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(4); // 1 + (1 + 2)
      expect(ptMod.toughness).toBe(0);
    });
  });

  describe('named-source exile count', () => {
    it('counts cards exiled by a named permanent on the battlefield', () => {
      const ir = parseOracleTextToIR(
        'Target creature gets +X/+X until end of turn where X is the number of cards exiled with verdant sungrove.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          {
            id: 'p1', name: 'P1', seat: 0, life: 40, library: [],
            hand: [],
            graveyard: [],
            exile: [
              { id: 'ex1', exiledBy: 'namedSrc' },
              { id: 'ex2', exiledBy: 'namedSrc' },
              { id: 'ex3', exiledBy: 'namedSrc' },
              { id: 'ex4', exiledBy: 'otherSrc' },
            ],
          } as any,
        ],
        battlefield: [
          { id: 'namedSrc', ownerId: 'p1', controller: 'p1', name: 'Verdant Sungrove', cardType: 'Enchantment', type_line: 'Enchantment', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tcNamedExile', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'namedSrc' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcNamedExile') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(3);
      expect(ptMod.toughness).toBe(3);
    });

    it('returns null (no modifier) when named permanent is not on battlefield', () => {
      const ir = parseOracleTextToIR(
        'Target creature gets +X/+X until end of turn where X is the number of cards exiled with verdant sungrove.',
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [{ id: 'ex1', exiledBy: 'namedSrc2' }] } as any,
        ],
        battlefield: [
          { id: 'tcNoSource', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcNoSource') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      expect(ptMod).toBeUndefined();
    });
  });

  describe('difference between those players\' life totals', () => {
    it('returns absolute difference of two identified players\' life totals', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the difference between those players' life totals.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 30, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 22, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'tcLifeDiff', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, {
        controllerId: 'p1',
        selectorContext: { eachOfThoseOpponents: ['p2', 'p3'] as any },
      });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcLifeDiff') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(8); // |30 - 22|
      expect(ptMod.toughness).toBe(0);
    });

    it('returns null (no modifier) when fewer than two opponents are identified', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the difference between those players' life totals.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 30, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'tcLifeDiffNone', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      // No selectorContext provided — eachOfThoseOpponents will be empty
      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcLifeDiffNone') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      expect(ptMod).toBeUndefined();
    });
  });

  describe('generic mana in that spell\'s mana cost', () => {
    it('counts the numeric component of a spell\'s mana cost', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the amount of generic mana in that spell's mana cost.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'srcSpellGeneric', ownerId: 'p1', controller: 'p1', name: 'Kindle', cardType: 'Instant', type_line: 'Instant', mana_cost: '{3}{B}{R}', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tcGenericMana', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'srcSpellGeneric' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcGenericMana') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(3);
      expect(ptMod.toughness).toBe(0);
    });

    it('handles multi-generic components summed correctly', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the amount of generic mana in that spell's mana cost.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'srcSpellMultiGen', ownerId: 'p1', controller: 'p1', name: 'Test Spell', cardType: 'Sorcery', type_line: 'Sorcery', mana_cost: '{2}{3}{R}', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tcGenericMana2', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'srcSpellMultiGen' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcGenericMana2') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');

      // {2}{3}{R} has generic mana 2+3=5
      expect(result.appliedSteps.some(s => s.kind === 'modify_pt')).toBe(true);
      expect(ptMod.power).toBe(5);
      expect(ptMod.toughness).toBe(0);
    });

    it('returns 0 and skips modifier for purely colored mana cost', () => {
      const ir = parseOracleTextToIR(
        "Target creature gets +X/+0 until end of turn where X is the amount of generic mana in that spell's mana cost.",
        'Test'
      );
      const steps = ir.abilities[0]?.steps ?? [];

      const start = makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
        ],
        battlefield: [
          { id: 'srcColoredOnly', ownerId: 'p1', controller: 'p1', name: 'Bolt', cardType: 'Instant', type_line: 'Instant', mana_cost: '{R}', tapped: false, summoningSick: false, counters: {} } as any,
          { id: 'tcGenericMana3', ownerId: 'p1', controller: 'p1', name: 'Test Bear', cardType: 'Creature', power: 1, toughness: 1, tapped: false, summoningSick: false, counters: {} } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      });

      const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', sourceId: 'srcColoredOnly' });
      const creature = ((result.state as any).battlefield || []).find((p: any) => p.id === 'tcGenericMana3') as any;
      const ptMod = (Array.isArray(creature?.modifiers) ? creature.modifiers : []).find((m: any) => m?.type === 'powerToughness');
      // {R} has no generic mana, X=0; a 0/0 modifier may still be applied with power=0
      expect(ptMod?.power ?? 0).toBe(0);
    });
  });

  it('applies Veinwitch Coven when allowOptional enables the mana payment gate', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your end step, if you gained life this turn, you may pay {B}. If you do, return target creature card from your graveyard to your hand.',
      'Veinwitch Coven'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'deadBear', name: 'Dead Bear', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      manaPool: {
        p1: { white: 0, blue: 0, black: 1, red: 0, green: 0, colorless: 0 },
      } as any,
      lifeGainedThisTurn: { p1: 1 },
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', targetCreatureId: 'deadBear' },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const pool = (result.state as any).manaPool?.p1;

    expect(result.appliedSteps.some(s => s.kind === 'pay_mana')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect((p1.hand || []).map((c: any) => c.id)).toContain('deadBear');
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('deadBear');
    expect(pool?.black).toBe(0);
  });

  it('keeps Genesis return gated off when the optional mana payment cannot be paid', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your upkeep, if Genesis is in your graveyard, you may pay {2}{G}. If you do, return target creature card from your graveyard to your hand.',
      'Genesis'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'genesis', name: 'Genesis', type_line: 'Creature - Incarnation' },
            { id: 'targetElf', name: 'Target Elf', type_line: 'Creature - Elf' },
          ],
          exile: [],
        } as any,
      ],
      manaPool: {
        p1: { white: 0, blue: 0, black: 0, red: 0, green: 1, colorless: 1 },
      } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'genesis', sourceName: 'Genesis', targetCreatureId: 'targetElf' },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const pool = (result.state as any).manaPool?.p1;

    expect(result.appliedSteps.some(s => s.kind === 'pay_mana')).toBe(false);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(false);
    expect(result.skippedSteps.some(s => s.kind === 'pay_mana')).toBe(true);
    expect((p1.hand || []).map((c: any) => c.id)).not.toContain('targetElf');
    expect((p1.graveyard || []).map((c: any) => c.id)).toContain('targetElf');
    expect(pool?.green).toBe(1);
    expect(pool?.colorless).toBe(1);
  });

  it('applies Nim Deathmantle when allowOptional enables the payment gate and conditional attach follow-up', () => {
    const ir = parseOracleTextToIR(
      'Whenever a nontoken creature is put into your graveyard from the battlefield, you may pay {4}. If you do, return that card to the battlefield and attach Nim Deathmantle to it.',
      'Nim Deathmantle'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'deadBear', name: 'Dead Bear', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'nim',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Nim Deathmantle',
          cardType: 'Artifact',
          type_line: 'Artifact - Equipment',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      manaPool: {
        p1: { white: 0, blue: 0, black: 0, red: 0, green: 0, colorless: 4 },
      } as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', sourceId: 'nim', targetCreatureId: 'deadBear' },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const equipment = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'nim');
    const revived = (result.state.battlefield as any[]).find((perm: any) => String(perm?.card?.id || '') === 'deadBear');
    const pool = (result.state as any).manaPool?.p1;

    expect(result.appliedSteps.some(s => s.kind === 'pay_mana')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect(result.appliedSteps.some(s => s.kind === 'attach')).toBe(true);
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('deadBear');
    expect(revived).toBeTruthy();
    expect(equipment?.attachedTo).toBe(String(revived?.id || ''));
    expect(pool?.colorless).toBe(0);
  });

  it('applies Echoing Return by returning the targeted card and all same-name cards from your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Return target creature card and all other cards with the same name as that card from your graveyard to your hand.',
      'Echoing Return'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'elf-1', name: 'Llanowar Elves', type_line: 'Creature - Elf Druid' },
            { id: 'shock-1', name: 'Shock', type_line: 'Instant' },
            { id: 'elf-2', name: 'Llanowar Elves', type_line: 'Creature - Elf Druid' },
          ],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, { controllerId: 'p1', targetCreatureId: 'elf-1' });
    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['elf-1', 'elf-2']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual(['shock-1']);
  });

  it('applies Skullwinder when choose_opponent and the opponent follow-up return are deterministic', () => {
    const ir = parseOracleTextToIR(
      'When Skullwinder enters, return target card from your graveyard to your hand, then choose an opponent. That player returns a card from their graveyard to their hand.',
      'Skullwinder'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'regrowth', name: 'Regrowth', type_line: 'Sorcery' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'bolt', name: 'Lightning Bolt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', targetPermanentId: 'regrowth' }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'choose_opponent', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['regrowth']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect((p2.hand || []).map((c: any) => c.id)).toEqual(['bolt']);
    expect((p2.graveyard || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies Court of Ardenvale monarchy branch when allowOptional is enabled', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your upkeep, return target permanent card from your graveyard to your hand. If you're the monarch, you may put it onto the battlefield instead.",
      'Court of Ardenvale'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'sword', name: 'Sword of Testing', type_line: 'Artifact - Equipment' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
      monarch: 'p1' as any,
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', targetPermanentId: 'sword' },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const battlefieldCard = (result.state.battlefield as any[]).find((perm: any) => String(perm?.card?.id || '') === 'sword');

    expect(result.appliedSteps.some(s => s.kind === 'move_zone')).toBe(true);
    expect((p1.hand || []).map((c: any) => c.id)).not.toContain('sword');
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('sword');
    expect(battlefieldCard).toBeTruthy();
  });

  it('applies Volcanic Vision by returning the card, dealing damage from its mana value, and exiling the source spell', () => {
    const ir = parseOracleTextToIR(
      "Return target instant or sorcery card from your graveyard to your hand. Volcanic Vision deals damage equal to that card's mana value to each creature your opponents control. Exile Volcanic Vision.",
      'Volcanic Vision'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'mortars', name: 'Mizzium Mortars', type_line: 'Sorcery', mana_cost: '{1}{R}{R}', manaValue: 3 },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      stack: [
        {
          id: 'volcanic-vision-spell',
          type: 'spell',
          controller: 'p1',
          card: { id: 'volcanic-vision-card', name: 'Volcanic Vision', type_line: 'Sorcery' },
        } as any,
      ],
      battlefield: [
        {
          id: 'opp-bear',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Opponent Bear',
          cardType: 'Creature',
          type_line: 'Creature - Bear',
          power: 4,
          toughness: 4,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
        {
          id: 'opp-wolf',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Opponent Wolf',
          cardType: 'Creature',
          type_line: 'Creature - Wolf',
          power: 5,
          toughness: 5,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'volcanic-vision-spell',
      targetPermanentId: 'mortars',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const opponentBear = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'opp-bear');
    const opponentWolf = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'opp-wolf');

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'deal_damage', 'exile']);
    expect((p1.hand || []).map((c: any) => c.id)).toContain('mortars');
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('mortars');
    expect(opponentBear?.damageMarked).toBe(3);
    expect(opponentWolf?.damageMarked).toBe(3);
    expect((p1.exile || []).map((c: any) => c.id)).toContain('volcanic-vision-card');
    expect((result.state.stack || []).map((item: any) => item.id)).not.toContain('volcanic-vision-spell');
  });

  it('applies Golbez, Crystal Collector by returning the creature and using its power for each opponent life loss', () => {
    const ir = parseOracleTextToIR(
      "At the beginning of your end step, if you control four or more artifacts, return target creature card from your graveyard to your hand. Each opponent loses life equal to that card's power.",
      'Golbez, Crystal Collector'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'golem', name: 'Myr Golem', type_line: 'Artifact Creature - Golem', power: 5, toughness: 4 },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        { id: 'art-1', controller: 'p1', owner: 'p1', card: { id: 'art-1-card', name: 'Sol Ring', type_line: 'Artifact' } } as any,
        { id: 'art-2', controller: 'p1', owner: 'p1', card: { id: 'art-2-card', name: 'Arcane Signet', type_line: 'Artifact' } } as any,
        { id: 'art-3', controller: 'p1', owner: 'p1', card: { id: 'art-3-card', name: 'Mind Stone', type_line: 'Artifact' } } as any,
        { id: 'art-4', controller: 'p1', owner: 'p1', card: { id: 'art-4-card', name: 'Wayfarer\'s Bauble', type_line: 'Artifact' } } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'golem',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'lose_life']);
    expect((p1.hand || []).map((c: any) => c.id)).toContain('golem');
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('golem');
    expect(p2.life).toBe(35);
    expect(p3.life).toBe(35);
  });

  it('applies Peerless Recycling gift branch by returning a second chosen permanent card', () => {
    const ir = parseOracleTextToIR(
      'Return target permanent card from your graveyard to your hand. If the gift was promised, return another target permanent card from your graveyard to your hand.',
      'Peerless Recycling'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'relic', name: 'Relic of Testing', type_line: 'Artifact' },
            { id: 'wake', name: 'Mirari\'s Wake', type_line: 'Enchantment' },
          ],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetPermanentId: 'relic',
      giftPromised: true,
      selectorContext: {
        chosenObjectIds: ['relic', 'wake'],
      },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['relic', 'wake']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
  });

  it('applies Reconstruct History by returning each chosen graveyard type slot and self-exiling', () => {
    const ir = parseOracleTextToIR(
      'Return up to one target artifact card, up to one target enchantment card, up to one target instant card, up to one target sorcery card, and up to one target planeswalker card from your graveyard to your hand. Exile Reconstruct History.',
      'Reconstruct History'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'bauble', name: 'Mishra\'s Bauble', type_line: 'Artifact' },
            { id: 'wake', name: 'Mirari\'s Wake', type_line: 'Enchantment' },
            { id: 'wrath', name: 'Wrath of God', type_line: 'Sorcery' },
          ],
          exile: [],
        } as any,
      ],
      stack: [
        {
          id: 'reconstruct-history-spell',
          type: 'spell',
          controller: 'p1',
          card: { id: 'reconstruct-history-card', name: 'Reconstruct History', type_line: 'Sorcery' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'reconstruct-history-spell',
      selectorContext: {
        chosenObjectIds: ['bauble', 'wake', 'wrath'],
      },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual([
      'move_zone',
      'move_zone',
      'move_zone',
      'move_zone',
      'move_zone',
      'exile',
    ]);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['bauble', 'wake', 'wrath']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.exile || []).map((c: any) => c.id)).toContain('reconstruct-history-card');
  });

  it('applies Awaken the Honored Dead when allowOptional enables the discard gate', () => {
    const ir = parseOracleTextToIR(
      'You may discard a card. When you do, return target creature card from your graveyard to the battlefield tapped.',
      'Awaken the Honored Dead'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [{ id: 'pitch', name: 'Spare Card', type_line: 'Instant' }],
          graveyard: [{ id: 'zombie', name: 'Diregraf Ghoul', type_line: 'Creature - Zombie' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      { controllerId: 'p1', targetCreatureId: 'zombie' },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const revived = (result.state.battlefield as any[]).find((perm: any) => String(perm?.card?.id || '') === 'zombie');

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['discard', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).toContain('pitch');
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('zombie');
    expect(revived?.tapped).toBe(true);
  });

  it('applies Consumed by Greed gift branch after the opponent sacrifice resolves deterministically', () => {
    const ir = parseOracleTextToIR(
      'Target opponent sacrifices a creature or enchantment. If the gift was promised, you return target permanent card from your graveyard to your hand.',
      'Consumed by Greed'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'wake', name: 'Mirari\'s Wake', type_line: 'Enchantment' }],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'opp-creature',
          ownerId: 'p2',
          controller: 'p2',
          name: 'Opponent Bear',
          cardType: 'Creature',
          type_line: 'Creature - Bear',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetPermanentId: 'wake',
      selectorContext: { targetOpponentId: 'p2' },
      giftPromised: true,
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['sacrifice', 'move_zone']);
    expect((result.state.battlefield || []).map((perm: any) => perm.id)).not.toContain('opp-creature');
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['wake']);
  });

  it('does not apply Grave Venerations end-step return when you are not the monarch', () => {
    const ir = parseOracleTextToIR(
      "When this enchantment enters, you become the monarch.\nAt the beginning of your end step, if you're the monarch, return up to one target creature card from your graveyard to your hand.\nWhenever a creature you control dies, each opponent loses 1 life and you gain 1 life.",
      'Grave Venerations'
    );
    const steps =
      ir.abilities.find(a => a.triggerCondition === 'the beginning of your end step')?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'angel', name: 'Angel of Tests', type_line: 'Creature - Angel' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'angel',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps).toEqual([]);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual(['angel']);
  });

  it('applies Grave Venerations end-step return when you are the monarch', () => {
    const ir = parseOracleTextToIR(
      "When this enchantment enters, you become the monarch.\nAt the beginning of your end step, if you're the monarch, return up to one target creature card from your graveyard to your hand.\nWhenever a creature you control dies, each opponent loses 1 life and you gain 1 life.",
      'Grave Venerations'
    );
    const steps =
      ir.abilities.find(a => a.triggerCondition === 'the beginning of your end step')?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'angel', name: 'Angel of Tests', type_line: 'Creature - Angel' }],
          exile: [],
        } as any,
      ],
      monarch: 'p1',
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'angel',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['angel']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
  });

  it('does not apply Aerith, Last Ancient raise ability when you did not gain life this turn', () => {
    const ir = parseOracleTextToIR(
      'Lifelink\nAt the beginning of your end step, if you gained life this turn, return target creature card from your graveyard to your hand. If you gained 7 or more life this turn, return that card to the battlefield instead.',
      'Aerith, Last Ancient'
    );
    const steps =
      ir.abilities.find(a => a.triggerCondition === 'the beginning of your end step')?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'cleric', name: 'Cleric of Tests', type_line: 'Creature - Cleric' }],
          exile: [],
        } as any,
      ],
      lifeGainedThisTurn: { p1: 0 },
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'cleric',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps).toEqual([]);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual(['cleric']);
    expect((result.state.battlefield || []).map((perm: any) => perm.card?.id || perm.id)).not.toContain('cleric');
  });

  it('applies Aerith, Last Ancient battlefield upgrade when you gained seven life this turn', () => {
    const ir = parseOracleTextToIR(
      'Lifelink\nAt the beginning of your end step, if you gained life this turn, return target creature card from your graveyard to your hand. If you gained 7 or more life this turn, return that card to the battlefield instead.',
      'Aerith, Last Ancient'
    );
    const steps =
      ir.abilities.find(a => a.triggerCondition === 'the beginning of your end step')?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'cleric', name: 'Cleric of Tests', type_line: 'Creature - Cleric' }],
          exile: [],
        } as any,
      ],
      lifeGainedThisTurn: { p1: 7 },
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'cleric',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const returned = (result.state.battlefield || []).find((perm: any) => String(perm?.card?.id || perm?.id || '') === 'cleric');

    expect(result.appliedSteps.map(s => s.kind)).toEqual(['move_zone', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect(returned).toBeTruthy();
  });

  it('applies Planewide Celebration with repeated mode selections', () => {
    const ir = parseOracleTextToIR(
      "Choose four. You may choose the same mode more than once.\n\u2022 Create a 2/2 white Citizen creature token that's all colors.\n\u2022 Return target permanent card from your graveyard to your hand.\n\u2022 Proliferate.\n\u2022 You gain 4 life.",
      'Planewide Celebration'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'relic', name: 'Relic of Tests', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        targetPermanentId: 'relic',
      },
      {
        selectedModeIds: ['Mode 2', 'Mode 4', 'Mode 4', 'Mode 1'],
      }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(p1.life).toBe(48);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['relic']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect(result.state.battlefield || []).toHaveLength(1);
    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(result.appliedSteps.filter(step => step.kind === 'gain_life')).toHaveLength(2);
  });

  it('applies Stitch Together threshold battlefield upgrade when seven or more cards are in your graveyard', () => {
    const ir = parseOracleTextToIR(
      'Return target creature card from your graveyard to your hand.\nThreshold - Return that card from your graveyard to the battlefield instead if there are seven or more cards in your graveyard.',
      'Stitch Together'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps);

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'ghoul', name: 'Diregraf Ghoul', type_line: 'Creature - Zombie' },
            { id: 'c1', name: 'Filler 1', type_line: 'Instant' },
            { id: 'c2', name: 'Filler 2', type_line: 'Instant' },
            { id: 'c3', name: 'Filler 3', type_line: 'Instant' },
            { id: 'c4', name: 'Filler 4', type_line: 'Instant' },
            { id: 'c5', name: 'Filler 5', type_line: 'Instant' },
            { id: 'c6', name: 'Filler 6', type_line: 'Instant' },
          ],
          exile: [],
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      targetCreatureId: 'ghoul',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const returned = (result.state.battlefield || []).find((perm: any) => String(perm?.card?.id || perm?.id || '') === 'ghoul');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).not.toContain('ghoul');
    expect(returned).toBeTruthy();
  });

  it('applies Emeria Shepherd plains branch when the triggering land is bound as a Plains', () => {
    const result = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'relic', name: 'Testing Relic', type_line: 'Artifact' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'plains1',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Plains',
            type_line: 'Basic Land - Plains',
            tapped: false,
            summoningSick: false,
            counters: {},
          } as any,
        ],
        priority: 'p1',
        turnPlayer: 'p1',
      }),
      {
        controllerId: 'p1',
        sourceId: 'emeria',
        sourceName: 'Emeria Shepherd',
        effect:
          'You may return target nonland permanent card from your graveyard to your hand. If that land is a Plains, you may return that nonland permanent card to the battlefield instead.',
      },
      {
        targetPermanentId: 'relic',
        chosenObjectIds: ['plains1'],
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const returned = (result.state.battlefield || []).find((perm: any) => String(perm?.card?.id || perm?.id || '') === 'relic');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect(returned).toBeTruthy();
  });

  it('applies Undead Butler by exiling the source from your graveyard and returning the chosen creature', () => {
    const result = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'butler', name: 'Undead Butler', type_line: 'Creature - Zombie' },
              { id: 'ghoul', name: 'Diregraf Ghoul', type_line: 'Creature - Zombie' },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
        priority: 'p1',
        turnPlayer: 'p1',
      }),
      {
        controllerId: 'p1',
        sourceId: 'butler',
        sourceName: 'Undead Butler',
        effect: 'You may exile it. When you do, return target creature card from your graveyard to your hand.',
      },
      {
        targetPermanentId: 'ghoul',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['exile', 'move_zone']);
    expect((p1.hand || []).map((c: any) => c.id)).toEqual(['ghoul']);
    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect((p1.exile || []).map((c: any) => c.id)).toEqual(['butler']);
  });

  it('applies Not Dead After All by returning the creature and attaching a Wicked Role token when it dies', () => {
    const ir = parseOracleTextToIR(
      'Until end of turn, target creature you control gains "When this creature dies, return it to the battlefield tapped under its owner\'s control, then create a Wicked Role token attached to it." (Enchanted creature gets +1/+1. When this token is put into a graveyard, each opponent loses 1 life.)',
      'Not Dead After All'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'bear',
          controller: 'p1',
          owner: 'p1',
          tapped: false,
          attachments: [],
          card: { id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
        } as any,
      ],
      priority: 'p1',
      turnPlayer: 'p1',
    });

    const granted = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'not-dead',
      sourceName: 'Not Dead After All',
      targetCreatureId: 'bear',
    });

    const registry = (granted.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.timing).toBe(DelayedTriggerTiming.WHEN_DIES);

    const diedState = makeState({
      turnNumber: 9,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
      delayedTriggerRegistry: registry,
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'bear' });
    expect(delayedCheck.triggersToFire).toHaveLength(1);

    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      diedState,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const p1 = resolved.state.players.find(p => p.id === 'p1') as any;
    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear'
    );
    const role = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.name || '') === 'Wicked Role');

    expect((p1.graveyard || []).map((c: any) => c.id)).toEqual([]);
    expect(returned?.tapped).toBe(true);
    expect(role?.attachedTo).toBe(returned?.id);
    expect((returned?.attachments || [])).toContain(role?.id);
  });

  it('applies Undying Malice by returning the creature tapped with a +1/+1 counter when it dies', () => {
    const ir = parseOracleTextToIR(
      'Until end of turn, target creature gains "When this creature dies, return it to the battlefield tapped under its owner\'s control with a +1/+1 counter on it."',
      'Undying Malice'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const granted = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 9,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'bear',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: { id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
          } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'undying-malice',
        sourceName: 'Undying Malice',
        targetCreatureId: 'bear',
      }
    );

    const registry = (granted.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'bear' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
        delayedTriggerRegistry: registry,
      } as any),
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear'
    ) as any;
    expect(returned?.tapped).toBe(true);
    expect(returned?.counters?.['+1/+1']).toBe(1);
  });

  it('applies Luminous Broodmoth trigger text by returning the creature with a flying counter', () => {
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'broodmoth',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: {
              id: 'broodmoth',
              name: 'Luminous Broodmoth',
              type_line: 'Creature - Insect',
              oracle_text:
                "Flying\nWhenever a creature you control without flying dies, return it to the battlefield under its owner's control with a flying counter on it.",
              power: '3',
              toughness: '4',
            },
          } as any,
        ],
      }),
      {
        controllerId: 'p1',
        sourceId: 'broodmoth',
        sourceName: 'Luminous Broodmoth',
        effect: "Return it to the battlefield under its owner's control with a flying counter on it.",
      },
      {
        sourceId: 'bear',
        sourceControllerId: 'p1',
        targetPermanentId: 'bear',
        chosenObjectIds: ['bear'],
      }
    );

    const player1 = resolved.state.players.find(player => player.id === 'p1') as any;
    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear'
    ) as any;

    expect((player1.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p1');
    expect(returned?.counters?.flying).toBe(1);
    expect(resolved.skippedSteps).toHaveLength(0);
  });

  it("applies Valkyrie's Call by returning the creature with its counter and added Angel type", () => {
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'bear',
                name: 'Test Bear',
                type_line: 'Creature - Bear',
                power: '2',
                toughness: '2',
                colors: ['G'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'valkyries-call',
        sourceName: "Valkyrie's Call",
        effect: "Return that card to the battlefield under its owner's control with a +1/+1 counter on it. It's an Angel in addition to its other types.",
      },
      {
        sourceId: 'bear',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'bear',
        chosenObjectIds: ['bear'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear'
    ) as any;

    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p1');
    expect(returned?.counters?.['+1/+1']).toBe(1);
    expect(returned?.grantedTypes).toContain('Angel');
  });

  it('applies Grave Betrayal delayed return by adding the black Zombie rewrite on entry', () => {
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dead-bear',
                name: 'Dead Bear',
                type_line: 'Creature - Bear',
                power: '3',
                toughness: '3',
                colors: ['G'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'grave-betrayal',
        sourceName: 'Grave Betrayal',
        effect:
          "Return it to the battlefield under your control with an additional +1/+1 counter on it. That creature is a black Zombie in addition to its other colors and types.",
      },
      {
        sourceId: 'dead-bear',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-bear',
        chosenObjectIds: ['dead-bear'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'dead-bear'
    ) as any;

    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
    expect(returned?.counters?.['+1/+1']).toBe(1);
    expect(returned?.grantedTypes).toContain('Zombie');
    expect(returned?.colors).toEqual(expect.arrayContaining(['G', 'B']));
  });

  it('applies Bronzehide Lion by returning it as an Aura attached to a creature you control', () => {
    const text =
      `When Bronzehide Lion dies, return it to the battlefield. It's an Aura enchantment with enchant creature you control and "{G}{W}: Return this card to its owner's hand." and it loses all other abilities.`;
    const abilities = parseTriggeredAbilitiesFromText(text, 'bronzehide-card', 'p1', 'Bronzehide Lion');
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'bronzehide-card',
                name: 'Bronzehide Lion',
                type_line: 'Creature - Cat',
                oracle_text:
                  '{1}{G}{W}: Bronzehide Lion gains indestructible until end of turn.\nWhen Bronzehide Lion dies, return it to the battlefield. It\'s an Aura enchantment with enchant creature you control and "{G}{W}: Return this card to its owner\'s hand." and it loses all other abilities.',
                power: '3',
                toughness: '3',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'friendly-bear',
            controller: 'p1',
            owner: 'p1',
            attachments: [],
            attachedTo: undefined,
            card: {
              id: 'friendly-bear-card',
              name: 'Grizzly Bears',
              type_line: 'Creature - Bear',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
      }),
      abilities[0],
      {
        sourceId: 'bronzehide-card',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'bronzehide-card',
        chosenObjectIds: ['bronzehide-card'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'bronzehide-card'
    ) as any;
    const host = (resolved.state.battlefield || []).find((perm: any) => perm?.id === 'friendly-bear') as any;
    const p1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;

    expect(returned?.card?.type_line).toBe('Enchantment - Aura');
    expect(returned?.card?.oracle_text).toBe(
      `Enchant creature you control\n{G}{W}: Return this card to its owner's hand.`
    );
    expect(returned?.attachedTo).toBe('friendly-bear');
    expect(host?.attachments).toContain(returned?.id);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect(String(returned?.card?.oracle_text || '')).not.toContain('gains indestructible');
    expect(resolved.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
  });

  it("applies Lim-Dul the Necromancer by returning the card as a Zombie when the payment condition succeeds", () => {
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        manaPool: { p1: { white: 0, blue: 0, black: 1, red: 0, green: 0, colorless: 1 } },
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dead-ogre',
                name: 'Dead Ogre',
                type_line: 'Creature - Ogre',
                power: '4',
                toughness: '2',
                colors: ['R'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'lim-dul',
        sourceName: 'Lim-Dul the Necromancer',
        effect:
          "You may pay {1}{B}. If you do, return that card to the battlefield under your control. If it's a creature, it's a Zombie in addition to its other creature types.",
      },
      {
        sourceId: 'dead-ogre',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-ogre',
        chosenObjectIds: ['dead-ogre'],
      } as any,
      { allowOptional: true }
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'dead-ogre'
    ) as any;

    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
    expect(returned?.grantedTypes).toContain('Zombie');
  });

  it.each([
    {
      name: 'Endless Cockroaches',
      permanentId: 'cockroaches',
      text: 'When Endless Cockroaches is put into your graveyard from the battlefield, return Endless Cockroaches to your hand.',
      typeLine: 'Creature - Insect',
    },
    {
      name: 'Mortus Strider',
      permanentId: 'mortus-strider',
      text: "When Mortus Strider dies, return it to its owner's hand.",
      typeLine: 'Creature - Skeleton',
    },
    {
      name: 'Weatherseed Treefolk',
      permanentId: 'weatherseed-treefolk',
      text: "When Weatherseed Treefolk dies, return it to its owner's hand.",
      typeLine: 'Creature - Treefolk',
    },
    {
      name: 'Shivan Phoenix',
      permanentId: 'shivan-phoenix',
      text: "When Shivan Phoenix is put into a graveyard from the battlefield, return Shivan Phoenix to its owner's hand.",
      typeLine: 'Creature - Phoenix',
    },
    {
      name: 'Immortal Phoenix',
      permanentId: 'immortal-phoenix',
      text: "When Immortal Phoenix dies, return it to its owner's hand.",
      typeLine: 'Creature - Phoenix',
    },
  ])('triggers and executes $name by returning itself to hand after dying', ({ name, permanentId, text, typeLine }) => {
    const abilities = parseTriggeredAbilitiesFromText(text, permanentId, 'p1', name);

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: permanentId, name, type_line: typeLine }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: permanentId,
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: permanentId,
        chosenObjectIds: [permanentId],
        permanentTypes: ['Creature'],
      }
    );

    const p1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;

    expect(resolved.triggers).toHaveLength(1);
    expect((p1.hand || []).map((card: any) => card.id)).toContain(permanentId);
    expect((p1.graveyard || []).map((card: any) => card.id)).not.toContain(permanentId);
  });

  it("triggers and executes Fool's Demise self-return by moving the Aura from graveyard to hand", () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When Fool's Demise is put into a graveyard from the battlefield, return Fool's Demise to its owner's hand.",
      'fools-demise',
      'p1',
      "Fool's Demise"
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'fools-demise', name: "Fool's Demise", type_line: 'Enchantment - Aura' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'fools-demise',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'fools-demise',
        chosenObjectIds: ['fools-demise'],
        permanentTypes: ['Enchantment'],
      }
    );

    const p1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;

    expect(resolved.triggers).toHaveLength(1);
    expect((p1.hand || []).map((card: any) => card.id)).toContain('fools-demise');
    expect((p1.graveyard || []).map((card: any) => card.id)).not.toContain('fools-demise');
  });

  it('resolves Gift of Immortality by returning the creature immediately and reattaching the Aura at the next end step', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When enchanted creature dies, return that card to the battlefield under its owner's control. Return Gift of Immortality to the battlefield attached to that creature at the beginning of the next end step.",
      'gift-aura',
      'p1',
      'Gift of Immortality'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        turnNumber: 12,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'dead-bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
              { id: 'gift-card', name: 'Gift of Immortality', type_line: 'Enchantment - Aura', oracle_text: 'Enchant creature' },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-bear',
        sourceControllerId: 'p1',
        targetPermanentId: 'dead-bear',
        permanentTypes: ['Creature'],
        attachedByPermanentIds: ['gift-aura'],
      }
    );

    const creatureReturned = (initial.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'dead-bear'
    ) as any;
    const initialPlayer = initial.state.players.find((player: any) => player.id === 'p1') as any;
    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const resolvedPlayer = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    const giftAura = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'gift-card'
    ) as any;

    expect(creatureReturned).toBeTruthy();
    expect((initialPlayer.graveyard || []).map((card: any) => card.id)).toContain('gift-card');
    expect((initialPlayer.graveyard || []).map((card: any) => card.id)).not.toContain('dead-bear');
    expect(giftAura).toBeTruthy();
    expect(giftAura?.attachedTo).toBe(String(creatureReturned?.id || ''));
    expect((resolvedPlayer.graveyard || []).map((card: any) => card.id)).not.toContain('gift-card');
  });

  it('resolves Nissa\'s Zendikon trigger text by returning the enchanted land to hand', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When enchanted land dies, return that card to its owner's hand.",
      'zendikon-aura',
      'p1',
      "Nissa's Zendikon"
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'forest-card', name: 'Forest', type_line: 'Basic Land - Forest' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'forest-card',
        sourceControllerId: 'p1',
        targetPermanentId: 'forest-card',
        permanentTypes: ['Land'],
        attachedByPermanentIds: ['zendikon-aura'],
      }
    );

    const player1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;

    expect((player1.hand || []).map((card: any) => card.id)).toContain('forest-card');
    expect((player1.graveyard || []).map((card: any) => card.id)).not.toContain('forest-card');
  });

  it.each([
    {
      name: 'Necrogen Communion',
      sourceId: 'necrogen-communion',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-zombie',
      hostCard: { id: 'dead-zombie', name: 'Dead Zombie', type_line: 'Creature - Zombie', power: '2', toughness: '2' },
      attachedByPermanentIds: ['necrogen-communion'],
      eventData: {
        sourceId: 'dead-zombie',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-zombie',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-zombie');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: 'Fungal Fortitude',
      sourceId: 'fungal-fortitude',
      text: "When enchanted creature dies, return it to the battlefield tapped under its owner's control.",
      hostId: 'dead-fungus',
      hostCard: { id: 'dead-fungus', name: 'Dead Fungus', type_line: 'Creature - Fungus', power: '3', toughness: '3' },
      attachedByPermanentIds: ['fungal-fortitude'],
      eventData: {
        sourceId: 'dead-fungus',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-fungus',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-fungus');
        expect(returned?.controller).toBe('p2');
        expect(returned?.tapped).toBe(true);
      },
    },
    {
      name: "Minion's Return",
      sourceId: 'minions-return',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-minion',
      hostCard: { id: 'dead-minion', name: 'Dead Minion', type_line: 'Creature - Imp', power: '2', toughness: '1' },
      attachedByPermanentIds: ['minions-return'],
      eventData: {
        sourceId: 'dead-minion',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-minion',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-minion');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: 'Unholy Indenture',
      sourceId: 'unholy-indenture',
      text: 'When enchanted creature dies, return that card to the battlefield under your control with a +1/+1 counter on it.',
      hostId: 'dead-warrior',
      hostCard: { id: 'dead-warrior', name: 'Dead Warrior', type_line: 'Creature - Warrior', power: '3', toughness: '2' },
      attachedByPermanentIds: ['unholy-indenture'],
      eventData: {
        sourceId: 'dead-warrior',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-warrior',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-warrior');
        expect(returned?.controller).toBe('p1');
        expect(returned?.counters?.['+1/+1']).toBe(1);
      },
    },
    {
      name: 'Abduction',
      sourceId: 'abduction-aura',
      text: "When enchanted creature dies, return that card to the battlefield under its owner's control.",
      hostId: 'abducted-bear',
      hostCard: { id: 'abducted-bear', name: 'Abducted Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
      attachedByPermanentIds: ['abduction-aura'],
      eventData: {
        sourceId: 'abducted-bear',
        sourceControllerId: 'p1',
        targetPermanentId: 'abducted-bear',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'abducted-bear');
        expect(returned?.controller).toBe('p2');
      },
    },
    {
      name: 'Unhallowed Pact',
      sourceId: 'unhallowed-pact',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-horror',
      hostCard: { id: 'dead-horror', name: 'Dead Horror', type_line: 'Creature - Horror', power: '4', toughness: '4' },
      attachedByPermanentIds: ['unhallowed-pact'],
      eventData: {
        sourceId: 'dead-horror',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-horror',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-horror');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: "Shade's Form",
      sourceId: 'shades-form',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-shade',
      hostCard: { id: 'dead-shade', name: 'Dead Shade', type_line: 'Creature - Shade', power: '1', toughness: '1' },
      attachedByPermanentIds: ['shades-form'],
      eventData: {
        sourceId: 'dead-shade',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-shade',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-shade');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: 'False Demise',
      sourceId: 'false-demise',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-merfolk',
      hostCard: { id: 'dead-merfolk', name: 'Dead Merfolk', type_line: 'Creature - Merfolk', power: '2', toughness: '1' },
      attachedByPermanentIds: ['false-demise'],
      eventData: {
        sourceId: 'dead-merfolk',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-merfolk',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-merfolk');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: "Fool's Demise",
      sourceId: 'fools-demise-main',
      text: 'When enchanted creature dies, return that card to the battlefield under your control.',
      hostId: 'dead-wizard',
      hostCard: { id: 'dead-wizard', name: 'Dead Wizard', type_line: 'Creature - Wizard', power: '2', toughness: '2' },
      attachedByPermanentIds: ['fools-demise-main'],
      eventData: {
        sourceId: 'dead-wizard',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-wizard',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'dead-wizard');
        expect(returned?.controller).toBe('p1');
      },
    },
    {
      name: 'Demonic Vigor',
      sourceId: 'demonic-vigor',
      text: "When enchanted creature dies, return that card to its owner's hand.",
      hostId: 'dead-devil',
      hostCard: { id: 'dead-devil', name: 'Dead Devil', type_line: 'Creature - Devil', power: '2', toughness: '2' },
      attachedByPermanentIds: ['demonic-vigor'],
      eventData: {
        sourceId: 'dead-devil',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-devil',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;
        expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-devil');
      },
    },
    {
      name: "Squee's Embrace",
      sourceId: 'squees-embrace',
      text: "When enchanted creature dies, return that card to its owner's hand.",
      hostId: 'dead-goblin',
      hostCard: { id: 'dead-goblin', name: 'Dead Goblin', type_line: 'Creature - Goblin', power: '1', toughness: '1' },
      attachedByPermanentIds: ['squees-embrace'],
      eventData: {
        sourceId: 'dead-goblin',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-goblin',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;
        expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-goblin');
      },
    },
    {
      name: 'Puppet Master',
      sourceId: 'puppet-master',
      text: "When enchanted creature dies, return that card to its owner's hand.",
      hostId: 'dead-puppet',
      hostCard: { id: 'dead-puppet', name: 'Dead Puppet', type_line: 'Creature - Construct', power: '1', toughness: '1' },
      attachedByPermanentIds: ['puppet-master'],
      eventData: {
        sourceId: 'dead-puppet',
        sourceControllerId: 'p2',
        targetPermanentId: 'dead-puppet',
        permanentTypes: ['Creature'],
      },
      assertResolved: (resolved: any) => {
        const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;
        expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-puppet');
      },
    },
  ])('resolves attached-host return families for $name', ({ name, sourceId, text, hostId, hostCard, attachedByPermanentIds, eventData, assertResolved }) => {
    const abilities = parseTriggeredAbilitiesFromText(text, sourceId, 'p1', name);

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [hostCard],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        ...eventData,
        attachedByPermanentIds,
      }
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;

    expect((owner.graveyard || []).map((card: any) => card.id)).not.toContain(hostId);
    assertResolved(resolved);
  });

  it('resolves Resurrection Orb by scheduling and applying the delayed equipped-creature return', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When equipped creature dies, return that card to the battlefield under your control at the beginning of the next end step.",
      'res-orb',
      'p1',
      'Resurrection Orb'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        turnNumber: 14,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-knight', name: 'Dead Knight', type_line: 'Creature - Human Knight', power: '3', toughness: '3' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-knight',
        sourceControllerId: 'p1',
        targetPermanentId: 'dead-knight',
        permanentTypes: ['Creature'],
        attachedByPermanentIds: ['res-orb'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'dead-knight'
    ) as any;

    expect(returned).toBeTruthy();
    expect(returned?.controller).toBe('p1');
  });

  it('resolves Oathkeeper, Takeno\'s Daisho only when the equipped creature card is a Samurai', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever equipped creature dies, return that card to the battlefield under your control if it's a Samurai card.",
      'oathkeeper',
      'p1',
      "Oathkeeper, Takeno's Daisho"
    );

    const samuraiResolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'samurai-card', name: 'Test Samurai', type_line: 'Creature - Human Samurai', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'samurai-card',
        sourceControllerId: 'p2',
        targetPermanentId: 'samurai-card',
        permanentTypes: ['Creature'],
        attachedByPermanentIds: ['oathkeeper'],
      }
    );

    const nonsamuraiResolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'wizard-card', name: 'Test Wizard', type_line: 'Creature - Human Wizard', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'wizard-card',
        sourceControllerId: 'p2',
        targetPermanentId: 'wizard-card',
        permanentTypes: ['Creature'],
        attachedByPermanentIds: ['oathkeeper'],
      }
    );

    const returnedSamurai = (samuraiResolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'samurai-card'
    ) as any;
    const p2AfterSkip = nonsamuraiResolved.state.players.find((player: any) => player.id === 'p2') as any;

    expect(returnedSamurai?.controller).toBe('p1');
    expect((p2AfterSkip.graveyard || []).map((card: any) => card.id)).toContain('wizard-card');
    expect((nonsamuraiResolved.state.battlefield || []).map((perm: any) => String(perm?.card?.id || ''))).not.toContain('wizard-card');
  });

  it('triggers and executes Dread Slaver by returning a creature it damaged this turn', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever a creature dealt damage by this creature this turn dies, return it to the battlefield under your control.',
      'dread-slaver',
      'p1',
      'Dread Slaver'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dead-bear',
                name: 'Dead Bear',
                type_line: 'Creature - Bear',
                power: '2',
                toughness: '2',
                damageSourceIds: ['dread-slaver'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'dread-slaver',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'dread-slaver',
              name: 'Dread Slaver',
              type_line: 'Creature - Zombie Horror',
              power: '3',
              toughness: '5',
            },
          } as any,
        ],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-bear',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-bear',
        permanentTypes: ['Creature'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'dead-bear'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
  });

  it('triggers and executes Soul Collector by returning a creature it damaged this turn', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever a creature dealt damage by this creature this turn dies, return that card to the battlefield under your control.',
      'soul-collector',
      'p1',
      'Soul Collector'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dead-wolf',
                name: 'Dead Wolf',
                type_line: 'Creature - Wolf',
                power: '3',
                toughness: '3',
                damageSourceIds: ['soul-collector'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'soul-collector',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'soul-collector',
              name: 'Soul Collector',
              type_line: 'Creature - Horror',
              power: '3',
              toughness: '4',
            },
          } as any,
        ],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-wolf',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-wolf',
        permanentTypes: ['Creature'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'dead-wolf'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
  });

  it('triggers and executes Scythe of the Wretched by returning a creature damaged by the equipped creature', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever a creature dealt damage by equipped creature this turn dies, return that card to the battlefield under your control.',
      'scythe',
      'p1',
      'Scythe of the Wretched'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'dead-knight',
                name: 'Dead Knight',
                type_line: 'Creature - Human Knight',
                power: '3',
                toughness: '3',
                damageSourceIds: ['equipped-attacker'],
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'equipped-attacker',
            controller: 'p1',
            owner: 'p1',
            attachedEquipment: ['scythe'],
            isEquipped: true,
            card: {
              id: 'equipped-attacker',
              name: 'Equipped Attacker',
              type_line: 'Creature - Zombie',
              power: '4',
              toughness: '4',
            },
          } as any,
          {
            id: 'scythe',
            controller: 'p1',
            owner: 'p1',
            attachedTo: 'equipped-attacker',
            card: {
              id: 'scythe',
              name: 'Scythe of the Wretched',
              type_line: 'Artifact - Equipment',
            },
          } as any,
        ],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-knight',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-knight',
        permanentTypes: ['Creature'],
      } as any
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'dead-knight'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
  });

  it('executes Edea, Possessed Sorceress effect text by returning the creature to its owner and drawing a card', () => {
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [{ id: 'drawn-card', name: 'Drawn Card', type_line: 'Instant' }],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'borrowed-creature', name: 'Borrowed Creature', type_line: 'Creature - Beast', power: '4', toughness: '4' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'edea',
        sourceName: 'Edea, Possessed Sorceress',
        effect: "return it to the battlefield under its owner's control and you draw a card.",
      },
      {
        sourceId: 'borrowed-creature',
        sourceControllerId: 'p1',
        targetPermanentId: 'borrowed-creature',
        chosenObjectIds: ['borrowed-creature'],
      }
    );

    const p1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'borrowed-creature'
    ) as any;

    expect(returned?.controller).toBe('p2');
    expect((p1.hand || []).map((card: any) => card.id)).toContain('drawn-card');
  });

  it('triggers and executes Edea, Possessed Sorceress for a borrowed creature that dies', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever a creature you control but don't own dies, return it to the battlefield under its owner's control and you draw a card.",
      'edea',
      'p1',
      'Edea, Possessed Sorceress'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [{ id: 'drawn-card', name: 'Drawn Card', type_line: 'Instant' }],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'borrowed-creature', name: 'Borrowed Creature', type_line: 'Creature - Beast', power: '4', toughness: '4' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'borrowed-creature',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p2',
        targetPermanentId: 'borrowed-creature',
        chosenObjectIds: ['borrowed-creature'],
        permanentTypes: ['Creature'],
      }
    );

    const p1 = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'borrowed-creature'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p2');
    expect((p1.hand || []).map((card: any) => card.id)).toContain('drawn-card');
  });

  it('triggers and resolves Marchesa, the Black Rose for your creature with a +1/+1 counter', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever a creature you control with a +1/+1 counter on it dies, return that card to the battlefield under your control at the beginning of the next end step.',
      'marchesa',
      'p1',
      'Marchesa, the Black Rose'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'fallen-creature', name: 'Fallen Creature', type_line: 'Creature - Human Rogue', power: '3', toughness: '3' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.CONTROLLED_CREATURE_DIED,
      abilities,
      {
        sourceId: 'fallen-creature',
        sourceControllerId: 'p1',
        targetPermanentId: 'fallen-creature',
        chosenObjectIds: ['fallen-creature'],
        permanentTypes: ['Creature'],
        counters: { '+1/+1': 1 },
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'fallen-creature'
    ) as any;

    expect(initial.triggers).toHaveLength(1);
    expect(delayedCheck.triggersToFire).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
  });

  it('triggers and executes Thunderbolts Conspiracy for a Villain you control', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever a Villain you control dies, return it to the battlefield under its owner's control with a finality counter on it.",
      'thunderbolts',
      'p1',
      'Thunderbolts Conspiracy'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'villain-creature', name: 'Villain Creature', type_line: 'Creature - Villain', power: '4', toughness: '4' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'villain-creature',
        sourceControllerId: 'p1',
        targetPermanentId: 'villain-creature',
        chosenObjectIds: ['villain-creature'],
        permanentTypes: ['Creature'],
        creatureTypes: ['Villain'],
      }
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || '') === 'villain-creature'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.counters?.finality).toBe(1);
  });

  it('triggers and executes Ashcloud Phoenix by returning it face down under your control', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'When this creature dies, return it to the battlefield face down under your control.',
      'ashcloud-phoenix',
      'p1',
      'Ashcloud Phoenix'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'ashcloud-phoenix', name: 'Ashcloud Phoenix', type_line: 'Creature - Phoenix', power: '4', toughness: '1' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'ashcloud-phoenix',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'ashcloud-phoenix',
        chosenObjectIds: ['ashcloud-phoenix'],
        permanentTypes: ['Creature'],
      }
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String((perm as any)?.card?.id || '') === 'ashcloud-phoenix'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.card?.faceDown).toBe(true);
    expect(returned?.basePower).toBe(2);
    expect(returned?.baseToughness).toBe(2);
    expect(returned?.effectiveTypes).toEqual(['Creature']);
  });

  it('triggers and executes Missy by returning the dead creature face down and tapped under your control', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever another nonartifact creature dies, return it to the battlefield under your control face down and tapped.',
      'missy-source',
      'p1',
      'Missy'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-creature', name: 'Runeclaw Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-creature',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-creature',
        chosenObjectIds: ['dead-creature'],
        permanentTypes: ['Creature'],
      }
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String((perm as any)?.card?.id || '') === 'dead-creature'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p1');
    expect(returned?.owner).toBe('p2');
    expect(returned?.tapped).toBe(true);
    expect(returned?.card?.faceDown).toBe(true);
  });

  it('triggers and executes Yarus by returning the dead face-down creature, then turning it face up', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever a face-down creature you control dies, return it to the battlefield face down under its owner's control if it's a permanent card, then turn it face up.",
      'yarus-source',
      'p1',
      'Yarus, Roar of the Old Gods'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'morphed-creature', name: 'Ainok Survivalist', type_line: 'Creature - Dog Shaman', power: '2', toughness: '1' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.CONTROLLED_CREATURE_DIED,
      abilities,
      {
        sourceId: 'morphed-creature',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p2',
        targetPermanentId: 'morphed-creature',
        chosenObjectIds: ['morphed-creature'],
        permanentTypes: ['Creature'],
        sourceIsFaceDown: true,
      }
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String((perm as any)?.card?.id || '') === 'morphed-creature'
    ) as any;

    expect(resolved.triggers).toHaveLength(1);
    expect(returned?.controller).toBe('p2');
    expect(returned?.card?.faceDown).not.toBe(true);
    expect(returned?.card?.name).toBe('Ainok Survivalist');
    expect(String(returned?.card?.type_line || '')).toContain('Creature');
  });

  it('schedules and resolves Phytotitan delayed return for your next upkeep', () => {
    const initial = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        turnNumber: 9,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'phytotitan', name: 'Phytotitan', type_line: 'Creature - Plant Elemental', power: '7', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'phytotitan',
        sourceName: 'Phytotitan',
        effect: "Return it to the battlefield tapped under its owner's control at the beginning of your next upkeep.",
      },
      {
        sourceId: 'phytotitan',
        sourceControllerId: 'p1',
        targetPermanentId: 'phytotitan',
        chosenObjectIds: ['phytotitan'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'upkeep', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const player1 = resolved.state.players.find(player => player.id === 'p1') as any;
    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String(perm?.card?.id || perm?.id || '') === 'phytotitan'
    ) as any;

    expect((player1.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect(returned?.tapped).toBe(true);
    expect(returned?.controller).toBe('p1');
  });

  it('applies Headstone by exiling a graveyard card and drawing at the next upkeep', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. Draw a card at the beginning of the next upkeep.',
      'Headstone'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 12,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [{ id: 'drawn-card', name: 'Swamp', type_line: 'Basic Land - Swamp' }],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'headstone-target', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const initial = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Headstone',
      targetPermanentId: 'headstone-target',
    });

    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'upkeep', activePlayerId: 'p2' as any });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const p1 = resolved.state.players.find(player => player.id === 'p1') as any;
    const p2 = resolved.state.players.find(player => player.id === 'p2') as any;

    expect(initial.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'schedule_delayed_trigger']);
    expect((p2.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect((p2.exile || []).map((card: any) => card.id)).toContain('headstone-target');
    expect(delayedCheck.triggersToFire).toHaveLength(1);
    expect(p1.hand).toHaveLength(1);
    expect(p1.hand[0]?.id).toBe('drawn-card');
  });

  it('schedules and resolves The Locust God delayed hand return at the next end step', () => {
    const initial = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        turnNumber: 11,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'locust-god', name: 'The Locust God', type_line: 'Legendary Creature - God', power: '4', toughness: '4' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      {
        controllerId: 'p1',
        sourceId: 'locust-god',
        sourceName: 'The Locust God',
        effect: "Return it to its owner's hand at the beginning of the next end step.",
      },
      {
        sourceId: 'locust-god',
        sourceControllerId: 'p1',
        targetPermanentId: 'locust-god',
        chosenObjectIds: ['locust-god'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state as any,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const player1 = resolved.state.players.find(player => player.id === 'p1') as any;

    expect((player1.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect((player1.hand || []).map((card: any) => card.id)).toEqual(['locust-god']);
  });

  it('schedules and resolves Molten Firebird while marking your next draw step to be skipped', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Flying\n' +
        "When this creature dies, return it to the battlefield under its owner's control at the beginning of the next end step and you skip your next draw step.\n" +
        '{4}{R}: Exile this creature.',
      'molten-firebird',
      'p1',
      'Molten Firebird'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        turnNumber: 11,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [{ id: 'draw-a' }],
            hand: [],
            graveyard: [{ id: 'molten-firebird', name: 'Molten Firebird', type_line: 'Creature - Phoenix', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'molten-firebird',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'molten-firebird',
        chosenObjectIds: ['molten-firebird'],
        permanentTypes: ['Creature'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const skipEffects = ((initial.state as any).skipNextDrawStepEffects || []) as any[];
    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p2' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find(
      (perm: any) => String((perm as any)?.card?.id || '') === 'molten-firebird'
    ) as any;

    expect(initial.triggers).toHaveLength(1);
    expect(registry?.triggers).toHaveLength(1);
    expect(skipEffects).toEqual([
      expect.objectContaining({
        playerId: 'p1',
        sourceName: 'Molten Firebird',
        remainingSkips: 1,
      }),
    ]);
    expect(returned?.controller).toBe('p1');
    expect(((resolved.state as any).skipNextDrawStepEffects || [])).toEqual(skipEffects);
  });

  it('applies Fake Your Own Death by returning the creature and creating a Treasure token when it dies', () => {
    const ir = parseOracleTextToIR(
      'Until end of turn, target creature gains "When this creature dies, return it to the battlefield under its owner\'s control, then create a Treasure token."',
      'Fake Your Own Death'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const granted = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 9,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'bear',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: { id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
          } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'fake-your-own-death',
        sourceName: 'Fake Your Own Death',
        targetCreatureId: 'bear',
      }
    );

    const registry = (granted.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'bear' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
        delayedTriggerRegistry: registry,
      } as any),
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    expect(
      (resolved.state.battlefield || []).some((perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear')
    ).toBe(true);
    expect((resolved.state.battlefield || []).some((perm: any) => String(perm?.card?.name || '') === 'Treasure')).toBe(true);
  });

  it('applies Presumed Dead by returning the creature and suspecting it when it dies', () => {
    const steps = parseOracleTextToIR(
      'Until end of turn, target creature gets +2/+0 and gains "When this creature dies, return it to the battlefield under its owner\'s control and suspect it."',
      'Presumed Dead'
    ).abilities.flatMap(ability => ability.steps);

    const granted = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 9,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'bear',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: { id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' },
          } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'presumed-dead',
        sourceName: 'Presumed Dead',
        targetCreatureId: 'bear',
      }
    );

    const registry = (granted.state as any).delayedTriggerRegistry;
    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'bear' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'bear', name: 'Test Bear', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
        delayedTriggerRegistry: registry,
      } as any),
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || perm?.id || '') === 'bear') as any;
    expect(resolved.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'suspect']);
    expect(returned).toBeTruthy();
    expect(returned.isSuspected).toBe(true);
  });

  it('applies Perigee Beckoner by granting a delayed tapped return to another target creature', () => {
    const steps = parseOracleTextToIR(
      'When this creature enters, until end of turn, another target creature you control gets +2/+0 and gains "When this creature dies, return it to the battlefield tapped under its owner\'s control."',
      'Perigee Beckoner'
    ).abilities[0]?.steps ?? [];

    const granted = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 9,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'beckoner',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: { id: 'beckoner', name: 'Perigee Beckoner', type_line: 'Creature - Bird', power: '2', toughness: '2' },
          } as any,
          {
            id: 'ally',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            attachments: [],
            counters: {},
            card: { id: 'ally', name: 'Target Ally', type_line: 'Creature - Soldier', power: '2', toughness: '2' },
          } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'beckoner',
        sourceName: 'Perigee Beckoner',
        targetCreatureId: 'ally',
      }
    );

    const registry = (granted.state as any).delayedTriggerRegistry;
    expect((registry?.triggers || []).some((trigger: any) => String(trigger?.watchingPermanentId || '') === 'ally')).toBe(true);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'ally' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'ally', name: 'Target Ally', type_line: 'Creature - Soldier', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
        delayedTriggerRegistry: registry,
      } as any),
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || perm?.id || '') === 'ally') as any;
    expect(returned).toBeTruthy();
    expect(returned.tapped).toBe(true);
  });

  it('applies Infuse with Vitality by granting the delayed dies trigger and gaining 2 life', () => {
    const steps = parseOracleTextToIR(
      'Until end of turn, target creature gains deathtouch and "When this creature dies, return it to the battlefield tapped under its owner\'s control."\nYou gain 2 life.',
      'Infuse with Vitality'
    ).abilities.flatMap(ability => ability.steps);

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          { id: 'target-bear', ownerId: 'p1', controller: 'p1', name: 'Target Bear', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'infuse-with-vitality',
        sourceName: 'Infuse with Vitality',
        targetPermanentId: 'target-bear',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const registry = (result.state as any).delayedTriggerRegistry;

    expect(p1.life).toBe(42);
    expect(registry).toBeTruthy();
    expect((registry?.triggers || []).some((trigger: any) => String(trigger?.watchingPermanentId || '') === 'target-bear')).toBe(true);
  });

  it('applies Verdant Rebirth by granting the delayed dies trigger and drawing a card', () => {
    const steps = parseOracleTextToIR(
      'Until end of turn, target creature gains "When this creature dies, return it to its owner\'s hand." Draw a card.',
      'Verdant Rebirth'
    ).abilities.flatMap(ability => ability.steps);

    const granted = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [{ id: 'drawn-card', name: 'Drawn Card', type_line: 'Sorcery' }],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          { id: 'target-wolf', ownerId: 'p2', controller: 'p2', name: 'Target Wolf', cardType: 'Creature', power: 2, toughness: 2, tapped: false, summoningSick: false, counters: {} } as any,
        ],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'verdant-rebirth',
        sourceName: 'Verdant Rebirth',
        targetPermanentId: 'target-wolf',
      }
    );

    const caster = granted.state.players.find((player: any) => player.id === 'p1') as any;
    const registry = (granted.state as any).delayedTriggerRegistry;

    expect((caster.hand || []).map((card: any) => card.id)).toContain('drawn-card');
    expect((registry?.triggers || []).some((trigger: any) => String(trigger?.watchingPermanentId || '') === 'target-wolf')).toBe(true);

    const diedState = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: caster.hand || [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'target-wolf', name: 'Target Wolf', type_line: 'Creature - Wolf', power: '2', toughness: '2' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
      delayedTriggerRegistry: registry,
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'target-wolf' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      diedState,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;
    expect((owner.hand || []).map((card: any) => card.id)).toContain('target-wolf');
  });

  it('applies Flame-Wreathed Phoenix trigger text by granting a watched dies trigger while it remains on the battlefield', () => {
    const ability = parseOracleTextToIR(
      'Flying, haste\nTribute 2\nWhen Flame-Wreathed Phoenix enters, if tribute wasn\'t paid, it gains haste and "When this creature dies, return it to its owner\'s hand."',
      'Flame-Wreathed Phoenix'
    ).abilities.find(parsedAbility => parsedAbility.triggerCondition === 'this permanent enters');
    const conditionalStep = ability?.steps?.[0] as any;
    const grantStep = conditionalStep?.steps?.[0];

    const granted = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          { id: 'phoenix', ownerId: 'p1', controller: 'p1', name: 'Flame-Wreathed Phoenix', cardType: 'Creature', power: 3, toughness: 3, tapped: false, summoningSick: false, counters: {} } as any,
        ],
      }),
      [grantStep],
      {
        controllerId: 'p1',
        sourceId: 'phoenix',
        sourceName: 'Flame-Wreathed Phoenix',
      }
    );

    const registry = (granted.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);
    expect(registry?.triggers?.[0]?.effectData?.expireAtEndOfTurn).toBe(false);
    expect(registry?.triggers?.[0]?.effectData?.expireWhenPermanentLeavesBattlefield).toBe(true);

    const diedState = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'phoenix', name: 'Flame-Wreathed Phoenix', type_line: 'Creature - Phoenix', power: '3', toughness: '3' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
      delayedTriggerRegistry: registry,
      priority: 'p1',
      turnPlayer: 'p1',
    } as any);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'dies', permanentId: 'phoenix' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      diedState,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    expect((owner.hand || []).map((card: any) => card.id)).toContain('phoenix');
  });

  it('schedules and resolves Liesa, Forgotten Archangel by returning the dead nontoken creature at the next end step', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever one or more nontoken creatures you control die, return those cards to their owner's hand at the beginning of the next end step.",
      'liesa',
      'p1',
      'Liesa, Forgotten Archangel'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-cleric', name: 'Dead Cleric', type_line: 'Creature - Cleric', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.CONTROLLED_CREATURE_DIED,
      abilities,
      {
        sourceId: 'dead-cleric',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'dead-cleric',
        chosenObjectIds: ['dead-cleric'],
        permanentTypes: ['Creature'],
        sourceIsToken: false,
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    expect((owner.graveyard || []).map((card: any) => card.id)).not.toContain('dead-cleric');
    expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-cleric');
  });

  it("schedules and resolves Molten Firebird while marking your next draw step to be skipped", () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When this creature dies, return it to the battlefield under its owner's control at the beginning of the next end step and you skip your next draw step.",
      'molten-firebird',
      'p1',
      'Molten Firebird'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'molten-firebird', name: 'Molten Firebird', type_line: 'Creature - Phoenix', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'molten-firebird',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p1',
        targetPermanentId: 'molten-firebird',
        chosenObjectIds: ['molten-firebird'],
        permanentTypes: ['Creature'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    const skipEffects = (((initial.state as any).skipNextDrawStepEffects || []) as any[]).filter(
      effect => String(effect?.playerId || '') === 'p1'
    );
    expect(registry?.triggers).toHaveLength(1);
    expect(skipEffects).toHaveLength(1);
    expect(skipEffects[0]?.remainingSkips).toBe(1);
    expect(skipEffects[0]?.sourceName).toBe('Molten Firebird');

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const returned = (resolved.state.battlefield || []).find((perm: any) => String(perm?.card?.id || '') === 'molten-firebird');
    expect(returned?.controller).toBe('p1');
  });

  it("schedules and resolves Rienne, Angel of Rebirth by returning the dead multicolored creature to its owner's hand", () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "Whenever another multicolored creature you control dies, return it to its owner's hand at the beginning of the next end step.",
      'rienne',
      'p1',
      'Rienne, Angel of Rebirth'
    );

    const initial = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-knight', name: 'Dead Knight', type_line: 'Creature - Human Knight', mana_cost: '{R}{W}', power: '3', toughness: '3' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-knight',
        sourceControllerId: 'p1',
        sourceOwnerId: 'p2',
        targetPermanentId: 'dead-knight',
        chosenObjectIds: ['dead-knight'],
        permanentTypes: ['Creature'],
        colors: ['R', 'W'],
      }
    );

    const registry = (initial.state as any).delayedTriggerRegistry;
    expect(registry?.triggers).toHaveLength(1);

    const delayedCheck = checkDelayedTriggers(registry, { type: 'end_step', activePlayerId: 'p1' });
    const [triggerInstance] = processDelayedTriggers(delayedCheck.triggersToFire, Date.now());
    const resolved = executeTriggeredAbilityEffectWithOracleIR(
      initial.state,
      {
        controllerId: triggerInstance.controllerId,
        sourceId: triggerInstance.sourceId,
        sourceName: triggerInstance.sourceName,
        effect: triggerInstance.effect,
      },
      triggerInstance.triggerEventDataSnapshot
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p2') as any;
    expect((owner.graveyard || []).map((card: any) => card.id)).not.toContain('dead-knight');
    expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-knight');
  });

  it('applies Athreos, God of Passage when the targeted opponent cannot pay 3 life', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever another creature you own dies, return it to your hand unless target opponent pays 3 life.',
      'athreos',
      'p1',
      'Athreos, God of Passage'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-cleric', name: 'Dead Cleric', type_line: 'Creature - Cleric', power: '2', toughness: '2' }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 2,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-cleric',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p1',
        targetPermanentId: 'dead-cleric',
        chosenObjectIds: ['dead-cleric'],
        permanentTypes: ['Creature'],
        targetOpponentId: 'p2',
      }
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    expect(resolved.triggers).toHaveLength(1);
    expect((owner.graveyard || []).map((card: any) => card.id)).not.toContain('dead-cleric');
    expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-cleric');
  });

  it('keeps Athreos, God of Passage as a player-choice gap when the targeted opponent can pay 3 life', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever another creature you own dies, return it to your hand unless target opponent pays 3 life.',
      'athreos',
      'p1',
      'Athreos, God of Passage'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-cleric', name: 'Dead Cleric', type_line: 'Creature - Cleric', power: '2', toughness: '2' }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 5,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-cleric',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p1',
        targetPermanentId: 'dead-cleric',
        chosenObjectIds: ['dead-cleric'],
        permanentTypes: ['Creature'],
        targetOpponentId: 'p2',
      }
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    expect(resolved.triggers).toHaveLength(1);
    expect((owner.graveyard || []).map((card: any) => card.id)).toContain('dead-cleric');
    expect((owner.hand || []).map((card: any) => card.id)).not.toContain('dead-cleric');
    expect(resolved.executions).toHaveLength(1);
    expect(resolved.executions[0]?.automationGaps.some((gap: any) => gap.reasonCode === 'player_choice_required')).toBe(true);
  });

  it('applies Athreos, God of Passage when the targeted opponent chooses to pay 3 life', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever another creature you own dies, return it to your hand unless target opponent pays 3 life.',
      'athreos',
      'p1',
      'Athreos, God of Passage'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-cleric', name: 'Dead Cleric', type_line: 'Creature - Cleric', power: '2', toughness: '2' }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 5,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-cleric',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p1',
        targetPermanentId: 'dead-cleric',
        chosenObjectIds: ['dead-cleric'],
        permanentTypes: ['Creature'],
        targetOpponentId: 'p2',
        unlessPaysLifeChoice: 'pay',
      }
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    const opponent = resolved.state.players.find((player: any) => player.id === 'p2') as any;
    expect((owner.graveyard || []).map((card: any) => card.id)).toContain('dead-cleric');
    expect((owner.hand || []).map((card: any) => card.id)).not.toContain('dead-cleric');
    expect(opponent.life).toBe(2);
    expect(resolved.executions[0]?.automationGaps.some((gap: any) => gap.reasonCode === 'player_choice_required')).toBe(false);
  });

  it('applies Athreos, God of Passage when the targeted opponent declines to pay 3 life', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever another creature you own dies, return it to your hand unless target opponent pays 3 life.',
      'athreos',
      'p1',
      'Athreos, God of Passage'
    );

    const resolved = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dead-cleric', name: 'Dead Cleric', type_line: 'Creature - Cleric', power: '2', toughness: '2' }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 5,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      TriggerEvent.DIES,
      abilities,
      {
        sourceId: 'dead-cleric',
        sourceControllerId: 'p2',
        sourceOwnerId: 'p1',
        targetPermanentId: 'dead-cleric',
        chosenObjectIds: ['dead-cleric'],
        permanentTypes: ['Creature'],
        targetOpponentId: 'p2',
        unlessPaysLifeChoice: 'decline',
      }
    );

    const owner = resolved.state.players.find((player: any) => player.id === 'p1') as any;
    const opponent = resolved.state.players.find((player: any) => player.id === 'p2') as any;
    expect((owner.graveyard || []).map((card: any) => card.id)).not.toContain('dead-cleric');
    expect((owner.hand || []).map((card: any) => card.id)).toContain('dead-cleric');
    expect(opponent.life).toBe(5);
    expect(resolved.executions[0]?.automationGaps.some((gap: any) => gap.reasonCode === 'player_choice_required')).toBe(false);
  });

  it('applies Pharika, God of Affliction by exiling the target and giving the Snake token to that card owner', () => {
    const ir = parseOracleTextToIR(
      '{B}{G}: Exile target creature card from a graveyard. Its owner creates a 1/1 black and green Snake enchantment creature token with deathtouch.',
      'Pharika, God of Affliction'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'pharika-target', owner: 'p2', name: 'Dead Mystic', type_line: 'Creature - Elf Druid' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'pharika',
        sourceName: 'Pharika, God of Affliction',
        targetPermanentId: 'pharika-target',
        selectorContext: { chosenObjectIds: ['pharika-target'] },
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect((p2.graveyard || []).map((card: any) => card.id)).not.toContain('pharika-target');
    expect((p2.exile || []).map((card: any) => card.id)).toContain('pharika-target');
    expect(result.state.battlefield).toHaveLength(1);
    expect((result.state.battlefield[0] as any)?.controller).toBe('p2');
  });

  it('applies Shamble Back by exiling the target, creating a Zombie, and gaining 2 life', () => {
    const ir = parseOracleTextToIR(
      'Exile target creature card from a graveyard. Create a 2/2 black Zombie creature token. You gain 2 life.',
      'Shamble Back'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'shamble-target', owner: 'p2', name: 'Dead Artist', type_line: 'Creature - Vampire' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Shamble Back',
        targetPermanentId: 'shamble-target',
        selectorContext: { chosenObjectIds: ['shamble-target'] },
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect((p2.exile || []).map((card: any) => card.id)).toContain('shamble-target');
    expect(result.state.battlefield).toHaveLength(1);
    expect((result.state.battlefield[0] as any)?.controller).toBe('p1');
    expect(p1.life).toBe(42);
  });

  it('applies Boneclad Necromancer when allowOptional enables the exile gate', () => {
    const ir = parseOracleTextToIR(
      'When Boneclad Necromancer enters, you may exile target creature card from a graveyard. If you do, create a 2/2 black Zombie creature token.',
      'Boneclad Necromancer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, library: [], hand: [], graveyard: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'boneclad-target', owner: 'p2', name: 'Dead Bear', type_line: 'Creature - Bear' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      }),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Boneclad Necromancer',
        targetPermanentId: 'boneclad-target',
        selectorContext: { chosenObjectIds: ['boneclad-target'] },
      },
      { allowOptional: true }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect((p2.exile || []).map((card: any) => card.id)).toContain('boneclad-target');
    expect(result.state.battlefield).toHaveLength(1);
    expect((result.state.battlefield[0] as any)?.controller).toBe('p1');
  });

  it('applies Oskar, Rubbish Reclaimer by granting cast permission to the discarded graveyard card', () => {
    const ir = parseOracleTextToIR(
      'Whenever you discard a nonland card, you may cast it from your graveyard.',
      'Oskar, Rubbish Reclaimer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 7,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'discarded-opt', name: 'Opt', type_line: 'Instant' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Oskar, Rubbish Reclaimer',
        selectorContext: { chosenObjectIds: ['discarded-opt'] },
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect((result.state as any).playableFromGraveyard?.p1?.['discarded-opt']).toBe(7);
    expect((p1.graveyard || [])[0]?.canBePlayedBy).toBe('p1');
    expect((p1.graveyard || [])[0]?.playableUntilTurn).toBe(7);
  });

  it("triggers and executes Archfiend's Vessel when it entered from your graveyard", () => {
    const abilities = parseTriggeredAbilitiesFromText(
      "When this creature enters, if it entered from your graveyard or you cast it from your graveyard, exile it.",
      'vessel',
      'p1',
      "Archfiend's Vessel"
    );

    const result = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'vessel',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            enteredFromZone: 'graveyard',
            card: {
              id: 'vessel-card',
              name: "Archfiend's Vessel",
              type_line: 'Creature - Demon',
              enteredFromZone: 'graveyard',
            },
          },
        ],
      } as any),
      TriggerEvent.ENTERS_BATTLEFIELD,
      abilities,
      {
        sourceId: 'vessel',
        sourceControllerId: 'p1',
        targetPermanentId: 'vessel',
        enteredFromZone: 'graveyard',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.triggers).toHaveLength(1);
    expect(result.state.battlefield).toHaveLength(0);
    expect((p1.exile || []).map((card: any) => card.id)).toContain('vessel-card');
  });

  it('triggers and schedules Prized Amalgam when a creature entered from your graveyard', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'Whenever a creature enters, if it entered from your graveyard or you cast it from your graveyard, return this card from your graveyard to the battlefield tapped at the beginning of the next end step.',
      'amalgam',
      'p1',
      'Prized Amalgam'
    );

    const result = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'amalgam',
                name: 'Prized Amalgam',
                type_line: 'Creature - Zombie',
                oracle_text:
                  'Whenever a creature enters, if it entered from your graveyard or you cast it from your graveyard, return this card from your graveyard to the battlefield tapped at the beginning of the next end step.',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'bear',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            enteredFromZone: 'graveyard',
            card: {
              id: 'bear-card',
              name: 'Reanimated Bear',
              type_line: 'Creature - Bear',
              enteredFromZone: 'graveyard',
            },
          },
        ],
      } as any),
      TriggerEvent.ENTERS_BATTLEFIELD,
      abilities,
      {
        sourceId: 'bear',
        sourceControllerId: 'p1',
        targetPermanentId: 'bear',
        enteredFromZone: 'graveyard',
      }
    );

    const delayedRegistry = (result.state as any).delayedTriggerRegistry;
    expect(result.triggers).toHaveLength(1);
    expect(Array.isArray(delayedRegistry?.triggers)).toBe(true);
    expect(delayedRegistry.triggers).toHaveLength(1);
    expect(delayedRegistry.triggers[0]?.sourceId).toBe('amalgam');
    expect(String(delayedRegistry.triggers[0]?.effect || '')).toContain('return this card from your graveyard to the battlefield tapped');
  });

  it('triggers and executes Rocket-Powered Goblin Glider when a target creature is supplied', () => {
    const abilities = parseTriggeredAbilitiesFromText(
      'When this Equipment enters, if it was cast from your graveyard, attach it to target creature you control.',
      'rocket',
      'p1',
      'Rocket-Powered Goblin Glider'
    );

    const result = processEventAndExecuteTriggeredOracle(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'rocket',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            castFromZone: 'graveyard',
            card: {
              id: 'rocket-card',
              name: 'Rocket-Powered Goblin Glider',
              type_line: 'Artifact - Equipment',
              castFromZone: 'graveyard',
            },
          },
          {
            id: 'bear',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            card: {
              id: 'bear-card',
              name: 'Target Bear',
              type_line: 'Creature - Bear',
            },
          },
        ],
      } as any),
      TriggerEvent.ENTERS_BATTLEFIELD,
      abilities,
      {
        sourceId: 'rocket',
        sourceControllerId: 'p1',
        targetPermanentId: 'bear',
        castFromZone: 'graveyard',
      },
      { allowOptional: true }
    );

    const rocket = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'rocket') as any;
    const bear = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'bear') as any;

    expect(result.triggers).toHaveLength(1);
    expect(rocket?.attachedTo).toBe('bear');
    expect((bear?.attachments || []).includes('rocket')).toBe(true);
  });

  it('applies Skyclave Shade by granting this-turn cast permission to the source card in your graveyard', () => {
    const ir = parseOracleTextToIR(
      "Landfall - Whenever a land you control enters, if this card is in your graveyard and it's your turn, you may cast it from your graveyard this turn.",
      'Skyclave Shade'
    );
    const steps = ir.abilities.flatMap(ability => ability.steps ?? []);

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 11,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'shade', name: 'Skyclave Shade', type_line: 'Creature - Shade' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'shade',
        sourceName: 'Skyclave Shade',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.shade).toBe(11);
  });

  it('applies parenthetical escape-style graveyard permissions like Confession Dial', () => {
    const ir = parseOracleTextToIR(
      '(You may cast it from your graveyard for its escape cost this turn.)',
      'Confession Dial'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 13,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dial', name: 'Confession Dial', type_line: 'Enchantment' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'dial',
        sourceName: 'Confession Dial',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.dial).toBe(13);
  });

  it('applies Woe Strider escape text by marking the graveyard card with escape-entry counters', () => {
    const ir = parseOracleTextToIR(
      'Escape-{3}{B}{B}, Exile four other cards from your graveyard. This creature escapes with two +1/+1 counters on it.',
      'Woe Strider'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 14,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'woe', name: 'Woe Strider', type_line: 'Creature - Horror' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'woe',
        sourceName: 'Woe Strider',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.woe).toBe(14);
    expect((result.state.players[0] as any).graveyard[0]?.graveyardAdditionalCost).toEqual({
      kind: 'exile_from_graveyard',
      count: 4,
      raw: 'Exile four other cards from your graveyard',
    });
    expect((result.state.players[0] as any).graveyard[0]?.entersBattlefieldWithCounters).toEqual({ '+1/+1': 2 });
  });

  it('applies flashback keyword permissions like Faithless Looting', () => {
    const ir = parseOracleTextToIR('Draw two cards, then discard two cards. Flashback {2}{R}', 'Faithless Looting');
    const steps = ir.abilities[1]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 17,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'looting', name: 'Faithless Looting', type_line: 'Sorcery' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'looting',
        sourceName: 'Faithless Looting',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.looting).toBe(17);
    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'looting')?.graveyardCastCostRaw).toBe('{2}{R}');
  });

  it('applies Dread Return flashback text by marking the graveyard card with its sacrifice additional cost', () => {
    const ir = parseOracleTextToIR(
      'Return target creature card from your graveyard to the battlefield. Flashback-Sacrifice three creatures.',
      'Dread Return'
    );
    const steps = ir.abilities[1]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 17,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dread', name: 'Dread Return', type_line: 'Sorcery' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'dread',
        sourceName: 'Dread Return',
      },
      { allowOptional: true }
    );

    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'dread')?.graveyardAdditionalCost).toEqual({
      kind: 'sacrifice',
      count: 3,
      filterText: 'creatures',
      raw: 'Sacrifice three creatures',
    });
  });

  it('applies Snapcaster Mage full-text flashback grants to the chosen graveyard card', () => {
    const ir = parseOracleTextToIR(
      'Flash. When Snapcaster Mage enters, target instant or sorcery card in your graveyard gains flashback until end of turn.\nThe flashback cost is equal to its mana cost.',
      'Snapcaster Mage'
    );
    const steps = ir.abilities[1]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 18,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'bolt', name: 'Lightning Bolt', type_line: 'Instant' },
              { id: 'bear', name: 'Runeclaw Bear', type_line: 'Creature - Bear' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'snapcaster',
        sourceName: 'Snapcaster Mage',
        selectorContext: { chosenObjectIds: ['bolt'] },
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.bolt).toBe(18);
    expect((result.state as any).playableFromGraveyard?.p1?.bear).toBeUndefined();
    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'bolt')?.graveyardCastCost).toBe('mana_cost');
  });

  it('applies Torrential Gearhulk by marking the chosen instant as castable without paying its mana cost', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, you may cast target instant card from your graveyard without paying its mana cost. If that spell would be put into your graveyard, exile it instead.',
      'Torrential Gearhulk'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 18,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'bolt', name: 'Lightning Bolt', type_line: 'Instant', mana_cost: '{R}' },
              { id: 'bear', name: 'Runeclaw Bear', type_line: 'Creature - Bear', mana_cost: '{1}{G}' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'gearhulk',
        sourceName: 'Torrential Gearhulk',
        selectorContext: { chosenObjectIds: ['bolt'] },
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.bolt).toBe(18);
    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'bolt')?.withoutPayingManaCost).toBe(true);
    expect(graveyard.find(card => card.id === 'bolt')?.exileInsteadOfGraveyard).toBe(true);
    expect(graveyard.find(card => card.id === 'bear')?.withoutPayingManaCost).toBeUndefined();
  });

  it("applies Uro, Titan of Nature's Wrath by sacrificing it only when it didn't escape", () => {
    const ir = parseOracleTextToIR(
      "When Uro enters, sacrifice it unless it escaped.",
      "Uro, Titan of Nature's Wrath"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const escapedResult = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'uro',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'uro-card',
              name: "Uro, Titan of Nature's Wrath",
              type_line: 'Legendary Creature - Elder Giant',
              castFromZone: 'graveyard',
            },
            castFromZone: 'graveyard',
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'uro',
        sourceName: "Uro, Titan of Nature's Wrath",
        targetCreatureId: 'uro',
        castFromZone: 'graveyard',
      },
      { allowOptional: true }
    );

    expect((escapedResult.state.battlefield as any[]).some((perm: any) => perm.id === 'uro')).toBe(true);

    const normalCastResult = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'uro',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'uro-card',
              name: "Uro, Titan of Nature's Wrath",
              type_line: 'Legendary Creature - Elder Giant',
              castFromZone: 'hand',
            },
            castFromZone: 'hand',
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'uro',
        sourceName: "Uro, Titan of Nature's Wrath",
        targetCreatureId: 'uro',
        castFromZone: 'hand',
      },
      { allowOptional: true }
    );

    expect((normalCastResult.state.battlefield as any[]).some((perm: any) => perm.id === 'uro')).toBe(false);
    expect((normalCastResult.state.players[0] as any).graveyard.map((card: any) => card.id)).toContain('uro-card');
  });

  it('applies static escape grants like Underworld Breach to matching graveyard cards', () => {
    const ir = parseOracleTextToIR(
      "Each nonland card in your graveyard has escape. The escape cost is equal to the card's mana cost plus exile three other cards from your graveyard.",
      'Underworld Breach'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 19,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'looting', name: 'Faithless Looting', type_line: 'Sorcery' },
              { id: 'swamp', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'breach',
        sourceName: 'Underworld Breach',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.looting).toBe(19);
    expect((result.state as any).playableFromGraveyard?.p1?.swamp).toBeUndefined();
    const breachGraveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(breachGraveyard.find(card => card.id === 'looting')?.graveyardAdditionalCost).toEqual({
      kind: 'exile_from_graveyard',
      count: 3,
      raw: 'exile three other cards from your graveyard',
    });
  });

  it('applies Past in Flames by marking matching graveyard spells with mana-cost flashback metadata', () => {
    const ir = parseOracleTextToIR(
      'Each instant and sorcery card in your graveyard gains flashback until end of turn. The flashback cost is equal to its mana cost.',
      'Past in Flames'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 20,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'bolt', name: 'Lightning Bolt', type_line: 'Instant', mana_cost: '{R}' },
              { id: 'ritual', name: 'Seething Song', type_line: 'Sorcery', mana_cost: '{2}{R}' },
              { id: 'swamp', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'past',
        sourceName: 'Past in Flames',
      },
      { allowOptional: true }
    );

    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'bolt')?.graveyardCastCost).toBe('mana_cost');
    expect(graveyard.find(card => card.id === 'ritual')?.graveyardCastCost).toBe('mana_cost');
    expect(graveyard.find(card => card.id === 'swamp')?.graveyardCastCost).toBeUndefined();
  });

  it('applies Six-style turn-gated retrace grants during your turn', () => {
    const ir = parseOracleTextToIR(
      'During your turn, nonland permanent cards in your graveyard have retrace. (You may cast permanent cards from your graveyard by discarding a land card in addition to paying their other costs.)',
      'Six'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        turnPlayer: 'p1',
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'relic', name: 'Mind Stone', type_line: 'Artifact' },
              { id: 'swamp', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'six',
        sourceName: 'Six',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.relic).toBe(21);
    expect((result.state as any).playableFromGraveyard?.p1?.swamp).toBeUndefined();
    const graveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(graveyard.find(card => card.id === 'relic')?.graveyardAdditionalCost).toEqual({
      kind: 'discard',
      count: 1,
      filterText: 'land',
      raw: 'discarding a land card',
    });
  });

  it('applies Gravecrawler only while you control a Zombie', () => {
    const ir = parseOracleTextToIR(
      'You may cast Gravecrawler from your graveyard as long as you control a Zombie.',
      'Gravecrawler'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const withZombie = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'crawler', name: 'Gravecrawler', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'zombie-perm',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            card: { id: 'zombie-card', name: 'Diregraf Ghoul', type_line: 'Creature - Zombie' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'crawler',
        sourceName: 'Gravecrawler',
      },
      { allowOptional: true }
    );

    expect((withZombie.state as any).playableFromGraveyard?.p1?.crawler).toBe(21);

    const withoutZombie = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'crawler', name: 'Gravecrawler', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'crawler',
        sourceName: 'Gravecrawler',
      },
      { allowOptional: true }
    );

    expect((withoutZombie.state as any).playableFromGraveyard?.p1?.crawler).toBeUndefined();
  });

  it('applies Squee, the Immortal from both graveyard and exile', () => {
    const ir = parseOracleTextToIR(
      'You may cast Squee, the Immortal from your graveyard or from exile.',
      'Squee, the Immortal'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const fromGraveyard = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'squee', name: 'Squee, the Immortal', type_line: 'Legendary Creature - Goblin' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'squee',
        sourceName: 'Squee, the Immortal',
      },
      { allowOptional: true }
    );

    expect((fromGraveyard.state as any).playableFromGraveyard?.p1?.squee).toBe(21);

    const fromExile = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [{ id: 'squee', name: 'Squee, the Immortal', type_line: 'Legendary Creature - Goblin' }],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'squee',
        sourceName: 'Squee, the Immortal',
      },
      { allowOptional: true }
    );

    expect((fromExile.state.players[0] as any).exile[0]?.canBePlayedBy).toBe('p1');
  });

  it('applies Rebound reminder text by marking the source card castable from exile for free', () => {
    const ir = parseOracleTextToIR('Rebound', 'Staggershock');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 5,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [{ id: 'staggershock', name: 'Staggershock', type_line: 'Instant' }],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'staggershock',
        sourceName: 'Staggershock',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const exiled = (p1.exile || []).find((card: any) => card.id === 'staggershock');

    expect(result.appliedSteps.some(s => s.kind === 'grant_exile_permission')).toBe(true);
    expect(result.skippedSteps.some(s => s.kind === 'grant_exile_permission')).toBe(false);
    expect(exiled?.canBePlayedBy).toBe('p1');
    expect(exiled?.withoutPayingManaCost).toBe(true);
  });

  it('applies Chainer, Nightmare Adept only to creature spells in your graveyard this turn', () => {
    const ir = parseOracleTextToIR(
      'Discard a card: You may cast a creature spell from your graveyard this turn.',
      'Chainer, Nightmare Adept'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'creature-card', name: 'Grizzly Bears', type_line: 'Creature - Bear' },
              { id: 'spell-card', name: 'Sign in Blood', type_line: 'Sorcery' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'chainer',
        sourceName: 'Chainer, Nightmare Adept',
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.['creature-card']).toBe(21);
    expect((result.state as any).playableFromGraveyard?.p1?.['spell-card']).toBeUndefined();
  });

  it('applies The Indomitable only while you control three or more tapped Pirates and/or Vehicles', () => {
    const ir = parseOracleTextToIR(
      'You may cast this card from your graveyard as long as you control three or more tapped Pirates and/or Vehicles.',
      'The Indomitable'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const withCrew = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'indomitable', name: 'The Indomitable', type_line: 'Legendary Artifact - Vehicle' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'pirate-a',
            controller: 'p1',
            owner: 'p1',
            tapped: true,
            card: { id: 'pirate-a-card', name: 'Pirate A', type_line: 'Creature - Pirate' },
          } as any,
          {
            id: 'pirate-b',
            controller: 'p1',
            owner: 'p1',
            tapped: true,
            card: { id: 'pirate-b-card', name: 'Pirate B', type_line: 'Creature - Pirate' },
          } as any,
          {
            id: 'vehicle-c',
            controller: 'p1',
            owner: 'p1',
            tapped: true,
            card: { id: 'vehicle-c-card', name: 'Vehicle C', type_line: 'Artifact - Vehicle' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'indomitable',
        sourceName: 'The Indomitable',
      },
      { allowOptional: true }
    );

    expect((withCrew.state as any).playableFromGraveyard?.p1?.indomitable).toBe(21);

    const withoutCrew = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 21,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'indomitable', name: 'The Indomitable', type_line: 'Legendary Artifact - Vehicle' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'pirate-a',
            controller: 'p1',
            owner: 'p1',
            tapped: true,
            card: { id: 'pirate-a-card', name: 'Pirate A', type_line: 'Creature - Pirate' },
          } as any,
          {
            id: 'vehicle-b',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            card: { id: 'vehicle-b-card', name: 'Vehicle B', type_line: 'Artifact - Vehicle' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'indomitable',
        sourceName: 'The Indomitable',
      },
      { allowOptional: true }
    );

    expect((withoutCrew.state as any).playableFromGraveyard?.p1?.indomitable).toBeUndefined();
  });

  it('applies Lurrus of the Dream-Den only during your turn and only to permanents with mana value 2 or less', () => {
    const ir = parseOracleTextToIR(
      'Once during each of your turns, you may cast a permanent spell with mana value 2 or less from your graveyard.',
      'Lurrus of the Dream-Den'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const onTurn = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 25,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'bauble', name: "Wayfarer's Bauble", type_line: 'Artifact', mana_value: 1 },
              { id: 'titan', name: 'Sun Titan', type_line: 'Creature - Giant', mana_value: 6 },
            ],
            exile: [],
          } as any,
        ],
        turnPlayer: 'p1',
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Lurrus of the Dream-Den',
      },
      { allowOptional: true }
    );

    expect((onTurn.state as any).playableFromGraveyard?.p1?.bauble).toBe(25);
    expect((onTurn.state as any).playableFromGraveyard?.p1?.titan).toBeUndefined();

    const offTurn = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 25,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'bauble', name: "Wayfarer's Bauble", type_line: 'Artifact', mana_value: 1 }],
            exile: [],
          } as any,
        ],
        turnPlayer: 'p2',
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Lurrus of the Dream-Den',
      },
      { allowOptional: true }
    );

    expect((offTurn.state as any).playableFromGraveyard?.p1?.bauble).toBeUndefined();
  });

  it('applies Rivaz of the Claw only during your turn and only to Dragon creature spells', () => {
    const ir = parseOracleTextToIR(
      'Once during each of your turns, you may cast a Dragon creature spell from your graveyard.',
      'Rivaz of the Claw'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const onTurn = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 26,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'dragon', name: 'Shivan Dragon', type_line: 'Creature - Dragon', mana_value: 6 },
              { id: 'goblin', name: 'Goblin Piker', type_line: 'Creature - Goblin', mana_value: 2 },
            ],
            exile: [],
          } as any,
        ],
        turnPlayer: 'p1',
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Rivaz of the Claw',
      },
      { allowOptional: true }
    );

    expect((onTurn.state as any).playableFromGraveyard?.p1?.dragon).toBe(26);
    expect((onTurn.state as any).playableFromGraveyard?.p1?.goblin).toBeUndefined();

    const offTurn = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 26,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dragon', name: 'Shivan Dragon', type_line: 'Creature - Dragon', mana_value: 6 }],
            exile: [],
          } as any,
        ],
        turnPlayer: 'p2',
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Rivaz of the Claw',
      },
      { allowOptional: true }
    );

    expect((offTurn.state as any).playableFromGraveyard?.p1?.dragon).toBeUndefined();
  });

  it('applies Ignite the Future graveyard rider by marking the exiled cards as free to play', () => {
    const ir = parseOracleTextToIR(
      'Exile the top three cards of your library. Until the end of your next turn, you may play those cards. If this spell was cast from a graveyard, you may play cards this way without paying their mana costs.',
      'Ignite the Future'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 22,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [
              { id: 'ignite-ex1', name: 'Lightning Bolt', type_line: 'Instant', mana_cost: '{R}' },
              { id: 'ignite-ex2', name: 'Mountain', type_line: 'Basic Land - Mountain' },
              { id: 'ignite-ex3', name: 'Seize the Spoils', type_line: 'Sorcery', mana_cost: '{2}{R}' },
            ],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'ignite',
        sourceName: 'Ignite the Future',
        castFromZone: 'graveyard',
      },
      { allowOptional: true }
    );

    const exile = ((result.state.players[0] as any)?.exile || []) as any[];
    expect(exile).toHaveLength(3);
    expect(exile.every(card => card.playableUntilTurn === 23)).toBe(true);
    expect(exile.every(card => card.withoutPayingManaCost === true)).toBe(true);
  });

  it("replays Sevinne's Reclamation-style copied spells from graveyard and auto-retargets a unique remaining card", () => {
    const ir = parseOracleTextToIR(
      'Return target permanent card with mana value 3 or less from your graveyard to the battlefield. If this spell was cast from a graveyard, you may copy this spell and may choose a new target for the copy.',
      "Sevinne's Reclamation"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'target-a', name: 'Soul-Guide Lantern', type_line: 'Artifact', mana_cost: '{1}', mana_value: 1 },
              { id: 'target-b', name: 'Wayfarer Bauble', type_line: 'Artifact', mana_cost: '{1}', mana_value: 1 },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'sev',
        sourceName: "Sevinne's Reclamation",
        targetPermanentId: 'target-a',
        castFromZone: 'graveyard',
      },
      { allowOptional: true }
    );

    expect((result.state.players[0] as any).graveyard).toEqual([]);
    expect(((result.state.battlefield || []) as any[]).map((perm: any) => perm.card?.id).sort()).toEqual([
      'target-a',
      'target-b',
    ]);
  });

  it('redirects Whip of Erebos-style returned creatures to exile when they are destroyed', () => {
    const ir = parseOracleTextToIR(
      'Return target creature card from your graveyard to the battlefield. It gains haste. Exile it at the beginning of the next end step. If it would leave the battlefield, exile it instead of putting it anywhere else.',
      'Whip of Erebos'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const reanimated = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'ghoul', name: 'Grave Ghoul', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'whip',
        sourceName: 'Whip of Erebos',
        targetCreatureId: 'ghoul',
      },
      { allowOptional: true }
    );

    const returnedPermanent = (reanimated.state.battlefield as any[]).find(
      (perm: any) => perm.card?.id === 'ghoul'
    ) as any;
    expect(returnedPermanent?.leaveBattlefieldReplacement).toBe('exile');

    const destroyed = applyOracleIRStepsToGameState(
      reanimated.state,
      [
        {
          kind: 'destroy',
          target: { kind: 'raw', text: 'that creature' },
          raw: 'Destroy that creature.',
        } as any,
      ],
      {
        controllerId: 'p1',
        targetCreatureId: returnedPermanent.id,
      },
      { allowOptional: true }
    );

    expect((destroyed.state.battlefield as any[]).some((perm: any) => perm.id === returnedPermanent.id)).toBe(false);
    expect((destroyed.state.players[0] as any).graveyard).toEqual([]);
    expect(((destroyed.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('ghoul');
  });

  it('applies Unearth keyword lines by returning the source card, scheduling next-end-step exile, and exiling it on leave', () => {
    const ir = parseOracleTextToIR(
      'Unearth {B} ({B}: Return this card from your graveyard to the battlefield. It gains haste. Exile it at the beginning of the next end step or if it would leave the battlefield. Unearth only as a sorcery.)',
      'Dregscape Zombie'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const unearthed = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'dreg', name: 'Dregscape Zombie', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'dreg',
        sourceName: 'Dregscape Zombie',
      },
      { allowOptional: true }
    );

    const returnedPermanent = (unearthed.state.battlefield as any[]).find(
      (perm: any) => perm.card?.id === 'dreg'
    ) as any;
    expect(unearthed.appliedSteps.map(step => step.kind)).toEqual([
      'move_zone',
      'schedule_delayed_battlefield_action',
      'grant_leave_battlefield_replacement',
    ]);
    expect(returnedPermanent?.controller).toBe('p1');
    expect(returnedPermanent?.leaveBattlefieldReplacement).toBe('exile');
    expect(((unearthed.state.players[0] as any).graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect((unearthed.state as any).delayedTriggerRegistry?.triggers).toHaveLength(1);

    const destroyed = applyOracleIRStepsToGameState(
      unearthed.state,
      [
        {
          kind: 'destroy',
          target: { kind: 'raw', text: 'that creature' },
          raw: 'Destroy that creature.',
        } as any,
      ],
      {
        controllerId: 'p1',
        targetCreatureId: returnedPermanent.id,
      },
      { allowOptional: true }
    );

    expect((destroyed.state.battlefield as any[]).some((perm: any) => perm.id === returnedPermanent.id)).toBe(false);
    expect(((destroyed.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('dreg');
  });

  it('applies Transmute keyword lines by tutoring a same-mana-value card into hand and shuffling', () => {
    const ir = parseOracleTextToIR('Transmute {1}{U}{U}', 'Muddle the Mixture');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [{ id: 'muddle', name: 'Muddle the Mixture', mana_cost: '{U}{U}', cmc: 2, type_line: 'Instant' }],
            library: [
              { id: 'drift', name: 'Drift of Phantasms', mana_cost: '{2}{U}', cmc: 3, type_line: 'Creature - Spirit' },
              { id: 'mana-rock', name: 'Arcane Signet', mana_cost: '{2}', cmc: 2, type_line: 'Artifact' },
              { id: 'land', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'muddle',
        sourceName: 'Muddle the Mixture',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find(p => p.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['search_library']);
    expect((player.hand || []).map((card: any) => card.id)).toContain('mana-rock');
    expect((player.library || []).map((card: any) => card.id)).not.toContain('mana-rock');
    expect((player.graveyard || []).map((card: any) => card.id)).toContain('muddle');
    expect(result.log.some(line => line.includes('revealed Arcane Signet'))).toBe(true);
    expect(result.log.some(line => line.includes('shuffled their library'))).toBe(true);
  });

  it('applies Basic landcycling keyword lines by tutoring a basic land into hand and shuffling', () => {
    const ir = parseOracleTextToIR('Basic landcycling {1}{B}', 'Absorb Vis');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'plains', name: 'Plains', type_line: 'Basic Land - Plains' },
              { id: 'mage', name: 'Silvercoat Lion', type_line: 'Creature - Cat' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'absorb-vis',
        sourceName: 'Absorb Vis',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find(p => p.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['search_library']);
    expect((player.hand || []).map((card: any) => card.id)).toContain('plains');
    expect((player.library || []).map((card: any) => card.id)).not.toContain('plains');
    expect(result.log.some(line => line.includes('revealed Plains'))).toBe(true);
    expect(result.log.some(line => line.includes('shuffled their library'))).toBe(true);
  });

  it('applies land-type cycling keyword lines by tutoring a matching subtype land into hand and shuffling', () => {
    const ir = parseOracleTextToIR('Plainscycling {2}', 'Eternal Dragon');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'fountain', name: 'Hallowed Fountain', type_line: 'Land - Plains Island' },
              { id: 'swamp', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'eternal-dragon',
        sourceName: 'Eternal Dragon',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find(p => p.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['search_library']);
    expect((player.hand || []).map((card: any) => card.id)).toContain('fountain');
    expect((player.library || []).map((card: any) => card.id)).not.toContain('fountain');
    expect(result.log.some(line => line.includes('revealed Hallowed Fountain'))).toBe(true);
    expect(result.log.some(line => line.includes('shuffled their library'))).toBe(true);
  });

  it('applies subtypecycling keyword lines by tutoring the matching card type into hand and shuffling', () => {
    const ir = parseOracleTextToIR('Wizardcycling {3}', 'Vedalken Aethermage');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'wizard', name: 'Vodalian Mage', type_line: 'Creature - Wizard Merfolk' },
              { id: 'land', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'aethermage',
        sourceName: 'Vedalken Aethermage',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find(p => p.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['search_library']);
    expect((player.hand || []).map((card: any) => card.id)).toContain('wizard');
    expect((player.library || []).map((card: any) => card.id)).not.toContain('wizard');
    expect(result.log.some(line => line.includes('revealed Vodalian Mage'))).toBe(true);
    expect(result.log.some(line => line.includes('shuffled their library'))).toBe(true);
  });

  it('applies Transfigure keyword lines by tutoring a same-mana-value creature onto the battlefield and shuffling', () => {
    const ir = parseOracleTextToIR('Transfigure {1}{B}{B}', 'Fleshwrither');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [
              { id: 'fleshwrither', name: 'Fleshwrither', mana_cost: '{3}{B}', cmc: 4, type_line: 'Creature - Horror', power: '3', toughness: '3' },
            ],
            library: [
              { id: 'solemn', name: 'Solemn Simulacrum', mana_cost: '{4}', cmc: 4, type_line: 'Artifact Creature - Golem', power: '2', toughness: '2' },
              { id: 'wrath', name: 'Wrath of God', mana_cost: '{2}{W}{W}', cmc: 4, type_line: 'Sorcery' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'fleshwrither',
        sourceName: 'Fleshwrither',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find(p => p.id === 'p1') as any;
    const battlefield = result.state.battlefield as any[];

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['search_library']);
    expect((battlefield || []).map((perm: any) => perm?.card?.name || perm?.name)).toContain('Solemn Simulacrum');
    expect((player.library || []).map((card: any) => card.id)).not.toContain('solemn');
    expect((player.library || []).map((card: any) => card.id)).toContain('wrath');
    expect(result.log.some(line => line.includes('shuffled their library'))).toBe(true);
  });

  it('applies Encore keyword lines by exiling the source and creating one tapped attacking copy per opponent', () => {
    const ir = parseOracleTextToIR('Encore {5}{U}{U}', 'Impaler Shrike');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [{ id: 'shrike', name: 'Impaler Shrike', type_line: 'Creature - Bird Horror', power: '3', toughness: '1' }],
            library: [],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'shrike',
        sourceName: 'Impaler Shrike',
      },
      { allowOptional: true }
    );

    const player1 = result.state.players.find(p => p.id === 'p1') as any;
    const tokens = (result.state.battlefield as any[]).filter((perm: any) => perm.isToken);

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect((player1.graveyard || []).map((card: any) => card.id)).not.toContain('shrike');
    expect((player1.exile || []).map((card: any) => card.id)).toContain('shrike');
    expect(tokens).toHaveLength(2);
    expect(tokens.map((token: any) => token.defendingPlayerId).sort()).toEqual(['p2', 'p3']);
    expect(tokens.every((token: any) => token.tapped === true)).toBe(true);
    expect(tokens.every((token: any) => token.attackingPlayerId === 'p1')).toBe(true);
    expect(tokens.every((token: any) => (token.grantedAbilities || []).includes('haste'))).toBe(true);
    expect((result.state as any).delayedTriggerRegistry?.triggers).toHaveLength(2);
  });

  it('applies Myriad keyword lines by creating tapped attacking copies for each other opponent', () => {
    const ir = parseOracleTextToIR('Myriad', 'Blade Envoy');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'envoy',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSickness: false,
            cardType: 'Creature - Human Warrior',
            type_line: 'Creature - Human Warrior',
            name: 'Blade Envoy',
            power: 4,
            toughness: 4,
            basePower: 4,
            baseToughness: 4,
            card: {
              id: 'envoy-card',
              name: 'Blade Envoy',
              type_line: 'Creature - Human Warrior',
              oracle_text: 'Myriad',
              power: '4',
              toughness: '4',
            },
          } as any,
        ],
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p4', name: 'P4', seat: 3, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'envoy',
        sourceName: 'Blade Envoy',
        selectorContext: {
          targetOpponentId: 'p2',
          targetPlayerId: 'p2',
        },
      },
      { allowOptional: true }
    );

    const tokens = (result.state.battlefield as any[]).filter((perm: any) => perm.isToken);

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token']);
    expect(tokens).toHaveLength(2);
    expect(tokens.map((token: any) => token.defendingPlayerId).sort()).toEqual(['p3', 'p4']);
    expect(tokens.every((token: any) => token.tapped === true)).toBe(true);
    expect(tokens.every((token: any) => token.attackingPlayerId === 'p1')).toBe(true);
    expect((result.state as any).delayedTriggerRegistry?.triggers).toHaveLength(2);
  });

  it('applies Annihilator keyword lines by making the defending player sacrifice permanents', () => {
    const ir = parseOracleTextToIR('Annihilator 2', 'Void Colossus');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'p2creature',
            controller: 'p2',
            owner: 'p2',
            card: { id: 'c2', name: 'Hill Giant', type_line: 'Creature - Giant' },
          },
          {
            id: 'p2artifact',
            controller: 'p2',
            owner: 'p2',
            card: { id: 'a2', name: 'Sol Ring', type_line: 'Artifact' },
          },
        ] as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        selectorContext: { targetOpponentId: 'p2' as any },
      }
    );
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['sacrifice']);
    expect(result.state.battlefield).toHaveLength(0);
    expect((p2.graveyard || []).map((card: any) => card.id).sort()).toEqual(['a2', 'c2']);
  });

  it('applies Afterlife keyword lines by creating white-black Spirit tokens with flying', () => {
    const ir = parseOracleTextToIR('Afterlife 2', 'Imperious Oligarch');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'oligarch',
        sourceName: 'Imperious Oligarch',
      }
    );
    const tokens = (result.state.battlefield as any[]).filter((perm: any) => perm.isToken);

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token']);
    expect(tokens).toHaveLength(2);
    expect(tokens.every((token: any) => token.controller === 'p1')).toBe(true);
    expect(tokens.every((token: any) => token.card?.power === '1' && token.card?.toughness === '1')).toBe(true);
    expect(tokens.every((token: any) => (token.card?.colors || []).join(',') === 'W,B')).toBe(true);
    expect(tokens.every((token: any) => String(token.card?.oracle_text || '').includes('Flying'))).toBe(true);
    expect(tokens.every((token: any) => String(token.card?.type_line || '').includes('Spirit'))).toBe(true);
  });

  it('applies Incubate keyword lines by creating an Incubator token with counters', () => {
    const ir = parseOracleTextToIR('Incubate 2', 'Norns Inquisitor');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'inquisitor',
        sourceName: 'Norns Inquisitor',
      }
    );
    const tokens = (result.state.battlefield as any[]).filter((perm: any) => perm.isToken);

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token']);
    expect(tokens).toHaveLength(1);
    expect(tokens[0]?.controller).toBe('p1');
    expect(tokens[0]?.counters).toEqual({ '+1/+1': 2 });
    expect(String(tokens[0]?.card?.type_line || '')).toContain('Incubator');
  });

  it('applies Amass by creating an Army token and putting counters on it when none exists', () => {
    const ir = parseOracleTextToIR('Amass 2', 'Lazotep Convert');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Lazotep Convert',
      }
    );

    const armies = ((result.state.battlefield || []) as any[]).filter((perm: any) =>
      String(perm.type_line || perm.card?.type_line || '').toLowerCase().includes('army')
    );

    expect(armies).toHaveLength(1);
    expect(armies[0]?.controller).toBe('p1');
    expect(armies[0]?.counters?.['+1/+1']).toBe(2);
    expect(result.appliedSteps.some(step => step.kind === 'create_token')).toBe(true);
    expect(result.appliedSteps.some(step => step.kind === 'add_counter')).toBe(true);
  });

  it('applies Amass to an existing Army instead of creating a new token', () => {
    const ir = parseOracleTextToIR('Amass 2', 'Lazotep Convert');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'existing-army',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Zombie Army',
            cardType: 'Creature',
            type_line: 'Creature - Zombie Army',
            power: 0,
            toughness: 0,
            tapped: false,
            summoningSick: false,
            counters: { '+1/+1': 1 },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Lazotep Convert',
      }
    );

    const armies = ((result.state.battlefield || []) as any[]).filter((perm: any) =>
      String(perm.type_line || perm.card?.type_line || '').toLowerCase().includes('army')
    );

    expect(armies).toHaveLength(1);
    expect(armies[0]?.id).toBe('existing-army');
    expect(armies[0]?.counters?.['+1/+1']).toBe(3);
    expect(result.appliedSteps.some(step => step.kind === 'create_token')).toBe(false);
    expect(result.appliedSteps.some(step => step.kind === 'add_counter')).toBe(true);
  });

  it('applies Amass Orcs by adding Orc to an existing non-Orc Army', () => {
    const ir = parseOracleTextToIR('Amass Orcs 2', 'Orc Muster');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'existing-army',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Zombie Army',
            cardType: 'Creature',
            type_line: 'Creature - Zombie Army',
            power: 0,
            toughness: 0,
            tapped: false,
            summoningSick: false,
            counters: {},
            card: {
              id: 'existing-army-card',
              name: 'Zombie Army',
              type_line: 'Creature - Zombie Army',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Orc Muster',
      }
    );

    const army = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'existing-army') as any;

    expect(army?.counters?.['+1/+1']).toBe(2);
    expect(String(army?.type_line || army?.card?.type_line || '')).toContain('Orc');
    expect(result.appliedSteps.some(step => step.kind === 'add_types')).toBe(true);
  });

  it('applies Explore by putting a revealed land into hand', () => {
    const ir = parseOracleTextToIR('Explore', 'Pathfinder');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'top-land', name: 'Forest', type_line: 'Basic Land - Forest' }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'explore-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Pathfinder',
            cardType: 'Creature',
            type_line: 'Creature - Scout',
            power: 2,
            toughness: 2,
            counters: {},
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'explore-source',
        sourceName: 'Pathfinder',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['explore']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['top-land']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual([]);
  });

  it('applies Explore by adding a counter when the revealed card is nonland', () => {
    const ir = parseOracleTextToIR('Explore', 'Pathfinder');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'top-spell', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'explore-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Pathfinder',
            cardType: 'Creature',
            type_line: 'Creature - Scout',
            power: 2,
            toughness: 2,
            counters: {},
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'explore-source',
        sourceName: 'Pathfinder',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'explore-source') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['explore']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['top-spell']);
    expect(source?.counters?.['+1/+1']).toBe(1);
  });

  it('applies Manifest by moving the top card of your library onto the battlefield face down', () => {
    const ir = parseOracleTextToIR('Manifest the top card of your library.', 'Whisperwood Elemental');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'manifest-card', name: 'Air Elemental', type_line: 'Creature - Elemental', power: '4', toughness: '4', mana_cost: '{3}{U}{U}' },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Whisperwood Elemental',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const manifested = ((result.state.battlefield || []) as any[])[0] as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual([]);
    expect(manifested?.card?.faceDown).toBe(true);
    expect(manifested?.faceUpCard?.id).toBe('manifest-card');
    expect(manifested?.basePower).toBe(2);
    expect(manifested?.baseToughness).toBe(2);
    expect(manifested?.effectiveTypes).toEqual(['Creature']);
  });

  it('applies Cloak by moving the top card of your library onto the battlefield face down with ward metadata', () => {
    const ir = parseOracleTextToIR('Cloak the top card of your library.', 'Cryptic Coat');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'cloak-card', name: 'Air Elemental', type_line: 'Creature - Elemental', power: '4', toughness: '4', mana_cost: '{3}{U}{U}' },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Cryptic Coat',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const cloaked = ((result.state.battlefield || []) as any[])[0] as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual([]);
    expect(cloaked?.card?.faceDown).toBe(true);
    expect(cloaked?.card?.wardCost).toBe('{2}');
    expect(cloaked?.faceUpCard?.id).toBe('cloak-card');
    expect(cloaked?.basePower).toBe(2);
    expect(cloaked?.baseToughness).toBe(2);
    expect(cloaked?.effectiveTypes).toEqual(['Creature']);
  });

  it("applies Manifest by moving the top card of that player's library onto your battlefield face down", () => {
    const ir = parseOracleTextToIR("Manifest the top card of that player's library.", 'Thieving Amalgam');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'their-manifest-card', name: 'Hill Giant', type_line: 'Creature - Giant', power: '3', toughness: '3', mana_cost: '{3}{R}' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Thieving Amalgam',
        selectorContext: { targetPlayerId: 'p2' },
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    const manifested = ((result.state.battlefield || []) as any[])[0] as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p2.library || []).map((card: any) => card.id)).toEqual([]);
    expect(manifested?.controller).toBe('p1');
    expect(manifested?.owner).toBe('p2');
    expect(manifested?.card?.faceDown).toBe(true);
    expect(manifested?.faceUpCard?.id).toBe('their-manifest-card');
  });

  it("applies Cloak by moving the top card of that player's library onto your battlefield face down with ward metadata", () => {
    const ir = parseOracleTextToIR("Cloak the top card of that player's library.", 'Etrata, Deadly Fugitive');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'their-cloak-card', name: 'Merrow Commerce', type_line: 'Kindred Enchantment - Merfolk', mana_cost: '{1}{U}' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Etrata, Deadly Fugitive',
        selectorContext: { targetPlayerId: 'p2' },
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    const cloaked = ((result.state.battlefield || []) as any[])[0] as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p2.library || []).map((card: any) => card.id)).toEqual([]);
    expect(cloaked?.controller).toBe('p1');
    expect(cloaked?.owner).toBe('p2');
    expect(cloaked?.card?.faceDown).toBe(true);
    expect(cloaked?.card?.wardCost).toBe('{2}');
    expect(cloaked?.faceUpCard?.id).toBe('their-cloak-card');
  });

  it('applies Forage by exiling three cards from your graveyard when that mode is selected', () => {
    const ir = parseOracleTextToIR('Forage', 'Camellia, the Seedmiser');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [
              { id: 'forage-gy-1', name: 'Card 1', type_line: 'Instant' },
              { id: 'forage-gy-2', name: 'Card 2', type_line: 'Sorcery' },
              { id: 'forage-gy-3', name: 'Card 3', type_line: 'Creature - Elf' },
            ],
            library: [],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Camellia, the Seedmiser',
      },
      {
        selectedModeIds: ['Exile three cards from your graveyard'],
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(result.appliedSteps.some(step => step.kind === 'move_zone')).toBe(true);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.exile || []).map((card: any) => card.id)).toEqual(['forage-gy-1', 'forage-gy-2', 'forage-gy-3']);
  });

  it('applies Forage by sacrificing a Food when that mode is selected', () => {
    const ir = parseOracleTextToIR('Forage', 'Camellia, the Seedmiser');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'forage-food',
            controller: 'p1',
            owner: 'p1',
            isToken: true,
            card: { id: 'forage-food-card', name: 'Food', type_line: 'Token Artifact - Food' },
          } as any,
          {
            id: 'forage-other',
            controller: 'p1',
            owner: 'p1',
            card: { id: 'forage-other-card', name: 'Mind Stone', type_line: 'Artifact' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Camellia, the Seedmiser',
      },
      {
        selectedModeIds: ['Sacrifice a Food'],
      }
    );

    const names = (result.state.battlefield || []).map((perm: any) => String((perm as any)?.card?.name || ''));
    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(result.appliedSteps.some(step => step.kind === 'sacrifice')).toBe(true);
    expect(names).toContain('Mind Stone');
    expect(names).not.toContain('Food');
  });

  it('applies clash win follow-ups when you reveal the higher mana value', () => {
    const ir = parseOracleTextToIR(
      'Clash with an opponent. If you win, put a +1/+1 counter on this creature.',
      "Adder-Staff Boggart"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'clash-top-you', name: 'Dragon', type_line: 'Creature - Dragon', mana_cost: '{5}{R}', cmc: 6 }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'clash-top-opp', name: 'Shock', type_line: 'Instant', mana_cost: '{R}', cmc: 1 }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'clash-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            card: {
              id: 'clash-source-card',
              name: 'Adder-Staff Boggart',
              type_line: 'Creature - Goblin Warrior',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'clash-source',
        sourceName: 'Adder-Staff Boggart',
        selectorContext: { targetOpponentId: 'p2' },
      }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'clash-source') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['clash', 'add_counter']);
    expect(source?.counters?.['+1/+1']).toBe(1);
  });

  it('skips clash win follow-ups when you do not reveal the higher mana value', () => {
    const ir = parseOracleTextToIR(
      'Clash with an opponent. If you win, put a +1/+1 counter on this creature.',
      "Adder-Staff Boggart"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'clash-low', name: 'Shock', type_line: 'Instant', mana_cost: '{R}', cmc: 1 }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'clash-high', name: 'Dragon', type_line: 'Creature - Dragon', mana_cost: '{5}{R}', cmc: 6 }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'clash-source-lose',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            card: {
              id: 'clash-source-card-lose',
              name: 'Adder-Staff Boggart',
              type_line: 'Creature - Goblin Warrior',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'clash-source-lose',
        sourceName: 'Adder-Staff Boggart',
        selectorContext: { targetOpponentId: 'p2' },
      }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'clash-source-lose') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['clash']);
    expect((source?.counters || {})['+1/+1'] || 0).toBe(0);
  });

  it('applies Learn by discarding a card and drawing a card when optional execution is enabled', () => {
    const ir = parseOracleTextToIR('Learn', 'Professor of Symbology');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [{ id: 'learn-discard', name: 'Plains', type_line: 'Basic Land - Plains' }],
            graveyard: [],
            library: [{ id: 'learn-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Professor of Symbology',
      },
      {
        allowOptional: true,
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['learn']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['learn-draw']);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual(['learn-discard']);
  });

  it('applies Learn by taking the only Lesson from outside the game when optional execution is enabled', () => {
    const ir = parseOracleTextToIR('Learn', 'Professor of Symbology');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'learn-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            sideboard: [
              { id: 'lesson-card', name: 'Mascot Exhibition', type_line: 'Sorcery - Lesson', mana_cost: '{7}', cmc: 7 },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Professor of Symbology',
      },
      {
        allowOptional: true,
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['learn']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['lesson-card']);
    expect((p1.sideboard || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['learn-draw']);
  });

  it('records a choice gap for Learn when both discard and Lesson branches are ambiguous', () => {
    const ir = parseOracleTextToIR('Learn', 'Professor of Symbology');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [
              { id: 'learn-discard-a', name: 'Plains', type_line: 'Basic Land - Plains' },
              { id: 'learn-discard-b', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            graveyard: [],
            library: [{ id: 'learn-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            sideboard: [
              { id: 'lesson-a', name: 'Academic Probation', type_line: 'Sorcery - Lesson', mana_cost: '{1}{W}', cmc: 2 },
              { id: 'lesson-b', name: 'Environmental Sciences', type_line: 'Sorcery - Lesson', mana_cost: '{2}', cmc: 2 },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Professor of Symbology',
      },
      {
        allowOptional: true,
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual([]);
    expect(result.automationGaps.some((gap: any) => gap.reasonCode === 'player_choice_required')).toBe(true);
  });

  it('applies villainous choice when a mode is selected explicitly', () => {
    const ir = parseOracleTextToIR(
      'Target opponent faces a villainous choice — You draw a card, or that player loses 3 life.',
      'Choice Test'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'choice-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Choice Test',
        selectorContext: { targetPlayerId: 'p2', targetOpponentId: 'p2' },
      },
      {
        selectedModeIds: ['that player loses 3 life'],
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(p2.life).toBe(37);
  });

  it("applies exert so the creature doesn't untap during its controller's next untap step", () => {
    const ir = parseOracleTextToIR('You may exert this creature as it attacks.', 'Gust Walker');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'gust-walker',
          controller: 'p1',
          owner: 'p1',
          tapped: true,
          card: {
            id: 'gust-walker-card',
            name: 'Gust Walker',
            type_line: 'Creature - Human Wizard',
            power: '2',
            toughness: '2',
          },
        },
      ] as any,
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] },
        { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] },
      ] as any,
    });

    const exerted = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'gust-walker',
        sourceName: 'Gust Walker',
      },
      { allowOptional: true }
    );

    const exertedPermanent = ((exerted.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'gust-walker');
    expect(exerted.appliedSteps.map(step => step.kind)).toEqual(['exert']);
    expect(exertedPermanent?.temporaryEffects?.map((effect: any) => effect.description)).toContain(
      "doesn't untap during your next untap step"
    );

    const afterFirstUntap = executeUntapStep(exerted.state as any, 'p1');
    const stillTapped = ((afterFirstUntap.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'gust-walker');
    expect(stillTapped?.tapped).toBe(true);
    expect(stillTapped?.temporaryEffects || []).toEqual([]);

    const afterSecondUntap = executeUntapStep(afterFirstUntap.state as any, 'p1');
    const untapped = ((afterSecondUntap.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'gust-walker');
    expect(untapped?.tapped).toBe(false);
  });

  it('applies open_attraction by moving the first Attraction from the attraction deck to the battlefield', () => {
    const ir = parseOracleTextToIR('Open an Attraction.', 'Scavenger Hunt');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [],
            attractionDeck: [
              {
                id: 'attraction-card-1',
                name: 'Balloon Stand',
                type_line: 'Artifact - Attraction',
                litUpNumbers: [2, 6],
                visitAbility: 'When you visit, draw a card.',
              },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Scavenger Hunt',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const attraction = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.card?.name === 'Balloon Stand');
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['open_attraction']);
    expect((p1.attractionDeck || []).length).toBe(0);
    expect(attraction?.controller).toBe('p1');
  });

  it('applies roll_visit_attractions by matching lit-up numbers against the die roll', () => {
    const ir = parseOracleTextToIR('Roll to visit your Attractions.', 'Park Map');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'visit-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'opened-attraction',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'attraction-card-1',
              name: 'Balloon Stand',
              type_line: 'Artifact - Attraction',
              litUpNumbers: [2, 6],
              visitAbility: 'When you visit, draw a card.',
            },
          },
        ] as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Park Map',
        dieRollResult: 2,
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['roll_visit_attractions', 'draw']);
    expect(result.log.some((line: string) => line.includes('Visited 1 Attraction'))).toBe(true);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['visit-draw']);
  });

  it('moves destroyed Attractions to their owner command-zone junkyard instead of the graveyard', () => {
    const ir = parseOracleTextToIR('Destroy all artifacts you control.', 'Test');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [],
            exile: [],
            commandZone: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'opened-attraction',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'attraction-card-1',
              name: 'Balloon Stand',
              type_line: 'Artifact - Attraction',
              litUpNumbers: [2, 6],
              visitAbility: 'When you visit, draw a card.',
            },
          } as any,
        ],
      } as any),
      steps,
      { controllerId: 'p1', sourceName: 'Test' }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.some(step => step.kind === 'destroy')).toBe(true);
    expect(result.state.battlefield).toEqual([]);
    expect((p1.graveyard || []).length).toBe(0);
    expect((p1.commandZone || []).map((card: any) => card.id)).toEqual(['attraction-card-1']);
    expect(Boolean((p1.commandZone || [])[0]?.inJunkyard)).toBe(true);
  });

  it('moves bounced Attractions to their owner command-zone junkyard instead of their hand', () => {
    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [],
            exile: [],
            commandZone: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'opened-attraction',
            controller: 'p1',
            owner: 'p1',
            card: {
              id: 'attraction-card-1',
              name: 'Balloon Stand',
              type_line: 'Artifact - Attraction',
              litUpNumbers: [2, 6],
              visitAbility: 'When you visit, draw a card.',
            },
          } as any,
        ],
      } as any),
      [
        {
          kind: 'move_zone',
          what: { kind: 'raw', text: 'that permanent' },
          to: 'hand',
          toRaw: "its owner's hand",
          raw: "Return that permanent to its owner's hand.",
        } as any,
      ],
      { controllerId: 'p1', targetPermanentId: 'opened-attraction' }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect(result.state.battlefield).toEqual([]);
    expect((p1.hand || []).length).toBe(0);
    expect((p1.commandZone || []).map((card: any) => card.id)).toEqual(['attraction-card-1']);
    expect(Boolean((p1.commandZone || [])[0]?.inJunkyard)).toBe(true);
  });

  it('applies take_initiative by setting the initiative designation to the controller', () => {
    const ir = parseOracleTextToIR('Take the initiative.', 'White Plume Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'White Plume Adventurer',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['take_initiative']);
    expect((result.state as any).initiative).toBe('p1');
  });

  it('applies become_monarch by setting the monarch designation to the controller', () => {
    const ir = parseOracleTextToIR('When this enchantment enters, you become the monarch.', 'Grave Venerations');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Grave Venerations',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['become_monarch']);
    expect((result.state as any).monarch).toBe('p1');
  });

  it('applies abandon_scheme by moving the source ongoing scheme to the bottom of the scheme deck', () => {
    const ir = parseOracleTextToIR('Abandon this scheme.', 'Dark Wings Bring Your Downfall');
    const steps = ir.abilities[0]?.steps ?? [];

    const scheme = {
      id: 'scheme-dark-wings',
      name: 'Dark Wings Bring Your Downfall',
      type_line: 'Ongoing Scheme',
      oracle_text: 'At the beginning of each end step, if two or more creatures died under your control this turn, abandon this scheme.',
    };

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
        ongoingSchemes: [scheme] as any,
        schemeDeck: [{ id: 'scheme-next', name: 'Next Scheme', type_line: 'Scheme' }] as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'scheme-dark-wings',
        sourceName: 'Dark Wings Bring Your Downfall',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['abandon_scheme']);
    expect(((result.state as any).ongoingSchemes || []).map((entry: any) => entry.id)).toEqual([]);
    expect(((result.state as any).schemeDeck || []).map((entry: any) => entry.id)).toEqual(['scheme-next', 'scheme-dark-wings']);
  });

  it('applies set_in_motion by replaying the referenced scheme set-in-motion trigger text', () => {
    const ir = parseOracleTextToIR('Set that scheme in motion again.', 'My Laughter Echoes');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'scheme-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [{ id: 'scheme-discard', name: 'Swamp', type_line: 'Basic Land - Swamp' }],
            graveyard: [],
            library: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'scheme-my-laughter',
        sourceName: 'My Laughter Echoes',
        lastSetInMotionScheme: {
          id: 'scheme-every-dream',
          name: 'Every Dream a Nightmare',
          type_line: 'Scheme',
          oracle_text: 'When you set this scheme in motion, each opponent discards a card. You draw a card for each land card discarded this way.',
        },
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['set_in_motion', 'discard', 'draw']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['scheme-draw']);
    expect((p2.hand || []).map((card: any) => card.id)).toEqual([]);
    expect((p2.graveyard || []).map((card: any) => card.id)).toEqual(['scheme-discard']);
  });

  it('applies venture_into_dungeon by entering Lost Mine of Phandelver on the first venture', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'scry']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'lost_mine',
      dungeonName: 'Lost Mine of Phandelver',
      roomIndex: 0,
    });
  });

  it('applies planeswalk by moving the current plane to the bottom and tracking the revealed plane', () => {
    const ir = parseOracleTextToIR('Planeswalk.', 'Spatial Merging');
    const steps = ir.abilities[0]?.steps ?? [];

    const currentPlane = {
      id: 'plane-current',
      name: 'The Maelstrom',
      type_line: 'Plane - Alara',
    };
    const nextPlane = {
      id: 'plane-next',
      name: 'Unyaro',
      type_line: 'Plane - Shandalar',
    };
    const laterPlane = {
      id: 'plane-later',
      name: 'Pools of Becoming',
      type_line: 'Plane - Bolas',
    };

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        houseRules: { enablePlanechase: true } as any,
        currentPlane: currentPlane as any,
        planarDeckFaceDown: [nextPlane, laterPlane] as any,
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Spatial Merging',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['planeswalk']);
    expect((result.state as any).currentPlane?.id).toBe('plane-next');
    expect(((result.state as any).planarDeckFaceDown || []).map((entry: any) => entry.id)).toEqual([
      'plane-later',
      'plane-current',
    ]);
    expect((result.state as any).planeswalkedThisTurn).toBe(true);
    expect((result.state as any).planeswalksAttempted).toBe(1);
    expect((result.state as any).planeswalkedToThisTurn?.p1).toEqual(['unyaro']);
    expect((result.state as any).planeswalkedToPlanesThisTurn?.p1).toEqual(['unyaro']);
  });

  it('applies assemble by moving the first Contraption from the contraption deck to the battlefield', () => {
    const ir = parseOracleTextToIR('Assemble a Contraption.', 'Finders, Keepers');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [],
            contraptionDeck: [
              {
                id: 'contraption-card-1',
                name: 'Auto-Key',
                type_line: 'Artifact - Contraption',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Finders, Keepers',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const contraption = (result.state.battlefield || []).find((perm: any) =>
      /contraption/i.test(String(perm?.card?.type_line || perm?.type_line || ''))
    ) as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['assemble']);
    expect((p1.contraptionDeck || []).length).toBe(0);
    expect(contraption?.controller).toBe('p1');
    expect(String(contraption?.card?.name || '')).toBe('Auto-Key');
  });

  it('applies regenerate by creating a regeneration shield on the targeted permanent', () => {
    const ir = parseOracleTextToIR('Regenerate target artifact.', 'Welding Jar');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'jar-target',
            controller: 'p1',
            owner: 'p1',
            name: 'Darksteel Ingot',
            type_line: 'Artifact',
            tapped: false,
            counters: {},
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Welding Jar',
        targetPermanentId: 'jar-target',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['regenerate']);
    expect(((result.state as any).regenerationShields || [])).toEqual([
      expect.objectContaining({
        permanentId: 'jar-target',
        controllerId: 'p1',
        isUsed: false,
        expiresAtEndOfTurn: true,
      }),
    ]);
  });

  it('consumes regeneration shields when a shielded permanent would be destroyed', () => {
    const ir = parseOracleTextToIR('Destroy target creature.', 'Murder');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'regen-creature',
            controller: 'p1',
            owner: 'p1',
            name: 'Drudge Skeletons',
            type_line: 'Creature - Skeleton',
            tapped: false,
            attacking: 'p2',
            markedDamage: 2,
            damageMarked: 2,
            damage: 2,
            counters: { damage: 2 },
          } as any,
        ],
        regenerationShields: [
          {
            id: 'regen-shield-1',
            permanentId: 'regen-creature',
            controllerId: 'p1',
            createdAt: 1,
            isUsed: false,
            expiresAtEndOfTurn: true,
          },
        ] as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Murder',
        targetPermanentId: 'regen-creature',
      }
    );

    const creature = (result.state.battlefield || []).find((perm: any) => perm.id === 'regen-creature') as any;
    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['destroy']);
    expect(creature).toBeTruthy();
    expect(creature.tapped).toBe(true);
    expect(creature.attacking).toBeUndefined();
    expect(creature.markedDamage).toBe(0);
    expect(creature.damageMarked).toBe(0);
    expect(creature.counters?.damage).toBe(0);
    expect((p1.graveyard || []).length).toBe(0);
    expect(((result.state as any).regenerationShields || [])[0]).toEqual(
      expect.objectContaining({
        permanentId: 'regen-creature',
        isUsed: true,
      })
    );
  });

  it("does not use regeneration shields when destroy says the permanent can't be regenerated", () => {
    const ir = parseOracleTextToIR("Destroy target creature. It can't be regenerated.", 'Terror');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'regen-creature',
            controller: 'p1',
            owner: 'p1',
            name: 'Drudge Skeletons',
            type_line: 'Creature - Skeleton',
            tapped: false,
            counters: {},
            card: {
              id: 'regen-card',
              name: 'Drudge Skeletons',
              type_line: 'Creature - Skeleton',
            },
          } as any,
        ],
        regenerationShields: [
          {
            id: 'regen-shield-1',
            permanentId: 'regen-creature',
            controllerId: 'p1',
            createdAt: 1,
            isUsed: false,
            expiresAtEndOfTurn: true,
          },
        ] as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Terror',
        targetPermanentId: 'regen-creature',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect((result.state.battlefield || []).find((perm: any) => perm.id === 'regen-creature')).toBeUndefined();
    expect((p1.graveyard || []).some((card: any) => card.id === 'regen-card')).toBe(true);
    expect(((result.state as any).regenerationShields || [])[0]).toEqual(
      expect.objectContaining({
        permanentId: 'regen-creature',
        isUsed: false,
      })
    );
  });

  it('applies venture_into_dungeon with an explicit dungeon choice for Tomb of Annihilation', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
        selectorContext: { chosenDungeonId: 'tomb' },
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'lose_life']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'tomb',
      dungeonName: 'Tomb of Annihilation',
      roomIndex: 0,
      roomId: 'trapped_entry',
    });
    expect(p1.life).toBe(39);
    expect(p2.life).toBe(39);
  });

  it('applies venture_into_dungeon room effects along the deterministic Lost Mine path', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
        dungeonProgress: {
          p1: {
            dungeonId: 'lost_mine',
            dungeonName: 'Lost Mine of Phandelver',
            roomIndex: 0,
          },
        } as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'create_token']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'lost_mine',
      dungeonName: 'Lost Mine of Phandelver',
      roomIndex: 1,
    });
    const battlefield = (result.state as any).battlefield || [];
    const goblinToken = battlefield.find((perm: any) => /goblin/i.test(String(perm?.name || perm?.card?.name || '')));
    expect(goblinToken).toBeTruthy();
  });

  it('applies venture_into_dungeon with an explicit branch choice for Mine Tunnels', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
        dungeonProgress: {
          p1: {
            dungeonId: 'lost_mine',
            dungeonName: 'Lost Mine of Phandelver',
            roomIndex: 0,
            roomId: 'cave_entrance',
          },
        } as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
        selectorContext: { chosenDungeonRoomId: 'mine_tunnels' },
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'create_token']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'lost_mine',
      dungeonName: 'Lost Mine of Phandelver',
      roomIndex: 1,
      roomId: 'mine_tunnels',
    });
    const battlefield = (result.state as any).battlefield || [];
    const treasureToken = battlefield.find((perm: any) => /treasure/i.test(String(perm?.name || perm?.card?.name || '')));
    expect(treasureToken).toBeTruthy();
  });

  it('applies venture_into_dungeon with an explicit branch choice for Dark Pool', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
        dungeonProgress: {
          p1: {
            dungeonId: 'lost_mine',
            dungeonName: 'Lost Mine of Phandelver',
            roomIndex: 1,
            roomId: 'goblin_lair',
          },
        } as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
        selectorContext: { chosenDungeonRoomId: 'dark_pool' },
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'lose_life', 'gain_life']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'lost_mine',
      dungeonName: 'Lost Mine of Phandelver',
      roomIndex: 2,
      roomId: 'dark_pool',
    });
    expect(p1.life).toBe(41);
    expect(p2.life).toBe(39);
  });

  it('applies venture_into_dungeon storeroom by placing a +1/+1 counter on the only legal creature', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon.', 'Triumphant Adventurer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'storeroom-target',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSick: false,
            counters: {},
            card: {
              id: 'storeroom-target-card',
              name: 'Storeroom Target',
              type_line: 'Creature - Human Soldier',
            },
          } as any,
        ],
        dungeonProgress: {
          p1: {
            dungeonId: 'lost_mine',
            dungeonName: 'Lost Mine of Phandelver',
            roomIndex: 1,
          },
        } as any,
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Triumphant Adventurer',
      }
    );

    const target = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'storeroom-target') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'add_counter']);
    expect((result.state as any).dungeonProgress?.p1).toMatchObject({
      dungeonId: 'lost_mine',
      dungeonName: 'Lost Mine of Phandelver',
      roomIndex: 2,
    });
    expect((target?.counters || {})['+1/+1']).toBe(1);
  });

  it('applies venture_into_dungeon by completing the dungeon on the final room and enabling completion conditionals', () => {
    const ir = parseOracleTextToIR('Venture into the dungeon. If you completed a dungeon this turn, draw a card.', 'Nadaar');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'dungeon-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 },
              { id: 'dungeon-draw-2', name: 'Ponder', type_line: 'Sorcery', mana_cost: '{U}', cmc: 1 },
            ],
            exile: [],
          } as any,
        ],
        dungeonProgress: {
          p1: {
            dungeonId: 'lost_mine',
            dungeonName: 'Lost Mine of Phandelver',
            roomIndex: 2,
          },
        } as any,
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Nadaar',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['venture_into_dungeon', 'draw', 'draw']);
    expect((result.state as any).dungeonProgress?.p1).toBeUndefined();
    expect((result.state as any).dungeonCompletedThisTurn?.p1).toBe(true);
    expect((result.state as any).dungeonCompleted?.p1).toBe(true);
    expect((result.state as any).completedDungeons?.p1).toBe(1);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['dungeon-draw', 'dungeon-draw-2']);
  });

  it('applies initiative-gated conditionals when you have the initiative', () => {
    const ir = parseOracleTextToIR('If you have the initiative, draw a card.', 'Seasoned Dungeoneer');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        initiative: 'p1' as any,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'initiative-draw', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Seasoned Dungeoneer',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['draw']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['initiative-draw']);
  });

  it('applies Manifest dread by manifesting the first of the top two cards and milling the rest', () => {
    const ir = parseOracleTextToIR('Manifest dread', 'Abhorrent Oculus');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'dread-manifest', name: 'Air Elemental', type_line: 'Creature - Elemental', power: '4', toughness: '4', mana_cost: '{3}{U}{U}' },
              { id: 'dread-rest', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 },
              { id: 'dread-third', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Abhorrent Oculus',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const manifested = ((result.state.battlefield || []) as any[])[0] as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['manifest_dread']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['dread-third']);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual(['dread-rest']);
    expect(manifested?.card?.faceDown).toBe(true);
    expect(manifested?.faceUpCard?.id).toBe('dread-manifest');
  });

  it('applies Connive by drawing, discarding, and adding counters for nonland cards discarded', () => {
    const ir = parseOracleTextToIR('Connive 2', 'Raffine Informant');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'draw-1', name: 'Island', type_line: 'Basic Land - Island' },
              { id: 'draw-2', name: 'Opt', type_line: 'Instant', mana_cost: '{U}', cmc: 1 },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'connive-source',
            ownerId: 'p1',
            controller: 'p1',
            name: 'Raffine Informant',
            cardType: 'Creature',
            type_line: 'Creature - Human Wizard',
            power: 2,
            toughness: 1,
            counters: {},
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'connive-source',
        sourceName: 'Raffine Informant',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'connive-source') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['draw', 'discard', 'add_counter']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual(['draw-1', 'draw-2']);
    expect(source?.counters?.['+1/+1']).toBe(1);
  });

  it('applies Discover by keeping the first legal hit exiled and bottoming the rest', () => {
    const ir = parseOracleTextToIR('Discover 4', 'Pantlaza');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 12,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'discover-land', name: 'Forest', type_line: 'Basic Land - Forest' },
              { id: 'discover-big', name: 'Ancient Brontodon', type_line: 'Creature - Dinosaur', mana_cost: '{6}{G}{G}', cmc: 8 },
              { id: 'discover-hit', name: 'Growth Spiral', type_line: 'Instant', mana_cost: '{G}{U}', cmc: 2 },
              { id: 'discover-after', name: 'Island', type_line: 'Basic Land - Island' },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Pantlaza',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['impulse_exile_top', 'modify_exile_permissions']);
    expect((p1.exile || []).map((card: any) => card.id)).toEqual(['discover-hit']);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['discover-after', 'discover-land', 'discover-big']);
    expect(p1.exile[0]?.canBePlayedBy).toBe('p1');
    expect(p1.exile[0]?.withoutPayingManaCost).toBe(true);
    expect(p1.exile[0]?.playableUntilTurn).toBe(12);
  });

  it('applies Discover with no legal hit by returning all exiled cards to the library bottom', () => {
    const ir = parseOracleTextToIR('Discover 3', 'Pantlaza');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 12,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [],
            library: [
              { id: 'discover-land', name: 'Forest', type_line: 'Basic Land - Forest' },
              { id: 'discover-big-1', name: 'Ancient Brontodon', type_line: 'Creature - Dinosaur', mana_cost: '{6}{G}{G}', cmc: 8 },
              { id: 'discover-big-2', name: 'Colossal Dreadmaw', type_line: 'Creature - Dinosaur', mana_cost: '{4}{G}{G}', cmc: 6 },
            ],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Pantlaza',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['impulse_exile_top', 'modify_exile_permissions']);
    expect((p1.exile || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['discover-land', 'discover-big-1', 'discover-big-2']);
  });

  it('applies Renown keyword lines by adding a counter once and marking the source as renowned', () => {
    const ir = parseOracleTextToIR('Renown 1', 'Topan Freeblade');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'renown-source',
          ownerId: 'p1',
          owner: 'p1',
          controller: 'p1',
          tapped: false,
          summoningSickness: false,
          counters: {},
          power: 2,
          toughness: 2,
          basePower: 2,
          baseToughness: 2,
          card: {
            id: 'renown-source-card',
            name: 'Topan Freeblade',
            type_line: 'Creature - Human Soldier',
            oracle_text: 'Renown 1',
            power: '2',
            toughness: '2',
          },
        } as any,
      ],
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
      ],
    } as any);

    const first = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'renown-source',
      sourceName: 'Topan Freeblade',
    });
    const firstSource = (first.state.battlefield as any[]).find((perm: any) => perm.id === 'renown-source');

    expect((firstSource?.counters || {})['+1/+1']).toBe(1);
    expect(firstSource?.isRenowned).toBe(true);

    const second = applyOracleIRStepsToGameState(first.state, steps, {
      controllerId: 'p1',
      sourceId: 'renown-source',
      sourceName: 'Topan Freeblade',
    });
    const secondSource = (second.state.battlefield as any[]).find((perm: any) => perm.id === 'renown-source');

    expect((secondSource?.counters || {})['+1/+1']).toBe(1);
    expect(secondSource?.isRenowned).toBe(true);
  });

  it('applies Monstrosity keyword lines by adding counters once and marking the source as monstrous', () => {
    const ir = parseOracleTextToIR('Monstrosity 3.', 'Hundred-Handed One');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      battlefield: [
        {
          id: 'monstrosity-source',
          ownerId: 'p1',
          owner: 'p1',
          controller: 'p1',
          tapped: false,
          summoningSickness: false,
          counters: {},
          power: 3,
          toughness: 5,
          basePower: 3,
          baseToughness: 5,
          card: {
            id: 'monstrosity-source-card',
            name: 'Hundred-Handed One',
            type_line: 'Creature - Giant',
            oracle_text: 'Monstrosity 3',
            power: '3',
            toughness: '5',
          },
        } as any,
      ],
      players: [
        { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
      ],
    } as any);

    const first = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'monstrosity-source',
      sourceName: 'Hundred-Handed One',
    });
    const firstSource = (first.state.battlefield as any[]).find((perm: any) => perm.id === 'monstrosity-source');

    expect((firstSource?.counters || {})['+1/+1']).toBe(3);
    expect(firstSource?.isMonstrous).toBe(true);
    expect(firstSource?.monstrosityX).toBe(3);

    const second = applyOracleIRStepsToGameState(first.state, steps, {
      controllerId: 'p1',
      sourceId: 'monstrosity-source',
      sourceName: 'Hundred-Handed One',
    });
    const secondSource = (second.state.battlefield as any[]).find((perm: any) => perm.id === 'monstrosity-source');

    expect((secondSource?.counters || {})['+1/+1']).toBe(3);
    expect(secondSource?.isMonstrous).toBe(true);
    expect(secondSource?.monstrosityX).toBe(3);
  });

  it('applies Endure by adding counters when that mode is selected', () => {
    const ir = parseOracleTextToIR('Endure 2.', 'Wary Watchdog');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'endure-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSickness: false,
            counters: {},
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            card: {
              id: 'endure-source-card',
              name: 'Wary Watchdog',
              type_line: 'Creature - Dog',
              oracle_text: 'Endure 2',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'endure-source',
        sourceName: 'Wary Watchdog',
      },
      {
        selectedModeIds: ['Put 2 +1/+1 counters on this permanent'],
      }
    );

    const source = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'endure-source');
    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect((source?.counters || {})['+1/+1']).toBe(2);
  });

  it('applies Endure by creating a Spirit token when that mode is selected', () => {
    const ir = parseOracleTextToIR('Endure 2.', 'Wary Watchdog');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'endure-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSickness: false,
            counters: {},
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            card: {
              id: 'endure-source-card',
              name: 'Wary Watchdog',
              type_line: 'Creature - Dog',
              oracle_text: 'Endure 2',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'endure-source',
        sourceName: 'Wary Watchdog',
      },
      {
        selectedModeIds: ['Create a 2/2 white Spirit creature token'],
      }
    );

    const battlefield = (result.state.battlefield as any[]).filter((perm: any) => String(perm?.controller || '') === 'p1');
    const spiritToken = battlefield.find((perm: any) => String(perm?.card?.type_line || '').includes('Spirit'));

    expect(result.appliedSteps.some(step => step.kind === 'choose_mode')).toBe(true);
    expect(spiritToken).toBeTruthy();
    expect(Number(spiritToken?.power ?? spiritToken?.basePower ?? spiritToken?.card?.power)).toBe(2);
    expect(Number(spiritToken?.toughness ?? spiritToken?.baseToughness ?? spiritToken?.card?.toughness)).toBe(2);
  });

  it('applies Collect evidence by exiling a deterministic graveyard subset with enough total mana value', () => {
    const ir = parseOracleTextToIR('Collect evidence 4.', 'Deadly Cover-Up');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [
              { id: 'evidence-1', name: 'One Drop', type_line: 'Creature - Human', mana_cost: '{W}', cmc: 1 },
              { id: 'evidence-2', name: 'Four Drop', type_line: 'Creature - Beast', mana_cost: '{3}{G}', cmc: 4 },
              { id: 'evidence-3', name: 'Two Drop', type_line: 'Instant', mana_cost: '{1}{U}', cmc: 2 },
            ],
            library: [],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Deadly Cover-Up',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect((p1.exile || []).map((card: any) => card.id)).toEqual(['evidence-2']);
    expect((p1.graveyard || []).map((card: any) => card.id)).toEqual(['evidence-1', 'evidence-3']);
  });

  it('applies immediate collect evidence follow-up conditionals when evidence was collected', () => {
    const ir = parseOracleTextToIR('Collect evidence 4. If evidence was collected, draw a card.', 'Deadly Cover-Up');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [
              { id: 'evidence-2', name: 'Four Drop', type_line: 'Creature - Beast', mana_cost: '{3}{G}', cmc: 4 },
            ],
            library: [{ id: 'drawn-after-evidence', name: 'Drawn', type_line: 'Instant' }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Deadly Cover-Up',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect((p1.exile || []).map((card: any) => card.id)).toEqual(['evidence-2']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['drawn-after-evidence']);
  });

  it('skips immediate collect evidence follow-up conditionals when evidence was not collected', () => {
    const ir = parseOracleTextToIR('Collect evidence 4. If evidence was collected, draw a card.', 'Deadly Cover-Up');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            hand: [],
            graveyard: [{ id: 'evidence-1', name: 'One Drop', type_line: 'Creature - Human', mana_cost: '{W}', cmc: 1 }],
            library: [{ id: 'undrawn-after-failed-evidence', name: 'Undrawn', type_line: 'Instant' }],
            exile: [],
          } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Deadly Cover-Up',
      }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    expect((p1.exile || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual([]);
    expect((p1.library || []).map((card: any) => card.id)).toEqual(['undrawn-after-failed-evidence']);
  });

  it('applies Ingest keyword lines by exiling the top card of the damaged player library', () => {
    const ir = parseOracleTextToIR('Ingest', 'Benthic Infiltrator');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            hand: [],
            graveyard: [],
            library: [{ id: 'p2c1' }, { id: 'p2c2' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'ingest-source',
        sourceName: 'Benthic Infiltrator',
        selectorContext: { targetPlayerId: 'p2' as any },
      }
    );
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['exile_top']);
    expect((p2.library || []).map((card: any) => card.id)).toEqual(['p2c2']);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['p2c1']);
  });

  it('applies Poisonous keyword lines by giving the damaged player poison counters', () => {
    const ir = parseOracleTextToIR('Poisonous 3', 'Pit Scorpion');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [], counters: {} } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [], counters: {} } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'poisonous-source',
        sourceName: 'Pit Scorpion',
        selectorContext: { targetPlayerId: 'p2' as any },
      }
    );
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['add_player_counter']);
    expect(p2.poisonCounters).toBe(3);
    expect((p2.counters || {}).poison).toBe(3);
    expect((result.state as any).poisonCounters?.p2).toBe(3);
  });

  it('applies Mobilize keyword lines by creating tapped attacking Warrior tokens for the defending player', () => {
    const ir = parseOracleTextToIR('Mobilize 3', 'Warhost Herald');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'warhost',
        sourceName: 'Warhost Herald',
        selectorContext: { targetOpponentId: 'p2' as any },
      }
    );
    const tokens = (result.state.battlefield as any[]).filter((perm: any) => perm.isToken);

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token']);
    expect(tokens).toHaveLength(3);
    expect(tokens.every((token: any) => token.defendingPlayerId === 'p2')).toBe(true);
    expect(tokens.every((token: any) => token.attackingPlayerId === 'p1')).toBe(true);
    expect(tokens.every((token: any) => token.tapped === true)).toBe(true);
    expect((result.state as any).delayedTriggerRegistry?.triggers).toHaveLength(1);
    expect((result.state as any).delayedTriggerRegistry?.triggers?.[0]?.effect).toBe('Sacrifice those tokens.');
  });

  it('applies Melee keyword lines by buffing the source for each player being attacked', () => {
    const ir = parseOracleTextToIR('Melee', 'Bloodboil Marauder');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p3', name: 'P3', seat: 2, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'melee-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 3,
            toughness: 3,
            basePower: 3,
            baseToughness: 3,
            card: {
              id: 'melee-source-card',
              name: 'Bloodboil Marauder',
              type_line: 'Creature - Human Berserker',
              oracle_text: 'Melee',
              power: '3',
              toughness: '3',
            },
          } as any,
          {
            id: 'support-attacker',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p3',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p3',
            summoningSickness: false,
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            card: {
              id: 'support-attacker-card',
              name: 'Support Raider',
              type_line: 'Creature - Human Warrior',
              oracle_text: '',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'melee-source',
        sourceName: 'Bloodboil Marauder',
      }
    );
    const source = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'melee-source');
    const ptMod = (Array.isArray(source?.modifiers) ? source.modifiers : []).find((m: any) => m?.type === 'powerToughness');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['modify_pt']);
    expect(ptMod?.power).toBe(2);
    expect(ptMod?.toughness).toBe(2);
  });

  it('applies Mentor keyword lines by adding a +1/+1 counter to the only smaller attacker', () => {
    const ir = parseOracleTextToIR('Mentor', 'Fresh-Faced Recruit');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'mentor-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 3,
            toughness: 3,
            basePower: 3,
            baseToughness: 3,
            counters: {},
            card: {
              id: 'mentor-source-card',
              name: 'Fresh-Faced Recruit',
              type_line: 'Creature - Human Soldier',
              oracle_text: 'Mentor',
              power: '3',
              toughness: '3',
            },
          } as any,
          {
            id: 'mentor-target',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            counters: {},
            card: {
              id: 'mentor-target-card',
              name: 'Mentored Ally',
              type_line: 'Creature - Human Soldier',
              oracle_text: '',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'mentor-source',
        sourceName: 'Fresh-Faced Recruit',
      }
    );
    const target = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'mentor-target');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['add_counter']);
    expect((target?.counters || {})['+1/+1']).toBe(1);
  });

  it('applies Mentor keyword lines to a chosen smaller attacker when multiple are legal', () => {
    const ir = parseOracleTextToIR('Mentor', 'Fresh-Faced Recruit');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'mentor-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 4,
            toughness: 4,
            basePower: 4,
            baseToughness: 4,
            counters: {},
            card: {
              id: 'mentor-source-card',
              name: 'Fresh-Faced Recruit',
              type_line: 'Creature - Human Soldier',
              oracle_text: 'Mentor',
              power: '4',
              toughness: '4',
            },
          } as any,
          {
            id: 'mentor-target-a',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            counters: {},
            card: {
              id: 'mentor-target-a-card',
              name: 'Mentored Ally A',
              type_line: 'Creature - Human Soldier',
              oracle_text: '',
              power: '2',
              toughness: '2',
            },
          } as any,
          {
            id: 'mentor-target-b',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 1,
            toughness: 1,
            basePower: 1,
            baseToughness: 1,
            counters: {},
            card: {
              id: 'mentor-target-b-card',
              name: 'Mentored Ally B',
              type_line: 'Creature - Human Soldier',
              oracle_text: '',
              power: '1',
              toughness: '1',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'mentor-source',
        sourceName: 'Fresh-Faced Recruit',
        targetCreatureId: 'mentor-target-b',
        selectorContext: { chosenObjectIds: ['mentor-target-b'] },
      }
    );
    const firstTarget = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'mentor-target-a');
    const secondTarget = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'mentor-target-b');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['add_counter']);
    expect((firstTarget?.counters || {})['+1/+1'] || 0).toBe(0);
    expect((secondTarget?.counters || {})['+1/+1']).toBe(1);
  });

  it('applies Battle cry keyword lines by buffing each other attacking creature', () => {
    const ir = parseOracleTextToIR('Battle cry', 'Signal Pest');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          { id: 'p1', name: 'P1', seat: 0, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
          { id: 'p2', name: 'P2', seat: 1, life: 40, hand: [], graveyard: [], library: [], exile: [] } as any,
        ],
        battlefield: [
          {
            id: 'battle-cry-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 1,
            toughness: 1,
            basePower: 1,
            baseToughness: 1,
            card: {
              id: 'battle-cry-source-card',
              name: 'Signal Pest',
              type_line: 'Artifact Creature - Pest',
              oracle_text: 'Battle cry',
              power: '1',
              toughness: '1',
            },
          } as any,
          {
            id: 'battle-cry-target',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: true,
            attacking: 'p2',
            attackingPlayerId: 'p1',
            defendingPlayerId: 'p2',
            summoningSickness: false,
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            card: {
              id: 'battle-cry-target-card',
              name: 'Battle Cry Ally',
              type_line: 'Creature - Human Soldier',
              oracle_text: '',
              power: '2',
              toughness: '2',
            },
          } as any,
          {
            id: 'battle-cry-non-attacker',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSickness: false,
            power: 2,
            toughness: 2,
            basePower: 2,
            baseToughness: 2,
            card: {
              id: 'battle-cry-non-attacker-card',
              name: 'Bench Creature',
              type_line: 'Creature - Human Soldier',
              oracle_text: '',
              power: '2',
              toughness: '2',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'battle-cry-source',
        sourceName: 'Signal Pest',
      }
    );
    const target = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'battle-cry-target');
    const source = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'battle-cry-source');
    const nonAttacker = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'battle-cry-non-attacker');
    const targetModifier = (Array.isArray(target?.modifiers) ? target.modifiers : []).find((modifier: any) => modifier?.type === 'powerToughness');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['modify_pt']);
    expect(targetModifier?.power).toBe(1);
    expect(targetModifier?.toughness).toBe(0);
    expect(Array.isArray(source?.modifiers) ? source.modifiers : []).toHaveLength(0);
    expect(Array.isArray(nonAttacker?.modifiers) ? nonAttacker.modifiers : []).toHaveLength(0);
  });

  it('applies double_counters by doubling each kind of counter on the target permanent', () => {
    const ir = parseOracleTextToIR(
      'For each kind of counter on target permanent, put another of that kind of counter on that permanent.',
      'Gilder Bairn'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'counter-target',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: { '+1/+1': 2, charge: 1 },
            card: {
              id: 'counter-target-card',
              name: 'Counter Engine',
              type_line: 'Artifact Creature - Construct',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'gilder-source',
        sourceName: 'Gilder Bairn',
        targetPermanentId: 'counter-target',
      }
    );
    const target = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'counter-target');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['double_counters']);
    expect((target?.counters || {})['+1/+1']).toBe(4);
    expect((target?.counters || {}).charge).toBe(2);
  });

  it('applies specific-counter double_counters wording without doubling unrelated counters', () => {
    const ir = parseOracleTextToIR(
      'Double the number of +1/+1 counters on target creature.',
      'Ornery Tumblewagg'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'counter-target',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: { '+1/+1': 3, charge: 2 },
            card: {
              id: 'counter-target-card',
              name: 'Test Creature',
              type_line: 'Creature - Beast',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'tumblewagg-source',
        sourceName: 'Ornery Tumblewagg',
        targetCreatureId: 'counter-target',
      }
    );
    const target = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'counter-target');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['double_counters']);
    expect((target?.counters || {})['+1/+1']).toBe(6);
    expect((target?.counters || {}).charge).toBe(2);
  });

  it('applies specific-counter double_counters wording to plural contextual targets', () => {
    const ir = parseOracleTextToIR(
      'Double the number of +1/+1 counters on those creatures.',
      'Omnivorous Flytrap'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'creature-a',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            counters: { '+1/+1': 2, shield: 1 },
            card: { id: 'creature-a-card', name: 'A', type_line: 'Creature - Beast' },
          } as any,
          {
            id: 'creature-b',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            counters: { '+1/+1': 1 },
            card: { id: 'creature-b-card', name: 'B', type_line: 'Creature - Beast' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'flytrap-source',
        sourceName: 'Omnivorous Flytrap',
        selectorContext: { chosenObjectIds: ['creature-a', 'creature-b'] },
      }
    );

    const creatureA = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'creature-a');
    const creatureB = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'creature-b');
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['double_counters']);
    expect((creatureA?.counters || {})['+1/+1']).toBe(4);
    expect((creatureA?.counters || {}).shield).toBe(1);
    expect((creatureB?.counters || {})['+1/+1']).toBe(2);
  });

  it('applies Support keyword lines to chosen other target creatures and excludes the source', () => {
    const ir = parseOracleTextToIR('Support 2', 'Shoulder to Shoulder');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'support-source',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            card: {
              id: 'support-source-card',
              name: 'Support Source',
              type_line: 'Creature - Human Soldier',
            },
          } as any,
          {
            id: 'support-target-a',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            card: {
              id: 'support-target-a-card',
              name: 'Support Target A',
              type_line: 'Creature - Human Soldier',
            },
          } as any,
          {
            id: 'support-target-b',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            card: {
              id: 'support-target-b-card',
              name: 'Support Target B',
              type_line: 'Creature - Human Soldier',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'support-source',
        sourceName: 'Shoulder to Shoulder',
        selectorContext: { chosenObjectIds: ['support-source', 'support-target-a', 'support-target-b'] },
      }
    );
    const source = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'support-source');
    const targetA = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'support-target-a');
    const targetB = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'support-target-b');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['add_counter']);
    expect((source?.counters || {})['+1/+1'] || 0).toBe(0);
    expect((targetA?.counters || {})['+1/+1']).toBe(1);
    expect((targetB?.counters || {})['+1/+1']).toBe(1);
  });

  it('applies Bolster keyword lines to a chosen least-toughness creature when there is a tie', () => {
    const ir = parseOracleTextToIR('Bolster 2', 'Abzan Skycaptain');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'bolster-big',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            power: 3,
            toughness: 3,
            basePower: 3,
            baseToughness: 3,
            card: {
              id: 'bolster-big-card',
              name: 'Stalwart Guard',
              type_line: 'Creature - Human Soldier',
              power: '3',
              toughness: '3',
            },
          } as any,
          {
            id: 'bolster-target-a',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            power: 1,
            toughness: 1,
            basePower: 1,
            baseToughness: 1,
            card: {
              id: 'bolster-target-a-card',
              name: 'Least Toughness A',
              type_line: 'Creature - Human Soldier',
              power: '1',
              toughness: '1',
            },
          } as any,
          {
            id: 'bolster-target-b',
            ownerId: 'p1',
            owner: 'p1',
            controller: 'p1',
            tapped: false,
            counters: {},
            power: 2,
            toughness: 1,
            basePower: 2,
            baseToughness: 1,
            card: {
              id: 'bolster-target-b-card',
              name: 'Least Toughness B',
              type_line: 'Creature - Human Soldier',
              power: '2',
              toughness: '1',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'bolster-source',
        sourceName: 'Abzan Skycaptain',
        targetCreatureId: 'bolster-target-b',
        selectorContext: { chosenObjectIds: ['bolster-target-b'] },
      }
    );
    const targetA = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'bolster-target-a');
    const targetB = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'bolster-target-b');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['add_counter']);
    expect((targetA?.counters || {})['+1/+1'] || 0).toBe(0);
    expect((targetB?.counters || {})['+1/+1']).toBe(2);
  });

  it('applies Morph keyword lines by turning the face-down source permanent face up', () => {
    const ir = parseOracleTextToIR('Morph {3}{G}', 'Barkhide Host');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'morph-perm',
            ownerId: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSick: false,
            counters: {},
            card: { id: 'morph-card', name: 'Face-Down Creature', faceDown: true, type_line: 'Creature' },
            faceUpCard: { id: 'morph-card', name: 'Barkhide Host', type_line: 'Creature - Beast', power: '4', toughness: '4' },
            basePower: 2,
            baseToughness: 2,
            effectiveTypes: ['Creature'],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'morph-perm',
        sourceName: 'Barkhide Host',
        targetPermanentId: 'morph-perm',
      },
      { allowOptional: true }
    );

    const turned = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'morph-perm') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['turn_face_up']);
    expect(turned?.card?.faceDown).not.toBe(true);
    expect(turned?.card?.name).toBe('Barkhide Host');
    expect(String(turned?.card?.type_line || '')).toContain('Creature - Beast');
  });

  it('applies Megamorph keyword lines by turning the source face up and adding a +1/+1 counter', () => {
    const ir = parseOracleTextToIR('Megamorph {4}{G}', 'Barkhide Host');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        battlefield: [
          {
            id: 'megamorph-perm',
            ownerId: 'p1',
            controller: 'p1',
            tapped: false,
            summoningSick: false,
            counters: {},
            card: { id: 'megamorph-card', name: 'Face-Down Creature', faceDown: true, type_line: 'Creature' },
            faceUpCard: { id: 'megamorph-card', name: 'Barkhide Host', type_line: 'Creature - Beast', power: '4', toughness: '4' },
            basePower: 2,
            baseToughness: 2,
            effectiveTypes: ['Creature'],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'megamorph-perm',
        sourceName: 'Barkhide Host',
        targetPermanentId: 'megamorph-perm',
      },
      { allowOptional: true }
    );

    const turned = (result.state.battlefield as any[]).find((perm: any) => perm.id === 'megamorph-perm') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['turn_face_up', 'add_counter']);
    expect(turned?.card?.faceDown).not.toBe(true);
    expect(turned?.counters?.['+1/+1']).toBe(1);
  });

  it('applies Embalm by exiling the graveyard source card and creating a white Zombie token copy', () => {
    const ir = parseOracleTextToIR('Embalm {3}{W}', 'Anointer Priest');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'embalm-source',
                name: 'Anointer Priest',
                type_line: 'Creature - Human Cleric',
                oracle_text: 'Whenever a creature token you control enters, you gain 1 life.',
                mana_cost: '{1}{W}',
                colors: ['W'],
                power: '1',
                toughness: '3',
              },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'embalm-source',
        sourceName: 'Anointer Priest',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find((entry: any) => entry.id === 'p1') as any;
    const token = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.card?.id !== 'embalm-source') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect((player.graveyard || []).map((card: any) => card.id)).not.toContain('embalm-source');
    expect((player.exile || []).map((card: any) => card.id)).toContain('embalm-source');
    expect(token?.isToken).toBe(true);
    expect(token?.card?.name).toBe('Anointer Priest');
    expect(token?.card?.colors).toEqual(['W']);
    expect(String(token?.card?.type_line || '')).toContain('Zombie');
    expect(token?.card?.mana_cost || token?.card?.manaCost || '').toBe('');
    expect(token?.power).toBe(1);
    expect(token?.toughness).toBe(3);
  });

  it('applies Eternalize by exiling the graveyard source card and creating a 4/4 black Zombie token copy', () => {
    const ir = parseOracleTextToIR('Eternalize {2}{B}{B}', 'Champion of Wits');
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'eternalize-source',
                name: 'Champion of Wits',
                type_line: 'Creature - Naga Wizard',
                oracle_text: 'When this creature enters, draw cards, then discard cards.',
                mana_cost: '{2}{U}',
                colors: ['U'],
                power: '2',
                toughness: '1',
              },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'eternalize-source',
        sourceName: 'Champion of Wits',
      },
      { allowOptional: true }
    );

    const player = result.state.players.find((entry: any) => entry.id === 'p1') as any;
    const token = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.card?.id !== 'eternalize-source') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect((player.graveyard || []).map((card: any) => card.id)).not.toContain('eternalize-source');
    expect((player.exile || []).map((card: any) => card.id)).toContain('eternalize-source');
    expect(token?.isToken).toBe(true);
    expect(token?.card?.name).toBe('Champion of Wits');
    expect(token?.card?.colors).toEqual(['B']);
    expect(String(token?.card?.type_line || '')).toContain('Zombie');
    expect(token?.card?.mana_cost || token?.card?.manaCost || '').toBe('');
    expect(token?.power).toBe(4);
    expect(token?.toughness).toBe(4);
  });

  it("redirects Whip of Erebos-style returned creatures to exile instead of owners' hands", () => {
    const ir = parseOracleTextToIR(
      'Return target creature card from your graveyard to the battlefield. It gains haste. Exile it at the beginning of the next end step. If it would leave the battlefield, exile it instead of putting it anywhere else.',
      'Whip of Erebos'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const reanimated = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'ghoul', name: 'Grave Ghoul', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'whip',
        sourceName: 'Whip of Erebos',
        targetCreatureId: 'ghoul',
      },
      { allowOptional: true }
    );

    const returnedPermanent = (reanimated.state.battlefield as any[]).find(
      (perm: any) => perm.card?.id === 'ghoul'
    ) as any;

    const bounced = applyOracleIRStepsToGameState(
      reanimated.state,
      parseOracleTextToIR("Return target creature to its owner's hand.", 'Test Bounce').abilities[0]?.steps ?? [],
      {
        controllerId: 'p1',
        targetCreatureId: returnedPermanent.id,
      },
      { allowOptional: true }
    );

    expect((bounced.state.players[0] as any).hand).toEqual([]);
    expect(((bounced.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('ghoul');
  });

  it('applies Kheru Lich Lord-style random graveyard return and preserves exile-on-leave replacement', () => {
    const ir = parseOracleTextToIR(
      'Return a creature card at random from your graveyard to the battlefield. If it would leave the battlefield, exile it instead of putting it anywhere else.',
      'Kheru Lich Lord'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const reanimated = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'ghoul', name: 'Grave Ghoul', type_line: 'Creature - Zombie' },
              { id: 'ritual', name: 'Dark Ritual', type_line: 'Instant' },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'kheru',
        sourceName: 'Kheru Lich Lord',
      },
      { allowOptional: true }
    );

    const returnedPermanent = (reanimated.state.battlefield as any[]).find(
      (perm: any) => perm.card?.id === 'ghoul'
    ) as any;
    expect(returnedPermanent).toBeTruthy();
    expect(returnedPermanent?.leaveBattlefieldReplacement).toBe('exile');
    expect(((reanimated.state.players[0] as any).graveyard || []).map((card: any) => card.id)).toEqual(['ritual']);

    const destroyed = applyOracleIRStepsToGameState(
      reanimated.state,
      [
        {
          kind: 'destroy',
          target: { kind: 'raw', text: 'that creature' },
          raw: 'Destroy that creature.',
        } as any,
      ],
      {
        controllerId: 'p1',
        targetCreatureId: returnedPermanent.id,
      },
      { allowOptional: true }
    );

    expect((destroyed.state.players[0] as any).graveyard).toEqual([{ id: 'ritual', name: 'Dark Ritual', type_line: 'Instant' }]);
    expect(((destroyed.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('ghoul');
  });

  it('redirects Moira and Teshar-style returned permanents to exile when they leave the battlefield', () => {
    const ir = parseOracleTextToIR(
      'Whenever you cast a historic spell, return target nonland permanent card from your graveyard to the battlefield. It gains haste. Exile it at the beginning of the next end step. If it would leave the battlefield, exile it instead of putting it anywhere else.',
      'Moira and Teshar'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const reanimated = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'relic', name: 'Lost Relic', type_line: 'Artifact' }],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'moira',
        sourceName: 'Moira and Teshar',
        targetPermanentId: 'relic',
      },
      { allowOptional: true }
    );

    const returnedPermanent = (reanimated.state.battlefield as any[]).find(
      (perm: any) => perm.card?.id === 'relic'
    ) as any;
    expect(returnedPermanent?.leaveBattlefieldReplacement).toBe('exile');

    const bounced = applyOracleIRStepsToGameState(
      reanimated.state,
      parseOracleTextToIR("Return target artifact to its owner's hand.", 'Test Bounce').abilities[0]?.steps ?? [],
      {
        controllerId: 'p1',
        targetPermanentId: returnedPermanent.id,
      },
      { allowOptional: true }
    );

    expect((bounced.state.players[0] as any).hand).toEqual([]);
    expect(((bounced.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('relic');
  });

  it("applies Desdemona, Freedom's Edge by granting this-turn graveyard permission to a qualified target", () => {
    const ir = parseOracleTextToIR(
      "Whenever Desdemona attacks, target creature card in your graveyard that's an artifact or that has mana value 3 or less gains escape until end of turn. The escape cost is equal to its mana cost plus exile two other cards from your graveyard. (You may cast it from your graveyard for its escape cost this turn.)",
      "Desdemona, Freedom's Edge"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 23,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              { id: 'gearhulk', name: 'Scrap Gearhulk', type_line: 'Artifact Creature - Construct', mana_value: 6 },
              { id: 'giant', name: 'Hill Giant', type_line: 'Creature - Giant', mana_value: 4 },
            ],
            exile: [],
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: "Desdemona, Freedom's Edge",
        selectorContext: { chosenObjectIds: ['gearhulk'] },
      },
      { allowOptional: true }
    );

    expect((result.state as any).playableFromGraveyard?.p1?.gearhulk).toBe(23);
    expect((result.state as any).playableFromGraveyard?.p1?.giant).toBeUndefined();
    const desdemonaGraveyard = (((result.state.players || [])[0] as any)?.graveyard || []) as any[];
    expect(desdemonaGraveyard.find(card => card.id === 'gearhulk')?.graveyardAdditionalCost).toEqual({
      kind: 'exile_from_graveyard',
      count: 2,
      raw: 'exile two other cards from your graveyard',
    });
  });

  it('applies Gravecrawler only while you control a Zombie', () => {
    const ir = parseOracleTextToIR(
      "This creature can't block.\nYou may cast this card from your graveyard as long as you control a Zombie.",
      'Gravecrawler'
    );
    const steps = ir.abilities[1]?.steps ?? [];

    const enabled = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 24,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'crawler', name: 'Gravecrawler', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'ally-zombie',
            controller: 'p1',
            owner: 'p1',
            card: { id: 'ally-zombie-card', name: 'Walking Corpse', type_line: 'Creature - Zombie' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'crawler',
        sourceName: 'Gravecrawler',
      },
      { allowOptional: true }
    );

    expect((enabled.state as any).playableFromGraveyard?.p1?.crawler).toBe(24);

    const disabled = applyOracleIRStepsToGameState(
      makeState({
        turnNumber: 24,
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'crawler', name: 'Gravecrawler', type_line: 'Creature - Zombie' }],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'ally-goblin',
            controller: 'p1',
            owner: 'p1',
            card: { id: 'ally-goblin-card', name: 'Goblin Piker', type_line: 'Creature - Goblin' },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'crawler',
        sourceName: 'Gravecrawler',
      },
      { allowOptional: true }
    );

    expect((disabled.state as any).playableFromGraveyard?.p1?.crawler).toBeUndefined();
  });

  it('exiles Personal Decoy when it would leave the battlefield', () => {
    const destroyed = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'decoy',
            controller: 'p1',
            owner: 'p1',
            counters: { loyalty: 40 },
            card: {
              id: 'decoy-card',
              name: 'Personal Decoy',
              type_line: 'Planeswalker - Duck',
              oracle_text:
                "Personal Decoy enters with a number of loyalty counters on it equal to your life total. If it would leave the battlefield, exile it instead of putting it anywhere else. You can't be attacked.",
            },
          },
        ],
      } as any),
      [
        {
          kind: 'destroy',
          target: { kind: 'raw', text: 'that permanent' },
          raw: 'Destroy that permanent.',
        } as any,
      ],
      {
        controllerId: 'p1',
        targetPermanentId: 'decoy',
      },
      { allowOptional: true }
    );

    expect(destroyed.state.battlefield).toEqual([]);
    expect((destroyed.state.players[0] as any).graveyard).toEqual([]);
    expect(((destroyed.state.players[0] as any).exile || []).map((card: any) => card.id)).toContain('decoy-card');
  });

  it('applies Mardu Woe-Reaper graveyard exile plus conditional life gain', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature or another Warrior you control enters, you may exile target creature card from a graveyard. If you do, you gain 1 life.',
      'Mardu Woe-Reaper'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'woe-target', name: 'Savannah Lions', type_line: 'Creature - Cat' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Mardu Woe-Reaper',
        targetPermanentId: 'woe-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'gain_life']);
    expect(p1.life).toBe(41);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('woe-target');
  });

  it('applies Diregraf Scavenger creature-card exile follow-up life gain', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, exile up to one target card from a graveyard. If a creature card was exiled this way, you gain 2 life. If a land card was exiled this way, add {G}.',
      'Diregraf Scavenger'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'diregraf-target', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Diregraf Scavenger',
      targetPermanentId: 'diregraf-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'gain_life']);
    expect(p1.life).toBe(42);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('diregraf-target');
  });

  it('applies Corpse Appraiser by exiling the target, drawing the top card, and milling the rest', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, exile up to one target creature card from a graveyard. If a card is put into exile this way, look at the top three cards of your library, then put one of those cards into your hand and the rest into your graveyard.',
      'Corpse Appraiser'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'corpse-top-1', name: 'Consider', type_line: 'Instant' },
            { id: 'corpse-top-2', name: 'Island', type_line: 'Basic Land - Island' },
            { id: 'corpse-top-3', name: 'Go for the Throat', type_line: 'Instant' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'corpse-target', name: 'Grizzly Bears', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Corpse Appraiser',
      targetPermanentId: 'corpse-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'draw', 'mill']);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('corpse-target');
    expect((p1.hand || []).map((entry: any) => entry.id)).toEqual(['corpse-top-1']);
    expect((p1.graveyard || []).map((entry: any) => entry.id)).toEqual(['corpse-top-2', 'corpse-top-3']);
    expect((p1.library || []).map((entry: any) => entry.id)).toEqual([]);
  });

  it('applies Deathgorge Scavenger noncreature exile follow-up self pump', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters or attacks, you may exile target card from a graveyard. If a creature card was exiled this way, you gain 2 life. If a noncreature card was exiled this way, this creature gets +1/+1 until end of turn.',
      'Deathgorge Scavenger'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'deathgorge-target', name: 'Cultivate', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'deathgorge-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Deathgorge Scavenger',
          cardType: 'Creature',
          type_line: 'Creature - Dinosaur',
          power: 3,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'deathgorge-source',
        sourceName: 'Deathgorge Scavenger',
        targetPermanentId: 'deathgorge-target',
      },
      { allowOptional: true }
    );

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'deathgorge-source') as any;
    const modifier = (Array.isArray(source?.modifiers) ? source.modifiers : []).find((entry: any) => entry?.type === 'powerToughness');
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'modify_pt']);
    expect(modifier).toBeTruthy();
    expect(modifier.power).toBe(1);
    expect(modifier.toughness).toBe(1);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('deathgorge-target');
  });

  it('applies Armored Scrapgorger graveyard exile plus oil counter in one clause', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature becomes tapped, exile target card from a graveyard and put an oil counter on this creature.',
      'Armored Scrapgorger'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'scrapgorger-target', name: 'Cultivate', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'scrapgorger-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Armored Scrapgorger',
          cardType: 'Creature',
          type_line: 'Creature - Phyrexian Beast',
          power: 0,
          toughness: 3,
          tapped: true,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'scrapgorger-source',
      sourceName: 'Armored Scrapgorger',
      targetPermanentId: 'scrapgorger-target',
    });

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'scrapgorger-source') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect(source.counters?.oil).toBe(1);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('scrapgorger-target');
  });

  it('applies Keen-Eyed Curator creature-card exile follow-up counter gain', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. If a creature card was exiled this way, put a +1/+1 counter on this creature.',
      'Keen-Eyed Curator'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'curator-target', name: 'Elvish Mystic', type_line: 'Creature - Elf Druid' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'curator-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Keen-Eyed Curator',
          cardType: 'Creature',
          type_line: 'Creature - Beast',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'curator-source',
      sourceName: 'Keen-Eyed Curator',
      targetPermanentId: 'curator-target',
    });

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'curator-source') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect(source.counters?.['+1/+1']).toBe(1);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('curator-target');
  });

  it('applies Scavenge by exiling the graveyard source card and adding counters equal to its power', () => {
    const ir = parseOracleTextToIR('Scavenge {4}{G}{G}', 'Deadbridge Goliath');
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'deadbridge-goliath',
              name: 'Deadbridge Goliath',
              type_line: 'Creature - Insect',
              power: '5',
              toughness: '5',
            },
          ],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'scavenge-target',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Runeclaw Bear',
          cardType: 'Creature',
          type_line: 'Creature - Bear',
          power: 2,
          toughness: 2,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'deadbridge-goliath',
      sourceName: 'Deadbridge Goliath',
      targetCreatureId: 'scavenge-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const target = (result.state.battlefield || []).find((perm: any) => perm.id === 'scavenge-target') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect((p1.graveyard || []).some((card: any) => card.id === 'deadbridge-goliath')).toBe(false);
    expect((p1.exile || []).some((card: any) => card.id === 'deadbridge-goliath')).toBe(true);
    expect(target.counters?.['+1/+1']).toBe(5);
  });

  it('applies Conversion Chamber exile plus charge counter follow-up', () => {
    const ir = parseOracleTextToIR(
      'Exile target artifact card from a graveyard. Put a charge counter on this artifact.',
      'Conversion Chamber'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'chamber-target', name: 'Mind Stone', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'chamber-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Conversion Chamber',
          cardType: 'Artifact',
          type_line: 'Artifact',
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'chamber-source',
      sourceName: 'Conversion Chamber',
      targetPermanentId: 'chamber-target',
    });

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'chamber-source') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect(source.counters?.charge).toBe(1);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('chamber-target');
  });

  it('applies Lara Croft by exiling the target and adding a discovery counter to the exiled card', () => {
    const ir = parseOracleTextToIR(
      'Whenever Lara Croft attacks, exile up to one target legendary artifact card or legendary land card from a graveyard and put a discovery counter on it.',
      'Lara Croft, Tomb Raider'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'lara-target', name: 'Mox Amber', type_line: 'Legendary Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'lara-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Lara Croft, Tomb Raider',
          cardType: 'Legendary Creature',
          type_line: 'Legendary Creature - Human Raider',
          power: 3,
          toughness: 4,
          tapped: false,
          summoningSick: false,
          counters: {},
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'lara-source',
      sourceName: 'Lara Croft, Tomb Raider',
      targetPermanentId: 'lara-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const exiled = (p2.exile || []).find((entry: any) => entry.id === 'lara-target') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect(p2.graveyard).toHaveLength(0);
    expect(exiled).toBeTruthy();
    expect(exiled.counters?.discovery).toBe(1);
  });

  it('applies Raven Eagle typed exiled-this-way token creation follow-up', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters or attacks, exile up to one target card from a graveyard. If a creature card was exiled this way, create a Clue token.',
      'Raven Eagle'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'raven-target', name: 'Sakura-Tribe Elder', type_line: 'Creature - Snake Shaman' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Raven Eagle',
      targetPermanentId: 'raven-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('raven-target');
    expect(result.state.battlefield).toHaveLength(1);
  });

  it('applies Restless Cottage create-token-plus-graveyard-exile in order', () => {
    const ir = parseOracleTextToIR(
      'Whenever this land attacks, create a Food token and exile up to one target card from a graveyard.',
      'Restless Cottage'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'cottage-target', name: 'Faithless Looting', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Restless Cottage',
      targetPermanentId: 'cottage-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['create_token', 'move_zone']);
    expect(result.state.battlefield).toHaveLength(1);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('cottage-target');
  });

  it('applies Lazav, Wearer of Faces exile-then-investigate follow-up', () => {
    const ir = parseOracleTextToIR(
      'Whenever Lazav attacks, exile target card from a graveyard, then investigate.',
      'Lazav, Wearer of Faces'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'lazav-target', name: 'Opt', type_line: 'Instant' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Lazav, Wearer of Faces',
      targetPermanentId: 'lazav-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'investigate']);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('lazav-target');
    expect((result.state.battlefield || []).some((perm: any) => perm.card?.name === 'Clue')).toBe(true);
  });

  it('applies Mastermind Plum artifact-or-land exile follow-up Treasure creation', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature attacks, exile up to one target card from a graveyard. If it was an artifact or land card, create a Treasure token.',
      'Mastermind Plum'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'plum-target', name: 'Darksteel Ingot', type_line: 'Artifact' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Mastermind Plum',
      targetPermanentId: 'plum-target',
    });

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect((result.state.battlefield || []).some((perm: any) => perm.card?.name === 'Treasure')).toBe(true);
  });

  it('applies Misfortune Teller noncreature-nonland exile follow-up life gain', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature enters or deals combat damage to a player, exile target card from a graveyard. If it was a creature card, create a 2/2 black Zombie creature token. If it was a land card, create a Treasure token. If it was a noncreature, nonland card, you gain 3 life.',
      'Misfortune Teller'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'teller-target', name: 'Cultivate', type_line: 'Sorcery' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Misfortune Teller',
      targetPermanentId: 'teller-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'gain_life']);
    expect(p1.life).toBe(43);
  });

  it('applies Klothys land exile follow-up by adding the first listed mana option', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. If it was a land card, add {R} or {G}. Otherwise, you gain 2 life and this permanent deals 2 damage to each opponent.',
      'Klothys, God of Destiny'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'klothys-land', name: 'Forest', type_line: 'Basic Land - Forest' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'klothys-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Klothys, God of Destiny',
          cardType: 'Legendary Enchantment Creature - God',
          type_line: 'Legendary Enchantment Creature - God',
          power: 4,
          toughness: 5,
          tapped: false,
          summoningSick: false,
          counters: {},
          damage: 0,
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'klothys-source',
      sourceName: 'Klothys, God of Destiny',
      targetPermanentId: 'klothys-land',
    });

    const manaPool = (result.state as any).manaPool?.p1;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_mana']);
    expect(manaPool?.red).toBe(1);
    expect(manaPool?.green ?? 0).toBe(0);
  });

  it('applies Selesnya Eulogist exile-then-populate when exactly one creature token is available', () => {
    const ir = parseOracleTextToIR(
      'Exile target creature card from a graveyard, then populate.',
      'Selesnya Eulogist'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'eulogist-target', name: 'Runeclaw Bear', type_line: 'Creature - Bear' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'soldier-token',
          controller: 'p1',
          owner: 'p1',
          ownerId: 'p1',
          tapped: false,
          counters: {},
          attachments: [],
          modifiers: [],
          summoningSickness: false,
          isToken: true,
          basePower: 1,
          baseToughness: 1,
          card: {
            id: 'soldier-token',
            name: 'Soldier',
            type_line: 'Token Creature - Soldier',
            oracle_text: '',
            power: '1',
            toughness: '1',
            colors: ['W'],
            mana_cost: '',
            cmc: 0,
            image_uris: {},
          },
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Selesnya Eulogist',
      targetPermanentId: 'eulogist-target',
    });

    const tokenCount = (result.state.battlefield || []).filter((perm: any) => perm.isToken).length;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'populate']);
    expect(tokenCount).toBe(2);
  });

  it('applies Selfless Exorcist exile plus moved-card power damage follow-up', () => {
    const ir = parseOracleTextToIR(
      '{T}: Exile target creature card from a graveyard. That card deals damage equal to its power to this creature.',
      'Selfless Exorcist'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'exorcist-target', name: 'Hill Giant', type_line: 'Creature - Giant', power: '3', toughness: '3' }],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'exorcist-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Selfless Exorcist',
          cardType: 'Creature',
          type_line: 'Creature - Human Cleric',
          power: 3,
          toughness: 4,
          tapped: true,
          summoningSick: false,
          counters: {},
          damage: 0,
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'exorcist-source',
      sourceName: 'Selfless Exorcist',
      targetPermanentId: 'exorcist-target',
    });

    const source = (result.state.battlefield || []).find((perm: any) => perm.id === 'exorcist-source') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'deal_damage']);
    expect(source.damage).toBe(3);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('exorcist-target');
  });

  it('applies Morbid Bloom by creating tokens from the exiled card toughness', () => {
    const ir = parseOracleTextToIR(
      "Exile target creature card from a graveyard, then create X 1/1 green Saproling creature tokens, where X is the exiled card's toughness.",
      'Morbid Bloom'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'morbid-target', name: 'Yargle', type_line: 'Legendary Creature - Frog Spirit', power: '9', toughness: '3' }],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Morbid Bloom',
      targetPermanentId: 'morbid-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const saprolings = (result.state.battlefield || []).filter((perm: any) => perm.card?.name === 'Saproling');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect(p2.graveyard).toHaveLength(0);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('morbid-target');
    expect(saprolings).toHaveLength(3);
  });

  it('applies Sequence Engine by creating Zombies from the exiled card mana value and putting counters on them', () => {
    const ir = parseOracleTextToIR(
      "Exile target creature card from a graveyard. Create X 2/2 black Zombie creature tokens, where X is that card's mana value. Put X +1/+1 counters on each of those Zombies.",
      'Sequence Engine'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'sequence-target',
              name: 'Centaur Courser',
              type_line: 'Creature - Centaur Warrior',
              mana_cost: '{1}{G}{G}',
              cmc: 3,
              power: '3',
              toughness: '3',
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Sequence Engine',
      targetPermanentId: 'sequence-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const zombies = (result.state.battlefield || []).filter((perm: any) => perm.card?.name === 'Zombie');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token', 'add_counter']);
    expect((p2.graveyard || []).map((entry: any) => entry.id)).toEqual([]);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('sequence-target');
    expect(zombies).toHaveLength(3);
    for (const zombie of zombies) {
      expect(zombie.counters?.['+1/+1']).toBe(3);
    }
  });

  it('applies Lara Croft, Tomb Raider by exiling the target and putting a discovery counter on the exiled card', () => {
    const ir = parseOracleTextToIR(
      'Whenever Lara Croft attacks, exile up to one target legendary artifact card or legendary land card from a graveyard and put a discovery counter on it.',
      'Lara Croft, Tomb Raider'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'lara-target',
              name: 'Mox Amber',
              type_line: 'Legendary Artifact',
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Lara Croft, Tomb Raider',
      targetPermanentId: 'lara-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const exiled = (p2.exile || []).find((entry: any) => entry.id === 'lara-target');

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect((p2.graveyard || []).map((entry: any) => entry.id)).toEqual([]);
    expect(exiled?.counters?.discovery).toBe(1);
  });

  it('applies The Animus only to a graveyard card with a memory counter on it', () => {
    const ir = parseOracleTextToIR(
      'At the beginning of your end step, exile up to one target legendary creature card from a graveyard with a memory counter on it.',
      'The Animus'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'animus-target',
              name: 'Isamaru, Hound of Konda',
              type_line: 'Legendary Creature - Dog',
              counters: { memory: 1 },
            },
            {
              id: 'animus-miss',
              name: 'Yoshimaru, Ever Faithful',
              type_line: 'Legendary Creature - Dog',
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'The Animus',
      targetPermanentId: 'animus-target',
    });

    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('animus-target');
    expect((p2.graveyard || []).map((entry: any) => entry.id)).toContain('animus-miss');
  });

  it('applies deterministic look-select-top by moving the first looked card to hand and the rest to graveyard', () => {
    const ir = parseOracleTextToIR(
      'When this creature dies, look at the top three cards of your library. Put one of them into your hand and the rest into your graveyard.',
      'Testament Bearer'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'top-1', name: 'Village Rites', type_line: 'Instant' },
            { id: 'top-2', name: 'Swamp', type_line: 'Basic Land - Swamp' },
            { id: 'top-3', name: 'Grim Haruspex', type_line: 'Creature - Human Wizard' },
            { id: 'top-4', name: 'Blood Artist', type_line: 'Creature - Vampire' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Testament Bearer',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['look_select_top']);
    expect((p1.hand || []).map((entry: any) => entry.id)).toEqual(['top-1']);
    expect((p1.graveyard || []).map((entry: any) => entry.id)).toEqual(['top-2', 'top-3']);
    expect((p1.library || []).map((entry: any) => entry.id)).toEqual(['top-4']);
  });

  it('applies Corpse Appraiser by gating the look-select-top follow-up on a successful graveyard exile', () => {
    const ir = parseOracleTextToIR(
      'When this creature enters, exile up to one target creature card from a graveyard. If a card is put into exile this way, look at the top three cards of your library, then put one of those cards into your hand and the rest into your graveyard.',
      'Corpse Appraiser'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [
            { id: 'corpse-top-1', name: 'Go for the Throat', type_line: 'Instant' },
            { id: 'corpse-top-2', name: 'Island', type_line: 'Basic Land - Island' },
            { id: 'corpse-top-3', name: 'Corpse Appraiser', type_line: 'Creature - Vampire Wizard' },
            { id: 'corpse-top-4', name: 'Mountain', type_line: 'Basic Land - Mountain' },
          ],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            { id: 'corpse-target', name: 'Silvercoat Lion', type_line: 'Creature - Cat', power: '2', toughness: '2' },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Corpse Appraiser',
      targetPermanentId: 'corpse-target',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'draw', 'mill']);
    expect((p2.exile || []).map((entry: any) => entry.id)).toEqual(['corpse-target']);
    expect((p1.hand || []).map((entry: any) => entry.id)).toEqual(['corpse-top-1']);
    expect((p1.graveyard || []).map((entry: any) => entry.id)).toEqual(['corpse-top-2', 'corpse-top-3']);
    expect((p1.library || []).map((entry: any) => entry.id)).toEqual(['corpse-top-4']);
  });

  it('applies Abyssal Harvester only to a creature card put into a graveyard this turn', () => {
    const ir = parseOracleTextToIR(
      '{T}: Exile target creature card from a graveyard that was put there this turn.',
      'Abyssal Harvester'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      turnNumber: 7,
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'abyssal-old',
              name: 'Silvercoat Lion',
              type_line: 'Creature - Cat',
              putIntoGraveyardTurn: 6,
            },
            {
              id: 'abyssal-fresh',
              name: 'Runeclaw Bear',
              type_line: 'Creature - Bear',
              putIntoGraveyardTurn: 7,
            },
          ],
          exile: [],
        } as any,
      ],
    });

    const staleResult = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Abyssal Harvester',
      targetPermanentId: 'abyssal-old',
    });
    expect(staleResult.appliedSteps).toHaveLength(0);
    expect(((staleResult.state.players[1] as any).graveyard || []).map((entry: any) => entry.id)).toEqual([
      'abyssal-old',
      'abyssal-fresh',
    ]);
    expect((staleResult.state.players[1] as any).exile || []).toEqual([]);

    const freshResult = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceName: 'Abyssal Harvester',
      targetPermanentId: 'abyssal-fresh',
    });
    expect(freshResult.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect(((freshResult.state.players[1] as any).graveyard || []).map((entry: any) => entry.id)).toEqual(['abyssal-old']);
    expect(((freshResult.state.players[1] as any).exile || []).map((entry: any) => entry.id)).toEqual(['abyssal-fresh']);
  });

  it('applies Urborg Scavengers by exiling the target, adding a counter, and projecting linked keyword abilities', () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature enters or attacks, exile target card from a graveyard. Put a +1/+1 counter on this creature.\nThis creature has flying as long as a card exiled with it has flying. The same is true for first strike, double strike, deathtouch, haste, hexproof, indestructible, lifelink, menace, reach, trample, and vigilance.',
      'Urborg Scavengers'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [
            {
              id: 'urborg-target',
              name: 'Aerial Baloth',
              type_line: 'Creature - Beast',
              oracle_text: 'Flying, trample',
              keywords: ['Flying', 'Trample'],
            },
          ],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'urborg-source',
          owner: 'p1',
          controller: 'p1',
          tapped: false,
          summoningSickness: false,
          counters: {},
          attachments: [],
          modifiers: [],
          card: {
            id: 'urborg-source-card',
            name: 'Urborg Scavengers',
            type_line: 'Creature - Spirit',
            oracle_text:
              'Whenever this creature enters or attacks, exile target card from a graveyard. Put a +1/+1 counter on this creature.\nThis creature has flying as long as a card exiled with it has flying. The same is true for first strike, double strike, deathtouch, haste, hexproof, indestructible, lifelink, menace, reach, trample, and vigilance.',
          },
        } as any,
      ],
    } as any);

    const result = applyOracleIRStepsToGameState(
      start,
      steps,
      {
        controllerId: 'p1',
        sourceId: 'urborg-source',
        sourceName: 'Urborg Scavengers',
        targetPermanentId: 'urborg-target',
      },
      { allowOptional: true }
    );

    const source = ((result.state.battlefield || []) as any[]).find((perm: any) => perm.id === 'urborg-source') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_counter']);
    expect(source?.counters).toEqual({ '+1/+1': 1 });
    expect(source?.grantedAbilities || []).toEqual(expect.arrayContaining(['flying', 'trample']));
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['urborg-target']);
    expect((p2.graveyard || []).map((card: any) => card.id)).toEqual([]);
  });

  it('applies Klothys land branch with an explicit mana choice', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. If it was a land card, add {R} or {G}. Otherwise, you gain 2 life and this permanent deals 2 damage to each opponent.',
      'Klothys, God of Destiny'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'klothys-land', name: 'Forest', type_line: 'Basic Land - Forest' }],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'klothys-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Klothys, God of Destiny',
          cardType: 'Legendary Enchantment Creature - God',
          type_line: 'Legendary Enchantment Creature - God',
          power: 4,
          toughness: 5,
          tapped: false,
          summoningSick: false,
          counters: {},
          damage: 0,
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'klothys-source',
      sourceName: 'Klothys, God of Destiny',
      targetPermanentId: 'klothys-land',
      selectorContext: {
        chosenMana: '{G}',
      },
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'add_mana']);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('klothys-land');
    expect((result.state as any).manaPool?.p1?.green).toBe(1);
    expect(p1.life).toBe(40);
    expect(p2.life).toBe(40);
  });

  it('applies Klothys otherwise branch for a nonland exiled card', () => {
    const ir = parseOracleTextToIR(
      'Exile target card from a graveyard. If it was a land card, add {R} or {G}. Otherwise, you gain 2 life and this permanent deals 2 damage to each opponent.',
      'Klothys, God of Destiny'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const start = makeState({
      players: [
        {
          id: 'p1',
          name: 'P1',
          seat: 0,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
        {
          id: 'p2',
          name: 'P2',
          seat: 1,
          life: 40,
          library: [],
          hand: [],
          graveyard: [{ id: 'klothys-spell', name: 'Shock', type_line: 'Instant' }],
          exile: [],
        } as any,
        {
          id: 'p3',
          name: 'P3',
          seat: 2,
          life: 40,
          library: [],
          hand: [],
          graveyard: [],
          exile: [],
        } as any,
      ],
      battlefield: [
        {
          id: 'klothys-source',
          ownerId: 'p1',
          controller: 'p1',
          name: 'Klothys, God of Destiny',
          cardType: 'Legendary Enchantment Creature - God',
          type_line: 'Legendary Enchantment Creature - God',
          power: 4,
          toughness: 5,
          tapped: false,
          summoningSick: false,
          counters: {},
          damage: 0,
        } as any,
      ],
    });

    const result = applyOracleIRStepsToGameState(start, steps, {
      controllerId: 'p1',
      sourceId: 'klothys-source',
      sourceName: 'Klothys, God of Destiny',
      targetPermanentId: 'klothys-spell',
    });

    const p1 = result.state.players.find(p => p.id === 'p1') as any;
    const p2 = result.state.players.find(p => p.id === 'p2') as any;
    const p3 = result.state.players.find(p => p.id === 'p3') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'gain_life', 'deal_damage']);
    expect((p2.exile || []).map((entry: any) => entry.id)).toContain('klothys-spell');
    expect(p1.life).toBe(42);
    expect(p2.life).toBe(38);
    expect(p3.life).toBe(38);
  });

  it("applies Narset, Enlightened Exile by exiling and copying a noncreature nonland card with mana value less than its power", () => {
    const ir = parseOracleTextToIR(
      "Whenever Narset attacks, exile target noncreature, nonland card with mana value less than Narset's power from a graveyard and copy it. You may cast the copy without paying its mana cost.",
      'Narset, Enlightened Exile'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [
              { id: 'narset-draw-1', name: 'Island', type_line: 'Basic Land - Island' },
              { id: 'narset-draw-2', name: 'Mountain', type_line: 'Basic Land - Mountain' },
            ],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [
              {
                id: 'narset-target',
                name: 'Chart a Course',
                type_line: 'Sorcery',
                mana_cost: '{1}{U}',
                oracle_text: 'Draw two cards.',
              },
            ],
            exile: [],
          } as any,
        ],
        battlefield: [
          {
            id: 'narset-source',
            controller: 'p1',
            owner: 'p1',
            tapped: false,
            summoningSick: false,
            card: {
              id: 'narset-card',
              name: 'Narset, Enlightened Exile',
              type_line: 'Legendary Creature - Human Monk',
              oracle_text:
                "Whenever Narset attacks, exile target noncreature, nonland card with mana value less than Narset's power from a graveyard and copy it. You may cast the copy without paying its mana cost.",
              power: '3',
              toughness: '4',
            },
          } as any,
        ],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceId: 'narset-source',
        sourceName: 'Narset, Enlightened Exile',
        targetPermanentId: 'narset-target',
      },
      { allowOptional: true }
    );

    const p1 = result.state.players.find((player: any) => player.id === 'p1') as any;
    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'copy_spell']);
    expect((p2.exile || []).map((card: any) => card.id)).toEqual(['narset-target']);
    expect((p1.hand || []).map((card: any) => card.id)).toEqual(['narset-draw-1', 'narset-draw-2']);
  });

  it("applies Kenrith, the Returned King by returning the target under its owner's control", () => {
    const ir = parseOracleTextToIR(
      "{4}{B}: Put target creature card from a graveyard onto the battlefield under its owner's control.",
      'Kenrith, the Returned King'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'kenrith-target', name: 'Grizzly Bears', type_line: 'Creature - Bear', power: '2', toughness: '2' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Kenrith, the Returned King',
        targetPermanentId: 'kenrith-target',
      }
    );

    const returned = (result.state.battlefield || []).find((perm: any) => perm.card?.name === 'Grizzly Bears') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect(returned?.controller).toBe('p2');
    expect(returned?.ownerId || returned?.owner).toBe('p2');
  });

  it("applies Vampire Charmseeker by returning an instant, sorcery, or creature card to its owner's hand", () => {
    const ir = parseOracleTextToIR(
      "When this creature enters, return target instant, sorcery, or creature card from a graveyard to its owner's hand.",
      'Vampire Charmseeker'
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
          {
            id: 'p2',
            name: 'P2',
            seat: 1,
            life: 40,
            library: [],
            hand: [],
            graveyard: [{ id: 'charmseeker-target', name: 'Lightning Bolt', type_line: 'Instant' }],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: 'Vampire Charmseeker',
        targetPermanentId: 'charmseeker-target',
      }
    );

    const p2 = result.state.players.find((player: any) => player.id === 'p2') as any;
    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone']);
    expect((p2.hand || []).map((card: any) => card.id)).toEqual(['charmseeker-target']);
    expect((p2.graveyard || []).map((card: any) => card.id)).toEqual([]);
  });

  it("applies Hazel's Brewmaster by creating a Food token even when no graveyard target is chosen", () => {
    const ir = parseOracleTextToIR(
      'Whenever this creature enters or attacks, exile up to one target card from a graveyard and create a Food token.',
      "Hazel's Brewmaster"
    );
    const steps = ir.abilities[0]?.steps ?? [];

    const result = applyOracleIRStepsToGameState(
      makeState({
        players: [
          {
            id: 'p1',
            name: 'P1',
            seat: 0,
            life: 40,
            library: [],
            hand: [],
            graveyard: [],
            exile: [],
          } as any,
        ],
        battlefield: [],
      } as any),
      steps,
      {
        controllerId: 'p1',
        sourceName: "Hazel's Brewmaster",
      }
    );

    expect(result.appliedSteps.map(step => step.kind)).toEqual(['move_zone', 'create_token']);
    expect((result.state.battlefield || []).some((perm: any) => perm.card?.name === 'Food')).toBe(true);
  });

});
