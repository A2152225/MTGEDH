{
  "name": "@mtgedh/rules-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'Rules engine - no build needed (TypeScript source only)'",
    "test": "vitest run",
    "dev:test": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "simulate": "tsx cli/simulate.ts",
    "keywords:status": "tsx cli/generate-keyword-status-table.ts"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.0.14"
  },
  "dependencies": {
    "vite": "7.2.4"
  }
}
