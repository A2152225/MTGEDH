# Oracle Automation Roadmap

Last updated: 2026-03-25
Owner: Copilot + project maintainers
Scope: rules-engine Oracle text intent parsing/execution reliability and coverage

## Objective

Increase deterministic Oracle-text automation so the engine executes more card intent correctly with minimal skips, while refusing unsafe/ambiguous actions.

- 2026-03-25: Closed `Summoner's Sending`, `Shade's Form`, and `Molten Firebird` by tightening shared follow-up parsing instead of baking in card-specific exceptions. `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` now prefers freshly created token targets before moved-card fallbacks so `Summoner's Sending` can put mana-value counters on the new Zombie, `rules-engine/src/oracleTextParser.ts` now ignores quoted granted activated text like `Enchanted creature has "{B}: ..."` when deciding whether a card itself has an activated ability so `Shade's Form` no longer misclassifies that residue, `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now model `skip_next_draw_step`, and the existing `rules-engine/src/oracleIRExecutorTurnStepHandlers.ts` plus `rules-engine/src/phaseTransitionTriggers.ts` corridor now records and honors one-shot next-draw-step skips while preserving the delayed-return path for `Molten Firebird`. Added focused parser/executor/phase-transition regressions in `rules-engine/test/oracleTextParser.test.ts`, `rules-engine/test/oracleIRParser.test.ts`, `rules-engine/test/oracleIRExecutor.test.ts`, and `rules-engine/test/phaseTransitionTriggers.test.ts`, removed all three cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleTextParser.test.ts -t "Shade's Form|hasActivatedAbility|parseActivatedAbility"`, `npx vitest run test/oracleIRParser.test.ts -t "Summoner's Sending|Shade's Form|Molten Firebird"`, `npx vitest run test/oracleIRExecutor.test.ts -t "Summoner's Sending|Mirror Golem|Mourner's Shield|Psionic Ritual|Molten Firebird|Liesa, Forgotten Archangel|Rienne, Angel of Rebirth"`, and `npx vitest run test/phaseTransitionTriggers.test.ts -t "skip draw|getStepEntryTriggers|collectDrawStepTriggers"`.
- 2026-03-25: Closed `Vincent's Limit Break` by extending the shared modal/dies-return corridor instead of hardcoding the card. `rules-engine/src/oracleTextParserPreprocess.ts` now keeps `Tiered (...)` headers attached to the following effect text, `rules-engine/src/oracleIRParserChooseMode.ts` now lifts tiered bullet blocks into a real `choose_mode` step with per-mode base-P/T substitution, `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserModifyPtClauses.ts` now model temporary `set_base_pt`, and `rules-engine/src/oracleIRExecutorModifyPtStepHandlers.ts` plus `rules-engine/src/staticAbilitiesEvaluation.ts` now apply and evaluate temporary base power/toughness setters alongside the existing temporary dies-trigger flow. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed the card from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck`, `npx vitest run test/oracleIRParser.test.ts -t "Vincent's Limit Break|Molten Firebird|Shade's Form|Summoner's Sending"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Vincent's Limit Break|Molten Firebird|Summoner's Sending|Shade's Form"`.
- 2026-03-25: Closed `Grixis Sojourners` by tightening shared triggered-text splitting instead of adding a card-only parser exception. `rules-engine/src/oracleTextParser.ts` now fans repeated trigger leads like `When X and when Y, ...` into separate triggered abilities before ability classification, which lets the existing Oracle IR path build two clean optional exile triggers for `Grixis Sojourners` while preserving the normal cycling line. Added focused parser regressions in `rules-engine/test/oracleTextParser.test.ts` and `rules-engine/test/oracleIRParser.test.ts`, removed the card from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck`, `npx vitest run test/oracleTextParser.test.ts -t "Grixis Sojourners|Shade's Form|Snapcaster Mage"`, and `npx vitest run test/oracleIRParser.test.ts -t "Grixis Sojourners|Vincent's Limit Break|Molten Firebird"`.

- 2026-03-25: Closed the linked-exile metadata projection seam for `Mirror Golem` and `Mourner's Shield` with shared parser/executor damage plumbing instead of card-only exceptions. `rules-engine/src/oracleIR.ts`, `rules-engine/src/oracleIRParserPostprocess.ts`, and `rules-engine/src/oracleIRParser.ts` now lift `Prevent all damage that would be dealt this turn by target source ... that shares a color with the exiled card` into a first-class `prevent_damage` step, `rules-engine/src/oracleIRDamagePrevention.ts` plus `shared/src/types.ts` now register turn-scoped source-based prevention effects keyed by linked exiled-card colors, `rules-engine/src/oracleIRExecutorDamageStepHandlers.ts` now applies those effects to deterministic Oracle IR damage, `rules-engine/src/actions/combat.ts` plus `rules-engine/src/AutomationService.ts` now consult the same prevention registry for combat-damage automation, and the sibling linked-exile type projection in `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` plus `rules-engine/src/combatAutomation.ts` now covers `Mirror Golem` protection text. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed both cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Mirror Golem|Mourner's Shield"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Mirror Golem|Mourner's Shield"`.

- 2026-03-25: Closed the linked-exile copy corridor for `Ardyn, the Usurper`, `Dino DNA`, and `Dimir Doppelganger` with shared parser/executor support instead of separate card exceptions. `rules-engine/src/oracleIRParserTokenSimpleClauses.ts` now preserves `create a token that's a copy of ...` phrasing as first-class `create_token` steps, `rules-engine/src/oracleTextParser.ts` broadens triggered-line label stripping so ability-word combat triggers like `Starscourge —` still normalize into a triggered ability, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIR.ts` now lift `this creature becomes a copy of that card` into a first-class `copy_permanent` step, `rules-engine/src/oracleIRExecutorMainLoop.ts` and `rules-engine/src/oracleIRExecutionTypes.ts` now thread moved-card runtime context through nested follow-up execution, `rules-engine/src/oracleIRExecutorTokenStepHandlers.ts` now resolves copy-token sources from same-resolution moved cards and source-linked exile while applying deterministic `except it's ...` overrides, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` now lets a battlefield permanent copy the exiled card's visible characteristics while retaining its activating ability text. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed all three cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Ardyn|Dino DNA|Dimir Doppelganger"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Ardyn|Dino DNA|Dimir Doppelganger"`.
- 2026-03-25: Closed `Klothys, God of Destiny` by tightening deterministic follow-up parsing and execution for same-resolution graveyard exile branches. `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now preserves `add {R} or {G}` as `manaOptions`, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now normalize `Otherwise, ...` into an inverted moved-card-type conditional and split deterministic `gain life and deal damage` bodies into separate executable steps, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates `it was not a <type>` against the last moved-card context, and `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` now deterministically applies the first listed mana option when no explicit mana choice is provided. Added/updated focused parser and executor regressions for both Klothys branches in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, then revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Klothys|Sequence Engine|Headstone|Morbid Bloom|Selfless Exorcist"`, and `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Klothys|Sequence Engine|Headstone|Morbid Bloom"`.
- 2026-03-25: Closed `Corpse Appraiser` by extending the same graveyard-exile conditional corridor instead of adding a card-specific exception. `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now rewrite conditional `look at the top N cards of your library, then put one of those cards into your hand and the rest into your graveyard` pairs into deterministic `draw` plus `mill` follow-up steps, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now treats article-only `a card is put into exile this way` wording as a successful moved-card gate, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` now prioritizes just-moved-card references like `it` before falling back to battlefield self-target resolution so the shared non-battlefield counter seam stays intact for `Lara Croft, Tomb Raider`. Added/updated focused parser and executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Corpse Appraiser` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot"`.
- 2026-03-25: Closed `Presumed Dead` and `Perigee Beckoner` with a shared pump-plus-temporary-dies-trigger seam instead of bespoke card handling. `rules-engine/src/oracleIRParserAbilityClauses.ts` now splits `gets +N/+M and gains "When this creature dies, ..."` packages into a normal `modify_pt` step plus a `grant_temporary_dies_trigger`, `rules-engine/src/oracleIRParserTemporaryTriggeredClauses.ts` now accepts `dies this turn` wording without stealing the whole pump clause, `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserSimpleActionClauses.ts` add a first-class `suspect` step, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now apply `suspect it` to the just-returned creature during delayed trigger resolution. Added focused parser and executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed both cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death"`.
- 2026-03-25: Closed the damage-provenance dies-trigger seam for `Scythe of the Wretched`, `Dread Slaver`, and `Soul Collector` by teaching the shared runtime to remember which permanent(s) damaged a creature this turn and by threading that provenance into dies-trigger matching. `rules-engine/src/actions/combat.ts`, `rules-engine/src/AutomationService.ts`, `rules-engine/src/oracleIRExecutorPermanentUtils.ts`, and `rules-engine/src/oracleIRExecutorDamageStepHandlers.ts` now record turn-scoped `damageSourceIds` on damaged creatures, `shared/src/zoneRetainedCounters.ts` plus `rules-engine/src/oracleIRExecutorZoneOps.ts` retain that metadata through death while clearing it on fresh battlefield entries, `rules-engine/src/cleanupStep.ts` plus `rules-engine/src/actions/turnActions.ts` now clear the turn-scoped provenance during cleanup, and `rules-engine/src/triggeredAbilitiesEventData.ts`, `rules-engine/src/triggeredAbilitiesChoiceSupport.ts`, `rules-engine/src/triggeredAbilitiesExecution.ts`, plus `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts` now infer/normalize the dead creature's damage provenance together with source attachment context so `dealt damage by this creature this turn` and `dealt damage by equipped creature this turn` filters match deterministically. Added focused trigger/executor regressions in `rules-engine/test/triggerParsing.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed all three cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/triggerParsing.test.ts -t "Dread Slaver|Soul Collector|Scythe of the Wretched"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Dread Slaver|Soul Collector|Scythe of the Wretched|damage provenance"`.
- 2026-03-25: Closed the face-down death-recursion seam for `Ashcloud Phoenix`, `Missy`, and `Yarus, Roar of the Old Gods` by extending the shared battlefield-return path instead of adding card-specific branches. `rules-engine/src/oracleIR.ts`, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts`, and `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now model `move_zone` battlefield entries that happen face down plus a first-class `turn_face_up` step, `rules-engine/src/oracleIRExecutorZoneOps.ts`, `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts`, `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`, and `rules-engine/src/oracleIRExecutorMainLoop.ts` now create hidden 2/2 face-down battlefield permanents, preserve their face-up card data, and deterministically restore them when later text says `turn it face up`, and `rules-engine/src/triggeredAbilitiesEventDetection.ts`, `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts`, `rules-engine/src/triggeredAbilitiesEventData.ts`, plus `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now recognize `face-down creature you control dies` filters and `it's a permanent card` gates in the same shared corridor. Added focused parser/trigger/executor regressions in `rules-engine/test/oracleIRParser.test.ts`, `rules-engine/test/triggerParsing.test.ts`, and `rules-engine/test/oracleIRExecutor.test.ts`, removed all three cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Ashcloud Phoenix|Missy|Yarus|Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot|Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death"`, `npx vitest run test/triggerParsing.test.ts -t "Yarus|Luminous Broodmoth|Marchesa|Valkyrie's Call|Edea|Thunderbolts"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Ashcloud Phoenix|Missy|Yarus|Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot|Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death|Luminous Broodmoth|Marchesa|Thunderbolts|Valkyrie's Call|Grave Betrayal|Edea"`.
- 2026-03-25: Closed `Lara Croft, Tomb Raider` and `The Animus` with a shared non-battlefield counter seam instead of separate card exceptions. `rules-engine/src/oracleIRExecutorZoneOps.ts` now understands counter-qualified graveyard selectors like `... from a graveyard with a memory counter on it`, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now let `add_counter` attach counters to the just-moved zone card when follow-up text says `put a discovery counter on it`. Added focused parser and executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed both cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Lara Croft|The Animus"`, and `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Lara Croft|The Animus"`.
- 2026-03-25: Closed the remaining `Athreos, God of Passage` blocker by threading explicit opponent pay/decline choices through the existing Oracle IR execution context instead of leaving all payable branches as unresolved gaps. `rules-engine/src/oracleIRExecutionTypes.ts` and `rules-engine/src/oracleIRExecutorExecutionContext.ts` now carry `unlessPaysLifeChoice`, `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` now applies the payer's chosen life payment or chosen decline deterministically, and `rules-engine/src/oracleIRExecutorMainLoop.ts` now treats a resolved paid branch as an applied wrapper instead of a skip. Added focused Athreos executor regressions for explicit pay and decline outcomes while keeping the no-choice case as a `player_choice_required` gap, removed `Athreos, God of Passage` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Athreos, God of Passage"`, `npx vitest run rules-engine/test/triggerParsing.test.ts -t "Athreos, God of Passage"`, and `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Athreos, God of Passage"`.

## Application-Level Alignment

This roadmap supports the broader MTGEDH goal:
- Multiplayer Commander gameplay with strong rules fidelity.
- Real-time reliability for up to 8 players.
- Less manual bookkeeping via safe automation.

## Milestone Model (3 Layers)

### Layer 1 â€” Reliability Foundation (short-term)
Goal: Make current automation paths robust against malformed context and multiplayer ambiguity.

Milestones:
- M1.1 Canonical context normalization across trigger/adapter/executor.
- M1.2 Deterministic selector binding precedence and sanitization.
- M1.3 Invalid-context safety (never over-resolve targets).
- M1.4 Regression guardrails for precedence/sanitization matrices.

Exit criteria:
- Shared normalization path used by major trigger/stack execution flows.
- Ambiguous target selectors skip safely rather than mis-target.
- Focused test suites + typecheck consistently green.

### Layer 2 â€” Coverage Expansion (mid-term)
Goal: Increase the percentage of Oracle templates that execute end-to-end without manual intervention.

Milestones:
- M2.1 Expand deterministic selector support (player/object/group patterns).
- M2.2 Broaden trigger/event hint derivation from runtime payloads.
- M2.3 Add parser coverage for frequent real-card phrasing variants.
- M2.4 Add integration tests for event->stack->resolution chains.

Exit criteria:
- More common Commander card templates execute automatically.
- New parser/executor additions ship with regression tests.
- No increase in false-positive executions.

### Layer 3 â€” Interaction Fidelity (long-term)
Goal: Improve correctness for complex interactions while preserving deterministic behavior.

Milestones:
- M3.1 Deepen intervening-if and condition-gated resolution handling.
- M3.2 Improve APNAP-sensitive and multiplayer edge sequencing.
- M3.3 Strengthen stack metadata continuity from source event to final resolution.
- M3.4 Add confidence reporting/telemetry for skipped vs applied effect paths.

Exit criteria:
- Complex multiplayer interactions are handled with predictable outcomes.
- Skip reasons are explicit and traceable.
- Fidelity improvements do not regress baseline reliability.

## Current Status Snapshot

Status key: `done`, `in-progress`, `not-started`, `blocked`

| ID | Milestone | Status | Notes |
|---|---|---|---|
| M1.1 | Canonical normalization pipeline | done | Shared trigger/stack context builders integrated. |
| M1.2 | Deterministic selector precedence | done | Explicit > inferred singleton > base fallback enforced in tests. |
| M1.3 | Invalid-context safety | done | Controller/opponent sanitization and malformed-ID hardening added. |
| M1.4 | Reliability regression guardrails | done | Matrix tests + focused adapter/trigger/executor regressions in place. |
| M2.1 | Deterministic selector expansion | done | Selector-context hardening completed for singleton/relational multiplayer cases across parser/executor/adapter. |
| M2.2 | Runtime hint derivation breadth | done | Trigger/event hint normalization unified across cast/activate/resolve paths, including legacy target shaping. |
| M2.3 | Oracle phrasing coverage | done | Added deterministic support for `its controller` selector phrasing (including possessive library forms) across parser/executor paths with regressions. |
| M2.4 | End-to-end chain integration tests | done | Added cast/activate and legacy stack event->stack->resolution regressions for target and relational contexts. |
| M3.1 | Intervening-if fidelity expansion | done | Opponent-control condition classes broadened with threshold handling across creatures/artifacts/enchantments/lands/planeswalkers/permanents, with direct tests. |
| M3.2 | APNAP/multiplayer edge sequencing | done | APNAP ordering now covers turn-order wrap-around and active-missing fallback behaviors, validated by regression matrix tests. |
| M3.3 | Stack metadata continuity | done | Canonical metadata shaping now covers primary and legacy stack resolution paths. |
| M3.4 | Skip/apply confidence telemetry | done | Trigger flow now exposes counters and emits structured telemetry summary logs for executions/applied/skipped paths. |

## Working Backlog (Next Up)

No uncovered milestone-critical items remain from the current roadmap pass.

Follow-up (non-blocking hardening):
1. Continue opportunistic parser template expansion for newly observed corpus phrasing variants.
2. Keep the remaining sacrifice automation residue intentionally scoped to silver/acorn/joke cards unless product scope expands; the practical black-border sacrifice backlog is now closed in the corpus audit.
3. Consider legacy `AutomationService` stack item modernization to align with canonical trigger metadata patterns used in active adapter/trigger pipelines.
4. Continue the corpus-first `move_zone` pass with the remaining constrained graveyard single-target families whose trailing effect text still needs extra state mutation or extra legality enforcement after the zone move, especially returned-card value riders and characteristic-changing return clauses.
   Boundary update: the death-recursion corridor now covers attached-host dies watchers for `enchanted creature dies`, `equipped creature dies`, `enchanted land dies`, and `enchanted permanent dies`, including delayed self-return / reattach patterns like `Gift of Immortality`, delayed equipped-creature recursion like `Resurrection Orb`, the Zendikon-style enchanted-land hand-return family, the straight attached-host return / hand-return Aura family (`Necrogen Communion`, `Fungal Fortitude`, `Minion's Return`, `Unholy Indenture`, `Abduction`, `Unhallowed Pact`, `False Demise`, `Demonic Vigor`, `Squee's Embrace`), conditional type-gated equipped-creature recursion like `Oathkeeper, Takeno's Daisho`, and quoted temporary dies-trigger grants that sit behind a preceding keyword grant such as `gains deathtouch and "When this creature dies..."` for `Infuse with Vitality` / `Pain 101`. Temporary until-end-of-turn dies-trigger grants still cover `Not Dead After All` / `Undying Malice` style effects, native dies filters now preserve keyword exclusions like `without flying` for `Luminous Broodmoth`, ownership exclusions like `but don't own` for `Edea, Possessed Sorceress`, subtype-qualified control filters like `a Villain you control dies`, counter-qualified filters like `with a +1/+1 counter on it dies`, and generic delayed next-end-step / next-upkeep scheduling still covers clean self-return templates like `Phytotitan`, `The Locust God`, and `The Scorpion God`.
   Current boundary: deterministic battlefield attachment now covers `attached to this creature`, singleton `attached to a creature you control`, self-attaching `Necromancy`-style `move then attach this enchantment/equipment/permanent to it` followups, delayed Aura self-return that must rebind `that creature` / `that land` / `that permanent`, payment-gated `If you do` return/attach packages like `Nim Deathmantle`, same-name graveyard bundles like `Echoing Return`, opponent-follow-up return chains like `Skullwinder` via a first-class `choose an opponent` binding seam, monarchy-gated “move it from hand to battlefield instead” branches like `Court of Ardenvale`, direct-IR intervening-if trigger wrappers for graveyard return abilities, repeatable choose-four modal resolution for `Planewide Celebration`-style spell text, threshold-style battlefield upgrade clauses that rebind `that card from your graveyard` against the pre-move graveyard snapshot for `Stitch Together`, landfall upgrade clauses that can inspect the bound triggering land for basic land types while reusing descriptive contextual references like `that nonland permanent card` for `Emeria Shepherd`, reflexive self-exile graveyard triggers where `You may exile it. When you do...` must move the source card out of your graveyard before the follow-up resolves, as on `Undead Butler`, self dies-to-hand recursion for named-or-self triggers like `Endless Cockroaches`, `Mortus Strider`, `Weatherseed Treefolk`, `Shivan Phoenix`, and `Immortal Phoenix`, and return-time characteristic rewrites that add colors/types to the permanent entering from a graveyard or delayed graveyard return, including `Valkyrie's Call`, `Grave Betrayal`, and `Lim-Dul the Necromancer`, with contextual type checks such as `If it's a creature` now resolving against the bound moved card as well. Quoted `You get an emblem with "..."` creation also now lands as a first-class executor step and created emblems now feed the trigger-discovery pipeline. The ordered queue has also been pushed through the remaining exact-target graveyard-to-hand / graveyard-to-library families, all currently clean graveyard-exile wrappers through `Trained Blessed Mind`, self graveyard-to-hand wrappers through `Evolution Charm`, self graveyard-to-library topdeck recursion for `Golgari Thug`, the previously deferred graveyard selector seam for `historic`, tribal subtype lists, compound subtype pairs, color-qualified creature cards, `with no abilities`, and `non-Assassin historic` targeting, plus deterministic follow-up tranches where a graveyard return is paired with same-line life gain, token creation, `then exile all graveyards`, inline or trailing `proliferate`, `The Ring tempts you`, simple leading conditional wrappers like `If a creature died this turn` / `If you do`, optional regular-mana payment gates like `Veinwitch Coven` / `Genesis`, same-name return riders keyed off a bound target card, post-return opponent choice chains, monarch-state alternate-destination upgrades for the same returned object, repeated modal mode selection with sentence-style bullet headers, graveyard-count wrappers that must still see the just-returned card for threshold checks, land-trigger-bound type checks for Plains-style alternate destinations, self-source graveyard exile follow-through for reflexive `When you do` returns, and comma-qualified dies-trigger parsing for `nontoken, non-Angel creature you control dies`. The remaining graveyard blockers in this corridor are now tighter around broader leave-zone exception tracking and more general post-entry characteristic/state rewrites.

## Update Protocol

When a milestone changes state:
1. Update `Last updated` date.
2. Change milestone status in **Current Status Snapshot**.
3. Add one line in **Milestone Change Log** with evidence (tests or typecheck command).

## Milestone Change Log

- 2026-03-25: Closed `Urborg Scavengers` by extending the shared linked-exile projection hook so move-to-exile effects can project keyword abilities from cards exiled with the current source. `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now recognizes the `This creature has flying as long as a card exiled with it has flying. The same is true for ...` family, recomputes the projected keyword set from linked exiled cards, and merges those abilities onto the source permanent while preserving other granted abilities. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Urborg Scavengers` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npx vitest run test/oracleIRParser.test.ts -t "Abyssal Harvester|Urborg Scavengers|Sequence Engine"`, `npx vitest run test/oracleIRExecutor.test.ts -t "Abyssal Harvester|Urborg Scavengers|Mardu Woe-Reaper|Corpse Appraiser"`, and `npm run typecheck` in `rules-engine`.
- 2026-03-25: Closed `Abyssal Harvester` by adding shared graveyard-entry turn provenance and graveyard legality matching for `that was put there this turn`. `rules-engine/src/oracleIRExecutorPlayerUtils.ts` and `rules-engine/src/oracleIRExecutorBattlefieldOps.ts` now stamp cards that enter graveyards with their current turn, `rules-engine/src/oracleIRExecutorZoneOps.ts` now parses and enforces the graveyard this-turn qualifier across graveyard target selection, and focused parser/executor regressions now lock the exact `Abyssal Harvester` text in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Revalidated with `npx vitest run test/oracleIRParser.test.ts -t "Abyssal Harvester|Sequence Engine|Corpse Appraiser"`, `npx vitest run test/oracleIRExecutor.test.ts -t "Abyssal Harvester|Corpse Appraiser|Sequence Engine|Mardu Woe-Reaper"`, and `npm run typecheck` in `rules-engine`.
- 2026-03-25: Closed the linked-exile provenance/permission return seam for `Intrepid Paleontologist`, `Boiling Rock Rioter`, `Emperor of Bones`, and `The Spot, Living Portal`. `rules-engine/src/oracleIRParser.ts` plus `rules-engine/src/oracleIRParserPostprocess.ts` now emit linked `grant_exile_permission` steps and preserve follow-up counter metadata, `rules-engine/src/oracleIRExecutorExileStepHandlers.ts` applies those linked permissions against cards exiled with the current source, `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` and `rules-engine/src/oracleIRExecutorBattlefieldOps.ts` now retain linked-source provenance for battlefield exiles and support source self-moves to owners' libraries, and `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now resolves linked-exile returns/reanimation plus self-to-library-bottom cleanup. Added/updated focused regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, then revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Intrepid Paleontologist|Boiling Rock Rioter|Emperor of Bones|The Spot, Living Portal"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Intrepid Paleontologist|Boiling Rock Rioter|Emperor of Bones|The Spot, Living Portal"`.
- 2026-03-25: Closed `Corpse Appraiser` with a shared conditional graveyard-exile follow-up seam. `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now rewrite conditional `look top N, choose one to hand, rest to graveyard` text into deterministic `draw` plus `mill` steps, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now recognizes article-only `a card is put into exile this way` conditions against the last moved-card payload, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` now prefers just-moved-card bindings over battlefield self-reference when `add_counter` targets raw `it`, which preserves the adjacent Lara counter seam. Added/updated focused regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Corpse Appraiser` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Corpse Appraiser|Lara Croft|Diregraf Scavenger|Mardu Woe-Reaper|Klothys|The Animus|The Spot"`.
- 2026-03-25: Closed `Presumed Dead` and `Perigee Beckoner` by teaching the parser to split `gets +N/+M and gains "When this creature dies, ..."` clauses into separate deterministic steps and by adding a first-class `suspect` battlefield action for delayed return follow-ups. `rules-engine/src/oracleIRParserAbilityClauses.ts` and `rules-engine/src/oracleIRParserTemporaryTriggeredClauses.ts` now preserve both the pump and the granted dies trigger, `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now model `suspect`, and `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now apply that status to the returned creature during delayed trigger execution. Added focused parser and executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed both cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Presumed Dead|Perigee Beckoner|Not Dead After All|Infuse with Vitality|Fake Your Own Death"`.
- 2026-03-24: Closed the `Headstone` graveyard-exile rider by moving delayed upkeep/end-step parsing ahead of immediate simple-action parsing in `rules-engine/src/oracleIRParser.ts`, so text like `Draw a card at the beginning of the next upkeep` now becomes a `schedule_delayed_trigger` instead of an immediate `draw`. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Headstone` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Headstone|Morbid Bloom|Selfless Exorcist"`, `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Headstone|Morbid Bloom"`, and `npm run typecheck --workspace=rules-engine`.
- 2026-03-24: Cleared the adjacent `Sequence Engine` graveyard-exile rider by carrying created-token ids forward through the Oracle IR executor and extending battlefield contextual resolution to understand plural follow-ups like `each of those Zombies`. `rules-engine/src/oracleIRExecutorMainLoop.ts` now preserves freshly created token ids for later steps, `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` now resolves subtype-qualified contextual plural targets and supports `X` add-counter amounts derived from the created token batch, and focused parser/executor regressions now lock the exile -> `create X` -> `put X +1/+1 counters on each` sequence end-to-end. Removed `Sequence Engine` from `docs/oracle-automation-batch-blockers.md` and revalidated with `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Sequence Engine|Headstone|Morbid Bloom|Selfless Exorcist"`, `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Sequence Engine|Headstone|Morbid Bloom"`, and `npm run typecheck --workspace=rules-engine`.
- 2026-03-24: Kept the graveyard-exile queue moving by teaching `rules-engine/src/oracleIRExecutorTokenStepHandlers.ts` to evaluate `create_token` X-amount clauses from attached `where X is ...` riders against runtime state, including just-moved exiled cards. Added focused parser/executor regressions for `Morbid Bloom` in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Morbid Bloom` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm test -- oracleIRParser.test.ts oracleIRExecutor.test.ts` plus `npm run typecheck` in `rules-engine`.
- 2026-03-24: Cleared the `Selfless Exorcist` graveyard-exile rider blocker by extending `rules-engine/src/oracleIRExecutorDamageStepHandlers.ts` so `deal_damage` can resolve moved-card power/toughness references like `That card deals damage equal to its power to this creature.` Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Selfless Exorcist` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm test -- oracleIRParser.test.ts oracleIRExecutor.test.ts` plus `npm run typecheck` in `rules-engine`.
- 2026-03-24: Kept the cast-from-graveyard queue moving by extending `rules-engine/src/oracleIRParserPostprocess.ts` to understand turn-gated graveyard keyword grants like `During your turn, nonland permanent cards in your graveyard have retrace`, emitting a guarded `grant_graveyard_permission` step behind an `it's your turn` conditional. Added parser/executor regressions for `Six`, which also tightens the shared corridor for similar turn-scoped graveyard cast permissions layered on top of retrace/flashback-style reminder text. Revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1922 passed`).
- 2026-03-24: Extended the queue straight into the cast-from-graveyard permission corridor by broadening `rules-engine/src/oracleIRParserPostprocess.ts` so unknown keyword/static clauses now recognize self-contained graveyard permission templates like `Flashback {2}{R}`, `Flashback-Sacrifice three creatures`, and `Each nonland card in your graveyard has escape`, and by teaching `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` to normalize graveyard permission criteria such as `instant and sorcery`. Added parser/executor regressions for `Faithless Looting`, `Dread Return`, and `Underworld Breach`, which also lifts the same keyword-permission seam for adjacent queue items like `Sevinne's Reclamation` and other straight flashback cards. Revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1920 passed`).
- 2026-03-24: Closed the next graveyard-resolution blocker by adding a reusable `copy_spell` Oracle IR step for `copy this spell` clauses and threading resolving-spell provenance (`castFromZone` / `enteredFromZone`) through Oracle IR execution context so spell-resolution conditionals can evaluate from stack metadata instead of only battlefield objects. `rules-engine/src/oracleIRParserPostprocess.ts` now expands `If this spell was cast from a graveyard, you may copy this spell and may choose a new target for the copy`, `rules-engine/src/oracleIRExecutorMainLoop.ts` now replays prior deterministic spell steps for copied spells, and new `rules-engine/src/oracleIRExecutorCopySpellSupport.ts` auto-retargets single-target graveyard reanimation copies when exactly one legal replacement target remains. Added parser/executor/adapter regressions for `Sevinne's Reclamation` and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1925 passed`).
- 2026-03-24: Cleared the random-graveyard-selection follow-on seam immediately after the leave-replacement tranche by extending `rules-engine/src/oracleIRExecutorZoneOps.ts` and `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` to recognize `a/an ... at random from your graveyard` selectors, choose a legal graveyard object via RNG, and feed that result through the existing battlefield-entry and leave-the-battlefield replacement pipeline. Added parser/executor regressions for `Kheru Lich Lord`, removed it from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1915 passed`).
- 2026-03-24: Cleared the leave-the-battlefield exile-replacement seam for the current graveyard queue tranche by adding a shared battlefield leave-zone redirect helper in `shared/src/leaveBattlefieldReplacement.ts`, a first-class `grant_leave_battlefield_replacement` IR step in `rules-engine/src/oracleIR.ts`, parser expansion for `If it would leave the battlefield, exile it instead of putting it anywhere else` in `rules-engine/src/oracleIRParserPostprocess.ts` / `rules-engine/src/oracleIRParser.ts`, and executor support in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`, `rules-engine/src/oracleIRExecutorMainLoop.ts`, `rules-engine/src/oracleIRExecutorBattlefieldOps.ts`, `rules-engine/src/rulesEngineAdapterStateSupport.ts`, and `rules-engine/src/actions/sacrifice.ts`. Finished the seam by wiring targeted battlefield-to-hand `move_zone` effects through the same redirect path in `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts`, which clears queue items `316. Whip of Erebos`, `317. Moira and Teshar`, and `319. Personal Decoy`, while leaving `318. Kheru Lich Lord` narrowed to its separate random-graveyard-selection blocker. Revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1913 passed`).
- 2026-03-24: Cleared the first graveyard trigger/provenance corridor in the 10k queue by teaching `rules-engine/src/oracleTextParser.ts` to preserve triggered abilities behind leading keyword labels like `Landfall -`, broadening `rules-engine/src/actions/triggersHandler.ts` to discover graveyard-active triggers on cards sitting in graveyards and to feed richer ETB provenance into trigger evaluation, threading `castFromZone` / `enteredFromZone` plus controller graveyard snapshots through `rules-engine/src/triggeredAbilitiesEventData.ts`, extending both `rules-engine/src/triggeredAbilitiesConditionRouting.ts` and `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` to evaluate graveyard self-reference, simple `and`/`or` trigger conditions, turn checks, and `entered from` / `cast from your graveyard` provenance gates, normalizing parenthetical `for its escape cost this turn` graveyard permissions in `rules-engine/src/oracleIRParserPostprocess.ts`, and parsing standalone `attach it to target creature you control` wording into first-class attach steps with trigger-choice prompts via `rules-engine/src/triggeredAbilitiesChoiceSupport.ts` / `rules-engine/src/oracleIRExecutorAttachStepHandlers.ts`. Paired that with battlefield-entry provenance tags in `rules-engine/src/oracleIRExecutorZoneOps.ts`, a self-reference fallback for graveyard permission targets in `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts`, focused parser/executor/trigger-handler regressions for `Rocket-Powered Goblin Glider`, `Skyclave Shade`, `Prized Amalgam`, `Archfiend's Vessel`, and `Confession Dial`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`1909 passed`).
- 2026-03-24: Cleared the graveyard-exile-plus-token seam for `Pharika, God of Affliction`, `Necrogenesis`, `Vile Rebirth`, `Shamble Back`, `Beckon Apparition`, `Boneclad Necromancer`, and `Funeral Pyre` by teaching `rules-engine/src/oracleTextParserPreprocess.ts` to keep `Its owner ...` continuation sentences attached to the same ability, parsing owner-token clauses as contextual `owner_of_moved_cards` selectors in `rules-engine/src/oracleIRParserTokenSimpleClauses.ts`, resolving those selectors from the just-moved card set in `rules-engine/src/oracleIRExecutorTokenStepHandlers.ts` / `rules-engine/src/oracleIRExecutorMainLoop.ts`, and broadening `rules-engine/src/tokenCreationDetection.ts` so subject-order `creates ... token` wording materializes tokens correctly. Added focused parser/executor regressions for `Pharika, God of Affliction`, `Funeral Pyre`, `Shamble Back`, and `Boneclad Necromancer`, removed the cleared entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, and `npx vitest run rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1767 passed`).
- 2026-03-24: Narrowed the `Athreos, God of Passage` seam by extending `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` and `rules-engine/src/oracleIR.ts` to parse `return it to your hand unless target opponent pays 3 life` as a first-class `unless_pays_life` wrapper, then adding executor support in `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` / `rules-engine/src/oracleIRExecutorMainLoop.ts` so the return resolves deterministically when the targeted opponent cannot pay life and otherwise records a `player_choice_required` automation gap instead of mis-resolving. Added focused parser/trigger/executor regressions for `Athreos, God of Passage`, kept the blocker entry in `docs/oracle-automation-batch-blockers.md` but narrowed it to the remaining opponent-choice payment seam, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1762 passed`).
- 2026-03-24: Cleared the delayed dies-to-hand trigger-filter seam for `Liesa, Forgotten Archangel` and `Rienne, Angel of Rebirth` by tightening `rules-engine/src/triggeredAbilitiesEventDetection.ts` so controlled-creature death filters keep their existing `CONTROLLED_CREATURE_DIED` classification while also accepting plural `die` wording, broadening `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts` / `rules-engine/src/triggeredAbilitiesMatching.ts` so `nontoken`, `multicolored`, ownership, and plural controlled-death checks survive both generic and controlled dies event paths, and reusing the existing contextual plural move-zone support for `return those cards to their owner's hand` delayed resolutions. Added focused trigger/executor regressions for both cards, removed their entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1759 passed`).
- 2026-03-24: Cleared the quoted granted-dies-trigger follow-up seam by extending `rules-engine/src/oracleIRParserAbilityClauses.ts` and `rules-engine/src/oracleIRParserTemporaryTriggeredClauses.ts` so quoted `When this creature dies, ...` grants can split off a same-line follow-up sentence and can also parse non-`until end of turn` grants like `it gains haste and "When this creature dies, ..."` into watched dies-trigger steps that persist only while the permanent remains on the battlefield. Paired that with watched-trigger expiry cleanup in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`, `rules-engine/src/rulesEngineAdapterDelayedTriggerSupport.ts`, and `rules-engine/src/oracleIR.ts`, added focused parser/executor/adapter regressions for `Verdant Rebirth`, `Flame-Wreathed Phoenix`, `Fool's Demise` main enchanted-creature return, and `Puppet Master`, removed those four entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1757 passed`).
- 2026-03-24: Cleared the named self-dies recursion seam by broadening `rules-engine/src/oracleTextParser.ts`, `rules-engine/src/triggeredAbilitiesParsing.ts`, `rules-engine/src/triggeredAbilitiesEventDetection.ts`, `rules-engine/src/triggeredAbilitiesConditionRouting.ts`, and `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts` so triggers like `When Endless Cockroaches is put into your graveyard from the battlefield` normalize to source-self dies filters, recognize `your graveyard` / `its owner's graveyard` variants, and only fire for the source permanent. Added focused trigger/executor regressions covering `Endless Cockroaches`, `Mortus Strider`, `Weatherseed Treefolk`, `Shivan Phoenix`, `Immortal Phoenix`, and `Fool's Demise` self-return, removed the five newly cleared dies-to-hand blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1872 passed`).
- 2026-03-24: Cleared the `Lim-Dul the Necromancer` seam by broadening `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` so contextual bound-card type checks accept the natural `If it's a creature` wording in addition to `... creature card`, which lets the existing battlefield-entry characteristic-rewrite path reuse the already merged `move_zone` upgrade cleanly instead of skipping the return. Added focused parser/executor regressions for `Lim-Dul the Necromancer`, kept the graveyard blocker ledger unchanged because this card had already been narrowed to the shared seam rather than tracked as a standalone item, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1860 passed`).
- 2026-03-24: Cleared the return-time characteristic-rewrite seam for death-recursion cards by teaching `rules-engine/src/oracleTextParser.ts` and `rules-engine/src/triggeredAbilitiesParsing.ts` to preserve comma-qualified trigger conditions like `a nontoken, non-Angel creature you control dies`, extending `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserPostprocess.ts` so battlefield `move_zone` steps can carry entry-time added colors/types from follow-up clauses such as `It's an Angel in addition to its other types`, broadening `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` to normalize `additional +1/+1 counter` wording on battlefield returns, and post-processing returned permanent ids in `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` / `rules-engine/src/oracleIRExecutorZoneOps.ts` so those characteristic rewrites land on the exact permanents that entered. Added focused parser/trigger/executor regressions for `Valkyrie's Call` and `Grave Betrayal`, removed both blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1858 passed`).
- 2026-03-24: Cleared the attached-host dies watcher seam by threading source-aware host attachment metadata through trigger event payloads and condition matching, preserving `enchanted creature dies` / `equipped creature dies` / `enchanted land dies` / `enchanted permanent dies` filters in `rules-engine/src/triggeredAbilitiesEventDetection.ts`, `rules-engine/src/triggeredAbilitiesEventData.ts`, `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts`, `rules-engine/src/triggeredAbilitiesConditionRouting.ts`, and `rules-engine/src/triggeredAbilitiesMatching.ts`, then carrying chosen moved-permanent ids into delayed triggers in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` and `rules-engine/src/oracleIRExecutorMainLoop.ts`. Also broadened `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` and `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` so delayed Aura self-return can resolve `this permanent` from graveyard/hand/exile and reattach to `that creature` / `that land` / `that permanent`, plus tightened the `return ... to ...` parser split with a new anchored destination-prefix helper in `rules-engine/src/oracleIRParserSacrificeHelpers.ts` so `Gift of Immortality`-style self-return no longer regresses `up to ... to your hand` graveyard-return bundles. Added regressions for `Gift of Immortality`, `Resurrection Orb`, and the Zendikon hand-return seam, removed the cleared blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1766 passed`).
- 2026-03-24: Followed that host-dies seam with a wider executor regression batch proving the same shared path across the simpler Aura return families. Added attached-host return tests for `Necrogen Communion`, `Fungal Fortitude`, `Minion's Return`, `Unholy Indenture`, `Abduction`, `Unhallowed Pact`, `False Demise`, `Demonic Vigor`, and `Squee's Embrace` in `rules-engine/test/oracleIRExecutor.test.ts`, narrowed the remaining `Shade's Form` blocker to its unrelated granted-ability parsing residue, and removed the newly cleared entries from `docs/oracle-automation-batch-blockers.md`. Revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1776 passed`).
- 2026-03-24: Extended the same corridor with conditional trailing-`if` move-zone wrapping and deterministic conjoined move-zone follow-up splitting. `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now lifts `return ... if it's a Samurai card` into a real conditional wrapper, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates bound-card type checks like `it's a Samurai card`, and `rules-engine/src/oracleIRParserPostprocess.ts` now splits parsed `move_zone` clauses followed by deterministic riders such as `and you draw a card`. Added parser/executor regressions for `Oathkeeper, Takeno's Daisho` and parser/effect-execution regressions for `Edea, Possessed Sorceress`, removed `Oathkeeper, Takeno's Daisho` from `docs/oracle-automation-batch-blockers.md`, and narrowed `Edea, Possessed Sorceress` to its remaining trigger-filter seam. Revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1780 passed`).
- 2026-03-24: Broadened temporary granted-dies-trigger parsing in `rules-engine/src/oracleIRParserTemporaryTriggeredClauses.ts` so the existing grant executor can see through leading keyword grants such as `target creature gains deathtouch and "When this creature dies, ..."`. Added parser coverage for `Infuse with Vitality` and `Pain 101`, plus executor coverage proving the full `Infuse with Vitality` spell now grants the delayed dies trigger and resolves its life-gain rider. Removed both blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1783 passed`).
- 2026-03-24: Cleared the remaining `Edea, Possessed Sorceress` trigger seam by threading owner identity through `rules-engine/src/triggeredAbilitiesEventData.ts`, extending `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts` so controlled-creature dies filters can also enforce `but don't own`, and adding focused parse/trigger/executor regressions in `rules-engine/test/triggerParsing.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Removed `Edea, Possessed Sorceress` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1786 passed`).
- 2026-03-24: Extended the same trigger-matching seam so death triggers can preserve richer subject qualifiers instead of collapsing to plain `dies`. `rules-engine/src/triggeredAbilitiesEventData.ts` now carries token/counter context for trigger evaluation, `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts` now matches dies filters with subtype, `nontoken` / `non-<type>`, `you don't control`, and `with a +1/+1 counter on it` qualifiers, and `rules-engine/src/triggeredAbilitiesEventDetection.ts` now preserves broader `... you control` / `... you don't control` death filters instead of dropping them. Added focused parse/trigger/executor regressions for `Marchesa, the Black Rose` and `Thunderbolts Conspiracy` in `rules-engine/test/triggerParsing.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, removed both blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1790 passed`).
- 2026-03-24: Bridged direct Oracle-IR intervening-if execution for triggered abilities by adding `interveningIf` metadata to `rules-engine/src/oracleIR.ts`, preserving parsed trigger clauses in `rules-engine/src/oracleIRParser.ts`, wrapping triggered IR abilities with a top-level `conditional` step after parser postprocess passes, and extending `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` with reusable checks for life-gained-this-turn thresholds, battlefield control counts, graveyard card-count thresholds, `this permanent is in your graveyard`, battlefield presence, and tapped-state predicates. Added regressions for `Grave Venerations` and `Aerith, Last Ancient`, updated older nested-step assumptions in focused tests, cleared both blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1740 passed`).
- 2026-03-24: Cleared the `Planewide Celebration` blocker by extending `rules-engine/src/oracleIRParserChooseMode.ts` to parse sentence-style bullet headers such as `Choose four. You may choose the same mode more than once.`, carrying `canRepeatModes` through `rules-engine/src/oracleIR.ts`, preserving repeated modal selections in `rules-engine/src/oracleIRExecutorChooseModeStepHandlers.ts` and `rules-engine/src/triggeredAbilitiesChoiceSupport.ts`, and exposing duplicate-legal mode prompts through `rules-engine/src/choiceEventsFactories.ts`. Also routed nested modal mode text back through full Oracle-IR postprocess parsing in `rules-engine/src/oracleIRParser.ts`, which lets keyword modes like `Proliferate.` resolve cleanly inside modal bullets. Added parser/executor regressions for `Planewide Celebration`, removed it from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1742 passed`).
- 2026-03-24: Cleared the `Stitch Together` blocker by extending `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` to treat `that card from your graveyard`-style phrases as contextual bound-card references, and by extending `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` so graveyard-count wrappers can still see the just-returned bound card when a trailing conditional immediately upgrades that move. Added parser/executor regressions for `Stitch Together`, removed it from `docs/oracle-automation-batch-blockers.md`, cleared stale resolved ledger entries for `Awaken the Honored Dead` and `Consumed by Greed`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1744 passed`).
- 2026-03-24: Cleared the `Emeria Shepherd` blocker by extending `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` to evaluate `that land is a Plains` against a bound triggering land id, broadening `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` to recognize descriptive contextual references like `that nonland permanent card`, and teaching `rules-engine/src/oracleIRExecutorZoneOps.ts` that `nonland permanent` selectors should bind as nonland graveyard targets instead of falling through to land matching. Added parser/executor regressions for `Emeria Shepherd`, removed it from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1746 passed`).
- 2026-03-24: Cleared the `Undead Butler` blocker by extending `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` with a zone-based fallback for self-referential exile steps so `You may exile it` can move the source card out of your graveyard after it dies, allowing the existing `When you do` conditional wrapper to resolve the follow-up graveyard return deterministically. Added executor regression coverage for `Undead Butler`, removed it from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1747 passed`).
- 2026-03-24: Opened the first death-recursion seam by adding first-class `grant_temporary_dies_trigger` Oracle IR support, watched `WHEN_DIES` delayed-trigger scheduling/execution, and same-resolution token attachment for `Not Dead After All` / `Undying Malice` / `Fake Your Own Death` style effects, including `Wicked Role` token creation attached to the returned creature. Followed that by extending triggered-ability parsing and condition evaluation so native dies triggers can preserve keyword-exclusion filters like `a creature you control without flying dies`, which lets `Luminous Broodmoth` reuse the existing contextual return-plus-counter executor path. Added focused parser/executor/trigger-handler/adapter regressions, removed the covered `Not Dead After All` family entries plus `Luminous Broodmoth` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1818 passed`).
- 2026-03-24: Extended the same death-recursion corridor with a generic `schedule_delayed_trigger` Oracle IR step so triggered effects can defer an arbitrary deterministic follow-up to the next end step or upkeep while preserving the current bound-card context. That cleanly unlocks self-return templates like `Phytotitan`, `The Locust God`, and `The Scorpion God`, whose delayed return effects now reuse the existing contextual `return it` move-zone executor path instead of needing card-specific glue. Added parser/executor regressions for delayed battlefield and hand returns, removed those three cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads` (`1822 passed`).
- 2026-03-24: Cleared the `Volcanic Vision`, `Golbez, Crystal Collector`, `Peerless Recycling`, and `Reconstruct History` blockers by extending life/combat clause parsing to recognize `deals damage equal to ...`, threading `lastMovedCards` into damage and move-zone resolution so returned-card mana value riders and `another target` graveyard follow-ups execute deterministically, adding a conservative self-exile fallback for source spells still on the stack when Oracle text says `Exile <this spell>`, widening life-gain/life-loss variable amount resolution to read returned-card power/toughness as well as mana value, broadening graveyard single-target parsing to accept `another target`, evaluating `If the gift was promised` from execution context, splitting differentiated `up to one target <type> card ... from your graveyard` bundles into separate move-zone steps, and resolving chosen graveyard targets by criteria from `chosenObjectIds` with safe no-op handling for unchosen `up to one` slots. Added parser/executor regressions for all four cards, removed them from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1731 passed`).
- 2026-03-24: Extended the graveyard follow-up corridor with first-class optional regular-mana payments, contextual `that card` move-zone references inside `If you do` wrappers, same-name graveyard return bundles keyed off a bound target, `choose an opponent` follow-up binding for opponent-return chains, and monarch-state alternate-destination upgrades for the already returned card. `rules-engine/src/oracleIR.ts` now carries `pay_mana` and `choose_opponent`, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now upgrade `you may pay {B}` / `you may pay {2}{G}` and `choose an opponent` into first-class steps while also parsing conditional zone-move bodies like `If you're the monarch, you may put it onto the battlefield instead`, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now recognizes subject-order graveyard return phrasing like `That player returns a card from their graveyard to their hand`, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates monarch-state wrappers, `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now spend supported mana costs from `state.manaPool` and carry per-resolution opponent choice bindings forward through later steps, `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now resolves contextual `that card` references from the already bound object id across graveyard/hand/exile, and `rules-engine/src/oracleIRExecutorZoneOps.ts` now supports `return target card and all other cards with the same name as that card from your graveyard to your hand`. Added parser/executor regressions for `Veinwitch Coven`, `Genesis`, `Nim Deathmantle`, `Echoing Return`, `Skullwinder`, and `Court of Ardenvale`, and cleared all six blocker entries from the batch ledger. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1723 passed`).
- 2026-03-24: Extended the same graveyard follow-up corridor with reusable simple conditional wrappers. `rules-engine/src/oracleIRParserPostprocess.ts` now upgrades unknown clauses of the form `If <condition>, <deterministic token/simple-action/graveyard-return body>` into first-class `conditional` steps, and `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates `If you do` off the prior step outcome plus `If a creature died this turn` from turn-state tracking. Added parser/executor regressions for `Fungal Rebirth` and `Toph, Hardheaded Teacher`, and cleared both blocker entries from the batch ledger. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1712 passed`).
- 2026-03-24: Added first-class keyword follow-up support for `proliferate` and `The Ring tempts you` in the Oracle IR corridor. `rules-engine/src/oracleIR.ts` now carries `proliferate` and `ring_tempts_you` steps, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now expand unknown keyword riders and merge adjacent same-spell follow-up abilities back into a single execution chain, and `rules-engine/src/oracleIRExecutorKeywordStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now execute deterministic singleton/no-target cases for proliferate and Ring temptation updates. Added parser/executor regressions for `Unnatural Restoration`, `Sam's Desperate Rescue`, and `Tezzeret's Gambit`, and cleared the two graveyard-return blocker entries that were waiting on these seams. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1708 passed`).
- 2026-03-24: Extended graveyard-return follow-up parsing in `rules-engine/src/oracleIRParserPostprocess.ts` so deterministic same-line riders no longer get swallowed when a clause starts with life gain or sacrifice, and so `exile all graveyards` is normalized into the existing all-graveyards `move_zone` seam. Added executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for `Sword of Light and Shadow`, `Abstergo Entertainment`, `Spawnbed Protector`, `Revive the Shire`, `True Ancestry`, and `Witch of the Moors`, which now preserve the graveyard return even when the non-deterministic opponent-sacrifice half still skips. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1703 passed`).
- 2026-03-24: Addressed the high-yield graveyard exact-target selector blocker seam in `rules-engine/src/oracleIRExecutorZoneOps.ts` by extending `move_zone` single-target criteria with reusable refinements for `historic`, creature-type lists (`Zombie`, `Dinosaur`, `Bat, Lizard, Rat, or Squirrel`), subtype pairs (`Equipment or Vehicle`, `Aura or Equipment`), color-qualified selectors (`black creature card`), `with no abilities`, and exclusion filters like `non-Assassin historic`. Added queue-backed executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for `Samwise Gamgee`, `Mudflat Village`, `Lord of the Undead`, `Cid, Freeflier Pilot`, `Atzocan Seer`, `Satsuki, the Living Lore`, `Red XIII, Proud Warrior`, `Call to the Netherworld`, `Layla Hassan`, `Ruxa, Patient Professor`, and `Elena, Turk Recruit`, plus a negative guard that rejects Assassin historic targets for Elena. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1697 passed`).
- 2026-03-24: Continued the ordered graveyard-recursion pass by extending self graveyard-to-hand executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for `Turntimber Sower`, `Flood of Recollection`, `Evolution Charm`, `Den Protector`, `Wrenn and Six`, `Mnemonic Wall` (optional path), `Cormela, Glamour Thief`, `Gloomshrieker`, `Dryad's Revival`, and `Spring-Leaf Avenger`, on top of the earlier `Nyx Weaver`/`Trading Post`/`Codex Shredder`/`Mystic Retrieval` tranche. Also advanced the blocker ledger in `docs/oracle-automation-batch-blockers.md` through the contiguous dies-return-to-battlefield and dies-return-to-hand seams (`Not Dead After All` through `Puppet Master`) so the ordered queue can continue past those unsupported trigger-return packages without re-triage. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1685 passed`).
- 2026-03-24: Continued the ordered self-recursion pass beyond `Tortured Existence` by adding focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for optional-path `Blood Operative`, `Oversold Cemetery`, `Graveshifter`, `Auramancer`, and `Greenwarden of Murasa`; clean graveyard-to-hand wrappers `Nyx Weaver`, `Trading Post`, `Codex Shredder`, `Memorial to Folly`, `Tezzeret, Master of the Bridge`, `Stormchaser's Talent`, `Kolaghan's Command`, `Trailtracker Scout`, `Colossal Skyturtle`, `Scour for Scrap`, `Nissa, Vital Force`, `Palace Siege`, `Mystic Retrieval`, `Hanna, Ship's Navigator`, `Cavalier of Dawn`, and `Auroral Procession`; plus self graveyard-to-library topdeck recursion for `Golgari Thug`. Deferred the newly confirmed selector / rider blockers in `docs/oracle-automation-batch-blockers.md`, including `Samwise Gamgee`, `Mudflat Village`, `Skullwinder`, `Lord of the Undead`, `Court of Ardenvale`, `Cid, Freeflier Pilot`, `Atzocan Seer`, `Satsuki, the Living Lore`, `Veinwitch Coven`, `Red XIII, Proud Warrior`, `Golbez, Crystal Collector`, and `Reconstruct History`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1675 passed`).
- 2026-03-24: Continued straight into the next ordered `your graveyard to hand` family by locking the first clean self-recursion tranche behind focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts`: `Buried Ruin`, `Phyrexian Reclamation`, `Archaeomancer`, `Evolution Witness`, `Haven of the Spirit Dragon`, `Timeless Witness`, `Regrowth`, `Honest Rutstein`, and `Tortured Existence`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1652 passed`).
- 2026-03-24: Continued the same ordered graveyard-exile queue all the way to the next family boundary by locking the remaining clean wrapper cards behind focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts`: `Magic Pot`, `Cleanup Crew`, `Mechanical Mobster`, `Ambush Wolf`, `Crossroads Candleguide`, `Wreck Remover`, `Phyrexian Furnace`, `Veteran Survivor`, `Sungold Sentinel`, `Offalsnout`, `Ashnod's Harvester`, `Crook of Condemnation`, `Territorial Kavu`, `Purify the Grave`, `Gravestone Strider`, `Apostle of Purifying Light`, `Fade from Memory`, `Crypt Creeper`, `Shadowfeed`, `Soul-Guide Gryff`, `Thraben Heretic`, `Rise of Extus`, `Mortiphobia`, `Sewerdreg`, `Moratorium Stone`, `Steamclaw`, and `Trained Blessed Mind`. Deferred the remaining graveyard-exile rider blockers to `docs/oracle-automation-batch-blockers.md`, including `Wizard's Spellbook`, `Pharika, God of Affliction`, `Necrogenesis`, `Keen-Eyed Curator`, `Deathgorge Scavenger`, `Urborg Scavengers`, `Dimir Doppelganger`, `Mardu Woe-Reaper`, `Diregraf Scavenger`, `Corpse Appraiser`, `Sequence Engine`, `The Spot, Living Portal`, `Morbid Bloom`, `Funeral Pyre`, `Headstone`, `Grixis Sojourners`, `Creakwood Ghoul`, `Grave Robbers`, `Selfless Exorcist`, `Color Pie`, and `The Many Deeds of Belzenlok`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1640 passed`).
- 2026-03-24: Continued the same ordered queue through the remaining exact-target graveyard-to-library family and the first clean graveyard-exile subgroup. Added focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for `Chrome Companion`, `Hoverstone Pilgrim`, `Reito Lantern`, `Reito Sentinel`, `Jade-Cast Sentinel`, `Malevolent Chandelier`, `Dutiful Knowledge Seeker`, `Phyrexian Archivist`, `Cogwork Archivist`, `Nantuko Tracer`, `Keeper of the Cadence`, `Grazing Kelpie`, `Deathrite Shaman`, `Return to Nature`, `Soul-Guide Lantern`, `Agatha's Soul Cauldron`, `Heritage Reclamation`, and `General's Enforcer`, while intentionally deferring rider-heavy queue items like `Scavenging Ooze`, `The Scarab God`, `Cemetery Reaper`, `Armored Scrapgorger`, and `Klothys, God of Destiny` to the end-of-batch blocker pass. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1597 passed`).
- 2026-03-25: Closed `Klothys, God of Destiny` from the deferred graveyard-exile blocker pass by finishing the branch follow-up seam instead of treating it as a one-off. `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now keeps `add {R} or {G}` on the structured mana-options path, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now expand `Otherwise, ...` unknowns into inverse moved-card-type conditionals and deterministically split their multi-action bodies, `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates `it was not a land`, and `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` now falls back to the first listed mana option when no explicit mana choice is supplied. Validation: `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleIRParser.test.ts -t "Klothys|Sequence Engine|Headstone|Morbid Bloom|Selfless Exorcist"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Klothys|Sequence Engine|Headstone|Morbid Bloom"`.
- 2026-03-24: Continued the same ordered corpus queue through adjacent graveyard-to-hand and graveyard-to-library exact-target families that were already supported by the current `move_zone` executor seam. Added focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for `Naya Charm`, `Pulse of Murasa`, `Revive the Fallen`, `Mine Excavation`, `Endbringer's Revel`, `Disturbing Plot`, `Noxious Revival`, `Junktroller`, and `Vessel of Endless Rest`, plus additional direct reanimation locks for `Fated Return`, `Rise from the Grave`, `Ashiok, Sculptor of Fears`, `Grave Upheaval`, `Endless Obedience`, `Sheoldred, Whispering One`, `Dread Return`, `Unburial Rites`, and `Phyrexian Delver`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1563 passed`).
- 2026-03-24: Continued the emblem seam by wiring created emblems into trigger discovery. `rules-engine/src/actions/triggersHandler.ts` now scans player-controlled emblems and parses any triggered ability text they carry through the same `parseTriggeredAbilitiesFromText(...)` path used for permanents, and `rules-engine/test/triggersHandler.test.ts` now locks a `Liliana, Waker of the Dead` emblem beginning-of-combat trigger discovery regression. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1545 passed`).
- 2026-03-24: Continued the same ordered graveyard queue by broadening saga chapter prefix normalization to tolerate mojibake punctuation variants and by locking in the next already-supported recursion items behind corpus regressions. `rules-engine/src/oracleIRParserUtils.ts` now strips roman-numeral chapter markers even when the separator arrives as `?` / corrupted punctuation instead of a clean dash, and `rules-engine/test/oracleIRExecutor.test.ts` now covers `The Cruelty of Gix`, `Vat Emergence`, `Chainer, Dementia Master`, `Too Greedily, Too Deep`, `Debtors' Knell`, `Gravespawn Sovereign`, `Demon of Dark Schemes`, and `Quest for the Necropolis` on top of the earlier queue slice. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1544 passed`).
- 2026-03-24: Continued the same queue by landing quoted emblem creation as a reusable Oracle IR seam. `rules-engine/src/oracleIR.ts` now carries a `create_emblem` step, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now expand unknown `You get an emblem with "..."` clauses into that step, and `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now create and store controller-owned emblems through the existing `emblemSupport` helpers. Added focused parser/executor regressions for `Liliana, Waker of the Dead` in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1536 passed`).
- 2026-03-24: Continued the same corpus-first graveyard `move_zone` pass by landing a reusable `attach` IR seam for self-attaching reanimation templates. `rules-engine/src/oracleIR.ts` now carries an explicit `attach` step, `rules-engine/src/oracleIRParserPostprocess.ts` plus `rules-engine/src/oracleIRParser.ts` now expand `Necromancy`-style `Put/Return ... onto the battlefield ... and attach this enchantment/equipment/permanent to it` unknown clauses into `move_zone` + `attach`, and the executor now threads newly created battlefield permanent ids through `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts`, `rules-engine/src/oracleIRExecutorZoneOps.ts`, new `rules-engine/src/oracleIRExecutorAttachStepHandlers.ts`, and `rules-engine/src/oracleIRExecutorMainLoop.ts` so the source permanent can deterministically attach to the just-returned creature. Added focused parser/executor regressions for `Necromancy` plus queue-backed executor locks for follow-on already-covered items `Portal to Phyrexia`, `Junji, the Midnight Sky`, and `Beacon of Unrest` in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1534 passed`).
- 2026-03-24: Continued the corpus-first graveyard `move_zone` pass by adding deterministic Aura reanimation support for battlefield-entry attachment wording. `rules-engine/src/oracleIR.ts` now carries optional `battlefieldAttachedTo` metadata on `move_zone` steps, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now parses `attached to this creature` and `attached to a creature you control`, and the single-target battlefield move helpers in `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorZoneOps.ts` now resolve deterministic attachment targets, refuse multi-creature choice cases, and wire both `attachedTo` and the host permanent's `attachments` list when a legal creature-enchanting Aura enters. Added focused parser/executor regressions for `Iridescent Drake` and singleton/choice-bound `Nomad Mythmaker` cases in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1518 passed`).
- 2026-03-24: Continued the same queue by normalizing Saga chapter markers for generic clause parsing, so graveyard recursion clauses prefixed with `II â€”` / `III â€”` now feed the normal `move_zone` parser instead of falling through as `unknown`. `rules-engine/src/oracleIRParserUtils.ts` now strips leading roman-numeral chapter prefixes during clause normalization, and focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts` now cover real queue-backed lines such as `The Eldest Reborn`, `The Cruelty of Gix`, and `Waking the Trolls`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1522 passed`).
- 2026-03-24: Continued the same exact-target graveyard recursion seam by adding criteria support for permanent-class targets and `not named ...` exclusions. `rules-engine/src/oracleIRExecutorZoneOps.ts` now recognizes `permanent`, `legendary permanent`, and `nonlegendary permanent` card families plus single-target `not named <...>` qualifiers, resolving `this permanent` against the current source name during deterministic exact-target checks. Focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` now cover `Staff of Eden, Vault's Key` both for the successful different-name legendary permanent case and the safe-skip same-name exclusion case. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads` (`1524 passed`).
- 2026-03-24: Added a reusable corpus-mined automation queue generator in `tools/build-next-automation-queue.js` and checked in refreshed outputs at `docs/oracle-automation-next-500.md` plus `tools/oracle-automation-next-500.json`. The generator now supports `--count` and seam-priority spillover: it exhausts the active graveyard / recursion families first, then automatically continues into other high-population automation seams (currently token creation, direct damage, draw, sacrifice, +1/+1 counters, life swings, targeted destroy, search, bounce, discard, topdeck manipulation, tap/untap, counterspells, mill, impulse-exile permissions, and smaller fight/goad spillover). Additional generated snapshots now include `docs/oracle-automation-next-2000.md` / `tools/oracle-automation-next-2000.json` and `docs/oracle-automation-next-10000.md` / `tools/oracle-automation-next-10000.json`. Regeneration examples: `node tools/build-next-automation-queue.js`, `node tools/build-next-automation-queue.js --count 2000`, `node tools/build-next-automation-queue.js --count 10000`.
- 2026-03-24: Added explicit support for Skullbriar-style zone-persistent counters so the `counters remain` exception survives both storage and return paths instead of being lost by generic zone-move rebuilds. Added shared retained-counter helpers in `shared/src/zoneRetainedCounters.ts`, wired battlefield-to-zone moves in `rules-engine/src/oracleIRExecutorBattlefieldOps.ts`, `rules-engine/src/rulesEngineAdapterStateSupport.ts`, and the live server battlefield removal / commander-replacement paths in `server/src/state/modules/counters_tokens.ts`, `server/src/state/modules/zones.ts`, `server/src/state/modules/stack.ts`, plus commander cast source tagging in `server/src/socket/commander.ts`. Reentry paths now merge retained Skullbriar counters back onto the battlefield in `rules-engine/src/oracleIRExecutorZoneOps.ts` and `server/src/state/modules/stack.ts` without making normal cards keep counters across zone changes. Added focused regressions in `rules-engine/test/oracleIRExecutor.test.ts` and `server/tests/skullbriar.zone-counters.test.ts`. Validation: `npm run typecheck --workspace=rules-engine`, `npm run typecheck --workspace=server`, `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads` (`1511 passed`), and `npx vitest run server/tests/skullbriar.zone-counters.test.ts --pool=threads`.
- 2026-03-24: Continued the same corpus-first graveyard `move_zone` pass by adding deterministic support for static numeric mana-value-capped exact-target selectors such as `Put target creature card with mana value 4 or less from a graveyard onto the battlefield under your control with a finality counter on it.` (`Coalstoke Gearhulk`). `rules-engine/src/oracleIRExecutorZoneOps.ts` now parses narrow single-target graveyard criteria with optional `mana value N or less` caps, enforces them against the already bound target card via `getCardManaValue(...)`, and keeps dynamic/runtime-dependent caps out of scope for now. Added focused executor regressions in `rules-engine/test/oracleIRExecutor.test.ts` for both the successful capped reanimation case and the higher-mana safe-skip case. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads` (`1513 passed`). Radar: Nim Deathmantle remains a likely follow-up seam because its death-triggered return/equip package will need tighter trigger-payment-resolution support than the current direct `move_zone` helpers provide.
- 2026-03-24: Continued the same corpus-first graveyard `move_zone` pass by adding deterministic battlefield-entry counter metadata for simple `with a/an <counter> counter on it` reanimation wording. `rules-engine/src/oracleIR.ts` now carries optional `withCounters` on `move_zone` steps, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now parses battlefield move modifiers like `Put target creature card from a graveyard onto the battlefield under your control with a corpse counter on it.` (`From the Catacombs`), and the battlefield move helpers in `rules-engine/src/oracleIRExecutorZoneOps.ts` plus `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now thread those counters onto the resulting permanent. Added focused parser/executor regressions in `rules-engine/test/oracleIRParser.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads` (`1510 passed`).
- 2026-03-24: Continued the same graveyard `move_zone` cleanup by covering the remaining harmless target-qualifier phrasing variants without changing the exact-target safety model. `rules-engine/src/oracleIRExecutorZoneOps.ts` now accepts `other target ... from a graveyard` alongside existing optional-target wording, and `rules-engine/test/oracleIRExecutor.test.ts` now validates `Put up to one other target card from a graveyard on the bottom of its owner's library.` (`Hoarding Recluse`) plus tapped exact-target reanimation for `Put target creature card from a graveyard onto the battlefield under your control tapped.` (`Demon of Dark Schemes`). Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Continued the same corpus-first graveyard `move_zone` pass by broadening exact-target coverage for the high-frequency generic and optional-target families, and by fixing a parser split bug for `Return up to one target ... to ...` wording. `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now splits `return ... to ...` clauses on the final destination `to`, and `rules-engine/src/oracleIRExecutorZoneOps.ts` now recognizes the real `instant, sorcery, or creature` graveyard target union. `rules-engine/test/oracleIRExecutor.test.ts` now validates deterministic execution for corpus-backed families such as `Exile target card from a graveyard.` (`Agatha's Soul Cauldron`, `Beckon Apparition`, `Cremate`), `Exile up to one target card from a graveyard.` (`Jack-o'-Lantern`, `Rotten Reunion`, `Territorial Kavu`), `Return target card from a graveyard to its owner's hand.` (`Naya Charm`), `Return up to one target instant, sorcery, or creature card from a graveyard to its owner's hand.` (`Vampire Charmseeker`), `Put up to one target creature or planeswalker card from a graveyard onto the battlefield under your control.` (`Yawgmoth's Vile Offering`), `Put target creature card from a graveyard onto the battlefield under its owner's control.` (`Kenrith, the Returned King`), and `Put up to one target card from a graveyard on the bottom of its owner's library.` (`Swiftgear Drake`) when the exact object binding is already present. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Continued the corpus-guided graveyard `move_zone` pass by locking in the remaining already-supported direct-target type families and extending the parser to accept real `up to one target ... from a graveyard` wording. `rules-engine/src/oracleIRExecutorZoneOps.ts` now recognizes `Saga` and optional-prefix singular graveyard targets, while `rules-engine/test/oracleIRExecutor.test.ts` now validates direct-target execution for real corpus families including `Exile target artifact card from a graveyard.` (`Conversion Chamber`, `Grave Robbers`, `Myr Welder`), `Exile target land card from a graveyard.` (`Deathrite Shaman`), `Exile target nonland card from a graveyard.` (`Moratorium Stone`), `Put target land card from a graveyard onto the battlefield under your control.` (`Restore`, `Waking the Trolls`), `Put target non-Dragon creature card from a graveyard onto the battlefield under your control.` (`Junji, the Midnight Sky`), and `Exile up to one target Saga card from a graveyard.` (`The Many Deeds of Belzenlok`) when the exact object binding is already present. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued shrinking the large `where X is ...` evaluator by extracting the self/reference stat and mana lookup family from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereReferenceStats.ts`. The new helper now owns half-life-total, self/that-object power-toughness, mana value, spell color/mana-spent, named-object stat, and contextual counter-count lookups, while `evaluateModifyPtWhereX(...)` preserves the same matcher order through a single delegation point before extrema handling. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued the same evaluator cleanup by extracting the adjacent player-state and sacrificed-reference matcher cluster from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWherePlayerAndReferenceState.ts`. The new helper now owns sacrificed creature/artifact stat reads, commander-tax and owned-commander mana-value lookups, player life/speed/experience queries, mutation counts, and die-roll result lookup, while `evaluateModifyPtWhereX(...)` keeps the same matcher order by delegating to the helper immediately before qualified-count and reference-stat handling. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued the same evaluator cleanup by extracting the first post-extrema exile/card-stat matcher cluster from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereExileAndCardStats.ts`. The new helper now owns `cards exiled with this/<named permanent>` counting plus the adjacent `greatest/highest power among creature cards` and `greatest/highest mana value among cards` lookups for graveyard / `... this way` runtime contexts, while `evaluateModifyPtWhereX(...)` preserves the same matcher order through a single delegation point immediately after extrema handling. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued the same evaluator cleanup by extracting the remaining late named-reference and point-in-time stat tail from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereLateReferenceStats.ts`. The new helper now owns the late `greatest artifacts an opponent controls`, named-counter, loyalty, selected-player life-difference, energy, damage-tracking, negative-power, and random-range matchers, while `evaluateModifyPtWhereX(...)` now falls through from its helper chain directly into the final safe-skip guards. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued the same evaluator cleanup by extracting the remaining specialized post-extrema maxima block from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereSpecializedExtrema.ts`. The new helper now owns subtype-specific creature-stat maxima, `other attacking` / tapped-opponent creature maxima, and the remaining elemental / artifact / commander / instant-or-sorcery mana-value maxima, leaving the main evaluator almost entirely as ordered helper dispatch plus final safe-skip guards. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Continued the same evaluator cleanup by extracting the mid-file board-resource and counter-reference block from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereBoardStateCounts.ts`. The new helper now owns basic-land-type, party, devotion/color, `counters on this/it/<named object>`, and target-player untapped-land snapshot/count matchers, while `evaluateModifyPtWhereX(...)` preserves the same matcher order through a single delegation point before the zone/turn/player-state helpers. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the last recommended `where X is ...` shrink items in `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` by extracting the remaining early negated/mixed-count prefix into new `rules-engine/src/oracleIRExecutorModifyPtWhereNegatedAndHybridCounts.ts` and activating the already-started tapped/attacking/misc family in new `rules-engine/src/oracleIRExecutorModifyPtWhereMiscCounts.ts`. The evaluator is now essentially ordered helper dispatch plus final safe-skip guards, which should make future automation-focused changes much easier to land without reworking this compatibility-heavy matcher surface. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Continued the same `move_zone` automation expansion by adding singular target-card support for your own graveyard when a direct target binding is already present in execution context. `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` and `rules-engine/src/oracleIRExecutorZoneOps.ts` now deterministically execute `Put target creature card from your graveyard into your hand.`, `Return target creature card from your graveyard to your hand.`, `Exile target creature card from your graveyard.`, and `Put target creature card from your graveyard onto the battlefield.` when `targetCreatureId`/`targetPermanentId` is bound, while preserving the existing safe-skip behavior when no binding is available. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Extended that same singular target-card `move_zone` path to cover your hand and exile when a direct target binding is already present in execution context. `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` and `rules-engine/src/oracleIRExecutorZoneOps.ts` now deterministically execute cases like `Exile target creature card from your hand.`, `Return target creature card from your exile to your hand.`, and `Put target creature card from your exile onto the battlefield.` when `targetCreatureId`/`targetPermanentId` is bound, while preserving the existing safe-skip behavior when no binding is available. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Extended singular direct-target `move_zone` execution from your own zones to target-bound player/opponent zones in `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` and `rules-engine/src/oracleIRExecutorZoneOps.ts`. The executor now deterministically resolves `target ... card from target/that player|opponent's graveyard|hand|exile` when both the player binding (`targetPlayerId`/`targetOpponentId`) and direct object binding (`targetCreatureId`/`targetPermanentId`) are present, while still safe-skipping battlefield moves that do not explicitly say `under your control` or `under their owners' control`, and returning non-persistent `impossible_action` when the bound card is not actually in the bound player's zone. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Started a corpus-guided `move_zone` expansion pass using real AtomicCards phrasing families instead of synthetic templates. `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts`, `rules-engine/src/oracleIRExecutorZoneOps.ts`, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts`, and `rules-engine/src/oracleIRParserSacrificeHelpers.ts` now deterministically resolve direct-target `target ... card from a graveyard` moves when the exact object id is already bound, including common black-border card families such as `Disturbing Plot` / `Another Night in Vegas` (`Return target creature card from a graveyard to its owner's hand.`), `Ashiok, Sculptor of Fears` / `Debtors' Knell` / `Chainer, Dementia Master` (`Put target creature card from a graveyard onto the battlefield under your control.`), `Cemetery Reaper` / `Boneclad Necromancer` (`Exile target creature card from a graveyard.`), plus explicit `Noxious Revival` / `Junktroller` style `Put target card from a graveyard on top of / on the bottom of its owner's library.` placement. Choice-bearing library wording like `on your choice of the top or bottom` still safe-skips. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-24: Continued that same corpus-guided graveyard pass by adding the next recurring direct-target hybrid type families in `rules-engine/src/oracleIRExecutorZoneOps.ts` and locking them with executor regressions in `rules-engine/test/oracleIRExecutor.test.ts`. The direct-target `from a graveyard` path now also handles real card wording such as `Return target artifact or enchantment card from a graveyard to its owner's hand.`, `Return target creature or land card from a graveyard to its owner's hand.`, `Put target artifact or creature card from a graveyard onto the battlefield under your control.`, `Put target creature or planeswalker card from a graveyard onto the battlefield under your control.`, `Exile target instant or sorcery card from a graveyard.`, and `Put target artifact, instant, or sorcery card from a graveyard on the bottom of its owner's library.` when the exact object id is already bound. Validation: `npm run typecheck --workspace=rules-engine` and `npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Split the shared token-creation interface surface out of `rules-engine/src/tokenCreation.ts` into `rules-engine/src/tokenCreationTypes.ts`, leaving the main token module focused on token definitions, parsing, trigger detection, and creation helpers while preserving the existing exports. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/tokenCreation.test.ts --pool=threads`.
- 2026-03-23: Extracted temporary `gets +/-N/+/-N until end of turn` clause parsing from `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserModifyPtClauses.ts`, so `parseEffectClauseToStep(...)` now delegates that self-contained `modify_pt` family before the remaining legacy inline parser branches. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred simple-token duplicate cleanup in `rules-engine/src/oracleIRParser.ts` by retiring the remaining in-file `create token` branch family after `rules-engine/src/oracleIRParserTokenSimpleClauses.ts` was already the active path, and removed the now-unused local token-counter decoder from the parser file. The extracted helper is now the single live parser path for that simple token-creation family, while the old mojibake-heavy inline regex block remains only as staged reference comments for now. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred life/combat duplicate cleanup in `rules-engine/src/oracleIRParser.ts` by retiring the remaining in-file gain-life / lose-life / direct-damage / tap-or-untap branch family after `rules-engine/src/oracleIRParserLifeAndCombatClauses.ts` was already the active path. The extracted helper is now the single live parser path for that clause family, while the old mojibake-heavy inline block remains only as staged reference comments for now. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred duplicate impulse/exile-top loop cleanup in `rules-engine/src/oracleIRParser.ts` by retiring the remaining inline second-pass subject-order impulse / exile-top checks after `rules-engine/src/oracleIRParserImpulseFlow.ts` was already the active path. The helper flow is now the single live parser path for those clauses, while the separate legacy fallback function remains staged for later cleanup. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Split shared casting-restriction enums/interfaces and spell-specific timing/target helper checks out of `rules-engine/src/castingRestrictions.ts` into `rules-engine/src/castingRestrictionsTypes.ts` and `rules-engine/src/castingRestrictionsSpellChecks.ts`, keeping the main module focused on restriction detection, state collection, and runtime enforcement behind the same public API. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/castingRestrictions.test.ts rules-engine/test/gameAutomationVerifier.test.ts --pool=threads`.
- 2026-03-23: Split shared replacement-effect enums/shapes and the pure ETB/application evaluators out of `rules-engine/src/replacementEffects.ts` into `rules-engine/src/replacementEffectsTypes.ts` and `rules-engine/src/replacementEffectsEvaluation.ts`, keeping `replacementEffects.ts` as the parser/collection facade with stable named exports. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/replacementEffects.test.ts --pool=threads`.
- 2026-03-23: Split shared choice-event enums/interfaces out of `rules-engine/src/choiceEvents.ts` into `rules-engine/src/choiceEventsTypes.ts`, leaving `choiceEvents.ts` as the stable factory facade while preserving existing imports. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/choiceEvents.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred `modalTriggeredAbilities.ts` mode-factory seam by moving spree/tiered/escalate/command/entwine construction into `rules-engine/src/modalTriggeredAbilitiesModeFactories.ts`, restoring the public wrappers in `modalTriggeredAbilities.ts`, and hardening modal text parsing against mojibake bullet/em-dash variants so the extracted seam remains behavior-identical. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/modalTriggeredAbilities.test.ts --pool=threads`.
- 2026-03-23: Split shared static-ability enums/shapes and the filter/PT evaluation family out of `rules-engine/src/staticAbilities.ts` into `rules-engine/src/staticAbilitiesShared.ts` and `rules-engine/src/staticAbilitiesEvaluation.ts`, keeping the existing public API intact through re-exports while leaving parsing and battlefield application local for now. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/staticAbilities.test.ts --pool=threads`.
- 2026-03-23: Split the executor entrypoint from its runtime loop by moving the step-application switch and recursive choose-mode/conditional flow into `rules-engine/src/oracleIRExecutorMainLoop.ts`, leaving `rules-engine/src/oracleIRExecutor.ts` as a thin public facade around shared types/context exports. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split Cipher and Ninjutsu support out of `rules-engine/src/modalTriggeredAbilities.ts` into `rules-engine/src/modalTriggeredAbilitiesCipher.ts` and `rules-engine/src/modalTriggeredAbilitiesNinjutsu.ts`, preserving the existing public API through re-exports while shrinking the modal-trigger module's leaf utility surface. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/modalTriggeredAbilities.test.ts --pool=threads`.
- 2026-03-23: Finished the `triggeredAbilities.ts` runtime seam split by moving queue/instance lifecycle into `rules-engine/src/triggeredAbilitiesQueue.ts` and Oracle-resolution execution flow into `rules-engine/src/triggeredAbilitiesExecution.ts`, then collapsing `rules-engine/src/triggeredAbilities.ts` into a thin public facade. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split intervening-if clause resolution and trigger-matching/filter evaluation out of `rules-engine/src/triggeredAbilities.ts` into `rules-engine/src/triggeredAbilitiesInterveningIf.ts` and `rules-engine/src/triggeredAbilitiesMatching.ts`, so the facade no longer owns the pure event-matching path. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split trigger-condition routing out of `rules-engine/src/triggeredAbilities.ts` into `rules-engine/src/triggeredAbilitiesConditionRouting.ts`, centralizing the top-level condition dispatcher around the already-extracted evaluator family. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split Oracle-text trigger parsing out of `rules-engine/src/triggeredAbilities.ts` into `rules-engine/src/triggeredAbilitiesParsing.ts`, leaving the facade focused on runtime trigger orchestration while preserving the same public `parseTriggeredAbilitiesFromText(...)` API. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split shared trigger enums/shapes and reusable trigger template constructors out of `rules-engine/src/triggeredAbilities.ts` into new `rules-engine/src/triggeredAbilitiesTypes.ts` and `rules-engine/src/triggeredAbilitiesTemplates.ts`, leaving `triggeredAbilities.ts` as the public facade plus orchestration/execution layer. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split direct `exile the top ...` / `look at the top ... then exile ...` clause parsing into new `rules-engine/src/oracleIRParserExileTopOnly.ts`, and routed `rules-engine/src/oracleIRParser.ts` through the helper as the active first path while keeping the legacy inline parser as a staged fallback because of the main file's encoding-noise brittleness. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split the pending `exile_top -> impulse_exile_top` follow-up upgrade scan into new `rules-engine/src/oracleIRParserPendingImpulseUpgrade.ts`, so `parseAbilityToIRAbility(...)` now delegates the reminder-skipping permission-window search and step rewrite instead of owning that control-flow block inline. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Restored full active-path parity for the extracted exile-top impulse permission matcher in `rules-engine/src/oracleIRParserEffectImpulsePermission.ts` by re-enabling explicit-subject normalization (`its owner` / `its controller` / `that permanent's owner` families, including `can` variants), preserving target-player `their next turn` windows, and re-adding the `until you exile another card with this ...` duration branch. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Split the subject-order exile-top impulse fallback (`its owner may exile ... They may play ...`) into new `rules-engine/src/oracleIRParserSubjectOrderImpulse.ts`, and routed `rules-engine/src/oracleIRParser.ts` through the helper as the active first path while intentionally keeping the brittle legacy inline fallback in place for now because of the parser file's encoding noise. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred zone/removal parser cleanup in `rules-engine/src/oracleIRParser.ts` by removing the remaining inline destroy/exile/sacrifice/return/put-into/put-onto block after `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` was already the active path. The helper module is now the single source of truth for that zone/removal clause family. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred delayed-battlefield parser cleanup in `rules-engine/src/oracleIRParser.ts` by removing the remaining inline duplicate of the scheduling matrix after `rules-engine/src/oracleIRParserDelayedBattlefieldActions.ts` was already the active path. The helper module is now the single source of truth for next-end-step / upkeep / cleanup / lose-control / leaves-battlefield delayed battlefield parsing. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Finished the deferred `where X is ...` evaluator cleanup in `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` by removing the remaining duplicated inline extrema matcher family after `tryEvaluateModifyPtWhereExtrema(...)` was already the active path. The helper module is now the single source of truth for that highest/lowest mana-value and creature-stat slice, while the evaluator keeps only the non-extrema follow-up handlers. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted plain `create token` clause parsing and token-counter decoding into `rules-engine/src/oracleIRParserTokenSimpleClauses.ts`, and rewired `parseEffectClauseToStep(...)` to route simple token-creation templates through the helper before the legacy inline block. This narrows the remaining parser bulk down further around the more coupled delayed-action and impulse logic. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted deterministic life-change/direct-damage/tap-or-untap clause parsing into `rules-engine/src/oracleIRParserLifeAndCombatClauses.ts`, and rewired `parseEffectClauseToStep(...)` to route those simple combat/life templates through the helper before the legacy inline block. This further trims the parserâ€™s high-churn middle section while keeping the more coupled token and delayed-action logic local for now. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted the top-level deterministic draw/counter/mana/scry/surveil/discard/mill/goad clause family into `rules-engine/src/oracleIRParserSimpleActionClauses.ts`, and rewired `parseEffectClauseToStep(...)` to route through the new helper before the legacy inline block. This trims the high-churn parser prelude while keeping the legacy implementation in place as a staged cleanup fallback. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted zone/removal clause parsing into `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts`, so `parseEffectClauseToStep(...)` now routes destroy/exile/sacrifice/return/put-into/put-onto zone-move handling through a dedicated helper before the legacy inline block. This keeps the active parser path cleaner while preserving behavior during the staged cleanup. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted archetype/deck utility helpers into `rules-engine/src/aiEngineDeckSupport.ts`, moving primary-archetype lookup, combat deck bias scoring, and mana-sink detection out of `AIEngine.ts` so the newer AI support modules can share the same leaf heuristics through thin engine wrappers. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted battlefield-threat analysis and attack-target selection into `rules-engine/src/aiEngineThreatSupport.ts`, moving `assessBattlefieldThreats(...)`, removal-reason labeling, and `selectAttackTarget(...)` logic out of `AIEngine.ts` while preserving the same card-analysis inputs and multiplayer scoring behavior. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted opponent-threat counting and spell-value scoring into `rules-engine/src/aiEngineSpellSupport.ts`, moving the remaining basic cast-evaluation constants and heuristics out of `AIEngine.ts` while keeping battlefield/type helpers injected from the engine. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted sacrifice-choice heuristics into `rules-engine/src/aiEngineSacrificeSupport.ts`, so `AIEngine.ts` now delegates `makeSacrificeDecision(...)` while continuing to use the engine-owned battlefield typing and sacrifice-target ranking helpers. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted aggressive/defensive/control/combo strategy branches into `rules-engine/src/aiEngineStrategySupport.ts`, so `AIEngine.ts` now delegates the higher-level archetype-specific attack/cast/sacrifice routing while preserving its existing helper-driven behavior. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted baseline spell-casting heuristics into `rules-engine/src/aiEngineCastSupport.ts`, so `AIEngine.ts` now delegates `makeBasicCastDecision(...)` while keeping spell-value and opponent-threat evaluation in the engine. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted baseline AI combat heuristics into `rules-engine/src/aiEngineCombatSupport.ts`, moving `makeBasicAttackDecision(...)` and `makeBasicBlockDecision(...)` out of `AIEngine.ts` so the engine now delegates combat attacker/blocker scoring and assignment while preserving existing AI behavior. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted delayed battlefield cleanup scheduling into `rules-engine/src/oracleIRParserDelayedBattlefieldActions.ts`, so `parseEffectClauseToStep(...)` now routes next-end-step / upkeep / cleanup / lose-control / leaves-battlefield delayed sacrifice-exile parsing through a dedicated helper instead of owning that timing matrix inline. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Extracted target-selection leaf logic into `rules-engine/src/aiEngineTargetSupport.ts`, moving player/permanent target ranking and removal-target fallback orchestration out of `AIEngine.ts` while keeping the same decision behavior and test surface. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted triggered-ability choice, combat damage-assignment, and blocker-order leaf decisions into `rules-engine/src/aiEngineResponseSupport.ts`, continuing the AIEngine modularization by moving low-coupling response/combat heuristics out of `AIEngine.ts` while preserving behavior. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted token-choice, modal-choice, and discard leaf decisions into `rules-engine/src/aiEngineChoiceSupport.ts`, further trimming `AIEngine.ts` so it delegates low-coupling choice heuristics instead of embedding them inline. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Introduced `rules-engine/src/oracleIRExecutorModifyPtWhereExtremaHandlers.ts` and routed the evaluator through a dedicated extrema handler entrypoint for the contiguous highest/least mana-value and creature-stat `where X is ...` family, preserving existing behavior while establishing the next cleanup seam for the remaining inline matcher residue. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRParser.test.ts --pool=threads`.
- 2026-03-23: Extracted reusable AI heuristic scoring into `rules-engine/src/aiEngineValueSupport.ts`, moving death-trigger valuation plus permanent/combat/token/mode/card scoring helpers out of `AIEngine.ts` so the engine class stays focused on decision flow. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted AI activated-ability heuristics into `rules-engine/src/aiEngineActivatedAbilitySupport.ts`, moving activated-ability detection, activation-timing checks, heuristic valuation, and best-ability selection out of `AIEngine.ts` while keeping the public AI decision flow unchanged. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/AIEngine.test.ts rules-engine/test/ai.activatedAbilities.test.ts rules-engine/test/goad-ai-integration.test.ts --pool=threads`.
- 2026-03-23: Extracted the shared setup block at the top of `evaluateModifyPtWhereX(...)` into `rules-engine/src/oracleIRExecutorModifyPtWhereContext.ts`, so battlefield snapshots, selector/source-target resolution, last-sacrificed snapshot lookup, and reusable count/stat helper wrappers now come from a dedicated evaluator-context factory instead of being redeclared inline inside `oracleIRExecutorModifyPtWhereEvaluator.ts`. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRParser.test.ts --pool=threads`.
- 2026-03-23: Extracted Oracle IR parser sacrifice/conditional helper logic into `rules-engine/src/oracleIRParserSacrificeHelpers.ts`, moving self-reference aliasing, battlefield-condition parsing, sacrifice-object splitting, leading conditional wrapper parsing, and related clause helpers out of `oracleIRParser.ts` while preserving parser behavior. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleTextParser.test.ts --pool=threads`.
- 2026-03-23: Extracted Oracle IR parser clause-preparation flow into `rules-engine/src/oracleIRParserAbilityClauses.ts`, so `parseAbilityToIRAbility(...)` now delegates the conditional-combine and conservative sacrifice-lead clause shaping pass instead of owning that preprocessing inline. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleTextParser.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads`.
- 2026-03-23: Extracted `RulesEngineAdapter` action routing into `rules-engine/src/rulesEngineAdapterActionDispatch.ts`, moving the large `performAction` type-switch behind a dedicated dispatch helper while preserving the adapter-owned handler closures and post-action state maintenance flow. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/delayedTriggeredAbilities.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/gameAutomation.test.ts --pool=threads`.
- 2026-03-23: Extracted `RulesEngineAdapter` control-loss delayed-trigger support into `rules-engine/src/rulesEngineAdapterDelayedTriggerSupport.ts`, moving active-player/turn-order/controller normalization plus lose-control watcher reconciliation and stack placement out of the main adapter while preserving the existing adapter entrypoint. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/delayedTriggeredAbilities.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Finished the remaining `rules-engine/src/oracleTextParser.ts` preprocessing handoff so the main parser now uses `splitOracleTextIntoParseLines(...)` from `oracleTextParserPreprocess.ts` instead of maintaining its own duplicate line/bullet/continuation splitting loop. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Extracted the `RulesEngineAdapter` state-based action / win-check support slice into `rules-engine/src/rulesEngineAdapterStateSupport.ts`, moving permanent-type detection, graveyard movement, creature/planeswalker death checks, legend-rule handling, aura-detachment checks, win resolution, and pay-life action shaping behind reusable helpers while preserving the adapter API. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/stateBasedActions.test.ts rules-engine/test/gameAutomation.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Split `rules-engine/src/oracleTextParser.ts` support concerns into dedicated helper modules: `oracleTextParserPreprocess.ts` now owns self-reference normalization plus continuation-sentence merging, and `oracleTextParserSupport.ts` now owns mana-targeting/keyword helper logic. `oracleTextParser.ts` remains the compatibility entry point while the duplicated helper implementations were removed from the main file. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleTextParser.test.ts --pool=threads`, and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads`.
- 2026-03-23: Collapsed `rules-engine/src/triggeredAbilitiesOracleIRSupport.ts` into a thin compatibility barrel over `triggeredAbilitiesEventData.ts` and `triggeredAbilitiesChoiceSupport.ts`, removing the duplicated trigger-event-data / choice-support implementation surface while preserving the public exports. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/triggerParsing.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/delayedTriggeredAbilities.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`.
- 2026-03-23: Closed the remaining practical sacrifice-wrapper backlog by adding parser/executor/audit parity for `Bucket List`, `Gargantuan Gorilla`, `Captain Rex Nebula`, and `Cocoon` style clauses. This slice added deterministic `draw one more card` parsing, preserved comma-delimited and `then`-split conditional sacrifice bodies, introduced conservative `remove a <counter> from this <permanent>` execution so `If you can't` fallbacks can key off real impossible actions, evaluated die-roll-equals-mana-value wrapper conditions against runtime state, and refreshed the corpus audit to normalize self-references plus antecedent threading for wrapper conditions. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/oracleIRParser.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, moving the sacrifice snapshot to `716` deterministic / `366` choice-required / `8` unsupported samples, with the remaining residue now limited to silver/acorn/joke cards.
- 2026-03-23: Added a narrow but reusable `If you can't` execution slice for conditional sacrifice wrappers whose antecedent is an exact-count graveyard move (currently exemplified by `Egon, God of Death`). The move-zone executor now distinguishes deterministic success, impossible action, and player-choice-required over-selection for `N cards from your graveyard`, and the conditional executor uses that prior-step outcome to fire the fallback only when the antecedent truly failed. Refreshed sacrifice audit/reporting now thread immediate antecedent context for this same family, moving the sacrifice snapshot to `711` deterministic / `362` choice-required / `17` unsupported samples. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/oracleIRParser.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`.
- 2026-03-23: Synced the sacrifice corpus audit with the newly supported mana-spent conditional-wrapper subfamily in `tools/audit-sacrifice-executor-coverage.js`. The audit now counts `If eight or more mana was spent to cast that spell, sacrifice Tellah and it deals that much damage to each opponent.` as deterministic support instead of leaving it in the unsupported residue, bringing the sacrifice snapshot to `710` deterministic / `362` choice-required / `18` unsupported samples. Validation: `node tools/audit-sacrifice-executor-coverage.js`.
- 2026-03-23: Added a conservative `conditional` Oracle IR wrapper for leading sacrifice-follow-up clauses like `If you don't, sacrifice this artifact and draw three cards.` and `If ... was spent, sacrifice Tellah and it deals ...`, plus executor handling that only resolves wrapped steps when the condition can be safely evaluated and otherwise records a precise conditional automation gap. Followed that with a first practical subfamily: source mana-spent wrappers can now evaluate against source cast metadata, and `that much damage` inside that wrapper resolves to the source's actual mana spent. Validation: `npm.cmd run typecheck --workspace=rules-engine` and `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`. Sacrifice audit totals remain `709` deterministic / `362` choice-required / `19` unsupported because the remaining corpus examples still mostly rely on unsupported wrapper-condition evaluation, but the parser/executor now preserves their structure for targeted follow-up work.
- 2026-03-23: Revalidated the named-self sacrifice conjunction slice and resynced the sacrifice audit/docs to current parser behavior. Unconditional named-self shorthand like `Sacrifice Tellah and it deals 3 damage to each opponent.` now has direct parser regression coverage as deterministic `sacrifice` + `deal_damage`, while leading condition-wrapped variants remain intentionally conservative. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, leaving the current sacrifice snapshot at `709` deterministic / `362` choice-required / `19` unsupported samples. This was a parser/audit parity correction, not an executor regression.
- 2026-03-23: Cleaned up the sacrifice corpus audit classification so non-executor replacement-cost wording (`If this land would enter, sacrifice a Forest/Plains/Swamp instead`), explicit opponent-choice surfaces (`any opponent may sacrifice a creature of their choice`), and quoted self-reference corpus noise no longer inflate the unsupported bucket. Validation: `node tools/audit-sacrifice-executor-coverage.js`, shifting sacrifice audit totals from `722` deterministic / `360` choice-required / `15` unsupported samples to `721` deterministic / `362` choice-required / `6` unsupported samples. This was an audit-accounting correction, not a rules-engine capability regression.
- 2026-03-23: Added deterministic delayed sacrifice support for lose-control watchers (`Sacrifice it/the creature when you lose control of this creature`) by extending Oracle IR parsing/scheduling, delayed-trigger timing, adapter battlefield-diff reconciliation, focused parser/executor/adapter regressions, and sacrifice corpus audit classification. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/delayedTriggeredAbilities.test.ts rules-engine/test/RulesEngineAdapter.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, moving deterministic sacrifice coverage from `720` to `722` cards and reducing unsupported sample count from `17` to `15`.
- 2026-03-23: Added deterministic sacrifice support for delayed cleanup at `the/your next upkeep` and for zero-counter battlefield conditions (`if there are no <counter> counters on it`), with parser/executor/audit parity updates and refreshed sacrifice corpus audit. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/delayedTriggeredAbilities.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, moving deterministic sacrifice coverage from `718` to `720` cards and reducing unsupported sample count from `19` to `17`.
- 2026-03-23: Added narrow deterministic support for condition-gated sacrifice clauses (`if it has mana value N or less/greater` and `if it has N or more/less <counter> counters on it`) across immediate sacrifice and delayed cleanup re-resolution, with false-condition no-op handling preserved and refreshed sacrifice corpus audit. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, moving deterministic sacrifice coverage from `716` to `718` cards and reducing unsupported sample count from `21` to `19`.
- 2026-03-23: Added token provenance tracking to created tokens plus new delayed-cleanup support for `when that token leaves the battlefield` and `at the beginning of the next cleanup step`, with focused parser/executor regressions and refreshed sacrifice corpus audit. Validation: `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, and `node tools/audit-sacrifice-executor-coverage.js`, moving deterministic sacrifice coverage from `709` to `716` cards while keeping delayed-cleanup residuals at `0`.
- 2026-03-23: Synced the sacrifice corpus audit with the existing delayed-cleanup scheduler so timing-qualified cleanup clauses (`... sacrifice it at end of combat`, `... sacrifice them at the beginning of the next end step`, contextual token/creature cleanup variants) now count as deterministic support or explicit player-choice, instead of remaining in a stale delayed-follow-up bucket; refreshed `tools/sacrifice-executor-coverage.json` moved deterministic sacrifice coverage from `547` to `709` cards with delayed-cleanup samples dropping to `0`.
- 2026-03-23: Added attachment-aware sacrifice selector support for both source-attached (`enchanted creature`) and source-anchored attached-object (`an Equipment attached to this permanent` / normalized self-name variants) cases, plus explicit `one or more ...` choice classification; validated with `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `npm.cmd run typecheck --workspace=rules-engine`, and refreshed corpus audit (`tools/sacrifice-executor-coverage.json`) improving deterministic sacrifice coverage from `545` to `547` cards while moving choice-classified cards from `357` to `358`.
- 2026-03-23: Added shorthand self-reference normalization for Oracle parsing (`Endrek Sahr` -> `this permanent` when the source is `Endrek Sahr, Master Breeder`, including similar front-face/title-trim aliases like `Egon`) and extended delayed/contextual sacrifice binding to support `that Vehicle`; validated with `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `npm.cmd run typecheck --workspace=rules-engine`, and refreshed corpus audit (`tools/sacrifice-executor-coverage.json`) improving deterministic sacrifice coverage from `542` to `545` cards.
- 2026-03-23: Added conservative parser splitting for imperative sacrifice-leading conjunction clauses so deterministic self/named-self sacrifice can execute even when the follow-up remains unsupported (`Sacrifice this artifact and draw three cards`, `Sacrifice this enchantment and counter that spell`, `Sacrifice Scavenger Hunt, then open an Attraction`); validated with `npm.cmd run typecheck --workspace=rules-engine`, `npx.cmd vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, and refreshed corpus audit (`tools/audit-sacrifice-executor-coverage.json`) improving deterministic sacrifice coverage from `504` to `542` cards.
- 2026-02-16: Initialized roadmap with 3-layer model and baseline status mapped to current implementation progress.
- 2026-02-16: Hardened direct-context selector normalization for player + battlefield controller filters (including invalid/whitespace controller handling) and added regressions; validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Normalized move-zone controller usage in `applyOracleIRStepsToGameState` (including each-opponent zone traversals and your-zone transfers) and added whitespace-controller regressions; validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Hardened each-opponent move-zone execution to require a valid controller in current state (prevents malformed-controller over-resolution to all players) and added regression coverage; validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Hardened resolution event-data fallback to avoid fabricating `lifeTotal=0` when controller lookup fails (prevents false life-threshold condition matches), with regression coverage in `triggerParsing.test.ts`; validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Hardened resolution turn-state derivation to require a valid controller before computing `isYourTurn`/`isOpponentsTurn` (prevents false turn-condition matches under malformed controller context) while preserving explicit base fallback flags; added regressions in `triggerParsing.test.ts` and validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Tightened trigger target inference so generic `targetId`/`targetIds` cannot pollute `targetPlayerId`/`targetOpponentId` bindings (object/permanent IDs no longer misclassified as player targets), with regressions in `triggerParsing.test.ts`; validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Tightened `affectedPlayerIds` fallback to player-scoped target fields only (excluding generic `targetIds` noise), and preserved multiplayer targeted stack resolution by explicitly threading selected target bindings into spell/ability stack metadata in `RulesEngineAdapter`; added regressions in `triggerParsing.test.ts` and validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Hardened multi-target semantics by enforcing singleton-only scalar inference for `targetPlayerId`/`targetOpponentId` across trigger normalization and hint adaptation (multi-opponent sets remain relational), plus adapter fallback updates to pass selected target sets while only scalar-binding singleton selections; added multiplayer ambiguity regressions in `RulesEngineAdapter.test.ts` and validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Unified singleton+relational target shaping in stack-resolution compatibility paths by converting raw legacy `targets` into `affectedPlayerIds`/`affectedOpponentIds` before normalization in `resolveStack`; added legacy stack regressions for singleton-target bind + multi-target ambiguity in `RulesEngineAdapter.test.ts`, validated with focused rules-engine suite (`oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Completed broad confidence pass after normalization/targeting hardening by running full rules-engine suite (`vitest run`, 97 files / 2894 tests passing) plus `npm run typecheck --workspace=rules-engine`; no regressions detected beyond focused coverage.
- 2026-02-16: Completed cross-workspace integration check via root `npm run build` (server TypeScript build + client Vite production build succeeded), confirming recent rules-engine hardening does not break monorepo build pipeline.
- 2026-02-16: Hardened draw-trigger event shaping in `checkDrawTriggers` to avoid premature opponent sanitization against the drawing player and to thread explicit drawing-target fields; added regression coverage in `triggersHandler.test.ts` and validated with focused suite (`triggersHandler.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Added pronoun-target support for follow-up templates (`that player`, `that opponent`, plus `he or she`/`they`) in Oracle IR parsing and deterministic selector mapping, with executor regressions and draw-trigger integration validation (`triggersHandler.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Expanded pronoun-target deterministic coverage across additional verbs (`draw`, `discard`, `mill`, `lose_life`) with executor regressions to ensure `that player/that opponent` bindings consistently resolve via selector context; validated with focused suite (`oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`, `RulesEngineAdapter.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Expanded adapter integration coverage for pronoun-target templates by adding full cast/activate -> stack -> resolve regressions for `That player loses 1 life.` in multiplayer, confirming stack metadata/hint propagation resolves pronoun targets correctly; validated with focused suite (`RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Added singleton-safety ambiguity regressions for pronoun templates in adapter stack flows (multi-target `That player ...` spell/ability cases do not auto-bind a first target), validating no first-target collapse under plural target sets; focused suite (`RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`) + `npm run typecheck --workspace=rules-engine` remains green.
- 2026-02-16: Final consolidation checkpoint after pronoun-template and ambiguity hardening: full rules-engine suite rerun clean (`vitest run`, 97 files / 2904 tests) with `npm run typecheck --workspace=rules-engine` also clean.
- 2026-02-16: Extended pronoun-target support to raw damage target resolution (`deals ... to that player/that opponent/he or she/they`) and added deterministic executor regressions, validated with focused suite (`oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`, `RulesEngineAdapter.test.ts`) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Added trigger confidence telemetry fields (`oracleStepsSkipped`, `oracleExecutions`) to `TriggerResult` and regression coverage for applied vs skipped deterministic execution paths in `triggersHandler.test.ts`; focused suite + typecheck green.
- 2026-02-16: Implemented full APNAP ranking with multiplayer turn-order input in `putTriggersOnStack` (beyond active/non-active split), wired turn-order forwarding from `processTriggers`, and added APNAP ordering regression in `triggerParsing.test.ts`; focused suite + typecheck green.
- 2026-02-16: Post-milestone broad verification rerun clean (`vitest run`, 97 files / 2908 tests) with `npm run typecheck --workspace=rules-engine` clean.
- 2026-02-16: Expanded opponent-control intervening-if evaluation in `evaluateOpponentControlCondition` to support count thresholds and additional permanent classes (`X or more creatures/artifacts/enchantments/permanents` plus enchantment/permanent presence), with direct condition regressions added in `triggerParsing.test.ts`; validated by focused suite (`triggerParsing.test.ts`, `triggersHandler.test.ts`, `oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, 367 tests passing) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Further expanded opponent-control condition classes to include `land` and `planeswalker` (including `X or more ...` thresholds) in `evaluateOpponentControlCondition`; added regressions in `triggerParsing.test.ts` and validated with focused suite (`triggerParsing.test.ts`, `triggersHandler.test.ts`, `oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, 368 tests passing) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Expanded deterministic selector phrasing coverage for `its controller` across parser/executor paths (including possessive library forms like `its controller's` and raw damage-target text), with parser + executor regressions (`oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`); validated via focused suite (`triggerParsing.test.ts`, `triggersHandler.test.ts`, `oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`, `RulesEngineAdapter.test.ts`, 587 tests passing) + `npm run typecheck --workspace=rules-engine`.
- 2026-02-16: Expanded APNAP sequencing regression matrix with turn-order wrap-around and active-player-missing fallback cases in `triggerParsing.test.ts`, and surfaced trigger telemetry summaries in logs (`[triggers] Oracle auto-execution: executions=..., applied=..., skipped=...`) with regression in `triggersHandler.test.ts`; validated via focused suite + typecheck, then full-suite confidence pass clean (`vitest run`, 97 files / 2914 tests) with `npm run typecheck --workspace=rules-engine` clean.
- 2026-02-16: Completed non-player selector normalization parity hardening by normalizing battlefield permanent controller IDs in object selector filtering (`you` / `opponents`) and rejecting malformed controller IDs for controller-scoped matches; added regressions in `oracleIRExecutor.test.ts` (whitespace controller IDs + malformed-controller exclusion) and validated with focused suite (`RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `triggersHandler.test.ts`, 373 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite confidence rerun clean (`vitest run`, 97 files / 2916 tests).
- 2026-02-16: Added missing support for previously unsupported multi-sentence combat-damage trigger effects by preserving full same-line trigger effect text in `parseTriggeredAbilitiesFromText` (instead of truncating at first period), enabling relational exile-top trigger execution in `dealCombatDamage` adapter flow; added regressions in `triggerParsing.test.ts` and `RulesEngineAdapter.test.ts`, validated with focused suite (`triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`, `triggersHandler.test.ts`, 375 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2918 tests).
- 2026-02-16: Expanded missing trigger-parser support further to preserve multiline trigger effect bodies until the next trigger header (while still splitting adjacent trigger headers correctly), covering newline-separated effect continuations in real oracle layouts; added regressions in `triggerParsing.test.ts`, validated with focused suite (`triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `oracleIRExecutor.test.ts`, `triggersHandler.test.ts`, 377 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2920 tests).
- 2026-02-16: Added deterministic `its owner` selector support across parser/executor paths (`parsePlayerSelector`, exile-until subject parsing, possessive library source mapping, and raw damage-target resolution), enabling context-bound player resolution for owner-referential phrasings; updated parser expectation and added executor regression in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suite (`oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `triggersHandler.test.ts`, 594 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2921 tests).
- 2026-02-16: Added missing deterministic coverage for the `its owner may exile ... from the top of their library` parser branch (including possessive source variants in create+exile split parsing), so owner-referential exile-top templates bind `target_player` consistently instead of falling through to unknown; added parser regression in `oracleIRParser.test.ts` and executor regression in `oracleIRExecutor.test.ts`, validated with focused suite (`oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `triggersHandler.test.ts`, 596 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2923 tests).
- 2026-02-16: Closed residual owner-possessive gaps in the large exile-top parsing path by adding `its owner's`/`its controller's` source support consistently across direct exile, variable-amount exile, and `put ... into exile` variants (including contextual `each of those opponents` branch adjacency); added regression in `oracleIRParser.test.ts` for `that many ... from the top of its owner's library`, validated with focused suite (`oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `triggersHandler.test.ts`, 597 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2924 tests).
- 2026-02-16: Expanded deterministic antecedent-controller phrasing support to include `that creature's controller` / `that permanent's controller` / `that card's controller` across parser selector mapping, impulse-exile subject parsing (including exile-until variants), and raw damage-target resolution in executor; added parser regression in `oracleIRParser.test.ts` and executor regression in `oracleIRExecutor.test.ts`, validated with focused suite (`oracleIRParser.test.ts`, `oracleIRExecutor.test.ts`, `triggerParsing.test.ts`, `RulesEngineAdapter.test.ts`, `triggersHandler.test.ts`, 599 tests passing) + `npm run typecheck --workspace=rules-engine`, then full-suite pass clean (`vitest run`, 97 files / 2926 tests).
- 2026-02-16: Completed antecedent-owner phrasing parity by adding deterministic support for `that creature's owner` / `that permanent's owner` / `that card's owner` across parser selector mapping, impulse-exile subject parsing branches, and executor raw damage-target resolution; added parser regression in `oracleIRParser.test.ts` and executor regression in `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2928 tests).
- 2026-02-16: Expanded antecedent phrase regression coverage to include additional deterministic variants already supported in code (`that creature's owner`, `that card's owner`, `that card's controller`) across parser and raw damage-target executor paths; added parser and executor regressions in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2932 tests).
- 2026-02-16: Closed the remaining complementary antecedent regression pair by adding parser coverage for `that card's owner` impulse-exile subject mapping and executor raw-damage target coverage for `that card's controller`; validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2934 tests).
- 2026-02-16: Generalized antecedent owner/controller selector handling from fixed noun lists to `that <object>'s owner/controller` pattern matching across parser selector mapping, impulse-exile subject branches, and executor raw damage target resolution; added regressions for `that artifact's controller` (`oracleIRParser.test.ts`) and `that enchantment's owner` (`oracleIRExecutor.test.ts`), validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2936 tests).
- 2026-02-16: Added deterministic pronoun-synonym support for `him or her` by normalizing it to existing `he or she` parser paths and extending raw damage target resolution to bind it as `target_player`; added regressions in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2938 tests).
- 2026-02-16: Added missing relational raw-damage target support for `each of those opponents` in executor damage target resolution (binding to `each_of_those_opponents` selector context), with regression coverage in `oracleIRExecutor.test.ts`; validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2939 tests).
- 2026-02-16: Extended mixed deterministic damage target parsing to support relational `each of those opponents` player groups (e.g. `each creature and each of those opponents`) by threading `each_of_those_opponents` through mixed-target execution; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2940 tests).
- 2026-02-16: Added relational selector alias support for bare `those opponents` / `all of those opponents` across parser normalization, player-selector mapping, raw damage target resolution, and mixed deterministic damage target parsing; added regressions in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2942 tests).
- 2026-02-16: Closed possessive relational alias gap in exile-top parsing by supporting `those opponents'` / `all of those opponents'` source forms (plus `each of those opponents'`) in the primary single-clause and impulse-exile parser branches, enabling deterministic `each_of_those_opponents` binding for both `Exile ... of ... libraries` and `Put ... into exile` templates; added regressions in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2944 tests).
- 2026-02-16: Extended face-down combined look+exile parsing to accept relational possessive source aliases (`those opponents'` / `all of those opponents'` / `each of those opponents'`) and map them to `each_of_those_opponents`, enabling deterministic relational execution for templates like `Look at the top card of those opponents' libraries and exile those cards face down`; added regressions in `oracleIRParser.test.ts` + `oracleIRExecutor.test.ts`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2946 tests).
- 2026-02-16: Generalized two-clause face-down look+exile parsing to support both legacy singular (`top card ... exile it`) and quantified/plural object forms (`top N cards ... exile those cards`), while adding relational possessive source aliases (`those opponents'` / `all of those opponents'` / `each of those opponents'`) with `each_of_those_opponents` binding; added parser + executor regressions for `Look at the top card of those opponents' libraries, then exile those cards face down ...`, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2948 tests).
- 2026-02-16: Added explicit alias regression coverage for `all of those opponents` / `all of those opponents'` across selector parsing, possessive exile-top parsing, and executor relational resolution (`deal_damage` + `exile_top`) to lock parity with existing `those opponents` handling; validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2952 tests).
- 2026-02-16: Reduced parser drift risk by centralizing relational alias detection into `isThoseOpponentsSelector` / `isThoseOpponentsPossessiveSource` helpers and replacing duplicated inline `startsWith(...)` chains across exile-top and face-down parsing branches (no behavior change intended); validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2952 tests).
- 2026-02-16: Extended relational alias regression coverage to include the compact `all those opponents` player phrase and mixed-target phrasing `each creature and all of those opponents` in executor deterministic damage paths, plus parser selector coverage for `all those opponents`; validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2955 tests).
- 2026-02-17: Extended parser support for exile-until subject aliases by accepting relational variants (`each of those opponents` / `those opponents` / `all of those opponents` / `all those opponents`) in the `... exiles cards from the top of their library until ...` branch and mapping them to `each_of_those_opponents`; added parser regressions for `each of those opponents` + `all those opponents` exile-until templates, validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2957 tests).
- 2026-02-17: Completed exile-until alias parity coverage by adding parser regressions for the remaining relational subject variants (`those opponents` and `all of those opponents`) in `... exiles cards from the top of their library until ...` templates, ensuring all supported alias forms are lock-tested; validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2959 tests).
- 2026-02-17: Reduced executor drift risk by centralizing relational alias checks into `isThoseOpponentsSelector(...)` and replacing duplicated inline string checks in deterministic damage-target parsing (`resolvePlayersFromDamageTarget`, `parseDeterministicMixedDamageTarget`) with no intended behavior change; validated with focused suites (`vitest test/oracleIRParser.test.ts`, `vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2959 tests).
- 2026-02-17: Expanded deterministic raw damage-target support to treat `defending player` / `the defending player` as `target_opponent` in `resolvePlayersFromDamageTarget`, with focused regressions added in `oracleIRExecutor.test.ts`; validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2961 tests).
- 2026-02-17: Extended mixed deterministic damage-target parsing/execution to treat `defending player` / `the defending player` (and context-bound singleton aliases like `that player` / `that opponent`) as valid player parts when combined with battlefield selectors (for example, `each creature and defending player`), with regressions added in `oracleIRExecutor.test.ts`; validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2963 tests).
- 2026-02-17: Added explicit mixed-target regression coverage for context-bound singleton player aliases `that player` and `that opponent` (e.g., `each creature and that player`) in `oracleIRExecutor.test.ts` to lock deterministic mixed parsing/execution parity with direct damage-target handling; validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2965 tests).
- 2026-02-17: Closed non-damage defending-player gap by extending player-selector parsing and gain/lose-life subject matching to accept `defending player` / `the defending player` as `target_opponent`, with executor regressions for `Defending player loses 1 life.` and `The defending player loses 1 life.` in `oracleIRExecutor.test.ts`; validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2967 tests).
- 2026-02-17: Extended draw subject parsing to accept `defending player` / `the defending player` in deterministic player-subject draw templates, enabling target-opponent context binding for `Defending player draws a card.`; added executor regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2969 tests).
- 2026-02-17: Extended mill subject parsing to accept `defending player` / `the defending player` in deterministic player-subject mill templates, enabling target-opponent context binding for `Defending player mills a card.`; added executor regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2971 tests).
- 2026-02-17: Extended discard subject parsing to accept `defending player` / `the defending player` across deterministic discard patterns (`discards ...`, `discards ... hand`, `discards all cards in ... hand`), enabling target-opponent context binding for `Defending player discards a card.`; added executor regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2973 tests).
- 2026-02-17: Extended `scry`/`surveil` subject parsing to accept `defending player` / `the defending player` in deterministic player-subject templates, and added executor regressions that validate target-opponent context binding in no-choice empty-library cases (`Defending player scries 1.` / `The defending player surveils 1.`); validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2975 tests).
- 2026-02-17: Extended deterministic `add_mana` subject parsing to accept `defending player` / `the defending player`, enabling target-opponent context binding for templates like `Defending player adds {G}.`; added executor regressions in `oracleIRExecutor.test.ts` and validated with focused parser+executor suites (`vitest test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2977 tests).
- 2026-02-17: Extended deterministic `sacrifice` subject parsing to accept `defending player` / `the defending player`, enabling target-opponent context binding for templates like `Defending player sacrifices a creature.`; added executor regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2979 tests).
- 2026-02-17: Extended deterministic `create_token` subject parsing to accept `defending player` / `the defending player` in both single-token and multi-token parser branches, enabling target-opponent context binding for templates like `Defending player creates a Treasure token.`; added executor regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`vitest test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2981 tests).
- 2026-02-17: Extended impulse exile subject parsing to accept `defending player` / `the defending player` in third-person `... may exile ... from the top of their library` and `... exiles cards from the top ... until ...` branches (including both primary and pending-upgrade parser paths), with parser regressions in `oracleIRParser.test.ts`; validated with focused parser+executor suites (`vitest test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2983 tests).
- 2026-02-17: Added possessive-source normalization for `the defending player's` phrasing in clause preprocessing (mapped to `target opponent's`) so deterministic exile-top/impulse templates bind `target_opponent` across parser paths without branch drift; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2985 tests).
- 2026-02-17: Extended subject-order deterministic exile-top parsing to accept `defending player` / `the defending player` in `... exiles the top card(s) of their library` and `... puts the top card(s) of their library into exile` branches (in both primary and fallback parse paths), and switched those branch mappings to `parsePlayerSelector(...)` to reduce selector drift; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2987 tests).
- 2026-02-17: Closed standalone face-down relational alias drift in `tryParseExileTopOnly` by extending `look at the top ... and exile ... face down` source matching to include `each of those opponents'` / `those opponents'` / `all of those opponents'` / `all those opponents'` and mapping those forms to `each_of_those_opponents`; added parser + executor regressions for the standalone no-permission template in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2989 tests).
- 2026-02-17: Closed split-clause face-down impulse drift by expanding the 2-clause `look at the top card ...` + `then exile ... face down` parser path to accept relational possessive aliases (`each of those opponents'` / `those opponents'` / `all of those opponents'` / `all those opponents'`) and plural second-clause refs (`them` / `those cards` / `the cards`), mapping relational forms to `each_of_those_opponents`; added parser + executor regressions for the split-clause template in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2991 tests).
- 2026-02-17: Extended the split-clause face-down impulse path to support quantified look clauses (`look at the top N cards ... . Then exile those cards face down ...`) while preserving deterministic selector binding for target-opponent and relational sources; added parser + executor regressions for the top-two split-clause template in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2993 tests).
- 2026-02-17: Added standalone no-permission regression coverage for split-clause quantified face-down exile (`look at the top N cards ... . Then exile those cards face down.`) to lock parity between `impulse_exile_top` and `exile_top` parsing/execution paths; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2995 tests).
- 2026-02-17: Expanded split-clause top-N look+exile parsing parity to also accept no-face-down second clauses (`Then exile those cards.`) in both impulse-upgrade and standalone no-permission paths, preventing parser drift between `impulse_exile_top` and `exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`vitest run test/oracleIRParser.test.ts`, `vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`vitest run`, 97 files / 2999 tests).
- 2026-02-17: Closed remaining single-clause look+exile drift by allowing combined templates to parse with or without `face down` in both impulse-upgrade (`look ... and exile ... . You may play ...`) and standalone no-permission (`look ... and exile ...`) paths, preserving selector parity across target-opponent and relational possessive sources; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3003 tests).
- 2026-02-17: Closed generic player-subject drift by extending deterministic effect-clause subject matching (draw/add_mana/scry/surveil/discard/mill/gain_life/lose_life/create_token/sacrifice and multi-create prefix) to accept `its owner` / `its controller` / `that <object>'s owner|controller`, aligning clause regexes with existing `parsePlayerSelector` support; added parser + executor regressions for `That creature's owner draws a card.` in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3005 tests).
- 2026-02-17: Closed cross-ability loose impulse upgrade drift for owner/controller third-person permission clauses by allowing global loose permission upgrades to apply to single-card `exile_top` seeds (amount = 1) when permission text is singular (`it` / `that card` / equivalent), enabling templates like `Target player exiles the top card ... Its owner may cast it this turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3007 tests).
- 2026-02-17: Extended cross-ability loose impulse upgrade parity for fixed plural exile-top seeds by allowing global loose permission upgrades to apply to numeric `exile_top` amounts greater than 1 when permission text is plural-referential (`them` / `those cards` / exiled-cards variants), enabling templates like `Target player exiles the top two cards ... Its owner may cast them this turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3009 tests).
- 2026-02-17: Closed loose-impulse phrasing parity gap for `spell(s) they exiled this way` by extending global loose permission object references and plural-upgrade gating to recognize those variants, enabling cross-ability owner/controller templates like `Target player exiles the top two cards ... Its owner may cast spells they exiled this way this turn.` to deterministically upgrade/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3011 tests).
- 2026-02-17: Closed remaining single-card loose-upgrade gating gap by allowing singular cross-ability permissions that reference `the card they exiled this way` / `the spell they exiled this way` to upgrade numeric-1 `exile_top` seeds (owner/controller third-person forms), enabling templates like `Target player exiles the top card ... Its owner may cast the spell they exiled this way this turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3013 tests).
- 2026-02-17: Closed cross-ability loose duration-phrase drift by extending global loose permission parsing to accept `until the end of this turn` / `until the end of that turn` (in addition to end-of-turn short forms), enabling templates like `Target player exiles the top card ... Its owner may cast it until the end of this turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3015 tests).
- 2026-02-17: Closed remaining cross-ability loose duration-phrase gap for `through end of turn` wording by extending global loose permission parsing to accept `through (the) end of (this|that) turn`, enabling templates like `Target player exiles the top card ... Its owner may cast it through end of this turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3017 tests).
- 2026-02-17: Extended cross-ability loose impulse duration coverage to explicit next-turn windows by adding support for `during (your|their|the) next turn`, `until (your|their|the) next turn`, and `until/through end of (your|their|the) next turn` in global loose permission parsing; this enables owner/controller third-person templates like `Target player exiles the top card ... Its owner may cast it during their next turn.` / `... until the end of their next turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3021 tests).
- 2026-02-17: Extended cross-ability loose impulse duration coverage to next-upkeep and next-end-step windows by adding support for `until (the beginning of) (your|their|the) next upkeep` and `until (the beginning of) (your|their|the) next end step` in global loose permission parsing; this enables owner/controller third-person templates like `Target player exiles the top card ... Its owner may cast it until the beginning of their next upkeep.` / `... until your next end step.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3025 tests).
- 2026-02-17: Closed remaining cross-ability loose duration alias gap for `through` next windows by extending global loose permission parsing to accept `through (your|their|the) next turn`, `through (the beginning of) (your|their|the) next upkeep`, and `through (the beginning of) (your|their|the) next end step` (mapped to existing `until_next_*` durations), enabling templates like `Target player exiles the top card ... Its owner may cast it through their next turn.` / `... through your next upkeep.` / `... through your next end step.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3031 tests).
- 2026-02-17: Closed cross-ability loose duration gap for `until end of combat on (your|their|the) next turn` by extending global loose permission parsing to map that wording to `until_end_of_combat_on_next_turn`, enabling templates like `Target player exiles the top card ... Its owner may cast it until end of combat on their next turn.` to deterministically parse/execute as `impulse_exile_top`; added parser + executor regressions in `oracleIRParser.test.ts` and `oracleIRExecutor.test.ts`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test --workspace=rules-engine -- --run`, 97 files / 3033 tests).
- 2026-02-17: Closed cross-ability loose leading-duration owner/controller gap by adding direct leading-duration global-loose permission parsing for explicit-subject forms (for example, `Until your next turn, its owner may cast it.` and `Until your next end step, its owner may cast it.`), ensuring deterministic upgrade of single-card `exile_top` seeds into `impulse_exile_top` without relying on clause rewrite ordering; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts`, `npx vitest run test/oracleIRExecutor.test.ts`), `npm run typecheck --workspace=rules-engine`, and full-suite pass clean (`npm run test -- --run`, 97 files / 3037 tests).
- 2026-02-17: Added regression parity for leading-duration owner **play** permission (`Until your next turn, its owner may play it.`) in cross-ability loose impulse upgrade coverage, including executor validation that a land card receives owner-scoped play permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`) and local `rules-engine` typecheck (`npm run typecheck`).
- 2026-02-17: Added regression parity for leading-duration controller **play** permission (`Until your next turn, its controller may play it.`) in cross-ability loose impulse upgrade coverage, including executor validation that a land card receives controller-scoped play permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added regression parity for leading-duration controller **cast** permission (`Until your next turn, its controller may cast it.`) in cross-ability loose impulse upgrade coverage, including executor validation that a nonland exiled card receives controller-scoped cast permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added regression parity for leading-duration next-end-step controller **cast** permission (`Until your next end step, its controller may cast it.`) in cross-ability loose impulse upgrade coverage, including executor validation that a nonland exiled card receives controller-scoped cast permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added regression parity for controller **play** permission with trailing `through your next end step` duration (`Its controller may play it through your next end step.`) in cross-ability loose impulse upgrade coverage, including executor validation that a land card receives controller-scoped play permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added regression parity for controller **cast** permission with trailing `through your next upkeep` duration (`Its controller may cast it through your next upkeep.`) in cross-ability loose impulse upgrade coverage, including executor validation that a nonland exiled card receives controller-scoped cast permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added regression parity for controller **cast** permission with trailing `during their next turn` duration (`Its controller may cast it during their next turn.`) in cross-ability loose impulse upgrade coverage, including executor validation that a nonland exiled card receives controller-scoped cast permission with the expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added batched controller **cast** duration parity regressions for trailing phrases `through their next turn`, `until the end of their next turn`, `until the beginning of their next upkeep`, and `until end of combat on their next turn` (target-player loose impulse templates), including executor validations that nonland exiled cards receive controller-scoped cast permissions with expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added further batched controller **cast** parity regressions for missing same-turn and trailing-next-end-step phrasings (`this turn`, `until the end of this turn`, `through end of this turn`, `until your next end step`, `through your next end step`) in target-player loose impulse templates, including executor validations that nonland exiled cards receive controller-scoped cast permissions with expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added batched owner **play** duration parity regressions for target-player loose impulse templates (`during their next turn`, `until the end of their next turn`, `through your next upkeep`, `through your next end step`), including executor validations that exiled lands receive owner-scoped play permissions with expected `playableUntilTurn`; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added batched controller **play** duration parity regressions for target-player loose impulse templates (`during their next turn`, `until the end of their next turn`, `through your next upkeep`), complementing existing controller play coverage (`until your next turn`, `through your next end step`); executor validations confirm exiled lands receive controller-scoped play permissions with expected `playableUntilTurn`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added a second controller **play** duration parity batch for target-player loose impulse templates (`through their next turn`, `until the beginning of their next upkeep`, `until your next end step`) to close remaining owner/controller `play` phrasing gaps; executor validations confirm exiled lands receive controller-scoped play permissions with expected `playableUntilTurn`, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`).
- 2026-02-17: Added owner **play** duration parity regressions for target-player loose impulse templates (`through their next turn`, `until the beginning of their next upkeep`, `until your next end step`) and added leading `Until your next end step, ... may play it` coverage for both owner/controller subjects; confirmed parser/executor parity and duration-token conventions (`through ... next turn` => `until_next_turn`) with focused suites green (`651/651`) plus clean package typecheck.
- 2026-02-17: Extended loose impulse permission parsing to accept `can` as an alias for `may` (explicit-subject normalization + leading/trailing duration templates in the shared helper), and added target-player regressions for `Its owner can cast it this turn` and `Its controller can play it until your next end step` across parser/executor; focused suites green (`655/655`) and typecheck clean.
- 2026-02-17: Expanded `can` alias parity for leading-duration target-player templates by adding owner/controller regressions for `Until your next turn, ... can cast/play it` across parser/executor (owner can-cast, owner can-play, controller can-play, controller can-cast); focused suites remain green (`663/663`) and package typecheck remains clean.
- 2026-02-17: Added trailing-duration `can` alias parity regressions for controller cast-permission templates (`can cast it through your next upkeep`, `can cast it during their next turn`, `can cast it until the beginning of their next upkeep`) in target-player loose impulse parsing/execution; focused suites remain green (`669/669`) and typecheck remains clean.
- 2026-02-17: Added trailing-duration `can` alias parity regressions for owner/controller play-permission templates (`can play it during their next turn`, `can play it through your next upkeep`, `can play it until the beginning of their next upkeep`) in target-player loose impulse parsing/execution; focused suites remain green (`681/681`) and typecheck remains clean.
- 2026-02-17: Added end-step/next-turn `can` alias parity regressions for owner/controller cast-permission templates (`can cast it until your next end step`, `can cast it through your next end step`, `can cast it through their next turn`, `can cast it until the end of their next turn`) in target-player loose impulse parsing/execution; focused suites remain green (`697/697`) and typecheck remains clean.
- 2026-02-17: Added end-step/next-turn `can` alias parity regressions for owner/controller play-permission templates (`can play it until your next end step`, `can play it through your next end step`, `can play it through their next turn`, `can play it until the end of their next turn`) in target-player loose impulse parsing/execution; focused suites remain green (`711/711`) and typecheck remains clean.
- 2026-02-17: Added first-person loose impulse `can` alias regressions for `You can play it until your next end step`, `You can play it through your next end step`, and `You can cast it this turn` across parser/executor (anchored to existing `You may ...` templates); focused suites remain green (`717/717`) and typecheck remains clean.
- 2026-02-17: Added first-person trailing next-turn `can` alias parity regressions for `You can play that card until end of your next turn` and `You can play that card until end of the next turn` across parser/executor (anchored next to existing `You may ... until end of ... next turn` coverage); focused suites remain green (`721/721`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded first-person `may cast` next-turn regression parity for `Until the end of your next turn, you may cast that card.` (seen in AtomicCards-style templates such as Hurl Through Hell / Retrieve Prey), with matching parser/executor coverage; focused suites remain green (`723/723`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded leading next-turn pronoun-parity regressions for `Until the end of your next turn, you may play them.` (seen in AtomicCards templates such as Season of the Bold mode text), with parser/executor coverage for top-two exile-top impulse behavior; focused suites remain green (`725/725`) and typecheck remains clean.
- 2026-02-17: Added executor parity regression for the corpus-grounded Lidless Gaze-style next-turn mana rider template (`Exile the top card of each player's library. Until the end of your next turn, you may play those cards, and mana of any type can be spent to cast those spells.`), aligning executor coverage with existing parser support; focused suites remain green (`726/726`) and typecheck remains clean.
- 2026-02-17: Added executor parity regression for the corpus-grounded target-opponent choose-one mana-rider template (`Exile the top three cards of target opponent's library. Choose one of them. Until the end of your next turn, you may play that card, and you may spend mana as though it were mana of any color to cast it.`), matching existing parser coverage for the AtomicCards `You Find a Cursed Idol`-style wording; focused suites remain green (`727/727`) and typecheck remains clean.
- 2026-02-17: Added parser+executor parity regressions for the exact corpus-grounded `Hurl Through Hell` next-turn cast + mana-rider wording (`Exile target creature. Until the end of your next turn, you may cast that card and you may spend mana as though it were mana of any color to cast that spell.`), anchoring `cast that spell` rider handling to real-card template usage; focused suites remain green (`729/729`) and typecheck remains clean.
- 2026-02-17: Added persistent-duration cast+rider parity regressions for corpus-grounded `for as long as it remains exiled` templates by covering `... you may cast that card for as long as it remains exiled, and mana of any type can be spent to cast that spell` in look-then-exile target-player flows (aligned with real card families such as Gonti/Hostage Taker/The Magic Bandit); focused parser+executor suites remain green (`731/731`) and typecheck remains clean.
- 2026-02-17: Extended corpus-grounded persistent-duration mana-rider parity with explicit `that player's library` play-template coverage (`Look at the top card of that player's library, then exile it face down. You may play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.`) across parser + executor, including context-bound executor validation via `selectorContext.targetPlayerId`; focused suites remain green (`733/733`) and typecheck remains clean.
- 2026-02-17: Added parser+executor parity regressions for the exact corpus-grounded `Thought-String Analyst` singular template (`At the beginning of your upkeep, exile the top card of target opponent's library face down. You lose life equal to its mana value. You may look at and play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.`), validating persistent-duration look+play+rider handling with intervening deterministic clause; focused suites remain green (`735/735`) and typecheck remains clean.
- 2026-02-17: Added parser+executor parity regressions for the exact corpus-grounded `Gonti, Night Minister` look-then-exile sentence (`... look at the top card of that player's library, then exile it face down. You may play that card for as long as it remains exiled, and mana of any type can be spent to cast that spell.`), including executor context-bound target-player resolution; focused suites remain green (`737/737`) and typecheck remains clean.
- 2026-02-17: Added executor parity regression for the corpus-grounded plural face-down look+exile mana-rider template (`Look at the top two cards of target opponent's library and exile those cards face down. You may play those cards for as long as they remain exiled, and mana of any type can be spent to cast them.`), aligning executor coverage with existing parser support; focused parser+executor suites remain green (`738/738`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded executor parity regression for the exact `Dead Man's Chest`-style unknown-amount template (`... exile cards equal to its power from the top of its owner's library ... cast spells from among those cards for as long as they remain exiled, and mana of any type can be spent to cast them.`), locking safe-skip behavior when amount is non-deterministic; focused parser+executor suites remain green (`739/739`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded executor parity regression for exact `Transforming Flourish` wording (`... its controller exiles cards from the top of their library until they exile a nonland card, then they may cast that card without paying its mana cost.`), locking deterministic safe-skip behavior for non-deterministic exile-until amounts while preserving target-player selector-context binding; focused parser+executor suites remain green (`740/740`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded executor parity regression for exact `Possibility Storm` wording (`... exiles cards from the top of their library until they exile a card that shares a card type with it. That player may cast that card without paying its mana cost ...`), locking deterministic safe-skip behavior for non-deterministic exile-until amounts with target-player selector-context binding; focused parser+executor suites remain green (`741/741`) and typecheck remains clean.
- 2026-02-17: Added corpus-grounded executor parity regression for exact `Chaos Wand` activated wording (`Target opponent exiles cards from the top of their library until they exile an instant or sorcery card. You may cast that card without paying its mana cost ...`), locking deterministic safe-skip behavior for non-deterministic exile-until amounts with `target_opponent` selector-context binding; focused parser+executor suites remain green (`742/742`) and typecheck remains clean.
- 2026-02-17: Implemented deterministic executor loop support for inferable unknown `until ...` exile amounts in `impulse_exile_top`/`exile_top` (currently supports `until ... nonland card`, `until ... instant or sorcery card`, `until ... legendary card`, and `until ... total mana value N or greater`), with regression updates/additions proving applied behavior for exact corpus templates `Transforming Flourish`, `Chaos Wand`, and `Dream Harvest` while preserving safe-skip for unsupported `until` criteria (for example, `shares a card type with it`); focused parser+executor suites remain green (`743/743`) and typecheck remains clean.
- 2026-02-17: Added executor parity regression for exact `Wand of Wonder` wording (`Each opponent exiles cards from the top of their library until they exile an instant or sorcery card ...`) to lock multi-opponent loop behavior under the new deterministic unknown-`until` amount support; focused parser+executor suites remain green (`744/744`) and typecheck remains clean.
- 2026-02-17: Extended deterministic unknown-`until` loop support to `until ... exile a card that shares a card type with it` by threading `spellType` through stack trigger metadata â†’ Oracle IR execution hints (`referenceSpellTypes`) and resolving per-player loop counts in executor; added regressions for both safe-skip (missing reference spell type) and applied behavior (reference type present) on exact `Possibility Storm` wording, plus context-normalization coverage in `buildOracleIRExecutionContext`; focused parser+executor suites remain green (`746/746`) and typecheck remains clean.
- 2026-02-17: Added executor parity regression for exact `Bismuth Mindrender` wording (`that player exiles cards ... until they exile a nonland card`), validating target-player loop execution under deterministic unknown-`until` support and preserving cast-permission flow; focused parser+executor suites remain green (`747/747`) and typecheck remains clean.
- 2026-02-17: Added deterministic support for Chaos-Wand/Possibility-Storm style bottom-of-library cleanup after impulse exiling (`Then put the exiled cards that weren't cast this way on the bottom ...` / `Then put all cards exiled ... on the bottom ...`), including parser-split fallback detection by impulse-step shape when trailing rider text is in a separate sentence; updated regressions now verify both hit and no-hit Chaos Wand outcomes (if no instant/sorcery is found, all revealed/exiled cards are returned to library bottom) and Possibility Storm applied path cleanup; focused parser+executor suites remain green (`748/748`) and typecheck remains clean.
- 2026-02-17: Added Trepanation-Blade-style reveal-until-land support by parsing `... reveals cards from the top of their library until they reveal a land card` into deterministic `mill` with unknown-until amount and implementing corresponding executor loop resolution for `until ... reveal a land card`; added parser and executor regressions proving target-opponent context binding and correct graveyard movement of all revealed cards; focused parser+executor suites remain green (`750/750`) and typecheck remains clean.
- 2026-02-17: Completed Trepanation full-effect deterministic path by introducing/using `modify_pt_per_revealed` execution (`The creature gets +1/+0 ... for each card revealed this way`) and adding conservative cross-ParsedAbility merge for parser-split trailing static `revealed this way` clauses into the preceding triggered reveal-until ability; added parser+executor regressions for pump-step parsing/application (including equipped-creature target resolution via source attachment), validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `754/754`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Introduced composable temporary P/T Oracle IR execution via new generic `modify_pt` step (target + base `power/toughness` delta + `duration` + optional `scaler`) and migrated Trepanation parsing to use `scaler: per_revealed_this_way` while adding direct Giant-Growth-style parsing/execution (`Target creature gets +3/+3 until end of turn`); executor now resolves deterministic creature targets for `target creature` and applies reusable end-of-turn P/T modifiers through shared logic, with focused parser+executor suites green (`756/756`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Generalized composable P/T clause parsing to explicitly treat trailing `for each ...` as scaler metadata on `modify_pt` (known scaler `per_revealed_this_way` plus captured `unknown` scaler raw text for forward compatibility), and updated executor to safely skip unsupported unknown scalers while still applying deterministic known scalers; added parser+executor regressions for unknown-scaler capture/skip behavior, with focused suites green (`758/758`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Hardened intervening-if handling across trigger pipelines by normalizing clause resolution from `interveningIfClause` with a safe fallback to `condition` when `hasInterveningIf` is set, applying that logic consistently at trigger-time checks (`checkTrigger` / `createTriggerInstance`) and auto-resolution checks (`processEventAndExecuteTriggeredOracle`), and adding stack-resolution safety in `RulesEngineAdapter` to skip execution when intervening-if is flagged but clause data is missing; added regressions in `triggerParsing.test.ts`, `triggersHandler.test.ts`, and `RulesEngineAdapter.test.ts`, validated with focused suites (`124/124`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Extended clause-role parsing for composable `modify_pt` steps to capture condition metadata (`if ...`, `as long as ...`, `where ...`) alongside existing duration/scaler parts, while preserving deterministic safety by skipping unsupported condition-backed modifiers in executor until condition evaluation is wired; added parser+executor regressions for condition capture and safe-skip behavior, validated with focused suites (`762/762`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Enabled first deterministic execution subset for clause-role `modify_pt` conditions by evaluating `if/as long as you control ...` (including `N or more` counts for creature/artifact/enchantment/land/planeswalker/permanent/nonland permanent) against current battlefield controller state, while keeping `where ...` conditions as explicit safe-skip; added executor regressions for true-condition apply path and where-clause skip path, with focused parser+executor suites green (`764/764`) and `npm run typecheck` clean in `rules-engine`.
- 2026-03-23: Added pre-sacrifice last-known-information snapshots to Oracle sacrifice execution and reused the shared value evaluator for `gain/lose life equal to ...`, so downstream sacrificed-creature power/toughness/mana-value lookups read battlefield state before zone change rather than printed graveyard values; validated with focused parser/executor coverage plus `npm.cmd run typecheck --workspace=rules-engine`.
- 2026-02-17: Added deterministic `where` evaluation support for composable `modify_pt` by resolving `where X is the number of <class> you control` (same supported class set as control-condition checks) and applying parsed `+X/-X` P/T components via signed X coefficients (`powerUsesX` / `toughnessUsesX`), while preserving safe-skip for unsupported `where` expressions; added parser+executor regressions for X-component parsing, supported apply paths, and unsupported-where skip behavior, with focused suites green (`767/767`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Expanded deterministic `where` evaluator coverage for composable `modify_pt` to include additional real-template forms: `X is the number of <classes> your opponents control`, `X is the number of opponents you have`, `X is the number of cards in your graveyard/hand/library/exile`, `X is half your life total (rounded up/down)`, `X is that creature's power/toughness`, `X is the number of counters on this/that creature`, `X is the greatest mana value among permanents you control`, and `X is the number of cards exiled with this permanent`; retained explicit safe-skip for still-unsupported expressions (for example, `cards named ...` qualifiers). Added/updated focused regressions in `oracleIRExecutor.test.ts`; final focused parser+executor suites green (`773/773`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Further expanded deterministic `where` support for composable `modify_pt` with arithmetic wrappers and broader count sources: `X is one/N plus ...`, `X is twice ...`, `X is half the ... (rounded up/down)`, typed zone-card counts such as `X is the number of creature cards in your graveyard`, and global battlefield counts such as `X is the number of artifacts on the battlefield`; also added pronoun-style aliases (`its power/toughness`, `this creature's power/toughness`) for target-bound lookups. Added focused executor regressions for these forms; parser+executor focused suites green (`777/777`) and `npm run typecheck` clean in `rules-engine`.
- 2026-02-17: Added deterministic support for `Wand of Wonder` shuffle-rest rider (`... then shuffles the rest into their library`) by returning non-hit exiled cards to library after `until instant or sorcery` loop resolution; includes parser-split fallback detection by impulse-step shape (`each_opponent` + during-resolution cast-permission + instant/sorcery until-criterion), with updated regression proving only the hit card remains exiled; focused parser+executor suites remain green (`750/750`) and typecheck remains clean.
- 2026-02-17: Performed targeted post-Chaos audit for recently implemented corpus templates; identified and documented remaining partial fidelity scope: Trepanation Blade's power pump sentence (`+1/+0 for each card revealed this way`) is still not executed as a deterministic combat stat modifier, and randomization semantics (`random order`/`shuffles`) remain deterministic approximations in current automation paths.
- 2026-02-17: Added explicit parser+executor parity regressions for exact `Undercity Informer` activated Oracle wording (`Target player reveals cards from the top of their library until they reveal a land card, then puts those cards into their graveyard.`), confirming deterministic target-player reveal-until-land milling works under activation context; focused parser+executor suites remain green (`752/752`) and typecheck remains clean.
- 2026-02-17: Added no-hit edge-case regression coverage for recently implemented cleanup templates to guard against partial implementations: `Possibility Storm` now explicitly verifies that when no shared card type is found all revealed/exiled cards are returned to library bottom, and `Wand of Wonder` now explicitly verifies per-opponent mixed outcomes (no-hit opponent returns all revealed cards; hit opponent keeps only the hit instant/sorcery exiled); focused parser+executor suites remain green (`754/754`) and typecheck remains clean.
- 2026-02-17: Closed a parser normalization edge case where passing card name `Test` could corrupt unrelated words containing that substring (for example, `greatest` -> `greathis permanent`) by switching card-name replacement to escaped, boundary-aware matching in `parseOracleText`; added regression coverage in `oracleTextParser.test.ts` and validated with focused suites (`npx vitest run test/oracleTextParser.test.ts test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `852/852`) plus clean package typecheck.
- 2026-02-17: Expanded deterministic `modify_pt` `where` support for context-bound player zones and source-referential stat phrases by adding `that player's` / `their` zone card-count evaluation (`graveyard/hand/library/exile`, typed and untyped) plus generic `this/that/its <object>'s power|toughness|mana value` evaluation with deterministic source/target binding; added executor regressions in `oracleIRExecutor.test.ts` and validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `784/784`) plus clean package typecheck.
- 2026-02-17: Added deterministic `where` support for Consuming-Aberration-style graveyard sizing (`X is the number of cards in your opponents' graveyards`, including equivalent all-opponents phrasing), counting only non-controller graveyards; added focused executor regression in `oracleIRExecutor.test.ts` and validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `785/785`) plus clean package typecheck.
- 2026-02-17: Expanded deterministic `where` phrase coverage for card-count and battlefield-relative templates by adding support for `X is the number of cards in all players' hands`, `X is the number of cards in your opponents' hands`, and `X is the number of other creatures on (the) battlefield` (source-bound), with a control-flow fix to avoid premature fallback nulls from the generic battlefield matcher; added focused executor regressions and validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `788/788`) plus clean package typecheck.
- 2026-02-17: Added deterministic `where` support for high-frequency Domain/tap templates: `X is the number of basic land types among lands you control` and `X is the number of tapped creatures you control` (plus `untapped` variant), and hardened matcher flow by preventing premature null returns in generic `you control`/`your opponents control` where branches so more-specific patterns can resolve; added focused executor regressions and validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `790/790`) plus clean package typecheck.
- 2026-02-17: Added deterministic `where` support for devotion templates (`X is your devotion to white|blue|black|red|green`) by counting color-matching mana symbols in mana costs of permanents you control (including hybrid/phyrexian-style symbols via symbol containment), and added focused executor regressions for blue/black devotion while keeping deterministic single-target fixtures; validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `792/792`) plus clean package typecheck.
- 2026-02-17: Expanded deterministic opponent-scoped `where` coverage in `evaluateModifyPtWhereX` for `X is the greatest mana value among permanents your opponents control` (plus parity logic for `greatest power|toughness among creatures your opponents control`), added focused executor regression in `oracleIRExecutor.test.ts`, and validated with focused executor suite (`npx vitest run test/oracleIRExecutor.test.ts`, `447/447`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-17: Added deterministic single-target resolution for controller-qualified selectors in `resolveSingleCreatureTargetId` (`target creature you control`, `target creature your opponents control`, `target creature an opponent controls`) so `modify_pt` effects no longer skip when battlefield has multiple creatures; added focused executor regression for `X is the greatest toughness among creatures your opponents control` with `target creature you control`, validated with focused executor suite (`npx vitest run test/oracleIRExecutor.test.ts`) and clean `rules-engine` typecheck.
- 2026-02-17: Fixed `modify_pt` parsing drift where controller-qualified targets were dropped by broadening the parser match from literal `target creature` to include `target creature you control` / `your opponents control` / `an opponent controls`, preserving raw target text for deterministic executor filtering; added parser+executor regressions for `target creature you control` with opponent-scoped greatest-toughness `where` evaluation, validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `796/796`) and clean `rules-engine` typecheck.
- 2026-02-17: Added sibling parser+executor coverage for opponent-qualified single-target selectors in `modify_pt` (`target creature your opponents control` and `target creature an opponent controls`) with deterministic fixtures that validate controller-filtered target resolution and `where X is the greatest power among creatures you control`; focused suites remain green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `800/800`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added explicit executor parity regression for `where X is the greatest power among creatures your opponents control`, using deterministic opponent-qualified targeting (`target creature your opponents control`) to lock both power and toughness branches for opponent-scoped `greatest` evaluation; focused suites remain green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `801/801`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added deterministic `where` support for `X is the greatest power|toughness among other creatures you control` with source/target-aware exclusion, then fixed exclusion precedence to prefer `targetCreatureId` over `sourceId` so equipped-creature/targeted modify paths correctly exclude the modified creature itself; added focused regression in `oracleIRExecutor.test.ts` and validated with focused suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `802/802`) plus clean `rules-engine` typecheck.
- 2026-02-17: Added deterministic opponent-side parity for `where X is the greatest power|toughness among other creatures your opponents control` with the same source/target-aware exclusion semantics; added focused executor regression using deterministic equipped-target binding (`the creature`) to avoid multiplayer target ambiguity while validating exclusion of the modified opponent creature, with focused suites green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `803/803`) and `rules-engine` typecheck clean.
- 2026-02-17: Added deterministic `where` support for `X is the greatest mana value among other permanents you control` and opponent-side parity `... your opponents control`, with focused executor regressions using deterministic equipped-target binding (`the creature`) and fixture tuning to preserve stable target-aware exclusion behavior; focused parser+executor suites are green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `805/805`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added deterministic global battlefield parity for mana-value `where` templates: `X is the greatest mana value among permanents on the battlefield` and `... among other permanents on the battlefield`, including source/target-aware exclusion for `other`; added focused executor regressions with deterministic equipped-target binding and validated with focused parser+executor suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `807/807`) plus clean `rules-engine` typecheck.
- 2026-02-17: Added deterministic global battlefield parity for creature stat `where` templates: `X is the greatest power|toughness among creatures on the battlefield` and `... among other creatures on the battlefield`, including source/target-aware exclusion for `other`; added focused executor regressions with deterministic equipped-target binding and validated with focused parser+executor suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `809/809`) plus clean `rules-engine` typecheck.
- 2026-02-17: Added deterministic subtype-scoped `where` support for corpus-backed `non-Human` templates in greatest-stat evaluation: `X is the greatest power|toughness among non-Human creatures you control` and opponent-side parity `... your opponents control`; added focused executor regressions (deterministic equipped-target binding where needed) and validated with focused parser+executor suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `811/811`) plus clean `rules-engine` typecheck.
- 2026-02-17: Hardened subtype-scoped greatest-stat matching to accept `nonhuman`/`non-human`/`non human` spellings and added battlefield parity for non-subtype variants (`X is the greatest power|toughness among (other )?non-Human creatures on the battlefield`), with focused executor regressions for nonhuman spelling + battlefield scope; focused parser+executor suites remain green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `813/813`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added `other` parity for subtype-scoped non-Human greatest-stat controller filters by extending control/opponent forms to `X is the greatest power|toughness among other non-Human creatures you control` and `... your opponents control`, with target/source-aware exclusion semantics; added focused deterministic executor regressions and validated with focused parser+executor suites (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `815/815`) plus clean `rules-engine` typecheck.
- 2026-02-17: Locked additional negation-scope greatest-stat coverage with focused regressions for `nonartifact` and `nonland` creature filters (`X is the greatest power among nonartifact creatures you control`, `X is the greatest toughness among other nonland creatures on the battlefield`), confirming current generic non-qualifier matcher behavior remains deterministic; focused parser+executor suites remain green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `817/817`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added deterministic non-qualifier parity for mana-value `where` templates across control/opponent/battlefield scopes: `X is the greatest mana value among (other )?nonartifact permanents ...` and `... nonland permanents ...` (including `you control`, `your opponents control`, and `on the battlefield`), with focused executor regressions using deterministic equipped-target binding for `other` exclusions; focused parser+executor suites remain green (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts`, `820/820`) and `rules-engine` typecheck remains clean.
- 2026-02-17: Added deterministic negated-count `where` support for `X is the number of (other )?non... creatures|permanents` across controller/opponent/battlefield scopes (`you control`, `your opponents control`, `on the battlefield`) with target/source-aware `other` exclusion semantics; added focused executor regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `474/474`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded negated-count regression parity to cover remaining scope variants and `other` exclusions (`other nonland creatures on the battlefield`, `other nonartifact permanents you control`, `other nonartifact permanents your opponents control`) in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `477/477`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added negated-count spelling-variant parity regressions for `nonhuman`/`non-human`/`non human` style where phrases in creature and permanent count templates (including battlefield and opponent scopes) within `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `480/480`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic `least/lowest` where evaluation support in `evaluateModifyPtWhereX` for `X is the least power|toughness among creatures ...` and `X is the lowest mana value among permanents ...` across controller/opponent/battlefield scopes, with focused regressions in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `486/486`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Extended `least/lowest` parity with `other` and `non...` variants in `evaluateModifyPtWhereX` for both creature-stat and permanent-mana-value templates across controller/opponent/battlefield scopes (including `least ... among other creatures ...`, `least ... among (other )?non... creatures ...`, and `lowest mana value among (other )?non... permanents ...`); added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `493/493`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Completed `least/lowest` phrasing+arithmetic parity by adding `lowest` synonym support for least-stat creature templates and validating arithmetic wrappers over least/lowest where forms (`one plus`, `twice`, and `half the ... rounded up/down`); fixed half-wrapper inner resolution in `evaluateModifyPtWhereX` by retrying with a leading `the` when needed, and added focused regressions in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `498/498`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic `highest` synonym parity for existing `greatest` where templates in `evaluateModifyPtWhereX` across creature stats and mana value scopes (including `other` and `non...` variants), and added focused regressions in `oracleIRExecutor.test.ts` for direct stat/mana usage plus arithmetic wrapper composition (`one plus the highest ...`); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `501/501`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic `smallest` synonym parity for existing `least/lowest` where templates in `evaluateModifyPtWhereX` across creature stats and mana value scopes (including `other` and `non...` variants), and added focused regressions in `oracleIRExecutor.test.ts` for direct stat, arithmetic composition (`twice the smallest ...`), and scoped non-qualifier forms; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `504/504`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic `converted mana cost` synonym parity for existing mana-extrema where templates in `evaluateModifyPtWhereX` (for `greatest/highest` and `least/lowest/smallest` across controller/opponent/battlefield scopes, including `other` and `non...` variants), and added focused regressions in `oracleIRExecutor.test.ts` for direct, arithmetic (`one plus the highest converted mana cost ...`), and non-qualifier scoped forms; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `507/507`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added opponent-scope alias parity for core deterministic where extrema templates in `evaluateModifyPtWhereX` by accepting `you don't control` and `an opponent controls` alongside `your opponents control` for creature-stat and mana-extrema forms (greatest/highest + least/lowest/smallest); added focused regressions in `oracleIRExecutor.test.ts` including arithmetic composition (`one plus the highest converted mana cost among permanents you don't control`) and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `510/510`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Extended opponent-scope alias parity to also accept `you do not control` in the same core deterministic where extrema templates (creature-stat and mana-extrema forms), with focused regressions in `oracleIRExecutor.test.ts` for `highest power among creatures you do not control` and `smallest converted mana cost among permanents you do not control`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `512/512`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Extended opponent-scope alias parity into remaining `other`/`non...` extrema opponent branches in `evaluateModifyPtWhereX` (for greatest/highest and least/lowest/smallest creature-stat + mana-extrema forms), accepting `an opponent controls`, `you don't control`, and `you do not control` alongside `your opponents control`; added focused regressions in `oracleIRExecutor.test.ts` for `highest power among other creatures you do not control` and `smallest converted mana cost among other nonartifact permanents an opponent controls`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `514/514`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Extended the same opponent-scope alias set into negated-count where templates by updating `X is the number of (other )?non... creatures/permanents ...` to accept `an opponent controls`, `you don't control`, and `you do not control` alongside `your opponents control`; added focused regressions in `oracleIRExecutor.test.ts` for `number of other nonhuman creatures an opponent controls` and `number of other nonartifact permanents you do not control`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `516/516`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic support for attacking-count where templates by evaluating `X is the number of attacking creatures` (plus `... on the battlefield`) and controller-scoped variants (`attacking creatures you control`, `attacking creatures an opponent controls` and existing opponent aliases) via battlefield `attacking` state; added focused regressions in `oracleIRExecutor.test.ts` for global and opponent-scoped attacking-count forms and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `518/518`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic `where` class-count coverage for subtype/plural templates by adding safe singular fallback normalization in `normalizeControlledClassKey` (enabling corpus-backed phrases such as `X is the number of shrines you control`, `... mountains you control`, and `... elves on the battlefield` to resolve through existing count branches); added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `521/521`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic Party where support in `evaluateModifyPtWhereX` for `X is the number of creatures in your party` (counts unique roles among creatures you control: Cleric, Rogue, Warrior, Wizard, up to 4), with focused regression coverage in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `522/522`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic specific-counter where support in `evaluateModifyPtWhereX` for `X is the number of <counter> counters on this|that|it ...` (including object-aware source binding for noncreature references like `this enchantment`), enabling corpus-backed templates such as `+1/+1 counters on this creature` and `verse counters on this enchantment`; added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `524/524`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic attacking-count where support to include `other` exclusion semantics for templates like `X is the number of other attacking creatures` (including opponent-scoped aliases such as `... an opponent controls`) by excluding the modified/source creature from attacking-count pools; added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `526/526`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic high-frequency where support for `X is its mana value` and `X is the number of colors among permanents you control` (including color extraction from explicit color arrays, color identity, and mana-cost symbols), with focused regressions in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `528/528`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic qualified-count where support with corpus-backed templates `X is the number of other creatures you control`, `... legendary creatures you control`, `... creatures you control with defender`, and `... differently named lands you control`; added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `532/532`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic stack/context-bound mana-value where support for high-frequency `X is that spell's mana value` by resolving `sourceId` across battlefield/stack/zones via shared object lookup; added focused regression in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `533/533`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded context-bound mana-value where support to adjacent high-frequency card-reference variants `X is that card's mana value` and `X is the mana value of the exiled card` (plus equivalent exiled/revealed/discarded-card phrasing), reusing shared `sourceId` object lookup across battlefield/stack/zones with conservative safe-skip when unresolved; added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `535/535`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic graveyard-aggregation where support for `X is the number of card types among cards in your graveyard` and `X is the number of cards in all graveyards with the same name as that spell`, using type-line aggregation and context-bound source-name matching across all players' graveyards; added focused regressions in `oracleIRExecutor.test.ts` and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `537/537`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic commander-scope mana-extrema where support for `X is the greatest mana value of a commander you own on the battlefield or in the command zone` by evaluating commander-marked objects from battlefield and command-zone payloads (with conservative owner/flag checks), with focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `538/538`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added high-frequency lock coverage for existing deterministic class-count templates (`instant and sorcery cards in your graveyard`, `permanent cards in your graveyard`, `zombies you control`) and fixed a root-cause plural normalization bug (`zombies` singularized incorrectly to `zomby`) in `normalizeControlledClassKey`; focused suite updated and validated (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `541/541`) with clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic high-frequency where support for `X is the amount of life you gained (this turn)` and `X is the sacrificed creature's power|toughness|mana value` by reading controller-scoped life-gain turn stats and context-bound sacrificed-creature attributes from shared object lookup; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `544/544`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic high-frequency where support for turn-history count phrases `X is the number of creatures that died this turn` and `X is the number of permanents you've sacrificed this turn` by reading `creaturesDiedThisTurnByController` (summed total) and controller-scoped `permanentsSacrificedThisTurn`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `546/546`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic high-frequency mana-spend where support for `X is the number of colors of mana spent to cast this/that spell` and `X is the amount of mana spent to cast this/that spell` by resolving source-bound spend metadata (`manaColorsSpent`/`manaSpentColors`, spend records, and total fields) with conservative safe-skip when unavailable; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `548/548`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic high-frequency where support for `X is the total power of creatures you control` and `X is the number of creatures you control with power N or greater` by evaluating controller-scoped battlefield creature stats directly; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `550/550`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for life-loss phrasing `X is the amount of life you lost this turn` (including `you've` variant) by reading controller-scoped life-loss turn stats (`lifeLostThisTurn`/`lifeLost`/`turnStats.lifeLost`) in `evaluateModifyPtWhereX`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `552/552`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for discard-history phrasing `X is the number of cards you've/you have discarded this turn` by reading controller-scoped discard turn stats (`cardsDiscardedThisTurn`/`cardsDiscarded`/`turnStats.cardsDiscarded`) in `evaluateModifyPtWhereX`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `554/554`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic reversed-subtraction where arithmetic support in `evaluateModifyPtWhereX` for `X is N minus <expr>` (including `one`), enabling corpus phrase forms like `X is 3 minus the number of cards in their hand`; added focused regressions in `oracleIRExecutor.test.ts` and stabilized revealed-this-way step collection to consume all parsed ability steps for multi-sentence templates; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `557/557`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is your speed` in `evaluateModifyPtWhereX` using conservative runtime fallbacks (`player.speed`/`player.playerSpeed` plus state maps), with focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `558/558`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic counter-based where support in `evaluateModifyPtWhereX` for `X is the number of <counter> counters on this <permanent-type>` and `X is the number of <counter> counters on it`, including robust counter-map/array reads and `this creature` target fallback; added focused regression for `+1/+1 counters on it` in `oracleIRExecutor.test.ts`, while preserving existing `this creature` and `verse counters on this enchantment` coverage; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `559/559`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the result` in `evaluateModifyPtWhereX`, sourcing die outcomes conservatively from `state.lastDieRollByPlayer[controllerId]`, then `state.lastDieRoll`, then latest `state.dieRollsThisTurn[controllerId]`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `560/560`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression locks for high-frequency where tails already covered by deterministic execution paths: `X is the result minus 1` (composed from `X is the result` + arithmetic suffix) and `X is the number of lands you control` (class-count path), with new `oracleIRExecutor.test.ts` cases; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `562/562`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression locks for additional high-frequency deterministic variants already supported in executor matching: `X is the amount of life you gained` (no `this turn` suffix) and `X is the number of cards in their hand minus 4`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `564/564`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the number of experience counters you have` in `evaluateModifyPtWhereX`, with conservative fallbacks across controller/state counter shapes (`player.experienceCounters`, `player.counters.experience`, `state.experienceCounters`, `state.experience`, `state.playerCounters.experience`); added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `565/565`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression locks for additional deterministic where tails already implemented in executor: `X is the sacrificed creature's toughness` and `X is the number of charge counters on this artifact`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `567/567`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the number of times this creature has mutated` in `evaluateModifyPtWhereX`, with target/source creature fallback and mutation-count shape fallbacks (`mutationCount`, `timesMutated`, `mutateCount`, or derived from `mutatedStack` length); added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `568/568`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the damage dealt to your opponents this turn` in `evaluateModifyPtWhereX`, summing opponent entries from `state.damageTakenThisTurnByPlayer`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `569/569`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression lock for deterministic subtype-class counting phrase `X is the number of bobbleheads you control` in `oracleIRExecutor.test.ts` (covered by existing generic class-count matcher), and revalidated focused suite + typecheck after stabilizing adjacent test edits (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `569/569`; `npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the total amount of mana paid this way` in `evaluateModifyPtWhereX` by reusing existing mana-spent aggregation (`getAmountOfManaSpent`) from the referenced source object; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `570/570`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for symbol-specific mana spend phrasing `X is the amount of {S} spent to cast this/that spell` via a new `getAmountOfSpecificManaSymbolSpent` helper (record + array payment shapes), and added focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `571/571`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is your highest commander tax among your commanders` in `evaluateModifyPtWhereX`, evaluating command-zone commander tax from `taxById` with conservative command-zone fallbacks; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `572/572`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the amount of life your opponents have lost this turn` in `evaluateModifyPtWhereX`, summing opponent values from controller-scoped life-loss maps (`lifeLostThisTurn`, `lifeLost`, `turnStats.lifeLost`) with conservative fallback behavior; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `573/573`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the amount of life your opponents have gained this turn` in `evaluateModifyPtWhereX`, summing opponent values from controller-scoped life-gain maps (`lifeGainedThisTurn`, `lifeGained`, `turnStats.lifeGained`) with conservative fallback behavior; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `574/574`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for controller draw-history phrasing `X is the number of cards you've/you have drawn this turn` and `X is the number of cards you drew this turn` in `evaluateModifyPtWhereX`, using controller-scoped draw counters from `cardsDrawnThisTurn`, `cardsDrawn`, and `turnStats.cardsDrawn`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `576/576`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for controller spell-history phrasing `X is the number of spells you've/you have cast this turn` and `X is the number of spells you cast this turn` in `evaluateModifyPtWhereX`, using controller-scoped spell counters from `spellsCastThisTurn`, `spellsCast`, and `turnStats.spellsCast`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `578/578`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for controller land-play history phrasing `X is the number of lands you've/you have played this turn` and `X is the number of lands you played this turn` in `evaluateModifyPtWhereX`, using controller-scoped counters from `landsPlayedThisTurn`, `landsPlayed`, and `turnStats.landsPlayed`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `580/580`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for opponent spell-history phrasing `X is the number of spells your opponents have cast this turn` and `X is the number of spells your opponents cast this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `spellsCastThisTurn`, `spellsCast`, and `turnStats.spellsCast`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `582/582`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for opponent land-play history phrasing `X is the number of lands your opponents have played this turn` and `X is the number of lands your opponents played this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `landsPlayedThisTurn`, `landsPlayed`, and `turnStats.landsPlayed`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `584/584`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for opponent draw-history phrasing `X is the number of cards your opponents have drawn this turn` and `X is the number of cards your opponents drew this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `cardsDrawnThisTurn`, `cardsDrawn`, and `turnStats.cardsDrawn`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `586/586`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for opponent discard-history phrasing `X is the number of cards your opponents have discarded this turn` and `X is the number of cards your opponents discarded this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `cardsDiscardedThisTurn`, `cardsDiscarded`, and `turnStats.cardsDiscarded`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `588/588`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for opponent sacrifice-history phrasing `X is the number of permanents your opponents have sacrificed this turn` and `X is the number of permanents your opponents sacrificed this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `permanentsSacrificedThisTurn`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `590/590`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic opponent life-loss where support in `evaluateModifyPtWhereX` to also accept no-suffix phrasing `X is the amount of life your opponents have lost` (in addition to `... this turn`), added focused regression in `oracleIRExecutor.test.ts`, and repaired an externally corrupted `qty.kind` literal in `resolveUnknownMillUntilAmountForPlayer`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `591/591`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic opponent life gain/loss phrasing parity in `evaluateModifyPtWhereX` by accepting forms that omit `have` (`X is the amount of life your opponents gained this turn` and `... your opponents lost this turn`) while preserving existing `have` and no-suffix loss forms; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `593/593`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression lock for no-`have`, no-`this turn` opponent life-loss phrasing (`X is the amount of life your opponents lost`) in `oracleIRExecutor.test.ts`, confirming parity with existing widened opponent life-loss matching; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `594/594`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression lock for no-`have`, no-`this turn` opponent life-gain phrasing (`X is the amount of life your opponents gained`) in `oracleIRExecutor.test.ts`, confirming parity with existing widened opponent life-gain matching; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `595/595`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic sacrifice-history phrasing parity in `evaluateModifyPtWhereX` by accepting no-`this turn` forms for both controller and opponents (`X is the number of permanents you've sacrificed` and `X is the number of permanents your opponents sacrificed`) while preserving explicit `... this turn` support; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `597/597`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the number of creatures your opponents control that died this turn` in `evaluateModifyPtWhereX`, summing non-controller values from `creaturesDiedThisTurnByController`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `598/598`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for `X is the number of creatures you control that died this turn` in `evaluateModifyPtWhereX`, reading controller-scoped values from `creaturesDiedThisTurnByController`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `599/599`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded deterministic died-history phrasing parity in `evaluateModifyPtWhereX` by accepting no-`this turn` forms for both controller and opponents (`X is the number of creatures you control that died` and `X is the number of creatures your opponents control that died`) while preserving explicit `... this turn` support; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `601/601`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic where support for control-synonym died-history phrases in `evaluateModifyPtWhereX` (`X is the number of creatures that died under your control` and `X is the number of creatures that died under your opponents' control`, with optional `this turn` suffix), mapped to `creaturesDiedThisTurnByController`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `603/603`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded control-synonym died-history alias coverage in `evaluateModifyPtWhereX` by accepting `X is the number of creatures that died under an opponent's control` (with optional `this turn`) alongside `your opponents' control`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `604/604`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression lock for no-`this turn` phrasing of the new alias `X is the number of creatures that died under an opponent's control` (without trailing `this turn`) in `oracleIRExecutor.test.ts`, confirming parity with optional-suffix matcher behavior; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `605/605`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added focused regression locks for explicit `this turn` forms of control-synonym died-history phrases (`X is the number of creatures that died under your control this turn` and `X is the number of creatures that died under your opponents' control this turn`) in `oracleIRExecutor.test.ts`, confirming parity with optional-suffix matcher behavior; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `607/607`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Expanded control-synonym died-history alias coverage in `evaluateModifyPtWhereX` to also accept `X is the number of creatures that died under opponents' control` (without `your`) alongside `your opponents' control` and `an opponent's control`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `608/608`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-18: Added deterministic runtime `where` support for `X is the number of cards discarded this way` in `evaluateModifyPtWhereX` by threading `lastDiscardedCardCount` through `applyOracleIRStepsToGameState` and tracking actual discarded totals in deterministic `discard` execution; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `609/609`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic runtime `where` support for `X is the number of cards exiled this way` in `evaluateModifyPtWhereX` by threading `lastExiledCardCount` through `applyOracleIRStepsToGameState` and tracking actual exiled totals in deterministic `exile_top` and `impulse_exile_top` execution; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `610/610`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic runtime `where` support for `X is the total power of (the) creature(s) sacrificed this way` in `evaluateModifyPtWhereX` by threading `lastSacrificedCreaturesPowerTotal` through `applyOracleIRStepsToGameState` and computing per-step sacrificed-creature power totals in deterministic `sacrifice` execution; added focused regression in `oracleIRExecutor.test.ts` and hardened shared `permanentMatchesType` to use the same `card.type_line`/`type_line`/`cardType` fallback chain used elsewhere, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `611/611`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic mana-paid-this-way `where` phrasing parity in `evaluateModifyPtWhereX` by accepting alias forms with an explicit payer subject (`X is the amount of mana that player paid this way`, with optional `total`) in addition to existing `X is the total amount of mana paid this way`; added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `612/612`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic runtime `where` support for excess-damage phrasing in `evaluateModifyPtWhereX` (`X is the amount of excess damage dealt this way` and `X is the excess damage dealt this way`) by threading `lastExcessDamageDealtThisWay` through `applyOracleIRStepsToGameState` and computing per-step excess from deterministic permanent damage assignments in `deal_damage`; added focused regressions in `oracleIRExecutor.test.ts`, and hardened shared battlefield selector type matching (`permanentMatchesSelector`) to use the same `card.type_line`/`type_line`/`cardType` fallback chain used elsewhere; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `614/614`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic runtime `where` support for `X is the number of cards looked at while scrying this way` in `evaluateModifyPtWhereX` by threading conservative `lastScryLookedAtCount` runtime state through `applyOracleIRStepsToGameState` and initializing/resetting it in deterministic `scry` execution paths (no-choice subset); added focused regression in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `615/615`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic symbol-paid-this-way `where` support in `evaluateModifyPtWhereX` by adding `X is the amount of {<symbol>} paid this way` and extending specific-symbol spend resolution to include `{E}` (`energy`) aliases in `getAmountOfSpecificManaSymbolSpent`; added focused regression for `X is the amount of {E} paid this way` in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `616/616`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Completed a bulk deterministic `where X` phrasing pass by adding a 50-entry alias-normalization table in `evaluateModifyPtWhereX` that canonicalizes high-frequency corpus variants to existing supported matchers (including spell/card mana-value aliases, excess-damage short forms, revealed/exiled/discarded card power/toughness phrasings, no-`your` opponent life/history phrasings, and opponent turn-history no-`your` forms for cast/play/draw/discard/sacrifice); added representative focused regressions in `oracleIRExecutor.test.ts` across these families and validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `621/621`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added generalized deterministic `where` matchers in `evaluateModifyPtWhereX` for additional high-frequency corpus families: `X is the number of players being attacked`, `X is the number of attacking creatures with flying`, `X is this spell's intensity`, and named-card possessive stats (`X is <Name>'s power|toughness|mana value`, including short-name-to-full-name matching such as `Skanos's` -> `Skanos, ...`); also fixed matcher precedence so generic attacking-creature fallback does not swallow `with flying` forms and prevented pronoun forms (`this|that|its`) from being intercepted by named-card matcher. Added focused regressions in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `625/625`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic `where` coverage for the next uncovered corpus cluster by adding `snow permanents` class normalization (`X is the number of snow permanents you control`) and a deterministic additive matcher for control+graveyard subtype phrasing (`X is the number of caves you control plus cave cards in your graveyard`); added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `627/627`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Continued deterministic `where X` corpus expansion with additional high-frequency phrase families: added tapped-state class counting for `X is the number of tapped|untapped <class> you control` (for example, `tapped artifacts`), added `X is the number of creatures attacking you`, added mana-spent alias parity for `X is the amount of mana spent to cast this|that creature`, extended class-list normalization for `and/or` phrasing (including `and or` normalization drift) to support templates like `X is 2 plus the number of creatures and/or spacecraft you control`, and added alias normalization for `X is the tapped creature's power`; added focused regressions in `oracleIRExecutor.test.ts`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `634/634`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused high-frequency regression lock for `X is the number of land cards in your graveyard` in `oracleIRExecutor.test.ts` (existing deterministic behavior), validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `635/635`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic `where X` coverage for additional corpus phrasing by adding support for `X is the number of <class> target opponent controls`, `X is the number of <class> cards in its controller's graveyard`, and alias normalization for `X is the total amount of life your opponents have lost ...`; added focused regressions in `oracleIRExecutor.test.ts` (including `islands target opponent controls`, `creature cards in its controller's graveyard`, and `total amount of life your opponents have lost this turn`), validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `638/638`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added high-frequency regression locks for existing pronoun/self power phrasing in `oracleIRExecutor.test.ts` (`X is this creature's power` and `X is its power`) to ensure these corpus-heavy templates stay covered; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `640/640`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic alias normalization for card-reference mana-value phrasing in `evaluateModifyPtWhereX` (`X is the exiled card's mana value`, `X is the revealed card's mana value`, `X is the discarded card's mana value` -> canonical `that card's mana value`) and expanded focused regression locks in `oracleIRExecutor.test.ts` for high-frequency uncovered phrases (`X is that creature's mana value`, `X is the number of elves you control`, and devotion variants `X is your devotion to white|green`), plus alias regressions for exiled/revealed/discarded mana value; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `647/647`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic relational/count phrasing coverage in `evaluateModifyPtWhereX` by adding support for `X is the number of <class> (the )?defending player controls` (bound via `targetOpponentId`) and `X is the number of <class> those opponents control`/`all of those opponents`/`all those opponents`/`each of those opponents` (bound via `eachOfThoseOpponents`); added focused regressions in `oracleIRExecutor.test.ts` for `lands defending player controls` and `creatures those opponents control`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `649/649`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added deterministic alias normalization in `evaluateModifyPtWhereX` for activation-context bobblehead phrasing (`X is the number of bobbleheads you control as you activate this ability` -> canonical `X is the number of bobbleheads you control`), with a focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `650/650`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused high-frequency regression lock in `oracleIRExecutor.test.ts` for plain hand-size phrasing (`X is the number of cards in your hand`) to preserve deterministic base-count behavior beyond arithmetic variants; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `651/651`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic hand-size phrasing parity in `evaluateModifyPtWhereX` by adding alias normalization for explicit target-opponent zone wording (`X is the number of cards in target opponent's hand|graveyard|library|exile` -> canonical `their <zone>` forms); added focused regressions in `oracleIRExecutor.test.ts` for plain `their hand` and explicit `target opponent's hand`, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `653/653`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused high-frequency regression locks in `oracleIRExecutor.test.ts` for existing deterministic class/counter templates used by corpus-heavy phrasing (`X is the number of shrines you control` and `X is the number of verse counters on this enchantment`) to preserve subtype counting and source-object counter resolution behavior; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `655/655`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic typed-zone phrasing support in `evaluateModifyPtWhereX` with direct matcher coverage for `X is the number of <class> cards in target opponent's|target player's <graveyard|hand|library|exile>` (context-resolved via selector bindings), and added focused regression in `oracleIRExecutor.test.ts` for `X is the number of creature cards in target opponent's graveyard`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `656/656`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused regression lock in `oracleIRExecutor.test.ts` for arithmetic target-opponent hand-size phrasing (`X is the number of cards in target opponent's hand minus 1`), confirming recursive `plus/minus` `where` evaluation composes with selector-context target-opponent normalization; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `657/657`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic alias normalization in `evaluateModifyPtWhereX` for graveyard name-match phrasing variants by canonicalizing `X is the number of cards in all graveyards with the same name as the spell|this spell` to existing supported `...same name as that spell`; added focused regression in `oracleIRExecutor.test.ts` for the `the spell` alias form, validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `658/658`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused high-frequency regression locks in `oracleIRExecutor.test.ts` for additional generic class-count templates that are covered by existing deterministic matchers (`X is the number of vampires you control` and `X is the number of clerics on the battlefield`), using deterministic attached-source fixture shaping where needed; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `660/660`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused high-frequency regression locks in `oracleIRExecutor.test.ts` for additional generic class-count phrases handled by existing deterministic matchers (`X is the number of forests you control`, `X is the number of swamps you control`, `X is the number of enchantments you control`, and `X is the number of allies you control`), with deterministic attached-source fixture shaping for the allies case; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `664/664`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Added focused regression locks in `oracleIRExecutor.test.ts` for high-frequency pronoun/alias variants already supported by deterministic matcher logic (`X is the mana value of that spell` alias parity with `that spell's mana value`, plus `X is that creature's toughness` and `X is its toughness`); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `667/667`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic `where` coverage in `evaluateModifyPtWhereX` with dedicated artifact-scoped extrema support for `X is the greatest|highest mana value among artifacts you control` (including converted-mana-cost synonym), and added focused regression in `oracleIRExecutor.test.ts` to verify artifact-only maxima selection ignores higher nonartifact mana values; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `668/668`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic `where` coverage in `evaluateModifyPtWhereX` with support for `X is the greatest number of creatures you control that have a creature type in common` by counting the largest shared creature-subtype frequency among creatures you control (using explicit subtype arrays when available, with type-line fallback parsing), and added focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `669/669`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-02-19: Expanded deterministic `where` coverage in `evaluateModifyPtWhereX` with support for `X is the number of permanents you control with oil counters on them` by counting controlled permanents with positive `oil` counters, and added focused regression in `oracleIRExecutor.test.ts`; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `670/670`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-05: Added focused high-frequency regression lock in `oracleIRExecutor.test.ts` for generic battlefield class-count phrasing already handled by deterministic matcher logic (`X is the number of creatures on the battlefield`), using deterministic attached-source fixture shaping (`The creature` + `sourceId`) to avoid target ambiguity; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `671/671`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-05: Added focused high-frequency regression lock in `oracleIRExecutor.test.ts` for generic controlled-class phrasing already handled by deterministic matcher logic (`X is the number of soldiers you control`), using deterministic attached-source fixture shaping (`The creature` + `sourceId`) for stable targeting; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `672/672`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-05: Added focused high-frequency regression lock in `oracleIRExecutor.test.ts` for generic controlled-class phrasing already handled by deterministic matcher logic (`X is the number of dragons you control`), using deterministic attached-source fixture shaping (`The creature` + `sourceId`) for stable targeting; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `673/673`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Added focused high-frequency regression lock in `oracleIRExecutor.test.ts` for generic controlled-class phrasing already handled by deterministic matcher logic (`X is the number of planeswalkers you control`), using deterministic attached-source fixture shaping (`The creature` + `sourceId`) for stable targeting; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `674/674`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Added a grouped Oracle-text variant lock batch in `oracleIRExecutor.test.ts` for high-frequency supported phrasing variants: life-gain alias forms (`X is the amount of life you have gained this turn`, `X is the amount of life you've gained this turn`) and battlefield class-count form (`X is the number of goblins on the battlefield`); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `677/677`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Added a larger grouped Oracle-text variant lock batch in `oracleIRExecutor.test.ts` for remaining supported `where X is ...` alias families, including card-reference stat aliases (`power/toughness of exiled/revealed/discarded card`), `X is the amount of mana spent to cast that creature`, `X is excess damage dealt`, `X is the number of cards in all graveyards with the same name as this spell`, and curly-apostrophe tapped-creature alias (`X is the tapped creature\u2019s power`); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `709/709`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Mined updated `oracle-cards.json` + `AtomicCards.json` (new-set refresh) for fresh `where X is ...` variants and added a grouped deterministic lock batch in `oracleIRExecutor.test.ts` for arithmetic/class forms already supported by executor paths (`X is 1 plus the number of basic land types among lands you control`, `X is 3 plus the number of artifacts you control`, `X is 4 minus the number of cards in their hand`, `X is half the number of artifacts you control, rounded down`, `X is one plus the number of foods you control`); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `714/714`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Hardened `evaluateModifyPtWhereX` subtype matching by updating `typeLineLower` to combine `cardType` with `type_line`/nested `card.type_line` (instead of selecting only the first present field), preventing subtype-loss in class checks when objects include both coarse and detailed type metadata; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `714/714`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Added explicit executor support for the new-set phrase family `X is the number of mounts and vehicles you control` (including arithmetic wrappers such as `X is 2 plus ...`) in `evaluateModifyPtWhereX`, with direct deterministic `modify_pt` executor regressions in `oracleIRExecutor.test.ts` (equipped-creature targeting to avoid selector ambiguity); validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `716/716`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Continued lock-only corpus expansion from updated `oracle-cards.json`/`AtomicCards.json` by adding grouped deterministic arithmetic/class phrase regressions in `oracleIRExecutor.test.ts` (`1 plus basic land types among lands you control`, `3 plus artifacts you control`, `4 minus cards in their hand`, `half the number of artifacts you control, rounded down`, `one plus foods you control`), validated with focused suite (`716/716`) and clean `rules-engine` typecheck.
- 2026-03-17: Extended opponent-scope control phrase support in `evaluateModifyPtWhereX` by broadening `X is the number of opponents who control ...` matching to allow plural/no-article forms (for example, `... control artifacts` and compound `... control artifacts and/or enchantments`), and added direct executor regressions in `oracleIRExecutor.test.ts` for both variants; validated with focused suite (`npm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts`, `720/720`) plus clean `rules-engine` typecheck (`npm run typecheck --workspace=rules-engine`).
- 2026-03-17: Added sacrificed-artifact mana-value phrasing support in evaluateModifyPtWhereX by aliasing X is the mana value of the sacrificed artifact to canonical X is the sacrificed artifact's mana value and implementing direct evaluator support for that canonical form; also added direct executor regressions in oracleIRExecutor.test.ts for X is one plus the mana value of the sacrificed artifact and X is 1 plus the sacrificed creature's mana value (with deterministic source binding), validated with focused suite (
pm run test --workspace=rules-engine -- test/oracleIRExecutor.test.ts, 723/723) plus clean ules-engine typecheck (
pm run typecheck --workspace=rules-engine).
- 2026-03-22: Added corpus-grounded sacrifice migration tooling via `tools/audit-sacrifice-executor-coverage.js`, which generates `tools/sacrifice-executor-coverage.json` and `docs/sacrifice-executor-coverage.md` from local `oracle-cards.json` plus AtomicCards-derived oracle text. Use `supportedEffectCards` as the first-pass compatibility set when refactoring standalone `sacrifice` executor behavior, and use `choiceOrGapSamples` / `unsupportedObjectPhrases` as the backlog for targeted coverage expansion.
- 2026-03-22: Added corpus-backed sacrifice regression locks in `oracleIRExecutor.test.ts` for representative supported templates (`Each opponent sacrifices an enchantment.`, `Target player sacrifices an enchantment.`, optional `sacrifice a land`, and optional `sacrifice a permanent` forced execution), then extracted selector-context normalization into `rules-engine/src/oracleIRExecutorExecutionContext.ts` so `oracleIRExecutor.ts` stays focused on execution flow and `where X` evaluation. Validated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `924/924`).
- 2026-03-22: Split parser normalization/selector helpers out of `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserUtils.ts`, keeping Unicode normalization and contextual-opponent selector handling shared while shrinking the parser prelude. Also removed the dead `parseSacrificeWhat(...)` helper from `rules-engine/src/oracleIRExecutor.ts`. Validated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `1275/1275`).
- 2026-03-22: Extracted the full `evaluateModifyPtWhereX(...)` implementation from `rules-engine/src/oracleIRExecutor.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts`, then trimmed `oracleIRExecutor.ts` back to a small execution-orchestration surface with matching imports. This keeps the high-churn `where X is ...` evaluator isolated for future corpus-driven expansion without inflating the main executor file. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `1275/1275`).
- 2026-03-22: Split parser postprocessing out of `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserPostprocess.ts`, moving global impulse-upgrade recognition plus cross-ability reveal-followup merging behind an explicit parse-postprocess pipeline. This keeps `parseOracleTextToIR(...)` focused on orchestration while isolating cross-ability heuristics for future work. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `1275/1275`).
- 2026-03-22: Extracted choose-mode parsing from `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserChooseMode.ts`, keeping bullet-mode detection and label parsing separate while preserving recursive mode-body parsing through a callback into `parseAbilityToIRAbility(...)`. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `1275/1275`).
- 2026-03-22: Extracted token-create follow-up parsing from `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserTokenFollowup.ts`, isolating the conservative parser for enters-tapped, counters, haste/keyword grants, and delayed sacrifice/exile cleanup clauses while keeping the merge/apply logic in `parseAbilityToIRAbility(...)`. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts --pool=threads`, `1275/1275`).
- 2026-03-22: Extracted token-create clause parsing into new `rules-engine/src/oracleIRParserTokenCreateClauses.ts`, moving the deterministic multi-token clause parser plus the `create token` + `exile the top ...` combined-clause parser behind explicit imports. The main parser now calls the shared module directly while the old in-file implementations remain only as temporary cleanup debt until a later safe-deletion pass. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-22: Extracted trigger-event normalization and Oracle-IR hint shaping from `rules-engine/src/triggeredAbilities.ts` into new `rules-engine/src/triggeredAbilitiesEventData.ts`, moving `TriggerEventData`, `buildTriggerEventDataFromPayloads(...)`, `buildStackTriggerMetaFromEventData(...)`, and `buildOracleIRExecutionEventHintFromTriggerData(...)` behind shared exports while keeping `triggeredAbilities.ts` as the compatibility surface for existing imports. `buildResolutionEventDataFromGameState(...)` remains in `triggeredAbilities.ts` for now because that deletion seam was more brittle than the helper moves. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-22: Completed the deferred cleanup for the previous two seams: removed the legacy in-file `tryParseMultiCreateTokensClause(...)` and `tryParseCreateTokenAndExileTopClause(...)` copies from `rules-engine/src/oracleIRParser.ts`, and finished moving `buildResolutionEventDataFromGameState(...)` into `rules-engine/src/triggeredAbilitiesEventData.ts` so `triggeredAbilities.ts` now re-exports the shared helper instead of owning a duplicate implementation. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-22: Extracted shared impulse-clause cleanup/reminder detection into new `rules-engine/src/oracleIRParserImpulseClauseUtils.ts`, wired the top-level parser helpers to the shared module, and added a narrow subject-order impulse fallback in `parseAbilityToIRAbility(...)` to preserve corpus-backed `its owner` / `that permanent's controller` / `defending player may exile ...` behavior while the larger duplicated permission-parser seam remains deferred. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-23: Closed the next duplicated parser hotspot by extracting shared impulse-permission clause normalization and leading-condition parsing into `rules-engine/src/oracleIRParserImpulseClauseUtils.ts`, then rewiring both `parseImpulsePermissionClause(...)` copies in `rules-engine/src/oracleIRParser.ts` to use the shared helpers while leaving the lower `exiled with this <permanent>` continuous-source matching local. This reduces the highest remaining churn in the parser without broad regex rewrites across the encoding-sensitive file. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-23: Split triggered-ability Oracle-IR choice support out of `rules-engine/src/triggeredAbilities.ts` into new `rules-engine/src/triggeredAbilitiesChoiceSupport.ts`, moving choice-selection extraction, trigger choice-event generation, and choice-derived event-data shaping into a dedicated helper module while keeping `triggeredAbilities.ts` as the public compatibility surface via imports/re-exports. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1368/1368`).
- 2026-03-23: Verified that skipped/unsafe Oracle IR effects already persist structured review records through `recordSkippedStep(...)` into `result.automationGaps` and capped `state.oracleAutomationGaps`, with existing executor/trigger regressions covering both direct and trigger-driven skips. In the same pass, closed a corpus-backed sacrifice gap by adding deterministic self-referential sacrifice support for exact `sacrifice it` / `sacrifice this <self>` clauses in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`, while deliberately keeping `unless` variants as persisted skips. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1370/1370`).
- 2026-03-23: Continued the corpus-backed sacrifice migration by adding conservative deterministic support for single-type `another ...` sacrifice clauses (currently exemplified by `another creature`) in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`. The executor now excludes the source permanent from candidate sacrifice pools when the clause is `another <single-type>`, applies only when the remaining choice set is deterministic, and still records a persisted player-choice gap when multiple non-source candidates remain. Added focused regressions in `rules-engine/test/oracleIRExecutor.test.ts` and revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1372/1372`).
- 2026-03-23: Extended the same conservative sacrifice executor path to mixed-type count clauses such as `an artifact or creature` and `another artifact or creature` in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`. The executor now treats supported `or` unions as a single deterministic candidate pool, applies only when the union resolves to at most the required count, and preserves player-choice skips when the union leaves multiple legal sacrifices. Added focused regressions in `rules-engine/test/oracleIRExecutor.test.ts` for deterministic union resolution, choice-required union skips, and source-excluding `another artifact or creature`, then revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1375/1375`).
- 2026-03-23: Added the next grouped sacrifice-selector family from the mined corpus backlog by supporting article-led subtype/token sacrifice wording in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` (for example `a Food`, `a Vampire`, `a Homunculus`, `a token`) using conservative type-line/token matching. Execution still requires the resulting candidate pool to be deterministic, so multiple matching subtype permanents continue to record player-choice skips. Added focused regressions in `rules-engine/test/oracleIRExecutor.test.ts` and revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1378/1378`). Also added `docs/sacrifice-executor-backlog.md` as the manual ordered progress tracker for remaining grouped sacrifice gaps.
- 2026-03-23: Continued the corpus-backed sacrifice pass by adding context-bound support for `sacrifice that creature` / `sacrifice that permanent` in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts`, and by reclassifying choice-bound sacrifice selectors (`of their choice`, `any number of ...`, and `unless you pay/discard/...` self-sacrifice variants) as explicit `player_choice_required` gaps instead of generic unsupported selectors. Refreshed `tools/audit-sacrifice-executor-coverage.js`, regenerated `tools/sacrifice-executor-coverage.json` + `docs/sacrifice-executor-coverage.md`, and updated `docs/sacrifice-executor-backlog.md` to isolate delayed-cleanup follow-ups as the main remaining sacrifice-selector implementation bucket. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRExecutor.test.ts --pool=threads`, `930/930`).
- 2026-03-23: Started the delayed-cleanup execution slice by threading `chosenObjectIds` through Oracle-IR trigger event hints/stack metadata, extending delayed triggers to preserve an `eventDataSnapshot`, and scheduling token-create cleanup follow-ups as exact-id delayed triggers in `rules-engine/src/oracleIRExecutorTokenStepHandlers.ts`. Also taught contextual battlefield removal to resolve `that token` / `those tokens` style exile/destroy references from bound object ids so delayed cleanup resolves against the original created permanents rather than lookalikes. Added focused regressions in `rules-engine/test/delayedTriggeredAbilities.test.ts` and `rules-engine/test/oracleIRExecutor.test.ts`, validated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/delayedTriggeredAbilities.test.ts test/oracleIRExecutor.test.ts --pool=threads`, `961/961`).
- 2026-03-23: Completed the next delayed-cleanup seam by adding a new Oracle IR step (`schedule_delayed_battlefield_action`) plus parser/executor support for generic battlefield cleanup clauses such as `sacrifice that creature at the beginning of the next end step`, `sacrifice this creature at the beginning of your next end step`, and `exile those tokens at end of combat`. The executor now binds exact permanent ids at schedule time for non-token cleanup too, emits parseable delayed effect text (`You sacrifice that creature.` / `Exile those tokens.`), and resolves those delayed actions against the original bound permanents rather than later lookalikes. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/delayedTriggeredAbilities.test.ts --pool=threads`, `1319/1319`).
- 2026-03-23: Followed that cleanup pass with a broader sacrifice-selector parity update in `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` and `tools/audit-sacrifice-executor-coverage.js`: added support for word-number quantity selectors (`two creatures`, `two lands`, etc.), subtype-token forms (`a Blood token` / `two Food tokens`-style shapes), missing self alias `this Equipment`, subtype-wide `all <Subtype> you control`, and contextual alias cleanup references (`the token`, `the creature`, `those creatures`), while classifying more semantic-but-choice-bound quantity selectors (`X ...`, `one of them`, `that many ...`, `up to ...`) as explicit `player_choice_required`. Added focused executor regressions and resynced the corpus audit/docs, lifting the supported sacrifice-effect baseline from `481` to `496` cards and choice-required baseline from `346` to `357`. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run test/oracleIRExecutor.test.ts --pool=threads`, `947/947`).
- 2026-03-23: Finished the deferred parser cleanup for the extracted simple-action seam in `rules-engine/src/oracleIRParser.ts` by removing the dead in-file copies of draw / remove-counter / add-mana / scry / surveil / discard / mill / goad clause parsing that now route through `rules-engine/src/oracleIRParserSimpleActionClauses.ts`, and trimmed the now-unused parser imports. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Split the active impulse-permission matcher used by the combined exile-top parser out of `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserEffectImpulsePermission.ts`, wiring the live `tryParseImpulseExileTop(...)` / pending-impulse follow-up paths through the shared helper while removing the nested in-file permission-window monolith. This isolates the high-churn `this turn` / `next turn` / `remains exiled` / `control source` permission parsing that combines with exile-top clauses without changing surrounding source/amount detection. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Extracted the main impulse/exile-top loop orchestration from `rules-engine/src/oracleIRParser.ts` into new `rules-engine/src/oracleIRParserImpulseFlow.ts`, moving the live pending-upgrade window, create-token-plus-exile-top clause handling, subject-order helper routing, and exile-top follow-up tracking behind a single helper call in `parseAbilityToIRAbility(...)`. The older subject-order legacy fallback still remains in `oracleIRParser.ts` for now because this part of the file is still encoding-brittle, but the active control-flow path is now isolated and validated. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Extracted conditional-wrapper evaluation support from `rules-engine/src/oracleIRExecutor.ts` into new `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts`, moving the trigger-style conditional truth evaluation, mana-spent threshold reference resolution, and `that much` amount substitution helpers behind explicit imports while keeping `applyOracleIRStepsToGameState(...)` focused on step dispatch and runtime state tracking. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Split the trigger-condition family evaluators out of `rules-engine/src/triggeredAbilities.ts` into new `rules-engine/src/triggeredAbilitiesConditionEvaluators.ts`, moving tap/untap subject checks plus control, opponent-control, life-total, graveyard, and hand-size condition helpers behind shared exports while preserving `evaluateTriggerCondition(...)` in `triggeredAbilities.ts` as the compatibility entry point. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Continued shrinking `rules-engine/src/triggeredAbilities.ts` by extracting the large trigger-event detection decision tree into new `rules-engine/src/triggeredAbilitiesEventDetection.ts`. `parseTriggeredAbilitiesFromText(...)` now delegates event inference through the helper while keeping trigger assembly local, which trims another high-churn block without introducing enum/module-cycle risk. Revalidated with `npm run typecheck --workspace=rules-engine` plus focused Vitest (`npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads`, `1447/1447`).
- 2026-03-23: Continued the compatibility-surface refactor by extracting replacement-effect Oracle-text parsing from `rules-engine/src/replacementEffects.ts` into new `rules-engine/src/replacementEffectsParsing.ts`, leaving `replacementEffects.ts` responsible for exports plus battlefield collection/sorting while preserving the existing `parseReplacementEffectsFromText(...)` entry point. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/replacementEffects.test.ts --pool=threads`, `31/31`).
- 2026-03-23: Continued the same compatibility-surface cleanup for static abilities by extracting Oracle-text static-ability parsing from `rules-engine/src/staticAbilities.ts` into new `rules-engine/src/staticAbilitiesParsing.ts`, keeping `staticAbilities.ts` as the stable export/collection/application surface while preserving the existing `parseStaticAbilities(...)` entry point. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/staticAbilities.test.ts --pool=threads`, `13/13`).
- 2026-03-23: Continued the same compatibility-surface refactor for casting restrictions by extracting the Oracle-text detection table and `detectCastingRestrictions(...)` implementation from `rules-engine/src/castingRestrictions.ts` into new `rules-engine/src/castingRestrictionsDetection.ts`, while preserving `castingRestrictions.ts` as the stable runtime API surface for collection, application, and validation helpers. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/castingRestrictions.test.ts --pool=threads`, `23/23`).
- 2026-03-23: Continued the compatibility-surface cleanup for player-choice helpers by extracting the choice-event factory implementations from `rules-engine/src/choiceEvents.ts` into new `rules-engine/src/choiceEventsFactories.ts`, leaving `choiceEvents.ts` as the stable export/default wrapper around the shared `ChoiceEventType` surface. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/choiceEvents.test.ts --pool=threads`, `24/24`).
- 2026-03-23: Continued the same compatibility-surface cleanup for modal triggered abilities by extracting the local parser, selection-validation, and Black Market Connections helper logic from `rules-engine/src/modalTriggeredAbilities.ts` into new `rules-engine/src/modalTriggeredAbilitiesCore.ts`, while preserving `modalTriggeredAbilities.ts` as the stable wrapper around core helpers plus the already-split cipher, ninjutsu, and mode-factory surfaces. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/modalTriggeredAbilities.test.ts --pool=threads`, `46/46`).
- 2026-03-23: Continued the same compatibility-surface cleanup for token creation by extracting the shared `COMMON_TOKENS` catalog and simple lookup helpers from `rules-engine/src/tokenCreation.ts` into new `rules-engine/src/tokenCreationCatalog.ts`, while preserving `tokenCreation.ts` as the stable runtime surface for token creation, parsing, and trigger detection. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/tokenCreation.test.ts --pool=threads`, `30/30`).
- 2026-03-23: Continued the same token-creation cleanup by extracting Oracle-text token parsing plus token ETB / token-created trigger detection from `rules-engine/src/tokenCreation.ts` into new `rules-engine/src/tokenCreationDetection.ts`, while preserving `tokenCreation.ts` as the stable creation/orchestration surface that re-exports the moved helpers. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/tokenCreation.test.ts --pool=threads`, `30/30`).
- 2026-03-23: Continued the same token-creation cleanup by extracting token permanent ID/type-line construction from `rules-engine/src/tokenCreation.ts` into new `rules-engine/src/tokenCreationPermanentFactory.ts`, leaving `tokenCreation.ts` as a thin orchestration wrapper around creation-by-request plus the already-split catalog and detection helpers. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/tokenCreation.test.ts --pool=threads`, `30/30`).
- 2026-03-23: Finished the next parser shrink-only cleanup in `rules-engine/src/oracleIRParser.ts` by collapsing the two remaining staged-reference comment monoliths for life/combat and simple-token clause parsing down to short helper-ownership notes. The live parsing path was already routing exclusively through `rules-engine/src/oracleIRParserLifeAndCombatClauses.ts` and `rules-engine/src/oracleIRParserTokenSimpleClauses.ts`; this pass simply removes dead commented bulk from the active file without changing parser behavior. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1457/1457`).
- 2026-03-23: Continued shrinking the large `where X is ...` evaluator by extracting two early matcher families from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new helpers: arithmetic/damage-leading forms now live in `rules-engine/src/oracleIRExecutorModifyPtWhereArithmetic.ts`, and generic control-scope count forms (`... you control`, `... your opponents control`, `... that player controls`, `opponents who control ...`, and the small `mounts and vehicles` / `opponents you have` variants) now live in `rules-engine/src/oracleIRExecutorModifyPtWhereControlledCounts.ts`. The public `evaluateModifyPtWhereX(...)` entry point and matcher order remain unchanged; this pass only trims the main evaluator into clearer subfamilies for future safe extractions. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1457/1457`).
- 2026-03-23: Continued the same `where X is ...` shrink pass by extracting the contiguous turn-stats/runtime-results family from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereTurnStats.ts`. This new helper now owns life gained/lost tallies, cards drawn/discarded, spells cast, lands played, `... this way` runtime counters, creature death counts, and sacrificed-permanent turn stats, while the public `evaluateModifyPtWhereX(...)` entry point still preserves the same matcher order by delegating through a single helper call. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1457/1457`).
- 2026-03-23: Continued the same evaluator cleanup by extracting the plain zone/graveyard card-count family from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereZoneCardCounts.ts`. The new helper now owns raw zone-size queries (`cards in your hand/library/graveyard/exile`, opponent/all-player hand and graveyard totals), graveyard class/name counting, card-type diversity in your graveyard, and `same name as that spell` graveyard matching, while `evaluateModifyPtWhereX(...)` preserves the existing matcher order through a single delegation point. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1457/1457`).
- 2026-03-23: Continued the same evaluator cleanup by extracting the class-qualified zone-count and simple battlefield-count family from `rules-engine/src/oracleIRExecutorModifyPtWhereEvaluator.ts` into new `rules-engine/src/oracleIRExecutorModifyPtWhereQualifiedCounts.ts`. The new helper now owns `number of <class> cards in <zone>` queries for you / target player / that player / its controllerâ€™s graveyard, plus `number of <class> on the battlefield` and `number of other creatures on the battlefield`, while `evaluateModifyPtWhereX(...)` still preserves the same matcher order through a single helper call. Revalidated with `npm run typecheck --workspace=rules-engine` and focused Vitest (`npx vitest run test/oracleIRParser.test.ts test/oracleIRExecutor.test.ts test/triggersHandler.test.ts test/triggerParsing.test.ts --pool=threads`, `1457/1457`).

- 2026-03-24: Extended the graveyard reanimation seam with deterministic support for Necromantic Summons-style conditional ETB counters. Spell mastery — If there are two or more instant and/or sorcery cards in your graveyard, that creature enters with two additional +1/+1 counters on it. now stays attached to the base reanimation line, folds into the prior battlefield move_zone step as gated withCounters metadata, and resolves through a new conservative graveyard card-count condition check in the move-zone executor. Added parser/executor regressions and revalidated with 
pm run typecheck --workspace=rules-engine plus focused Vitest (
px vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads, 1527/1527).

- 2026-03-24: Added reusable post-move runtime tracking for single-card move_zone execution so immediate follow-up clauses can reference the moved card deterministically. Reanimate-style You lose life equal to its mana value. now resolves by carrying the just-moved card forward through the Oracle IR main loop and reading its mana value in life-gain/loss amount resolution. Added executor regression coverage and revalidated with 
pm run typecheck --workspace=rules-engine plus focused Vitest (
px vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/triggersHandler.test.ts rules-engine/test/triggerParsing.test.ts --pool=threads, 1528/1528).
- 2026-03-24: Cleared the mixed keyword-plus-trigger parse seam that was blocking full-text `Snapcaster Mage` automation by teaching `rules-engine/src/oracleTextParser.ts` to split true standalone keyword clauses like `Flash.` from a same-line following triggered/replacement sentence before ability classification, while deliberately leaving spell-effect continuations intact. Added focused parser/IR/executor regressions in `rules-engine/test/oracleTextParser.test.ts`, `rules-engine/test/oracleIRParser.test.ts`, and `rules-engine/test/oracleIRExecutor.test.ts` covering full-text `Snapcaster Mage`, removed the card-specific blocker from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`2003 passed`).
- 2026-03-24: Cleared the `Ignite the Future` graveyard-rider seam by adding a reusable exile-permission modifier path instead of a card-specific exception. `rules-engine/src/oracleIR.ts` now includes `modify_exile_permissions`, `rules-engine/src/oracleIRParserPostprocess.ts` lifts conditional text like `If this spell was cast from a graveyard, you may play cards this way without paying their mana costs` into a real conditional nested step, `rules-engine/src/oracleIRExecutorExileStepHandlers.ts` and `rules-engine/src/oracleIRExecutorMainLoop.ts` now mark the just-exiled cards as `withoutPayingManaCost`, and `rules-engine/src/RulesEngineAdapter.ts` now honors that exile permission marker by skipping mana payment when those cards are cast. Added focused parser/executor/adapter regressions for full-text `Ignite the Future`, removed it from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`2006 passed`).
- 2026-03-24: Cleared the shared graveyard cast-cost seam behind `Past in Flames` and the remaining cast-execution part of `Snapcaster Mage` by adding a reusable `modify_graveyard_permissions` IR step plus follow-up merge logic for reminder text like `The flashback cost is equal to its mana cost.` `rules-engine/src/oracleIRParserPostprocess.ts` now lifts and merges that reminder onto the preceding graveyard-permission ability, `rules-engine/src/oracleIRExecutorPlayerStepHandlers.ts` and `rules-engine/src/oracleIRExecutorPlayerUtils.ts` now thread `graveyardCastCost: 'mana_cost'` onto granted graveyard cards, `rules-engine/src/playableFromGraveyard.ts` strips that metadata when the card leaves the zone, and `rules-engine/src/RulesEngineAdapter.ts` now derives the spell's real mana payment from the card itself when a graveyard permission carries `mana_cost` casting metadata. Added focused parser/executor/adapter regressions for `Past in Flames`, upgraded the `Snapcaster Mage` regressions to assert the new metadata, removed `Past in Flames` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleTextParser.test.ts rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts rules-engine/test/RulesEngineAdapter.test.ts rules-engine/test/triggerParsing.test.ts rules-engine/test/triggersHandler.test.ts --pool=threads` (`2009 passed`).
- 2026-03-24: Cleared the next graveyard-exile follow-up seam with shared parser/runtime hooks instead of card-specific exceptions. `rules-engine/src/oracleIRParserPostprocess.ts` now reuses `tryParseLifeAndCombatClause(...)` when lifting simple conditional unknown steps and routes leading `If ..., this creature gets +N/+N until end of turn` text through the same conditional-wrapper path, which unlocks rider text like `If you do, you gain 1 life`, `If a creature card was exiled this way, you gain 2 life`, and `If a noncreature card was exiled this way, this creature gets +1/+1 until end of turn`; `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now evaluates typed `a <type> card was exiled this way` / `a card is put into exile this way` conditions, including `noncreature` negation, against the immediately moved exile payload; `rules-engine/src/oracleIRExecutorCreatureStepUtils.ts` now resolves self-referential creature targets like `this creature` for temporary P/T effects; and `rules-engine/src/oracleIRParserTokenSimpleClauses.ts` plus `rules-engine/src/oracleIRParserAbilityClauses.ts` now conservatively split `create a Food token and exile ...` style mixed clauses when the right-hand half already parses cleanly. Added focused parser/executor regressions for `Restless Cottage`, `Mardu Woe-Reaper`, `Diregraf Scavenger`, `Deathgorge Scavenger`, and `Raven Eagle`, removed those cards plus stale already-cleared entries (`Hazel's Brewmaster`, `Lantern of the Lost`, `Ignis Scientia`, `Rotfeaster Maggot`, `Carrion Imp`, `Creakwood Ghoul`, `Grave Robbers`) from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads` (`1744 passed`).
- 2026-03-24: Cleared the next adjacent graveyard-exile counter seam by adding a reusable positive-counter action instead of leaving same-resolution rider text as `unknown`. `rules-engine/src/oracleIR.ts` now includes `add_counter`, `rules-engine/src/oracleIRParserSimpleActionClauses.ts` now parses `put a/an/<n> <counter> counter on ...`, `rules-engine/src/oracleIRExecutorBattlefieldStepHandlers.ts` plus `rules-engine/src/oracleIRExecutorMainLoop.ts` now execute deterministic single-target counter placement on battlefield permanents, and `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` plus `rules-engine/src/oracleIRParserAbilityClauses.ts` now conservatively split `exile ... from a graveyard and put a counter on this creature` clauses so the exile and counter halves execute in order. Added focused parser/executor regressions for `Armored Scrapgorger`, `Immersturm Predator`, `Keen-Eyed Curator`, and `Conversion Chamber`, updated an older conditional parser expectation to reflect the new `add_counter` step for `put a +1/+1 counter on enchanted creature`, removed those newly cleared blocker entries from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads` (`1750 passed`).
- 2026-03-24: Cleared the next graveyard-exile keyword/type-branch seam with shared parser/runtime support rather than card-specific patches. `rules-engine/src/oracleIR.ts` now includes deterministic `investigate` and `populate` Oracle IR steps, `rules-engine/src/oracleIRParserSimpleActionClauses.ts` parses those keyword actions directly, `rules-engine/src/oracleIRParserZoneAndRemovalActions.ts` now also conservatively splits `exile ... from a graveyard, then investigate/populate`, and `rules-engine/src/oracleIRParserPostprocess.ts` now lifts simple conditional text whose condition contains comma-delimited card descriptors (for example, `If it was a noncreature, nonland card, ...`). On the runtime side, `rules-engine/src/oracleIRExecutorKeywordStepHandlers.ts`, `rules-engine/src/oracleIRExecutorMainLoop.ts`, and `rules-engine/src/oracleIRExecutorConditionalStepSupport.ts` now execute deterministic investigate/populate follow-ups and evaluate `it was a <type> card` branches against the immediately moved card, including `artifact or land` disjunctions and `noncreature, nonland` conjunctions. Added focused parser/executor regressions for `Lazav, Wearer of Faces`, `Mastermind Plum`, `Misfortune Teller`, and `Selesnya Eulogist`, removed those cards from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads` (`1758 passed`).
- 2026-03-24: Started the next `cards exiled with this ...` provenance seam by teaching graveyard-to-exile `move_zone` resolution to stamp source-linked exile metadata directly onto the moved card objects. `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now tags deterministically exiled cards with `exiledWithSourceId` / `exiledWith` (and a matching `exiledBy` fallback) when a source object is known, so later same-turn/same-source effects can count or reference those cards without bespoke test fixtures. Added a focused executor regression proving a same-ability `where X is the number of cards exiled with this creature` follow-up sees the just-exiled card, and revalidated with `npm run typecheck --workspace=rules-engine` plus `npx vitest run rules-engine/test/oracleIRParser.test.ts rules-engine/test/oracleIRExecutor.test.ts --pool=threads` (`1759 passed`).
- 2026-03-25: Cleared the `Wizard's Spellbook` d20 result-table seam with shared Oracle IR/runtime support instead of a card-specific executor branch. `rules-engine/src/oracleIR.ts` now includes `roll_die` and `die_roll_results` steps plus richer `copy_spell` casting metadata, `rules-engine/src/oracleIRParserPostprocess.ts` now lifts inline die-roll result tables like `1-9 | ...`, `10-19 | ...`, and `20 | ...` into a real result-branch wrapper, `rules-engine/src/oracleIRExecutorMainLoop.ts` now records die rolls and resolves matching result branches, and `rules-engine/src/oracleIRExecutorCopySpellSupport.ts` now replays copied linked-exile cards while honoring normal mana cost, fixed `{1}` alternative cost, or free casting. Added focused parser/executor regressions for `Wizard's Spellbook` alongside `Psionic Ritual`, removed `Wizard's Spellbook` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run rules-engine/test/oracleIRParser.test.ts -t "Wizard's Spellbook|Psionic Ritual"`, and `npx vitest run rules-engine/test/oracleIRExecutor.test.ts -t "Wizard's Spellbook|Psionic Ritual"`.
- 2026-03-25: Cleared `The Many Deeds of Belzenlok` by merging the saga chapter-copy follow-up back onto the exile move-zone line so the copied chapter ability resolves deterministically from the exiled Saga. Parser postprocess now recognizes `Copy its chapter I/II/III ability` as a follow-up to the exile step, the executor already had `copy_chapter_ability` replay support, and focused parser/executor regressions now lock the full `move_zone -> copy_chapter_ability` path. Revalidated with focused Vitest and clean `npm run typecheck --workspace=rules-engine`, then removed `The Many Deeds of Belzenlok` from `docs/oracle-automation-batch-blockers.md`.
- 2026-03-25: Cleared `Bronzehide Lion` by adding a shared battlefield-entry Aura rewrite seam instead of a card-specific dies-trigger branch. `rules-engine/src/oracleTextParser.ts` now refuses to classify lines whose first colon lives inside quoted granted text as activated abilities, `rules-engine/src/triggeredAbilitiesParsing.ts` preserves quoted mana symbols in trigger effects, `rules-engine/src/oracleIR.ts` plus `rules-engine/src/oracleIRParserPostprocess.ts` now let battlefield `move_zone` steps carry entry-time type/text overrides and merge `It's an Aura enchantment with enchant creature you control and "{G}{W}: ..."` follow-ups onto the return step, and `rules-engine/src/oracleIRExecutorZoneOps.ts` plus `rules-engine/src/oracleIRExecutorMoveZoneStepHandlers.ts` now apply those overrides early enough for deterministic Aura attachment legality. Added focused regressions in `rules-engine/test/oracleTextParser.test.ts`, `rules-engine/test/triggerParsing.test.ts`, `rules-engine/test/oracleIRParser.test.ts`, and `rules-engine/test/oracleIRExecutor.test.ts`, removed `Bronzehide Lion` from `docs/oracle-automation-batch-blockers.md`, and revalidated with `npm run typecheck --workspace=rules-engine`, `npx vitest run test/oracleTextParser.test.ts -t "Bronzehide Lion|Shade's Form"`, `npx vitest run test/triggerParsing.test.ts -t "Bronzehide Lion|quoted mana symbols"`, `npx vitest run test/oracleIRParser.test.ts -t "Bronzehide Lion|Valkyrie's Call|Grave Betrayal"`, and `npx vitest run test/oracleIRExecutor.test.ts -t "Bronzehide Lion|Valkyrie's Call|Grave Betrayal"`.
