{
  "name": "@mtgedh/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "echo 'Shared module - no build needed (TypeScript source only)'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
